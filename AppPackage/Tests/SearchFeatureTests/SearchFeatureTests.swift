import Testing

// Placeholder. This target is declared ahead of the search-analytics coverage that will fill it,
// so the package manifest does not have to change again while sibling work is in flight. SwiftPM
// warns on a target whose directory holds no Swift source, hence this empty suite.
@Suite
struct SearchFeatureTests {}
