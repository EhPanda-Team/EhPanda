---
phase: quick-260921-f5u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Package.swift
  - EhPanda.xcodeproj/project.pbxproj
  - .planning/config.json
  - .github/workflows/test.yml
  - .github/workflows/deploy.yml
  - .github/workflows/deploy-pre-release.yml
  - README.md
  - READMEs/README.chs.md
  - READMEs/README.cht.md
  - READMEs/README.de.md
  - READMEs/README.jpn.md
  - READMEs/README.ko.md
  - AppPackage/Sources/HomeFeature/HomeView.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularView.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedView.swift
  - AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift
  - AppPackage/Sources/HomeFeature/History/HistoryView.swift
  - AppPackage/Sources/SearchFeature/SearchRootView.swift
  - AppPackage/Sources/SearchFeature/SearchView.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/DetailFeature/Components/PostCommentView.swift
  - AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift
  - AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchView.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift
  - AppPackage/Sources/DetailFeature/Components/TagDetailView.swift
  - AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift
  - AppPackage/Sources/FiltersFeature/FiltersView.swift
  - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift
  - AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/ReadingFeature/Support/AdvancedList.swift
  - AppPackage/Sources/ReadingSettingFeature/ReadingSettingView.swift
  - AppPackage/Sources/SettingFeature/SettingView.swift
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/SettingFeature/Login/LoginView.swift
  - AppPackage/Sources/SettingFeature/Components/WebView.swift
  - AppPackage/Sources/SettingFeature/Components/ChallengeWebView.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift
  - AppPackage/Sources/SettingFeature/Components/DownloadSettingView.swift
  - AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift
  - AppPackage/Sources/SettingFeature/Components/AboutView.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift
  - AppPackage/Sources/AppComponents/NewDawnView.swift
  - AppPackage/Sources/AppComponents/ErrorInfoView.swift
  - AppPackage/Sources/AppComponents/ActivityView.swift
  - AppPackage/Sources/HomeFeature/GalleryRankingCell.swift
  - AppPackage/Sources/SystemNotification/ToastMessageView.swift
  - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections1.swift
  - AppPackage/Sources/AppComponents/PreviewImageView.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
  - AppPackage/Sources/AppComponents/ToolbarItems.swift
  - AppPackage/Sources/AppComponents/AdaptiveStack.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/AppComponents/TagCloudView.swift
  - AppPackage/Sources/DetailFeature/GalleryDestination.swift
  - AppPackage/Sources/AppComponents/SubSection.swift
  - AppPackage/Sources/AppComponents/AppToggle.swift
  - AppPackage/Sources/AppComponents/ViewModifiers.swift
  - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift
  - AppPackage/Sources/AppComponents/AccessibilityNavigationTitleWorkaround.swift
  - AppPackage/Sources/AppComponents/AccessibilitySearchableWorkaround.swift
  - AppPackage/Sources/DetailFeature/DetailView+Navigation.swift
  - AppPackage/Sources/ReadingFeature/Support/ReadingToolbar.swift
  - AppPackage/Sources/GalleryListComponents/GalleryList.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Sources/AppFeature/RootView.swift
  - EhPandaUITests/IOS27MigrationUITests.swift
  - EhPandaUITests/AccessibilityAuditUITests.swift
  - EhPandaUITests/ReaderPageSyncUITests.swift
  - EhPandaUITests/ShareSheetUITests.swift
  - EhPandaUITests/Support/DeepLinkLauncher.swift
  - EhPandaUITests/Support/UITestConstants.swift
  - AGENTS.md
  - .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/SCROLL-EDGE-INVENTORY.md
  - .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/260921-f5u-VERIFICATION.md
autonomous: true
requirements: [QUICK-27-01, QUICK-27-02, QUICK-27-03, QUICK-27-04, QUICK-27-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "App, extension, tests, and package require iOS/iPadOS 27 and build with Xcode 27."
    - "Every existing secondary toolbar action remains reachable through native overflow with its original enabled state."
    - "Native navigation and searchable views replace the temporary accessibility workarounds, preserving designed inlineLarge titles."
    - "Every inventory page carries the native soft top scroll-edge policy, including modal roots and native web scroll hosts."
    - "Baseline tests precede refactoring; final FeatureTests and relevant UI tests pass on iPhone and iPad 27 without masking failures."
  artifacts:
    - path: AppPackage/Package.swift
      provides: "Swift 6.4 manifest with iOS 27 minimum"
    - path: AGENTS.md
      provides: "Native iOS27 navigation policy and every-page soft top-edge requirement"
    - path: .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/260921-f5u-VERIFICATION.md
      provides: "Baseline, final test and visual coverage evidence"
  key_links:
    - from: "SwiftUI page content roots"
      to: "actual List/Form/ScrollView/TextEditor descendants"
      via: "scrollEdgeEffectStyle(.soft, for: .top)"
    - from: "seven custom overflow call sites"
      to: "existing reducer actions and presentation bindings"
      via: "ToolbarOverflowMenu at ToolbarContent level"
---

<objective>
Migrate EhPanda to native iOS/iPadOS 27 behavior and APIs without user-visible regression, with the soft top scroll edge on every page.

QUICK-27-01: OS/toolchain/support alignment.
QUICK-27-02: native modern APIs and removal of temporary title/search workarounds.
QUICK-27-03: every-page soft top edge.
QUICK-27-04: baseline tests, evidence-driven refactoring, final phone/tablet verification.
QUICK-27-05: record durable policy in AGENTS.md.

This is one authorized QUICK plan with three sequential work packages. The broad file list reflects a mechanical whole-app OS/API migration; do not turn it into unrelated architectural redesign. No ROADMAP or phase-status changes.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.swiftlint.yml
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/config.json
@.planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/SCROLL-EDGE-INVENTORY.md
@.planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/COVERAGE.md
@.planning/phases/16-dynamic-type-accessibility/16-AX-POLICY-REVIEW.md
@$HOME/.codex/skills/swiftui-pro/SKILL.md
@$HOME/.codex/skills/swift-testing-pro/SKILL.md

Apply the skills' relevant API/navigation/accessibility and testing references. The user's iOS27 target overrides the skill's older default target. Installed SDK declarations are authoritative for API availability.

Follow this approved plan exactly. Pause immediately and ask the orchestrator if ambiguity, unexpected repository state, missing information, any deviation, a new page/file outside this ownership list, or an unrelated failure requires another choice. Report the concrete evidence and proposed amendment; do not implement your own deviation. Existing user changes belong to their owners. The orchestrator owns GSD bookkeeping/commits.

Confirmed planning evidence: selected /Applications/Xcode-27.0.0.app, Xcode27.0 27A266a, Swift6.4. iPhone Air UUID 35988D34-B4E9-44C6-82B5-CDA2CF391339 existed, but rediscover live IDs before use. iOS27 runtime exists. Use ordinary sandbox escalation for CoreSimulator/build access when needed; failure of sandbox access is not evidence that an SDK API is absent.
</context>

<tasks>
<task type="tracer">
<name>Task 1: Baseline, supported platform migration, and native Home scroll-edge slice</name>
<files>AppPackage/Package.swift, EhPanda.xcodeproj/project.pbxproj, .planning/config.json, .github/workflows/test.yml, .github/workflows/deploy.yml, .github/workflows/deploy-pre-release.yml, README.md, READMEs/README.chs.md, READMEs/README.cht.md, READMEs/README.de.md, READMEs/README.jpn.md, READMEs/README.ko.md, AppPackage/Sources/HomeFeature/HomeView.swift, .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/260921-f5u-VERIFICATION.md</files>
<action>
Before source edits, run xcodebuild -version, xcrun swift --version, xcrun simctl list devices available -j, xcrun simctl list devicetypes -j, and xcrun simctl list runtimes -j. Choose the existing iPhone Air on iOS27. Reuse an available iPad on iOS27 or create one using the first available 11-inch iPad Pro device type and the exact installed iOS27 runtime identifier; record the resulting UUID. Boot each destination before its run. Set task-local IPHONE_27_ID and IPAD_27_ID variables, never persistent system variables.

Run existing FeatureTests on the iPhone and iPad before any test or implementation refactor, and existing AccessibilityAuditUITests/testHomeRootAudit on iPhone as the tracer baseline. Save logs and xcresult bundles under /tmp/ehpanda-ios27 (unique stage-specific names); record exact commands, destination versions and result counts in the verification document. Baseline build/test failure: capture the first real diagnostic and checkpoint with a concrete amendment; do not skip suites or manufacture a green baseline.

Update all eight IPHONEOS_DEPLOYMENT_TARGET values (including project-level 16.0 defaults) to 27.0. Change Package.swift tools-version from 6.3.1 to 6.4 and platforms to .iOS(.v27); preserve dependency versions and resolved pins. Update only current support requirement paragraphs in the six READMEs to 27.0; historical release metadata and previous planning evidence remain historical. Preserve line endings.

For each of the three workflows, use the provider-documented runs-on: xcode-27 label, remove the obsolete DEVELOPER_DIR pin so the runner selects its Xcode27, and rename platform-install step to iOS27. Add a fail-fast Xcode major version and iOS27 SDK check before builds. Keep test destinations pinned to OS27.0 (iPhone Air) and explicitly select FeatureTests; do not change release publishing behavior or run deployment workflows. The dedicated xcode-27 runner is confirmed by official runner-images documentation. Preserve the unsigned IPA packaging contract; it is independent of simulator validation.

Remove existing macro/plugin validation bypass options only together with a verified legitimate trust path. First try the clean xcodebuild invocation with installed normal Xcode trust. If Xcode requires package/macro approval, checkpoint before changing workflows: provide package identity/revision, exact diagnostic and clean provisioning requirement. Do not write fabricated trust records, disable checks through defaults/environment settings, or change dependency pins. Do not claim remote CI tested locally. If remote runner trust provisioning cannot be established, record the unresolved CI blocker and seek an orchestrator amendment; do not leave a knowingly failing CI definition claimed complete.

Replace workflow.test_command in .planning/config.json with the portable direct command: xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air,OS=27.0'. Preserve other config values and requested planner/executor overrides. No machine cache script or stale simulator UUID.

Apply .scrollEdgeEffectStyle(.soft, for: .top) to HomeView's actual main ScrollView, inside its NavigationStack. Build the full app and run the existing Home-root UI test as one real path through package target, app shell, navigation and scrolling. Inspect top-edge scrolling on iPhone to establish the pattern for Task2. This production slice is retained.
</action>
<verify>
<automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan UITests -destination "platform=iOS Simulator,id=$IPHONE_27_ID" -only-testing:EhPandaUITests/AccessibilityAuditUITests/testHomeRootAudit</automated>
<human-check>Inspect Home content scrolling behind its top bar on iPhone27; attach initial and scrolled screenshots, retaining the designed inlineLarge title.</human-check>
</verify>
<done>Recorded pre-edit FeatureTests outcomes on both device classes; all app-owned build minima and current support docs are 27; Xcode27 Home path builds and runs; CI uses verified runner selection with no newly bypassed validation or unreported trust blocker.</done>
</task>

<task type="auto">
<name>Task 2: Native API replacement and complete page scroll-edge coverage</name>
<files>AppPackage/Sources/HomeFeature/HomeView.swift, AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift, AppPackage/Sources/HomeFeature/Popular/PopularView.swift, AppPackage/Sources/HomeFeature/Watched/WatchedView.swift, AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift, AppPackage/Sources/HomeFeature/History/HistoryView.swift, AppPackage/Sources/SearchFeature/SearchRootView.swift, AppPackage/Sources/SearchFeature/SearchView.swift, AppPackage/Sources/FavoritesFeature/FavoritesView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift, AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift, AppPackage/Sources/DetailFeature/Comments/CommentsView.swift, AppPackage/Sources/DetailFeature/Components/PostCommentView.swift, AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift, AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchView.swift, AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift, AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift, AppPackage/Sources/DetailFeature/Components/TagDetailView.swift, AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift, AppPackage/Sources/FiltersFeature/FiltersView.swift, AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift, AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift, AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/ReadingFeature/Support/AdvancedList.swift, AppPackage/Sources/ReadingSettingFeature/ReadingSettingView.swift, AppPackage/Sources/SettingFeature/SettingView.swift, AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift, AppPackage/Sources/SettingFeature/Login/LoginView.swift, AppPackage/Sources/SettingFeature/Components/WebView.swift, AppPackage/Sources/SettingFeature/Components/ChallengeWebView.swift, AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift, AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift, AppPackage/Sources/SettingFeature/Components/DownloadSettingView.swift, AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift, AppPackage/Sources/SettingFeature/Components/AboutView.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift, AppPackage/Sources/AppComponents/NewDawnView.swift, AppPackage/Sources/AppComponents/ErrorInfoView.swift, AppPackage/Sources/AppComponents/ActivityView.swift, AppPackage/Sources/HomeFeature/GalleryRankingCell.swift, AppPackage/Sources/SystemNotification/ToastMessageView.swift, AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections1.swift, AppPackage/Sources/AppComponents/PreviewImageView.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift, AppPackage/Sources/AppComponents/ToolbarItems.swift, AppPackage/Sources/AppComponents/AdaptiveStack.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift, AppPackage/Sources/AppComponents/TagCloudView.swift, AppPackage/Sources/DetailFeature/GalleryDestination.swift, AppPackage/Sources/AppComponents/SubSection.swift, AppPackage/Sources/AppComponents/AppToggle.swift, AppPackage/Sources/AppComponents/ViewModifiers.swift, AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift, AppPackage/Sources/AppComponents/AccessibilityNavigationTitleWorkaround.swift, AppPackage/Sources/AppComponents/AccessibilitySearchableWorkaround.swift, AppPackage/Sources/DetailFeature/DetailView+Navigation.swift, AppPackage/Sources/ReadingFeature/Support/ReadingToolbar.swift, AppPackage/Sources/GalleryListComponents/GalleryList.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift, AppPackage/Sources/AppFeature/RootView.swift, AGENTS.md, .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/SCROLL-EDGE-INVENTORY.md</files>
<action>
Use the inventory as the complete page checklist. Apply .scrollEdgeEffectStyle(.soft, for: .top) directly to every app-owned page content root inside its navigation container. Attach inside every presented page rather than trusting propagation across sheets/covers. An ancestor is acceptable where the inventory records the exact path to the actual host. Check both GalleryList display modes; adding the modifier at GalleryList covers its descendant hosts but each empty/non-scrolling page branch must carry the page policy. Cover Archives' two scroll layouts, QuickSearch word editing, AppIconView, activity-log run picker, TextEditor, reader horizontal and vertical hosts and both Settings presentations. Inline navigation-link pickers must inherit the Form policy or receive an explicit destination modifier after proof. Do not add blur overlays, custom gradients, introspection or a wrapper that just renames the native modifier.

For the two existing WKWebView controllers, set webView.scrollView.topEdgeEffect.style to .soft at the owned scroll view's initialization and apply the SwiftUI modifier at each presentation's content root. Preserve cookie storage, navigation delegate behavior, challenge completion, identity and observer cleanup. Existing controllers add authentication behavior beyond wrapping; replacing that protocol with a different web architecture is outside this migration. Keep UIActivityViewController system-owned and apply the presentation-root policy without modifying private subviews. Reader hosts are native SwiftUI ScrollViews; no UIKit reader bridge was found.

Replace all seven ToolbarFeaturesMenu consumers with ToolbarOverflowMenu directly in ToolbarContent: DetailView+Navigation, WatchedView, FavoritesView, DetailSearchView, SearchRootView, SearchView and ReadingToolbar. The definition and comment in ToolbarItems are not consumers. Remove redundant outer ToolbarItem/ToolbarItemGroup where it exists only to hold the menu. Keep adjacent primary items as toolbar siblings. ToolbarOverflowMenu is ToolbarContent, not View: transfer original View-level disabled modifiers to a Group or the individual actions inside the menu. Preserve every original action, binding, label, ordering, enabled state, and anchored alert/dialog. Remove the ToolbarFeaturesMenu type from ToolbarItems.swift, retaining useful existing action buttons; update its comment to describe native overflow. Native overflow owns its icon/label; do not recreate custom ellipsis rendering or retain symbolRenderingMode arguments for it.

Use visibilityPriority(.high) on reader close and page indicator toolbar items; keep secondary live-text, dual-page and auto-play actions at automatic priority, and retry/reload/settings explicitly in ToolbarOverflowMenu. All controls must remain reachable when space compresses; do not force unrelated item placements or move dialog anchors. Preserve the reading page indicator identifier.

Remove AccessibilityNavigationTitleWorkaround.swift and AccessibilitySearchableWorkaround.swift. Replace each title call with ordinary native automatic title behavior (remove the modifier, or explicit .toolbarTitleDisplayMode(.automatic) only where needed to retain the designed mode). Replace each search call with ordinary .searchable(text:...) and the same optional prompt, using generated localized resources accepted by the SDK. Do not introduce Dynamic Type branches for title/search placement. Preserve all existing .inlineLarge root-title designs and sheet-specific inline modes. Native failures at AX sizes require a checkpoint and native design amendment, not restoration of either workaround.

Replace existing explicit SwiftUI @ViewBuilder annotations in the enumerated files with @ContentBuilder, preserving semantics and isolation. Do not change non-SwiftUI builders or TCA reducer composition. Replace ReadingView's legacy status-bar visibility modifier with .toolbarVisibility(store.showsPanel ? .visible : .hidden, for: .statusBar), retaining its state. Use the installed SwiftUI/SwiftUICore interfaces to confirm overloads; avoid availability fallbacks because minimum OS is27. Only address additional deprecations if they are in these files and have an unambiguous semantic replacement; otherwise checkpoint with the diagnostic.

Update AGENTS.md Accessibility navigation and search policy to require native iOS27 navigation/search, preservation and AX verification of inlineLarge titles, and no reintroduction of old workarounds. Add a scroll-edge policy requiring the exact native soft/top modifier at every page's effective scroll hierarchy, including separately presented pages and the equivalent native property for owned UIKit scroll hosts; state that non-scrolling states have no visible edge effect. Keep other project rules intact, including action-source alert/dialog anchoring and download ownership. Fix any absolute home path encountered in generated docs to a home-relative reference.

Append actual modifier location, inherited host and implementation status to every inventory row. Audit runtime destinations once more; newly discovered destinations outside the plan are a checkpoint, not a silent omission.
</action>
<verify>
<automated>xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination "platform=iOS Simulator,id=$IPHONE_27_ID"</automated>
<human-check>Review the complete page inventory against actual navigation/presentation sites and native hosts. Confirm no page is counted only because an unrelated root has a modifier.</human-check>
</verify>
<done>Native overflow and priority preserve all actions; obsolete title/search modifiers and custom menu type are removed; builders/status-bar usage use current native APIs; every inventory row has a concrete soft-top coverage path; AGENTS policies match the migration.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Phone/tablet regression tests, scoped refactoring, and visual evidence</name>
<files>EhPandaUITests/IOS27MigrationUITests.swift, EhPandaUITests/AccessibilityAuditUITests.swift, EhPandaUITests/ReaderPageSyncUITests.swift, EhPandaUITests/ShareSheetUITests.swift, EhPandaUITests/Support/DeepLinkLauncher.swift, EhPandaUITests/Support/UITestConstants.swift, .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/260921-f5u-VERIFICATION.md, .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/SCROLL-EDGE-INVENTORY.md</files>
<behavior>
- Native overflow exposes the previous secondary actions, with their original disabled conditions, at standard and accessibility sizes.
- Home, Search, Favorites, Downloads and Settings keep their designed titles; affected pushed title/search screens remain usable after cold entry and live text-size changes.
- Reader panel toggling preserves status-bar behavior; horizontal, vertical and dual-page reading preserve current-page synchronization and close/settings access.
- Page navigation, scrolling, sheets, confirmation anchors and share presentation continue working on iPhone and iPad27.
</behavior>
<action>
Before refactoring a test, run its existing form and preserve the observed failure. Add IOS27MigrationUITests.swift using the existing hermetic launcher/fixtures and XCTest for UI; Swift Testing remains the unit-test framework. Exercise real menu actions and destination outcomes, not a source-symbol assertion or button-count-only check. Cover Search/Detail/reader native overflow, reading close/page indicator, and native search title presence; preserve existing localization and use stable identifiers where already available. The new tests must expose regressions before any corresponding implementation fix. Do not fabricate a failure for a behavior that already passes.

Run the entire FeatureTests plan on each device class and the complete UITests plan on each. XCTest runner retries already configured may remain, but report first-attempt failures and ultimate outcomes. Preserve original result bundles. Diagnose failures from xcresult/logs; only migration-caused test query assumptions, timing synchronization with observable state, and native toolbar interactions in the exact listed test files may be refactored in this task. Implementation fixes inside Task2's owned files require orchestrator approval of the specific failing behavior and correction before editing, in accordance with the strict executor contract. Any reducer/unit-test/dependency failure outside this allowlist requires a concrete plan amendment; do not relax assertions, serialize suites, suppress diagnostics, exclude tests, or add fixed sleeps.

Perform visual native UI checks on both iPhone27 and iPad27: standard size, AX1, AX3 and AX5; portrait and landscape; cold page entry and live size changes; search unfocused/focused/query-cleared; representative expanded/collapsed titles. At minimum cover all five roots, each former title/search-workaround page (Frontpage, Popular, Watched, Toplists, History, Search results, Activity logs, Archives), Settings sheet, gallery detail, quick-search/editor, reader controls/settings, web login and challenge when reachable with safe existing fixtures. Verify every inventory page's effective scroll hierarchy; attach top-edge initial/scrolled screenshots for each page that can scroll. Record a reason when a state cannot produce a visible effect (non-scrolling or system-owned), not an assumed visual pass. Authentication-only states without safe fixture access remain explicitly unverified and checkpoint for an authorized completion path rather than entering real credentials.

Use available simulator UI tooling and native simctl content-size controls; load the applicable simulator skill before interaction. Read its current supported command help; no invented UI automation commands. Keep private browsing data and credentials out of screenshots/logs by using hermetic fixtures. Validate reader page indicator and all secondary action reachability in constrained tablet windows as well as full screen. Confirm popover arrows remain at their stable action controls.

Run normal plugin-backed lint through the build, inspect changed files for project lint rules and compiler warnings, and use swiftui-pro plus swift-testing-pro review guidance. After approved fixes rerun affected tests, then final full FeatureTests/UI gates on both devices once. Record all commands, toolchain/device IDs, totals, first-attempt failures, warnings, screenshot paths and remaining coverage in the verification document. Do not call incomplete visual or CI trust coverage a pass. git diff --check must pass.
</action>
<verify>
<automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination "platform=iOS Simulator,id=$IPHONE_27_ID"; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination "platform=iOS Simulator,id=$IPAD_27_ID"; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan UITests -destination "platform=iOS Simulator,id=$IPHONE_27_ID"; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan UITests -destination "platform=iOS Simulator,id=$IPAD_27_ID"; git diff --check</automated>
<human-check>Completed inventory and screenshot evidence prove native title/search behavior, toolbar accessibility, anchored dialogs and soft top scroll edges on both device classes.</human-check>
</verify>
<done>Full feature/UI runs pass on both iOS27 device classes with retained results; migration-caused regressions are root-fixed and reviewed; no required visual coverage or trust blocker is hidden; every user requirement has evidence.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries and STRIDE register

ASVS level1, high-severity blockers. Boundaries: third-party build plugins → build host; web login/challenge → cookie store; UI action → destructive/download operation; test evidence → public repository.

| ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-Q27-01 | Tampering | Build plugins/macros | high | mitigate | Keep pinned packages and native validation; stop on missing legitimate trust provisioning, never fabricate trust or bypass validation. |
| T-Q27-02 | Information disclosure | Web authentication and evidence | high | mitigate | Preserve cookie/navigation protocol; hermetic fixture captures; no credentials or contributor home paths in repository artifacts. |
| T-Q27-03 | Tampering | Overflow actions and download dialogs | medium | mitigate | Preserve enabled conditions, reducer routing and stable source anchors; test actual outcomes and deletion confirmation. |
| T-Q27-04 | Denial of service | Native title/search and compressed toolbar | medium | mitigate | Device-class/AX/orientation verification preserves access to controls; no hidden failures or fallback hacks. |
| T-Q27-05 | Spoofing | CI toolchain | medium | mitigate | Provider-documented xcode-27 runner with fail-fast installed-version/SDK check; no arbitrary toolchain downloads. |
</threat_model>

<verification>
Commands above are grounded in existing project, scheme, FeatureTests and UITests plans. They exceed60seconds; execute asynchronously and report progress, never replace them with a syntax-only check. Use explicit distinct -resultBundlePath values when saving evidence. The old configured cache-wrapper command is intentionally retired because its Xcode26.6 path and UUID are obsolete.

Audit current support values, all inventory rows, native API wiring and remaining compiler diagnostics. The git whitespace check intentionally applies to the uncommitted working tree before the orchestrator commits. Do not rewrite historical docs/release manifests solely because they mention26. No npm/pip/cargo installs, external service setup, database schema changes, or package-version changes are planned.
</verification>

<success_criteria>
QUICK-27-01 through QUICK-27-05 all have recorded passing evidence or the executor is paused at a concrete unresolved checkpoint. Passing source/build checks alone cannot satisfy runtime and visual criteria.
</success_criteria>

<output>
Create .planning/quick/260921-f5u-migrate-to-ios-27-and-ipados-27-with-mod/260921-f5u-SUMMARY.md after completion. Orchestrator owns STATE tracking and commits; do not update ROADMAP. Report unresolved blockers immediately.
</output>

## Grounding sources

- [Apple SwiftUI updates](https://developer.apple.com/documentation/updates/swiftui): confirms ContentBuilder and toolbar modernization.
- [ToolbarOverflowMenu](https://developer.apple.com/documentation/swiftui/toolbaroverflowmenu): native ToolbarContent, directly placed in .toolbar.
- [Toolbar visibility priority](https://developer.apple.com/documentation/swiftui/toolbarcontent/visibilitypriority(_:)): .high preserves important items under compression.
- [Scroll-edge style](https://developer.apple.com/documentation/swiftui/view/scrolledgeeffectstyle(_:for:)): propagates to scroll views in the hierarchy.
- [Official runner images](https://github.com/actions/runner-images): documents xcode-27 runner label.
- Installed iOS27 SDK SwiftUI/SwiftUICore swiftinterfaces and UIKit UIScrollView.h confirm availability and native web-scroll property.

## Dependencies and risks

Tasks1 → 2 → 3 are sequential because configuration, API adoption, page files and verification overlap. No external setup is assumed. Native AX rendering on27 is unproven until tests and visual checks run; any failure is surfaced for a native design amendment. CI package trust has not been proven on a fresh hosted runner and must not be claimed green merely because local trust works. Full-suite runtime and broad visual coverage may require continuation; this changes neither scope nor acceptance criteria.

## Orchestrator amendment 1 — recorded red baseline

The pre-edit iPhone FeatureTests run exposed two failures: Favorites `LoginReturnObservationTests.cookieLoginTriggersReloadWithoutAViewCallback` receives a second login/fetch action pair, and Settings `SettingReducerNavigationTests.generalFilePickedImportsAndStoresTagTranslator` observes its shared-state mutation at a different assertion boundary. Preserve the original logs and result bundles. These are recorded failures, not a passing baseline.

Task 1 may continue with the pre-edit iPad FeatureTests and Home UI baseline, then the planned platform migration. Reoccurrence of these same two failures is expected and does not require another checkpoint. Complete Tasks 1 and 2 before their final repair in Task 3. A new distinct failure still requires a checkpoint. Do not rerun an unchanged failing baseline merely to seek a green result.

Task 3 additionally owns `AppPackage/Tests/FavoritesFeatureTests/LoginReturnObservationTests.swift`, `AppPackage/Tests/HomeFeatureTests/LoginReturnObservationTests.swift`, and `AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift` for diagnosis and regression assertions. Preserve exhaustive action/state checks, parallel execution and production semantics. Before implementing a repair, report the demonstrated root cause and exact proposed code change to the orchestrator. No production reducer or cookie-client edit is authorized by this amendment alone. Apply the Point-Free TCA, testing, dependencies and custom-dump skills alongside the existing Swift Testing skill.

Use existing prepared simulator IDs: iPhone Air `35988D34-B4E9-44C6-82B5-CDA2CF391339`, iPad Pro 11-inch (M5) `E70AEE51-3CC5-4420-9E1C-DE8190DB6FC2`; confirm availability without recreating them. The first clean build reached tests without a macro/plugin trust diagnostic. Remote hosted-runner trust remains unverified and must be reported honestly; it does not block independent source migration.

## Orchestrator amendment 2 — serial validation ownership

Two baseline Xcode processes remained alive after suite output and caused Task 1's build database lock. The orchestrator terminated only those verified task-owned processes. The clean serial Task 1 build passed. The orchestrator now owns simulator builds, tests and visual verification; the Luna executor owns source/test edits and reports when an edit batch is ready. This prevents build-directory contention and source mutation during active tests. No test, lint or trust validation is waived. Do not create another simulator or start another Xcode build from an executor continuation.

Task 1 workflow review found the Xcode-version regular expression double escaped inside a shell single-quoted string. Correct it to match the literal dot in `Xcode 27.0` (one backslash before the dot), and verify the actual guard against installed Xcode output. Use readable `iOS 27` and `Xcode 27` step labels.

The Xcode 27 UI-test compile also exposed actor-isolation warnings in `EhPandaUITests/Support/AccessibilityAuditReport.swift`: `SurfaceInventory.visibleBounds(in:)` reads main-actor XCTest snapshot properties. Add this file to Task 3 ownership and mark that snapshot-reading method `@MainActor`, consistently with its existing initializer caller. Preserve the pure report/geometry value model and all audit assertions. This is an isolation correction, not a warning suppression.

## Orchestrator amendment 3 — deterministic asynchronous test inputs

The login tests describe a refresh while continuously signed in, but `importAutomationCookies` first removes every credential. A concurrent observer can correctly see logged-out then logged-in state. Refactor both authorized Home/Favorites login tests to use a controllable `cookiesDidChange` AsyncStream, following the existing AccountSettingReducerTests pattern. Import the initial cookies, explicitly yield the initial notification, exhaustively receive exactly one login/fetch pair, update the refresh fixture and yield the refresh notification. Finish the notification stream and await the observation effect's completion before `store.finish()` so an extra login action cannot be concealed by early cancellation. Assert the jar remains signed in at each emitted notification. This verifies reducer event routing independently from the cookie client's separately tested mutation-notification granularity. Do not change production cookie or reducer behavior.

The Settings import mock returns its asynchronous result immediately, allowing shared-state mutation before the test has finished asserting the originating send. Replace that mock with a controlled AsyncStream of `Result<TagTranslator, AppError>`. Send the file-picked action, explicitly yield the imported result, then exhaustively assert the response and both shared-state changes. Finish the stream and the store. A prematurely closed stream must record a test issue and return an explicit failure; never silently substitute a success. Preserve parallel test execution and no arbitrary sleeps, retries, unchecked concurrency, or assertion suppression.

These test changes are approved within Task 3. Validate the three focused cases before running the full feature suites. If the controlled response still exposes the same assertion failure, checkpoint with that evidence rather than moving expected mutations to a different action speculatively.

## Orchestrator amendment 4 — coverage corrections from source review

The serial Task 2 build passes. Source review found uncovered page roots: `TagDetailView` has no modifier; Favorites and Watched omit the logged-out branch; ReadingView lacks its documented page-root modifier. Apply the exact soft/top modifier to these roots inside their navigation containers. Add the presentation-root modifier to AccountSettingView and EhSettingView web sheets, alongside the already configured native WKWebView scroll property. TagDetailView is already an inventory requirement and is explicitly added to source ownership. Keep the existing shared GalleryList coverage for list-based destinations.

Replace the three remaining explicit `@ToolbarContentBuilder` annotations in DownloadsView, FavoritesView and AppActivityLogsView with `@ContentBuilder`, supported by the selected SDK and already used by this migration. Correct excess indentation in the seven native overflow bodies without changing actions.

Replace the inventory's blanket implementation statements with the actual file, enclosing content root and inheritance path for each row. Restore the Source audit table's four-column shape and describe validation as pending where it is pending. Audit runtime destinations against these rows; record any newly discovered gap before changing files outside this ownership. Do not label source inspection as visual proof.

## Orchestrator amendment 5 — controlled-stream compile correction

The first focused validation failed to compile the Settings test: `results.first` names the predicate-taking AsyncSequence method rather than awaiting an element. In that mock create a local iterator with `results.makeAsyncIterator()` and await `iterator.next()`. Preserve the existing guard, issue recording and explicit failure. The remaining key-path diagnostics are downstream inference failures; recompile before changing those assertions.

## Orchestrator amendment 6 — native multi-destination validation

Xcode supports multiple `-destination` arguments in one test invocation. The orchestrator may run each complete test plan against both prepared devices in that form, preserving all tests, assertions, runner settings and result evidence while avoiding independently launched processes contending over one build database. This changes command orchestration only. The focused login suites pass; the narrow Settings method filter selected zero tests, so its complete 12-test navigation suite was then run and passed, including the repaired import test.

## Orchestrator amendment 7 — remaining native navigation API modernization

SwiftUI Pro review found the tab root still uses legacy `.tabItem(...).tag(...)`, plus four `.navigationBarTrailing` placements and five `.navigationBarTitleDisplayMode` calls in already changed files. The installed iOS 27 SwiftUI interface provides `Tab(value:content:label:)`, `.topBarTrailing` and `.toolbarTitleDisplayMode`; the placement interface explicitly directs callers to the newer name.

Add `AppFeature/View/TabBar/TabBarView.swift` to Task 2 ownership. Inside the existing ForEach use `Tab(value: type) { ...existing switch... } label: { Label(type.title, systemSymbol: type.symbol) }`, preserving selection binding, order, labels, images, destinations and presentation modifiers. Remove the now-unused single-use `label()` forwarding method. Keep default native tab roles and layout; do not invent a search role or sidebar redesign.

Replace `.navigationBarTrailing` with `.topBarTrailing` in AppActivityLogsView (two), EhSettingView and LoginView. Replace `.navigationBarTitleDisplayMode` with `.toolbarTitleDisplayMode` in DownloadsView+Subviews, DateSeekPickerView, FolderManagerView, AppActivityLogsView and PostCommentView, preserving each mode. Draft outside the repository while UI tests are running, apply after Xcode exits, then rebuild and run the final suites against the final source.

## Orchestrator amendment 8 — platform-correct native search regression

The new UI test successfully opens Quick Search and its editor, returns to Search, enters text and clears it on iPad, but fails at its assumption that focused Search exposes the iPhone's Close button. The native iPad search drawer has no such button (also observed independently on Frontpage). Correct the test to exercise a complete search instead: after verifying clear, enter `fixture` and submit, require the results navigation title `fixture`, use the native Search back button, and require the root Search title. This preserves and strengthens behavioral assertions while following the actual cross-device user flow. No production search fallback is authorized.

The phone landscape cold-entry test separately exposes an unresponsive Home loading layout before the detail route is presented. A process sample records the main thread repeatedly laying out SwiftUI stacks. Preserve that failure and diagnose it; do not avoid landscape, change the launch route, or relax its assertions to make the test pass.

## Orchestrator amendment 9 — distinguish exhaustive source coverage from visual sampling

The user's every-page requirement is verified by exhaustive navigation/presentation discovery and an explicit native modifier inheritance path for all 46 inventory rows. Runtime checks verify native behavior and representative scroll hosts: shared GalleryList, ScrollView, Form/List, TextEditor, reader, and presented sheets on both device classes, plus existing full fixture UI suites. Standard and accessibility-size samples remain separate evidence. An authenticated live service, system-owned internal scrolling, and every possible orientation/window/state combination cannot be certified by the fixture suite; record their limits rather than requesting credentials or claiming exhaustive visual proof.

This replaces Task 3's overbroad requirement for initial/scrolled screenshots of every page in every matrix combination. It does not omit any app-owned page from source coverage, skip an automated test, relax a failure, or authorize a fallback. Required runtime failures discovered by the tests remain blockers to completion. Keep all collected screenshots and report exactly which states were observed.

## Orchestrator amendment 10 — native focus and measured rotation diagnosis

The revised iPad Search test proves that native Clear removes keyboard focus. After the existing assertions that text was cleared, tap the same search field with the existing hittability-aware helper, then enter and submit the next query. This is the normal user interaction; no assertion, timeout, production search behavior or platform-native control is changed.

Update the current deployment-target comment in `EhPandaUITests/AccessibilityAuditUITests.swift` from iOS 26 to iOS 27. Preserve historical version references describing earlier measured behavior.

The revised reader test passes all behavioral assertions on both devices, but iPad portrait restoration subsequently waits 60 seconds for a main-run-loop idle notification. Its app log records `OnScrollGeometryChange` updating multiple times per frame, while animation-idle responds immediately. This differs from proof of a continuously busy app and needs measurement. During the identical reader case in the full UI plan, the orchestrator will capture simultaneous bounded process samples of the affected app and runner during the portrait idle window. Preserve the existing orientation sequence and all assertions. If the warning recurs with active mutations, a further amendment will authorize bounded nonobservable instrumentation of the Home carousel and GalleryList geometry actions. Do not add instrumentation or refactor layout speculatively, and do not treat one faster phone run as proof of a fix.

## Orchestrator amendment 11 — stop a demonstrated failing run for diagnosis

The complete UI invocation found a reproducible cold Gallery Detail stall on iPad before presenting the destination: the app consumes approximately 100% CPU, and a three-second sample places 1,770 of 1,794 main-thread samples under SwiftUI graph/layout work. The visible last frame is the blank Home content beneath its tabs and reload control. The phone's Search root audit separately reports a hit-region failure. Preserve both process samples, the screen and the live runner/application logs.

The orchestrator may gracefully interrupt this failing invocation to diagnose these concrete issues instead of continuing unchanged repetitions. Record the invocation as incomplete/failed, never as a full passing suite. No test is removed, skipped or relaxed: the complete plan must run again after corrections. The executor and planner continue read-only diagnosis or prepare drafts outside the repository until Xcode exits. Production changes require the next evidence-based amendment.

## Orchestrator amendment 12 — real touch target and bounded graph attribution

The Search root accessibility report identifies the history row's delete button as a 14 by 12.67 point target. In `SearchRootView+Keywords.swift`, give that button's label a minimum 44 by 44 point frame and rectangular content shape, preserving the small icon, action and localized label. This enlarges the actual interactive region. Keep history visible for verification; do not clear it, exclude the control from the audit or change audit criteria.

The cold Detail process sample proves repeated graph/layout work but contains no app source frame that identifies the responsible graph. Before selecting a layout refactor, temporarily instrument route milestones, Home and Detail body evaluations, Home viewport/carousel geometry actions, and the two GalleryCard layouts. Use a nonobservable, thread-safe bounded diagnostic recorder in AppTools, gated by the existing fixture-network environment. Record only numeric layout data and fixed event names, with first-few and exponentially spaced output; do not record links or user content. Preserve all original actions, mutation guards, scheduling and layout results. The recorder and every call must be removed before final validation and commits.

The Luna executor may draft these changes outside the repository while the interrupted Xcode run finalizes. The orchestrator authorizes application only after that process exits. Reproduce the unchanged cold Gallery Detail audit and collect its diagnostics and simultaneous samples. A production layout refactor requires demonstrated attribution and a subsequent explicit amendment; do not infer the responsible view merely from the last rendered screen.

## Orchestrator amendment 13 — asynchronous native background submission

A full recompilation exposed the iOS 27 deprecation of `BGTaskScheduler.submit` and Swift 6.4's ambiguous outer strong/inner weak manager capture in `DownloadClient+Networking.swift`. Replace both at their root.

Keep `ContinuedProcessingSession.start` synchronous so user actions and background event/heartbeat setup never wait for Apple's arbitrarily delayed submission completion. Make only `ContinuedTaskScheduling.submit` asynchronous. Its live implementation awaits an `@concurrent` adapter that receives identifier/title/subtitle values, constructs the native request off MainActor, preserves the queue strategy, and awaits `submitTaskRequest`. Registration, adoption and progress remain MainActor-isolated.

The session owns an asynchronous submission record with its session ID, request identifier and task handle. Include that record in the start reentry guard. Establish session/awaiting identity before launching submission. Catch every submission error and handle settlement on MainActor; only adoption yields granted. Finishing or expiration ends the matching session and stream immediately, but retains the submission record until native completion. Do not cancel the submission task to discard its outcome: Swift cancellation does not prove native rejection. If that session ended before settlement, cancel its request before releasing the submission gate. No successor may submit the reused identifier during that interval. If the original session remains current, successful submission leaves it awaiting launch and failure follows the existing unavailable path. Check both submission and session identity before cleanup. Preserve one-time registration, stray launches, explicit finish ownership, and the existing rule that only a qualifying user action starts new coverage. Do not couple lifetime to the transient caller task or change stream termination policy.

Extend the existing scheduler spy with explicit submission-entry acknowledgement and controlled success/error release. Update existing assertions for the asynchronous handoff without sleeps. Add deterministic coverage for immediate stream return while submission is held, progress and launch before completion, expiration/finish during suspension, late acceptance cleanup, reentry exclusion through settlement, identifier reuse afterward, delayed failure isolation, and a coordinator action/transfer progressing while submission remains held. Run lifecycle, identity, interleave, expiration and heartbeat suites.

In `DownloadClient+Networking.swift`, snapshot the Sendable page-downloader value before creating the attempt Task and use that value inside it. Preserve the weak manager progress callback, attempt attachment, defer cleanup, cancellation forwarding and error classification. Validate existing page-transfer progress/cancellation tests.

The Luna executor owns these two background source files, the networking capture correction and directly affected test files. Draft outside the repository while diagnostic UI tests run; apply only after the orchestrator confirms the source-edit gate. The orchestrator owns temporary layout diagnostics and all builds/tests. No compiler or lint warning is suppressed.

## Orchestrator amendment 14 — render Detail content only when its model exists

Two measured cold stalls reach the nil-detail loading graph, while Home carousel/card callbacks are absent. This is evidence for a controlled native design refactor, not proof of a particular framework cycle. In `DetailView.swift`, retain the stable ScrollView, modifiers, overlays, navigation/presentation bindings, accessibility identifier and soft top edge. Inside it, render the complete existing VStack only under `if let galleryDetail = store.galleryDetail`. Supply that actual model to Header, Description and Action, and its page count to Previews. The existing offline notice needs only its error condition within that branch. Remove the ScrollView's opacity-only visibility animation for missing detail; retain all loading/error overlay logic and the existing gallery-detail arrival animation.

Preserve unrelated action closures, toolbar eligibility, reducer behavior, automation, focus, rating/title state, and dialog anchors. Loaded content remains mounted during refresh and offline fallback. No artificial delay, alternate route, fixed dimensions or test relaxation is permitted. The orchestrator owns this single-file refactor while the Luna executor drafts amendment 13. Prepare outside the repository until diagnostic UI execution exits. Keep attribution temporarily for an unchanged cold-flow comparison, then remove it before the final suites. Repeated reproduction and the broader UI plan, not one successful attempt, determine whether the stall was resolved.

Repeated unchanged diagnostic stalls may be stopped after their raw logs and samples are preserved. An aborted diagnostic experiment is never a passing test result; its task-owned post-abort processes may be terminated to release the build/simulator resources. Final verification runs retain normal test and diagnostic collection settings and must finalize successfully.

The loading-model refactor still stalls after real Detail data arrives on both devices. Add temporary native Layout probes around Home and Detail scroll content, forwarding the original proposals, sizes, placement and explicit alignment, to attribute the stalled measurement. These probes are diagnostic only and must be removed with the recorder. Do not infer that quiet Home carousel counters clear the always-present Home sections.

Amendment 13 test synchronization refinement: expose the actual submission record internally with `private(set)` and immutable internal fields, so `@testable` lifecycle tests can capture its owned Task and await `task.value`. This is a settlement barrier, not a production callback or forwarding wrapper. Coordinator integration must invoke a qualifying public action and acknowledge actual transfer progress while native submission remains held.

## Orchestrator amendment 15 — flatten Detail header fitting

Root attribution records repeated Detail content measurement at the same 580-point proposal and 580 by 860.6765 result, while Home measurements stop. The sample contains the Detail layout probe and nested fitting engines around a layout-priority engine, consistent with the header's nested fitting structure. Individual measurements return; this is repeated layout evaluation. This supports a controlled refactor, not a claim that the root cause is already proved.

Replace the nested header fitting with one horizontal ViewThatFits and six explicit candidates in outer-first order: category beside horizontal actions, wrapped 2+1 actions, vertical actions; then category above those three arrangements. Keep the six-point action spacing, eight-point category spacing, intrinsic action widths, priority, trailing alignment, scaled metrics, styling, callbacks and accessibility. Existing substantive button properties are reused. Preserve the rest of Detail, its soft top edge and all route/test behavior. Keep attribution for the unchanged cold-flow comparison, then remove all diagnostics before final verification. Do not claim success from a single pass; require repeated cold Detail/reader flows plus broader UI and Dynamic Type checks.

## Orchestrator amendment 16 — persistent header controls in native Layout

The flattened candidates still stall. Header-specific attribution now records tens of thousands of action measurements/alignment evaluations; its named probe appears above the fitting engine in the CPU sample. Replace that action fitting container with a dedicated native `HeaderActionsLayout` containing exactly one category and one instance of Download, Favorite and Read. Calculate the same six preferred arrangements from intrinsic sizes, with the existing six/eight-point gaps; preserve action order, centered beside placement and trailing action alignment. Report the intrinsic minimum when a proposal is too narrow; never clip or shrink controls. Resolve the widest intrinsic action before measuring the category so measurement and placement use the same effective width. Nil/infinite proposals produce finite ideal geometry. SwiftUI supplies RTL mirroring; do not mirror twice. No observable state, asynchronous layout, geometry feedback or OS-specific fallback is permitted.

The root owns production layout and call-site changes. Luna owns deterministic placement regression tests drafted outside the repository while UI execution runs. Test responsive boundaries, scaled/localized content, unequal controls, finite geometry and intrinsic-size preservation. Real category wrapping, live focus/menu behavior and RTL still require runtime validation. Retain temporary attribution only for the controlled comparison and remove all probes before final verification. This supersedes the six alternative view trees from amendment 15.

The single-instance layout comparison recorded three phone cold-detail passes (about 9–10 seconds each) and three landscape-reader passes (about 35 seconds each), without sustained layout activity. The iPad comparison was obstructed by a visible SpringBoard Open-in-app prompt and remains red. All temporary recorder/probe source has now been removed. The loading-model experiment from amendment 14 did not resolve the issue and is reverted, preserving the original loading behavior. The dedicated header layout is the retained production correction, subject to clean final suites and visual checks. Restarting the task-created iPad simulator clears its observed stuck system/input state without deleting application data or altering test behavior.

Amendment 13 compile/review refinement: snapshotting the downloader alone still emits Swift 6.4's implicit-strong/inner-weak capture warning. Give the outer attempt Task an explicit `[pageDownloader, weak self]` capture list, preserving the weak progress callback and all cancellation/error behavior. Held-submission test cleanup must release continuation gates and await owned tasks on both success and throwing assertion paths before fixture teardown; cancellation alone does not resume checked continuations. Prepare incremental edits outside the repository while the feature invocation runs, then apply at the source-edit gate and revalidate.

## Orchestrator amendment 17 — integration fixture routing and terminal acknowledgement

The held-submission integration first tried to hash its intentionally missing page, then used an incomplete custom image-dispatch router. Correct the fixture by hashing only its 155 existing pages before appending the pending page, and reuse the suite's established request router. Parse the existing gallery fixture to supply its cover URL through the router's explicit image allowlist. Do not widen that router or change production behavior.

An operation can fail before entering the controlled downloader, so its transfer probe must acknowledge operation completion as well as transfer start. Record completion after the real scheduled operation returns, release pending start waiters, and require a successful start result in the test. This turns the observed terminal-before-transfer case into a throwing assertion that reaches existing cleanup, rather than an indefinite wait. Preserve byte/completion gates, actual in-flight progress assertions and joining all owned tasks. No sleep-based ordering, arbitrary delay, global timeout architecture or weakened assertion is authorized.

## Orchestrator amendment 18 — attribute the Home rotation feedback

The clean UI run isolates a second loop during iPad portrait restoration. Its CPU sample directly enters Home carousel's geometry action and page-index binding, with repeated card/body rebuilding. It does not justify rewriting card fitting. Prepare a bounded, fixture-gated, nonobservable trace only in `HomeView+Sections.swift`: live midpoint/container/insets, captured carousel width/pitch, old/new logical index, window base, current target and phase; record width observation and target writes. Preserve the transform result and every production guard/action. Draft outside the repository while tests run, apply only after that invocation exits, and reproduce the unchanged landscape-reader-to-portrait case. Remove the trace before final verification.

If values prove a native target-anchor mismatch, test explicit center anchoring on both native position and view-aligned behavior while preserving width, margins, nearest-center gradient handoff, per-gesture limit and VO middle-block rebasing. If values instead prove mixed layout generations, correct that measurement relationship first. Do not combine unproven fixes or add debounce, event-discarding guards, or alternate layouts.

Make successful portrait restoration an explicit part of the migration reader test, including normal XCTest idling and Detail Read hittability, measured with ContinuousClock against the existing 15-second screen-readiness budget. Keep failure-path portrait cleanup. A sixty-second native idle wait must fail this regression criterion. Source changes remain gated on attribution.


## Orchestrator amendment 19 — select the actual visible carousel target

The trace proves that the native lazy stack preserves a translated estimated content origin after rotation. Replace absolute-offset division in `HomeView+Sections.swift` with actual card-relative viewport measurement. Attach `onGeometryChange(for: Bool.self)` to each existing card after its width frame. Obtain the nearest horizontal viewport with `GeometryProxy.bounds(of: .scrollView(axis: .horizontal))`. Compare its local midpoint with half the measured card width; use half of measured width plus the existing spacing as the nearest-card interval. Make the interval half-open and orient midpoint ties with `layoutDirection`, so only one adjacent card owns a boundary. Missing viewport bounds mean no candidate.

Act only when a card becomes the nearest candidate and pass its real buffered ID through existing positive modulo. Preserve the changed-page guard, gradient handoff, idle-only middle-block synchronization, phase rebasing, native snap/binding configuration, width/margins, accessibility exposure and view identity. Remove the absolute-offset observer and unused pitch property. Do not add stored frames, timing guards, lifecycle callbacks, a geometry reader or alternative card trees. Update the comments to explain lazy-stack origin independence and the existing VoiceOver reason for idle synchronization.

Luna owns an outside-repository draft of this single source change while Xcode is active. Retain bounded diagnostic logging of candidate IDs, measured width, relative midpoint and phase for the controlled comparison; remove the recorder, imports and calls before final verification. Follow this amendment exactly and pause for any deviation. Root applies at the source-edit gate, verifies unchanged selected gallery through rotation, nearest-center handoff, initial selection, rapid swipes and both wrap directions, then checks existing VoiceOver behavior. The portrait responsiveness assertion must pass on both devices with normal XCTest idling. No explicit center-anchor experiment is included.


Amendment 19 regression extension: identify the existing horizontal host with `home_carousel`. Add a UI test that launches the stubbed Home in portrait, captures the label of the actual nearest-center visible button inside that host, then rotates to landscape and back. Select the button using intersecting actual frames and minimum horizontal center distance, never a first-match peek. Require the same centered gallery after both rotations, with normal XCTest idling and a fifteen-second transition budget; restore portrait on failure. No production selection hook or sleep-based synchronization is authorized. Luna drafts the test and independent identifier patch outside the repository while Xcode finalizes.


## Orchestrator amendment 20 — native carousel sizing and center preservation

The relative-target experiment removes the feedback loop but its iPad trace exposes a second defect: the viewport shrinks before the observed width state changes. Card 43 is still 968 points wide when its viewport is already 834 points; the subsequent 667.2-point card layout reports transient candidates 45 and 41, which overwrite the retained target. A fast run that changes the selected gallery is not a successful correction.

Replace the card width frame with native `containerRelativeFrame(.horizontal) { width, _ in width * 0.8 }`. Remove `carouselWidth`, its geometry observer, derived card-width and centering-margin properties, and the explicit percentage content margins. Express the existing centered presentation directly with `scrollPosition(id: $scrollPositionID, anchor: .center)` and `.viewAligned(limitBehavior: .always, anchor: .center)`. Retain the maximum-card-height readiness condition, relative per-card candidate measurement, initial buffered selection, idle VoiceOver synchronization, window rebasing, card spacing, accessibility exposure, and all existing actions. This is one coherent sizing/alignment correction, not the previously rejected anchor-only experiment. It removes the delayed layout-state dependency and tells the native scroll view which point of the identified view to preserve during resizing.

Luna owns the outside-repository draft and comment updates. Include the independently reviewed `home_carousel` identifier and same-gallery rotation regression. The executor must follow this amendment exactly and pause for any ambiguity or deviation. Root applies only after the active Xcode process exits, runs both orientation directions on both devices and the reader restoration case, then verifies swipe/wrap and VoiceOver behavior. Retain the bounded trace only for this comparison and remove it before final verification. No timing guard, event discard, fixed viewport size, geometry reader, or test relaxation is permitted.

Amendment 20 isolation refinement: the geometry transform is Sendable. Capture the immutable layout-direction value explicitly instead of reading its main-actor environment property inside the transform. The temporary trace similarly captures the ScrollPhase value; remove that capture with the diagnostic trace. Do not suppress actor-isolation warnings.


## Orchestrator amendment 21 — distinguish native target identity from candidate commands

Native container sizing still changes the selected gallery on both devices. The trace also reports a twenty-percent viewport displacement before any logged application target write, so its cause cannot be inferred from selection writes alone. Add bounded, nonmutating diagnostics: native target-ID changes with page/phase; candidate viewport bounds, frame in horizontal scroll coordinates, size, actual ID and current target; numeric carousel and visible-button accessibility frames in the existing Home regression. Capture actor-isolated values before entering Sendable geometry transforms. Preserve all geometry results, source guards, assertions and native idling. No inferred-inset subtraction, added margins or resize suppression is authorized. These diagnostics must be removed before final validation.

The rejected native-sizing run completed its test actions (all three Home attempts failed on each device; both reader timing cases passed). Its raw logs are preserved, and it may be interrupted during diagnostic collection as failed/incomplete evidence. The next architectural decision must use the independent frame and target evidence. Native target identity should own gesture selection; actual accessibility-focus identity may own VoiceOver synchronization if demonstrated to preserve the existing focus behavior. Do not remove the existing handoff until that semantic replacement is explicitly approved and tested.


## Orchestrator amendment 22 — separate geometry observation from navigation intent

Independent accessibility frames and screenshots confirm that amendment 20 changes the initial centered presentation before any application target write. Revert that failed sizing/anchor experiment completely to amendment 19's measured width, eighty-percent frame, symmetric margins and default native anchors. Retain actual-card relative geometry rather than absolute lazy-stack offsets.

The target trace proves native ID 43 remains unchanged until an application's geometry candidate commands ID 44. Geometry cannot distinguish a resize from VoiceOver navigation and must not issue scroll commands. Its candidate action now updates only `pageIndex`, preserving nearest-center gradient timing. Observe actual gallery-button accessibility focus with one optional buffered integer `AccessibilityFocusState`, attached to the existing buttons. On a non-nil focus change while idle, assign `scrollPositionID` to that exact focused ID only if different. Never gate focus synchronization on the logical page index, map it to a repeated copy, or assign accessibility focus programmatically. Nil focus leaves selection intact. Preserve native gesture binding, phase-settle reconciliation, window rebasing, gallery-count handling and initial selection.

Luna owns this exact outside-repository draft; pause for any deviation or ambiguity. Keep the bounded diagnostics through the controlled comparison, including focus-ID changes, and remove every trace before final tests. Root verifies selected-gallery retention in both rotation directions and behind Detail, initial centering, ordinary/rapid swipes and both loop directions, and actual VoiceOver next/previous across the six exposed cards through Frontpage and back. The focus replacement is not verified until that runtime check passes. This corrects command ownership using a semantic native event; it adds no timing, resize suppression, alternate layout or OS fallback.


## Orchestrator amendment 23 — rely on verified native iOS 27 VoiceOver scrolling

Actual VoiceOver gestures on both devices now produce native interacting/idle phases and update the bound scroll target before accessibility-focus notifications. All six exposed cards can be traversed, exited to Frontpage and reached again; the temporary semantic focus synchronizer issued zero writes. Therefore remove its property, button attachment and handler entirely, alongside all diagnostics. Keep native scroll binding, phase reconciliation/window rebasing, existing centered sizing, and relative geometry exclusively for gradient/page-index observation. Remove obsolete claims that VoiceOver scrolling produces no phase. Add no compatibility branch or replacement fallback.

This supersedes amendment 22's additional focus synchronizer. Luna drafts the clean source and UI test outside the repository, following this scope exactly. The Home regression retains selection and time assertions and additionally checks that the selected button midpoint equals the carousel midpoint within one point, including initial presentation and both rotations. Root runs the complete clean UI plan and verifies actual VoiceOver next/previous through all six cards, Frontpage exit and return on both devices after its test actions finish. This final walkthrough checks that removing the focus attachment itself preserves the observed native behavior.

Amendment 23 cleanup also removes the now-unused stored scroll phase and its assignment; the native phase callback uses its new-phase argument directly for settlement and rebasing.

## Orchestrator amendment 24 — simplify redundant package declarations

The final comprehensive lint scope includes the changed manifest and reveals its existing 1,129-line violation. Simplify `AppPackage/Package.swift` without whitespace compression, a new DSL, exclusions or a package-graph change. In both existing target helpers, default `plugins` to `swiftLintPlugins` and remove the 66 identical call-site plugin arguments. Remove unused helper parameters and forwarding arguments: `path`, `exclude`, `sources`, `packageAccess`, `cSettings`, `cxxSettings`, `linkerSettings`, and regular-target `publicHeadersPath`. Remove unused `Exclude` and redundant `Path`/`Resource` wrappers, accept native `[PackageDescription.Resource]?`, forward resources directly and use `.process("Resources")` at existing call sites. Preserve all dependencies, target/product ordering, resources, settings, custom LegacyCFReadStream settings and C++ standard.

Luna owns an outside-repository draft only and must follow this amendment exactly, pausing on any ambiguity. Root applies after the active UI invocation exits. Capture same-toolchain `swift package --package-path AppPackage dump-package` before and after from the same package directory and require complete parsed JSON equality, including plugins/settings/resources for all 66 targets. Run strict lint over all changed/new Swift files. Graph equality establishes that the prior runtime suites still cover the resulting build inputs; no behavior or target configuration is authorized to change.

## Orchestrator amendment 25 — identify the reader toolbar by its page indicator

The final iPad session trace proves the post-autoplay indicator is under a navigation bar outside `reading_view`, while the page scroll view remains inside `reading_view`. The scoped overflow query returns zero matches. A manual reproduction with VoiceOver off shows More present after Off and successfully opening its native menu, so no production toolbar fix is indicated.

Correct only `ReaderPageProbe.moreMenuButton`: identify `app.navigationBars.containing(.staticText, identifier: "reading_page_indicator")`, assert exactly one matching navigation bar, query its native `OverflowBarButtonItem` buttons and assert exactly one, then return that uniquely identified button. Preserve normal XCTest idling, existing hittability wait, every page assertion and all app behavior. Add no hit-filter fallback, retry, delay, panel toggle or production identifier. Luna drafts outside the repository and pauses on ambiguity. Root applies after the current invocation finishes and runs the existing complete ReaderPageSyncUITests class on both devices; retain any W-38 result explicitly, without reinstating the owner's withdrawn fix.

Amendment 24 type-check refinement: PackageDescription's shared plugin values are main-actor-owned and non-Sendable under Swift 6.4. Mark the existing target-helper extension `@MainActor`, matching its manifest construction context. Do not suppress isolation diagnostics or copy non-Sendable globals across actors. The complete before/after package JSON is equal after this correction; the manifest is 994 lines and the comprehensive 80-file strict lint gate is clean.
