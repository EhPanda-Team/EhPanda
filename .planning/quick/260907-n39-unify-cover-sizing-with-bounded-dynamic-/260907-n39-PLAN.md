---
quick_id: 260907-n39
mode: quick
status: complete
---

# Unify cover sizing with bounded Dynamic Type scaling

Approved scope: apply the cover design discussed with the owner. Use compact/standard/hero
heights of 90/120/150 points and one bounded scale up to 1.25, retain 8:11 containers,
prefer full artwork in lists/detail, retain fill for the home hero, and preserve masonry.
The exact maxima are 112.5/150/187.5 points (the discussion rounded these for display).

Execution: inline, sequential quick workflow on the current clean checkout. The runtime
resolves orchestrator-worktree, and the quick dispatch gate degrades to sequential execution.
No subagents or runtime model claims. No new modules, dependency changes, or roadmap edits.

## Task 1: Shared cover policy and rendering

Files: new GalleryCoverStyle.swift, GalleryCoverMetrics.swift, GalleryCover.swift in
AppPackage/Sources/AppComponents; the six fixed-cover consumers in GalleryListComponents,
HomeFeature, SearchFeature and DetailFeature; HomeView+Sections.swift; GalleryCardLayout.swift.

Action: centralize role sizes, common headline-relative bounded scaling, placeholder, fitting,
background and clipping. Keep image source and success callback behavior. Keep list/detail
artwork complete and hero fill explicit. Top-align rows. Preserve narrow accessibility stacking.
Remove carousel viewport compression in favor of intrinsic height and a four-line reserved title
preview at all sizes; use width-aware accessibility stacking and compact rating when needed.
Resolve cover/title/rating in one Layout pass so lazy height estimates never use a provisional
narrow arrangement. Keep the carousel's paging identities, margins, and gradient behavior. Scale the frontpage strip
and its containing height together. Remove stale comments about the old sizing behavior.

Verify: lint changed Swift files with existing rules; build AppFeature for iOS Simulator.
Done: every fixed cover uses the same role policy and cannot shrink based on title length;
masonry and long-image cropping remain unchanged.

## Task 2: Verify layout and record results

Files: relevant existing test targets if meaningful coverage needs adding; this quick summary;
.planning/STATE.md.

Action: verify size hierarchy/defaults/caps and render representative production views with
fixture artwork on simulator at default and accessibility sizes, narrow phone and wide tablet,
including long titles. Inspect stable cover geometry, no overflow and carousel layout.
Use temporary verification harnesses only outside shipped application behavior. Record any
unavailable device verification candidly. Commit source atomically and docs separately.

Verify: relevant checks pass; inspect final diff and git status; no lint suppression or home
paths in generated Markdown. Simulator screenshots are not physical-device verification.
Done: summary describes checks and actual limitations, STATE quick table updated.
