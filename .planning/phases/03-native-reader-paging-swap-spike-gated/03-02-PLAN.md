---
phase: 03-native-reader-paging-swap-spike-gated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/HomeFeature/HomeView+Sections.swift
autonomous: true
requirements: [DEP-05]

must_haves:
  truths:
    - "CardSlideSection renders via a native horizontal ScrollView (no SwiftUIPager Pager) with .scrollTargetBehavior(.viewAligned) + .scrollTargetLayout() snap, LazyHStack(spacing: 20), item sizing matching Defaults.FrameSize.cardCellSize, .scrollClipDisabled() peek, and 0.2 neighbor opacity via .scrollTransition"
    - "The infinite loop (D-08) is preserved via a tripled-data buffer with an idle-phase, transaction-suppressed silent re-center (replacing .loopPages()) that is invisible at rest"
    - "The outward pageIndex Binding stays in sync with the logical gallery index (the .synchronize($pageIndex,$page.index) equivalent) as the buffer scrolls and re-centers"
    - "HomeFeature no longer imports SwiftUIPager; the package builds clean under SwiftLint-as-error"
  artifacts:
    - path: "AppPackage/Sources/HomeFeature/HomeView+Sections.swift"
      provides: "native CardSlideSection carousel + tripled-buffer infinite loop (spike-to-keep implementation)"
  key_links:
    - from: "current buffer scroll id"
      to: "outward $pageIndex Binding"
      via: "buffer-id → logical-gallery-index mapping"
      pattern: "pageIndex ="
    - from: "card"
      to: "0.2 opacity when off-center"
      via: ".scrollTransition { c, p in c.opacity(p.isIdentity ? 1 : 0.2) }"
      pattern: "scrollTransition"
    - from: ".onScrollPhaseChange == .idle at an edge copy"
      to: "silent re-center to the middle block"
      via: "transaction-suppressed scrollPosition write"
      pattern: "onScrollPhaseChange"
---

<objective>
Replace the Home `CardSlideSection` SwiftUIPager `Pager` (HomeView+Sections.swift:42-62) with a stock
horizontal paging `ScrollView` and reproduce every carousel parity item with standard components only
(D-01/D-06): peek + 0.2 opacity fade + 20pt spacing + snap + the MANDATORY infinite loop (D-08). This is
the single most-likely D-02 parity gap (RESEARCH Pitfall 5 / Open Question 2 — the tripled-buffer
re-center has no in-repo analog), so it is built first (Wave 1) to prove or surface it as early as
possible. Spike-to-keep (D-11): this is the real implementation, committed and evolvable — not a throwaway.

Purpose: the dependency can only leave `Package.swift` if BOTH call sites convert (D-02); the carousel's
infinite loop is the riskiest half. Proving the stock loop re-centers invisibly here de-risks the whole
phase before the reader work and long before removal.

Output: a native `CardSlideSection` with a tripled-buffer loop, HomeFeature free of the SwiftUIPager import.
The dependency declaration and acknowledgements are NOT removed here (that is gated on the go/no-go, Plan 05).

Implements D-01 (standard components only), D-06 (carousel modifier mapping), D-08 (infinite loop mandatory),
D-03 (exhaust standard loop approaches before declaring a gap), D-10/D-11 (full-surface, spike-to-keep).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-CONTEXT.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-RESEARCH.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-PATTERNS.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-VALIDATION.md

# File touched + scroll-idiom analog
@AppPackage/Sources/HomeFeature/HomeView+Sections.swift
@AppPackage/Sources/ReadingFeature/Support/AdvancedList.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild CardSlideSection as a native viewAligned paging ScrollView with peek + fade + spacing</name>
  <files>AppPackage/Sources/HomeFeature/HomeView+Sections.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/HomeView+Sections.swift — the whole `CardSlideSection` struct (lines 11-64): the `@StateObject private var page: Page = .withIndex(1)`, `@Binding private var pageIndex`, the `Pager(page:data:)` block and its modifier chain `.preferredItemSize(Defaults.FrameSize.cardCellSize).interactive(opacity: 0.2).itemSpacing(20).loopPages().pagingPriority(.high).synchronize($pageIndex,$page.index).frame(height:...)`, plus the `Equatable` conformance (must stay — the struct is `View, Equatable`). Preserve the inner `Button { navigateAction(gallery) } label: { GalleryCardCell(...).tint(.primary).multilineTextAlignment(.leading) }` verbatim.
    - AppPackage/Sources/ReadingFeature/Support/AdvancedList.swift — the stock `.scrollTargetLayout()` + `.scrollPosition(id:)` + `.onScrollPhaseChange` idiom to mirror (this is the in-repo native-scroll reference).
    - .planning/phases/03-native-reader-paging-swap-spike-gated/03-RESEARCH.md — § Architecture Patterns Pattern 4 (viewAligned peek + scrollTransition fade + tripled-buffer loop) and § Code Examples "Reproduce .interactive(opacity: 0.2)".
    - .planning/phases/03-native-reader-paging-swap-spike-gated/03-PATTERNS.md — the `HomeView+Sections.swift` mapping table (SwiftUIPager modifier → stock modifier).
    - .swiftlint.yml — active rules (force_unwrapping/force_try at error; line_length/file_length 120/1000; no @unchecked Sendable / @preconcurrency). Write to the spirit of the commented `unchecked_subscript_index_access` rule (guard buffer subscripts).
  </read_first>
  <action>
    Remove `import SwiftUIPager` from HomeView+Sections.swift and replace the `Pager` block in
    `CardSlideSection.body` with a stock `ScrollView(.horizontal, showsIndicators: false)` wrapping a
    `LazyHStack(spacing: 20)` (matches `.itemSpacing(20)`) over the card items, marked `.scrollTargetLayout()`.
    Apply `.scrollTargetBehavior(.viewAligned)` + `.pagingPriority(.high)` equivalent via viewAligned snap, size
    each card with `.containerRelativeFrame(.horizontal)` tuned to `Defaults.FrameSize.cardCellSize` (no
    `GeometryReader` — PROJECT/D-05 fence), add `.scrollClipDisabled()` so neighbor cards peek, and apply
    `.scrollTransition { content, phase in content.opacity(phase.isIdentity ? 1 : 0.2) }` per card to reproduce
    `.interactive(opacity: 0.2)`. Keep the outer `.frame(height: Defaults.FrameSize.cardCellHeight)` and the
    `Equatable` conformance. Bind the current item with `.scrollPosition(id:)` (a `@State` id var). Preserve the
    inner `Button`/`GalleryCardCell`/`.tint(.primary)`/`.multilineTextAlignment(.leading)` label verbatim.

    Do NOT add the tripled buffer yet — in this task the data is the plain `galleries` array so the peek/fade/
    spacing/snap are verifiable in isolation. Do NOT yet remove the SwiftUIPager dependency from Package.swift or
    the AboutView acknowledgement (Plan 05, gated on go/no-go). Keep `@Observable`/`@State` (not `@StateObject`)
    for the new id state — SwiftUIPager's `Page` `@StateObject` is gone.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'</automated>
  </verify>
  <acceptance_criteria>
    - `HomeView+Sections.swift` no longer contains `import SwiftUIPager` and `CardSlideSection.body` has no `Pager(`.
    - The carousel uses `ScrollView(.horizontal`, `.scrollTargetBehavior(.viewAligned)`, `.scrollTargetLayout()`, `LazyHStack(spacing: 20)`, `.scrollClipDisabled()`, `.scrollTransition`, and `.containerRelativeFrame(.horizontal)` (no `GeometryReader`).
    - The inner `Button`/`GalleryCardCell` label and the `.frame(height: Defaults.FrameSize.cardCellHeight)` + `Equatable` conformance are preserved.
    - `xcodebuild build -scheme AppPackage-Package` exits 0 (compiles clean under SwiftLint-as-error).
  </acceptance_criteria>
  <done>The carousel renders natively with peek + 0.2 fade + 20pt spacing + viewAligned snap over the plain galleries array; HomeFeature compiles without SwiftUIPager.</done>
</task>

<task type="auto">
  <name>Task 2: Add the tripled-buffer infinite loop with a suppressed idle-phase re-center and pageIndex sync</name>
  <files>AppPackage/Sources/HomeFeature/HomeView+Sections.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/HomeView+Sections.swift — the `CardSlideSection.body` from Task 1 and the `_pageIndex = pageIndex` Binding wiring in `init` (the outward binding the parent Home view observes — was `.synchronize($pageIndex,$page.index)`).
    - AppPackage/Sources/ReadingFeature/Support/AdvancedList.swift — the `.onScrollPhaseChange { _, newValue in if newValue == .idle ... }` + `performingChanges` guard idiom (mirror the "only act on idle" + "suppress the programmatic write" shape).
    - .planning/phases/03-native-reader-paging-swap-spike-gated/03-RESEARCH.md — § Architecture Patterns Pattern 4 "Infinite loop (D-08, MANDATORY)" (tripled buffer `[galleries]+[galleries]+[galleries]`, block-distinct ids, start centered, re-center at `.idle` without animation, map buffer id → logical index) and Pitfall 5 (visible re-center = D-02 gap; suppress the transaction, Phase 2 D-31 idiom).
    - .planning/phases/02-native-masonry-grid-swap/02-CONTEXT.md — the D-31 transaction/animation-suppression precedent used in Phase 2.
  </read_first>
  <action>
    Build a tripled data buffer: three concatenated copies of `galleries` with stable, block-distinct element
    ids (e.g. an index-encoded id per (blockOffset, galleryIndex) so the three copies of one gallery are three
    distinct scroll ids). Start the `.scrollPosition(id:)` centered on the middle block's entry for the current
    logical index. On `.onScrollPhaseChange` reaching `.idle`, if the settled id falls in the front or back copy,
    silently set `.scrollPosition` to the equivalent id in the MIDDLE block WITHOUT animation — wrap the write in
    a transaction that suppresses the animation (Phase 2 D-31 idiom) and guard against re-entrancy with a
    `performingChanges`-style flag so the re-center does not itself refire the mapping. Map the settled buffer id
    back to the logical `galleries` index and write it to the outward `$pageIndex` Binding on every settle
    (the `.synchronize` equivalent — MANDATORY, D-08). Clamp every buffer/logical index into the valid `indices`
    range before any subscript (bounds-safe; also the security mitigation and the spirit of `unchecked_subscript_index_access`).

    If, on manual observation later (go/no-go, Plan 05), the re-center is not flash-free, exhaust D-03 options
    first (key the re-center strictly off `.idle`, tighten the buffer id math, try `.onScrollGeometryChange`
    detection) before concluding a gap — "it's hard" is not "it's impossible" (D-03). Do not add a third-party loop
    helper (D-01).
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'</automated>
  </verify>
  <acceptance_criteria>
    - `CardSlideSection` builds a tripled buffer of `galleries` with block-distinct ids and starts centered in the middle block.
    - A `.onScrollPhaseChange`/`.idle` re-center writes `.scrollPosition` to the middle-block equivalent id inside an animation-suppressed transaction, guarded against re-entrancy.
    - The settled buffer id is mapped to the logical gallery index and written to `$pageIndex` on every settle (outward sync preserved).
    - Every buffer/logical index is clamped to a valid `indices` range before subscripting (no unchecked subscript).
    - `xcodebuild build -scheme AppPackage-Package` exits 0 under SwiftLint-as-error.
  </acceptance_criteria>
  <done>The carousel loops infinitely via a tripled buffer with a suppressed idle re-center and keeps pageIndex in sync; loop invisibility is pending the owner go/no-go (manual, Plan 05).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new files/targets. New in-file symbols introduced (exclude from drift checks): the `CardSlideSection`
tripled-buffer helper(s) and its `@State` scroll-position id / re-center guard state. The SwiftUIPager
`Page` usage in this file is removed.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI scroll engine → CardSlideSection buffer indexing | The layout engine drives the scrolled id; the view maps it to a buffer/logical index and subscripts `galleries`. No untrusted/network/user-text input crosses; galleries are already-fetched app data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Denial of Service (crash) | tripled-buffer id ↔ index mapping subscript | low | mitigate | Clamp every buffer/logical index to a valid `indices` range before subscripting `galleries`; re-center math never produces an out-of-range id. |
| T-03-SC | Tampering | package installs (npm/pip/cargo) | n/a | accept | No packages installed; SwiftUIPager removal is deferred to Plan 05 (gated on go/no-go). |

**No high-severity threats.** ASVS L1: a client-side UI carousel over already-fetched data; no auth/crypto/network/persistence-schema/input surface added (RESEARCH § Security Domain).
</threat_model>

<verification>
- `xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'` — compiles clean under SwiftLint-as-error.
- Behavior parity (peek/fade/spacing/snap + infinite-loop invisibility + pageIndex sync) is inherently manual and is verified by the owner via the D-11 go/no-go checklist in Plan 05 — it cannot be asserted headlessly (03-VALIDATION § Manual-Only Verifications).
- Run exactly ONE `xcodebuild` invocation at a time (MEMORY: no-overlapping-xcodebuild-test).
</verification>

<success_criteria>
- The carousel is a stock paging ScrollView reproducing peek + 0.2 fade + 20pt spacing + snap, with a tripled-buffer infinite loop and a suppressed re-center, keeping `pageIndex` in sync.
- HomeFeature no longer imports SwiftUIPager and the package builds clean.
- No dependency removed and no acknowledgement deleted (gated on the go/no-go, Plan 05).
</success_criteria>

<output>
Create `.planning/phases/03-native-reader-paging-swap-spike-gated/03-02-SUMMARY.md` when done — record the
modifier mapping used, the tripled-buffer re-center approach, and any D-03 options tried, so the go/no-go
checklist (Plan 05) can mark the carousel parity items with evidence.
</output>
