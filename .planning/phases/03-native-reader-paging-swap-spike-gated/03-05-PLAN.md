---
phase: 03-native-reader-paging-swap-spike-gated
plan: 05
type: execute
wave: 4
depends_on: [03-02, 03-04]
files_modified:
  - .planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Package.swift
  - AppPackage/Package.resolved
  - AppPackage/Sources/SettingFeature/Components/AboutView.swift
  - AppPackage/Sources/SettingFeature/Resources/Constant.xcstrings
autonomous: false
requirements: [DEP-05]

must_haves:
  truths:
    - "A committed go/no-go checklist (03-GO-NO-GO.md) enumerates every D-10 parity item — reader: horizontal, RTL, dual-page-landscape, PageHandler index mapping, autoplay, slider seek, tap-to-turn, resume-seed, zoom/pan/tap coexistence; carousel: peek + 0.2 opacity + 20pt spacing + snap + pageIndex sync + infinite-loop invisibility — each with a pass/gap mark, and is owner-signed-off before any dependency removal"
    - "On GO: SwiftUIPager is removed from AppPackage/Package.swift (dependency decl line ~21, the swiftUIPager static ~47, and all 3 .targetDependency(.swiftUIPager) entries incl. the stale appFeature one ~299 and the real homeFeature ~718 / readingFeature ~780), Package.resolved is regenerated, the AboutView acknowledgement row (~161-164) is deleted, and the acknowledgement.swiftUIPager + acknowledgement.swiftUIPager_link xcstrings keys (~508/550) are removed; the throwaway landed-id logging from Plan 04 is removed"
    - "On GO: a grep for the SwiftUIPager product name across AppPackage/Sources and AppPackage/Package.swift returns no matches; the package builds clean under SwiftLint-as-error and the full AppPackage-Package suite is green"
    - "On NO-GO (a genuine gap on ANY parity item): SwiftUIPager is KEPT, DEP-05 is documented not-viable/deferred in 03-GO-NO-GO.md with the failing item, and no half-migration is committed (D-02 all-or-nothing)"
  artifacts:
    - path: ".planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md"
      provides: "the D-11 signed parity checklist / D-02 decision record"
    - path: "AppPackage/Package.swift"
      provides: "SwiftUIPager-free dependency set (on GO)"
    - path: "AppPackage/Sources/SettingFeature/Components/AboutView.swift"
      provides: "SwiftUIPager acknowledgement row removed (on GO)"
    - path: "AppPackage/Sources/SettingFeature/Resources/Constant.xcstrings"
      provides: "acknowledgement.swiftUIPager + _link keys removed (on GO)"
  key_links:
    - from: "owner GO decision (blocking checkpoint)"
      to: "Package.swift SwiftUIPager removal"
      via: "gated Task 3 (runs only on GO)"
      pattern: "targetDependency"
---

<objective>
The D-02 all-or-nothing gate and the D-13 cleanup. Author the D-11 go/no-go checklist enumerating every
D-10 parity item (both call sites), pause for the owner to walk it on device/simulator and sign GO or
NO-GO, then — ONLY on GO — remove SwiftUIPager from the dependency set and its acknowledgements (mirroring
Phase 2's WaterfallGrid removal). This is the HARD human gate before the dependency leaves `Package.swift`:
a genuine gap on ANY parity item is a NO-GO → SwiftUIPager is kept, DEP-05 is marked not-viable/deferred,
and no half-migration is committed.

Purpose: the reader (Plans 03-04) and carousel (Plan 02) are built and evolvable (spike-to-keep, D-11), but
the dependency can only be removed if BOTH sites reached full parity (D-02). The checkpoint co-located with
the removal is the explicit gate that makes the executor pause for owner confirmation.

Output (GO): a SwiftUIPager-free `Package.swift` + regenerated `Package.resolved`, acknowledgement row and
xcstrings keys gone, throwaway logging removed, clean build + green suite, signed checklist. Output (NO-GO):
the signed checklist documenting the failing item and the deferral; SwiftUIPager retained.

Implements D-02 (all-or-nothing skip path), D-10 (full parity surface), D-11 (go/no-go checklist,
spike-to-keep), D-13 (dependency + acknowledgement cleanup), D-03 (a gap is only declared after standard
approaches were exhausted).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-CONTEXT.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-RESEARCH.md
@.planning/phases/03-native-reader-paging-swap-spike-gated/03-VALIDATION.md

# Cleanup targets + the Phase 2 removal precedent
@AppPackage/Package.swift
@AppPackage/Sources/SettingFeature/Components/AboutView.swift
@.planning/phases/02-native-masonry-grid-swap/02-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the D-11 go/no-go parity checklist artifact</name>
  <files>.planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md</files>
  <read_first>
    - .planning/phases/03-native-reader-paging-swap-spike-gated/03-CONTEXT.md — D-10 (the full parity surface to enumerate) and D-11 (checklist = every parity item with a pass/gap mark; a gap on any item triggers the D-02 skip); the § Specifics list of items.
    - .planning/phases/03-native-reader-paging-swap-spike-gated/03-VALIDATION.md — § Manual-Only Verifications (the exact behaviors + test instructions to reproduce per item).
    - .planning/phases/03-native-reader-paging-swap-spike-gated/03-03-SUMMARY.md, 03-04-SUMMARY.md (reader), 03-02-SUMMARY.md (carousel) — the built behaviors + the landed-id log evidence to reference per item.
    - AGENTS.md — "No absolute home paths in generated docs" (use `$HOME/…` or repo-relative in this markdown).
  </read_first>
  <action>
    Create `03-GO-NO-GO.md`: a markdown checklist with one row per D-10 parity item, grouped Reader / Carousel,
    each with a Pass/Gap column, a how-to-verify instruction (from 03-VALIDATION § Manual-Only Verifications), and
    an evidence column (e.g. the Plan 04 landed-id logs for programmatic jumps). Reader rows: horizontal paging;
    RTL direction (layoutDirection flip) with correct index; dual-page landscape paging over stacks (watch for the
    `.paging` landscape FB16486510 misalignment); PageHandler index mapping incl. dual-page cover math;
    programmatic jumps (slider seek, autoplay `.next`, tap-to-turn, resume-seed) smooth with no glitch/off-by-one;
    gesture coexistence (paging frozen at scale != 1, pan while zoomed, RTL edge single-tap turn). Carousel rows:
    peek; 0.2 opacity fade; 20pt spacing; viewAligned snap; `pageIndex` sync; infinite-loop invisibility (the
    tripled-buffer re-center — the single most-likely gap). Add a final "Decision: GO / NO-GO" line and a "D-02
    outcome" note (on NO-GO: which item failed, SwiftUIPager kept, DEP-05 deferred). Leave the Pass/Gap marks and
    the Decision blank for the owner — this task authors the STRUCTURE and instructions, not the verdict. No source
    code changes in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md; grep -qi 'infinite' .planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md; grep -qi 'dual-page' .planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-GO-NO-GO.md` exists with grouped Reader / Carousel rows, one per D-10 parity item, each with a Pass/Gap column, a how-to-verify instruction, and an evidence column.
    - It includes the reader dual-page-landscape row and the carousel infinite-loop-invisibility row (the two most-likely gaps), plus a "Decision: GO / NO-GO" line and a D-02 NO-GO outcome note.
    - Pass/Gap marks and the Decision are left blank (owner fills them at the checkpoint).
    - The file uses only repo-relative or `$HOME/…` paths (no absolute home path).
  </acceptance_criteria>
  <done>The go/no-go checklist enumerates every parity item with verify instructions and evidence slots, ready for the owner to sign.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Owner walks the parity checklist and signs GO or NO-GO (D-02 all-or-nothing gate)</name>
  <action>Manual verification only — no code changes in this task. Build and run the app, walk every row of `03-GO-NO-GO.md` on device + iOS 26 simulators (portrait + landscape), mark each parity item Pass/Gap, and record the GO/NO-GO decision in the checklist. This is the D-02 all-or-nothing gate that must be signed before Task 3 removes the dependency; a genuine gap on ANY item is a NO-GO (SwiftUIPager kept, DEP-05 deferred) and Task 3 is skipped.</action>
  <what-built>
    The reader (native paging ScrollView on the shared `PageModel`, RTL, dual-page, autoplay/slider/tap on the
    guarded shared-index jump, zoom/pan/tap coexistence — Plans 03-04) and the Home carousel (viewAligned peek +
    0.2 fade + 20pt spacing + snap + tripled-buffer infinite loop + pageIndex sync — Plan 02) are built with
    standard components only (D-01) and committed (spike-to-keep, D-11). SwiftUIPager is still declared, so
    rollback is a single revert. `03-GO-NO-GO.md` lists every D-10 parity item with verify instructions and the
    Plan 04 landed-id log evidence.
  </what-built>
  <how-to-verify>
    Build and run the app on device + iOS 26 simulators (portrait and landscape). Walk EVERY row of
    `03-GO-NO-GO.md`, marking Pass or Gap:
    1. Reader horizontal: page in .leftToRight — pages snap one-per-swipe, index tracks.
    2. Reader RTL: set .rightToLeft — order flips (layoutDirection), index stays logical, seek lands correctly.
    3. Reader dual-page landscape: enable dual-page, rotate to landscape — swipes page over stacks with exact snap
       (no half-way / skipped stack; watch FB16486510).
    4. PageHandler index mapping: confirm the on-screen page matches the slider/progress in single + dual-page.
    5. Programmatic jumps: slider seek, autoplay, tap-to-turn edges, and re-open (resume-seed) all land on the
       exact target with no glitch/off-by-one — cross-check against the landed-id logs (requested == settled).
    6. Gesture coexistence (D-09): zoom in → swipe does NOT page (frozen); pan moves the zoomed image; single-tap
       the RTL-correct edge turns the page the right direction; tap center toggles the panel.
    7. Carousel: peek shows neighbors; off-center cards dim to ~0.2; 20pt spacing; snap; the outward pageIndex
       tracks; scroll through the wrap boundary repeatedly — the loop re-center is INVISIBLE (no flash/stutter).

    GO only if EVERY item passes. If ANY item shows a genuine gap that standard-component approaches cannot close
    (D-03 exhausted), it is a NO-GO: SwiftUIPager is kept and DEP-05 is deferred (D-02). Record the marks and the
    decision in `03-GO-NO-GO.md`.
  </how-to-verify>
  <resume-signal>Reply "go" (all parity items pass — proceed to remove SwiftUIPager) or "no-go: &lt;which item(s) failed and on which device/orientation&gt;" (keep SwiftUIPager, defer DEP-05 — Task 3 is skipped).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: On GO — remove SwiftUIPager from the dependency set and its acknowledgements (D-13)</name>
  <files>AppPackage/Package.swift, AppPackage/Package.resolved, AppPackage/Sources/SettingFeature/Components/AboutView.swift, AppPackage/Sources/SettingFeature/Resources/Constant.xcstrings, AppPackage/Sources/ReadingFeature/ReadingView.swift</files>
  <read_first>
    - AppPackage/Package.swift — the `.package(url: ".../fermoya/SwiftUIPager", from: "2.5.0")` decl (~line 21); the `static let swiftUIPager: Self = .product(...)` (~47); the 3 `.targetDependency(.swiftUIPager)` entries — appFeature (~299, STALE: AppFeature does not import it), homeFeature (~718), readingFeature (~780).
    - AppPackage/Sources/SettingFeature/Components/AboutView.swift — the acknowledgement row `.init(urlString: String(localized: .Constant.acknowledgementSwiftUIPagerLink), text: .Constant.acknowledgementSwiftUIPager)` (~161-164).
    - AppPackage/Sources/SettingFeature/Resources/Constant.xcstrings — the keys `acknowledgement.swiftUIPager` (~508) and `acknowledgement.swiftUIPager_link` (~550); both are `shouldTranslate:false` proper-noun/URL entries carrying all-locale copies — delete each whole key entry cleanly (AGENTS.md non-translated-key rule; matches the Phase 2 WaterfallGrid removal shape).
    - AppPackage/Sources/ReadingFeature/ReadingView.swift — the throwaway landed-id `logger`/log lines added in Plan 04 (remove them now that the go decision is in).
    - .planning/phases/02-native-masonry-grid-swap/02-04-PLAN.md — the WaterfallGrid removal precedent (dep decl + target-deps + Package.resolved regen + acknowledgement removal).
    - MEMORY (ehpanda-standalone-swiftlint / no-overlapping-xcodebuild-test) — `swift package resolve` regenerates `AppPackage/Package.resolved` + `originHash`; run one xcodebuild invocation at a time.
  </read_first>
  <action>
    Run ONLY if the Task 2 checkpoint returned GO. First remove the AboutView acknowledgement row AND the two
    xcstrings keys together (order matters: the `.Constant.acknowledgementSwiftUIPager*` symbols are generated from
    the catalog — removing the row (usage) and the keys (definition) in the same change keeps the build coherent).
    Then, in `Package.swift`, delete the SwiftUIPager `.package(...)` dependency declaration, the `swiftUIPager`
    static product, and ALL 3 `.targetDependency(.swiftUIPager)` entries (appFeature stale + homeFeature +
    readingFeature). Remove the throwaway landed-id `logger`/log lines from `ReadingView.swift` (Plan 04
    instrumentation). Regenerate the resolved graph with `swift package resolve` in `AppPackage/` so
    `Package.resolved` no longer pins `fermoya/SwiftUIPager`. Confirm no source imports remain and that a grep for
    the SwiftUIPager product name across `AppPackage/Sources` and `AppPackage/Package.swift` returns nothing.

    On NO-GO: make NO changes here — leave SwiftUIPager and its acknowledgements in place; the deferral is recorded
    in `03-GO-NO-GO.md` (Task 1/2). Do not commit a half-migration (D-02).
  </action>
  <verify>
    <automated>xcodebuild clean test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'; echo "---grep---"; grep -rn 'SwiftUIPager' AppPackage/Sources AppPackage/Package.swift || echo 'NO SwiftUIPager references remain'</automated>
  </verify>
  <acceptance_criteria>
    - On GO: `AppPackage/Package.swift` contains no `SwiftUIPager` dependency decl, no `swiftUIPager` static, and no `.targetDependency(.swiftUIPager)` entry; `AppPackage/Package.resolved` no longer pins `fermoya/SwiftUIPager`.
    - On GO: the AboutView acknowledgement row and both `acknowledgement.swiftUIPager` / `acknowledgement.swiftUIPager_link` xcstrings keys are removed; the Plan 04 throwaway `logger` lines are gone.
    - On GO: `grep -rn 'SwiftUIPager' AppPackage/Sources AppPackage/Package.swift` returns no matches; `xcodebuild clean test -scheme AppPackage-Package` exits 0 (full suite green under SwiftLint-as-error).
    - On NO-GO: no source/config changes were made; `03-GO-NO-GO.md` records the failing item and the DEP-05 deferral.
  </acceptance_criteria>
  <done>On GO, SwiftUIPager is fully removed (dependency, targets, resolved, acknowledgement, xcstrings, throwaway logging) with a clean green build; on NO-GO, the dependency is retained and DEP-05 is deferred with no half-migration.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New artifact introduced by this plan (exclude from drift checks):
- `.planning/phases/03-native-reader-paging-swap-spike-gated/03-GO-NO-GO.md` — the D-11 signed parity checklist / D-02 decision record.
All other changes here are REMOVALS (SwiftUIPager dependency, target-deps, acknowledgement row, xcstrings keys, throwaway logging) — no new source symbols.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config (Package.swift / Package.resolved) → dependency graph | Removing a package dependency and regenerating the resolved pin. No runtime/untrusted input crosses; the change is compile-time only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | `swift package resolve` regenerating Package.resolved | low | mitigate | Regeneration only DROPS the `fermoya/SwiftUIPager` pin; the remaining pins/originHash are unchanged and verified by the clean build + full green suite. No new package is added. |
| T-03-SC | Tampering | package installs (npm/pip/cargo) | n/a | accept | This plan REMOVES a package and installs none; the SwiftLint plugin and all other pins are untouched. No `[ASSUMED]`/`[SUS]` install to gate. |

**No high-severity threats.** ASVS L1: a build-config removal + string-catalog edit; no auth/crypto/network/persistence-schema/input surface (RESEARCH § Security Domain).
</threat_model>

<verification>
- Manual go/no-go (Task 2) is the D-02 gate — every parity item owner-signed before removal.
- On GO: `xcodebuild clean test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'` — full suite green under SwiftLint-as-error; `grep -rn 'SwiftUIPager' AppPackage/Sources AppPackage/Package.swift` returns nothing; `AppPackage/Package.resolved` no longer pins SwiftUIPager.
- Run exactly ONE `xcodebuild` invocation at a time (MEMORY: no-overlapping-xcodebuild-test).
</verification>

<success_criteria>
- The go/no-go checklist covers every D-10 parity item and is owner-signed-off before any removal.
- On GO: SwiftUIPager is removed from the dependency set, acknowledgements, and resolved graph; build is clean and the full suite green (DEP-05 satisfied).
- On NO-GO: SwiftUIPager is kept, DEP-05 deferred, no half-migration committed (D-02).
</success_criteria>

<output>
Create `.planning/phases/03-native-reader-paging-swap-spike-gated/03-05-SUMMARY.md` when done — record the
go/no-go decision, the parity marks, and (on GO) the removal + resolve result, or (on NO-GO) the failing item
and the DEP-05 deferral.
</output>
