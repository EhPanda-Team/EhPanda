---
phase: 10-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/SystemNotificationExt/ (renamed to AppPackage/Sources/SystemNotification/)
  - AppPackage/Tests/SystemNotificationExtTests/ (renamed to AppPackage/Tests/SystemNotificationTests/)
  - AppPackage/Package.swift
  - AppPackage/Tests/FeatureTests.xctestplan
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
  - AppPackage/Sources/AppComponents/AppAlertState.swift
autonomous: true
requirements: [CRIT-11]

must_haves:
  truths:
    - "The toast module builds and its tests run under the name SystemNotification"
    - "Every importer compiles against the renamed module with zero source-behavior change"
    - "The renamed test target actually executes in the full-suite run (not just builds)"
  artifacts:
    - path: "AppPackage/Sources/SystemNotification/View+Toast.swift"
      provides: "Toast presentation implementation under the new module name"
    - path: "AppPackage/Sources/SystemNotification/.swiftlint.yml"
      provides: "SwiftLint coverage for the renamed module"
      contains: "parent_config: ../../../.swiftlint.yml"
    - path: "AppPackage/Tests/SystemNotificationTests/ToastInteractionTests.swift"
      provides: "Renamed test target source"
      contains: "@testable import SystemNotification"
  key_links:
    - from: "AppPackage/Package.swift"
      to: "AppPackage/Sources/SystemNotification/"
      via: "module enum case and .module(.systemNotification) target-dependency refs"
      pattern: "SystemNotification"
    - from: "AppPackage/Tests/FeatureTests.xctestplan"
      to: "AppPackage/Tests/SystemNotificationTests/"
      via: "test-target identifier and name entries"
      pattern: "SystemNotificationTests"
---

<objective>
Rename the `SystemNotificationExt` module to `SystemNotification` (ROADMAP Phase 10 criterion 11): the module contains the full toast implementation, not a thin extension, so the `Ext` suffix is a misnomer. Pure parity rename — zero behavior change, zero new warnings (D-10), sequential build/test (D-12).

Purpose: criterion 11; runs first because it is fully self-contained and every later sweep may touch importer files.
Output: renamed module + test target, updated Package.swift / imports / xctestplan / doc comments, full suite green with the renamed test target visibly running.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
</context>

<!-- planner-discipline-allow: SystemNotificationExt -->

<tasks>

<task type="auto">
  <name>Task 1: Rename module, test target, and every reference</name>
  <files>AppPackage/Sources/SystemNotificationExt/ → AppPackage/Sources/SystemNotification/, AppPackage/Tests/SystemNotificationExtTests/ → AppPackage/Tests/SystemNotificationTests/, AppPackage/Package.swift, AppPackage/Tests/FeatureTests.xctestplan, 8 importer files, AppPackage/Sources/AppComponents/AppAlertState.swift</files>
  <read_first>
    - AppPackage/Package.swift (enum case at ~:103 `systemNotificationExt = "SystemNotificationExt"`, tests enum at ~:128, 8 `.module(.systemNotificationExt)` refs at ~:292, :361, :605, :653, :734, :763, :1014, :1016)
    - AppPackage/Tests/FeatureTests.xctestplan (lines ~81-82: test-target identifier AND name reference the old test-target name — Pitfall 8, the easy-to-miss item)
    - AppPackage/Sources/SystemNotificationExt/.swiftlint.yml (must survive move byte-identical; path depth is unchanged so `parent_config: ../../../.swiftlint.yml` stays as-is)
    - .swiftlint.yml (repo root — read before writing any Swift, per repo CLAUDE.md)
  </read_first>
  <action>
    Perform the full parity rename per the verified touch list in 10-RESEARCH.md criterion 11 (analog process: Phase 6 GenericList→GalleryList rename):
    1. `git mv AppPackage/Sources/SystemNotificationExt AppPackage/Sources/SystemNotification` (carries ToastMessageView.swift, View+Toast.swift, .swiftlint.yml).
    2. `git mv AppPackage/Tests/SystemNotificationExtTests AppPackage/Tests/SystemNotificationTests`; update `@testable import` in ToastInteractionTests.swift to the new module name.
    3. AppPackage/Package.swift: rename the sources enum case to `systemNotification = "SystemNotification"`, the tests enum case to the renamed test dir, and all 8 `.module(...)` refs to the new case name.
    4. Update the 8 `import SystemNotificationExt` statements to the new name: AccountSettingView.swift:8, ReadingView.swift:10, TabBarView.swift:8, TorrentsView.swift:5, CommentsView.swift:7, GalleryInfosView.swift:6, ArchivesView.swift:6, DownloadsView+Subviews.swift:7. Keep `sorted_imports` ordering valid after the rename (the name change can alter alphabetical position within the import block).
    5. Update the 2 doc comments in AppComponents/AppAlertState.swift (:11, :132) to the new module name.
    6. AppPackage/Tests/FeatureTests.xctestplan:81-82 — update both the target identifier and name strings to SystemNotificationTests.
    No source logic changes anywhere. No collision risk: no external package named SystemNotification exists in Package.resolved (verified in research).
  </action>
  <verify>
    <automated>grep -rnF "SystemNotificationExt" AppPackage App ShareExtension --include="*.swift" --include="*.xctestplan" --include="Package.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnF "SystemNotificationExt" AppPackage App ShareExtension` (all file types) returns 0 hits
    - `AppPackage/Sources/SystemNotification/.swiftlint.yml` exists and contains `parent_config: ../../../.swiftlint.yml`
    - `AppPackage/Tests/SystemNotificationTests/ToastInteractionTests.swift` contains `@testable import SystemNotification`
    - `AppPackage/Tests/FeatureTests.xctestplan` contains `SystemNotificationTests` and no old-name entries
    - `git log --diff-filter=R` shows the moves as renames (history preserved)
  </acceptance_criteria>
  <done>Old module name has zero references anywhere in the repo; module/test dirs, Package.swift, imports, doc comments, and xctestplan all use SystemNotification.</done>
</task>

<task type="auto">
  <name>Task 2: Clean build, SwiftLint, and full suite with renamed target visible</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Validation Architecture — exact commands)
  </read_first>
  <action>
    D-10/D-12 gate, strictly sequential (ONE xcodebuild invocation at a time on this machine — never overlap):
    1. Run the full test suite: `xcodebuild test -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` (adjust destination to the installed simulator). Pitfall 8 check: confirm the renamed `SystemNotificationTests` suite APPEARS in the test output and passes — a rename that only builds could silently drop the target from the plan.
    2. Run SwiftLint via the DerivedData artifactbundle binary (not on PATH; delete AppPackage/.build first if package resolution is stale — established practice): zero new violations.
    Note: xcodebuild buffers stdout until exit — sparse log lines do not mean a hang; do not kill a run mid-launch.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0 with all tests passing (505-test baseline; DownloadSchedulingTests failures are real regressions, not flakes)
    - Test output contains the string "SystemNotificationTests" (target ran, not just built)
    - SwiftLint reports zero violations in changed files; zero new compiler warnings
  </acceptance_criteria>
  <done>Full suite green with SystemNotificationTests executing under its new name; build and lint clean (D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Compile-time module rename in a client-side app; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | package installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none); local module rename only |
</threat_model>

<verification>
- Old-name grep = 0 repo-wide (Swift + xctestplan + Package.swift)
- Full suite green with SystemNotificationTests visible in output
- SwiftLint + compiler: zero new warnings (D-10)
</verification>

<success_criteria>
- Criterion 11 fully satisfied: module renamed, every import/reference updated, `parent_config` lint wiring intact under the new name
- No behavior change (parity rename; full suite is the proof)
</success_criteria>

## Artifacts this phase produces (this plan)

- Module name `SystemNotification` (directory `AppPackage/Sources/SystemNotification/`)
- Package.swift enum case `systemNotification`
- Test target name `SystemNotificationTests` (directory `AppPackage/Tests/SystemNotificationTests/`)

<output>
Create `.planning/phases/10-ui-polish/10-01-SUMMARY.md` when done.
</output>
