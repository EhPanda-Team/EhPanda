---
phase: 10-ui-polish
plan: 09
type: execute
wave: 9
depends_on: [10-08]
files_modified:
  - "34 screen files with legacy preview structs (the 42-file inventory in 10-RESEARCH.md §POLISH-03 minus the 8 files migrated in 10-08): SettingFeature (10: SettingView, AccountSettingView, DownloadSettingView, AboutView, AppearanceSettingView, LaboratorySettingView, EhSettingView, GeneralSettingView, LoginView, AppActivityLogsView), HomeFeature screens (6: HomeView, ToplistsView, PopularView, WatchedView, HistoryView, FrontpageView), SearchFeature (2: SearchRootView, SearchView), DetailFeature (9: DetailView, TorrentsView, CommentsView, FolderManagerView, GalleryInfosView, DetailSearchView, ArchivesView, PreviewsView, TagDetailView), one-each (7: ReadingView, TabBarView, FavoritesView, DownloadsView, QuickSearchView, FiltersView, ReadingSettingView)"
autonomous: true
requirements: [POLISH-03]

must_haves:
  truths:
    - "Zero legacy preview structs remain anywhere in the codebase (D-07 — the grep gate is global, not per-file)"
    - "Whole-screen TCA views get one clean named #Preview (or an initial/loaded pair at most) — a full state matrix is deliberately NOT built for deep-store screens (D-08 pragmatic tier)"
    - "Previews stay at the default environment — no DT/color-scheme variants, no pinned size or scheme (D-09)"
  artifacts:
    - path: "AppPackage/Sources/HomeFeature/HomeView.swift"
      provides: "Macro preview with the lint-mandated store shorthand form"
      contains: "#Preview"
    - path: "AppPackage/Sources/SettingFeature/SettingView.swift"
      provides: "Macro preview for the Setting root screen"
      contains: "#Preview"
  key_links:
    - from: "TCA screen previews"
      to: "feature reducers"
      via: "Store(initialState:reducer: SomeFeature.init) shorthand (child_reducer_shorthand_store, error-level lint)"
      pattern: "reducer: .*\\.init"
  prohibitions:
    - statement: "No lifecycle hooks (.onAppear/.task), no Binding(get:set:), no single-line trailing closures in preview bodies (Phase 11 ratchet rules, Pitfall 5)"
      status: resolved
      verification: "SwiftLint run clean over all 34 files after migration"
    - statement: "No DT/color-scheme preview variants and no pinned fixed size or scheme (D-09 — content states only)"
      status: resolved
      verification: "No preferredColorScheme/type-size environment modifiers appear in the migrated preview regions"
---

<objective>
Migrate the remaining 34 legacy preview structs to the `#Preview` macro (POLISH-03 / ROADMAP criterion 12, completing D-07 after 10-08's 8 files): all whole-screen TCA views and remaining screens. Enrichment here is the PRAGMATIC tier of D-08 — these screens require deep store fixtures, so one clean named case (or an initial-vs-loaded pair where a loaded fixture already exists) is the correct scope; the full matrix was spent on the cells in 10-08.

Purpose: complete D-07 — no legacy preview struct may remain in the codebase (global grep gate).
Output: all 42 preview migrations done; global negative grep at zero.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift
</context>

<!-- planner-discipline-allow: PreviewProvider -->

<tasks>

<task type="auto">
  <name>Task 1: Migrate SettingFeature (10) + one-each screens (7)</name>
  <files>SettingFeature: SettingView, AccountSettingView, DownloadSettingView, AboutView, AppearanceSettingView, LaboratorySettingView, EhSettingView, GeneralSettingView, LoginView, AppActivityLogsView; one-each: ReadingView, TabBarView, FavoritesView, DownloadsView, QuickSearchView, FiltersView, ReadingSettingView (locate each via grep for its legacy preview struct — module paths vary)</files>
  <read_first>
    - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift (lines ~144-170 — named-case exemplar)
    - .swiftlint.yml (repo root — `child_reducer_shorthand_store` is error: store construction in previews must use the `reducer: SomeFeature.init` form, never a trailing-closure reducer)
    - each target file's existing preview struct (bottom of file) — its store/state construction carries over verbatim into the macro body
  </read_first>
  <action>
    Mechanical migration per 10-PATTERNS.md §POLISH-03: replace each legacy preview struct with a named `#Preview("…")` block whose body is the old `static var previews` content, preserving the existing store construction (`.init(initialState: .init(), reducer: SomeFeature.init)` — reducers carry the project's Feature suffix). Rules:
    - One clean named case per screen (name it for the state it shows, e.g. "Initial"); add a second "Loaded" case ONLY where the old preview or an existing `.preview` fixture already provides a loaded state — do not build deep store fixtures (D-08 pragmatic tier).
    - `@Previewable @State` (first statements) only where the old preview used a wrapper for a Binding; no wrapper container views survive.
    - D-09: default environment — strip nothing, add nothing (no scheme/size pinning).
    - Preview code follows production lint rules (Pitfall 5): no lifecycle hooks, no Binding(get:set:), no single-line trailing closures.
    - If a legacy struct wrapped multiple variants in a Group, split them into separate named cases.
  </action>
  <verify>
    <automated>grep -rlF "PreviewProvider" AppPackage/Sources/SettingFeature --include="*.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rF "PreviewProvider" AppPackage/Sources/SettingFeature --include="*.swift"` returns 0 hits
    - The 7 one-each files each contain `#Preview` and no legacy preview struct
    - Every TCA store in a migrated preview uses the `reducer: SomeFeature.init` shorthand form
  </acceptance_criteria>
  <done>17 screens migrated at the pragmatic tier; lint-safe preview bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate HomeFeature (6) + SearchFeature (2) + DetailFeature (9)</name>
  <files>HomeFeature: HomeView, ToplistsView, PopularView, WatchedView, HistoryView, FrontpageView; SearchFeature: SearchRootView, SearchView; DetailFeature: DetailView, TorrentsView, CommentsView, FolderManagerView, GalleryInfosView, DetailSearchView, ArchivesView, PreviewsView, TagDetailView (all under AppPackage/Sources/)</files>
  <read_first>
    - each target file's existing preview struct (bottom of file) — DetailView's preview uses a Gallery.preview fixture; carry such fixtures over as-is
  </read_first>
  <action>
    Same migration rules as Task 1 for the remaining 17 screens. DetailFeature note: DetailView's existing preview already constructs a loaded gallery fixture — keep it as the "Loaded" named case; screens whose old preview built only an empty initial state get a single "Initial" case. Do not invent loading/error store fixtures for deep-store screens (D-08: "a loaded-vs-initial pair is realistic; a full matrix is not pragmatic").
  </action>
  <verify>
    <automated>grep -rlF "PreviewProvider" AppPackage/Sources/HomeFeature AppPackage/Sources/SearchFeature AppPackage/Sources/DetailFeature --include="*.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero legacy preview structs remain in HomeFeature, SearchFeature, DetailFeature
    - DetailView keeps its loaded-gallery fixture as a named case
  </acceptance_criteria>
  <done>All 34 plan screens migrated.</done>
</task>

<task type="auto">
  <name>Task 3: Global D-07 grep gate + build/lint</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Validation Architecture — POLISH-03 static check)
  </read_first>
  <action>
    1. Global D-07 gate: `grep -rnF "PreviewProvider" AppPackage App ShareExtension --include="*.swift"` must return ZERO hits — this proves the full 42-struct inventory (10-08's 8 + this plan's 34) is migrated with nothing missed.
    2. `xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — zero warnings (macro previews compile with the target; the build is the migration's correctness proof). SwiftLint (artifactbundle binary) zero new violations. Strictly sequential (D-12).
  </action>
  <verify>
    <automated>grep -rnF "PreviewProvider" AppPackage App ShareExtension --include="*.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Global legacy-preview grep returns 0 hits across AppPackage, App, ShareExtension
    - Build exits 0 with zero warnings; SwiftLint zero new violations
  </acceptance_criteria>
  <done>D-07 satisfied codebase-wide; clean build/lint (D-10/D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Compile-time preview fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Information Disclosure | preview fixtures | low | mitigate | Synthetic `.preview` helpers only — never real credentials or gallery data (existing convention) |
| T-10-12 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Global legacy-preview grep = 0 (D-07 complete)
- Store shorthand form everywhere; D-09 environment rules held
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
</verification>

<success_criteria>
- POLISH-03 / criterion 12 fully satisfied: all 42 structs migrated, enrichment tiered per D-08, zero legacy previews remain
</success_criteria>

## Artifacts this phase produces (this plan)

- Named `#Preview` blocks in the 34 listed screen files (no other new symbols)

<output>
Create `.planning/phases/10-ui-polish/10-09-SUMMARY.md` when done.
</output>
