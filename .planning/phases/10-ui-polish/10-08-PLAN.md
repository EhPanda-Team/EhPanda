---
phase: 10-ui-polish
plan: 08
type: execute
wave: 8
depends_on: [10-07]
files_modified:
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/HomeFeature/GalleryRankingCell.swift
  - AppPackage/Sources/SearchFeature/GalleryHistoryCell.swift
  - AppPackage/Sources/AppComponents/RatingView.swift
  - AppPackage/Sources/AppComponents/NewDawnView.swift
  - AppPackage/Sources/AppComponents/SubSection.swift
autonomous: true
requirements: [POLISH-03]

must_haves:
  truths:
    - "The 8 stateful cell/component files use named #Preview cases only — no legacy preview structs remain in them"
    - "Stateful cells carry the full realistic-state matrix: boundary values (min/max rating 0/5, 0 vs large counts, page 1 vs max, short vs long titles) as named cases (D-08)"
    - "Previews stay at the default environment — no DT/color-scheme variants, no pinned size or scheme (D-09)"
  artifacts:
    - path: "AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift"
      provides: "Named #Preview state matrix for the primary list cell"
      contains: "#Preview("
    - path: "AppPackage/Sources/AppComponents/RatingView.swift"
      provides: "Interactive rating previews via @Previewable at min/max/half values"
      contains: "@Previewable"
  key_links:
    - from: "enriched #Preview blocks"
      to: "existing synthetic fixture helpers"
      via: ".preview model helpers (e.g. Gallery.preview) — never real credentials/gallery data"
      pattern: "\\.preview"
  prohibitions:
    - statement: "No lifecycle hooks (.onAppear/.task), no Binding(get:set:), no single-line trailing closures in preview bodies (Phase 11 ratchet rules, Pitfall 5)"
      status: resolved
      verification: "SwiftLint run clean; grep of the 8 files shows no onAppear/Binding(get: in preview regions"
    - statement: "No DT/color-scheme preview variants and no pinned fixed size or scheme (D-09 — content states only)"
      status: resolved
      verification: "grep of the 8 files' preview regions shows no preferredColorScheme/environment(\\.sizeCategory or dynamic-type trait usage"
---

<objective>
Migrate and ENRICH the 8 stateful cell/component previews — the full-matrix tier of D-08 (POLISH-03 / ROADMAP criterion 12): GalleryThumbnailCell, GalleryDetailCell, GalleryCardCell, GalleryRankingCell, GalleryHistoryCell, RatingView, NewDawnView, SubSection. These are the views where realistic-state coverage pays; the remaining 34 screen files follow in 10-09.

Purpose: POLISH-03 heavy tier — named `#Preview("…")` state matrices with modern features (`@Previewable`, `.sizeThatFitsLayout`).
Output: 8 files on enriched macro previews; zero legacy preview structs within them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift
</context>

<!-- planner-discipline-allow: PreviewProvider -->

<tasks>

<task type="auto">
  <name>Task 1: Migrate + enrich the 5 gallery cells</name>
  <files>AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift, AppPackage/Sources/HomeFeature/GalleryCardCell.swift, AppPackage/Sources/HomeFeature/GalleryRankingCell.swift, AppPackage/Sources/SearchFeature/GalleryHistoryCell.swift</files>
  <read_first>
    - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift (lines ~144-170 — the repo's only modern preview exemplar: named cases, @Previewable @State as first statements, file-private fixture constants)
    - .swiftlint.yml (repo root — `child_reducer_shorthand_store` requires the `reducer: SomeFeature.init` store form in previews too; `lifecycle_modifiers`/`binding_initializer`/`single_line_trailing_closure` must not be violated)
    - each target file's existing preview struct (bottom of file) — the initial state it constructs is the "loaded" baseline to enrich from
  </read_first>
  <action>
    For each of the 5 cells, replace the legacy PreviewProvider struct with named `#Preview("…")` cases per D-08's full-matrix tier:
    - Content states: loaded baseline (from the old preview) plus boundary cases — min/max rating (0/5), 0 vs large counts, page 1 vs max page, short vs long titles; loading/error/empty states where the cell renders them (e.g. thumbnail placeholder state).
    - Fixtures: file-private constants built from existing synthetic `.preview` model helpers (e.g. Gallery.preview) — extend fixture values inline (long-title strings, zeroed counts) rather than adding new model API; never real data.
    - Use `traits: .sizeThatFitsLayout` for these intrinsic-size cells; `@Previewable @State` (first statements of the body) only where the cell takes a Binding.
    - D-09: default environment only — no color-scheme, no type-size variants, no fixed frame pinning beyond what the old preview already did for layout sanity.
    Keep preview code production-grade (Pitfall 5): no lifecycle hooks, no Binding(get:set:), no single-line trailing closures.
  </action>
  <verify>
    <automated>grep -cF "PreviewProvider" AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift AppPackage/Sources/HomeFeature/GalleryCardCell.swift AppPackage/Sources/HomeFeature/GalleryRankingCell.swift AppPackage/Sources/SearchFeature/GalleryHistoryCell.swift  # every count must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 files contains at least 2 named `#Preview("…")` cases and zero legacy preview structs
    - Boundary-value cases present: at minimum a min/max-rating pair on rating-bearing cells and a short/long-title pair on title-bearing cells
    - All fixtures synthetic (`.preview` helpers or file-private constants)
  </acceptance_criteria>
  <done>5 gallery cells on enriched named previews at the full-matrix tier.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate + enrich RatingView, NewDawnView, SubSection; build gate</name>
  <files>AppPackage/Sources/AppComponents/RatingView.swift, AppPackage/Sources/AppComponents/NewDawnView.swift, AppPackage/Sources/AppComponents/SubSection.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/RatingView.swift (existing preview struct — RatingView is the interactive-state candidate: rating value drives star fill)
    - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift (@Previewable usage shape)
  </read_first>
  <action>
    1. RatingView: named cases at min (0), max (5), and a half-value rating; where the view takes a binding or tap-to-rate interaction, drive it with `@Previewable @State var rating` as the body's first statement. `traits: .sizeThatFitsLayout`.
    2. NewDawnView and SubSection: pragmatic tier — one clean named `#Preview` each (plus a second case only if the view has an obvious second content state, e.g. SubSection with/without its trailing action). No state matrix padding for trivial leaves (D-08).
    3. Build + lint gate (D-10/D-12, sequential): `xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` zero warnings; SwiftLint (artifactbundle binary) zero new violations. Macro previews compile as part of the target — the build is the migration's correctness proof.
  </action>
  <verify>
    <automated>grep -cF "PreviewProvider" AppPackage/Sources/AppComponents/RatingView.swift AppPackage/Sources/AppComponents/NewDawnView.swift AppPackage/Sources/AppComponents/SubSection.swift  # every count must be 0; then the build command with zero warnings</automated>
  </verify>
  <acceptance_criteria>
    - The 3 component files contain `#Preview(` and zero legacy preview structs
    - RatingView previews cover 0, 5, and a half value; interactive state (if any) via `@Previewable @State`
    - Build zero warnings; SwiftLint zero new violations
  </acceptance_criteria>
  <done>All 8 plan files migrated + enriched; clean build/lint (D-10/D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Compile-time preview fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Information Disclosure | preview fixtures | low | mitigate | Synthetic `.preview` helpers / file-private constants only — never real credentials or gallery data (existing convention, RESEARCH Security Domain) |
| T-10-10 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Legacy-preview grep = 0 across the 8 plan files
- Named-case + boundary-value criteria met; D-09 environment rules held
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
</verification>

<success_criteria>
- POLISH-03 full-matrix tier delivered for the 8 stateful cells/components (D-07 partial — completed across the codebase by 10-09)
</success_criteria>

## Artifacts this phase produces (this plan)

- Named `#Preview("…")` blocks in the 8 listed files
- File-private preview fixture constants (per file, synthetic data)

<output>
Create `.planning/phases/10-ui-polish/10-08-SUMMARY.md` when done.
</output>
