---
phase: 10-ui-polish
plan: 12
type: execute
wave: 12
depends_on: [10-11]
files_modified: []
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03, CRIT-05]

must_haves:
  truths:
    - "Full test suite green (sequential xcodebuild), with the renamed SystemNotificationTests target visibly executing"
    - "Every phase-wide negative grep holds: no legacy previews, no removed environment key, no deprecated color/corner/autocorrection/status-bar modifier calls, no DT caps"
    - "Owner has signed the D-03 gate: every user-facing screen (incl. sheets) readable and operable at XXL, AX3, and AX5"
    - "Numeric values animate as numeric transitions with no layout jitter (criteria 2-3, owner-visible)"
  artifacts:
    - path: ".planning/phases/10-ui-polish/10-12-SUMMARY.md"
      provides: "Phase-final gate record: grep battery results, suite output, D-03 sign-off"
  key_links:
    - from: "D-03 evidence set (10-10/10-11 verdict table + this plan's final pass)"
      to: "owner sign-off"
      via: "checkpoint:human-verify with organized per-screen evidence"
      pattern: "approved"
---

<objective>
Phase-final verification: run the full test suite on the settled surface, execute the phase-wide grep battery (every sweep's negative gate re-checked once, together), then present the owner-signed D-03 Dynamic Type gate (XXL + AX3 + AX5 across every user-facing screen including sheets — D-03 is an owner-signed visual gate; static checks cannot prove readable-and-operable).

Purpose: certify all 12 ROADMAP criteria on the final surface exactly once (RESEARCH ordering rationale: DT runs last so D-03 certifies the final surface).
Output: green suite + grep battery + owner-signed D-03; phase ready for /gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
@.planning/phases/10-ui-polish/10-10-SUMMARY.md
@.planning/phases/10-ui-polish/10-11-SUMMARY.md
</context>

<!-- planner-discipline-allow: PreviewProvider -->
<!-- planner-discipline-allow: inSheet -->
<!-- planner-discipline-allow: SystemNotificationExt -->
<!-- planner-discipline-allow: .foregroundColor( -->
<!-- planner-discipline-allow: .accentColor( -->
<!-- planner-discipline-allow: .cornerRadius( -->
<!-- planner-discipline-allow: disableAutocorrection -->
<!-- planner-discipline-allow: RoundedCorner -->
<!-- planner-discipline-allow: dynamicTypeSize -->

<tasks>

<task type="auto">
  <name>Task 1: Full suite + phase-wide grep battery</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Validation Architecture — Phase Requirements → Test Map; the full-suite command)
  </read_first>
  <action>
    Strictly sequential (D-12), one invocation at a time:
    1. Full suite: `xcodebuild test -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — all tests pass; confirm `SystemNotificationTests` appears in the output (criterion 11's run-not-just-build proof). A DownloadSchedulingTests failure is a real regression, not flake.
    2. Grep battery over AppPackage, App, ShareExtension (--include="*.swift"), each must return 0 hits: `PreviewProvider` (D-07), `inSheet`, `SystemNotificationExt` (also in Package.swift + xctestplan), `.foregroundColor(` (-F), `.accentColor(` (-F), `.cornerRadius(` (-F), `disableAutocorrection`, `.statusBar(hidden` (-F), `RoundedCorner`, `dynamicTypeSize` (D-02). Plus the counts: `.privacyMask()` == 42 (T-10-04 re-check), `minimumScaleFactor` <= 8, `GeometryReader` == 0, `Color.accentColor` lines == 4.
    3. SwiftLint (artifactbundle binary): zero violations in the phase's touched files; zero violations of the Phase 11 ratchet rules introduced this phase.
    4. Record every result in the SUMMARY (the criterion-by-criterion table).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0; output contains "SystemNotificationTests"
    - All 10 negative greps return 0; all 4 count checks hold (42 / <=8 / 0 / 4)
    - SwiftLint clean; zero new compiler warnings (D-10)
  </acceptance_criteria>
  <done>Automated phase gates all green, recorded per criterion.</done>
</task>

<task type="auto">
  <name>Task 2: D-03 evidence pass — XXL/AX3/AX5 across every screen</name>
  <files>none (evidence capture)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Criterion 5 — the FULL screen inventory: Home + Frontpage/Popular/Watched/History/Toplists sheets, Search + history cells, Favorites, Downloads + detail subviews, Detail + Archives/Torrents/Comments/Previews/GalleryInfos/DetailSearch/PostComment/TagDetail/FolderManager, Reading + ControlPanel + ReadingSetting sheet, Setting + Account/Login/EhSetting/General/Appearance/Reading/Laboratory/About/AppActivityLogs, Filters, QuickSearch, DateSeek, NewDawn, error surface, toasts)
    - .planning/phases/10-ui-polish/10-11-SUMMARY.md (final verdict table — screens already evidenced there need only a confirming spot re-check, not full re-capture)
  </read_first>
  <action>
    Via the sim-use skill, on the final surface (post-10-11):
    1. Drive every screen in the inventory at XXL, AX3, and AX5; capture screenshots organized by screen×size. Reuse 10-11 evidence where the screen is unchanged since — capture fresh only what is missing or stale.
    2. While in the reader and downloads screens at default size, exercise the numeric transitions once more (page swipe, progress tick if available) — criteria 2-3 evidence (numeric animation, no jitter).
    3. Assemble the owner-facing evidence set: per-screen worst-size (AX5) screenshots first, the full matrix behind them, the 10-10/10-11 verdict table linked, plus the numeric-transition observations and any escalations 10-11 recorded.
  </action>
  <verify>
    <automated>ls .planning/phases/10-ui-polish/ | grep -c "SUMMARY"  # evidence assembly recorded in this plan's SUMMARY draft; prior-plan SUMMARYs present (expect >= 11)</automated>
  </verify>
  <acceptance_criteria>
    - Evidence set covers every inventory screen at AX5 minimum, with XXL/AX3 sampling per the D-03 rule
    - Numeric-transition observations recorded (criteria 2-3)
    - Escalations from 10-11 (if any) surfaced at the top of the evidence set
  </acceptance_criteria>
  <done>Complete, organized D-03 evidence ready for owner review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner-signed D-03 gate and phase sign-off</name>
  <files>none (owner review)</files>
  <action>Present the Task 1 gate results and the Task 2 D-03 evidence set to the owner and pause for the sign-off. D-03 is an owner-signed visual gate — do not close the phase without an explicit response. On issues, reflow the named sites per D-02 (never cap), re-run the affected gates, and re-present.</action>
  <what-built>Phase 10 complete on the settled surface: numeric-text pairing (POLISH-01), ZStack conversions (POLISH-02), all 42 preview migrations (POLISH-03), inSheet removal, deprecated-API sweeps, custom corner-modifier removal, Label conversions, module rename, and the whole-app Dynamic Type audit-and-reflow — full suite green, all grep gates at zero.</what-built>
  <how-to-verify>
    1. Review the Task 2 evidence set: every user-facing screen (including sheets) at AX5 — confirm readable (no clipped essential text, no overlap) and operable (controls reachable); spot-check XXL/AX3 samples.
    2. Optionally drive it live: set Larger Text to AX5 in the simulator and walk Home → Detail → Reading → Setting, plus one sheet (Filters or QuickSearch).
    3. Confirm the reader page indicator animates numerically without layout shift at default size (criteria 2-3).
    4. Review any escalations (sites needing redesign-level change, if 10-11 recorded any) and decide their disposition.
  </how-to-verify>
  <resume-signal>Type "approved" to sign the D-03 gate and close the phase for /gsd-verify-work, or describe issues (screen + size + what broke) — the executor reflows the named sites per D-02 (never cap) and re-presents evidence.</resume-signal>
  <verify>
    <human-check>Owner reviewed the D-03 evidence set (every screen incl. sheets at XXL/AX3/AX5) and responded</human-check>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -5  # suite still green at sign-off</automated>
  </verify>
  <done>Owner-signed D-03 gate recorded; phase certified for /gsd-verify-work (or issues fed back into reflow).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Switcher / screen capture | Final re-check that the Phase 7 privacy-mask guarantee survived the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Information Disclosure | privacyMask coverage | high | mitigate | Task 1 count check: privacyMask() == 42 on the final tree (re-verifies T-10-04 after all sweeps) |
| T-10-16 | Tampering | package installs | low | accept | No packages installed this phase (RESEARCH audit: none) |
</threat_model>

<verification>
- Full suite green with renamed test target executing; grep battery + count checks all hold
- D-03 evidence complete; owner sign-off recorded
- Zero new warnings phase-wide (D-10); all xcodebuild invocations sequential (D-12)
</verification>

<success_criteria>
- All 12 ROADMAP Phase 10 success criteria certified on the final surface; owner-signed D-03; phase ready for /gsd-verify-work
</success_criteria>

## Artifacts this phase produces (this plan)

- No code symbols — verification evidence and the phase-final gate record only

<output>
Create `.planning/phases/10-ui-polish/10-12-SUMMARY.md` when done.
</output>
