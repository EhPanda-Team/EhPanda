---
phase: 10-ui-polish
plan: 07
type: execute
wave: 7
depends_on: [10-06]
files_modified:
  - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift
  - AppPackage/Sources/GalleryListComponents/DownloadBadgeLabel.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/SettingFeature/Components/DownloadSettingView.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Every value that visibly changes on screen carries BOTH monospacedDigit and the numericText content transition (D-04 pairing)"
    - "Static one-off numbers carry NEITHER modifier — an untreated static number is the intended outcome, not a coverage gap (verifier note, D-04)"
    - "No layout jitter on value change — monospacedDigit is the jitter guarantee (D-06)"
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift"
      provides: "Reader current/total page indicator with the paired treatment"
      contains: "contentTransition(.numericText"
    - path: "AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift"
      provides: "Cache-size text with the paired treatment"
      contains: "monospacedDigit"
  key_links:
    - from: "treated Text sites"
      to: "changing state values"
      via: ".animation(_:value:) keyed on the changing value (transition animates; inert otherwise per A6)"
      pattern: "\\.animation\\(.*value:"
---

<objective>
Apply the paired numeric-text treatment (POLISH-01, D-04..D-06): `.monospacedDigit()` + `.contentTransition(.numericText())` — always together, and ONLY on values that visibly change on screen (the D-05 set: live values + user-driven changes). Copy the in-repo exemplar at DownloadsView+Subviews.swift:179-186 verbatim as the pattern, including the `.animation(_:value:)` keyed on the changing value.

Purpose: POLISH-01 / ROADMAP criteria 1-3 — numeric text animates as numeric transitions with zero layout jitter.
Output: all D-05 sites treated; deliberately-narrow scope documented for the verifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
@AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Treat the D-05 numeric-text sites</name>
  <files>AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift, AppPackage/Sources/GalleryListComponents/DownloadBadgeLabel.swift, AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift, AppPackage/Sources/SettingFeature/Components/DownloadSettingView.swift, AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift (lines ~177-190 — THE exemplar: both modifiers plus .animation(_:value:) keyed on the changing value; copy this shape exactly)
    - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift (lines ~20-40 DescScrollInfo rows: favoritedCount/ratingCount/pageCount/sizeCount/rating; line ~104 live rating display)
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§POLISH-01 — the two site tables: add-missing-half vs add-both)
  </read_first>
  <action>
    Per D-04 (always paired) and D-05 (only visibly-changing values):
    1. Add the missing `.contentTransition(.numericText())` half at the 4 sites already carrying monospacedDigit: ControlPanel.swift:170 (reader current/total — changes every page swipe), DownloadBadgeLabel.swift:17 (live download progress), ArchivesView.swift:139 (GP/credits funds after purchase), DownloadSettingView.swift:15 (thread-limit slider value).
    2. Add BOTH modifiers at the currently-untreated D-05 sites: GeneralSettingView.swift:114 (diskImageCacheSize — changes after clearing cache), DetailView+Subviews.swift:20-40 DescScrollInfo rows (favoritedCount, ratingCount, pageCount, sizeCount, rating — change on refresh/user rating) and :104 (rating on tap).
    3. Variant rule: `.numericText(value:)` with the numeric value where the text is a pure number whose count direction matters; plain `.numericText()` for mixed strings like a formatted size. Pair each treated Text with `.animation(_:value:)` keyed on its changing value (per the exemplar; without an animation scope the transition is inert — harmless but pointless, A6).
    4. Best-judgment sweep (D-05 tail): while editing, treat any OTHER Text in these files whose value visibly changes on screen; leave every static one-off number untreated and say so in the SUMMARY (verifier note: untreated static numbers are correct, not gaps).
    Line numbers are pre-phase references — re-grep before editing (waves 2-6 shifted lines).
  </action>
  <verify>
    <automated>for f in $(find AppPackage/Sources \( -name "ControlPanel.swift" -o -name "DownloadBadgeLabel.swift" -o -name "ArchivesView.swift" -o -name "DownloadSettingView.swift" -o -name "GeneralSettingView.swift" -o -name "DetailView+Subviews.swift" \)); do echo "$f: $(grep -c monospacedDigit "$f") / $(grep -c numericText "$f")"; done  # pair-check: both counts nonzero in every listed file</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 listed files contains BOTH `monospacedDigit` and `numericText` tokens (grep pair-check), and every treated Text carries both modifiers co-located on the same view
    - Every treated Text has an `.animation(_:value:)` keyed on its changing value (or sits inside an existing animation scope — documented if so)
    - SUMMARY lists treated sites AND deliberately-untreated static sites (the D-04 verifier note)
  </acceptance_criteria>
  <done>All D-05 sites paired-treated; static numbers untouched and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Build/lint gate and jitter check</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-UI-SPEC.md (§Numeric Text Contract)
  </read_first>
  <action>
    1. `xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — zero warnings; SwiftLint zero new violations (D-10, sequential D-12).
    2. Via sim-use: open the reader and swipe pages — the current/total indicator must tick as a numeric transition with NO horizontal layout shift (D-06 jitter check; monospacedDigit is the guarantee). If a download can be started cheaply, also observe the progress badge; otherwise note that the D-03/final gate covers it.
    3. Screenshot/observation evidence in the SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -ci "warning" # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Build zero warnings; SwiftLint zero new violations
    - Reader page-indicator observation recorded: numeric transition visible, no layout jitter at default type size
  </acceptance_criteria>
  <done>POLISH-01 criteria 1-3 hold: paired modifiers, numeric animation, no jitter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Text-modifier application; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Pair-check grep: both tokens present per treated file, co-located per site
- Reader jitter check via sim-use recorded
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
</verification>

<success_criteria>
- POLISH-01 satisfied under its D-04/D-05 reading: changing values paired-treated, static values deliberately untouched, no jitter
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — modifier applications on existing Text views

<output>
Create `.planning/phases/10-ui-polish/10-07-SUMMARY.md` when done.
</output>
