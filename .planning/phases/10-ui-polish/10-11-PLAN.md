---
phase: 10-ui-polish
plan: 11
type: execute
wave: 11
depends_on: [10-10]
files_modified:
  - "The broke-at-AX5 subset of the 10-10 audit verdict table (files known only after the audit; candidates come from the 33 lineLimit(1) + 35 fixed frame(height:) risk sites across AppPackage/Sources)"
autonomous: true
requirements: [CRIT-05]

must_haves:
  truths:
    - "Every broke-at-AX5 verdict from the 10-10 audit is fixed by REFLOW — text grows to the user's chosen size everywhere; no site is fixed by capping or shrinking (D-02, absolute)"
    - "Every reflowed screen is pixel-equivalent at default (.large) size — reflow constructs are no-ops at default size (redesign guard, Pitfall 6)"
    - "Fixed sites re-verified readable AND operable at AX5"
  artifacts:
    - path: "AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift"
      provides: "The repo's ViewThatFits exemplar (existing) — the pattern reflow fixes copy"
      contains: "ViewThatFits"
  key_links:
    - from: "reflowed layouts"
      to: "AX5 type size"
      via: "ViewThatFits alternative arrangements / wrapping / ScrollView / dropped fixed heights and line limits"
      pattern: "ViewThatFits|\\.scrollBounceBehavior|lineLimit"
  prohibitions:
    - statement: "No Dynamic Type max-size caps — count stays 0 (D-02; the cap-as-last-resort alternative was explicitly rejected, even for compact chrome)"
      status: resolved
      verification: "grep -rn 'dynamicTypeSize' AppPackage/Sources App --include='*.swift' | wc -l returns 0"
    - statement: "No new shrink-to-fit: minimumScaleFactor count stays <= 8 (pre-existing baseline); no GeometryReader (count stays 0) — use onGeometryChange/containerRelativeFrame/ViewThatFits per Phase 5 convention"
      status: resolved
      verification: "grep -rn 'minimumScaleFactor' AppPackage/Sources App ShareExtension --include='*.swift' | wc -l <= 8; grep -rn 'GeometryReader' ... | wc -l == 0"
---

<objective>
Fix every breakage the 10-10 audit observed — reflow, never cap (ROADMAP criterion 5, D-02): layouts adapt via ViewThatFits, wrapping, ScrollView, and dropped fixed heights / line limits, so text always grows to the user's chosen size on every user-facing screen including sheets. Fix ONLY where breakage was observed (the UI-checker rule: the risk inventories are an audit surface, not a fix list); every fix must be a no-op at default (.large) size.

Purpose: criterion 5 remediation, completing D-01's audit-and-fix on the settled surface before the D-03 gate certifies it.
Output: zero broke-at-AX5 verdicts remaining; default-size parity proven per reflowed screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
@.planning/phases/10-ui-polish/10-10-SUMMARY.md
</context>

<!-- planner-discipline-allow: dynamicTypeSize -->
<!-- planner-discipline-allow: minimumScaleFactor -->
<!-- planner-discipline-allow: GeometryReader -->

<tasks>

<task type="auto">
  <name>Task 1: Reflow every broke-at-AX5 site</name>
  <files>the broke-at-AX5 subset from the 10-10 verdict table</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-10-SUMMARY.md (the verdict table — THE work order; fix nothing marked fine)
    - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift (lines ~216-239 — the ViewThatFits exemplar: horizontal arrangement first, vertical fallback; a no-op at default size)
    - .planning/phases/10-ui-polish/10-PATTERNS.md (§Dynamic Type reflow — primitives and rules)
    - .swiftlint.yml (repo root)
  </read_first>
  <action>
    For each broke-at-AX5 verdict, apply the smallest reflow that makes the site readable AND operable at AX5 (D-02 toolbox, in preference order):
    1. Drop the constraint: remove a lineLimit(1) or fixed frame(height:) where the layout tolerates natural growth (most list rows and labels).
    2. Alternative arrangement: ViewThatFits(in: .horizontal) with the current layout first and a stacked fallback second (copy the HeaderSection exemplar — the fallback only engages at large sizes, guaranteeing default-size parity).
    3. Wrapping/stacking: HStack → adaptive layout for label+value rows that collide at AX sizes.
    4. Scroll: wrap fixed-viewport content (e.g. dense control panels, sheets with tall content) in a ScrollView so controls stay reachable.
    5. @ScaledMetric(relativeTo:) for any remaining fixed metric that fights text growth (badges, thumbnails tied to text rows).
    FORBIDDEN fixes (D-02 absolute): DT max-size caps, shrink-to-fit factors, reintroducing GeometryReader, and any change visible at default size. Where a heavily-broken site would need redesign to reflow (visual change at default size), stop and record it for the 10-12 checkpoint rather than redesigning — the milestone forbids visual redesign.
    Update the verdict table in place: each broke verdict gains its fix description.
  </action>
  <verify>
    <automated>grep -rn "dynamicTypeSize" AppPackage/Sources App --include="*.swift" | wc -l  # must be 0; grep -rn "minimumScaleFactor" AppPackage/Sources App ShareExtension --include="*.swift" | wc -l  # must be <= 8; grep -rn "GeometryReader" AppPackage/Sources App ShareExtension --include="*.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Every broke-at-AX5 verdict in the 10-10 table has a corresponding reflow fix (or an explicitly-recorded escalation for the 10-12 checkpoint)
    - Prohibition greps hold: cap count 0, shrink count <= 8, GeometryReader 0
    - Build zero warnings; SwiftLint zero new violations (D-10, sequential D-12)
  </acceptance_criteria>
  <done>All observed breakage reflowed within D-02's rules; escalations (if any) documented.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify fixes at AX5 and default-size parity</name>
  <files>possible follow-up tweaks to Task 1's reflowed sites</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-UI-SPEC.md (§Verification Gates — the Default-size parity row)
  </read_first>
  <action>
    Build once (sequential, D-12), then via sim-use:
    1. AX5 re-check: revisit every screen that had a broke verdict — confirm readable (no clipped essential text, no overlap) and operable (controls reachable, tappable) at AX5. Screenshot each.
    2. Default-size parity (Pitfall 6 redesign guard): set DT back to default (.large) and screenshot every REFLOWED screen — each must look unchanged versus its pre-phase appearance (ViewThatFits fallbacks and dropped limits must be no-ops at default size). Any visible default-size change → rework the fix until parity holds.
    3. Update the verdict table to final state: every site `fine` (originally or after fix), with the screenshot evidence linked in the SUMMARY. This table + evidence is the D-03 gate's input in 10-12.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -c "warning: " # printed count must be 0 (grep exits 1 on zero matches — gate on the output value, not exit status)</automated>
  </verify>
  <acceptance_criteria>
    - AX5 screenshots show every formerly-broken site readable and operable
    - Default-size screenshots of every reflowed screen show no visible change
    - Final verdict table has zero unresolved broke verdicts (or documented escalations only)
  </acceptance_criteria>
  <done>Criterion 5 remediation complete: reflow-only fixes verified at AX5 and at default-size parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Layout reflow within existing views; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Prohibition greps hold (0 / <=8 / 0)
- Final verdict table: zero unresolved breakage; AX5 + default-size screenshot evidence complete
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
</verification>

<success_criteria>
- Every observed DT breakage fixed by reflow (D-02); default-size appearance unchanged everywhere; evidence ready for the owner-signed D-03 gate in 10-12
</success_criteria>

## Artifacts this phase produces (this plan)

- Reflow constructs at fixed sites (ViewThatFits arrangements, ScrollView wrappers, @ScaledMetric properties) — exact symbols determined by the 10-10 verdict table

<output>
Create `.planning/phases/10-ui-polish/10-11-SUMMARY.md` when done.
</output>
