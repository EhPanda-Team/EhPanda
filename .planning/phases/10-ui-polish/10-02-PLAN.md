---
phase: 10-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - "AppPackage/Sources/** (grep-driven color-modifier sweep; 30 files with foregroundColor sites + 13 files with accentColor-modifier sites, verified lists in tasks)"
  - AppPackage/Sources/AppComponents/ToolbarItems.swift
  - AppPackage/Sources/AppFeature/RootView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
autonomous: true
requirements: [CRIT-07]

must_haves:
  truths:
    - "No deprecated color modifiers remain: every foregroundColor call is foregroundStyle, every accentColor modifier call is tint"
    - "Rendered appearance is identical everywhere (pure mechanism swap — diff-review tier per D-11)"
    - "CustomToolbarItem still inherits tint when its Optional tint is nil (no forced default color)"
  artifacts:
    - path: "AppPackage/Sources/AppComponents/ToolbarItems.swift"
      provides: "Optional-tint handled by conditional foregroundStyle application"
      contains: "foregroundStyle"
    - path: "AppPackage/Sources/AppFeature/RootView.swift"
      provides: "Root tint via .tint(.primary)"
      contains: "tint(.primary)"
  key_links:
    - from: "AppPackage/Sources/AppFeature/RootView.swift"
      to: "tab bar / link tinting app-wide"
      via: "root-level tint modifier"
      pattern: "\\.tint\\("
  prohibitions:
    - statement: "Static accent-color reads (written implicit-member form `.accentColor`, 4 lines across 3 files) must NOT be converted — the static property is not deprecated"
      status: resolved
      verification: "grep -rnE '\\.accentColor([^(]|$)' AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections1.swift AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift | wc -l returns 4 (live-verified baseline: :102, :380, :71+:79 — NOTE the source spells them implicit-member `.accentColor`, so a `Color.accentColor` grep matches nothing)"
---

<objective>
Sweep the two highest-count deprecated color modifiers (ROADMAP Phase 10 criterion 7): `.foregroundColor(_:)` → `.foregroundStyle(_:)` (43 sites, 30 files) and the `.accentColor(_:)` modifier → `.tint(_:)` (27 sites, 13 files), at exact appearance parity with zero new warnings (D-10). Pure-appearance swaps — diff-review verification tier per D-11, plus one root-tint spot-check.

Purpose: criterion 7 (deprecated SwiftUI API sweep) and a down-payment on the Phase 11 lint ratchet.
Output: zero deprecated color-modifier calls; identical rendering.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
</context>

<!-- planner-discipline-allow: .foregroundColor( -->
<!-- planner-discipline-allow: .accentColor( -->

<tasks>

<task type="auto">
  <name>Task 1: foregroundColor → foregroundStyle (43 sites, incl. Optional-tint trap)</name>
  <files>30 files (verified grep list): AppComponents/{AlertView,CategoryView,Placeholder,SubSection,TagCloudView,TagSuggestionView,ToolbarItems,ViewModifiers}.swift; DetailFeature/{Archives/ArchivesView,Components/TagDetailView,DetailView+Subviews,Previews/PreviewsView}.swift; GalleryListComponents/{Cells/GalleryThumbnailCell,GalleryList}.swift; HomeFeature/{GalleryCardCell,GalleryRankingCell,HomeView+Sections}.swift; QuickSearchFeature/QuickSearchView.swift; ReadingFeature/{ReadingViewComponents,Support/ControlPanel}.swift; SearchFeature/{GalleryHistoryCell,SearchRootView+Keywords}.swift; SettingFeature/{AccountSetting/AccountSettingView,AppActivityLogs/AppActivityLogsView,Components/AboutView,EhSetting/EhSettingView+Sections1,EhSetting/EhSettingView+Sections2,EhSetting/EhSettingView+Sections3,GeneralSetting/GeneralSettingView,SettingView}.swift (all under AppPackage/Sources/)</files>
  <read_first>
    - .swiftlint.yml (repo root — read before writing Swift; `line_length` 120 is error, swaps may lengthen lines)
    - AppPackage/Sources/AppComponents/ToolbarItems.swift (line ~29: `CustomToolbarItem` applies the deprecated modifier to `tint: Color?` where nil means inherit — THE trap)
    - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift (line ~172 — in-repo modern exemplar `.foregroundStyle(status.tintColor)`)
  </read_first>
  <action>
    Grep-driven 1:1 swap of every deprecated foreground-color modifier call to `.foregroundStyle(_:)` across the 30 files listed (43 sites). Same argument, same position in the modifier chain — appearance is identical (Pitfall: none for the plain sites; this is the diff-review tier of D-11).
    THE exception — Pitfall 2: `CustomToolbarItem` (ToolbarItems.swift:29) passes `Optional<Color>` (nil = inherit ambient tint). `foregroundStyle` has no Optional overload. Restructure to conditional application: apply `.foregroundStyle(tint)` only inside an `if let tint` branch, else return the content unmodified — never force-unwrap, never substitute a default color (that would change appearance where nil today inherits). Before sweeping, grep the 43 sites for any OTHER Optional-color argument and handle identically.
    Honor `single_line_trailing_closure` and `line_length` while editing (Phase 11 ratchet rules — add zero violations, D-10).
  </action>
  <verify>
    <automated>grep -rnF ".foregroundColor(" AppPackage/Sources App ShareExtension --include="*.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnF ".foregroundColor(" AppPackage/Sources App ShareExtension --include="*.swift"` returns 0 hits
    - ToolbarItems.swift contains an `if let` (or equivalent non-defaulting conditional) around its foregroundStyle application; no force-unwrap, no substituted default color
    - No `AnyShapeStyle`/type-erasure workaround introduced (conditional application only)
  </acceptance_criteria>
  <done>All 43 sites converted; Optional-tint site inherits exactly as before when nil.</done>
</task>

<task type="auto">
  <name>Task 2: accentColor modifier → tint (27 sites; leave static accent reads)</name>
  <files>13 files (verified grep list, all under AppPackage/Sources/): AppFeature/RootView.swift, AppFeature/View/TabBar/TabBarView.swift, DetailFeature/{Comments/CommentsView,DetailSearch/DetailSearchView,DetailView,Previews/PreviewsView}.swift, DownloadsFeature/DownloadsView.swift, FavoritesFeature/FavoritesView.swift, HomeFeature/{Frontpage/FrontpageView,Watched/WatchedView}.swift, ReadingFeature/ReadingView.swift, SearchFeature/{SearchRootView,SearchView}.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingView.swift (lines ~104-110: already stacks the deprecated modifier AND `.tint` with the same value — the conversion there is DELETING the redundant deprecated line, not swapping it)
    - AppPackage/Sources/AppFeature/RootView.swift (line ~13: root-level accent → `.tint(.primary)`)
  </read_first>
  <action>
    Convert every deprecated `.accentColor(_:)` MODIFIER call to `.tint(_:)` with the same argument (27 sites, 13 files). Two special cases:
    1. ReadingView.swift:104-110 — delete the redundant deprecated line (`.tint` with the identical value is already present).
    2. RootView.swift:13 — becomes `.tint(.primary)`; this drives tab-bar and link tinting app-wide, so it is this task's one spot-check site.
    Do NOT touch the static accent-color READ sites (EhSettingView+Sections1.swift:102, ControlPanel.swift:380, LiveTextView.swift:71,79) — the static property is not deprecated; converting it would be wrong. NOTE: the source spells these implicit-member form (`.accentColor` with no call parens, e.g. inside a ternary or a `.color(...)` argument) — do not grep for a `Color.`-prefixed spelling, it matches nothing. Other non-modifier `.accentColor` value reads elsewhere (e.g. DetailView+HeaderSection.swift:54, ArchivesView.swift:156/216, LinkedText.swift:52) are equally NOT sweep targets — only MODIFIER calls with parens convert.
  </action>
  <verify>
    <automated>grep -rnF ".accentColor(" AppPackage/Sources App ShareExtension --include="*.swift" | wc -l  # must be 0 (static implicit-member .accentColor reads have no call parens and don't match)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnF ".accentColor(" AppPackage/Sources App ShareExtension --include="*.swift"` returns 0 hits
    - `grep -rnE '\.accentColor([^(]|$)' AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections1.swift AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift | wc -l` returns 4 (unchanged: the 3 files' 4 static implicit-member reads survive)
    - ReadingView.swift:104-110 region carries exactly one `.tint` and no duplicate-value modifier
  </acceptance_criteria>
  <done>All 27 modifier sites converted or deleted-as-redundant; static reads untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build, lint, and root-tint spot-check</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Validation Architecture)
  </read_first>
  <action>
    D-10/D-12 gate, strictly sequential:
    1. `xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — zero warnings.
    2. SwiftLint (DerivedData artifactbundle binary) — zero new violations.
    3. Spot-check (UI-SPEC Color table): launch the app via the sim-use skill, confirm tab-bar item tinting and one link/button accent render as before the RootView tint swap (D-11: pure-appearance tier, single sanity check — full diff-review covers the rest).
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -c "warning: " # printed count must be 0 (grep exits 1 on zero matches — gate on the output value, not exit status)</automated>
  </verify>
  <acceptance_criteria>
    - Build exits 0 with zero warnings; SwiftLint zero violations in changed files
    - sim-use screenshot of the tab bar shows unchanged tint appearance (recorded in SUMMARY)
  </acceptance_criteria>
  <done>Clean build + lint (D-10/D-12); root tint visually unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure view-modifier mechanism swap; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Both negative greps at 0; static implicit-member `.accentColor` baseline of 4 lines (3 files) unchanged
- Build + SwiftLint clean (D-10); sequential xcodebuild only (D-12)
- Tab-bar tint spot-check recorded
</verification>

<success_criteria>
- Criterion 7 (color-modifier portion) complete at appearance parity; Optional-tint inherit behavior preserved
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — mechanism swap only (conditional `if let` foregroundStyle application inside `CustomToolbarItem` is a restructure of an existing body, not a new API)

<output>
Create `.planning/phases/10-ui-polish/10-02-SUMMARY.md` when done.
</output>
