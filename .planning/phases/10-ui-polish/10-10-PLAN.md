---
phase: 10-ui-polish
plan: 10
type: execute
wave: 10
depends_on: [10-09]
files_modified:
  - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift
  - AppPackage/Sources/HomeFeature/HomeView+Sections.swift
  - AppPackage/Sources/AppComponents/AlertView.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift
autonomous: true
requirements: [CRIT-05]

must_haves:
  truths:
    - "The 7 fixed-pixel font sites scale with Dynamic Type, appearance-identical at default (.large) size"
    - "Every lineLimit(1) and fixed-height risk site has a recorded per-site verdict: broke-at-AX5 or fine (the D-03 gate's auditable evidence — audit surface, NOT a fix list)"
    - "The whole-app screen inventory (including sheets) has been driven at XXL, AX3, and AX5 with per-screen findings recorded"
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift"
      provides: "DT-scaling font at the former 30pt fixed site"
      contains: "relativeTo"
  key_links:
    - from: "fixed-metric constants tied to text"
      to: "user's chosen type size"
      via: "@ScaledMetric(relativeTo:) or text-style-relative .font forms"
      pattern: "@ScaledMetric|relativeTo"
  prohibitions:
    - statement: "No Dynamic Type max-size caps anywhere — the cap modifier count must stay 0 (D-02, absolute; even compact chrome)"
      status: resolved
      verification: "grep -rn 'dynamicTypeSize' AppPackage/Sources App --include='*.swift' | wc -l returns 0 (baseline 0, verified 2026-07-17)"
    - statement: "No new shrink-to-fit escape hatches — minimumScaleFactor count must not exceed the pre-existing baseline of 8 (a shrink is a cap in disguise, D-02); flag the 8 pre-existing sites to the owner in the SUMMARY, do not add"
      status: resolved
      verification: "grep -rn 'minimumScaleFactor' AppPackage/Sources App ShareExtension --include='*.swift' | wc -l returns <= 8"
    - statement: "No GeometryReader (Phase 5 ban) — count must stay 0"
      status: resolved
      verification: "grep -rn 'GeometryReader' AppPackage/Sources App ShareExtension --include='*.swift' | wc -l returns 0"
---

<objective>
Start the phase's heaviest sub-task (ROADMAP criterion 5, D-01..D-03): remediate the 7 fixed-pixel font sites (which do not scale with Dynamic Type at all — the highest-risk tier), then drive the FULL whole-app Dynamic Type audit at XXL/AX3/AX5 across every user-facing screen including sheets, recording per-site verdicts for the 33 lineLimit(1) + 35 fixed-height risk sites. Per the UI-checker recommendation these inventories are an AUDIT surface, not a fix list — reflow fixes (next plan, 10-11) happen only where breakage is observed. Runs on the settled surface (all mechanical sweeps + previews done).

Purpose: D-01 whole-app audit; produces the auditable verdict table the D-03 gate needs.
Output: 7 font sites scaling; complete per-screen/per-site audit verdict table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
</context>

<!-- planner-discipline-allow: dynamicTypeSize -->
<!-- planner-discipline-allow: minimumScaleFactor -->
<!-- planner-discipline-allow: GeometryReader -->

<tasks>

<task type="auto">
  <name>Task 1: Remediate the 7 fixed-pixel font sites</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift (:268, 30pt), AppPackage/Sources/HomeFeature/HomeView+Sections.swift (:457, 50pt icon), AppPackage/Sources/AppComponents/AlertView.swift (:112, 50pt icon), AppPackage/Sources/DetailFeature/DetailView+Subviews.swift (:62 20pt, :104 12pt rating, :144 24pt), AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift (:210, 10pt semibold)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Code Examples — DT reflow primitives: text-style-relative font forms and the @ScaledMetric shape; the repo has zero @ScaledMetric uses today, this introduces the first)
    - each listed file at its site (line numbers are pre-phase references — re-grep `.font(.system(size:` for current positions)
    - .swiftlint.yml (repo root)
  </read_first>
  <action>
    Convert each fixed `.font(.system(size: N))` to a DT-scaling form that is appearance-identical at default (.large) size (UI-SPEC Typography row: "remediate via text-style-relative sizing or @ScaledMetric(relativeTo:)"):
    - Preferred: the nearest text style whose default point size matches N (with an explicit .weight where the site had one, e.g. the 10pt semibold at DetailView+HeaderSection:210). Check Apple's default text-style sizes; where no style matches N exactly, use `@ScaledMetric(relativeTo: <nearest style>) private var ...= N` and feed it to `.font(.system(size: scaledValue))` — that keeps N at default size and scales beyond it.
    - Icon sites (50pt symbols in HomeView+Sections and AlertView): same rule — a ScaledMetric-driven size keeps default-size parity.
    - Verify at default size nothing moved (the milestone forbids visual change at default size — Pitfall 6); the Task 2 audit then observes their behavior at AX sizes.
    Do not touch any other font in these files.
  </action>
  <verify>
    <automated>grep -rnE '\.font\(\.system\(size: [0-9]' AppPackage/Sources --include="*.swift" | wc -l  # must be 0 — no numeric-literal fixed font sizes remain (ScaledMetric-variable-fed .system(size:) forms don't match and are acceptable)</automated>
  </verify>
  <acceptance_criteria>
    - No `.font(.system(size:` call with a NUMERIC LITERAL remains in AppPackage/Sources (ScaledMetric-variable-fed forms are acceptable; literal fixed points are not)
    - Any @ScaledMetric added carries `relativeTo:` with an explicit text style
    - Build zero warnings; SwiftLint zero new violations (D-10, sequential D-12)
    - sim-use screenshot at default size of Reading overlay, Home section icon, AlertView icon, Detail rows: pixel-equivalent to before (recorded)
  </acceptance_criteria>
  <done>All 7 sites scale with DT; default-size appearance unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-app DT audit at XXL/AX3/AX5 with per-site verdicts</name>
  <files>none modified (audit + evidence)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Criterion 5 — the risk inventory counts and the FULL screen inventory for the D-03 pass: Home + its 5 sheets, Search, Favorites, Downloads, Detail + its 9 sub-screens, Reading + ControlPanel + ReadingSetting, Setting + its 9 sub-screens, Filters, QuickSearch, DateSeek, NewDawn, error surface, toasts)
  </read_first>
  <action>
    Build once (sequential, D-12), then drive the simulator with the sim-use skill:
    1. For each screen in the RESEARCH criterion-5 inventory (INCLUDING every sheet), set Dynamic Type to XXL, then AX3, then AX5 (Settings → Accessibility → Display & Text Size → Larger Text, or the simulator's accessibility override) and capture a screenshot per screen×size where a risk site exists; for obviously-clean static screens one AX5 shot suffices.
    2. Record the verdict table the D-03 gate requires (UI-checker recommendation: audit surface, not fix list): for each of the 33 lineLimit(1) sites and 35 fixed frame-height sites — verdict `broke at AX5` (clipped essential text, overlapping content, unreachable/shrunken control) or `fine` (readable and operable). Grep the current tree for the two patterns first to get live line numbers; map each site to the screen where it renders.
    3. Also record any breakage found OUTSIDE the inventoried sites (the inventories are risk heuristics, not exhaustive).
    4. EhSetting webview screens: audit the native chrome only; WebKit-rendered text scales via its own machinery and is out of remediation scope (RESEARCH Open Question 3 — note it in the table).
    5. Output: the verdict table + screenshot set, organized by screen, into the SUMMARY (this is 10-11's work order and the D-03 gate's evidence baseline).
  </action>
  <verify>
    <automated>grep -rnF "lineLimit(1)" AppPackage/Sources --include="*.swift" | wc -l  # records the live site count that the verdict table must cover (expected ~33)</automated>
  </verify>
  <acceptance_criteria>
    - Verdict table covers every live lineLimit(1) and fixed frame(height:) site with `broke at AX5` or `fine` plus its host screen
    - Every screen in the RESEARCH criterion-5 inventory (incl. sheets) appears in the audit record with at least one AX5 observation
    - The three prohibition greps hold: DT-cap count 0, shrink-to-fit count <= 8 (pre-existing, flagged to owner), GeometryReader count 0
  </acceptance_criteria>
  <done>Complete, auditable whole-app DT audit; fix list for 10-11 derived from observed breakage only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Font-scaling conversions and a simulator audit; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Literal fixed-size font grep = 0; prohibition greps hold (0 / <=8 / 0)
- Verdict table complete across the full screen inventory at XXL/AX3/AX5
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
</verification>

<success_criteria>
- Criterion 5 groundwork done: highest-risk font tier remediated at default-size parity; observed-breakage fix list produced for 10-11 with auditable evidence (D-01/D-02 honored, D-03 evidence baseline ready)
</success_criteria>

## Artifacts this phase produces (this plan)

- `@ScaledMetric(relativeTo:)` properties at the converted font sites (first uses in the repo; names per site, e.g. private scaled size vars in ReadingViewComponents, HomeView+Sections, AlertView, DetailView+Subviews, DetailView+HeaderSection)

<output>
Create `.planning/phases/10-ui-polish/10-10-SUMMARY.md` when done.
</output>
