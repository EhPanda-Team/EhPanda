---
phase: 10-ui-polish
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - AppPackage/Sources/AppComponents/ViewModifiers.swift
  - AppPackage/Sources/AppComponents/CategoryView.swift
  - AppPackage/Sources/AppComponents/TagCloudView.swift
  - AppPackage/Sources/AppComponents/NewDawnView.swift
  - AppPackage/Sources/AppComponents/SettingTextField.swift
  - AppPackage/Sources/AppComponents/Placeholder.swift
  - AppPackage/Sources/SettingFeature/SettingView.swift
  - AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift
  - AppPackage/Sources/SettingFeature/Login/LoginView.swift
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/HomeFeature/GalleryRankingCell.swift
  - AppPackage/Sources/HomeFeature/HomeView+Sections.swift
  - AppPackage/Sources/SearchFeature/GalleryHistoryCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/DetailFeature/DetailView+CommentCells.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift
  - AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
autonomous: true
requirements: [CRIT-07, CRIT-08]

must_haves:
  truths:
    - "No bare deprecated corner-radius modifier calls remain; all clipping goes through clipShape with .rect shorthand"
    - "The custom cornerRadius(_:corners:) modifier and its UIBezierPath-backed RoundedCorner shape are deleted"
    - "Thumbnail bottom-leading radius-15 corner renders identically (D-11 spot-check)"
    - "Autocorrection and status-bar modifiers use their current non-deprecated names"
  artifacts:
    - path: "AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift"
      provides: "Uneven corner clip via native shorthand"
      contains: "clipShape(.rect(bottomLeadingRadius: 15"
    - path: "AppPackage/Sources/AppComponents/CategoryView.swift"
      provides: "Even corner clip, dead corners parameter removed"
      contains: "clipShape(.rect(cornerRadius:"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections1.swift"
      to: "AppPackage/Sources/AppComponents/CategoryView.swift"
      via: "CategoryView init call (corners parameter dropped from signature)"
      pattern: "CategoryView\\("
  prohibitions:
    - statement: "Do NOT add style: .continuous to any .rect clip — the default .circular matches the deprecated behavior; continuous would change appearance (Pitfall 3)"
      status: resolved
      verification: "grep -rnF 'style: .continuous' AppPackage/Sources --include='*.swift' returns no NEW hits versus pre-plan baseline"
---

<objective>
Finish the mechanical deprecated-API sweep (ROADMAP criterion 7 remainder) and remove the custom corner-rounding machinery (criterion 8): 16 bare deprecated corner-radius calls → `.clipShape(.rect(cornerRadius:))`, 6 deprecated autocorrection calls → `.autocorrectionDisabled(_:)`, 1 deprecated status-bar call → `.statusBarHidden(_:)`, then delete `cornerRadius(_:corners:)` + `RoundedCorner` from ViewModifiers.swift and replace its 2 call sites with native `.rect` shorthands. All at appearance parity (D-10/D-11).

Purpose: criteria 7 + 8; kills a UIKit bridge (`UIBezierPath`) that iOS 26 SwiftUI covers natively.
Output: zero deprecated modifier calls in these families; custom shape machinery gone; radius-15 corners visually unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
</context>

<!-- planner-discipline-allow: .cornerRadius( -->
<!-- planner-discipline-allow: disableAutocorrection -->
<!-- planner-discipline-allow: .statusBar(hidden -->
<!-- planner-discipline-allow: RoundedCorner -->

<tasks>

<task type="auto">
  <name>Task 1: Bare corner-radius, autocorrection, and status-bar sweeps</name>
  <files>16 corner sites: SettingView.swift:109, LaboratorySettingView.swift:73, GalleryCardCell.swift:74+90, GalleryRankingCell.swift:21, HomeView+Sections.swift:284+462, GalleryHistoryCell.swift:19, GalleryThumbnailCell.swift:94, CategoryView.swift:91, TagCloudView.swift:125, NewDawnView.swift:167, SettingTextField.swift:45, Placeholder.swift:21, DetailView+CommentCells.swift:40, DetailView+Subviews.swift:207; 6 autocorrection sites: AccountSettingView.swift:170, EhSettingView+Sections3.swift:195, LoginView.swift:139, SettingTextField.swift:45, FolderManagerView.swift:107, QuickSearchView.swift:155; 1 status-bar site: ReadingView.swift:120</files>
  <read_first>
    - .swiftlint.yml (repo root — `shape_initializer_argument` is error: NEVER pass RoundedRectangle/UnevenRoundedRectangle as an argument; the `.rect(...)` shorthand is mandatory)
    - AppPackage/Sources/AppComponents/Placeholder.swift (its `.cornerRadius(cornerRadius)` at :21 uses a stored property named cornerRadius — the property stays, only the modifier call converts)
    - AppPackage/Sources/AppComponents/NewDawnView.swift (:167 uses a computed radius `width / 3` — carries over verbatim as the clipShape argument)
  </read_first>
  <action>
    Three mechanical 1:1 swaps per the verified inventories in 10-RESEARCH.md criterion 7 (line numbers are pre-phase references — re-grep before editing since waves 1-2 may have shifted lines):
    1. Each bare deprecated corner-radius call becomes `.clipShape(.rect(cornerRadius: N))` with the same N (literal, property, or `width / 3`). The `.rect` default corner style is `.circular`, which matches the deprecated modifier — do NOT add a style argument (Pitfall 3). Where the call follows `.background(color)` (e.g. GalleryThumbnailCell.swift:94) the clip applies to the composite exactly as before — keep chain order.
    2. Each deprecated autocorrection call becomes `.autocorrectionDisabled(_:)` with the same Bool (or default).
    3. ReadingView.swift:120 status-bar call becomes `.statusBarHidden(_:)` with the same argument (A1/RESEARCH: confirm it compiles on the iOS 26 SDK; if the rename differs, use the SDK's current non-deprecated equivalent from Xcode docs).
    While in the files, note (do not fix beyond scope) any other doc-deprecated modifiers the compiler or SDK docs flag (A5, e.g. navigationBarTitleDisplayMode) — convert only if doc-deprecated on the iOS 26 SDK; record verdicts in the SUMMARY.
  </action>
  <verify>
    <automated>grep -rnF ".cornerRadius(" AppPackage/Sources App ShareExtension --include="*.swift" | wc -l  # must be 0; then grep -rnF "disableAutocorrection" (0) and grep -rnF ".statusBar(hidden" (0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnF ".cornerRadius(" AppPackage/Sources App ShareExtension --include="*.swift"` returns 0 hits
    - `grep -rnF "disableAutocorrection" AppPackage/Sources App ShareExtension --include="*.swift"` returns 0 hits
    - `grep -rnF ".statusBar(hidden" AppPackage/Sources --include="*.swift"` returns 0 hits
    - No `RoundedRectangle(`/`UnevenRoundedRectangle(` passed as arguments anywhere new (`shape_initializer_argument` lint stays clean)
    - Every replacement keeps the identical radius expression and chain position
  </acceptance_criteria>
  <done>All 23 sweep sites converted to current APIs at parity; per-site verdicts for any additional doc-deprecated candidates recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Delete cornerRadius(_:corners:) + RoundedCorner; convert 2 call sites</name>
  <files>AppPackage/Sources/AppComponents/ViewModifiers.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/AppComponents/CategoryView.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/ViewModifiers.swift (modifier at ~:27-29, `RoundedCorner` UIBezierPath shape at ~:176-196 — both to delete)
    - AppPackage/Sources/AppComponents/CategoryView.swift (:33 — parameterized corners; BOTH callers, EhSettingView+Sections1.swift:222 and FiltersView.swift:87, use the default .allCorners, so the corners init parameter is dead flexibility)
    - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift (:50 — bottomLeft radius 15)
  </read_first>
  <action>
    Criterion 8, per the verified mapping in 10-RESEARCH.md:
    1. GalleryThumbnailCell.swift:50 — replace the custom corners call with `.clipShape(.rect(bottomLeadingRadius: 15))`. Leading==left in all shipped locales (en/de/ja/ko/zh-Hans/zh-Hant, no RTL — verified), so bottomLeft→bottomLeading is appearance-identical.
    2. CategoryView.swift:33 — drop the dead `corners` init parameter (and any stored property backing it) from CategoryView entirely; the clip becomes `.clipShape(.rect(cornerRadius: cornerRadius))`. Both callers pass no corners argument today, so their call sites need no edits — verify by grepping `CategoryView(` callers after the change.
    3. Delete the custom modifier (ViewModifiers.swift:27-29) and the `RoundedCorner` shape (:176-196) wholesale. If the file's remaining content leaves an unused `import UIKit`-only dependency, remove the now-unused import.
    A2 note: the uneven-corner shorthand's default style needs on-device confirmation — pin `style: .circular` explicitly ONLY if the Task 3 spot-check shows drift.
  </action>
  <verify>
    <automated>grep -rnF "RoundedCorner" AppPackage/Sources --include="*.swift" | wc -l  # must be 0; grep -rnF "corners:" AppPackage/Sources/AppComponents/CategoryView.swift | wc -l # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnF "RoundedCorner" AppPackage/Sources --include="*.swift"` returns 0 hits
    - GalleryThumbnailCell.swift contains `clipShape(.rect(bottomLeadingRadius: 15`
    - CategoryView.swift contains `clipShape(.rect(cornerRadius:` and has no corners parameter
    - `grep -rnF "UIBezierPath" AppPackage/Sources/AppComponents/ViewModifiers.swift` returns 0 hits
  </acceptance_criteria>
  <done>Custom corner machinery fully deleted; both call sites on native shorthands.</done>
</task>

<task type="auto">
  <name>Task 3: Build, lint, and D-11 corner spot-checks</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-UI-SPEC.md (§Conversion Parity Contracts — spot-check tiers)
  </read_first>
  <action>
    D-10/D-12 gate, strictly sequential:
    1. `xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — zero warnings; SwiftLint (artifactbundle binary) zero new violations.
    2. D-11 sim-use spot-checks (radius 15 makes circular-vs-continuous drift visible — A2/Pitfall 3): (a) a gallery-list thumbnail's bottom-leading corner (GalleryThumbnailCell); (b) one card corner site (GalleryCardCell radius 15); (c) the CategoryView chip corners in Filters. Compare against pre-change appearance (screenshots from the working tree at the wave-2 commit if needed via git stash/checkout, or visual judgment at radius scale). If corner-shape drift appears on the uneven clip, pin `style: .circular` explicitly and re-check.
    3. Record screenshots + verdicts in the SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -c "warning: " # printed count must be 0 (grep exits 1 on zero matches — gate on the output value, not exit status)</automated>
  </verify>
  <acceptance_criteria>
    - Build exits 0 with zero warnings; SwiftLint zero new violations
    - sim-use screenshots of thumbnail corner, card corner, and category chips recorded; no visible corner-shape drift (or drift resolved by explicit .circular pin, re-verified)
  </acceptance_criteria>
  <done>Clean build/lint; radius-15 corner parity visually confirmed per D-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | View-modifier swaps and dead-code deletion; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Negative greps at 0 for all four swept families + RoundedCorner
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
- D-11 spot-check screenshots recorded for radius-15 sites
</verification>

<success_criteria>
- Criterion 7 fully swept (with criterion-7 color portion done in 10-02); criterion 8 fully satisfied (modifier + shape deleted, call sites native)
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — deletions (`RoundedCorner`, custom corners modifier, CategoryView `corners` parameter) plus native-API call sites

<output>
Create `.planning/phases/10-ui-polish/10-03-SUMMARY.md` when done.
</output>
