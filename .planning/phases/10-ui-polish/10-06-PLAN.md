---
phase: 10-ui-polish
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - "Up to 29 files hosting the 35 verified ZStack occurrences (full inventory in 10-RESEARCH.md §POLISH-02), notably: AppPackage/Sources/AppComponents/{NewDawnView,Placeholder,CategoryView,AlertView}.swift, ReadingFeature/{ReadingView,ReadingViewComponents,Support/LiveTextView}.swift, DetailFeature/{DetailView+HeaderSection,DetailView+CommentCells,DetailView,Torrents/TorrentsView,Comments/CommentsView,FolderManager/FolderManagerView,Archives/ArchivesView}.swift, SettingFeature/{AboutView,EhSettingView,GeneralSettingView,LoginView,AppActivityLogsView}.swift, HomeFeature/{GalleryCardCell,HomeView,Watched/WatchedView}.swift, SearchFeature/SearchRootView.swift, FavoritesFeature/FavoritesView.swift, GalleryListComponents/GalleryList.swift, DownloadsFeature/{DownloadsView,DownloadsView+Subviews}.swift, QuickSearchFeature/QuickSearchView.swift, SystemNotification/View+Toast.swift"
autonomous: true
requirements: [POLISH-02]

must_haves:
  truths:
    - "Every ZStack expressing an overlay/background relationship is converted to .overlay/.background sized to the primary content, at layout/appearance parity"
    - "Genuine union-sized multi-child stacks remain ZStack — keeping them is a correct outcome, recorded per site"
    - "All 35 sites carry a recorded convert/keep verdict with the size-defining-child rationale"
    - "Converted screens show no before/after visual drift (D-11 spot-checks)"
  artifacts:
    - path: "AppPackage/Sources/AppComponents/Placeholder.swift"
      provides: "Canonical conversion: Color stays the size-definer with a ProgressView overlay"
      contains: ".overlay"
  key_links:
    - from: "converted view files"
      to: "primary-content sizing"
      via: ".overlay/.background sized to the modified view (not union-sized)"
      pattern: "\\.(overlay|background)"
  prohibitions:
    - statement: "No blanket regex conversion — a conversion that changes which child defines the composite size is forbidden; keep the ZStack instead (criterion 4 permits it)"
      status: resolved
      verification: "SUMMARY verdict table shows per-site classification; D-11 before/after screenshots show no composite-size drift"
---

<objective>
Convert `ZStack`s that express an overlay/background relationship to `.overlay`/`.background` (POLISH-02, ROADMAP criterion 4): 35 verified occurrences across 29 files, classified per site by the size-defining-child rule. The core hazard (Pitfall 1): ZStack sizes to the UNION of children; overlay/background size to the MODIFIED view — a wrong conversion silently shrinks composites. Runs after the inSheet swap (10-04) so the Placeholder conversion works on the trait-color version.

Purpose: POLISH-02 at layout/appearance parity, D-11 verified.
Output: converted sites + a complete convert/keep verdict table + before/after screenshot evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify all 35 sites and capture before-screenshots</name>
  <files>none modified (classification + evidence capture)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§POLISH-02 — the full 29-file inventory and the classification rule)
    - AppPackage/Sources/AppComponents/Placeholder.swift (lines ~15-19 — the canonical convertible shape: a Color fill decorated by a ProgressView)
    - AppPackage/Sources/GalleryListComponents/GalleryList.swift (line ~43 — the canonical KEEP: content and loading/error surface both contribute to size)
  </read_first>
  <action>
    1. Grep the current tree for ZStack occurrences (line numbers may have shifted since research; the count should still be ~35 across ~29 files — reconcile any drift and note it).
    2. Classify EVERY site by the rule: identify the size-defining child. Exactly one size-definer + decorators → CONVERT (decorator behind → background, decorator in front → overlay). Size genuinely the union of 2+ independently-sized children → KEEP (explicitly permitted). Ambiguous → decide which child is primary; if neither is, KEEP. Record the verdict table: file:line, verdict, size-defining child, rationale.
    3. Build the current tree once (sequential, D-12) and, via the sim-use skill, capture BEFORE screenshots of each screen hosting a to-be-converted site (one screenshot per distinct screen is enough — name them by screen for the Task 3 comparison).
  </action>
  <verify>
    <automated>grep -rn "ZStack" AppPackage/Sources --include="*.swift" | wc -l  # baseline count recorded in SUMMARY (expected ~35)</automated>
  </verify>
  <acceptance_criteria>
    - Verdict table in progress notes/SUMMARY lists every ZStack occurrence with CONVERT or KEEP + size-defining-child rationale
    - Before-screenshots exist for every screen hosting a CONVERT site
  </acceptance_criteria>
  <done>Complete classification with evidence baseline; zero code changes yet.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the CONVERT-verdict sites</name>
  <files>the CONVERT subset of the 29 inventory files</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-PATTERNS.md (§POLISH-02 — the exact before/after shape for Placeholder)
    - .swiftlint.yml (repo root — `single_line_trailing_closure` ratchet rule: overlay/background closures must not collapse to one line)
  </read_first>
  <action>
    For each CONVERT site: the size-defining child becomes the modified view; each decorator behind it moves into `.background { }`, each decorator in front into `.overlay { }`. Preserve: modifier chain order relative to the old ZStack's trailing modifiers (e.g. Placeholder's aspectRatio and clip stay attached to the composite), alignment arguments (ZStack alignment maps to the overlay/background alignment parameter), spacing, and all existing child modifiers verbatim. Canonical example: Placeholder.swift — Color keeps its trait-based fill from 10-04 and gains an overlay holding the ProgressView.
    Do not touch KEEP sites. If a conversion turns out ambiguous mid-edit (size-definer unclear), flip its verdict to KEEP and note it — keeping is always safe.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -ci "warning" # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Every CONVERT-verdict site uses .overlay/.background with the size-definer as the modified view; alignment/modifier order preserved
    - Remaining ZStack count equals the KEEP-verdict count from Task 1
    - Build zero warnings; SwiftLint zero new violations (D-10)
  </acceptance_criteria>
  <done>All CONVERT sites transformed; KEEP sites untouched; clean build.</done>
</task>

<task type="auto">
  <name>Task 3: D-11 before/after comparison; revert any drift</name>
  <files>possible reverts within the CONVERT subset</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Common Pitfalls 1 — warning signs: hit-target shrinkage, clipped ProgressView, changed cell heights in lists)
  </read_first>
  <action>
    1. Via sim-use, capture AFTER screenshots of the same screens captured in Task 1 and compare pairwise (executor-signed per UI-SPEC: D-11 spot-checks are executor tier; only the inSheet delta and D-03 are owner-signed).
    2. Watch specifically for: composite-size shrinkage, clipped decorators, changed list cell heights, moved hit targets.
    3. Any site showing drift → revert that site to its original ZStack (keeping is explicitly permitted by criterion 4) and update its verdict to KEEP-after-drift. Re-build (sequential) and re-screenshot after any revert.
    4. Final SUMMARY carries the verdict table + screenshot evidence per converted screen.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -ci "warning" # must be 0 (re-run only if reverts occurred)</automated>
  </verify>
  <acceptance_criteria>
    - Pairwise before/after screenshots recorded for every converted screen; no unresolved visual drift
    - Any drift-reverted site documented with its updated verdict
    - Final build + SwiftLint clean (D-10/D-12)
  </acceptance_criteria>
  <done>POLISH-02 delivered at parity with complete auditable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Layout-mechanism swap; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Verdict table covers all ~35 sites; remaining ZStack count == KEEP count
- Before/after screenshot pairs for every converted screen, no drift
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
</verification>

<success_criteria>
- POLISH-02 / criterion 4 satisfied: overlay-relationship stacks converted and sized to primary content; union-sized stacks preserved; parity proven
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — layout-mechanism conversions within existing view bodies

<output>
Create `.planning/phases/10-ui-polish/10-06-SUMMARY.md` when done.
</output>
