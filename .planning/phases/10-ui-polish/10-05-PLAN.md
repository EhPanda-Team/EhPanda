---
phase: 10-ui-polish
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - AppPackage/Sources/AppComponents/ToolbarItems.swift
  - "AppPackage/Sources/** (audit-driven: any additional text+image button sites converted where fitting)"
autonomous: true
requirements: [CRIT-09, CRIT-10]

must_haves:
  truths:
    - "The three ToolbarItems buttons (Filters, QuickSearch, JumpPage) use Label with the systemSymbol shorthand; toolbar appearance is unchanged (no title suddenly appears beside an icon)"
    - "Every text+image button in the codebase has a recorded convert/leave verdict; toolbar text-only/image-only buttons likewise"
    - "The empty-string-literal audit is executed and its result documented (expected: zero matches)"
  artifacts:
    - path: "AppPackage/Sources/AppComponents/ToolbarItems.swift"
      provides: "Label-based toolbar buttons with conditional icon-only style"
      contains: "Label("
  key_links:
    - from: "AppPackage/Sources/AppComponents/ToolbarItems.swift"
      to: "SFSafeSymbols"
      via: "Label(_:systemSymbol:) shorthand init (lint-mandated)"
      pattern: "systemSymbol:"
  prohibitions:
    - statement: "Never pass systemImage:/systemName: parameters (lint-banned) and never use the Label closure form with separate Text/Image (lint-banned shorthand rule)"
      status: resolved
      verification: "SwiftLint run reports zero system_name_image_parameter / label_text_image_shorthand violations"
    - statement: "Never hide a label by passing an empty string literal — hide via conditional application or .labelStyle(.iconOnly) only"
      status: resolved
      verification: "Task 2 audit greps return 0 empty-string-literal view arguments; accessibility_empty_string lint stays clean"
---

<objective>
Convert text+image button labels to `Label(_:systemSymbol:)` where fitting (ROADMAP criterion 10 — all buttons; toolbar buttons additionally audited for text-only/image-only), and run the empty-string-literal audit (criterion 9 — research found zero current matches; the deliverable is the documented audit result). Appearance/layout parity throughout (D-10/D-11).

Purpose: criteria 9 + 10; Label gives correct toolbar/menu/accessibility adaptivity for free.
Output: ToolbarItems buttons on Label, per-site verdict table for the wider audit, documented empty-string audit result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-PATTERNS.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert ToolbarItems buttons and audit all text+image buttons</name>
  <files>AppPackage/Sources/AppComponents/ToolbarItems.swift (FiltersButton :63-70, QuickSearchButton :82-89, JumpPageButton :103-111), plus any audit-found convertible sites across AppPackage/Sources</files>
  <read_first>
    - .swiftlint.yml (repo root — `system_name_image_parameter` BANS systemImage:/systemName: → use `Label(_:systemSymbol:)` per SFSafeSymbols; `label_text_image_shorthand` BANS the Label closure form)
    - AppPackage/Sources/AppComponents/ToolbarItems.swift (the three target buttons all share the shape: an Image(systemSymbol:) plus a Text gated behind a hideText flag; also confirm whether any of them anchors a confirmationDialog/alert — the repo CLAUDE.md placement rule requires such modifiers to stay on the stable triggering control through the conversion)
    - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift (line ~68 — the house-style Label exemplar to copy)
  </read_first>
  <action>
    Criterion 10 conversion per 10-PATTERNS.md §Label conversion (D-11 layout-affecting tier):
    1. FiltersButton, QuickSearchButton, JumpPageButton: replace each button's Image-plus-conditional-Text content with `Label(<existing localized title>, systemSymbol: <existing symbol>)`; when hideText is true apply `.labelStyle(.iconOnly)` via an if/else branch (there is no AnyLabelStyle — two branches, same Label, differing only in labelStyle). Titles reuse the existing localized strings (e.g. `.RLocalizable.filters`) — zero wording changes, zero new .xcstrings keys.
    2. Codebase-wide audit (criterion 10 "all buttons"): grep Button label closures combining `Image(systemSymbol:)` and `Text(` — convert to Label where fitting at parity; record a per-site verdict (converted / left: reason) for every candidate.
    3. Menu-row Text+trailing-checkmark buttons (FavoritesIndexMenu :149-153, ToplistsTypeMenu :177-181, SortOrderMenu :204-209): LEAVE AS-IS — a Label would move the icon to the leading slot while Menu renders the trailing checkmark natively; "where fitting" = no (decision per RESEARCH Open Question 2 / UI-SPEC). Record this verdict.
    4. Toolbar text-only/image-only audit (criterion 10's toolbar clause): enumerate toolbar buttons with text-only or image-only labels (~25 image-only Button-with-symbol sites exist; only TOOLBAR ones are in scope — non-toolbar image-only buttons are explicitly out of scope). Convert where fitting (a Label title feeds accessibility/Voice Control even when rendered icon-only); where a site already carries a Phase 7 accessibilityLabel, converting must not regress it — parity is the veto. Record per-site verdicts.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -ci "warning" # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - ToolbarItems.swift contains three `Label(` usages with `systemSymbol:` and `.labelStyle(.iconOnly)` in the hideText branches; no bare Image+Text composition remains in those buttons
    - `grep -rnF "systemImage:" AppPackage/Sources --include="*.swift"` returns 0 hits (lint rule proof)
    - SUMMARY contains a verdict table covering: every text+image button candidate, the three menu-row checkmark buttons (left as-is), and every toolbar text-only/image-only button
    - Any confirmationDialog/alert previously anchored to a converted button remains attached to that same control
  </acceptance_criteria>
  <done>Three ToolbarItems buttons on Label; complete audit verdicts recorded; zero lint-banned parameter forms.</done>
</task>

<task type="auto">
  <name>Task 2: Empty-string audit, build/lint gate, toolbar spot-check</name>
  <files>none expected (audit; edits only if a match is found)</files>
  <read_first>
    - .planning/phases/10-ui-polish/10-RESEARCH.md (§Criterion 9 — the grep patterns and the expected-zero result)
  </read_first>
  <action>
    1. Criterion 9 audit: run the greps over AppPackage/Sources, App, ShareExtension — `Text("")`, `Label(""`, `Button("")`, `TextField("")`, and the general regex forms for any capitalized initializer taking an empty string literal. Expected result: zero matches (verified at research time; the `accessibility_empty_string` lint rule already guards a11y modifiers). Document the actual result in the SUMMARY. If a site appears (e.g. introduced by Task 1), apply the criterion rule: meaningful localized string, or hide via conditional application / `.labelStyle(.iconOnly)` — never an empty literal. Any new .xcstrings key must follow repo rules (named `%#@variable@` numeric substitutions; `shouldTranslate: false` keys carry every locale).
    2. SwiftLint (artifactbundle binary): zero new violations (D-10).
    3. D-11 toolbar spot-check (A4): via sim-use, open the screens hosting the three converted toolbar buttons (Home/Search toolbars) and confirm NO title text suddenly appears beside the icons — toolbar placement should render Label icon-only where it rendered icon-only before. If a title appears, add `.labelStyle(.iconOnly)` explicitly at that site and re-check. Screenshot evidence into the SUMMARY.
  </action>
  <verify>
    <automated>grep -rnE '[A-Z][A-Za-z]*\("",|[A-Z][A-Za-z]*\(""\)' AppPackage/Sources App ShareExtension --include="*.swift" | wc -l  # must be 0</automated>
  </verify>
  <acceptance_criteria>
    - Empty-string-literal greps return 0 hits; audit result (patterns run + counts) documented in SUMMARY
    - SwiftLint zero new violations; build zero warnings
    - sim-use screenshots show converted toolbar buttons rendering identically (icon-only where previously icon-only, text+icon where previously both)
  </acceptance_criteria>
  <done>Criterion 9 audit documented (expected zero); toolbar parity confirmed; clean build/lint (D-10/D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Button-label mechanism swap and a static audit; no input, network, storage, or auth surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | package installs | low | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Build + SwiftLint clean (D-10); sequential xcodebuild (D-12)
- Verdict table complete (buttons, menu rows, toolbar text/image-only)
- Empty-string audit documented; toolbar spot-check screenshots recorded
</verification>

<success_criteria>
- Criterion 10 satisfied with auditable per-site verdicts; criterion 9 satisfied as a documented audit result
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — Label-based bodies inside existing `FiltersButton`, `QuickSearchButton`, `JumpPageButton` components

<output>
Create `.planning/phases/10-ui-polish/10-05-SUMMARY.md` when done.
</output>
