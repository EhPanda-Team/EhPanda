---
phase: 16-dynamic-type-accessibility
plan: 22
type: execute
wave: 20
depends_on: ["16-21"]
files_modified:
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift
  - .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "No information is conveyed by colour alone: the activity-log level dot becomes a per-level SF Symbol — `OSLogEntryLog.Level.symbol` declared as a `private extension` in `SettingFeature` (`AppActivityLogsView.swift`), beside the view that renders it, because `AppModels` does not depend on `SFSafeSymbols` and the Architectural Responsibility Map assigns accessibility semantics to the view layer — at the same `.caption2` size so row height is unchanged, with the level title as its accessibility label; the Laboratory feature cell gains a size-neutral on/off glyph if the audit judged its luminance-only cue `weak`; every other DWC row the audit marked `pass` stays as is (criterion 11, D-20)."
    - "`AppPackage/Package.swift` and `AppModels` are untouched: the symbol mapping lives where `SFSafeSymbols` is already imported, so no new module dependency is introduced (D-20)."
    - "Every visible glyph added by this plan (log-level symbols, Laboratory glyph) is appended to `16-CONTRAST-AUDIT.md § D-25 re-sweep candidates` with its screen # (D-24, D-25)."
    - "Each added glyph is re-measured at 3:1 non-text contrast in light, dark and Increase Contrast and checked in a grayscale-desaturated screenshot; the numbers are appended to the audit; screenshots stay under the evidence root `$HOME/Library/Caches/ehpanda-phase16/`, never the repo (D-28, D-32, D-33)."
    - "Every deliberate choice (which symbol per level, why the mapping is view-side, why the Laboratory glyph is hidden from VoiceOver) carries a doc comment; no lint suppression (D-19)."
    - "Precision: re-measured ratios are compared `>=` the threshold with no rounding up; a glyph that measures 2.99 is still a failure."
  artifacts:
    - path: "AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift"
      provides: "private extension OSLogEntryLog.Level { var symbol: SFSymbol } (distinct per level) and its use in AppActivityLogRow"
      contains: "var symbol"
    - path: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      provides: "### 16-22 result (DWC): per-glyph 3:1 numbers, grayscale note, D-25 candidates updated"
      contains: "16-22 result"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift (AppActivityLogRow)"
      to: "AppPackage/Sources/AppModels/Support/AppActivityLog.swift (OSLogEntryLog.Level.color / .title)"
      via: "`Image(systemSymbol: log.level.symbol)` + `.foregroundStyle(log.level.color)` + `.accessibilityLabel(log.level.title)` — symbol (view-side), colour and title (model-side) are three views of one enum"
      pattern: "level.symbol"
  prohibitions:
    - statement: "Never add `SFSafeSymbols` (or any dependency) to `AppModels`, and never edit `AppPackage/Package.swift` in this plan; the mapping is view-side (D-20)."
      status: resolved
      verification: test
    - statement: "Never move a category background colour; the standard-44 pin passes after this plan (D-26)."
      status: resolved
      verification: test
    - statement: "Never fix a colour-only state by adding label text VoiceOver reads but sighted users cannot see; the non-colour carrier must be visible (shape, glyph or text) (criterion 11)."
      status: resolved
      verification: judgment
---

<objective>
Give every colour-only state a visible non-colour carrier, as the audit-first pass (plan 16-13) measured: a per-level SF Symbol for the activity-log level dot, and a Laboratory on/off glyph if the audit judged its luminance-only cue weak — then re-measure the added glyphs and track the D-25 re-sweep candidates. The D-28 colour fixes and the rating-star decision are plan 16-23; the two were split so that the glyph work (two files, layout-sensitive) and the colour work (nine files, layout-neutral) are each verified on their own.

Purpose: criterion 11; Differentiate Without Color becomes claimable on evidence, not assumption.
Output: `AppActivityLogsView.swift` and (conditionally) `LaboratorySettingView.swift` edited; `16-CONTRAST-AUDIT.md § 16-22 result (DWC)` with the glyph measurements and the updated D-25 list.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating (plans 16-20, 16-21); `OSLogEntryLog.Level.symbol` in `SettingFeature`, log-level glyphs, optional Laboratory glyph (this plan); D-28 colour fixes and the optional `RatingStar` colorset (plan 16-23).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `ReduceMotionGatingSourceTests`, `AccessibilityAuditUITests` (plan 16-24).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings` (none added by this plan).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Differentiate Without Color — log-level symbols (view-side mapping), Laboratory glyph if weak</name>
  <files>AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift, AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Differentiate Without Color — the per-row verdicts (log level `fail`, Laboratory `weak`/`pass`, the grayscale numbers) and § Decisions.
    - `AppPackage/Sources/AppModels/Support/AppActivityLog.swift` lines 72–95 — `OSLogEntryLog.Level.color` and `.title` (the `@unknown default` handling to mirror). Read only; do not edit.
    - `AppPackage/Package.swift` — the `appModels` target depends on `appTools`, `resources`, `osLogExt`, `casePaths`, `sharing` and NOT on `sfSafeSymbols`; `SettingFeature` already imports `SFSafeSymbols` (see the `import` block of `AppActivityLogsView.swift`). This is why the symbol mapping is view-side.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Architectural Responsibility Map — VoiceOver / Voice Control semantics belong to the SwiftUI view layer.
    - `AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift` lines 205–230 — `AppActivityLogRow` (`Image(systemSymbol: .circleFill).foregroundStyle(log.level.color).font(.caption2)` + the label added in plan 16-17).
    - `AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift` lines 45–80 — the feature cell (tint vs `.secondary`, tinted vs gray background; the toggle representation from plan 16-17).
    - `$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md` § Differentiate Without Color.
    - `.swiftlint.yml` — the `nesting` rule (one level) and the custom rules, so the extension is a top-level `private extension`, not nested in the view.
  </read_first>
  <action>
In `AppActivityLogsView.swift` add a top-level `private extension OSLogEntryLog.Level { var symbol: SFSymbol { … } }` next to the row that uses it, one distinct shape per level so no two levels differ by colour alone — debug `.ant`, info `.infoCircleFill`, notice `.bellFill`, error `.exclamationmarkTriangleFill`, fault `.xmarkOctagonFill`, undefined and `@unknown default` `.questionmarkCircleFill` (confirm each symbol exists in the vendored SFSafeSymbols set; substitute the nearest if one does not, and record it). Doc-comment two things: that shape is the non-colour carrier for criterion 11, and why the mapping is here rather than beside `color` / `title` in `AppModels` — that module does not depend on `SFSafeSymbols`, and a glyph choice is presentation, not model data (do not add the dependency; `AppPackage/Package.swift` is not in this plan's files). `AppActivityLogRow`: `Image(systemSymbol: log.level.symbol)` keeping `.foregroundStyle(log.level.color)` and `.font(.caption2)` (same glyph size → row height unchanged; verify by comparing a `content_size large` screenshot before/after under `$EVIDENCE_ROOT/round2/dwc/`); the `.accessibilityLabel(log.level.title)` from plan 16-17 stays. Laboratory: only if the audit verdict is `weak`, add a leading `Image(systemSymbol: isOn ? .checkmarkCircleFill : .circle)` at the cell's text size, hidden from accessibility (`.accessibilityHidden(true)` — the toggle representation already carries the state; doc-comment that), keeping the cell height; if `pass`, change nothing and record. Build; commit `feat(16-22): non-colour carriers for log levels`.
  </action>
  <verify>
    <automated>grep -c "var symbol" AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift; grep -c "log.level.symbol" AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift; grep -c "var symbol" AppPackage/Sources/AppModels/Support/AppActivityLog.swift; awk '/module: .appModels,/,/\]/' AppPackage/Package.swift | grep -c sfSafeSymbols; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var symbol" …/SettingFeature/AppActivityLogs/AppActivityLogsView.swift` prints `1` inside a top-level `private extension OSLogEntryLog.Level` with six distinct `case` symbols plus `@unknown default`; `grep -c "log.level.symbol" …/AppActivityLogsView.swift` prints `1`; `grep -c "systemSymbol: .circleFill" …/AppActivityLogsView.swift` prints `0`.
    - `grep -c "var symbol" …/AppModels/Support/AppActivityLog.swift` prints `0`; `awk '/module: .appModels,/,/\]/' AppPackage/Package.swift | grep -c sfSafeSymbols` prints `0` and `git diff --quiet <plan-start>..HEAD -- AppPackage/Package.swift AppPackage/Sources/AppModels` succeeds (neither the manifest nor `AppModels` changed).
    - A grayscale-desaturated screenshot of the Activity Logs list under `$HOME/Library/Caches/ehpanda-phase16/round2/dwc/` shows the levels distinguishable by shape; a `content_size large` before/after pair shows unchanged row height; both recorded in the summary by evidence-root path.
    - Laboratory: either the glyph is present with `.accessibilityHidden(true)` and a doc comment, or the summary records `pass — unchanged`.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Every colour-coded state carries a visible shape, glyph or text, with the glyph mapping in the view layer.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure the added glyphs, record, and update the D-25 list</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Non-category colours (the measurement procedure) and § Decisions › D-25 re-sweep candidates.
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` § Infrastructure (UDIDs, `BUNDLE_ID`, `EVIDENCE_ROOT`, baselines) and § Protocol › Install-over.
  </read_first>
  <action>
Install-over on `IPHONE_UDID` per the `16-SWEEP.md § Protocol › Install-over` rule (build by UDID, `plutil -extract CFBundleIdentifier raw` equals `BUNDLE_ID`, `xcrun simctl install`; never uninstall / erase / clear-app-state). Repeat the plan 16-13 pixel measurement for the six log-level symbols (and the Laboratory glyph if added) at the 3:1 non-text threshold across `appearance light|dark` × `increase_contrast disabled|enabled` (`xcrun simctl ui <IPHONE_UDID> …`, baselines read first), screenshots to `$EVIDENCE_ROOT/round2/dwc/`, then restore both settings and read them back. Append `### 16-22 result (DWC)` to the audit: a table `glyph | level/state | L | D | L+IC | D+IC | threshold 3:1 | verdict`, every ratio ≥ 3:1 or the glyph colour raised (the level colours are `AppModels` values — if one fails, record it as a D-28 row for plan 16-23 rather than editing `AppModels` here); the grayscale note for the Activity Logs list; and update `D-25 re-sweep candidates` — add `#32 Activity Logs` (new glyph shapes), `#36 Laboratory` only if the glyph was added. Send the before/after screenshot paths to the owner in chat (D-33). Commit `docs(16): record DWC remediation measurements` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "### 16-22 result (DWC)" "$f"; grep -c "#32 Activity Logs" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `### 16-22 result (DWC)` exists with four ratios per glyph, each ≥ 3:1, or a named D-28 row handed to plan 16-23.
    - `D-25 re-sweep candidates` lists `#32 Activity Logs` (and `#36 Laboratory` iff the glyph was added).
    - `xcrun simctl ui <IPHONE_UDID> appearance` and `… increase_contrast` print the recorded baselines.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): record DWC remediation measurements` exists; `grep -c "/Users/" 16-CONTRAST-AUDIT.md` prints `0`.
  </acceptance_criteria>
  <done>Criterion 11 is backed by after-measurements; the D-25 list carries the glyph screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View-side mapping → module graph | The symbol mapping stays in `SettingFeature`; `AppModels` gains no dependency and the manifest is untouched. |
| Measurement screenshots → evidence root | Evidence root (`$HOME/Library/Caches/ehpanda-phase16/`) only; paths in chat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | category backgrounds | medium | mitigate | No colorset touched; invariant test green. |
| T-16-14 | Repudiation | a claim resting on an unmeasured glyph | medium | mitigate | After-measurements table with four ratios per glyph. |
| T-16-21 | Tampering (accidental) | a new module dependency smuggled into `AppModels` | low | mitigate | Acceptance proves `Package.swift` and `AppModels` unchanged. |
| T-16-01 | Information disclosure | measurement screenshots | high | mitigate | Evidence-root only; image check before the docs commit. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | § Protocol install-over rule (bundle id checked); settings restored. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps; `AppModels` / `Package.swift` untouched; lint build green.
2. Glyph measurements ≥ 3:1; D-25 candidates updated; settings restored; two commits.
</verification>

<success_criteria>
- Criterion 11 met (no colour-only state) with the glyph mapping in the view layer, every added glyph measured after the fact, and the D-25 list complete (D-20, D-24, D-25).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-22-SUMMARY.md` when done: symbols per level, Laboratory outcome, the after-measurement table, D-25 updates, commit hashes.
</output>
