---
phase: 16-dynamic-type-accessibility
plan: 22
type: execute
wave: 20
depends_on: ["16-21"]
files_modified:
  - AppPackage/Sources/AppModels/Support/AppActivityLog.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/SystemNotification/ToastMessageView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/AppComponents/RatingView.swift
  - AppPackage/Sources/AppComponents/Resources/Colors.xcassets
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "No information is conveyed by colour alone: the activity-log level dot becomes a per-level SF Symbol (`OSLogEntryLog.Level.symbol` beside the existing `color` and `title` in `AppModels`) at the same `.caption2` size so row height is unchanged, with the level title as its accessibility label; the Laboratory feature cell gains a size-neutral on/off glyph if the audit judged its luminance-only cue `weak`; every other DWC row the audit marked `pass` stays as is (criterion 11, D-20)."
    - "Every non-category colour the audit measured below 4.5:1 (text) or 3:1 (non-text) in any of light / dark / Increase Contrast is fixed per the owner's `D28=ok` (or left untouched if named in `D28=veto:`), using modern colour APIs (`Color.mix(with:by:)`, asset colorsets with light/dark/high-contrast variants) and re-measured ≥ threshold from rendered pixels (D-28, D-22)."
    - "Rating stars follow the owner's `STARS=` decision: under B a `RatingStar` colorset in the AppComponents module bundle (light ≥ 3:1 on the system background, dark `.yellow`-equivalent, high-contrast variants not less contrasty) replaces every `.foregroundStyle(.yellow)` star call site; under A the call sites stay `.yellow` and the recommendation carries the decorative-duplicate caveat (D-22, D-28)."
    - "Category backgrounds are untouched — the standard-44 pin still passes; the only colorset this plan may add is the module-local `RatingStar` (D-26)."
    - "Every visible glyph added by this plan (log-level symbols, Laboratory glyph) is appended to `16-CONTRAST-AUDIT.md § D-25 re-sweep candidates` with its screen #; colour-only changes add nothing (D-24, D-25)."
    - "Each changed colour/glyph is re-measured in light, dark and Increase Contrast and the before/after numbers are appended to the audit; screenshots stay in the scratchpad (D-28, D-32, D-33)."
    - "Every deliberate choice (which symbol per level, why a colorset for the stars, why a darker tint) carries a doc comment; no lint suppression (D-19)."
    - "Precision: re-measured ratios are compared `>=` the threshold with no rounding up; a site that measures 4.49 is still a failure."
  artifacts:
    - path: "AppPackage/Sources/AppModels/Support/AppActivityLog.swift"
      provides: "OSLogEntryLog.Level.symbol: SFSymbol (distinct per level)"
      contains: "var symbol"
    - path: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      provides: "### 16-22 result: before/after ratios per fixed site; D-25 candidates updated"
      contains: "16-22 result"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift"
      to: "AppPackage/Sources/AppModels/Support/AppActivityLog.swift"
      via: "`Image(systemSymbol: log.level.symbol)` + `.accessibilityLabel(log.level.title)` — symbol, colour and title are three views of one enum"
      pattern: "level.symbol"
  prohibitions:
    - statement: "Never move a category background colour; the standard-44 pin passes after this plan (D-26)."
      status: resolved
      verification: test
    - statement: "Never apply a colour fix the owner vetoed (`D28=veto:<site>`); record it as an accepted residual instead (D-22)."
      status: resolved
      verification: judgment
    - statement: "Never fix a colour-only state by adding label text VoiceOver reads but sighted users cannot see; the non-colour carrier must be visible (shape, glyph or text) (criterion 11)."
      status: resolved
      verification: judgment
---

<objective>
Remediate what the audit-first pass (plan 16-13) measured and the owner decided: give colour-only states a visible non-colour carrier (activity-log level symbols; a Laboratory on/off glyph if judged weak), fix the failing non-category colours with modern colour APIs (offline notice, toast text, swipe-action tints, anything else the audit listed) unless vetoed, and implement the rating-star decision — then re-measure everything and track the D-25 re-sweep candidates.

Purpose: criteria 10 (non-category half) and 11; Sufficient Contrast and Differentiate Without Color become claimable on evidence, not assumption.
Output: the listed view/model files edited per decision; `16-CONTRAST-AUDIT.md § 16-22 result` with before/after numbers and the updated D-25 list.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating (plans 16-20, 16-21); `OSLogEntryLog.Level.symbol`, log-level glyphs, optional Laboratory glyph, D-28 colour fixes, optional `RatingStar` colorset in `AppPackage/Sources/AppComponents/Resources/Colors.xcassets` (this plan).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `ReduceMotionGatingSourceTests`, `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings` (none added by this plan).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Differentiate Without Color — log-level symbols, Laboratory glyph if weak</name>
  <files>AppPackage/Sources/AppModels/Support/AppActivityLog.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift, AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Differentiate Without Color — the per-row verdicts (log level `fail`, Laboratory `weak`/`pass`, the grayscale numbers) and § Decisions.
    - `AppPackage/Sources/AppModels/Support/AppActivityLog.swift` lines 72–95 — `OSLogEntryLog.Level.color` and `.title` (the `@unknown default` handling to mirror).
    - `AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift` lines 205–230 — `AppActivityLogRow` (`Image(systemSymbol: .circleFill).foregroundStyle(log.level.color).font(.caption2)` + the label added in plan 16-17).
    - `AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift` lines 45–80 — the feature cell (tint vs `.secondary`, tinted vs gray background; the toggle representation from plan 16-17).
    - `$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md` § Differentiate Without Color.
  </read_first>
  <action>
`AppModels`: add `public var symbol: SFSymbol` on `OSLogEntryLog.Level` next to `color`/`title`, one distinct shape per level so no two levels differ by colour alone — debug `.ant`, info `.infoCircleFill`, notice `.bellFill`, error `.exclamationmarkTriangleFill`, fault `.xmarkOctagonFill`, undefined and `@unknown default` `.questionmarkCircleFill` (confirm each symbol exists in the vendored SFSafeSymbols set; substitute the nearest if one does not, and record it); doc-comment that shape is the non-colour carrier for criterion 11. `AppActivityLogRow`: `Image(systemSymbol: log.level.symbol)` keeping `.foregroundStyle(log.level.color)` and `.font(.caption2)` (same glyph size → row height unchanged; verify by comparing a `large` screenshot before/after); the `.accessibilityLabel(log.level.title)` from plan 16-17 stays. Laboratory: only if the audit verdict is `weak`, add a leading `Image(systemSymbol: isOn ? .checkmarkCircleFill : .circle)` at the cell's text size, hidden from accessibility (the toggle representation already carries the state), keeping the cell height; if `pass`, change nothing and record. Build; commit `feat(16-22): non-colour carriers for log levels`.
  </action>
  <verify>
    <automated>grep -c "var symbol" AppPackage/Sources/AppModels/Support/AppActivityLog.swift; grep -c "log.level.symbol" AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var symbol" …/AppActivityLog.swift` prints `1` with six distinct `case` symbols plus `@unknown default`; `grep -c "log.level.symbol" …/AppActivityLogsView.swift` prints `1`; `grep -c "circleFill" …/AppActivityLogsView.swift` prints `0`.
    - A grayscale-desaturated screenshot of the Activity Logs list (scratchpad) shows the levels distinguishable by shape; recorded in the summary.
    - Laboratory: either the glyph is present with `.accessibilityHidden(true)` or the summary records `pass — unchanged`.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Every colour-coded state carries a visible shape, glyph or text.</done>
</task>

<task type="auto">
  <name>Task 2: D-28 colour fixes and the rating-star decision</name>
  <files>AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/SystemNotification/ToastMessageView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/AppComponents/RatingView.swift, AppPackage/Sources/AppComponents/Resources/Colors.xcassets, AppPackage/Sources/HomeFeature/GalleryCardCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Non-category colours — the failing sites with their four ratios and the proposed fix per site; § Decisions — `STARS=`, `D28=ok|veto:…`.
    - `AppPackage/Sources/DetailFeature/DetailView.swift` lines 270–290 — the offline notice `Label(.savedDetails, systemSymbol: .wifiExclamationmark).font(.subheadline.weight(.semibold)).foregroundStyle(.orange)`.
    - `AppPackage/Sources/SystemNotification/ToastMessageView.swift` — title/subtitle styles on the glass background.
    - `AppPackage/Sources/DownloadsFeature/DownloadsView.swift` lines 195–235 — the swipe-action `.tint(.orange|.teal|.indigo|.red)` buttons (keep the Phase 15 plan-77 role-less delete and its comment).
    - The star call sites: `GalleryCardCell.swift:75`, `GalleryThumbnailCell.swift:101`, `GalleryDetailCell.swift:127`, `DetailView+Subviews.swift:186`, `RatingView.swift` previews (`foregroundStyle(.yellow)`); `AppPackage/Package.swift` — whether `AppComponents` declares `resources:` (a new `Colors.xcassets` under `AppComponents/Resources` is picked up by `.process`/`.copy` rules already used for `Localizable.xcstrings`; confirm).
    - `AppPackage/Sources/AppModels/Gallery/Category.swift` line 25 — how an asset colour is resolved by name (main bundle); the new colorset must instead use `Bundle.module`.
  </read_first>
  <action>
Apply each D-28 fix the audit proposed unless its site is in `D28=veto:`: offline notice — keep the orange hue but darken for 4.5:1 with `Color.orange.mix(with: .black, by: <factor the audit computed>)` (iOS 18 `mix`) in light mode and verify dark mode separately (a `Color` that differs per scheme belongs in a colorset variant rather than an inline `colorScheme` switch — prefer a module colorset if both schemes need different factors); toast title/subtitle — raise to `.primary` / the measured weight or switch the glass to a material the audit shows passes; swipe-action tints — darken the failing tints with `mix` to ≥ 4.5:1 against white label text (record the new factors), leaving passing tints alone; any other listed site per its proposal. Rating stars: under `STARS=B` add `Colors.xcassets/RatingStar.colorset` to `AppComponents/Resources` with light (the audit's ≥ 3:1 amber), dark (the `.yellow` value), and high-contrast variants that are not less contrasty than their siblings, expose `public static let ratingStar = Color("RatingStar", bundle: .module)` (an extension on `Color` in `RatingView.swift` or a small `AppComponentsColors` enum — doc-comment why a colorset: per-scheme values without a `colorScheme` read at five call sites), and replace the five `.foregroundStyle(.yellow)` star sites with it (leave `GeneralSettingView.swift:53`'s yellow — that is not a star; measure it as its own D-28 row); under `STARS=A` change no star site. Do not touch any category colorset. Build; run the package test targets for the touched modules on the spare simulator (`SystemNotificationTests`, `DownloadsFeatureTests`, `GalleryListComponentsTests`, `HomeFeatureTests`, `DetailFeatureTests` — one `xcodebuild test` invocation with several `-only-testing:` flags). Commit `feat(16-22): fix measured contrast failures`.
  </action>
  <verify>
    <automated>grep -rc "foregroundStyle(.yellow)" AppPackage/Sources/HomeFeature/GalleryCardCell.swift AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift AppPackage/Sources/DetailFeature/DetailView+Subviews.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Under `STARS=B`: the four star call sites print `0` for `foregroundStyle(.yellow)` and `AppPackage/Sources/AppComponents/Resources/Colors.xcassets/RatingStar.colorset/Contents.json` exists with light, dark and high-contrast entries; under `STARS=A`: the star sites are byte-identical and the summary says so.
    - Every non-vetoed failing site from the audit has a code change; every vetoed site is listed in the summary as `accepted residual (owner veto)`.
    - `CategoryColorsetInvariantTests` still passes (standard pin unchanged) — run as part of the wave's `FeatureTests`.
    - The touched module test targets pass on the spare simulator; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Every measured contrast failure the owner did not veto is fixed with a modern colour API.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure, record, and update the D-25 list</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Non-category colours (the before numbers and the measurement procedure) and § Decisions › D-25 re-sweep candidates.
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` § Infrastructure — UDIDs, baselines.
  </read_first>
  <action>
Install-over on `IPHONE_UDID`; repeat the plan 16-13 pixel measurement for every changed site (and the log-level symbols' 3:1 non-text check) across `appearance light|dark` × `increase_contrast disabled|enabled`, restore both settings and read them back. Append `### 16-22 result` to the audit: a table `site | before (L/D/L+IC/D+IC) | after (L/D/L+IC/D+IC) | threshold | verdict`, every `after` ≥ threshold or explicitly `accepted residual (owner veto)`; the grayscale note for the Activity Logs list; and update `D-25 re-sweep candidates` — add `#32 Activity Logs` (new glyph shapes), `#36 Laboratory` only if the glyph was added, and note that colour-only changes (offline notice, toast, tints, stars) add no screen per D-24. Send the before/after screenshot paths to the owner in chat (D-33). Commit `docs(16): record contrast remediation measurements` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "### 16-22 result" "$f"; grep -c "#32 Activity Logs" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `### 16-22 result` exists; every changed site has four `after` ratios ≥ its threshold, or is marked `accepted residual (owner veto)`.
    - `D-25 re-sweep candidates` lists `#32 Activity Logs` (and `#36 Laboratory` iff the glyph was added).
    - `xcrun simctl ui <IPHONE_UDID> appearance` and `… increase_contrast` print the recorded baselines.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): record contrast remediation measurements` exists; `grep -c "/Users/" 16-CONTRAST-AUDIT.md` prints `0`.
  </acceptance_criteria>
  <done>Criteria 10 and 11 are backed by after-measurements; the D-25 list is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colour edits → brand identity | Only non-category colours and the module-local `RatingStar` colorset change; the standard category pin guards the rest. |
| Measurement screenshots → scratchpad | Scratchpad-only; paths in chat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | category backgrounds | medium | mitigate | No category colorset touched; invariant test green. |
| T-16-14 | Repudiation | a claim resting on an unmeasured fix | medium | mitigate | After-measurements table with four ratios per site; vetoes recorded as residuals. |
| T-16-01 | Information disclosure | measurement screenshots | high | mitigate | Scratchpad-only; image check before the docs commit. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps per task; lint build green; module tests green on the spare simulator; invariant test green.
2. After-measurements ≥ thresholds; D-25 candidates updated; settings restored; three commits.
</verification>

<success_criteria>
- Criterion 11 met (no colour-only state) and criterion 10's non-category half met or explicitly owner-accepted, with every change measured after the fact (D-20, D-22, D-28).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-22-SUMMARY.md` when done: per-site changes with factors/symbols, vetoed residuals, the after-measurement table, D-25 updates, commit hashes.
</output>
