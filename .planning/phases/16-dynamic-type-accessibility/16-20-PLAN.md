---
phase: 16-dynamic-type-accessibility
plan: 20
type: execute
wave: 18
depends_on: ["16-19"]
files_modified:
  - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "Meaningful motion in Detail and the reader is gated on `@Environment(\\.accessibilityReduceMotion)`: the spinning download icon (`rotationEffect` 0→360 `repeatForever`) is replaced under Reduce Motion by a static symbol with `.symbolEffect(.pulse)` (Apple names spinning explicitly — never a slower spin); the Detail expand/collapse height animations (`showsUserRating`, `showsFullTitle`, `galleryDetail`) become `nil`; the deep-linked comment `scrollTo` and the reader page jump (`withAnimation { scrollPositionID = … }`) become un-animated; the reader pan/zoom settle (`offset`, `scale`) becomes `nil`; the control panel's slide (`.offset(y:)` ± 50 driven by `showsPanel`) and the slider-preview pop become opacity crossfades (D-29)."
    - "Subtle crossfades and `.contentTransition(.numericText())` stay ungated: the page indicator's numeric transition and title crossfade, the Live Text opacity swaps, the favourite-state opacity swap — nothing colour- or opacity-only is touched (D-29)."
    - "Each gating carries a doc comment stating why that site is meaningful motion (position / size / rotation) and what it degrades to, following the house pattern (`View+Toast`, `PrivacyMaskModifier`, `GalleryCardCell`) (D-29)."
    - "The environment value is read at the animating view and never mirrored into TCA state or set in a `#Preview` (read-only) (RESEARCH Pitfall 10)."
    - "Reduce Motion is verified on the simulator by toggling the real setting (Settings › Accessibility › Motion › Reduce Motion via `agent-device` on the sweep simulator, restored afterwards) — not by `.environment(…)` in a preview, which is silently ignored — and each gated site is observed in both states with screen recordings or paired screenshots in the scratchpad (D-29, D-31)."
    - "Gating changes no static layout; this plan adds no D-25 screen (D-24)."
    - "Idempotency: the Reduce Motion setting on the sweep simulator is restored to its prior value and read back."
  artifacts:
    - path: "AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift"
      provides: "reduceMotion-gated download icon (pulse instead of spin)"
      contains: "accessibilityReduceMotion"
    - path: "AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift"
      provides: "opacity-only panel transition under reduceMotion"
      contains: "accessibilityReduceMotion"
  key_links:
    - from: "AppPackage/Sources/ReadingFeature/ReadingView.swift"
      to: "AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift"
      via: "`showsPanel` drives both the container animation and the panel offsets; both read reduceMotion so the slide and the crossfade agree"
      pattern: "showsPanel"
  prohibitions:
    - statement: "Never gate an opacity crossfade or a `.contentTransition(.numericText())` on Reduce Motion (D-29)."
      status: resolved
      verification: judgment
    - statement: "Never replace the spinning icon with a slower spin; under Reduce Motion the replacement is static + pulse or a ProgressView (Apple's Reduced Motion criteria)."
      status: resolved
      verification: judgment
    - statement: "Never mirror `accessibilityReduceMotion` into TCA state or set it in a `#Preview`."
      status: resolved
      verification: judgment
---

<objective>
Gate the meaningful motion in the Detail and Reading features (D-29): the spinning download icon, the Detail height animations, the deep-linked comment scroll, the reader's pan/zoom settle and page jump, and the control panel's slide and preview pop — each replaced by a dissolve or nothing under Reduce Motion, with the crossfades and numeric text left exactly as they are.

Purpose: criterion 9 for the app's core surfaces, judged by Apple's Reduced Motion criteria (spinning and multi-axis motion replaced; meaningful animation dissolved; decorative removed).
Output: five files gated with doc-commented reasons; simulator verification in both states.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating in Detail + reader (this plan) and in lists / inspector (plan 16-21); `OSLogEntryLog.Level.symbol` (plan 16-22).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings` (none added by this plan).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail — spinning download icon, expand/collapse heights, deep-linked comment scroll</name>
  <files>AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift, AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DetailFeature/Comments/CommentsView.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Reduce Motion inventory" (in-scope table; the out-of-scope list — leave those alone) and § "Pattern 4: Reduce Motion gating".
    - `.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md` § "Reduce Motion gating" — Analogs A/B/C (`View+Toast.swift:34,97–105`, `DownloadsView+Subviews.swift:129–131,152–158`, `ViewModifiers.swift:9–24`) and the spinning-icon note.
    - `AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` lines 120–160 — the download button's `.animation(showsMetadataPreparation ? .linear(0.9).repeatForever : .default, value:)` and `downloadIconLabel`'s `.rotationEffect(.degrees(showsMetadataPreparation ? 360 : 0))`; lines 162–190 (favourite opacity swap — out of scope).
    - `AppPackage/Sources/DetailFeature/DetailView.swift` lines 24–30 — `.animation(.default, value: store.showsUserRating / showsFullTitle / galleryDetail)`.
    - `AppPackage/Sources/DetailFeature/Comments/CommentsView.swift` lines 85–96 — `withAnimation { proxy.scrollTo(scrollCommentID, anchor: .top) }`.
    - `$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md` § Reduce Motion.
  </read_first>
  <action>
HeaderSection: add `@Environment(\.accessibilityReduceMotion) private var reduceMotion`; under `reduceMotion` the preparation state renders the download symbol without `rotationEffect` and with `.symbolEffect(.pulse, isActive: showsMetadataPreparation)` (iOS 17+ symbol effects), and the `.animation(…)` predicate becomes `.default` (no `repeatForever`); under normal motion the spin stays exactly as today. Doc-comment: Apple's Reduced Motion criteria name spinning explicitly; the pulse keeps the "preparing" affordance without rotation. DetailView: make the three `.animation(.default, value:)` modifiers `.animation(reduceMotion ? nil : .default, value:)` (height expand/collapse is position/size motion) with one doc comment on the group. CommentsView: `withAnimation(reduceMotion ? nil : .default) { proxy.scrollTo(…) }` — the deep-linked comment still lands at the same anchor, instantly. Do not touch the favourite opacity swap, the vote-glyph opacity, or any `.contentTransition`. Build; commit `feat(16-20): gate detail motion on Reduce Motion`.
  </action>
  <verify>
    <automated>grep -c "accessibilityReduceMotion" AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift AppPackage/Sources/DetailFeature/DetailView.swift AppPackage/Sources/DetailFeature/Comments/CommentsView.swift; grep -c "symbolEffect(.pulse" AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityReduceMotion"` ≥ 1 in each of the three files; `grep -c "symbolEffect(.pulse" …/DetailView+HeaderSection.swift` ≥ 1; `grep -c "repeatForever" …/DetailView+HeaderSection.swift` ≥ 1 (the normal-motion spin is kept, selected by the predicate).
    - `grep -c "reduceMotion ? nil : .default" …/DetailView.swift` prints `3`; `grep -c "withAnimation(reduceMotion ? nil" …/Comments/CommentsView.swift` prints `1`.
    - The favourite swap and every `.contentTransition` in DetailFeature are byte-identical (`git diff` shows no change on those lines).
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Detail's meaningful motion dissolves or stops under Reduce Motion; its crossfades still play.</done>
</task>

<task type="auto">
  <name>Task 2: Reader — pan/zoom settle, page jump, panel slide, preview pop; simulator verification in both states</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift</files>
  <read_first>
    - `AppPackage/Sources/ReadingFeature/ReadingView.swift` lines 105–116 (`.animation(.linear(0.1), value: gestureHandler.offset)`, `.animation(.default, value: gestureHandler.scale)`, `.animation(.default, value: store.showsPanel)`, the two Live Text opacity animations — out of scope) and lines 340–356 (`withAnimation { scrollPositionID = clampedIndex }` page jump with the `performingChanges` echo guard — keep the guard timing identical).
    - `AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift` lines 80–106 (`.offset(y: showsPanel ? 0 : -50)` / `: 50`, `.animation(.default, value: showsSliderPreview)`, `.opacity(showsPanel ? 1 : 0)`, `.disabled`) and lines 170–181 (page indicator `.contentTransition(.numericText())` + `.animation(.default, value: title)` — out of scope).
    - `AppPackage/Sources/SystemNotification/View+Toast.swift` lines 30–40, 95–106 — computed `Animation` / `AnyTransition` selected by the environment read.
  </read_first>
  <action>
ReadingView: add the environment read; `.animation(reduceMotion ? nil : .linear(duration: 0.1), value: gestureHandler.offset)` and `.animation(reduceMotion ? nil : .default, value: gestureHandler.scale)` (content still follows the finger; only the eased settle goes); keep `.animation(.default, value: store.showsPanel)` for the opacity half but make the panel's slide opacity-only (below); the page jump becomes `withAnimation(reduceMotion ? nil : .default) { scrollPositionID = clampedIndex }` with the `performingChanges` / `echoGuardDuration` choreography unchanged (doc-comment that the guard window is independent of the animation). ControlPanel: add the environment read; the upper/lower `.offset(y:)` become `0` when `reduceMotion` (so the panel only fades via the existing `.opacity(showsPanel ? 1 : 0)`), and `.animation(reduceMotion ? nil : .default, value: showsSliderPreview)` for the preview pop — or, if the preview's appearance is itself an opacity change, leave it and record why. Leave the page indicator's `numericText` + title animation and the Live Text opacity swaps untouched. Build; run `ReadingFeatureTests` on the spare simulator. Verification: install-over on `IPHONE_UDID`; read the current Reduce Motion state by opening Settings (`agent-device open com.apple.Preferences --platform ios --udid <IPHONE_UDID> --foreground`, Accessibility › Motion) and record it; with Reduce Motion OFF then ON, exercise: Detail with a download being prepared (spin vs pulse), expanding the full title, opening a deep-linked comment (`ehpanda://e-hentai.org/g/<gid>/<token>/#c<id>` of a gallery with comments — or the compose flow), the reader page jump via the slider, showing/hiding the panel, pinch settle — capture paired screenshots (`$SCRATCHPAD/reduce-motion/<site>-{off,on}-{t0,t1}.png`) or a short `agent-device` recording per site; restore the Reduce Motion setting to the recorded value and confirm it in Settings. Record per site: OFF behaviour, ON behaviour, pass/fail. Commit `feat(16-20): gate reader motion on Reduce Motion`.
  </action>
  <verify>
    <automated>grep -c "accessibilityReduceMotion" AppPackage/Sources/ReadingFeature/ReadingView.swift AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift; grep -c "numericText" AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:ReadingFeatureTests 2>&1 | grep -E "passed|failed|error:" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityReduceMotion"` ≥ 1 in each of `ReadingView.swift` and `ControlPanel.swift`; `grep -c "withAnimation(reduceMotion ? nil" …/ReadingView.swift` ≥ 1; `grep -c "reduceMotion ? 0 :" …/ControlPanel.swift` ≥ 2 (both offsets) or the documented equivalent.
    - `grep -c "numericText" …/ControlPanel.swift` is unchanged from before the plan; the Live Text opacity animations in `ReadingView.swift` are byte-identical in `git diff`.
    - `ReadingFeatureTests` passes on the spare simulator; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`.
    - The summary has a per-site table (spin/pulse, heights, comment scroll, page jump, panel, preview, pan/zoom) with OFF/ON observations and scratchpad evidence paths, and records that the simulator's Reduce Motion setting was restored.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif|mov|mp4)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>Reader motion dissolves under Reduce Motion; numeric text and crossfades still animate; verified in both states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Animation predicates → page choreography | The reader's echo-guard timing must not depend on whether the jump animates. |
| Simulator settings → owner's environment | Reduce Motion toggled and restored on the sweep simulator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-20 | Tampering (accidental) | reader page-jump echo guard | medium | mitigate | Guard window unchanged; `ReadingFeatureTests` green; OFF/ON page jump observed. |
| T-16-21 | Tampering (accidental) | over-gating flattens non-vestibular motion | low | mitigate | Out-of-scope list honoured; `numericText` and opacity sites byte-identical in the diff. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | Install-over; setting restored and confirmed. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps (gates present; out-of-scope sites untouched); lint build green; `ReadingFeatureTests` green.
2. OFF/ON observation table with evidence paths; Reduce Motion restored; two commits.
</verification>

<success_criteria>
- Detail and reader meet criterion 9 under Apple's Reduced Motion criteria: spinning replaced, meaningful animations dissolved or removed, decorative removed, crossfades and numeric text intentionally kept (D-29).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-20-SUMMARY.md` when done: per-site gating with the reason, the OFF/ON table, evidence paths, commit hashes.
</output>
