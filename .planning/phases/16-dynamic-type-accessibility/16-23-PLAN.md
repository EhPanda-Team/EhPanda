---
phase: 16-dynamic-type-accessibility
plan: 23
type: execute
wave: 21
depends_on: ["16-22"]
files_modified:
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/SystemNotification/ToastMessageView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/AppComponents/RatingView.swift
  - AppPackage/Sources/AppComponents/Resources/Colors.xcassets
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "Every non-category colour the audit measured below 4.5:1 (text) or 3:1 (non-text) in any of light / dark / Increase Contrast — including any level-colour row plan 16-22 handed over — is fixed per the owner's `D28=ok` (or left untouched if named in `D28=veto:`), using modern colour APIs (`Color.mix(with:by:)`, asset colorsets with light/dark/high-contrast variants) and re-measured ≥ threshold from rendered pixels (D-28, D-22)."
    - "Rating stars follow the owner's `STARS=` decision: under B a `RatingStar` colorset in the AppComponents module bundle (light ≥ 3:1 on the system background, dark `.yellow`-equivalent, high-contrast variants not less contrasty) replaces every `.foregroundStyle(.yellow)` star call site; under A the call sites stay `.yellow` and the recommendation carries the decorative-duplicate caveat (D-22, D-28)."
    - "Category backgrounds are untouched — the standard-44 pin still passes; the only colorset this plan may add is the module-local `RatingStar` (D-26)."
    - "Colour-only changes move no layout, so this plan adds no screen to the D-25 re-sweep list and says so in the audit (D-24, D-25)."
    - "Each changed colour is re-measured in light, dark and Increase Contrast and the before/after numbers are appended to the audit; screenshots stay under the evidence root `$HOME/Library/Caches/ehpanda-phase16/`, never the repo (D-28, D-32, D-33)."
    - "Every deliberate choice (why a colorset for the stars, why a darker tint, which `mix` factor) carries a doc comment; no lint suppression (D-19)."
    - "Precision: re-measured ratios are compared `>=` the threshold with no rounding up; a site that measures 4.49 is still a failure."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      provides: "### 16-23 result (contrast): before/after ratios per fixed site; vetoed residuals; D-25 note"
      contains: "16-23 result"
    - path: "AppPackage/Sources/AppComponents/RatingView.swift"
      provides: "star colour from the RatingStar colorset under STARS=B (or unchanged under STARS=A)"
      contains: "foregroundStyle"
  key_links:
    - from: "AppPackage/Sources/AppComponents/Resources/Colors.xcassets/RatingStar.colorset"
      to: "the five star call sites (GalleryCardCell, GalleryDetailCell, GalleryThumbnailCell, DetailView+Subviews, RatingView)"
      via: "`Color(\"RatingStar\", bundle: .module)` exposed once from AppComponents and used at every star site — one source of truth for the star colour"
      pattern: "RatingStar"
    - from: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md § Decisions"
      to: "this plan's edits"
      via: "`D28=ok|veto:` and `STARS=A|B` decide which sites change"
      pattern: "D28="
  prohibitions:
    - statement: "Never move a category background colour; the standard-44 pin passes after this plan (D-26)."
      status: resolved
      verification: test
    - statement: "Never apply a colour fix the owner vetoed (`D28=veto:<site>`); record it as an accepted residual instead (D-22)."
      status: resolved
      verification: judgment
    - statement: "Never change a rendered element's size or layout while fixing a colour; a colour fix that needs a layout change is reported, not made (D-24)."
      status: resolved
      verification: judgment
---

<objective>
Remediate the measured non-category contrast failures the owner did not veto (offline notice, toast text, swipe-action tints, anything else the audit listed, plus any level-colour row plan 16-22 handed over) with modern colour APIs, implement the rating-star decision, then re-measure everything. Split from plan 16-22 so that the layout-neutral colour work (nine files, mostly one-line tint changes) is verified separately from the glyph work.

Purpose: criterion 10's non-category half; Sufficient Contrast becomes claimable on evidence, not assumption.
Output: the listed view files edited per decision; the optional `RatingStar` colorset; `16-CONTRAST-AUDIT.md § 16-23 result (contrast)` with before/after numbers.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating (plans 16-20, 16-21); `OSLogEntryLog.Level.symbol` in `SettingFeature` and the DWC glyphs (plan 16-22); D-28 colour fixes and the optional `RatingStar` colorset in `AppPackage/Sources/AppComponents/Resources/Colors.xcassets` (this plan).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `ReduceMotionGatingSourceTests`, `AccessibilityAuditUITests` (plan 16-24).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings` (none added by this plan).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-28 colour fixes — offline notice, toast text, swipe-action tints, any handed-over row</name>
  <files>AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/SystemNotification/ToastMessageView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Non-category colours — the failing sites with their four ratios and the proposed fix per site; § 16-22 result (DWC) — any level-colour row handed to this plan; § Decisions — `D28=ok|veto:…`.
    - `AppPackage/Sources/DetailFeature/DetailView.swift` lines 270–290 — the offline notice `Label(.savedDetails, systemSymbol: .wifiExclamationmark).font(.subheadline.weight(.semibold)).foregroundStyle(.orange)`.
    - `AppPackage/Sources/SystemNotification/ToastMessageView.swift` — title/subtitle styles on the glass background.
    - `AppPackage/Sources/DownloadsFeature/DownloadsView.swift` lines 195–235 — the swipe-action `.tint(.orange|.teal|.indigo|.red)` buttons (keep the Phase 15 plan-77 role-less delete and its comment).
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadsSwipeActionSourceTests.swift` — the region-scoped source test that pins the swipe block (a tint change must keep its assertions green).
  </read_first>
  <action>
Apply each D-28 fix the audit proposed unless its site is in `D28=veto:`: offline notice — keep the orange hue but darken for 4.5:1 with `Color.orange.mix(with: .black, by: <factor the audit computed>)` (iOS 18 `mix`) in light mode and verify dark mode separately (a `Color` that differs per scheme belongs in a colorset variant rather than an inline `colorScheme` switch — prefer a module colorset if both schemes need different factors); toast title/subtitle — raise to `.primary` / the measured weight or switch the glass to a material the audit shows passes, without changing the toast's size; swipe-action tints — darken the failing tints with `mix` to ≥ 4.5:1 against white label text (record the new factors), leaving passing tints alone; any handed-over level-colour row — if a level colour in `AppModels` fails 3:1 as a glyph, darken it with `mix` at the use site in `AppActivityLogsView.swift` rather than editing `AppModels` (note the site in the summary; `AppActivityLogsView.swift` is touched only in that case). Doc-comment every factor with the measured before/after ratio. Build; run the touched module test targets on the spare simulator (`DetailFeatureTests`, `SystemNotificationTests`, `DownloadsFeatureTests` — one `xcodebuild test` invocation with several `-only-testing:` flags; `DownloadsSwipeActionSourceTests` must stay green). Commit `feat(16-23): fix measured contrast failures`.
  </action>
  <verify>
    <automated>grep -c "mix(with:" AppPackage/Sources/DetailFeature/DetailView.swift AppPackage/Sources/DownloadsFeature/DownloadsView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Every non-vetoed failing site from the audit has a code change with a doc-commented factor; every vetoed site is listed in the summary as `accepted residual (owner veto)`.
    - `DownloadsSwipeActionSourceTests` and the other touched module tests pass on the spare simulator; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
    - No category colorset changed (`git diff --stat <plan-start>..HEAD -- App/Assets.xcassets/Category` is empty).
  </acceptance_criteria>
  <done>Every measured non-category contrast failure the owner did not veto is fixed with a modern colour API.</done>
</task>

<task type="auto">
  <name>Task 2: The rating-star decision — `RatingStar` colorset under B, unchanged under A</name>
  <files>AppPackage/Sources/AppComponents/RatingView.swift, AppPackage/Sources/AppComponents/Resources/Colors.xcassets, AppPackage/Sources/HomeFeature/GalleryCardCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions — `STARS=A|B` and the audit's ≥ 3:1 light-mode amber.
    - The star call sites: `GalleryCardCell.swift:75`, `GalleryThumbnailCell.swift:101`, `GalleryDetailCell.swift:127`, `DetailView+Subviews.swift:186`, `RatingView.swift` previews (`foregroundStyle(.yellow)`).
    - `AppPackage/Package.swift` — whether `AppComponents` declares `resources:` (a new `Colors.xcassets` under `AppComponents/Resources` is picked up by the `.process` rule already used for `Localizable.xcstrings`; confirm, and list the manifest in the summary only if a `resources:` entry had to be added).
    - `AppPackage/Sources/AppModels/Gallery/Category.swift` line 25 — how an asset colour is resolved by name (main bundle); the new colorset must instead use `Bundle.module`.
  </read_first>
  <action>
Under `STARS=B`: add `Colors.xcassets/RatingStar.colorset` to `AppComponents/Resources` with light (the audit's ≥ 3:1 amber), dark (the `.yellow` value), and high-contrast variants that are not less contrasty than their siblings; expose `public static let ratingStar = Color("RatingStar", bundle: .module)` as an extension on `Color` in `RatingView.swift` (doc-comment why a colorset: per-scheme values without a `colorScheme` read at five call sites), and replace the five `.foregroundStyle(.yellow)` star sites with it (leave `GeneralSettingView.swift:53`'s yellow — that is not a star; it is measured as its own D-28 row in Task 1's audit list). Under `STARS=A`: change no star site and record `STARS=A — unchanged` in the summary. Do not touch any category colorset. Build; run `GalleryListComponentsTests`, `HomeFeatureTests`, `DetailFeatureTests` on the spare simulator (one `xcodebuild test` invocation). Commit `feat(16-23): rating star colour per owner decision` (only under B).
  </action>
  <verify>
    <automated>grep -rc "foregroundStyle(.yellow)" AppPackage/Sources/HomeFeature/GalleryCardCell.swift AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift AppPackage/Sources/DetailFeature/DetailView+Subviews.swift; ls AppPackage/Sources/AppComponents/Resources/Colors.xcassets/RatingStar.colorset/Contents.json 2>/dev/null | wc -l; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Under `STARS=B`: the four star call sites print `0` for `foregroundStyle(.yellow)` and `AppPackage/Sources/AppComponents/Resources/Colors.xcassets/RatingStar.colorset/Contents.json` exists with light, dark and high-contrast entries; under `STARS=A`: the star sites are byte-identical and the summary says so.
    - `CategoryColorsetInvariantTests` still passes (standard pin unchanged) — run as part of the wave's `FeatureTests`.
    - The touched module test targets pass on the spare simulator; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>The star colour follows the owner's decision, from one source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure and record</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Non-category colours (the before numbers and the measurement procedure) and § Decisions › D-25 re-sweep candidates.
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` § Infrastructure (UDIDs, `BUNDLE_ID`, `EVIDENCE_ROOT`, baselines) and § Protocol › Install-over.
  </read_first>
  <action>
Install-over on `IPHONE_UDID` per the `16-SWEEP.md § Protocol › Install-over` rule (build by UDID, `plutil -extract CFBundleIdentifier raw` equals `BUNDLE_ID`, `xcrun simctl install`; never uninstall / erase / clear-app-state). Repeat the plan 16-13 pixel measurement for every changed site across `appearance light|dark` × `increase_contrast disabled|enabled` (`xcrun simctl ui <IPHONE_UDID> …`, baselines read first), screenshots to `$EVIDENCE_ROOT/round2/contrast-after/`, then restore both settings and read them back. Append `### 16-23 result (contrast)` to the audit: a table `site | before (L/D/L+IC/D+IC) | after (L/D/L+IC/D+IC) | threshold | verdict`, every `after` ≥ threshold or explicitly `accepted residual (owner veto)`; and a one-line note under `D-25 re-sweep candidates` that colour-only changes (offline notice, toast, tints, stars) add no screen per D-24. Send the before/after screenshot paths to the owner in chat (D-33). Commit `docs(16): record contrast remediation measurements` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "### 16-23 result (contrast)" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `### 16-23 result (contrast)` exists; every changed site has four `after` ratios ≥ its threshold, or is marked `accepted residual (owner veto)`.
    - `D-25 re-sweep candidates` carries the "colour-only changes add no screen (D-24)" note and no new screen from this plan.
    - `xcrun simctl ui <IPHONE_UDID> appearance` and `… increase_contrast` print the recorded baselines.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): record contrast remediation measurements` exists; `grep -c "/Users/" 16-CONTRAST-AUDIT.md` prints `0`.
  </acceptance_criteria>
  <done>Criterion 10's non-category half is backed by after-measurements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colour edits → brand identity | Only non-category colours and the module-local `RatingStar` colorset change; the standard category pin guards the rest. |
| Measurement screenshots → evidence root | Evidence root (`$HOME/Library/Caches/ehpanda-phase16/`) only; paths in chat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | category backgrounds | medium | mitigate | No category colorset touched; invariant test green; acceptance diff over `App/Assets.xcassets/Category` empty. |
| T-16-14 | Repudiation | a claim resting on an unmeasured fix | medium | mitigate | After-measurements table with four ratios per site; vetoes recorded as residuals. |
| T-16-01 | Information disclosure | measurement screenshots | high | mitigate | Evidence-root only; image check before the docs commit. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | § Protocol install-over rule (bundle id checked); settings restored. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps per task; lint build green; module tests green on the spare simulator; invariant test green.
2. After-measurements ≥ thresholds; D-25 note; settings restored; two or three commits.
</verification>

<success_criteria>
- Criterion 10's non-category half met or explicitly owner-accepted, with every change measured after the fact and no layout moved (D-20, D-22, D-24, D-28).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-23-SUMMARY.md` when done: per-site changes with factors, vetoed residuals, the `STARS=` outcome, the after-measurement table, commit hashes.
</output>
