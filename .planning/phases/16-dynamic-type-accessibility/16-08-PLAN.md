---
phase: 16-dynamic-type-accessibility
plan: 08
type: execute
wave: 7
depends_on: ["16-07"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every in-scope Group B surface (#14–#27) has six iPad rows that are no longer `pending`; on the iPad, Detail is a modal presentation from the tab root and the reader uses the regular-width control panel (more thumbnails, `.callout` labels), so the cells are judged on the iPad's own layouts (D-10)."
    - "Account-gated surfaces (live Detail, Comments, Archives, Torrents, Reading) are reached through the owner's iPad session or recorded `blocked: no iPad session`; no credential is handled (D-09)."
    - "Verdicts follow D-03 / D-04 strictly; the three D-13 rows rendering in Group B (stats-strip abbreviation, long-tag clip, reader counter wrap) get an iPad `Observed` line (D-03, D-04, D-13)."
    - "Findings are recorded and the sweep moves on; screenshots stay in the scratchpad; before/after paths go to the owner in chat (D-02, D-32, D-33)."
    - "Three sizes only, AX5 the maximum, large end only (D-05, D-06, D-07)."
    - "Idempotency / concurrency: the iPad baseline is restored and read back; only the iPad simulator is driven in this plan."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Group B × iPad matrix rows filled; Findings entries; D-04 and D-13 rows updated"
      contains: "iPad"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § D-13 named edge cases"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Matrix (iPad rows)"
      via: "each D-13 row carries an iPad `Observed` line naming the cells"
      pattern: "Observed"
  prohibitions:
    - statement: "No screenshot or any image file is ever added to the repository (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow code and no reflow work order during the sweep (D-01)."
      status: resolved
      verification: judgment
    - statement: "The sweep never purchases an archive, posts a comment, votes, or changes a favorite — mutation sheets are opened, judged and cancelled."
      status: resolved
      verification: judgment
---

<objective>
Walk Group B (#14–#27: Detail family, sheets, Reading and its panel) on the iPad Pro 11 simulator at six cells each, on the iPad's own modal/regular-width layouts, judged against D-03 / D-04, findings recorded without interruption.

Purpose: the detail header, stats strip, tag cloud and reader control panel are the surfaces where the iPad's different layouts make its failure modes genuinely different from the iPhone's.
Output: 84 iPad rows non-`pending`; D-13 rows for #14 (two) and #25 observed on the iPad.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (rows filled here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iPad × Group B surfaces #14–#20</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Infrastructure (`IPAD_UDID`, `IPAD_LOGIN`), § Protocol, § Inventory rows #14–#20, the iPad § Matrix rows, the iPhone verdicts for the same rows, § D-13 rows "Detail stats-strip abbreviation" and "long-tag right-edge clip", § D-04 rows mapped to #14–#20.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 14–20.
    - `AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift` lines 26–40 — the pad-idiom modal presentation of Detail and Setting (read to know the expected presentation; do not edit).
  </read_first>
  <action>
Start the iPad session per § Protocol on `IPAD_UDID` / `BUNDLE_ID`. Open a gallery with a long title, long uploader and dense tag cloud from the Frontpage list — on the iPad it presents modally; judge the modal Detail (#14) header, stats strip, description, tag cloud, previews and comments preview to the bottom at each of the six cells (`xcrun simctl ui <IPAD_UDID> content_size …`, `agent-device orientation …`, screenshots at `--scale 0.5` under `$SCRATCHPAD/sweep/ipad-…`); add iPad `Observed` lines to the two D-13 rows. Then Previews (#15), Comments (#16 + post sheet, cancelled), Detail Search (#17), Gallery Infos (#18), Archives (#19, never purchase), Torrents (#20) — each six cells, scrolled to the bottom, judged strictly under D-04 (uploader, date, page count, category token, funds/price, torrent meta). Reuse finding numbers for defects the iPhone already recorded (append the iPad cells); new numbers for iPad-only defects; escalate D-04 rows if an iPad cell fails a row the iPhone passed. If `IPAD_LOGIN=none`, the live gated surfaces are `blocked: no iPad session`. Commit with `docs(16): sweep iPad group B rows 14-20` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 14 15 16 17 18 19 20; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPad" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 14–20, the six iPad matrix rows are non-`pending` (`blocked: no iPad session` only when `IPAD_LOGIN=none`).
    - D-13 rows "Detail stats-strip abbreviation" and "long-tag right-edge clip" each have an iPad `Observed` line.
    - No comment posted, no archive purchased, no vote cast (summary states this).
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPad group B rows 14-20` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>#14–#20 verified on the iPad at six cells.</done>
</task>

<task type="auto">
  <name>Task 2: iPad × Group B surfaces #21–#27 and restore</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Protocol, § Inventory rows #21–#27, the iPad § Matrix rows, the iPhone verdicts, § D-13 row "reader total-page counter wrap", § D-04 rows for `ControlPanel.swift:176,166,296`.
    - `AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift` lines 370–415 — the regular-width (`horizontalSizeClass == .regular`) branches of the slider preview (`.callout` font, 30-pt thumbnails, 5/7 thumbnails); read to know what differs on the iPad, do not edit.
  </read_first>
  <action>
Continue on the iPad. Tag Detail (#21), NewDawn (#22, `blocked: greeting not presented this session` if not shown), download confirm dialogs (#23, cancelled), Reading (#24), Control panel (#25 — the iPad uses the regular-width slider preview with more thumbnails and `.callout` labels; judge the preview strip, the `n / total` indicator in both orientations and add an iPad line to the D-13 row "reader total-page counter wrap" `Observed` column; open the dual-page menu — it exists only in landscape with non-vertical direction — and the auto-play menu as cells), Reading Setting sheet (#26), Live Text overlay (#27, `n/a: no app-drawn text` if so). Six cells each per § Protocol; reuse or add finding numbers; escalate D-04 rows where needed. Restore `xcrun simctl ui <IPAD_UDID> content_size <iPad baseline>` and `agent-device orientation portrait`, read back, commit with `docs(16): sweep iPad group B rows 21-27` after the image check. Summary: findings opened/extended (numbers, one-liners, scratchpad paths — D-33), D-13 iPad observations, blocked / n/a rows.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 21 22 23 24 25 26 27; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPad" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 21–27, the six iPad matrix rows are non-`pending`; `blocked:` / `n/a:` only with a stated reason on #22 / #27 (or gated surfaces under `IPAD_LOGIN=none`).
    - The D-13 row "reader total-page counter wrap" has an iPad `Observed` line and is still not dispositioned.
    - `xcrun simctl ui <IPAD_UDID> content_size` prints the recorded iPad baseline after the restore.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPad group B rows 21-27` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>Group B verified on the iPad; simulator restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator screenshots → scratchpad → chat | Detail/reader screenshots show gallery content; scratchpad only. |
| Agent → owner's account state | Mutation sheets cancelled, never confirmed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | gallery-content screenshots | high | mitigate | Scratchpad-only; written descriptions; image check before each docs commit. |
| T-16-10 | Tampering | owner's account state | medium | mitigate | Cancel-only on post / vote / archive / favorite sheets. |
| T-16-03 | Denial of service (phase infrastructure) | iPad logged-in simulator | medium | mitigate | UDID-addressed; forbidden list; baseline restored and read back. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. All 84 iPad Group B rows non-`pending`; findings numbered or extended.
2. D-13 rows for #14 (two) and #25 carry iPad observations.
3. No account mutation; iPad simulator restored; no image in the repo; two docs commits.
</verification>

<success_criteria>
- Group B verified on the iPad's own layouts under D-03 / D-04 at all six cells.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-08-SUMMARY.md` when done: findings opened/extended, D-13 iPad observations, blocked / n/a rows, commit hashes.
</output>
