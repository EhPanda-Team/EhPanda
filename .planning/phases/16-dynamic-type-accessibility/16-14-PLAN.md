---
phase: 16-dynamic-type-accessibility
plan: 14
type: execute
wave: 12
depends_on: ["16-12"]
files_modified:
  - AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift
  - AppPackage/Tests/AppToolsTests/ColorContrastTests.swift
  - AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "A pure colour-math helper exists in `AppTools`: `Color.Resolved.relativeLuminance` (WCAG 2.x on the already-linear `linearRed/Green/Blue` channels), `Color.Resolved.composited(over:opacity:)` (linear source-over for the 0.3-opacity excluded `CategoryCell`), `Color.contrastRatio(_:_:)` (≥ 1, (L_hi + 0.05)/(L_lo + 0.05)), `Color.contrastingForeground(on:)` and `Color.contrastingForeground(in:)` (black if the resolved luminance is strictly above 0.1791, else white — the better-of rule whose floor is 4.58:1 for any background), each with a doc comment stating the why (D-26)."
    - "The helper is unit-tested with hand-built `Color.Resolved` values: black, white, the crossover gray (linear 0.1791 → both ratios ≈ 4.583), one step either side of the crossover (black chosen above, white chosen at and below), and the worst real variant (white 4.55, black 4.62 → black) (D-26)."
    - "A repository-walk invariant test parses all 22 category colorsets (84 variants; hex `0x11`, float `0.910` and decimal `163` component encodings all normalised; `color-space == srgb` asserted), refuses a vacuous walk with known members, asserts best-of-black/white ≥ 4.5:1 for every variant, asserts the worst best-of is 4.62 ± 0.01 and the black-flip count is 47, and pins two SHA-256 hashes — one over the 44 standard (non-HC) variants' canonical serialization, one over the 40 `contrast: high` variants — so the standard backgrounds can never move while HC re-authoring (if the owner chose it) re-pins only the HC hash (D-26, D-27)."
    - "Adjacency: at exactly L = 0.1791 the helper resolves to white (the comparison is strict `>`), one step above resolves to black; the unit test pins the tie and both neighbours."
    - "Encoding: the invariant test proves the three component encodings yield the same 0…1 channel for equal colours (a hex `0xFF`, a float `1.000` and a decimal `255` fixture all map to 1.0) and that the decimal-encoded ExHentai Cosplay variants produce a luminance in [0, 1]."
    - "Precision: luminance is computed on linear channels as `Double`; ratios are compared with `>= 4.5` and no rounding-up; the pinned worst value is asserted with a ± 0.01 tolerance, the counts exactly."
    - "Tests are Swift Testing (`@Suite` / `@Test` / `#require`), no `try?`, no force unwrap, sorted imports, 120-column lines; the existing package suite stays green and the lint build passes (D-19)."
    - "Concurrency / idempotency: the test target runs on `SPARE_UDID` only and one `xcodebuild test` at a time; re-running yields the same hashes and counts."
  artifacts:
    - path: "AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift"
      provides: "relativeLuminance, composited(over:opacity:), contrastRatio(_:_:), contrastingForeground(on:), contrastingForeground(in:)"
      contains: "contrastingForeground"
    - path: "AppPackage/Tests/AppToolsTests/ColorContrastTests.swift"
      provides: "crossover / neighbours / worst-variant / black-white fixtures"
      contains: "0.1791"
    - path: "AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift"
      provides: "84-variant parse, 84/84 ≥ 4.5, worst 4.62, 47 flips, standard-44 and HC-40 SHA-256 pins"
      contains: "SHA256"
  key_links:
    - from: "AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift"
      to: "App/Assets.xcassets/Category/Colors/{E-Hentai,ExHentai}/*.colorset/Contents.json"
      via: "repository-root walk (the `DownloadLogPrivacyInvariantTests` pattern) reading the live JSON"
      pattern: "Contents.json"
    - from: "AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift"
      to: "AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift"
      via: "the test computes luminance through the helper (`Color.Resolved(colorSpace: .sRGB, …).relativeLuminance`) so the helper is proven against the 84-row table"
      pattern: "relativeLuminance"
  prohibitions:
    - statement: "Never move a category background colour: the 44 standard variants are pinned byte-for-byte by hash and the pin is only ever re-derived for the HC group under the owner's HC=A decision (D-26)."
      status: resolved
      verification: test
    - statement: "Never compute luminance from the gamma-encoded `red/green/blue` fields of `Color.Resolved` (double decode); use the linear fields (RESEARCH Pitfall 8)."
      status: resolved
      verification: test
    - statement: "Never suppress a lint rule in the new files; resolve violations at the root (D-19)."
      status: resolved
      verification: judgment
---

<objective>
Build the contrast foundation that D-26 rests on: the `Color` luminance/contrast helper in `AppTools` (the adaptive badge text's single source of truth), its unit tests, and the colorset invariant test that pins the 44 standard backgrounds byte-for-byte, proves 84/84 ≥ 4.5:1 with best-of text, and pins the HC group separately so the owner's D-27 decision can re-author HC entries without ever loosening the standard pin.

Purpose: the adaptive text colour (plan 16-15) must be a pure function of a frozen background; this plan makes both halves testable.
Output: `Color+Contrast.swift`, `ColorContrastTests.swift`, `CategoryColorsetInvariantTests.swift`, green on the spare simulator.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` (this plan) — `extension Color.Resolved { public var relativeLuminance: Double; public func composited(over backdrop: Color.Resolved, opacity: Double) -> Color.Resolved }`, `extension Color { public static func contrastRatio(_ a: Color.Resolved, _ b: Color.Resolved) -> Double; public static func contrastingForeground(on background: Color.Resolved) -> Color; public func contrastingForeground(in environment: EnvironmentValues) -> Color }`; adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); `OSLogEntryLog.Level.symbol` in `SettingFeature` (plan 16-22); D-28 colour fixes + rating-star decision (plan 16-23); accessibility semantics and Reduce Motion gating (plans 16-16 … 16-21).
- Tests: `AppPackage/Tests/AppToolsTests/ColorContrastTests.swift` (`ColorContrastTests`) and `AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift` (`CategoryColorsetInvariantTests`) (this plan); `EhPandaUITests/AccessibilityAuditUITests.swift` (plan 16-24).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `Color+Contrast.swift` helper with `ColorContrastTests`</name>
  <files>AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift, AppPackage/Tests/AppToolsTests/ColorContrastTests.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md` § "`AppPackage/Sources/AppTools/Color+Contrast.swift`" and § "`ColorContrastTests.swift`" — module placement, extension shape, the core pattern, the parameterised-fixture shape, sorted imports.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Pattern 5: Adaptive badge text colour" and § Contrast › Formula (crossover derivation), Pitfalls 8–9.
    - `AppPackage/Sources/AppTools/Extensions/URL+ImageCacheKey.swift` — public computed-property extension with a WHY doc comment (the shape to copy).
    - `AppPackage/Sources/AppTools/ColorCodable.swift` — the existing `Color` extension in `AppTools` (`import SwiftUI` is already a module convention).
    - `AppPackage/Tests/AppToolsTests/GalleryURLParserTests.swift` — the test-file shape in this target (`import AppTools` / `CustomDump` / `Foundation` / `Testing`, `@Test(arguments:)`, `CustomTestStringConvertible` fixtures).
    - `.swiftlint.yml` — `labeled_tuple_elements`, `single_line_trailing_closure`, `sorted_imports`, `line_length`.
  </read_first>
  <behavior>
    - `relativeLuminance` of `Color.Resolved(colorSpace: .sRGBLinear, red: 0, green: 0, blue: 0)` is 0; of linear (1, 1, 1) is 1; of linear (0.1791, 0.1791, 0.1791) is 0.1791 ± 1e-9 (it reads the linear fields directly).
    - `contrastRatio(white, black)` is 21 ± 1e-6 and is symmetric; `contrastRatio(crossoverGray, white)` and `contrastRatio(crossoverGray, black)` are both 4.583 ± 0.001.
    - `contrastingForeground(in:)` resolved through `EnvironmentValues()`: for `Color(.sRGBLinear, red: 0.1791, …)` → `.white` (tie resolves to white, strict `>`); for linear 0.1792 gray → `.black`; for linear 0.1790 gray → `.white`; for the worst real variant (ExHentai Game CG light — take its sRGB components from `App/Assets.xcassets/Category/Colors/ExHentai/Game CG.colorset/Contents.json`, construct with `.sRGB`) → `.black`, with `contrastRatio` vs black 4.62 ± 0.01 and vs white 4.55 ± 0.01.
    - Black and white inputs: `.black` background → `.white`; `.white` background → `.black`.
    - `Color.Resolved.composited(over:opacity:)`: linear-space alpha compositing — the crossover gray at opacity 0.3 over linear white gives linear channels 0.3 × 0.1791 + 0.7 × 1.0 = 0.75373 ± 1e-6; opacity 1 returns the receiver's channels; opacity 0 returns the backdrop's. `Color.contrastingForeground(on:)` (static, takes a `Color.Resolved`) agrees with the instance method for the same resolved colour.
  </behavior>
  <action>
RED: write `ColorContrastTests.swift` first (suite `ColorContrastTests`, parameterised fixtures with `CustomTestStringConvertible`, `expectNoDifference` / `#expect(abs(a - b) < tolerance)`), run it, observe the compile failure / RED. GREEN: create `Color+Contrast.swift` under `AppTools/Extensions/` with `extension Color.Resolved { public var relativeLuminance: Double }` computing `0.2126 * Double(linearRed) + 0.7152 * Double(linearGreen) + 0.0722 * Double(linearBlue)` (doc comment: `Color.Resolved` already stores linearised sRGB, so no transfer-function decode — decoding twice is the classic bug); `extension Color { public static func contrastRatio(_ a: Color.Resolved, _ b: Color.Resolved) -> Double }` returning `(hi + 0.05) / (lo + 0.05)` with `hi`/`lo` ordered (a `(hi, lo)` destructure is fine; no unlabeled tuple type); `public static func contrastingForeground(on background: Color.Resolved) -> Color` returning `background.relativeLuminance > 0.1791 ? .black : .white`, and the instance convenience `public func contrastingForeground(in environment: EnvironmentValues) -> Color` that resolves `self` and calls the static — with a doc comment stating the WHY: the better-of rule, the crossover L = √0.0525 − 0.05 ≈ 0.1791 where both ratios equal ≈ 4.583, hence a structural floor of 4.58:1 for any background, and why "white unless it fails" was rejected (no floor, different flip count). Name the threshold as a `private static let` with the derivation in its doc comment. Also add `extension Color.Resolved { public func composited(over backdrop: Color.Resolved, opacity: Double) -> Color.Resolved }` — linear-space source-over (`opacity × self + (1 − opacity) × backdrop` per linear channel, result in `.sRGBLinear` with opacity 1) — for the Filters `CategoryCell`, whose excluded state draws the category colour at 0.3 over the sheet background (the text colour must be chosen against the composite, not the raw colour); doc-comment why the blend is linear. Run the test target on the spare simulator; REFACTOR if needed. Commit `feat(16-14): add Color contrast helper` (source + test together is fine; or two commits per the RED/GREEN protocol).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:AppToolsTests/ColorContrastTests 2>&1 | grep -E "Test Suite|passed|failed|error:" | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` exists; `grep -c "linearRed" AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` ≥ 1 and `grep -c "0.1791" AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` ≥ 1.
    - `ColorContrastTests` passes on `88B217DA-A166-4BAD-820D-DE13B1C4EB54` with ≥ 8 test cases (black/white, crossover tie, one step above, one step below, worst variant, symmetry, composited at 0.3 / 1 / 0, static-vs-instance agreement).
    - The RED run (test failing before the helper existed) is recorded in the summary.
    - `xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator'` → `** BUILD SUCCEEDED **` (lint plugin green; no `swiftlint:disable`).
  </acceptance_criteria>
  <done>The helper is the single, tested source of the adaptive text rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `CategoryColorsetInvariantTests` — 84/84, worst 4.62, 47 flips, standard-44 and HC-40 hash pins</name>
  <files>AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` — `repositoryRoot()`, `isRepositoryRoot(_:)`, `repositoryRelativePath(of:under:)`, the enumerator loop, `knownMembers` guard, equality pins (copy the helpers; the other target's private helpers cannot be imported).
    - `.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md` § "`CategoryColorsetInvariantTests.swift`" and § "Repository-walk tests refuse vacuous passes" (its target note recommends `AppModelsTests`; this plan deviates — see the action).
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Pattern 6: Colorset invariant test" and § Contrast › "Colorset facts the test must handle" (three encodings; appearance keys).
    - `App/Assets.xcassets/Category/Colors/ExHentai/Cosplay.colorset/Contents.json` (decimal-encoded components) and `App/Assets.xcassets/Category/Colors/E-Hentai/Private.colorset/Contents.json` (light/dark only — the 2 × 2 exception that makes 84, not 88).
    - `AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` (from Task 1) — the helper the test routes luminance through.
  </read_first>
  <behavior>
    - Parsing: walking `App/Assets.xcassets/Category/Colors/E-Hentai` and `…/ExHentai` yields exactly 22 `Contents.json` files and exactly 84 variants; `knownMembers` includes `ExHentai/Game CG.colorset/Contents.json` and `E-Hentai/Manga.colorset/Contents.json`; an empty walk fails via `#require`.
    - Encoding: a private normaliser maps `"0xFF"`, `"1.000"` and `"255"` all to 1.0 (and `"0x00"`, `"0.000"`, `"0"` to 0.0); every variant's `color-space` is `srgb`.
    - Contrast: for each variant, luminance via `Color.Resolved(colorSpace: .sRGB, red:green:blue:opacity: 1).relativeLuminance`, best-of = max(ratio vs white, ratio vs black) ≥ 4.5 for all 84 (assert per variant with the variant name in the failure message); the minimum best-of is 4.62 ± 0.01 and belongs to `ExHentai / Game CG / light`; the count of variants with L > 0.1791 is exactly 47.
    - Pins: two SHA-256 hex digests over canonical serializations (`<host>/<category>/<appearance>:<r>,<g>,<b>` lines, normalised channels printed with 6 decimals, sorted) — one over the 44 standard variants (no `contrast: high`), one over the 40 HC variants — equal pinned constants computed at authoring time; changing any standard byte fails the standard pin.
  </behavior>
  <action>
Target deviation (recorded here, not in PATTERNS): the suite lives in `AppToolsTests`, not the `AppModelsTests` the PATTERNS target note recommends, because it routes every luminance through the Task 1 helper in `AppTools` (`import AppTools` is a declared dependency of `AppToolsTests`; `AppModelsTests` declares only `AppModels`), it never calls `Category.color(host:)` (it reads the colorset JSON from disk), and keeping it beside `ColorContrastTests` puts both contrast tests in the target that owns the maths; no new test target and no new `.swiftlint.yml` are needed either way. RED: write the suite with placeholder pin constants (empty strings) and run it to observe the pin assertions fail; then compute the two digests from the test's own canonical serialization (print them once from a temporary `#expect` message or a scratchpad script that reproduces the serialization exactly) and pin them. Use `import CryptoKit` for `SHA256`; `JSONSerialization` or a small `Codable` model for `colors[].appearances[]` (`appearance: luminosity|contrast`, `value: dark|high`) and `colors[].color.components` (strings). Appearance classification: no `appearances` → light; `luminosity: dark` only → dark; `contrast: high` only → light+HC; both → dark+HC. Route luminance through the Task 1 helper (so a helper regression fails here too). Doc-comment the suite header with the WHY: the standard pin encodes D-26 (background is the identity, foreground is derived); the HC pin exists so D-27 re-authoring can be adopted deliberately without loosening the standard pin. No `try?`, no force unwrap, sorted imports, 120 columns. Run the test target on the spare simulator, then the full `FeatureTests` plan once (one `xcodebuild test` at a time). Commit `test(16-14): pin category colorset invariants`.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:AppToolsTests 2>&1 | grep -E "Test Suite|passed|failed|error:" | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `CategoryColorsetInvariantTests` passes on the spare simulator; `grep -c "SHA256" AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift` ≥ 1; `grep -c "4.62\|4\.62" AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift` ≥ 1; `grep -c "47" AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift` ≥ 1; two distinct 64-hex pin constants are present.
    - The RED run (placeholder pins failing) is recorded in the summary together with the two digests.
    - Mutation check recorded in the summary: temporarily changing one standard component (e.g. E-Hentai Manga light red channel) makes the standard pin fail and the HC pin pass; the change is reverted and `git status --porcelain` shows no colorset modified.
    - Full `FeatureTests` plan green on `88B217DA-A166-4BAD-820D-DE13B1C4EB54` (summary records the pass line); `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`.
  </acceptance_criteria>
  <done>The 84-variant table is a permanent test; the standard backgrounds are pinned; HC can be re-pinned deliberately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset catalog → app rendering | A background colour edit silently changes contrast; the pins make it loud. |
| Test → filesystem | The repository-root walk must refuse vacuous passes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | category background bytes | medium | mitigate | Standard-44 SHA-256 pin; mutation check recorded; HC-40 pinned separately. |
| T-16-15 | Tampering (accidental) | luminance math on gamma channels | medium | mitigate | Helper reads `linear*` fields; unit test pins the crossover and the worst variant; invariant test routes through the helper. |
| T-16-03 | Denial of service (phase infrastructure) | sweep simulators | medium | mitigate | Tests run on `88B217DA…` only. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. `ColorContrastTests` RED → GREEN; `CategoryColorsetInvariantTests` RED (placeholder pins) → GREEN with two pins; mutation check.
2. Full `FeatureTests` plan green on the spare simulator; lint build green; two commits.
</verification>

<success_criteria>
- The adaptive-text rule and the frozen-background invariant are both code-tested before any view changes (D-26), with the HC group pinned separately for the owner's D-27 decision.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-14-SUMMARY.md` when done: helper API, RED/GREEN evidence, the two pin digests, mutation-check outcome, test-run lines, commit hashes.
</output>
