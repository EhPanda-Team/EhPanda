---
phase: 16-dynamic-type-accessibility
plan: 19
type: execute
wave: 17
depends_on: ["16-18"]
files_modified:
  - AppPackage/Sources/AppComponents/ToolbarItems.swift
  - AppPackage/Sources/AppComponents/RatingView.swift
  - AppPackage/Sources/AppComponents/TagCloudView.swift
  - AppPackage/Sources/AppComponents/ViewModifiers.swift
  - AppPackage/Sources/AppComponents/TagSuggestionView.swift
  - AppPackage/Sources/AppComponents/ListNoticeView.swift
  - AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "The three shared toolbar menus (favorites index, toplists type, sort order) and the Downloads filter menu announce selection as a trait — `Picker` inside the `Menu` where it renders at parity, otherwise `.isSelected` on the selected button with the checkmark image hidden — so VoiceOver says 'selected' instead of reading a checkmark glyph (D-20)."
    - "`RatingView` is one element: children ignored, label `accessibility.rating`, value from `accessibility.rating_value` carrying the half-star rating, so every list cell and the Detail header read 'Rating, 4.5 out of 5' once (the star colour itself follows the owner's `STARS=` decision in plan 16-22) (D-20, D-22)."
    - "Decorative glyphs in shared components are hidden: the `TagCloudView` opacity-0 spacer image, the `withArrow` chevron in `ViewModifiers`, the list-notice glyph beside its text, the trailing `photoOnRectangleAngled` symbols in `GalleryDetailCell` / `GalleryThumbnailCell` (the page count is text beside them) (D-20)."
    - "Custom tappables become native: the `TagSuggestionView` row is a `Button`; `DownloadListRow` (already `.isButton` + label) gains named `accessibilityAction`s mirroring its swipe actions — inspect, move, update, pause/resume, delete — using the same `LocalizedStringResource`s the swipe buttons show, so Voice Control can say the action by name (Apple's VC criterion 2) (D-20, D-30)."
    - "Tag cells' `.contextMenu` items (copy / search / translation) are mirrored as named `accessibilityAction`s only under the owner's `CONTEXTMENU=not-exposed` observation; under `exposed` nothing is added and the reason is recorded (D-22)."
    - "Every gallery list cell is announced as a single element with title, category, uploader, rating and download status, verified in the AX snapshot (combine children only if the `Button`/`NavigationLink` label does not already do it) (D-20)."
    - "No hardcoded accessibility strings; no over-labelling; menu conversions are pixel-parity at `.large` or fall back to the trait path; nothing rendered changes, so no D-25 screen is added (D-24, D-30)."
    - "Precision: the rating value presents the half-star value exactly as the stars render (`rawRating.halfRounded`, e.g. 4.5), never the raw float."
  artifacts:
    - path: "AppPackage/Sources/AppComponents/RatingView.swift"
      provides: "single-element rating with label + value"
      contains: "accessibilityValue"
    - path: "AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift"
      provides: "named actions mirroring swipe actions on DownloadListRow"
      contains: "accessibilityAction"
    - path: "AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings"
      provides: "accessibility.rating, accessibility.rating_value (six locales)"
      contains: "accessibility.rating"
  key_links:
    - from: "AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift (DownloadListRow)"
      to: "AppPackage/Sources/DownloadsFeature/DownloadsView.swift (swipe action buttons)"
      via: "the named actions invoke the same closures / store sends as the swipe buttons and use the same title resources — one source of truth for names"
      pattern: "accessibilityAction"
  prohibitions:
    - statement: "Never pass a string literal or `Text(` to an accessibility label / value / hint / input-label modifier (D-30; lint-enforced)."
      status: resolved
      verification: test
    - statement: "Never add an `accessibilityLabel` to a control that already has visible text (over-labelling)."
      status: resolved
      verification: judgment
    - statement: "Never touch the `.lineLimit(1)` sites or any layout in the list cells — round-1 owner territory (D-01)."
      status: resolved
      verification: judgment
---

<objective>
The VoiceOver / Voice Control pass over the shared components and the Home / Search / Downloads surfaces (screens #2–#12, #39 cells, and every list host): selection-as-trait in the four checkmark menus, a single-element `RatingView` with label and value, hidden decorative glyphs, a native tag-suggestion row, named actions for the download row's swipe actions, and context-menu mirrors only if the owner's device check says they are needed.

Purpose: criteria 7 and 8 for browsing, searching and download management — the common tasks the Nutrition Label requires beyond the main screen.
Output: eleven files edited across AppComponents, GalleryListComponents and DownloadsFeature; two catalog keys; AX-tree evidence.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); Detail / Setting / reader semantics (plans 16-16 … 16-18); shared-component, list-cell and download-row semantics (this plan: menu selection traits, `RatingView` label + value, hidden glyphs, `TagSuggestionView` button, `DownloadListRow` named actions); `OSLogEntryLog.Level.symbol` (plan 16-22); Reduce Motion gating (plans 16-20, 16-21).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys: AppComponents `accessibility.rating`, `accessibility.rating_value` (this plan); others per plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voiceover-swiftui.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voice-control.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolbar menus (selection as trait) and `RatingView` (label + value)</name>
  <files>AppPackage/Sources/AppComponents/ToolbarItems.swift, AppPackage/Sources/AppComponents/RatingView.swift, AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings</files>
  <read_first>
    - `AppPackage/Sources/AppComponents/ToolbarItems.swift` lines 125–200 — `FavoritesIndexMenu`, `ToplistsTypeMenu`, `SortOrderMenu`: each a `Menu` of `Button`s with `Text(…)` + conditional `Image(systemSymbol: .checkmark)`, labelled by a `Label(.key, systemSymbol:)`.
    - `AppPackage/Sources/AppComponents/RatingView.swift` — `HStack` of star images driven by `rawRating.halfRounded`; the `#Preview` with the slider.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Don't Hand-Roll" (Picker in Menu — verify parity), § "Code Examples" (the `RatingView` example), § Pattern 2 (named substitutions), and `16-CONTRAST-AUDIT.md § Decisions` (`STARS=`).
    - `AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings` key `accessibility.downloading` — the named-substitution template.
    - `AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings` — this module's key style.
  </read_first>
  <action>
Menus: for each of the three menus, replace the `ForEach` of checkmark `Button`s with a `Picker(selection:)` over the same cases (`Text(case.value).tag(case)`) using `.pickerStyle(.inline)` inside the `Menu`, keeping the outer `Label` as the menu label; take `--scale 1` before/after screenshots of each open menu at `content_size large` on `IPHONE_UDID` (install-over) and keep the `Picker` only where the rendering is identical — otherwise restore the buttons and add `.accessibilityAddTraits(.isSelected)` to the selected one plus `.accessibilityHidden(true)` on its checkmark image; record the path per menu. `RatingView`: add `.accessibilityElement(children: .ignore)`, `.accessibilityLabel(.accessibilityRating)` and `.accessibilityValue(.accessibilityRatingValue(rating: …))` where the value is the half-rounded rating the stars show; author `accessibility.rating` (plain) and `accessibility.rating_value` in six locales — the rating is numeric, so use a named `%#@rating@` substitution; if the catalog/`xcstrings` generator cannot express a non-integer substitution (test by building: the generated symbol must accept the rating type), fall back to a positional `%@` taking the rating pre-formatted by `rating.formatted(.number.precision(.fractionLength(0...1)))` and doc-comment why (a localized, already-formatted number is a string argument). Leave the `#Preview` as is. Build; commit `feat(16-19): selection traits in menus; rating semantics`.
  </action>
  <verify>
    <automated>grep -c "Picker(\|isSelected" AppPackage/Sources/AppComponents/ToolbarItems.swift; grep -c "accessibilityRating" AppPackage/Sources/AppComponents/RatingView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Picker(\|isSelected" …/ToolbarItems.swift` ≥ 3 (one mechanism per menu); any remaining checkmark `Image` in that file is followed by `.accessibilityHidden(true)`.
    - `grep -c "accessibilityElement(children: .ignore)" …/RatingView.swift` prints `1`; `grep -c "accessibilityRating" …/RatingView.swift` ≥ 2 (label + value).
    - The AppComponents catalog has `accessibility.rating` and `accessibility.rating_value` in six locales (`missing locales: []`); the value key's argument shape is recorded in the summary (named substitution or the documented positional fallback).
    - Before/after screenshot paths for the three menus in the summary with the per-menu path; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Menus announce selection; ratings read once with a value.</done>
</task>

<task type="auto">
  <name>Task 2: Decorative glyphs hidden; `TagSuggestionView` row native; tag context-menu mirrors per decision</name>
  <files>AppPackage/Sources/AppComponents/TagCloudView.swift, AppPackage/Sources/AppComponents/ViewModifiers.swift, AppPackage/Sources/AppComponents/TagSuggestionView.swift, AppPackage/Sources/AppComponents/ListNoticeView.swift</files>
  <read_first>
    - `AppPackage/Sources/AppComponents/TagCloudView.swift` — the `Image(systemSymbol: .photo).opacity(0)` spacer (line ~117), the tag cells (how they are tapped — `Button` or gesture) and their `.contextMenu`.
    - `AppPackage/Sources/AppComponents/ViewModifiers.swift` lines 30–45 — the `withArrow` chevron.
    - `AppPackage/Sources/AppComponents/TagSuggestionView.swift` lines 90–130 — the suggestion row (`Image`s at `:94,:101`, `.onTapGesture` at `:122`).
    - `AppPackage/Sources/AppComponents/ListNoticeView.swift` — the glyph beside the notice text.
    - `16-CONTRAST-AUDIT.md § Decisions` — `CONTEXTMENU=`.
  </read_first>
  <action>
`TagCloudView`: `.accessibilityHidden(true)` on the opacity-0 spacer image; if the tag cells are gesture-tapped, convert to `Button(.plain)`; under `CONTEXTMENU=not-exposed` mirror each context-menu item as `.accessibilityAction(named: <the same resource the menu button uses>, …)` on the cell (under `exposed`, add nothing and record). `ViewModifiers`: `.accessibilityHidden(true)` on the `withArrow` chevron (the row's text is the element). `TagSuggestionView`: replace the row's `.onTapGesture` with a `Button(.plain)` keeping the layout; the leading symbol images become `.accessibilityHidden(true)` if the row's text already conveys them, else combine. `ListNoticeView`: hide the glyph or combine with the text. Build; commit `feat(16-19): hide decorative glyphs; native suggestion row`.
  </action>
  <verify>
    <automated>grep -c "accessibilityHidden(true)" AppPackage/Sources/AppComponents/TagCloudView.swift AppPackage/Sources/AppComponents/ViewModifiers.swift AppPackage/Sources/AppComponents/ListNoticeView.swift; grep -c "onTapGesture" AppPackage/Sources/AppComponents/TagSuggestionView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityHidden(true)"` ≥ 1 in each of `TagCloudView.swift`, `ViewModifiers.swift`, `ListNoticeView.swift` (or `children: .combine` in `ListNoticeView.swift`).
    - `grep -c "onTapGesture" …/TagSuggestionView.swift` prints `0`.
    - Under `CONTEXTMENU=not-exposed`: `grep -c "accessibilityAction(named:" …/TagCloudView.swift` ≥ 1; under `exposed`: `0` with the reason in the summary.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Shared components no longer announce decoration; suggestion rows are buttons; tag actions are reachable.</done>
</task>

<task type="auto">
  <name>Task 3: List cells and download rows — hidden glyphs, single-element cells, named swipe actions, Downloads menu, verification</name>
  <files>AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift</files>
  <read_first>
    - `AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift` (trailing `photoOnRectangleAngled` glyph, the `RatingView`, `CategoryLabel`, `DownloadBadgeLabel`) and `GalleryThumbnailCell.swift` (the footnote glyph at `:89`).
    - `AppPackage/Sources/DownloadsFeature/DownloadsView.swift` lines 120–235 — the filter `Menu` with a checkmark item (`:158`), the leading/trailing `.swipeActions` (`inspectButton`, move, `updateButton`, pause/resume, delete — note the role-less red-tinted delete from Phase 15 plan 77 and its comment), `downloadContextMenu()`.
    - `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` lines 385–415 — `DownloadListRow` (`.allowsHitTesting(false)` cell, `.contentShape`, `.onTapGesture(perform: openAction)`, `.accessibilityAddTraits(.isButton)`, `.accessibilityLabel(download.title)`).
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadsSwipeActionSourceTests.swift` — the region-scoped source test that pins the swipe block (do not break its assertions).
  </read_first>
  <action>
Cells: `.accessibilityHidden(true)` on the trailing page glyphs in both cells; confirm in the snapshot that a list cell reads as one element (title, category, uploader, rating value, download badge label) — if children are read separately, add `.accessibilityElement(children: .combine)` on the cell's content (never on the list). Downloads filter menu: same `Picker`-or-trait treatment as Task 1 for the checkmark item. `DownloadListRow`: add `.accessibilityAction(named: …)` for each swipe action the row offers — pass the same title resources and the same actions the `swipeActions` buttons use (thread them from `DownloadsView` into the row, or build the actions where the row is constructed) so names match Voice Control expectations; keep the existing `.isButton` + title label. Build; run `DownloadsFeatureTests` on the spare simulator (`DownloadsSwipeActionSourceTests` and `DownloadSchedulingTests` must stay green — a failure there is a real regression). Install-over on `IPHONE_UDID`; `agent-device snapshot -i --actions` on Frontpage (a cell), Favorites (index menu), Toplists (type menu), Search results (sort menu), Downloads (a row with actions, the filter menu); record excerpts. Commit `feat(16-19): list cell and download row semantics`.
  </action>
  <verify>
    <automated>grep -c "accessibilityAction(named:" AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift AppPackage/Sources/DownloadsFeature/DownloadsView.swift; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:DownloadsFeatureTests 2>&1 | grep -E "passed|failed|error:" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityAction(named:"` over `DownloadsView+Subviews.swift` + `DownloadsView.swift` ≥ 3 (at least inspect / pause-or-resume / delete).
    - `grep -c "accessibilityHidden(true)"` ≥ 1 in each of `GalleryDetailCell.swift` and `GalleryThumbnailCell.swift`.
    - `DownloadsFeatureTests` passes on the spare simulator (including `DownloadsSwipeActionSourceTests` and `DownloadSchedulingTests`).
    - The summary has snapshot excerpts showing: a cell as one element with rating value; the three toolbar menus' selected state; the download row's named actions.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>Browsing lists and managing downloads work by name under VoiceOver and Voice Control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Named actions → download operations | Actions call the same closures as the swipe buttons; delete still goes through the confirmation alert (manifest-SSOT and folder invariant untouched). |
| Simulator install → owner's data container | Install-over only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-19 | Tampering (accidental) | a named delete action bypassing the alert | high | mitigate | Actions invoke the same `deleteDownloadButtonTapped`-style sends as the swipe button (alert first); `DownloadsFeatureTests` green. |
| T-16-16 | Tampering (accidental) | Voice Control name ≠ visible text | medium | mitigate | Same resources as the visible buttons; lint rules. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | Install-over by UDID; no uninstall/erase. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps per task; menu parity screenshots; lint build green.
2. `DownloadsFeatureTests` green on the spare simulator; AX snapshot excerpts; three commits.
</verification>

<success_criteria>
- Shared components, list hosts and Downloads meet criteria 7 and 8: selection as traits, ratings as values, decoration hidden, custom rows native, swipe actions nameable (D-20, D-30).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-19-SUMMARY.md` when done: per-site changes with reasons, menu paths, the rating value argument shape, CONTEXTMENU outcome, snapshot excerpts, commit hashes.
</output>
