---
phase: 16-dynamic-type-accessibility
plan: 04
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every in-scope Group A surface (#1–#13: tab bar shell, Home, Frontpage, Popular, Watched, History, Toplists, Favorites, Search root, Search results, Downloads, Downloads inspector, FolderManager) has six iPhone rows (portrait + landscape × XXL / AX3 / AX5) that are no longer `pending` (D-05, D-10)."
    - "Each cell was judged from screenshots after scrolling to the bottom, against the owner's rule — degraded means the interface provides less information at a larger font size; decoration dropped to save space is fine, contents dropped is a finding (D-03)."
    - "Any value that reads in full at `.large` but clips, ellipsises or abbreviates at XXL / AX3 / AX5 is a finding regardless of whether the field is primary or secondary — Phase 10's secondary-text exemption is not applied; the D-04 checklist rows whose screen is in Group A are dispositioned `fine` or `finding:#N` (D-04)."
    - "Findings are recorded and the sweep moves on; nothing interrupts the walk to raise a finding; each finding carries a written description and an evidence-root screenshot path (under `$HOME/Library/Caches/ehpanda-phase16/`) sent to the owner in chat, never a committed image (D-02, D-32, D-33)."
    - "The two D-13 rows that render in Group A — hero-carousel title truncation (#2) and Favorites trailing-glyph clip (#8) — carry an `Observed` note from the AX5 cells (D-13)."
    - "AX5 is the largest size sampled and the three sizes are exactly XXL / AX3 / AX5 via the `content_size` tokens recorded in the protocol; no `xSmall` or Bold Text cell is walked (D-05, D-06, D-07)."
    - "The sweep simulator is driven by `IPHONE_UDID` from `16-SWEEP.md § Infrastructure`, the app by `BUNDLE_ID`; nothing erases, resets or reinstalls that simulator, and `content_size` / orientation are restored to the recorded baseline at the end of the session (D-08, D-09)."
    - "Adjacency: a label that exactly fills its width at AX5 — touching the edge, not clipped — is `pass` under D-03; only clipping, ellipsis, overlap or an unreachable control is a finding."
    - "Encoding: verdicts are judged on rendered glyphs in the screenshot, never on the AX snapshot's string length (a truncated `Text` still reports its full label)."
    - "Empty: the empty states of Favorites / Downloads / History / Search history are walked and judged as rows if the simulator shows them; a content-free screen is still a row."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Group A × iPhone matrix rows filled; Findings entries; D-04 and D-13 rows updated"
      contains: "iPhone"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Matrix"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Findings"
      via: "every `finding:#N` cell references a numbered Findings entry with a written description"
      pattern: "finding:#"
  prohibitions:
    - statement: "No screenshot or any image file is ever added to the repository; every screenshot path starts with the evidence root `$HOME/Library/Caches/ehpanda-phase16/` (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow code and no reflow work order during the sweep — record and move on (D-01, D-02)."
      status: resolved
      verification: judgment
    - statement: "Never address the simulator as `booted`; never erase / uninstall / clear-app-state the sweep simulator (D-09)."
      status: resolved
      verification: judgment
---

<objective>
Walk the first third of the inventory on the iPhone: surfaces #1–#13 (Group A — the tab shell, Home and its pushed lists, Favorites, Search, Downloads and their sheets), six cells each (portrait + landscape × XXL / AX3 / AX5), scrolling every screen to the bottom, judging each cell against D-03 / D-04, recording findings without interrupting, and filling the matrix rows in `16-SWEEP.md`.

Purpose: round 1 is the owner's to fix and the agent's to verify; this is the verification, on the harshest device/orientation pairs first (iPhone landscape at AX5 has the least vertical space).
Output: 78 iPhone rows (13 surfaces × 6 cells) non-`pending`, numbered findings with written descriptions, D-04 rows for Group A screens dispositioned, D-13 rows #2 and #8 observed.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (rows filled here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iPhone × Group A, surfaces #1–#7 (tab bar, Home, Frontpage, Popular, Watched, History, Toplists)</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Infrastructure (`IPHONE_UDID`, `BUNDLE_ID`, baselines, forbidden list), § Verdict rule, § Protocol (the per-screen procedure — follow it verbatim), § Inventory rows #1–#7 (routes), § Matrix rows for these screens, § D-04 checklist rows mapped to screens #2, #6, #7, § D-13 row "hero-carousel title truncation".
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Round-1 verification mechanics" (token table, driver choice, Pitfalls 1–3, 12) and § Screen inventory rows 1–7.
    - `$HOME/.claude/skills/agent-device/SKILL.md` — session, `snapshot -i`, `press @ref`, `scroll down … --settle`, `screenshot --out … --scale 0.5`, `orientation`.
  </read_first>
  <action>
Start a session per § Protocol: read and note the current `content_size` / orientation of `IPHONE_UDID` (they must equal the recorded baseline — if not, record the discrepancy and continue), then `agent-device open <BUNDLE_ID> --platform ios --udid <IPHONE_UDID> --foreground`. For each surface #1–#7 in order: navigate via the inventory route (Home tab; Home › the "Show All"/section push for Frontpage, Popular, Watched, History, Toplists; open the Toplists type menu as its own cell set since menus reflow at AX5); for the tab bar shell (#1) judge the tab bar itself at each cell. For each of the six cells — `agent-device orientation portrait` then `landscape-left`, and within each `xcrun simctl ui <IPHONE_UDID> content_size extra-extra-extra-large`, then `accessibility-extra-large`, then `accessibility-extra-extra-extra-large` (always the UDID, never `booted`) — wait for re-layout (A1 per pre-flight; use `agent-device open … --relaunch` only if pre-flight recorded that relaunch is required), screenshot the top (`--scale 0.5`, path `$EVIDENCE_ROOT/sweep/iphone-<orientation>-<size>-<screen#>-top.png`), scroll to the bottom with repeated `agent-device scroll down --settle` (the Home root is long: keep scrolling until the settle diff reports no change), screenshot the bottom, and judge: `pass`, or `finding:#N`. A finding is any clipped / ellipsised / abbreviated value that reads in full at `.large`, any overlap, any control pushed off-screen or unreachable; wrapping to more lines, taller rows, fewer rows per screen and dropped decoration are `pass` (D-03, D-04). For the hero carousel (#2) record under the D-13 row's `Observed` column what the title does at AX5 portrait and landscape (wraps within `lineLimit(4)`, or ellipsises — the latter is the pre-registered case). For Watched (#5): with the session present walk the list; if the login placeholder shows, judge the placeholder as the row's content and note `login placeholder` in the Finding column (not a finding by itself). Record each finding immediately in § Findings with `#N`, screen, cells affected, a written description (what text, which value, which size/orientation; no gallery titles, no gids, no filenames that imply repo recoverability), and status `open`; then move on (D-02). Disposition the D-04 checklist rows for screens #2, #6, #7 (`GalleryRankingCell.swift:39`, `GalleryHistoryCell.swift:32`; list cells shared across hosts are recorded once and tagged `all list hosts`). After #7, commit `16-SWEEP.md` with `docs(16): sweep iPhone group A rows 1-7` after the image check. If context approaches ~50% before #7, commit what is done and stop; the plan resumes at the first `pending` row.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 1 2 3 4 5 6 7; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPhone" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 1–7, the six iPhone matrix rows have status ∈ {`pass`, `finding:#N`} — `grep -E "^\| *<n> *\| .*iPhone" 16-SWEEP.md | grep -c "| pending"` prints `0` for every n in 1…7.
    - Every `finding:#N` in those rows has a matching `#N` entry in § Findings with a non-empty written description and status `open`.
    - The D-13 row "hero-carousel title truncation" has a non-empty `Observed` note naming AX5 portrait and landscape.
    - D-04 rows mapped to screens #2, #6, #7 are no longer `pending`.
    - `grep -c "/Users/" 16-SWEEP.md` prints `0`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPhone group A rows 1-7` exists.
  </acceptance_criteria>
  <done>Surfaces #1–#7 verified on the iPhone at all six cells, findings numbered, table committed.</done>
</task>

<task type="auto">
  <name>Task 2: iPhone × Group A, surfaces #8–#13 (Favorites, Search root, Search results, Downloads, Inspector, FolderManager) and session restore</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Protocol, § Inventory rows #8–#13, § Matrix rows for these screens, § D-04 rows mapped to screens #8–#13 (`GalleryDetailCell.swift:107,152,163` uploader/stats — back in scope under D-04 — `DownloadBadgeLabel.swift:19`, `GalleryThumbnailCell.swift:99`), § D-13 row "Favorites trailing-glyph clip".
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 8–13 and § D-04 checklist.
    - `AGENTS.md` § "Download folders are user-owned" — never delete a download the sweep did not create.
  </read_first>
  <action>
Continue the session. Favorites (#8): walk the root list, open the favorites-index menu and the sort menu as their own cells (menus reflow at AX5), and record under the D-13 row "Favorites trailing-glyph clip" what the trailing glyphs in the toolbar/menu and the `GalleryDetailCell` trailing symbol (`photoOnRectangleAngled`) do at AX5 in both orientations. Search root (#9): with history keywords and quick-search chips visible (type and submit one harmless keyword if the history is empty, so the chips and history rows exist). Search results (#10): submit a search that returns results and walk the list. Downloads (#11): walk the root; if no download exists, start one small download from a gallery on the Frontpage so the row, its swipe actions (swipe a row open as a cell — judge the swipe-action labels), and the confirm dialogs (open the delete confirmation and cancel it — judge the dialog as a cell, then cancel) exist; never delete an existing download, and never delete outside the app the folder the sweep created (AGENTS.md invariant). Inspector sheet (#12): open it from the row, walk to the bottom. FolderManager (#13): open Move-to-folder from the row action (login-gated; if unreachable record `blocked: <reason>`). Six cells each, same procedure: orientation × size, top and bottom screenshots at `--scale 0.5` into `$EVIDENCE_ROOT/sweep/`, judge against D-03 / D-04 (the uploader line `GalleryDetailCell.swift:107` and the stats `:152,:163` are explicitly back in scope — an ellipsised uploader at AX3 is a finding even though it was "fine" in Phase 10), record findings and move on. Disposition the D-04 rows mapped to these screens. At the end of the session restore `xcrun simctl ui <IPHONE_UDID> content_size <baseline>` and `agent-device orientation portrait`, re-read `content_size` to prove the restore, and commit with `docs(16): sweep iPhone group A rows 8-13` after the image check. The summary lists every finding number opened in this plan with its one-line description and the evidence-root paths of its screenshots (D-33), and names the download the sweep created (by folder name only) so the owner knows it is sweep-made.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 8 9 10 11 12 13; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPhone" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 8–13, the six iPhone matrix rows have status ∈ {`pass`, `finding:#N`, `blocked: <reason>`} with `blocked` only on #13 when the session cannot reach it — `grep -E "^\| *<n> *\| .*iPhone" 16-SWEEP.md | grep -c "| pending"` prints `0` for every n in 8…13.
    - The D-13 row "Favorites trailing-glyph clip" has a non-empty `Observed` note.
    - D-04 rows for `GalleryDetailCell.swift:107`, `:152`, `:163`, `DownloadBadgeLabel.swift:19`, `GalleryThumbnailCell.swift:99` are no longer `pending`.
    - `xcrun simctl ui <IPHONE_UDID> content_size` prints the baseline recorded in § Infrastructure after the restore.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPhone group A rows 8-13` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>Group A is fully verified on the iPhone; the simulator is back at its baseline; every finding is numbered and described.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator screenshots → evidence root → chat | Real adult gallery content at AX5 lives only in the evidence root (`$HOME/Library/Caches/ehpanda-phase16/`) and in chat to the owner; never in the repo. |
| Agent → owner's logged-in simulator | Read/drive only; no erase, no reinstall, no credential access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | screenshots of gallery content | high | mitigate | Evidence-root-only paths; written descriptions in the table; `git status --porcelain` image check before each docs commit; no gallery identifiers in rows. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | UDID-addressed commands only; forbidden list honoured; baseline restored and re-read. |
| T-16-09 | Tampering | the owner's download folders | medium | mitigate | The sweep never deletes a download it did not create and never deletes folders outside the app (AGENTS.md invariant). |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. All 78 iPhone Group A rows non-`pending`; every `finding:#N` has a Findings entry.
2. D-13 rows #2 and #8 observed; Group A D-04 rows dispositioned.
3. Simulator restored to baseline (read back); no image in the repo; two docs commits.
</verification>

<success_criteria>
- Group A verified on the iPhone at XXL / AX3 / AX5 in both orientations, judged strictly by D-03 / D-04, with findings recorded and the walk never interrupted.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-04-SUMMARY.md` when done: findings opened (numbers + one-liners + evidence-root paths), D-13 observations, any `blocked` rows with reasons, the sweep-created download folder name, and the commit hashes.
</output>
