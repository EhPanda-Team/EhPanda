---
phase: 16-dynamic-type-accessibility
plan: 24
type: execute
wave: 22
depends_on: ["16-23"]
files_modified:
  - EhPandaUITests/AccessibilityAuditUITests.swift
  - .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
autonomous: false
requirements: [A11Y-02]

must_haves:
  truths:
    - "`EhPandaUITests/AccessibilityAuditUITests.swift` runs `performAccessibilityAudit(for:_:)` on every fixture-reachable surface of the Research inventory — Home root, Frontpage, Popular, History, Search root, Downloads (empty), Setting root and each local Setting child (Account, General, Activity Logs, Appearance, Reading, Download, Laboratory, About), Gallery Detail, Previews, Comments, Gallery Infos, Reading + control panel + Reading Setting, Filters, Quick Search, Date Seek — each reached through the hermetic launcher (`launchStubbed(extraEnvironment: [\"EHPANDA_AUTOMATION_TAB\": …])` or `openCold(_:)` with the fixture URLs), with no `#available` guard because the deployment target is iOS 26 (D-31)."
    - "The issue handler logs every `XCUIAccessibilityAuditIssue` (`compactDescription`, `detailedDescription`, `auditType`, element) and returns `false`, except for entries of two explicit allow-lists that both start empty: `systemOwnedExclusions` (elements EhPanda does not draw — a UITabBar / UINavigationBar / UIPicker part) and `ownerApprovedExclusions` (app-owned issues that are documented false positives and cannot be resolved without an unauthorised visible change — the research-documented `.contrast` reports on text over images or gradients in the hero carousel and the reader); every entry of either list carries a doc comment naming the element, the audit type and the reason, and an `ownerApprovedExclusions` entry is added only after the owner approves it by id at this plan's checkpoint (Task 3) with the measured numbers in hand — no app-owned issue is ever silently excluded (D-22, D-31)."
    - "Every app-owned issue the audit reports is either fixed in the owning file in this plan (a missing label, an undersized hit region, clipped text, a contrast miss, a trait) with the fix listed in the summary next to the audit line that motivated it, or — only when fixing it would need a visible change the owner has not authorised — tabled for the owner as an `E-n` candidate with the measured numbers and resolved by the owner's `approve` / `fix:` / `reopen:` reply; a fix that changes layout is checked at `.large` for parity and appended to the D-25 candidates (D-15, D-22, D-25)."
    - "The suite runs on the spare iPhone `88B217DA-A166-4BAD-820D-DE13B1C4EB54` and on an iPad simulator that is not the D-09 iPad (created with `xcrun simctl create` if none exists); the D-09 simulators are never a test destination (D-09, D-31)."
    - "Favorites, Watched, Archives, Torrents, EhSetting, FolderManager and Detail Search are recorded as an explicit assumption — unreachable by the audit because their views render a login placeholder without a session and the credential seam is forbidden, so no fixture would render the real surface — and are routed to the manual walkthrough (16-25) and the D-25 re-sweep instead (D-22 / RESEARCH Open Question 4)."
    - "Idempotency: two consecutive runs of the `UITests` plan produce the same set of passing tests, and the `.xcresult` shows each audit test ran once — no pass obtained through the plan's `retryOnFailure` repetition."
    - "Concurrency: the machine runs exactly one `xcodebuild test` at a time; the iPhone and iPad runs are sequential, never overlapped, and none is killed mid-launch."
    - "The thirteen pre-existing UI tests (`DeepLinkEntry`, `DeepLinkPad`, `DeepLinkScheme`, `DeepLinkSmoke`, `ShareSheet`) stay green in the same run."
    - "Encoding: the fixture launch keeps `-AppleLanguages (en)`; audit descriptions that quote a localized label are compared by identifier, never by translated text, so the suite is locale-stable."
  artifacts:
    - path: "EhPandaUITests/AccessibilityAuditUITests.swift"
      provides: "performAccessibilityAudit per fixture-reachable surface; systemOwnedExclusions and ownerApprovedExclusions allow-lists (both start empty)"
      contains: "performAccessibilityAudit"
    - path: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      provides: "### Automated audit (16-24): per-surface result, exclusions (system-owned and owner-approved, with the owner's reply), fixes, reachability assumption"
      contains: "Automated audit (16-24)"
  key_links:
    - from: "EhPandaUITests/AccessibilityAuditUITests.swift"
      to: "EhPandaUITests/Support/DeepLinkLauncher.swift"
      via: "launchStubbed / openCold keep the hermetic stub network and fixture directory"
      pattern: "launchStubbed|openCold"
    - from: "EhPandaUITests/AccessibilityAuditUITests.swift"
      to: "AppPackage/Sources/AppLaunchAutomationClient/AppLaunchAutomation.swift"
      via: "EHPANDA_AUTOMATION_TAB selects the initial tab (home|favorites|search|downloads|setting)"
      pattern: "EHPANDA_AUTOMATION_TAB"
  prohibitions:
    - statement: "Never run the UI-test plan against a D-09 simulator UDID, and never pass `booted` as the destination."
      status: resolved
      verification: judgment
    - statement: "Never silence an app-owned audit issue through the handler without the owner's explicit approval by id; `systemOwnedExclusions` takes system-owned elements only, `ownerApprovedExclusions` takes owner-approved documented false positives only, each with a doc comment (D-22)."
      status: resolved
      verification: test
    - statement: "Never add a credential seam or a login fixture to reach the gated screens; record the reachability assumption instead (RESEARCH Open Question 4)."
      status: resolved
      verification: judgment
    - statement: "Never overlap two `xcodebuild test` invocations or `pkill -9` one mid-launch."
      status: resolved
      verification: judgment
---

<objective>
Add the permanent mechanical gate D-31 requires: an `AccessibilityAuditUITests` suite on the non-default `UITests` plan that audits every fixture-reachable surface with `performAccessibilityAudit`, fixes what it finds in app code, brings any app-owned false positive that cannot be fixed without a visible change to the owner with the numbers (D-22) before it may be excluded, records an explicit reachability assumption for the login-gated screens, and proves idempotency on two spare simulators.

Purpose: D-31's automated half of the Nutrition Label bar; a regression gate that outlives this phase.
Output: `EhPandaUITests/AccessibilityAuditUITests.swift`; app-code fixes for reported issues; `16-CONTRAST-AUDIT.md § Automated audit (16-24)`.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating (plans 16-20, 16-21); DWC glyphs (plan 16-22); colour remediation (plan 16-23).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `ReduceMotionGatingSourceTests`, `AccessibilityAuditUITests` (this plan).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@AGENTS.md
@.swiftlint.yml
@EhPandaUITests/.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AccessibilityAuditUITests over every fixture-reachable surface</name>
  <files>EhPandaUITests/AccessibilityAuditUITests.swift</files>
  <read_first>
    - `EhPandaUITests/Support/DeepLinkLauncher.swift` (full) — `launchStubbed(extraEnvironment:)`, `openCold(_:)`, `requireForeground`, `requireElement(_:matching:timeout:)`; the hermetic launch sets `-AppleLanguages (en)`, `EHPANDA_UITEST_STUB_NETWORK`, `EHPANDA_UITEST_FIXTURE_DIR`.
    - `EhPandaUITests/Support/UITestConstants.swift` (full) — `galleryURL(scheme:)`, `singlePageURL(scheme:)`, `commentURL(scheme:)`, marker titles.
    - `EhPandaUITests/DeepLinkSmokeUITests.swift` and `DeepLinkPadUITests.swift` (full) — the house test shape (`@MainActor final class`, plain `override func setUpWithError`, `continueAfterFailure = false`) and how the pad test skips on iPhone.
    - `AppPackage/Sources/AppLaunchAutomationClient/AppLaunchAutomation.swift` lines 47–120 — `EHPANDA_AUTOMATION_TAB` values `home|favorites|search|downloads|setting` (case-insensitive, trimmed).
    - `AppPackage/Sources/AppFeature/UITestSupport/UITestStubURLProtocol.swift` — which paths map to which fixture (`/g/*`, `/s/*`, `/`, `/popular`); anything else 404s.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Pattern 7 and the screen inventory's "Fixture-reachable" column (rows 1–4, 6, 9, 11, 14–16, 18, 21, 24–26, 28–37, 39–42).
    - The accessibility identifiers already present: `detail_view`, `comments_view`, `reading_view`, `reading_page_indicator`, `error_info_view`, `toast_message`, `comment_cell_<id>` (grep `accessibilityIdentifier("` under `AppPackage/Sources`).
    - `EhPanda.xcodeproj/project.pbxproj` lines 92–122 — `EhPandaUITests` is a `PBXFileSystemSynchronizedRootGroup`, so a new file needs no project edit.
    - `$HOME/.claude/skills/swift-accessibility-skill/SKILL.md` § Automated audit.
  </read_first>
  <behavior>
    - Each test launches hermetically, waits for its surface (identifier or a visible navigation title / `requireForeground`), then `try app.performAccessibilityAudit(for: .all) { issue in … }`.
    - The handler prints `issue.auditType`, `issue.compactDescription`, `issue.detailedDescription`, `issue.element?.description` and returns `systemOwnedExclusions.contains { $0.matches(issue) } || ownerApprovedExclusions.contains { $0.matches(issue) }` — `false` for everything while both lists are empty.
    - Tab roots: one test per `EHPANDA_AUTOMATION_TAB` value `home`, `search`, `downloads`, `setting` (not `favorites` — it renders the login placeholder; audit that placeholder once and label the test accordingly).
    - Pushed Home children (Frontpage, Popular, History) and pushed Setting children (Account, General, Activity Logs, Appearance, Reading, Download, Laboratory, About) are reached by tapping the row whose accessibility label is the English catalog value; the test asserts the destination's navigation title exists before auditing.
    - Deep-link surfaces: Detail via `openCold(galleryURL)` (`detail_view`), Comments via `commentURL` (`comments_view`), Reading via `singlePageURL` (`reading_view`), then Previews / Gallery Infos by pushing from Detail, Reading Setting and the control panel by tapping the reader once (`reading_page_indicator` visible) and the gear.
    - Toolbar sheets: Filters, Quick Search, Date Seek opened from Frontpage's toolbar; each audited while presented, then dismissed.
    - Error surface: a deep link whose path the stub 404s (`galleryURL` with a gid the stub does not know) surfaces `error_info_view` — audit it.
    - An iPad-only test mirrors `DeepLinkPadUITests`' skip and audits the Setting modal + Detail modal presentations when `UIDevice.current.userInterfaceIdiom == .pad`.
  </behavior>
  <action>
Write `EhPandaUITests/AccessibilityAuditUITests.swift` per the behavior list: `@MainActor final class AccessibilityAuditUITests: XCTestCase`, `override func setUpWithError() throws { continueAfterFailure = false }` (never `@MainActor override`), a file-private `struct AuditExclusion { let id: String; let reason: String; let matches: (XCUIAccessibilityAuditIssue) -> Bool }` and two empty lists — `private let systemOwnedExclusions: [AuditExclusion] = []` (doc comment: system-owned elements only; each entry names the element and the Apple component that owns it) and `private let ownerApprovedExclusions: [AuditExclusion] = []` (doc comment: app-owned documented false positives only — e.g. a `.contrast` report on text over an image or gradient — each entry names the element, the audit type, the measured reason, and the owner's approval reply recorded in `16-CONTRAST-AUDIT.md § Automated audit (16-24)`; nothing enters this list before that reply, per D-22). One helper `audit(_ app: XCUIApplication, surface: String)` wraps `performAccessibilityAudit(for: .all)` and prefixes every log line with the surface name; no `#available`. Reach each surface as listed; prefer identifiers over label text where an identifier exists, and when a label must be used, use the English catalog value (the launch forces `en`). Lint the file with the standalone SwiftLint against `EhPandaUITests/.swiftlint.yml` (120-column limit applies). Run `xcodebuild build-for-testing -project EhPanda.xcodeproj -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54'`, then ONE `xcodebuild test … -testPlan UITests … -resultBundlePath "$EVIDENCE_ROOT/round2/audit/a11y-audit-iphone.xcresult"` (the persistent evidence root from `16-SWEEP.md § Infrastructure`; `mkdir -p` first — the result bundle is the evidence the owner's Task 3 decision rests on). Red means the app has audit findings — that is the RED step; Task 2 fixes them. Commit the test file only after the test build succeeds: `test(16-24): add accessibility audit UI tests` (the file may be red at this commit; the summary records which tests are red and why).
  </action>
  <verify>
    <automated>grep -c "performAccessibilityAudit" EhPandaUITests/AccessibilityAuditUITests.swift; grep -c "#available" EhPandaUITests/AccessibilityAuditUITests.swift; grep -c "func test" EhPandaUITests/AccessibilityAuditUITests.swift; xcodebuild build-for-testing -project EhPanda.xcodeproj -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "performAccessibilityAudit" …` prints `1` or more (the helper) and `grep -c "#available" …` prints `0`; `grep -c "func test" …` prints at least `20` (one per surface or surface group listed in the behavior).
    - `grep -c "systemOwnedExclusions" …` and `grep -c "ownerApprovedExclusions" …` each print at least `2` (declaration + use) and both array literals are empty at this commit.
    - `grep -c "88B217DA\|ADE09605\|8250D97E" EhPandaUITests/AccessibilityAuditUITests.swift` prints `0` (no UDID in source).
    - `build-for-testing -testPlan UITests` → `** TEST BUILD SUCCEEDED **`; the standalone SwiftLint reports 0 violations for the file.
    - The commit `test(16-24): add accessibility audit UI tests` exists; the summary lists every red test from the first run with its audit lines.
  </acceptance_criteria>
  <done>Every fixture-reachable surface has an audit test; the run's findings are captured verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Classify and fix app-owned findings; stage system-owned exclusions and owner-approval candidates</name>
  <files>EhPandaUITests/AccessibilityAuditUITests.swift, .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - The Task 1 run log / `.xcresult` (`xcrun xcresulttool get test-results tests --path "$HOME/Library/Caches/ehpanda-phase16/round2/audit/a11y-audit-iphone.xcresult"`) — every issue line with its element.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § `performAccessibilityAudit()` › Caveats — contrast audits produce false positives on text over images/gradients (hero carousel, reader); never a blanket ignore.
    - For each issue, the owning view file (the audit names the element's label/identifier; map it through the inventory's Primary files column).
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` § D-25 re-sweep — where layout-affecting fixes are appended.
    - `xcrun simctl list devices available` — whether an iPad simulator other than `8250D97E-9AB0-42FD-99DB-07B0094BF8C7` exists.
  </read_first>
  <action>
Classify each reported issue into exactly one of three classes and record the classification in the summary with the audit line that motivated it. (a) App-owned and fixable without a visible change → fix in the owning file using the round-2 idioms (catalog-key label, `.accessibilityHidden(true)`, `.contentShape`/`.frame(minWidth: 44, minHeight: 44)` for hit regions, `.accessibilityAddTraits`, a colour fix via the 16-23 approach) and note the file; a layout-affecting fix is re-checked at `.large` against a capture under `$EVIDENCE_ROOT/round2/audit/` and appended to `16-SWEEP.md § D-25 re-sweep` with the screen number. (b) System-owned (a UITabBar / UINavigationBar / UIPicker element EhPanda does not draw) → add one `systemOwnedExclusions` entry with a doc comment naming the element and the Apple component. (c) App-owned but a documented false positive that cannot be resolved without a visible change the owner has not authorised — the RESEARCH caveat's `.contrast` reports on text drawn over an image or gradient (hero-carousel title over artwork, reader overlay text over the page) are the expected members — → do NOT exclude it yet and do NOT change the rendering; instead write one `### Owner-approval candidates` row in `16-CONTRAST-AUDIT.md § Automated audit (16-24)` — `id (E-1, E-2, …) | surface | element | audit type | what the audit measured | why a fix needs a visible change (which decision it would cross) | proposed exclusion scope` — with the numbers from the `.xcresult`. Commit the class-(a) app fixes as `fix(16-24): resolve accessibility audit findings` (test build required because `EhPandaUITests/` may also change) and the class-(b) entries with them. Then run the full `UITests` plan once on the iPhone spare to a fresh `-resultBundlePath` under `$EVIDENCE_ROOT/round2/audit/`; every test must pass except those whose only remaining issues are class-(c) candidates, and those must be listed by name in the candidates section. If there is no class-(c) candidate, write `### Owner-approval candidates — none` and Task 3 is skipped (a checkpoint with nothing to decide is not raised).
  </action>
  <verify>
    <automated>grep -c "ownerApprovedExclusions: \[AuditExclusion\] = \[\]" EhPandaUITests/AccessibilityAuditUITests.swift; f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "### Owner-approval candidates" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Every reported issue is classified (a), (b) or (c) in the summary with its audit line; no issue is unclassified and none is excluded under a class it does not belong to.
    - `grep -c "ownerApprovedExclusions: \[AuditExclusion\] = \[\]" EhPandaUITests/AccessibilityAuditUITests.swift` prints `1` at the end of this task (nothing owner-approved exists yet).
    - Every `systemOwnedExclusions` entry has a doc comment naming a system element and Apple component; the summary lists them.
    - `### Owner-approval candidates` exists in `16-CONTRAST-AUDIT.md § Automated audit (16-24)` with one row per class-(c) issue carrying the measured numbers and the decision a fix would cross, or reads `— none`.
    - The post-fix iPhone-spare run shows every test green except tests named in the candidates section; `git status --porcelain` image count is `0`; the commit `fix(16-24): resolve accessibility audit findings` exists if any class-(a) fix was made.
  </acceptance_criteria>
  <done>Everything fixable is fixed; everything system-owned is excluded with a reason; everything that needs the owner is on the table with numbers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner approves or rejects each audit exclusion candidate (D-22)</name>
  <action>Raised only if Task 2 produced at least one `### Owner-approval candidates` row; otherwise skipped. Stop and present the candidates; the agent adds nothing to `ownerApprovedExclusions` before the reply.</action>
  <what-built>`AccessibilityAuditUITests` is written and committed; class-(a) findings are fixed; class-(b) system-owned exclusions are in place; `16-CONTRAST-AUDIT.md § Automated audit (16-24) › Owner-approval candidates` lists every app-owned audit issue the agent cannot resolve without a visible change you have not authorised — each with the audit's measured numbers and the decision a fix would cross.</what-built>
  <how-to-verify>
    1. Read `16-CONTRAST-AUDIT.md § Automated audit (16-24) › Owner-approval candidates`: for each `E-n` row, the element, the audit type, what was measured, and why fixing it would be a visible change (for example darkening a hero-carousel title scrim, which crosses the "no visual redesign" parity constraint).
    2. For each row decide: `approve` (the issue becomes an `ownerApprovedExclusions` entry with your reply quoted in its doc comment; the category remains claimable only if the skill's criteria allow a documented exception, which the nutrition label will state), or `fix` (you authorise the visible change; the agent implements it in the owning file and the issue is not excluded), or `reopen` (the blocking decision is revisited before this plan continues).
    3. Reply with one line per id.
  </how-to-verify>
  <resume-signal>Reply `E-1=approve`, `E-1=fix: <what visible change is authorised>`, or `E-1=reopen: <decision id> <note>` — one line per candidate id.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Apply the owner's replies, prove idempotency on iPhone and iPad spares, record</name>
  <files>EhPandaUITests/AccessibilityAuditUITests.swift, .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - The owner's Task 3 reply (or `— none` from Task 2).
    - `EhPandaUITests/AccessibilityAuditUITests.swift` — the two allow-lists and the handler.
    - `xcrun simctl list devices available` — whether an iPad simulator other than `8250D97E-9AB0-42FD-99DB-07B0094BF8C7` exists.
  </read_first>
  <action>
For each `E-n=approve`: add one `ownerApprovedExclusions` entry whose doc comment names the element, the audit type, the measured reason and quotes the owner's reply verbatim with the date; for each `E-n=fix:`: implement the authorised visible change in the owning file, re-check `.large` parity of the touched screen against a capture under `$EVIDENCE_ROOT/round2/audit/`, append the screen to `16-SWEEP.md § D-25 re-sweep`, and leave the issue un-excluded; for each `E-n=reopen:`: stop, record the reopen line in the audit section, and return to the orchestrator — do not proceed to the idempotency runs with a reopened candidate. Commit `fix(16-24): apply owner-approved audit exclusions` (test build required). Run the full `UITests` plan on the iPhone spare TWICE in sequence (two `xcodebuild test` invocations, the second only after the first exits) with distinct `-resultBundlePath`s under `$EVIDENCE_ROOT/round2/audit/`; verify with `xcrun xcresulttool get test-results tests` that every `AccessibilityAuditUITests` test passed and ran once (no repetition). Then the iPad leg: pick an available iPad simulator that is not the D-09 iPad, or create one — `xcrun simctl create "A11yAudit iPad" "iPad Pro 11-inch (M5)" <latest iOS runtime id>` — boot it, run the plan once, then shut it down (`xcrun simctl shutdown <UDID>`; keep the device for future runs). Complete `### Automated audit (16-24)` in `16-CONTRAST-AUDIT.md`: a table `surface | test | iPhone run 1 | iPhone run 2 | iPad | issues found | fix / exclusion`, the `systemOwnedExclusions` list, the `ownerApprovedExclusions` list with the owner's reply per id (this is the record the nutrition label cites for any documented exception), the reachability assumption paragraph naming Favorites, Watched, Archives, Torrents, EhSetting, FolderManager and Detail Search (login-gated; no credential seam; covered by 16-25's manual walkthrough and the D-25 re-sweep), and the simulator UDIDs used. Commit `docs(16): record automated accessibility audit`.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "### Automated audit (16-24)" "$f"; grep -c "Favorites, Watched, Archives, Torrents, EhSetting, FolderManager" "$f"; grep -c "ownerApprovedExclusions" EhPandaUITests/AccessibilityAuditUITests.swift; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l; git log -3 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - Two consecutive iPhone-spare runs of `-testPlan UITests` report every test passed, and `xcresulttool` shows each `AccessibilityAuditUITests` test with a single run (no retry); the iPad-spare run passes.
    - The 13 pre-existing UI tests pass in the same runs.
    - Every `ownerApprovedExclusions` entry has a doc comment quoting an `E-n=approve` reply; the count of entries equals the count of `approve` replies; every `fix:` reply has a code change and no exclusion; no `reopen:` reply is outstanding.
    - `### Automated audit (16-24)` exists with the per-surface table, both exclusion lists, the owner's replies, and the reachability assumption sentence naming all seven gated surfaces.
    - `git status --porcelain` image count is `0`; `grep -c "/Users/" "$f"` is `0`; the commit `docs(16): record automated accessibility audit` exists.
  </acceptance_criteria>
  <done>The audit suite is a green, idempotent gate on two spares; every exclusion is either system-owned or owner-approved by id; gated screens are an explicit, routed assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-test destination → simulators | Only spares; D-09 UDIDs never receive a runner install. |
| Handler allow-lists → audit integrity | App-owned issues are excluded only with the owner's approval by id; system-owned entries are doc-commented; nothing is excluded before classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03 | Tampering | D-09 logged-in simulators | high | mitigate | Destinations are explicit spare UDIDs; acceptance greps the test file for UDID strings (none). |
| T-16-15 | Repudiation | a green audit that hides findings | medium | mitigate | Two allow-lists that start empty; system-owned entries doc-commented; app-owned false positives excluded only after the owner's `E-n=approve` reply by id (Task 3, D-22); both lists recorded in the audit section. |
| T-16-16 | Denial of service | overlapping xcodebuild test runs wedging testmanagerd | medium | mitigate | Strictly sequential runs; never kill one mid-launch. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Test file greps; `build-for-testing` green; SwiftLint 0 for the file.
2. Classification (a)/(b)/(c) recorded; owner checkpoint on the (c) candidates (skipped if none); two sequential iPhone runs + one iPad run green with single executions; docs section present; image check 0.
</verification>

<success_criteria>
- D-31's automated half is in place as a permanent `UITests`-plan gate covering every fixture-reachable surface, with findings fixed in app code, documented false positives excluded only on the owner's say-so by id, and the login-gated surfaces recorded as a routed assumption (D-22).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-24-SUMMARY.md` when done: test inventory, classification per issue, fixes per file, both exclusion lists with the owner's replies, simulators used, run results, commit hashes.
</output>
