---
phase: 16-dynamic-type-accessibility
plan: 24
type: execute
wave: 22
depends_on: ["16-23"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: false
requirements: [A11Y-02]

must_haves:
  truths:
    - "The current round-2 build is installed on the physical `Owner-iPhone-Test` (install-over, data kept) from a device build of the `EhPanda` scheme, and the bundle identifier actually installed is read from the built app's `Info.plist`, never assumed (D-31)."
    - "`16-SWEEP.md § Round-2 manual walkthrough (16-24)` holds a script the owner can follow without the agent: one row per (common task × check) drawn from the skill's `qa-checklist.md` sections 1 (VoiceOver 1.1–1.11), 2 (Voice Control 2.1–2.7) and the device-only display checks (Grayscale colour filter, Dark + Increase Contrast, Bold Text, Reduce Transparency, Reduce Motion, AX5 spot check), each row `pending` until the owner reports (D-31, D-33)."
    - "The common tasks are the user flows the Nutrition Label is judged on — browse Home/Frontpage, search, open a gallery detail, read a gallery (paging, zoom, slider, panel), manage favorites (logged in), download and manage a download, change a setting, post/vote on a comment — and every round-2 surface class (icon-only controls, custom tappables, menus, toggles, adjustable slider, named actions, announcements, gated motion, adaptive badges, log-level symbols) appears in at least one row."
    - "The owner's `CONTEXTMENU=exposed|not-exposed` answer from plan 16-13 is confirmed on the device in this walkthrough (VoiceOver rotor › Actions on a tag chip, a comment cell and a download row) and recorded; if the device contradicts the earlier answer, the mirrors added in 16-16 / 16-19 are revisited in this plan's fix loop (D-22 / OQ2)."
    - "Every `fail` row the owner reports is fixed in the owning file with the round-2 idioms, rebuilt, reinstalled, and re-walked until the owner reports `pass` or records `accepted: <reason>`; no row is closed by the agent alone (D-20)."
    - statement: "VoiceOver reading order on every walked screen is visual order (top-to-bottom, left-to-right) and focus lands sensibly after push and after sheet dismiss — judged by the owner's ear on the device, with no automated oracle."
      verification: backstop
    - "Idempotency: re-walking a `pass` row after an unrelated fix yields `pass` again — the record keeps both walks."
    - "Encoding: the walkthrough is performed in the device's language; labels are reported by their visible text, and any row whose spoken text is wrong in a non-English locale is recorded with the locale and the catalog key."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "## Round-2 manual walkthrough (16-24): script rows, owner verdicts, CONTEXTMENU confirmation, fix loop"
      contains: "Round-2 manual walkthrough (16-24)"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      to: "$HOME/.claude/skills/swift-accessibility-skill/resources/qa-checklist.md"
      via: "row ids 1.1–1.11 and 2.1–2.7 are the checklist's ids"
      pattern: "1\\.11|2\\.7"
  prohibitions:
    - statement: "Never close a walkthrough row without the owner's verdict; the agent cannot hear VoiceOver (D-31)."
      status: resolved
      verification: judgment
    - statement: "Never erase, reset or re-login the physical device; install-over only (D-09 spirit)."
      status: resolved
      verification: judgment
    - statement: "Never commit a device screenshot or recording; image check before every docs commit (D-32)."
      status: resolved
      verification: judgment
---

<objective>
Run D-31's manual half: build and install the round-2 app on the owner's physical iPhone, hand the owner a per-task script drawn from the skill's QA checklist (VoiceOver, Voice Control and the device-only display settings), confirm the context-menu question on the device, and loop fixes until every row is `pass` or owner-accepted.

Purpose: the Simulator has neither VoiceOver nor Voice Control; reading order, focus and spoken labels are judged only by ear.
Output: `16-SWEEP.md § Round-2 manual walkthrough (16-24)` with owner verdicts; fixes for reported rows.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (walkthrough recorded here), `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating (plans 16-20, 16-21); colour and DWC remediation (plan 16-22); fixes from this walkthrough.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `ReduceMotionGatingSourceTests`, `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/resources/qa-checklist.md
@$HOME/.claude/skills/swift-accessibility-skill/references/testing-auditing.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device build, install-over, and the walkthrough script</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `Config/Signing.xcconfig` (full) — `DEVELOPMENT_TEAM` and `BUNDLE_ID_SUFFIX` come from the gitignored `Config/LocalSigning.xcconfig`; the installed bundle id is `app.ehpanda<suffix>` and must be read from the built product, not guessed.
    - `$HOME/.claude/skills/swift-accessibility-skill/resources/qa-checklist.md` sections 1, 2, 3, 4, 5, 6, 7 and § Summary Template — the row ids and pass criteria.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Round-2 inventories — the icon-only controls, custom tappables, toolbar menus, motion sites; § Open Questions item 2 (context menus).
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions — the recorded `CONTEXTMENU=` answer and the `STARS` / `CATEGORYCELL` outcomes (they determine which visual cues to ask about).
    - The 16-16 … 16-22 SUMMARY files in this phase directory — every semantic added (labels, values, actions, representations, announcements, symbols) becomes a row.
    - `xcrun devicectl list devices` — confirm `Owner-iPhone-Test` is connected and unlocked (ask the owner in chat to unlock it before the install; do not retry in a loop).
  </read_first>
  <action>
Build for the device: `xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -configuration Debug -destination 'platform=iOS,name=Owner-iPhone-Test' -derivedDataPath <scratchpad>/device-dd` (signing comes from the local xcconfig; if signing fails, surface the error as a checkpoint — never edit `Config/Signing.xcconfig`). Read the bundle id with `plutil -extract CFBundleIdentifier raw <scratchpad>/device-dd/Build/Products/Debug-iphoneos/EhPanda.app/Info.plist`, install-over with `xcrun devicectl device install app --device Owner-iPhone-Test <that .app>`, launch with `xcrun devicectl device process launch --device Owner-iPhone-Test <bundle id>`. Append `## Round-2 manual walkthrough (16-24)` to `16-SWEEP.md`: a header (build commit hash, device, iOS version from `devicectl`, bundle id, date), a `### Common tasks` list (the eight tasks from the truths, each with its start screen and end state), and a `### Rows` table `id | common task | check (qa-checklist id) | what to do | expected | status | note` with one row per (task × applicable check) — VoiceOver 1.1–1.11, Voice Control 2.1–2.7 (2.7 specifically on every icon-only control given an input label), the display checks (Grayscale filter on the category grid, rating stars, log levels; Dark + Increase Contrast on category badges and the D-28 sites; Bold Text and Reduce Transparency on the reader panel and toasts; Reduce Motion on the Detail header spinner, reader page jump, list insertions; AX5 spot check on Detail and Reader), and a `### Context menus on device (OQ2)` block with three rows (tag chip, comment cell, download row: "rotor › Actions lists the context-menu items? yes/no"). All statuses `pending`. Commit `docs(16): add round-2 walkthrough script` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "## Round-2 manual walkthrough (16-24)" "$f"; grep -c "| 1.11 \|| 2.7 " "$f"; grep -c "Context menus on device (OQ2)" "$f"; xcrun devicectl list devices 2>/dev/null | grep -c "Owner-iPhone-Test"</automated>
  </verify>
  <acceptance_criteria>
    - `xcrun devicectl device info apps --device Owner-iPhone-Test` lists the bundle id read from the built `Info.plist`, and the app launches on the device.
    - The walkthrough section exists with: a header naming the build commit and bundle id; eight common tasks; rows covering every id 1.1–1.11 and 2.1–2.7 at least once; the six display checks; the three OQ2 rows; every status `pending`.
    - `grep -c "/Users/" "$f"` prints `0`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif|mov|mp4)$' | wc -l` prints `0`; the commit `docs(16): add round-2 walkthrough script` exists.
  </acceptance_criteria>
  <done>The owner has the build on the device and a script that needs no agent narration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walks VoiceOver, Voice Control and the display checks on the device</name>
  <action>Stop and hand the script to the owner; the agent cannot hear VoiceOver and does not close any row itself (D-31).</action>
  <what-built>The round-2 build is installed on `Owner-iPhone-Test` and `16-SWEEP.md § Round-2 manual walkthrough (16-24)` lists every row to walk: VoiceOver 1.1–1.11 and Voice Control 2.1–2.7 per common task, the device-only display checks, and the three context-menu (OQ2) rows.</what-built>
  <how-to-verify>
    1. On the device, enable VoiceOver (Settings › Accessibility › VoiceOver) and walk the `### Rows` VoiceOver rows task by task, in order; for each row say `pass` or `fail: <what you heard / saw>`.
    2. Disable VoiceOver, enable Voice Control, walk the 2.x rows ("Show numbers", "Show names", "Tap <label>" on each icon-only control named in the row).
    3. Walk the display-check rows: Grayscale (Display & Text Size › Color Filters), Dark + Increase Contrast, Bold Text, Reduce Transparency, Reduce Motion, AX5 (Larger Text slider at maximum) — restore each setting afterwards.
    4. With VoiceOver on, answer the three OQ2 rows: open the rotor on a tag chip, a comment cell and a download row, choose Actions, and report whether the context-menu items appear.
    5. Reply with one line per row you did not pass: `<row id>=fail: <description>` or `<row id>=accepted: <reason>`; every unmentioned row is taken as `pass`. Add `CONTEXTMENU=exposed|not-exposed` for the OQ2 block.
  </how-to-verify>
  <resume-signal>Type "all pass" or the per-row lines described in step 5 (plus `CONTEXTMENU=…`).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record verdicts, fix reported rows, loop until clear</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - The owner's reply — each `fail`/`accepted` line and the `CONTEXTMENU=` answer.
    - For each `fail`, the owning view file from the Research inventory and the 16-16 … 16-22 summaries (what was added, so the fix extends rather than duplicates).
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions — the earlier `CONTEXTMENU=` answer, to compare with the device answer.
  </read_first>
  <action>
Record every row's status (`pass` / `fail: …` / `accepted: …`) and the device `CONTEXTMENU=` answer in the section. If the device answer contradicts the 16-13 answer, apply the rule from 16-16 / 16-19: `not-exposed` → the mirrors must exist (add any missing `.accessibilityAction(named:)`); `exposed` → remove mirrors that now read twice in the rotor. Fix each `fail` in its owning file with the round-2 idioms (catalog-key label / input label, `.accessibilityRepresentation`, `.accessibilityValue`, `.accessibilityHidden(true)`, `@AccessibilityFocusState`, `AccessibilityNotification.Announcement`, a Reduce Motion gate) — never a hardcoded string, never a suppression; run the owning module's tests on the spare simulator and a lint build; commit `fix(16-24): <area> walkthrough findings`. Rebuild, reinstall (Task 1's commands), and re-present only the failed rows plus any `pass` row the fix could plausibly affect (same screen) as a new `checkpoint:human-verify` with the same reply format; record each re-walk as an additional status column (`walk 2`, `walk 3`) so earlier verdicts stay visible. Repeat until every row is `pass` or `accepted`. Finish with `### Walkthrough closure` (counts: rows, passes, accepted with reasons, fix commits) and commit `docs(16): record round-2 manual walkthrough`.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; sed -n '/## Round-2 manual walkthrough (16-24)/,$p' "$f" | grep -c "| pending"; grep -c "### Walkthrough closure" "$f"; grep -c "CONTEXTMENU=" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif|mov|mp4)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Within the walkthrough section, `grep -c "| pending"` prints `0`; every row is `pass` or `accepted: <reason>` in its latest walk column.
    - `### Walkthrough closure` exists with row / pass / accepted counts and the list of `fix(16-24)` commits (possibly none).
    - The section records the device `CONTEXTMENU=` answer and, if it differed from 16-13, which mirrors were added or removed.
    - Each `fix(16-24)` commit passed its module tests and a lint build; no `swiftlint:disable`; no hardcoded accessibility string (the `accessibility_hardcoded_string` rule stays at 0).
    - The image check prints `0`; `grep -c "/Users/" "$f"` prints `0`; the commit `docs(16): record round-2 manual walkthrough` exists.
  </acceptance_criteria>
  <done>Every walkthrough row is owner-passed or owner-accepted; D-31's manual half is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → physical device | Install-over only; no erase, no credential handling. |
| Owner verdicts → record | Transcribed verbatim; the agent never closes a row alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03 | Tampering | owner's device data / session | high | mitigate | `devicectl install app` over the existing install; never uninstall or reset. |
| T-16-01 | Information disclosure | device screenshots or recordings | high | mitigate | None are taken into the repo; image/video check before docs commits. |
| T-16-17 | Repudiation | a walkthrough row closed without the owner's ear | medium | mitigate | Rows change from `pending` only through the checkpoint reply; re-walk columns preserve history. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. App installed and launched on the device; script section present with all ids and `pending` rows.
2. After the loop: zero `pending`, closure section, device `CONTEXTMENU=` recorded, fix commits green.
</verification>

<success_criteria>
- D-31's manual walkthrough is complete on the physical device with every row owner-passed or owner-accepted, OQ2 confirmed on-device, and every reported failure fixed with round-2 idioms (D-20, D-22, D-33).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-24-SUMMARY.md` when done: build/bundle id, row counts, findings and fixes per file, CONTEXTMENU device answer, commit hashes.
</output>
