---
phase: 16-dynamic-type-accessibility
plan: 10
type: execute
wave: 9
depends_on: ["16-09"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: false
requirements: [A11Y-01]

must_haves:
  truths:
    - "The complete findings list is reported to the owner once, after every page of the matrix has been scanned — never piecemeal during the walk — as a numbered report with a written description per finding, the cells affected, and the source files mapped through the inventory (D-02)."
    - "The owner finds and fixes by hand; the agent hands over the report and stops — it writes no reflow code, no patch, no work order, and no suggested diff; the checkpoint resumes only on the owner's signal (D-01)."
    - "Each of the five D-13 named rows is presented with its iPhone/iPad observations and the owner is asked for an explicit per-item disposition — `fixed` or `accepted (reason)` — so criterion 4 ticks off item by item; the reader counter wrap is flagged as a candidate for `accepted` on the D-03 rule alone, but the owner decides (D-03, D-13)."
    - "The D-04 checklist outcome (which Phase-10 'fine' sites became findings under the strict reading) is reported alongside, not merged into, the sweep findings (D-04)."
    - "The five `minimumScaleFactor` sites are listed for the owner with the note that default-size parity outranks the ban where they collide (the 0.72 header site especially), and that the `no_minimum_scale_factor` lint rule lands only after the count reaches 0 (D-14, D-15, D-16)."
    - "Every finding's before image is sent to the owner in chat as an evidence-root path plus description; no image enters the repo (D-32, D-33)."
    - "Any iPad rows blocked by a missing iPad session are surfaced in the report as an explicit gap for the owner to resolve (log in, or accept), never silently skipped (D-09, D-10)."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "`## Round-1 report` section: numbered findings with files, D-13 dispositions requested, D-04 outcome, D-14 site list, blocked rows"
      contains: "Round-1 report"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      via: "Findings link to the Inventory's Primary files because each finding names the primary files of its screen so the owner's fix commits can be mapped back to rows for re-verification"
      pattern: "Primary files"
  prohibitions:
    - statement: "The agent produces no reflow code, patch, diff or work order for round-1 findings; the owner implements (D-01)."
      status: resolved
      verification: judgment
    - statement: "No image file is ever added to the repository; before/after evidence is chat-only (D-32)."
      status: resolved
      verification: judgment
---

<objective>
Hand round 1 to the owner: consolidate the walked matrix into one complete, numbered findings report (D-02), present the five D-13 items for explicit disposition, report the D-04 outcome and the five D-14 sites, send every before image in chat (D-33), and stop at a blocking checkpoint while the owner fixes by hand (D-01).

Purpose: D-01 makes this the one legitimate "do not automate" step of round 1 — the agent's job is the report, the owner's is the fix.
Output: `16-SWEEP.md § Round-1 report` committed; the owner's fix commits and D-13 dispositions captured in the resume signal for plan 16-11.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (report section added here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the complete round-1 report</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — the whole file: § Matrix (every row), § Findings, § D-13 named edge cases, § D-04 checklist, § Inventory (Primary files column), § Infrastructure (`IPAD_LOGIN`, D-15 baseline paths).
    - `.planning/phases/16-dynamic-type-accessibility/16-*-SUMMARY.md` for plans 16-04 … 16-09 — the per-plan finding lists and evidence-root screenshot paths.
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md` D-01, D-02, D-13, D-14, D-15.
  </read_first>
  <action>
Add `## Round-1 report` at the end of `16-SWEEP.md` (after § D-25 re-sweep) with: (1) a numbered findings table — `#N | Screen(s) | Device/orientation/size cells | Description | Primary files (from the inventory)` — consolidated from § Findings (merge duplicates that describe the same defect on different cells; keep one number per defect); (2) `### D-13 dispositions requested` — the five named items with their iPhone and iPad `Observed` notes and an empty `Disposition` column for the owner (`fixed` | `accepted (reason)`), with a note under the reader counter row that a wrap is not degradation under D-03 and `accepted` on the rule alone is legitimate; (3) `### D-04 outcome` — counts of checklist rows `fine` vs `finding:#N`, and the list of Phase-10 "fine" sites that became findings under the strict reading; (4) `### D-14 sites` — the five `minimumScaleFactor` sites with their current line numbers (re-grep `minimumScaleFactor` under `AppPackage/Sources` for the live lines), the D-15 note that parity at `.large` outranks the ban where the shrink currently engages at default (the 0.72 site on the Detail header category label is the likely collision), and the statement that the `no_minimum_scale_factor` rule lands in plan 16-12 only once the count is 0; (5) `### Blocked rows` — every `blocked:` / `n/a:` row with its reason (a missing iPad session is an explicit gap for the owner); (6) `### Evidence` — for each finding, the evidence-root path(s) of its before screenshot(s), marked as chat-only evidence. Send the report in chat with each finding's before image referenced by path and described (D-33). Commit with `docs(16): round-1 findings report` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "^## Round-1 report" "$f"; grep -c "### D-13 dispositions requested\|### D-04 outcome\|### D-14 sites\|### Blocked rows\|### Evidence" "$f"; grep -rn "minimumScaleFactor" AppPackage/Sources | wc -l; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Round-1 report" 16-SWEEP.md` prints `1` and the five `###` sub-sections are present.
    - Every `#N` in § Findings appears in the report table with a non-empty Description and at least one Primary file.
    - The `### D-14 sites` list has exactly as many entries as `grep -rn "minimumScaleFactor" AppPackage/Sources | wc -l` prints at the time of writing (5 unless the owner already started).
    - `grep -c "/Users/" 16-SWEEP.md` prints `0`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): round-1 findings report` exists.
  </acceptance_criteria>
  <done>The complete report is committed and sent; nothing remains unreported.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner fixes the round-1 findings by hand</name>
  <action>Stop. The owner implements every reflow fix, removes the five `minimumScaleFactor` sites (preserving `.large` parity where the shrink engaged at default — D-15), and decides each D-13 disposition. The agent does not write, suggest or stage any reflow code while this checkpoint is open (D-01).</action>
  <what-built>The complete round-1 findings report (`16-SWEEP.md § Round-1 report`, sent in chat with before images): numbered findings with files, the five D-13 items awaiting disposition, the D-04 outcome, the five D-14 sites, and any blocked rows.</what-built>
  <how-to-verify>
    1. Fix each finding in the report by reflow (wrap / `ViewThatFits` / `@Environment(\.dynamicTypeSize)` stacking / `@ScaledMetric(relativeTo:)`), never by capping `dynamicTypeSize`, `GeometryReader`, or a shrink — the three landed lint rules will error on a cap, a `GeometryReader`, or a numeric `.system(size:)` at build time.
    2. Remove all five `minimumScaleFactor` sites (`grep -rn minimumScaleFactor AppPackage/Sources` must print nothing). Where a shrink engaged at the default size (the Detail header category label at 0.72 is the likely one), the replacing reflow must keep the `.large` appearance unchanged — parity outranks the ban (D-15); the agent will compare against the `.large` baselines captured before your edits.
    3. For each D-13 item choose `fixed` or `accepted` with a one-line reason (a wrap is not degradation under D-03, so the reader counter wrap may be accepted on the rule alone).
    4. Any finding you choose not to fix: say `accepted` with the reason; it is recorded, not dropped.
    5. If iPad rows were blocked by a missing iPad session, either sign in on the iPad simulator now (the agent will walk those rows in the re-verification) or say they stay accepted as a gap.
    6. Commit your fixes on `feature/gsd-phase-16`; several commits are fine — the agent re-verifies batched per commit by mapping touched files to screens.
  </how-to-verify>
  <resume-signal>Reply with: `FIXED=<comma-separated commit hashes>` then one line per D-13 item `D13-<n>=fixed|accepted: <reason>` (1 stats strip, 2 tag clip, 3 counter wrap, 4 Favorites glyph, 5 carousel title), one line per finding you accept instead of fixing `#N=accepted: <reason>`, and `IPAD_LOGIN=present|none`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent report → owner | The report carries descriptions and evidence-root paths; before images go to chat, never the repo. |
| Owner fix commits → agent re-verification | The agent only reads the owner's commits to map files → screens; it writes no round-1 code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | before images of gallery content | high | mitigate | Chat-only evidence by evidence-root path; image check before the docs commit. |
| T-16-06 | Repudiation | findings / dispositions traceability | low | mitigate | Numbered report in the committed table; D-13 dispositions captured verbatim from the resume signal in plan 16-11. |
| T-16-12 | Elevation of privilege (process) | agent writing reflow code despite D-01 | medium | mitigate | Checkpoint is `human-action`; the plan's prohibitions forbid any reflow code, patch or work order. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. `## Round-1 report` with its five sub-sections committed; every finding present with files; D-14 site count matches the live grep.
2. Checkpoint resumed with `FIXED=`, five `D13-n=` lines, accepted findings, `IPAD_LOGIN=`.
</verification>

<success_criteria>
- The complete findings list reached the owner once and in full (D-02); the owner's fixes and dispositions are captured for re-verification; the agent wrote no reflow code (D-01).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-10-SUMMARY.md` when done: the report commit hash, the owner's resume signal verbatim (commits, D-13 dispositions, accepted findings, iPad login state).
</output>
