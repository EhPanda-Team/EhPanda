---
phase: 16-dynamic-type-accessibility
plan: 06
type: execute
wave: 5
depends_on: ["16-05"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every in-scope Group C surface (#28–#42: Setting root, Account, Login native form, General, Activity Logs + detail sheet, Appearance + App Icon picker, Setting › Reading, Download, Laboratory, About, EhSetting native sections, Filters sheet, Quick Search sheet + editor, Date Seek picker, Error surface + toasts) has six iPhone rows that are no longer `pending` (D-05, D-10)."
    - "Scope per D-11: the Login screen's native form and chrome are judged; the WKWebView login page and the Cloudflare challenge are excluded; EhSetting's native sections 1–3 are judged, its web pages are excluded; the share sheet and photo picker are excluded (D-11)."
    - "Verdicts follow D-03 / D-04 strictly: a settings value, a log category chip, a picker row or a toast subtitle that ellipsises at XXL / AX3 / AX5 is a finding; the fixed-width controls (`GeneralSettingView.swift:69` 50 pt, `EhSettingView+Sections2.swift` 200 pt) are judged at AX5 for clipped content (D-03, D-04)."
    - "The error surface and toasts are reached deliberately (a malformed deep link `ehpanda://attacker.example/g/…` raises the error sheet; a pause-refusal or copy toast raises a toast) and judged as rows, including the toast title/subtitle `lineLimit(1)` sites (D-11, D-04)."
    - "Findings are recorded and the sweep moves on; screenshots stay in the scratchpad; before/after paths go to the owner in chat (D-02, D-32, D-33)."
    - "The iPhone half of the matrix is complete after this plan: no iPhone row in § Matrix is `pending` (D-10)."
    - "Empty: Activity Logs with zero entries, Quick Search with zero items and an empty download folder are still judged as rows."
    - "Idempotency: `content_size` and orientation are restored to the baseline and read back at the end of the session."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Group C × iPhone matrix rows filled; Findings entries; D-04 rows updated; iPhone half complete"
      contains: "iPhone"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Matrix"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Findings"
      via: "every `finding:#N` cell references a numbered entry"
      pattern: "finding:#"
  prohibitions:
    - statement: "No screenshot or any image file is ever added to the repository (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow code and no reflow work order during the sweep (D-01)."
      status: resolved
      verification: judgment
    - statement: "The sweep never logs out, never clears the cache, never deletes the EhSetting profile, never changes a persisted setting it does not restore — every destructive settings confirmation is opened, judged and cancelled (D-09 infrastructure discipline)."
      status: resolved
      verification: judgment
---

<objective>
Walk the settings-and-sheets third of the inventory on the iPhone: surfaces #28–#42, six cells each, scroll-to-bottom, judged against D-03 / D-04, findings recorded without interruption. This completes the iPhone half of the 12-cell matrix.

Purpose: settings screens are dense with secondary values, fixed-width controls and confirmation dialogs — exactly the sites D-04 puts back in scope.
Output: 90 iPhone rows (15 surfaces × 6 cells) non-`pending`; the iPhone column of the matrix complete.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (rows filled here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iPhone × Group C surfaces #28–#35 (Setting root, Account, Login native, General, Activity Logs, Appearance, Reading setting, Download setting)</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Infrastructure, § Protocol, § Inventory rows #28–#35, § Matrix rows, § D-04 rows mapped to these screens (`SettingView.swift:109` 45-pt icon frame, `GeneralSettingView.swift:69` 50-pt width, `AppActivityLogsView.swift:224` category chip, `AppearanceSettingView.swift:146` 60-pt icon).
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 28–35, § D-04 checklist (fixed frames), § Pitfalls 1–3.
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md` D-11 — Login: native form and chrome in, WKWebView and Cloudflare out.
  </read_first>
  <action>
Start the session per § Protocol on `IPHONE_UDID` / `BUNDLE_ID`. Setting root (#28): judge the rows and their 45-pt icon chrome (`SettingView.swift:109`) — an icon overflowing its frame at AX5 is a finding only if it clips content, chrome shrink is fine. Account (#29): judge the cookie-state rows and open the logout confirmation as a cell, then cancel — never confirm (the session is phase infrastructure). Login (#30): open the native login form, judge its fields, buttons and chrome; do not submit; do not enter the WebView or trigger the challenge (D-11 excludes both). General (#31): judge the analytics opt-out row, translation rows, cache row and the 50-pt fixed width at `GeneralSettingView.swift:69`; open each confirmation dialog (clear cache etc.) as a cell and cancel. Activity Logs (#32): judge the list (category chips `AppActivityLogsView.swift:224` are D-04 rows), open a log detail sheet as a cell; if the list is empty, judge the empty state. Appearance (#33): judge the sections and open the App Icon picker (60-pt icon frames) as a cell set. Setting › Reading (#34), Download (#35): judge every row and picker. Six cells each per § Protocol (orientation × size, top/bottom screenshots at `--scale 0.5` under `$SCRATCHPAD/sweep/iphone-…`, scroll to the bottom, judge, record, move on). Disposition the D-04 rows mapped to these screens. Commit with `docs(16): sweep iPhone group C rows 28-35` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 28 29 30 31 32 33 34 35; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPhone" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 28–35, the six iPhone matrix rows are non-`pending`.
    - D-04 rows for `SettingView.swift:109`, `GeneralSettingView.swift:69`, `AppActivityLogsView.swift:224`, `AppearanceSettingView.swift:146` are no longer `pending`.
    - The summary states that no logout, no cache clear, and no login submission occurred.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPhone group C rows 28-35` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>Settings surfaces #28–#35 verified at six cells on the iPhone.</done>
</task>

<task type="auto">
  <name>Task 2: iPhone × Group C surfaces #36–#42 (Laboratory, About, EhSetting native, Filters, Quick Search, Date Seek, Error + toasts) and restore</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Protocol, § Inventory rows #36–#42, § Matrix rows, § D-04 rows mapped to these screens (`EhSettingView+Sections3.swift:131`, `EhSettingView+Sections2.swift:44,164,177` fixed widths, `CategoryView.swift:87` `CategoryCell`, `QuickSearchView.swift:40`, `DateSeekPickerView.swift:122`, `ToastMessageView.swift:65,70`, `TagSuggestionView.swift:111,116`, `View+Toast.swift:57` / `StateViews.swift:49` minimums).
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 36–42 and § D-04 checklist.
    - `EhPandaUITests/Support/UITestConstants.swift` — `malformedURL` shape (`ehpanda://attacker.example/g/<gid>/<token>/`) for raising the error surface by deep link.
  </read_first>
  <action>
Continue the session. Laboratory (#36): judge the feature cells (the tinted/gray cells are a round-2 DWC item — record layout only here). About (#37). EhSetting (#38): judge native sections 1–3 including the 200-pt fixed-width controls (`EhSettingView+Sections2.swift:164,177`) at AX5 — a control whose text clips inside 200 pt is a finding; open the delete-profile confirmation as a cell and cancel; do not open any web page row (D-11). Filters sheet (#39): open from the Frontpage toolbar; judge the category grid (`CategoryCell` names at AX5 — `CategoryView.swift:87` D-04 row) and the advanced section to the bottom. Quick Search sheet (#40) and its editor (add a throwaway item only if the list is empty, then delete the item you added — and nothing else). Date Seek picker (#41): judge the picker rows (`DateSeekPickerView.swift:122`). Error surface + toasts (#42): raise the error sheet by opening `ehpanda://attacker.example/g/3103480/0000000000/` via `agent-device open` with the URL (the app reports an unrecognised link) and judge it; raise a toast (e.g. copy a link from the Detail share/copy action, or the pause-refusal toast on a downloading gallery) and judge its title/subtitle (`ToastMessageView.swift:65,70`) at the six cells. Also judge the tag-suggestion rows (`TagSuggestionView.swift:111,116`) wherever the suggestion list appears (Search field typing) if not already dispositioned in Group A. Six cells each per § Protocol. Disposition the remaining D-04 rows. Restore `xcrun simctl ui <IPHONE_UDID> content_size <baseline>` and `agent-device orientation portrait`, read back, and commit with `docs(16): sweep iPhone group C rows 36-42` after the image check. Confirm in the summary that no iPhone row in § Matrix remains `pending`, list the findings opened (numbers, one-liners, scratchpad paths — D-33), and list any D-04 row still `pending` with the reason.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -E "iPhone" "$f" | grep -c "| pending"; for n in 36 37 38 39 40 41 42; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPhone" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "iPhone" 16-SWEEP.md | grep -c "| pending"` prints `0` — the iPhone half of the matrix is complete.
    - D-04 rows for `EhSettingView+Sections3.swift:131`, `EhSettingView+Sections2.swift:44,164,177`, `CategoryView.swift:87`, `QuickSearchView.swift:40`, `DateSeekPickerView.swift:122`, `ToastMessageView.swift:65,70`, `TagSuggestionView.swift:111,116` are no longer `pending`.
    - The summary states no EhSetting profile was deleted and no persisted setting was left changed.
    - `xcrun simctl ui <IPHONE_UDID> content_size` prints the recorded baseline after the restore.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPhone group C rows 36-42` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>The iPhone column of the 12-cell matrix is complete; every finding is numbered; the simulator is restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator screenshots → scratchpad → chat | Settings screens may show account state (cookie validity); scratchpad only, and the table records layout verdicts, never account values. |
| Agent → persisted settings / session | Destructive confirmations (logout, clear cache, delete profile) are opened and cancelled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | screenshots incl. Account screen | high | mitigate | Scratchpad-only; no cookie/account values in the table; image check before each docs commit. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in session | medium | mitigate | Logout and delete-profile confirmations cancelled; no login submission; baseline restored. |
| T-16-11 | Tampering | owner's persisted settings | low | mitigate | Any throwaway Quick Search item is removed; no other setting left changed. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. All 90 iPhone Group C rows non-`pending`; the whole iPhone column non-`pending`.
2. Every `finding:#N` has a Findings entry; all Group C D-04 rows dispositioned.
3. Simulator restored and read back; no image in the repo; two docs commits.
</verification>

<success_criteria>
- The iPhone half of the D-10 matrix is complete under D-03 / D-04, including the error surface and toasts, with the WebView / system-UI exclusions of D-11 honoured.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-06-SUMMARY.md` when done: findings opened, blocked / n/a rows, confirmation that the iPhone column is complete, commit hashes.
</output>
