---
phase: 16-dynamic-type-accessibility
plan: 21
type: execute
wave: 19
depends_on: ["16-20"]
files_modified:
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/SearchFeature/SearchRootView.swift
  - AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift
  - AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
  - AppPackage/Sources/HomeFeature/HomeView.swift
  - AppPackage/Tests/AppToolsTests/ReduceMotionGatingSourceTests.swift
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "The remaining meaningful-motion sites are gated: the inspector's `.scale(0.85)` transition becomes `.opacity` only (the view already reads `reduceMotion`); the list insert/remove/move animations keyed on membership (`filteredDownloads.map(\\.id)`, `quickSearchWords`, `historyGalleries`, `historyKeywords`, `listEditMode`, `folders`, `torrents`, the General tag-translation rows, Home's popular loading state) become `nil` under Reduce Motion so rows appear and disappear instantly instead of sliding (D-29)."
    - "Animations that only change opacity or colour stay ungated — Home's `.opacity(isEmpty ? 0 : 1)` crossfade is judged per site: if the keyed animation drives only opacity it stays, if it also drives row movement it is gated — and every decision is doc-commented at the site (D-29)."
    - "A Swift Testing source scan (`ReduceMotionGatingSourceTests`, repository-root walk with known members) pins the D-29 inventory: every in-scope site file contains an `accessibilityReduceMotion` read, the per-file count of gated predicates equals a pinned table, and the ungated `.contentTransition(.numericText` count repo-wide equals its pinned value — so a future regression or an over-gate both fail a test (D-29)."
    - "The environment value is read at the animating view, never mirrored into TCA state (RESEARCH Pitfall 10)."
    - "Verified on the simulator with the real Reduce Motion setting toggled and restored; each gated list observed in both states (D-31)."
    - "Gating changes no static layout; no D-25 screen is added (D-24)."
    - "Idempotency: the scan's pinned counts are exact equalities (never `>=`); re-running yields the same numbers; the simulator's Reduce Motion setting is restored and read back."
  artifacts:
    - path: "AppPackage/Tests/AppToolsTests/ReduceMotionGatingSourceTests.swift"
      provides: "pinned per-file reduceMotion read / gated-predicate counts and the repo-wide numericText count"
      contains: "accessibilityReduceMotion"
    - path: "AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift"
      provides: "opacity-only inspector transition under reduceMotion"
      contains: "accessibilityReduceMotion"
  key_links:
    - from: "AppPackage/Tests/AppToolsTests/ReduceMotionGatingSourceTests.swift"
      to: "AppPackage/Sources/**/*.swift (the D-29 inventory files)"
      via: "repository-root walk (`DownloadSourceInventoryTests` pattern): known members, fragment-assembled tokens, equality-pinned per-file table"
      pattern: "accessibilityReduceMotion"
  prohibitions:
    - statement: "Never gate an opacity crossfade or a `.contentTransition(.numericText())` on Reduce Motion; the scan pins the numericText count so an over-gate fails (D-29)."
      status: resolved
      verification: test
    - statement: "Never mirror `accessibilityReduceMotion` into TCA state or set it in a `#Preview`."
      status: resolved
      verification: judgment
    - statement: "Never fix parallel-test pollution with `.serialized`; the scan is a pure file read with no shared state."
      status: resolved
      verification: judgment
---

<objective>
Finish the Reduce Motion pass (D-29): the inspector's scale transition and the implicit list-diff slides across Downloads, Search root, Quick Search, FolderManager, Torrents, General settings and Home — `nil` or opacity-only under Reduce Motion — and pin the whole D-29 inventory with a source-scan test so neither a regression nor an over-gate can land silently.

Purpose: criterion 9 across the remaining surfaces, with a permanent, exact-count regression gate.
Output: eight view files gated with doc-commented reasons; `ReduceMotionGatingSourceTests`; simulator verification in both states.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating in Detail + reader (plan 16-20) and in lists / inspector (this plan); `OSLogEntryLog.Level.symbol` in `SettingFeature` (plan 16-22); D-28 colour fixes + rating-star decision (plan 16-23).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests` (plan 16-14), `AppPackage/Tests/AppToolsTests/ReduceMotionGatingSourceTests.swift` (`ReduceMotionGatingSourceTests`, this plan), `AccessibilityAuditUITests` (plan 16-24).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings` (none added by this plan).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Downloads inspector + list, Search root, Quick Search, FolderManager</name>
  <files>AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/SearchFeature/SearchRootView.swift, AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift, AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Reduce Motion inventory" (in-scope list-diff rows; the out-of-scope `countAnimation` / numericText sites in `DownloadsView+Subviews.swift:180–190` that must stay) and the planner's rule of thumb (position/size in, opacity/colour/digits out).
    - `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` lines 125–160 (`reduceMotion` already read at `:13` and `:152`; the `ProgressView` with `.transition(.opacity.combined(with: .scale(scale: 0.85)))` at `:140–143`) and lines 175–195 (the gated `countAnimation` — reference, untouched).
    - `AppPackage/Sources/DownloadsFeature/DownloadsView.swift` lines 85–95 — `.animation(.default, value: store.filteredDownloads.map(\.id))` with the Phase 15 plan-77 comment explaining the identity-keyed delete collapse (keep the comment; gate the animation).
    - `AppPackage/Sources/SearchFeature/SearchRootView.swift` lines 145–155; `AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift` lines 70–90; `AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift` lines 45–52.
    - `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` lines 129–131 — the `progressAnimation` computed property (Analog B) to copy.
  </read_first>
  <action>
Inspector: change the transition to `reduceMotion ? .opacity : .opacity.combined(with: .scale(scale: 0.85))` (the environment read already exists in that view). DownloadsView: `.animation(reduceMotion ? nil : .default, value: store.filteredDownloads.map(\.id))` with the environment read added and one doc-comment line appended to the existing explanation: under Reduce Motion the confirmed delete snaps out instead of collapsing — the manifest/SSOT and confirmation flow are unchanged. SearchRootView: gate the three membership-keyed animations (`quickSearchWords`, `historyGalleries`, `historyKeywords`). QuickSearchView: gate `quickSearchWords` and `listEditMode`. FolderManagerView: gate `folders` (rows) and judge `editingField` — gate only if it moves rows; if it only reveals a text field in place, leave it and doc-comment why. Each file gets `@Environment(\.accessibilityReduceMotion) private var reduceMotion` at the view that animates and a computed `private var listAnimation: Animation? { reduceMotion ? nil : .default }` where used more than once. Build; run `DownloadsFeatureTests` on the spare simulator (`DownloadSchedulingTests` deterministic — a failure is real). Commit `feat(16-21): gate downloads, search list motion`.
  </action>
  <verify>
    <automated>grep -c "accessibilityReduceMotion" AppPackage/Sources/DownloadsFeature/DownloadsView.swift AppPackage/Sources/SearchFeature/SearchRootView.swift AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift; grep -c "reduceMotion ? .opacity" AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:DownloadsFeatureTests 2>&1 | grep -E "passed|failed|error:" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityReduceMotion"` ≥ 1 in each of the four list files; `grep -c "reduceMotion ? .opacity" …/DownloadsView+Subviews.swift` ≥ 1.
    - `grep -c "numericText" …/DownloadsView+Subviews.swift` unchanged; the `countAnimation` block is byte-identical in `git diff`.
    - `DownloadsFeatureTests` passes on the spare simulator; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>Download, search and folder lists stop sliding under Reduce Motion; digits and crossfades still animate.</done>
</task>

<task type="auto">
  <name>Task 2: Torrents, General settings, Home; simulator verification in both states</name>
  <files>AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift, AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift, AppPackage/Sources/HomeFeature/HomeView.swift</files>
  <read_first>
    - `AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift` lines 50–58 (`.animation(.default, value: store.torrents)`); `AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift` lines 140–150 (three keyed animations: `tagTranslatorHasCustomTranslations`, `enableTagsExtension`, `tagTranslatorEmpty` — rows appear/disappear); `AppPackage/Sources/HomeFeature/HomeView.swift` lines 58–70 (`.opacity(isEmpty ? 0 : 1)` + `.animation(.default, value: store.popularLoadingState)` + `LoadingView` overlay).
    - `AppPackage/Sources/HomeFeature/GalleryCardCell.swift` lines 10–35 — the already-gated hero gradient and its comment style (reference only).
  </read_first>
  <action>
Torrents: gate `store.torrents`. General: gate the three row-visibility animations (rows inserting/removing = position motion). Home: judge `popularLoadingState` — if the keyed animation drives only the `.opacity` crossfade and the overlay's fade it stays ungated (doc-comment: opacity-only, D-29 out of scope); if it also moves section content, gate it; record the judgement. Build. Verification on `IPHONE_UDID` (install-over per the `16-SWEEP.md § Protocol › Install-over` rule (build by UDID into `$EVIDENCE_ROOT/DerivedData`, `plutil -extract CFBundleIdentifier raw` on the built `Info.plist` must equal `BUNDLE_ID` before `xcrun simctl install <UDID>`; never uninstall / erase / clear-app-state)): with Reduce Motion OFF then ON (Settings › Accessibility › Motion via `agent-device`, value recorded first and restored after): delete-then-cancel and filter the Downloads list, open the inspector (scale vs fade), add/remove a Quick Search item (then remove what you added), toggle the General tags-extension row, open Torrents, watch Home load — capture paired screenshots or short recordings under `$EVIDENCE_ROOT/round2/reduce-motion/`. Record the per-site OFF/ON table. Commit `feat(16-21): gate torrents, settings, home motion`.
  </action>
  <verify>
    <automated>grep -c "accessibilityReduceMotion" AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityReduceMotion"` ≥ 1 in `TorrentsView.swift` and `GeneralSettingView.swift`; `HomeView.swift` either gated or carrying the opacity-only doc comment (recorded).
    - The summary's OFF/ON table covers inspector, Downloads list, Search root, Quick Search, FolderManager (if reachable), Torrents, General rows, Home, with evidence paths; the Reduce Motion setting is recorded as restored.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif|mov|mp4)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>Every D-29 in-scope site is gated and observed in both states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `ReduceMotionGatingSourceTests` — pin the D-29 inventory</name>
  <files>AppPackage/Tests/AppToolsTests/ReduceMotionGatingSourceTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the doc comment (lines 4–40) and the pattern: repository-root walk, known-member guard, detection tokens assembled from fragments (so the test file does not itself match), per-file equality table, separately counted joined total.
    - `AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift` (plan 16-14) — the repository-root helpers already present in this target (reuse by extracting a small shared `RepositoryWalk` helper in the test target if both need it; do not duplicate silently).
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Reduce Motion inventory" — the in-scope and out-of-scope lists; the summaries of plans 16-20 and this plan — the final gated-site list.
  </read_first>
  <behavior>
    - Walking `AppPackage/Sources` yields the in-scope files (known members: `DetailView+HeaderSection.swift`, `ReadingView.swift`, `ControlPanel.swift`, `DownloadsView.swift`, `SearchRootView.swift`, `GeneralSettingView.swift`, …); each contains ≥ 1 `accessibilityReduceMotion` read (token assembled from fragments).
    - A pinned per-file table of the count of `reduceMotion ?` predicates (plus `withAnimation(reduceMotion` occurrences) equals the live counts exactly; a file outside the table containing a `reduceMotion ?` predicate fails (the table is exhaustive in both directions), so an over-gate elsewhere is caught.
    - The repo-wide count of `.contentTransition(.numericText` equals its pinned value (the D-29 "stays ungated" set) and none of those lines sits inside a `reduceMotion ?` predicate on the same line.
  </behavior>
  <action>
RED: write the suite with an empty table and observe it fail; GREEN: fill the table from the live tree (`grep -rn` to derive, then pin as equalities); refactor shared walk helpers into a `private` extension or a small `RepositoryWalk` enum in the test target. Doc-comment the suite with the WHY: D-29's scope is a judgement call made once (meaningful motion in, opacity/colour/digits out); this test freezes that call so neither a regression nor a well-meaning over-gate lands silently. Run `AppToolsTests` on the spare simulator; commit `test(16-21): pin Reduce Motion gating inventory`.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:AppToolsTests/ReduceMotionGatingSourceTests 2>&1 | grep -E "passed|failed|error:" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ReduceMotionGatingSourceTests` passes on the spare simulator; the RED run is recorded in the summary.
    - The per-file table in the test lists every file gated by plans 16-20 and 16-21 with exact counts; the numericText pin equals `grep -rc "contentTransition(.numericText" AppPackage/Sources | awk -F: '{s+=$2} END {print s}'` at HEAD (recorded).
    - Mutation check recorded: temporarily gating one numericText line (or removing one `reduceMotion ?`) makes the test fail; reverted.
    - Full `FeatureTests` plan green on the spare simulator after this plan (one invocation); lint build green.
  </acceptance_criteria>
  <done>The D-29 inventory is a test: exact, two-directional, and cheap to run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Animation predicate → download deletion presentation | Gating the identity-keyed List animation changes only presentation; the alert-first delete flow and manifest SSOT are untouched. |
| Simulator settings → owner's environment | Reduce Motion toggled and restored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-22 | Tampering (accidental) | over-gating numericText / crossfades | low | mitigate | Source scan pins the numericText count and the exhaustive per-file table. |
| T-16-19 | Tampering (accidental) | download delete flow | medium | mitigate | Only the `.animation` modifier changes; `DownloadsFeatureTests` green. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | § Protocol install-over rule (bundle id checked); setting restored and confirmed. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps; `DownloadsFeatureTests` green; lint build green.
2. OFF/ON observation table; setting restored.
3. `ReduceMotionGatingSourceTests` RED → GREEN with mutation check; full `FeatureTests` green; three commits.
</verification>

<success_criteria>
- Criterion 9 complete across the app under Apple's Reduced Motion criteria, with the D-29 scope frozen as an exact-count test (D-29).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-21-SUMMARY.md` when done: per-site gating with reasons (including the Home judgement), the OFF/ON table, the pinned counts, commit hashes.
</output>
