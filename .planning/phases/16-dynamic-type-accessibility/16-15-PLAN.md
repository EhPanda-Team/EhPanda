---
phase: 16-dynamic-type-accessibility
plan: 15
type: execute
wave: 13
depends_on: ["16-13", "16-14"]
files_modified:
  - AppPackage/Sources/AppComponents/CategoryView.swift
  - AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings
  - App/Assets.xcassets/Category/Colors/E-Hentai
  - App/Assets.xcassets/Category/Colors/ExHentai
  - AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift
  - .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "`CategoryLabel`'s hardcoded white text becomes black or white chosen from the resolved background's relative luminance via `Color.contrastingForeground(in:)` with `@Environment(\\.self)`, so the light / dark / Increase Contrast variant of the asset resolves correctly; 84/84 variants pass 4.5:1, 47 badges read black, and no category background byte moves (D-26)."
    - "The Filters `CategoryCell` — the second white-on-category site — adopts the same rule; its excluded state (category colour at 0.3 opacity over the Form's grouped background) chooses its text against the linear composite via `Color.Resolved.composited(over:opacity:)`, and the treatment of that state follows the owner's `CATEGORYCELL=` decision recorded in `16-CONTRAST-AUDIT.md` (D-26, D-22)."
    - "`CategoryCell` is a real control: a `Button` (`.buttonStyle(.plain)`) carrying the visible category name as its label, `.accessibilityAddTraits(.isSelected)` when included (the inverted `isFiltered` sense is documented), the haptic kept in the action — reachable by VoiceOver and listed by Voice Control under its visible name (D-20, D-30)."
    - "Increase Contrast variants are re-authored only under the owner's `HC=A` decision: the ≤ 40 `contrast: high` entries are rewritten from the audit's proposal table in each file's existing component encoding, the HC hash in `CategoryColorsetInvariantTests` is re-pinned deliberately, and the standard-44 pin is untouched; under `HC=B` no colorset byte changes and the residual is recorded (D-26, D-27)."
    - "Every non-obvious deliberate design carries a doc comment stating the why: the better-of rule, the composite for the excluded state, the inverted selection sense; no lint rule is suppressed (D-19)."
    - "Text-colour changes move no layout (D-24), so this plan adds a screen to the D-25 re-sweep list only when `CATEGORYCELL=B` adds a visible cue to the Filters sheet (D-24, D-25)."
    - "The visible result is reviewed by the owner from light / dark / Increase Contrast screenshots sent in chat (scratchpad paths), never committed images (D-20, D-32, D-33)."
    - "Adjacency: at the luminance crossover the text is white (strict `>`); the unit tests from plan 16-14 pin this and the view calls the same helper, so the tie never resolves differently on screen."
  artifacts:
    - path: "AppPackage/Sources/AppComponents/CategoryView.swift"
      provides: "Adaptive `CategoryLabel`, adaptive + native-control `CategoryCell`"
      contains: "contrastingForeground"
    - path: "AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift"
      provides: "HC pin re-derived under HC=A (standard pin unchanged)"
      contains: "SHA256"
  key_links:
    - from: "AppPackage/Sources/AppComponents/CategoryView.swift"
      to: "AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift"
      via: "`color.contrastingForeground(in: environment)` / `Color.contrastingForeground(on: composite)` — the view never re-implements the rule"
      pattern: "contrastingForeground"
    - from: "App/Assets.xcassets/Category/Colors"
      to: "AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift"
      via: "standard-44 pin must still pass after this plan; HC-40 pin re-pinned only under HC=A"
      pattern: "SHA256"
  prohibitions:
    - statement: "Never move a standard (non-HC) category background colour; the standard-44 pin must pass unchanged after this plan (D-26)."
      status: resolved
      verification: test
    - statement: "Never re-implement the luminance rule inline in a view or use a `white unless it fails` rule; the helper is the single source (D-26)."
      status: resolved
      verification: judgment
    - statement: "Never touch the `.lineLimit(1)` on `CategoryLabel` / `CategoryCell` in round 2 — those were round-1 owner items (D-01, D-04)."
      status: resolved
      verification: judgment
---

<objective>
Make the category badge text adaptive at both white-on-category sites (D-26) and turn the Filters `CategoryCell` into a real, selectable control, implementing the owner's `CATEGORYCELL=` decision for the excluded state and the `HC=` decision for the Increase Contrast variants (D-27) — with the invariant test proving the standard backgrounds never moved.

Purpose: Sufficient Contrast becomes claimable for the category system without moving a brand colour; the Filters grid becomes VoiceOver/Voice Control operable.
Output: `CategoryView.swift` adaptive + native semantics; colorsets unchanged (HC=B) or HC entries re-authored + HC pin re-derived (HC=A); owner-reviewed screenshots in chat.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` with `CategoryCell` as a `Button` + `.isSelected` (this plan); `OSLogEntryLog.Level.symbol` (plan 16-22); accessibility semantics (plans 16-16 … 16-19) and Reduce Motion gating (plans 16-20, 16-21).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests` (plan 16-14; HC pin re-derived here under HC=A), `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings` (this plan adds `accessibility.category_excluded` to AppComponents only if the chosen excluded-state treatment needs a spoken value).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voiceover-swiftui.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adaptive text on `CategoryLabel` and `CategoryCell`; `CategoryCell` becomes a selectable `Button`</name>
  <files>AppPackage/Sources/AppComponents/CategoryView.swift, AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings</files>
  <read_first>
    - `AppPackage/Sources/AppComponents/CategoryView.swift` — the whole file (`CategoryLabel` lines 28–37 `.foregroundStyle(.white)`; `CategoryCell` lines 74–100: `.white` text, `Rectangle().animation(.default) { $0.foregroundStyle(color.opacity(isFiltered ? 0.3 : 1)) }`, `.onTapGesture` + haptic, `.clipShape`).
    - `AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` — the helper API from plan 16-14 (`contrastingForeground(in:)`, `contrastingForeground(on:)`, `composited(over:opacity:)`).
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions — `CATEGORYCELL=A|B[:cue]`, `STARS=` (not this plan), and the measured grayscale number for the excluded state.
    - `AppPackage/Sources/FiltersFeature/FiltersView.swift` lines 25–95 — `CategoryView` sits inside a `Form` `Section` (grouped background): the backdrop for the composite is the section's card background in that context.
    - `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` lines 391–413 (`DownloadListRow`) and `AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` lines 162–190 (`favoriteButton`) — the house patterns for a real control and for state-as-trait.
    - `$HOME/.claude/skills/swift-accessibility-skill/references/voiceover-swiftui.md` — `.isSelected`, `.isButton`, `accessibilityRepresentation`; `SKILL.md` First-Draft Rules (state is a trait, not a label).
    - `.swiftlint.yml` — `lifecycle_modifiers`, `shape_initializer_argument`, `single_line_trailing_closure`, `accessibility_*` rules.
  </read_first>
  <behavior>
    - `CategoryLabel` renders black text on a light-luminance category (e.g. E-Hentai Manga light, L > 0.1791) and white on a dark one, in light and dark schemes and with Increase Contrast, without any change to padding, font, corner radius or background.
    - `CategoryCell` included: full-opacity background, adaptive text, `.isSelected` trait present; excluded: the chosen treatment (A: 0.3 opacity with text chosen against the composite; B: the named cue added, text still adaptive), `.isSelected` absent; tapping toggles `isFiltered` and fires the soft haptic exactly as before.
    - Source assertions: `grep -c "foregroundStyle(.white)" CategoryView.swift` prints `0`; `grep -c "onTapGesture" CategoryView.swift` prints `0`; `grep -c "Button" CategoryView.swift` ≥ 1; `grep -c "isSelected" CategoryView.swift` ≥ 1.
  </behavior>
  <action>
In `CategoryLabel`: add `@Environment(\.self) private var environment` and replace the white foreground with `color.contrastingForeground(in: environment)`; doc-comment on the property why the full environment is captured (the asset colour resolves its light/dark/HC variant from `colorScheme` + `colorSchemeContrast`). In `CategoryCell`: replace the `.onTapGesture` with a `Button` whose action toggles `isFiltered` and calls `hapticsClient.generateFeedback(.soft)`, `.buttonStyle(.plain)`, keeping the `Text(category.value)` label, padding, `frame(maxWidth: .infinity, maxHeight: .infinity)`, `.lineLimit(1)` (untouched — round-1 item), the animated background and the `.clipShape(.rect(cornerRadius: 5))`; add `.accessibilityAddTraits(isFiltered ? [] : .isSelected)` with a doc comment that `isFiltered == true` means excluded (inverted sense). Text colour: add `@Environment(\.self) private var environment`; for the included state `category.color(host:).contrastingForeground(in: environment)`; for the excluded state compute the composite — `category.color(host:).resolve(in: environment).composited(over: Color(.secondarySystemGroupedBackground).resolve(in: environment), opacity: 0.3)` (verify against `FiltersView` which grouped background the section card actually paints; use that one) — and `Color.contrastingForeground(on: composite)`; animate the text colour with the same `.animation(.default)` predicate as the background so the flip crossfades (a colour-only animation stays ungated per D-29). Under `CATEGORYCELL=B` add the owner's named cue for the excluded state (`strike`: `.strikethrough(isFiltered)` on the text; `nosign`: an overlaid `Image(systemSymbol: .nosign)` hidden from accessibility; `outline`: a dashed `.stroke` border) — keep it parity-neutral when not excluded, and add `accessibility.category_excluded` to the AppComponents catalog (six locales) as an `accessibilityValue` only if the cue is not already conveyed by the missing `.isSelected` trait (prefer the trait alone; add the value only under B). Doc-comment the WHY of the composite choice. Build with the lint plugin; run `AppToolsTests` (helper unchanged but sanity) and any AppComponents tests; commit `feat(16-15): adaptive category badge text`.
  </action>
  <verify>
    <automated>f=AppPackage/Sources/AppComponents/CategoryView.swift; grep -c "foregroundStyle(.white)" "$f"; grep -c "onTapGesture" "$f"; grep -c "contrastingForeground" "$f"; grep -c "isSelected" "$f"; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "foregroundStyle(.white)" AppPackage/Sources/AppComponents/CategoryView.swift` prints `0`; `grep -c "onTapGesture" …/CategoryView.swift` prints `0`; `grep -c "contrastingForeground" …/CategoryView.swift` ≥ 2; `grep -c "isSelected" …/CategoryView.swift` ≥ 1; `grep -c "lineLimit(1)" …/CategoryView.swift` prints the same number as before the edit (2 unless the owner changed it in round 1 — record the before count).
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`, no `swiftlint:disable` added (`grep -c "swiftlint:disable" …/CategoryView.swift` prints `0`).
    - Doc comments present on: the adaptive foreground (why better-of), the composite (why linear over the grouped background), the inverted `isFiltered` sense.
    - If `CATEGORYCELL=B`: the named cue is present only in the excluded state; if a catalog key was added, all six locales are filled (`python3 -c` check that `accessibility.category_excluded` has `en`, `de`, `ja`, `ko`, `zh-Hans`, `zh-Hant` localizations).
  </acceptance_criteria>
  <done>Both white-on-category sites are adaptive; the Filters grid cells are real selectable controls.</done>
</task>

<task type="auto">
  <name>Task 2: Increase Contrast variants per the `HC=` decision; re-pin the HC hash only</name>
  <files>App/Assets.xcassets/Category/Colors/E-Hentai, App/Assets.xcassets/Category/Colors/ExHentai, AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Increase Contrast variants (the proposal table: per `lower` HC variant, the re-authored sRGB value and resulting best-of ratio) and § Decisions (`HC=A|B`).
    - `AppPackage/Tests/AppToolsTests/CategoryColorsetInvariantTests.swift` — the two pins and the canonical serialization (from plan 16-14).
    - `App/Assets.xcassets/Category/Colors/E-Hentai/Manga.colorset/Contents.json` — the `contrast: high` entries' shape and the file's component encoding (hex / float / decimal differ per file; write in the file's own encoding).
  </read_first>
  <action>
Under `HC=B`: change nothing; append to `16-CONTRAST-AUDIT.md § Decisions` the line `HC residual: 19/40 HC variants remain below their standard sibling's best-of contrast (all ≥ 4.5:1); documented should-fix, not done by owner decision` and skip to the verification. Under `HC=A`: with a scratchpad script, rewrite only the `colors[]` entries whose `appearances` contain `contrast: high` and whose proposal row says `lower`, setting `components.red/green/blue` to the proposed sRGB value encoded in the same style the file already uses (`"0xRR"` hex byte, `"0.ddd"` float, or `"ddd"` decimal — match the sibling entries byte-for-byte in style), leaving `alpha`, `color-space`, `appearances`, key order and whitespace untouched so the diff is values-only; never touch an entry without `contrast: high`. Run `CategoryColorsetInvariantTests`: the standard pin must still pass, the HC pin must fail; compute the new HC digest from the test's serialization, re-pin it with a comment naming this plan and the decision, re-run: both pins pass, 84/84 ≥ 4.5 still holds, worst best-of re-asserted (it may improve; update the pinned worst value/owner only if the worst variant is an HC one — record which). Run the full `FeatureTests` plan on the spare simulator once. Commit `feat(16-15): re-author Increase Contrast category variants` (colorsets + test pin together).
  </action>
  <verify>
    <automated>git diff --stat HEAD~1 -- App/Assets.xcassets/Category/Colors | tail -1; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:AppToolsTests/CategoryColorsetInvariantTests 2>&1 | grep -E "passed|failed|error:" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Under `HC=B`: `git diff --stat <plan-start>..HEAD -- App/Assets.xcassets` is empty and the residual line is in the audit's Decisions section.
    - Under `HC=A`: every changed `Contents.json` diff touches only `red` / `green` / `blue` values inside entries that carry `contrast: high` (`git diff -U0 -- App/Assets.xcassets | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vc '"red"\|"green"\|"blue"'` prints `0`); the standard pin constant in the test is byte-identical to plan 16-14's; the HC pin constant changed.
    - `CategoryColorsetInvariantTests` passes; the full `FeatureTests` plan passes on `88B217DA-A166-4BAD-820D-DE13B1C4EB54`.
  </acceptance_criteria>
  <done>D-27 is implemented exactly as the owner decided, with the standard backgrounds provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Visual review evidence, D-25 tracking, commit</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` § Infrastructure (`IPHONE_UDID`, `BUNDLE_ID`, baselines) — the simulator to review on.
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions › D-25 re-sweep candidates.
  </read_first>
  <action>
Install the current build on `IPHONE_UDID` without touching its data container: `xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'platform=iOS Simulator,id=<IPHONE_UDID>'` then `xcrun simctl install <IPHONE_UDID> <path to the built .app under DerivedData>` (an install over an existing bundle id keeps the container; never `uninstall`, never `erase`); if the owner's `BUNDLE_ID` is `app.ehpanda.personal`, build the matching variant or review on the spare simulator instead and say so. Capture `--scale 1` screenshots to `$SCRATCHPAD/badge-review/` of a list with several category badges, the Detail header badge, and the Filters sheet (included + excluded cells) at `xcrun simctl ui <UDID> appearance light` / `dark` × `increase_contrast disabled` / `enabled`, then restore both settings and read them back. Send the paths with a one-line description each to the owner in chat (round 2 is owner-reviewed — D-20). Append `### 16-15 result` to the audit: which badges flipped to black (count, expected 47 of the rendered set), the excluded-cell composite luminance and chosen text in light/dark, and update `D-25 re-sweep candidates` (add `#39 Filters` only under `CATEGORYCELL=B`; note that text-colour changes add nothing per D-24). Commit `docs(16): record adaptive badge review` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "### 16-15 result" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `### 16-15 result` exists with the flip count, the composite numbers, and the D-25 candidate update.
    - `xcrun simctl ui <IPHONE_UDID> appearance` and `… increase_contrast` print the recorded baselines after the task.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): record adaptive badge review` exists; `grep -c "/Users/" 16-CONTRAST-AUDIT.md` prints `0`.
  </acceptance_criteria>
  <done>The owner can judge the visible change from chat; the D-25 list is current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset catalog edits → brand identity | Only HC entries may change, and only under HC=A; the standard pin guards the rest. |
| Simulator install → owner's data container | Install-over keeps the container; `uninstall` / `erase` are forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | standard category backgrounds | medium | mitigate | Standard-44 pin must pass after the plan; diff acceptance restricts edits to `red/green/blue` inside `contrast: high` entries. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | `simctl install` over the existing bundle only; baselines restored; no uninstall/erase. |
| T-16-01 | Information disclosure | review screenshots | high | mitigate | Scratchpad-only; image check before the docs commit. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps (no white foreground, no tap gesture, helper used, `.isSelected`); lint build green.
2. HC decision implemented; invariant test green with the standard pin unchanged; full `FeatureTests` green on the spare simulator.
3. Review screenshots sent; audit updated; simulator settings restored.
</verification>

<success_criteria>
- Criterion 10's category half: backgrounds byte-identical (standard), badge text adaptive at both sites, 84/84 ≥ 4.5:1, with the owner's HC and excluded-state decisions implemented exactly (D-26, D-27, D-22) and the Filters cells operable by assistive tech.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-15-SUMMARY.md` when done: what changed in `CategoryView.swift`, the HC outcome (and digests if re-pinned), review screenshot paths, D-25 update, commit hashes.
</output>
