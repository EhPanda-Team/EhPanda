---
phase: 16-dynamic-type-accessibility
plan: 25
type: execute
wave: 23
depends_on: ["16-24"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
  - .planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md
autonomous: false
requirements: [A11Y-01, A11Y-02]

must_haves:
  truths:
    - "Every screen where round 2 added a visible element or changed a rendered element's size — the union of `16-CONTRAST-AUDIT.md § D-25 re-sweep candidates`, the rows 16-23 appended to `16-SWEEP.md § D-25 re-sweep`, and any 16-24 fix that added a glyph — is re-walked on the D-09 iPhone and iPad at XXL / AX3 / AX5 in both orientations with the round-1 protocol (scroll to bottom, verdict rule D-03, strict truncation D-04), and nothing else is re-run (D-24, D-25)."
    - "Each re-swept cell is recorded in `16-SWEEP.md § D-25 re-sweep` with the round-1 status vocabulary (`pass | finding:#N | re-verify | accepted`), the screen number from the round-1 inventory, and the round-2 plan that touched it; a finding caused by a round-2 addition is fixed by the agent in the owning file and re-verified, and a finding in owner-owned reflow is reported as `finding:#N` for the owner exactly as in round 1 (D-01, D-20, D-25)."
    - "`16-NUTRITION-LABEL.md` follows the skill's §4 template: app version evaluated (commit hash), scope reviewed, `You could claim` / `You should not claim` with reasons, the common-task verification matrix (the eight common tasks from 16-24 × the nine categories), and the recommendation summary; every cell carries an evidence pointer (a sweep row, an audit section, a walkthrough row id, or a test name) and every one of the nine categories is addressed, with Captions and Audio Descriptions marked `—` with the reason that EhPanda renders images and text only (D-21, D-31)."
    - "A category that is not claimable is never silently claimed and never silently dropped: it appears under `You should not claim` with the measured numbers and the blocking decision named, and the owner checkpoint presents it for an explicit accept-or-reopen (D-22)."
    - "Phase-close gates pass before the owner checkpoint: `FeatureTests` plan green on the spare iPhone, `UITests` plan green on the spare iPhone (run one after the other, never together), standalone SwiftLint reports 0 violations, the five custom rules are present at error severity, and no image or video file was added by any phase-16 commit (`git log --diff-filter=A --name-only --format= a8829305..HEAD` filtered for image extensions prints nothing) (D-16, D-19, D-32)."
    - "The requirement ids `A11Y-01` and `A11Y-02` are each traced in the nutrition label's scope section to the evidence that satisfies them (round-1 sweep sign-off; round-2 sections) (D-34)."
    - "Idempotency: a round-1 `pass` row whose screen round 2 did not touch is not re-run, and a re-swept cell that passes is recorded once; a second walk of the same cell would produce the same verdict because the app build and the simulator settings are pinned in the section header."
    - "Concurrency: one simulator action at a time — `content_size`, orientation and screenshots are never issued to the iPhone and iPad in the same instant, and the two test-plan runs are sequential."
    - "Precision: every number quoted in the label (contrast ratios, counts of variants, counts of rows) is copied from the audit or sweep tables, never recomputed by hand."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "## D-25 re-sweep filled: header (build, settings), per-cell rows, closure"
      contains: "D-25 re-sweep"
    - path: ".planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md"
      provides: "Accessibility Nutrition Label recommendation per SKILL §4 with evidence pointers and owner sign-off"
      contains: "Accessibility Nutrition Label recommendation"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      via: "Larger Text column cites the round-1 sign-off and the D-25 re-sweep rows"
      pattern: "16-SWEEP.md"
    - from: ".planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md"
      to: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      via: "Sufficient Contrast and Differentiate Without Color columns cite the audit sections"
      pattern: "16-CONTRAST-AUDIT.md"
  prohibitions:
    - statement: "Never claim a category whose common-task column holds a ❌, and never mark a category `—` that applies to the app (D-21, D-22)."
      status: resolved
      verification: judgment
    - statement: "Never re-run the full 12-pass matrix as the re-sweep; only touched screens (D-25)."
      status: resolved
      verification: judgment
    - statement: "Never erase, uninstall or `clear-app-state` the D-09 simulators; install-over only, always by UDID (D-09)."
      status: resolved
      verification: judgment
    - statement: "Never commit an image; the phase-range added-files check is part of the gate (D-32)."
      status: resolved
      verification: test
---

<objective>
Close the phase: re-walk only the screens round 2 touched (D-25) on the logged-in simulators at the three sizes, write the App Store Accessibility Nutrition Label recommendation in the skill's §4 format with an evidence pointer in every cell, run the final build/test/lint/image gates, and obtain the owner's sign-off — reopening, not hiding, any category that is not claimable.

Purpose: D-21 is the bar; D-25 closes the staleness hole round 2 opened in round 1's evidence; D-22 guarantees an unclaimable category comes back with numbers.
Output: `16-SWEEP.md § D-25 re-sweep` filled; `16-NUTRITION-LABEL.md`; owner sign-off.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (re-sweep closed here), `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (this plan creates it).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); VoiceOver / Voice Control semantics (plans 16-16 … 16-19); Reduce Motion gating (plans 16-20, 16-21); colour and DWC remediation (plan 16-22); walkthrough fixes (plan 16-24); any re-sweep fix (this plan).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `ReduceMotionGatingSourceTests`, `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/nutrition-labels.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-25 targeted re-sweep of touched screens</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions › D-25 re-sweep candidates (16-13, 16-15, 16-22 entries) and `16-SWEEP.md § D-25 re-sweep` (16-23 entries) plus the 16-24 summary's fix list — the complete touched-screen set.
    - `16-SWEEP.md § Infrastructure` and § Protocol — UDIDs, bundle id, baseline settings, the cell procedure (content size by UDID, `agent-device orientation`, `open --foreground`, scroll to bottom, screenshot to scratchpad), the verdict rule and the D-04 reading.
    - `16-SWEEP.md § Inventory` — the screen numbers and routes for each touched screen.
    - The 16-04 … 16-09 plans' Task 1 action text — the exact cell procedure to repeat (same commands, same restore step).
  </read_first>
  <action>
Build the simulator app once (`xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'platform=iOS Simulator,id=<IPHONE_UDID>' -derivedDataPath <scratchpad>/sim-dd`) and install-over on both D-09 simulators with `xcrun simctl install <UDID> <scratchpad>/sim-dd/Build/Products/Debug-iphonesimulator/EhPanda.app` (never `uninstall`/`erase`). Write the re-sweep header into `16-SWEEP.md § D-25 re-sweep`: build commit, both UDIDs, the touched-screen list with the round-2 plan that touched each, and the restore baselines. For each touched screen run the 12 cells (iPhone/iPad × portrait/landscape × `extra-extra-extra-large` / `accessibility-extra-large` / `accessibility-extra-extra-extra-large`), one simulator action at a time, screenshots to `<scratchpad>/resweep/<device>-<orientation>-<size>-<screen#>-{top,bottom}.png --scale 0.5`, and append a row `screen # | device | orientation | size | status | note | touched by` per cell. A `finding:#N` whose cause is a round-2 addition (a glyph that now wraps, a badge that now truncates) is fixed by the agent in the owning file under the round-2 idioms and `.large` parity (D-15), re-verified across the same 12 cells, and committed `fix(16-25): <screen> re-sweep finding`; a finding in untouched owner-owned reflow is left `finding:#N` with a description for the owner (it is surfaced at the Task 3 checkpoint). If the iPad has no session (`IPAD_LOGIN=none`), iPad cells for gated screens are `blocked: no iPad session`, as in round 1. Restore `content_size`, orientation, appearance and `increase_contrast` on both simulators and read them back. Send the owner the before/after screenshot paths for any finding (D-33). Close with `### Re-sweep closure` (screens, cells, passes, findings, fixes) and commit `docs(16): D-25 targeted re-sweep` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; sed -n '/^## D-25 re-sweep/,/^## /p' "$f" | grep -c "| pass\|| finding:#\|| accepted\|| blocked"; grep -c "### Re-sweep closure" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The `## D-25 re-sweep` section has a header naming the build commit and both UDIDs, one row per (touched screen × 12 cells), and `### Re-sweep closure`; the touched-screen list equals the union of the candidate lists (none omitted, none extra).
    - Every row's status is `pass`, `accepted`, `blocked: no iPad session`, or `finding:#N` with a description; no `pending`.
    - `xcrun simctl ui <IPHONE_UDID> content_size` and the iPad equivalent print the recorded baselines; orientation is portrait on both.
    - Any `fix(16-25)` commit passed the owning module's tests and a lint build; image check prints `0`; `grep -c "/Users/" "$f"` prints `0`; the commit `docs(16): D-25 targeted re-sweep` exists.
  </acceptance_criteria>
  <done>Round 1's evidence is current again for exactly the screens round 2 touched.</done>
</task>

<task type="auto">
  <name>Task 2: Nutrition Label recommendation and phase-close gates</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md</files>
  <read_first>
    - `$HOME/.claude/skills/swift-accessibility-skill/SKILL.md` § 4 (the exact output template) and `references/nutrition-labels.md` (the nine categories' official criteria — cite the criterion number per cell where one applies).
    - `16-SWEEP.md` § Owner sign-off (round 1), § Round-2 manual walkthrough (16-24) › Walkthrough closure, § D-25 re-sweep › Re-sweep closure.
    - `16-CONTRAST-AUDIT.md` — every section (category variants, Increase Contrast, non-category, DWC, 16-15 result, 16-22 result, Automated audit (16-23)).
    - The 16-16 … 16-21 summaries — the semantics and motion gates shipped, with their test names (`ReduceMotionGatingSourceTests`, `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`).
    - `.planning/REQUIREMENTS.md` § A11Y — the exact `A11Y-01` / `A11Y-02` text to trace.
  </read_first>
  <action>
Run the gates first, one at a time: `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54'`; after it exits, the same with `-testPlan UITests`; the standalone SwiftLint from the DerivedData artifact bundle over the repo (0 violations; `grep -c "no_minimum_scale_factor\|no_dynamic_type_size_modifier\|no_geometry_reader\|no_fixed_system_font_size\|accessibility_hardcoded_string" .swiftlint.yml` prints `5` rule headings); `git log --diff-filter=A --name-only --format= a8829305..HEAD | grep -Ei '\.(png|jpe?g|heic|gif|mov|mp4)$' | wc -l` prints `0`. Then write `16-NUTRITION-LABEL.md` exactly in the §4 shape: `**Accessibility Nutrition Label recommendation**`, `**App version evaluated:**` (HEAD hash), `**Scope reviewed:**` (the eight common tasks; the 42-surface inventory; the A11Y-01 / A11Y-02 trace), `**You could claim:**` / `**Why you could claim them:**`, `**You should not claim:**` / `**Why you should not claim them:**`, the `**Common-task verification**` table (eight rows × nine columns, each cell ✅ / ❌ / — followed by a bracketed evidence pointer such as `[sweep #14 AX5 pass]`, `[audit § 16-22 result]`, `[walk 1.7 pass]`, `[AccessibilityAuditUITests.testDetail]`), and `**Recommendation summary**`. Captions and Audio Descriptions are `—` with the reason "image and text content only; no audio or video is played" cited to `references/nutrition-labels.md`. A column with any ❌ goes under `You should not claim` with the measurement and the decision it traces to (D-22). Commit `docs(16): nutrition label recommendation` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md; grep -c "Accessibility Nutrition Label recommendation" "$f"; grep -c "^| " "$f"; grep -c "A11Y-01\|A11Y-02" "$f"; grep -c "/Users/" "$f"</automated>
  </verify>
  <acceptance_criteria>
    - Both test-plan runs report every test passed (run sequentially); standalone SwiftLint prints `0` violations; the five-rule grep prints `5`; the phase-range added-image check prints `0`.
    - `16-NUTRITION-LABEL.md` contains every heading of the §4 template in order; the common-task table has exactly eight task rows and nine category columns; every cell has an evidence pointer in brackets; `grep -c "A11Y-01\|A11Y-02" "$f"` is at least `2`; `grep -c "/Users/" "$f"` is `0`.
    - Every category is addressed: Captions and Audio Descriptions are `—` with a reason; no category is `—` elsewhere without a criterion citation.
    - Any `You should not claim` entry names the measurement and the decision to reopen.
    - The commit `docs(16): nutrition label recommendation` exists.
  </acceptance_criteria>
  <done>The recommendation is written to the skill's template, fully evidenced, and the repository gates are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner reviews the Nutrition Label recommendation and the re-sweep</name>
  <action>Stop and present the recommendation and the re-sweep closure; accept-or-reopen is the owner's call (D-22).</action>
  <what-built>`16-NUTRITION-LABEL.md` (the App Store Accessibility Nutrition Label recommendation with an evidence pointer in every common-task cell), `16-SWEEP.md § D-25 re-sweep` (touched screens re-walked at XXL / AX3 / AX5 on both logged-in simulators), and green phase-close gates (FeatureTests, UITests, SwiftLint 0, no images added in the phase).</what-built>
  <how-to-verify>
    1. Read `.planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md`: check each `You could claim` category against its column in the common-task table and the evidence pointers; open any pointer you doubt (sweep row, audit section, walkthrough row, test name).
    2. Read `16-SWEEP.md § D-25 re-sweep`: confirm the touched-screen list matches what round 2 changed and that no row is `pending`; any `finding:#N` left for you is listed in `### Re-sweep closure`.
    3. If a category sits under `You should not claim`, read its numbers and the decision it traces to, and choose: accept the non-claim, or reopen that decision.
    4. Confirm in a terminal that `git log --diff-filter=A --name-only --format= a8829305..HEAD | grep -Ei '\.(png|jpe?g|heic|gif|mov|mp4)$'` prints nothing.
  </how-to-verify>
  <resume-signal>Type "approved", or "reopen: <category>=<decision id> <note>" for each category whose blocking decision you want revisited, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the owner's sign-off or reopen list</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md</files>
  <read_first>
    - The owner's checkpoint reply.
    - `.planning/STATE.md` — the phase-16 position lines to be synced by the orchestrator after this plan.
  </read_first>
  <action>
Append `## Owner review` to `16-NUTRITION-LABEL.md`: date, HEAD hash, the reply verbatim. On `approved`, write `Status: signed off` and commit `docs(16): record nutrition label sign-off`. On `reopen:` lines, write each as `Reopen: <category> — <decision id> — <note>` with the measurement that blocked it, state that the reopen is to be planned as a gap round (`/gsd-plan-phase 16 --gaps`, which reads the verification gaps — the orchestrator's verifier must transcribe these reopen lines into `16-VERIFICATION.md`'s `gaps:` so they are not lost), and commit `docs(16): record nutrition label review`; do not edit any decision in `16-CONTEXT.md` yourself. Either way, the summary lists the phase's final commit range and the three artifact paths.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-NUTRITION-LABEL.md; grep -c "## Owner review" "$f"; grep -c "Status: signed off\|Reopen: " "$f"; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `## Owner review` exists with the reply verbatim and either `Status: signed off` or one `Reopen:` line per reopened category with its measurement and decision id.
    - The last commit is `docs(16): record nutrition label sign-off` or `docs(16): record nutrition label review`; the image check prints `0`.
    - No decision text in `16-CONTEXT.md` was edited by this plan (`git diff --stat <plan-start>..HEAD -- .planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md` is empty).
  </acceptance_criteria>
  <done>The phase ends with an owner-signed recommendation or an explicit, routed reopen list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → D-09 simulators | Install-over by UDID; settings restored; never erased. |
| Recommendation → App Store claim | Every claimed category rests on cited evidence; unclaimable ones are surfaced, not hidden. |
| Scratchpad screenshots → repo | Phase-range added-files check makes an accidental image commit visible at the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03 | Tampering | D-09 logged-in simulators | high | mitigate | `simctl install` over the existing app by UDID; no erase/uninstall/clear-app-state. |
| T-16-01 | Information disclosure | adult-content screenshots | high | mitigate | Scratchpad-only; `git log --diff-filter=A` image check over the whole phase range. |
| T-16-18 | Repudiation | an over-claimed label | medium | mitigate | Evidence pointer per cell; ❌ forbids the claim; owner checkpoint. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Re-sweep section complete with no `pending`; simulators restored.
2. FeatureTests + UITests green sequentially; lint 0; five rules present; phase-range image check 0.
3. Nutrition label in §4 shape with evidence pointers; owner review recorded.
</verification>

<success_criteria>
- D-25 closes the staleness hole on exactly the touched screens; the Nutrition Label recommendation meets D-21's bar with evidence per cell, honours D-22 for anything unclaimable, traces A11Y-01 / A11Y-02 (D-34), and the owner has signed off or explicitly reopened.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-25-SUMMARY.md` when done: touched screens and cells, findings/fixes, gate results, claimable vs non-claimable categories, the owner's reply, commit hashes.
</output>
