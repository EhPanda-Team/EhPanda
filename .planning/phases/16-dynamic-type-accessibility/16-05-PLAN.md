---
phase: 16-dynamic-type-accessibility
plan: 05
type: execute
wave: 4
depends_on: ["16-04"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every in-scope Group B surface (#14–#27: Gallery Detail, Previews, Comments + post sheet, Detail Search, Gallery Infos, Archives, Torrents, Tag Detail, NewDawn, download confirm dialogs, Reading, Control panel, Reading Setting sheet, Live Text overlay) has six iPhone rows that are no longer `pending` (D-05, D-10)."
    - "Verdicts follow the owner's rule — less information at a larger size is degraded; wraps, taller rows and dropped decoration are not — and the strict truncation reading: an ellipsised uploader, date, page count or category token at XXL / AX3 / AX5 is a finding even though Phase 10 waved it through (D-03, D-04)."
    - "The three D-13 rows that render in Group B — Detail stats-strip abbreviation (#14), long-tag right-edge clip in the tag cloud (#14), reader total-page counter wrap (#25) — carry an `Observed` note from the AX5 cells; the counter wrap is observed, not yet dispositioned (a wrap is not degradation under D-03, so `accepted` is a legitimate later close) (D-13)."
    - "Reference screenshots at `.large` (`content_size large`) of the four surfaces that host the five `minimumScaleFactor` sites — Detail header category label, Detail comment cells, Comments view header, a `GalleryDetailCell` in a list — are captured BEFORE the owner's removals to the persistent evidence root `$HOME/Library/Caches/ehpanda-phase16/d15-baseline/` under the fixed names `large-gallery-detail-cell.png`, `large-detail-header.png`, `large-detail-comment-cells.png`, `large-comments-view.png`, so they survive the owner checkpoints (16-10, 16-11) that separate capture from comparison and D-15 parity can be judged before/after in plan 16-11 by file name (D-14, D-15)."
    - "Findings are recorded and the sweep moves on; screenshots live only under the evidence root (`$HOME/Library/Caches/ehpanda-phase16/`), never the repo; before/after paths go to the owner in chat (D-02, D-32, D-33)."
    - "Account-gated surfaces (live Detail, Comments, Archives, Torrents, Reading) are reached through the owner's hand-logged-in session on `IPHONE_UDID`; no credential is handled and the simulator is never erased or reinstalled (D-09)."
    - "Adjacency: a value that exactly fills its width at AX5 without clipping is `pass`; a value abbreviated away (the stats-strip case) is a finding even if nothing is visibly cut."
    - "Empty: the reader's control panel and the comments list are judged even when the gallery has zero comments; the empty comments state is a row."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Group B × iPhone matrix rows filled; Findings entries; D-04 and D-13 rows updated; D-15 baseline capture recorded"
      contains: "D-15 baseline"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § D-13 named edge cases"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Matrix"
      via: "each D-13 row names the screen # and cells where it was observed"
      pattern: "Observed"
  prohibitions:
    - statement: "No screenshot or any image file is ever added to the repository (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow code and no reflow work order during the sweep (D-01)."
      status: resolved
      verification: judgment
    - statement: "The sweep never purchases an archive, never posts a comment, never votes, never changes a favorite category — every sheet that can mutate the account is opened, judged, and cancelled (D-09 infrastructure discipline)."
      status: resolved
      verification: judgment
---

<objective>
Walk the detail-and-reader third of the inventory on the iPhone: surfaces #14–#27, six cells each, scroll-to-bottom, judged against D-03 / D-04, findings recorded without interruption. Also capture the `.large` reference screenshots of the four surfaces that host the five `minimumScaleFactor` sites before the owner touches them, so D-15's "no visible change at the default size" half can be verified later.

Purpose: the detail screens hold three of the five pre-registered D-13 edge cases and four of the five D-14 sites; this group is where D-04's strict reading bites hardest (uploader, date, page count, category token).
Output: 84 iPhone rows (14 surfaces × 6 cells) non-`pending`, findings numbered, D-13 rows #14/#14/#25 observed, D-15 baselines captured.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (rows filled here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-15 `.large` baselines, then iPhone × Group B surfaces #14–#20 (Detail, Previews, Comments, Detail Search, Gallery Infos, Archives, Torrents)</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Infrastructure, § Protocol, § Inventory rows #14–#20, § Matrix rows for these screens, § D-04 rows mapped to #14–#20 (`DetailView+HeaderSection.swift:72,324`, `DetailView+Subviews.swift:99,116`, `DetailView+CommentCells.swift:37,43,51`, `CommentsView.swift:166`, `TorrentsView.swift:110,124`, `ArchivesView.swift:143,202`, `TagCloudView.swift:122`, `CategoryView.swift:31`), § D-13 rows "Detail stats-strip abbreviation" and "long-tag right-edge clip".
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md` D-14 (the five `minimumScaleFactor` sites) and D-15 (parity outranks the ban; the 0.72 factor on the Detail header category label plausibly engages at default).
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 14–20, § D-04 checklist, Pitfalls 1–3.
  </read_first>
  <action>
First the D-15 baselines, before any owner edit exists: set `xcrun simctl ui <IPHONE_UDID> content_size large` and `agent-device orientation portrait`, open the app via `BUNDLE_ID`, `mkdir -p "$EVIDENCE_ROOT/d15-baseline"` (the persistent evidence root from § Infrastructure), and capture full-scale (`--scale 1`) reference screenshots under it with exactly these names: `large-gallery-detail-cell.png` — a Frontpage list showing at least one `GalleryDetailCell` with a long uploader name and full stats (`GalleryDetailCell.swift:155,166` shrink sites); `large-detail-header.png` — the Detail header of a gallery whose category name is long enough to plausibly engage the 0.72 shrink (`DetailView+HeaderSection.swift:73`) — prefer a Non-H / Western / Asian Porn gallery on the host the session is on; `large-detail-comment-cells.png` — the Detail comment-cells section (`DetailView+CommentCells.swift:42`); `large-comments-view.png` — the full Comments view header (`CommentsView.swift:165`). Record, under a new `### D-15 baseline` note in § Infrastructure, the four file names, the screen each shows, the gallery's category (not its title or id), the HEAD hash the build was made from and the `content_size large` setting used — written with the `$HOME` form of the root, never expanded; these are evidence for chat and for plan 16-11's comparison, not repo artifacts, and the table rows never carry a filename. Then the matrix: for Detail (#14) pick a gallery with a long title, long uploader and a dense tag cloud; walk header, stats strip, description, tag cloud, previews and comments preview to the bottom at each of the six cells; record under the D-13 rows `Observed` what the stats strip does at AX5 (abbreviated values = the pre-registered case) and whether any long tag clips at the right edge of the cloud. Previews (#15), Comments (#16 — also open the post-comment sheet, judge it as a cell set, cancel without posting), Detail Search (#17 — tap a tag), Gallery Infos (#18), Archives sheet (#19 — judge, never purchase; if funds/price lines `ArchivesView.swift:143,202` ellipsise, that is a finding), Torrents sheet (#20 — the meta lines `:110,:124` were "shrink-absorbed" in Phase 10 and now have no shrink to absorb them: judge strictly). Same six-cell procedure as § Protocol (orientation × size, top/bottom screenshots at `--scale 0.5` under `$EVIDENCE_ROOT/sweep/iphone-…`, scroll to the bottom, judge, record, move on). Disposition the D-04 rows mapped to these screens. Commit with `docs(16): sweep iPhone group B rows 14-20` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "### D-15 baseline" "$f"; for n in 14 15 16 17 18 19 20; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPhone" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `### D-15 baseline` exists in § Infrastructure naming the four files and the surface each shows; `ls "$HOME/Library/Caches/ehpanda-phase16/d15-baseline/"` lists exactly `large-comments-view.png`, `large-detail-comment-cells.png`, `large-detail-header.png`, `large-gallery-detail-cell.png`.
    - For each screen # in 14–20, the six iPhone matrix rows are non-`pending` (`pass`, `finding:#N`, or `blocked: <reason>` only where the session genuinely cannot reach the surface).
    - D-13 rows "Detail stats-strip abbreviation" and "long-tag right-edge clip" carry non-empty `Observed` notes naming the cells.
    - D-04 rows for `HeaderSection.swift:72,324`, `Subviews.swift:99,116`, `CommentCells.swift:37,43,51`, `CommentsView.swift:166`, `TorrentsView.swift:110,124`, `ArchivesView.swift:143,202`, `TagCloudView.swift:122`, `CategoryView.swift:31` are no longer `pending`.
    - No comment posted, no archive purchased, no vote cast (summary states this explicitly).
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPhone group B rows 14-20` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>D-15 baselines banked; #14–#20 verified at six cells; findings numbered.</done>
</task>

<task type="auto">
  <name>Task 2: iPhone × Group B surfaces #21–#27 (Tag Detail, NewDawn, download confirm dialogs, Reading, Control panel, Reading Setting sheet, Live Text overlay) and restore</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Protocol, § Inventory rows #21–#27, § Matrix rows, § D-04 rows mapped to #24–#26 (`ControlPanel.swift:176` page indicator, `:166,:296` 44-pt touch targets), § D-13 row "reader total-page counter wrap".
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 21–27 and § "Reader (screen #24) specifics".
    - `AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift` lines 160–240 — the page indicator `Text(title)` with `.lineLimit(1)` and the panel buttons (read to know what to look for; do not edit).
  </read_first>
  <action>
Continue the session. Tag Detail (#21): long-press a tag in the Detail tag cloud and open its detail sheet. NewDawn (#22): only if the greeting is presented in this session; otherwise record all six rows `blocked: greeting not presented this session` (it is time-of-day gated) and list it in the summary for the owner. Download confirm dialogs (#23): on a gallery already downloaded by the sweep (plan 16-04 created one), tap the download button to raise the delete / retry-mode dialog, judge it as a cell set, cancel. Reading (#24): open the reader from Detail, judge the page, the tap zones' reachability and the initial page indicator; Control panel (#25): show the panel, judge upper and lower bars, the slider and its preview, the dual-page and auto-play menus as cells, and record under the D-13 row "reader total-page counter wrap" what the `n / total` indicator does at AX5 in both orientations (wraps to two lines = observed wrap, the pre-registered case; remember D-03 makes a wrap non-degraded — record only, do not disposition). Reading Setting sheet (#26): from the panel gear. Live Text overlay (#27): enable Live Text from the panel; if the overlay draws no app-owned text, record the six rows `n/a: no app-drawn text (system overlay)`; the share sheet is excluded by D-11. Same six-cell procedure; scroll where scrollable; screenshots at `--scale 0.5` under `$EVIDENCE_ROOT/sweep/iphone-…`. Disposition the D-04 rows mapped to these screens (`ControlPanel.swift:176` is the D-13 item; the 44-pt frames at `:166,:296` are touch-target chrome — `fine` unless the glyph overflows the frame). Restore `xcrun simctl ui <IPHONE_UDID> content_size <baseline>` and `agent-device orientation portrait`, re-read `content_size`, commit with `docs(16): sweep iPhone group B rows 21-27` after the image check. The summary lists the findings opened in this plan (numbers, one-liners, evidence-root paths — D-33), the D-13 observations, and any `blocked` / `n/a` rows with reasons.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 21 22 23 24 25 26 27; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPhone" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 21–27, the six iPhone matrix rows are non-`pending`; `blocked:` appears only on #22 (greeting not presented) or with a stated reason; `n/a:` appears only on #27 with the stated reason.
    - The D-13 row "reader total-page counter wrap" has a non-empty `Observed` note naming AX5 portrait and landscape and is still not dispositioned (`fixed` / `accepted` reserved for plan 16-11).
    - D-04 rows for `ControlPanel.swift:176`, `:166`, `:296` are no longer `pending`.
    - `xcrun simctl ui <IPHONE_UDID> content_size` prints the recorded baseline after the restore.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPhone group B rows 21-27` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>Group B fully verified on the iPhone; reader D-13 item observed; simulator restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator screenshots → evidence root → chat | Detail and reader screenshots show real gallery content; evidence root (`$HOME/Library/Caches/ehpanda-phase16/`) only. |
| Agent → owner's account state | Sheets that mutate the account (post comment, vote, purchase archive, favorite) are opened and cancelled, never confirmed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | gallery-content screenshots | high | mitigate | Evidence-root-only paths; written descriptions; image check before each docs commit; no gallery identifiers in the table. |
| T-16-10 | Tampering | owner's account state via sheets | medium | mitigate | Cancel-only discipline on post / vote / archive / favorite sheets; summary states no mutation happened. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | UDID-addressed; forbidden list; baseline restored and re-read. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. All 84 iPhone Group B rows non-`pending`; findings entries present for every `finding:#N`.
2. D-13 rows for #14 (two) and #25 observed; Group B D-04 rows dispositioned; D-15 baselines captured (four fixed-name files under the evidence root, names recorded).
3. No account mutation; simulator restored; no image in the repo; two docs commits.
</verification>

<success_criteria>
- Group B verified on the iPhone under D-03 / D-04 at all six cells, with the D-14/D-15 reference state banked for the parity check that follows the owner's removals.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-05-SUMMARY.md` when done: findings opened, D-13 observations, D-15 baseline file names, blocked / n/a rows, commit hashes.
</output>
