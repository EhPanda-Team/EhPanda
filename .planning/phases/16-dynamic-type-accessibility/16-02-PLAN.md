---
phase: 16-dynamic-type-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "`16-SWEEP.md` exists as the single committed round-1 artifact: a text-only verdict table keyed by (screen #, device, orientation, size) with the status vocabulary `pending | pass | finding:#N | re-verify | accepted`, and no image file anywhere in the repo (D-32)."
    - "The screen inventory is re-derived against today's tree (HEAD after plan 16-01), not inherited from Phase 10: it starts from RESEARCH § Screen inventory (42 surfaces), re-greps the presentation sites, and records every addition since Phase 10 — Cloudflare challenge (excluded), analytics opt-out row, Phase 15 download inspector / validation rows / pause-refusal toast, `SystemNotification` toasts, the App Icon picker (D-12)."
    - "Scope follows D-11: every SwiftUI screen, sheet, popover, menu, alert, confirmation dialog and toast is a row; WebView-rendered pages (EhSetting web pages, Cloudflare challenge), the ShareExtension, and system UI (`BGContinuedProcessingTask` card, share sheet, photo picker) are listed once as excluded with the reason (D-11)."
    - "The matrix is 12 cells per in-scope screen — iPhone + iPad × portrait + landscape × XXL / AX3 / AX5 — with the `content_size` tokens `extra-extra-extra-large` / `accessibility-extra-large` / `accessibility-extra-extra-extra-large` recorded next to the size names; iPad rows are first-class, never derived from iPhone rows (D-05, D-06, D-10)."
    - "The five ROADMAP-criterion-4 edge cases are pre-registered as named rows in their own section (Detail stats-strip abbreviation, long-tag right-edge clip, reader total-page counter wrap, Favorites trailing-glyph clip, hero-carousel title truncation), each starting `pending` and closing only as `fixed` or `accepted (owner reason recorded)` (D-13)."
    - "The D-04 re-judgement checklist is a section of rows — the 30 `lineLimit(1)` code sites, the 5 `minimumScaleFactor` sites and the fixed-frame sites from RESEARCH § D-04 — each mapped to the screen # where it renders, each starting `pending`, with the note that Phase 10's secondary-text exemption no longer applies (D-04)."
    - "The large end only is sampled: the table has no `xSmall` or Bold Text columns, and the protocol section says why (D-07)."
    - "Boundary: AX5 (`accessibility-extra-extra-extra-large`) is the maximum sampled size and `.large` is the parity anchor; no row claims a size beyond AX5 and the protocol names `large` as the restore/parity value (D-06, D-15)."
    - "Ordering: rows are keyed by (screen #, device, orientation, size); a session resumes at the first `pending` or `re-verify` row in key order, so verdict order is stable across sessions."
    - "Empty: screens that can be content-free (empty Favorites, empty Downloads, empty History, empty Search history) are still rows; the empty-state view is judged like any other content."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Inventory, 12-cell matrix skeleton, findings section, D-13 rows, D-04 checklist, sweep protocol, infrastructure header"
      contains: "accessibility-extra-extra-extra-large"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      to: ".planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md"
      via: "inventory rows carry the RESEARCH route + primary-files columns so owner fix commits map files → screens for batched re-verification"
      pattern: "Primary files"
  prohibitions:
    - statement: "No image file (`*.png`, `*.jpg`, `*.jpeg`, `*.heic`, `*.gif`) is ever added to the repository — not even of a content-free screen; screenshots live only under the session scratchpad (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow or layout code in round 1 and produces no reflow work order; it audits, drives, records, reports and re-verifies only (D-01)."
      status: resolved
      verification: judgment
---

<objective>
Build the round-1 state machine: `16-SWEEP.md`, the text-only verdict table that every sweep plan appends to, every owner fix is re-verified against, and the owner eventually signs. It carries the re-derived screen inventory (D-12), the 12-cell matrix skeleton (D-10), the findings section (D-02), the five pre-registered D-13 rows, the D-04 re-judgement checklist, the sweep protocol (commands, restore rules, evidence rules), and an infrastructure header that plan 16-03 fills in with the owner's simulator UDIDs and bundle id.

Purpose: make the sweep resumable across sessions and plans (Claude's-discretion item from CONTEXT, resolved here), and make "report the complete list once every page has been scanned" (D-02) mechanical.
Output: `16-SWEEP.md` committed, with zero `pass` rows (nothing is verified yet) and zero image files.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (this plan creates it), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `AppPackage/Sources/AppTools/Extensions/Color+Contrast.swift` (`Color.Resolved.relativeLuminance`, `Color.contrastRatio(_:_:)`, `Color.contrastingForeground(in:)`); `CategoryLabel` / `CategoryCell` adaptive text colour and `CategoryCell` as a `Button`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@.planning/phases/10-ui-polish/10-10-SUMMARY.md
@.planning/phases/10-ui-polish/10-11-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the screen inventory and write the matrix skeleton, D-13 rows and D-04 checklist</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Screen inventory, re-derived (focus area 2 / D-12)" — the 42-row table with route, primary files, login-gated and D-11 columns; § "D-04 re-judgement checklist" — the 30 `lineLimit(1)` sites, 5 `minimumScaleFactor` sites, fixed frames; § "Round-1 verification mechanics" — token table and resumability shape.
    - `.planning/phases/10-ui-polish/10-10-SUMMARY.md` § "Per-screen coverage" — Phase 10's starting checklist (use as a diff base only; D-12 says do not inherit it).
    - `.planning/phases/10-ui-polish/10-11-SUMMARY.md` lines 105–112 — the B1–B10 verdict-table shape to copy (PATTERNS § `16-SWEEP.md`).
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md` D-03, D-04, D-11, D-12, D-13 — the verdict rule in the owner's words and the scope/inventory decisions.
  </read_first>
  <action>
Re-derive the inventory against HEAD: re-run the presentation-site greps the research used (`.sheet(`, `.fullScreenCover(`, `.popover(`, `.alert(`, `.confirmationDialog(`, `Menu {`, `NavigationLink`/`.navigationDestination(`, `toast`) across `AppPackage/Sources` and diff the result against the RESEARCH 42-row table; add any surface the research missed, drop nothing silently (a dropped row needs a reason). Write `16-SWEEP.md` with these sections, in this order:

1. `## Infrastructure` — placeholders `IPHONE_UDID`, `IPAD_UDID`, `BUNDLE_ID`, `SPARE_UDID` (prefill `SPARE_UDID = 88B217DA-A166-4BAD-820D-DE13B1C4EB54` — iPhone 17e, iOS 26.4, UI tests only), baseline `content_size` / `appearance` / `increase_contrast` / orientation per sweep simulator (filled by plan 16-03), and the forbidden-command list for the sweep UDIDs (`simctl erase`, `simctl uninstall`, `agent-device settings clear-app-state`, any `xcodebuild test` destination).
2. `## Verdict rule` — D-03 verbatim in the owner's words ("degraded means the interface provides less information under larger font size setting; removing decoration to save space is okay, but the interface should always provide same contents"), the fine / degraded examples, and the D-04 strict truncation reading (no secondary-text exemption; Phase 10 "fine" verdicts do not carry).
3. `## Inventory` — one row per surface: `# | Screen | Route | Primary files | Login-gated | Group | D-11`, Group ∈ `A (#1–#13) | B (#14–#27) | C (#28–#42)` matching the sweep plans 16-04…16-09; excluded surfaces (WebView pages, Cloudflare challenge, ShareExtension, system UI) listed in a short `### Excluded (D-11)` table with reasons.
4. `## Matrix` — the skeleton: one row per (screen #, device, orientation, size) with `Status = pending` and an empty `Finding` column. Twelve rows per in-scope screen; sizes named `XXL (extra-extra-extra-large)`, `AX3 (accessibility-extra-large)`, `AX5 (accessibility-extra-extra-extra-large)`; devices `iPhone`, `iPad`; orientations `portrait`, `landscape`. Note under the heading: "XXL" = iOS `xxxLarge` (slider 7), the Phase 10 D-03 meaning; AX5 is the maximum (D-06); no `xSmall` / Bold Text columns (D-07).
5. `## Findings` — empty numbered list template: `#N | Screen | Cells affected | Description (written, no filenames) | Status (open | fixed-by <commit> | re-verified | accepted)`.
6. `## D-13 named edge cases` — five rows, each `pending`: Detail stats-strip abbreviation (#14, `DetailView+Subviews.swift` stats strip), long-tag right-edge clip (#14, `TagCloudView.swift`), reader total-page counter wrap (#25, `ControlPanel.swift` page indicator — note D-03 makes a wrap non-degraded, so `accepted` on the rule alone is a valid close, still recorded), Favorites trailing-glyph clip (#8, `FavoritesView.swift` toolbar/menu + `GalleryDetailCell` trailing symbols), hero-carousel title truncation (#2, `GalleryCardCell.swift` `lineLimit(4)`). Closing vocabulary: `fixed` | `accepted (owner reason: …)`.
7. `## D-04 checklist` — one row per RESEARCH § D-04 site (30 `lineLimit(1)`, 5 `minimumScaleFactor`, fixed frames / widths), columns `Site (file:line at HEAD) | What is clipped | Screen # | Phase-10 verdict | D-04 status (pending | fine | finding:#N | removed-by <commit>)`.
8. `## D-25 re-sweep` — empty section reserved for round 2 (rows appended by plan 16-25).

Paths are repository-relative; never write an absolute home path. Do not write any image path that implies recoverability from the repo.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "^## " "$f"; grep -c "| pending" "$f"; grep -c "accessibility-extra-extra-extra-large" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " 16-SWEEP.md` prints `8` (Infrastructure, Verdict rule, Inventory, Matrix, Findings, D-13 named edge cases, D-04 checklist, D-25 re-sweep).
    - The matrix has exactly 12 rows per in-scope inventory screen: `grep -c "| pending" 16-SWEEP.md` ≥ 12 × (number of in-scope rows) + 5 (D-13) + 40 (D-04 rows), and `grep -c "| pass" 16-SWEEP.md` prints `0`.
    - The five D-13 names appear verbatim: `grep -c "stats-strip abbreviation\|long-tag right-edge clip\|total-page counter wrap\|trailing-glyph clip\|hero-carousel title truncation" 16-SWEEP.md` prints `5` or more.
    - `grep -c "xSmall\|Bold Text" 16-SWEEP.md` prints a number ≥ 1 only inside the D-07 note (the matrix has no such column: `grep -c "| xSmall |" 16-SWEEP.md` prints `0`).
    - `grep -c "/Users/" 16-SWEEP.md` prints `0`.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>The verdict table exists with every cell `pending`, the five D-13 rows and the D-04 checklist registered, and no image anywhere in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Write the sweep protocol section and commit the skeleton</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Pattern 1: Round-1 sweep command sequence (verified)", § "Common Pitfalls" 1–3 and 12, § "Evidence discipline".
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md` § Specifics — the literal procedure ("max out the font size, open every screen, scroll down to the bottom, confirm nothing is degraded") and D-02 / D-32 / D-33.
    - `$HOME/.claude/skills/agent-device/SKILL.md` — `open --platform ios --udid`, `snapshot -i`, `press`, `scroll down … --settle`, `screenshot --out … --scale`, `orientation`, one action at a time.
  </read_first>
  <action>
Add a `## Protocol` section (it becomes the ninth `## ` heading) between `## Verdict rule` and `## Inventory`, stating the per-screen procedure every sweep plan follows verbatim: (1) at session start read and record `xcrun simctl ui <UDID> content_size`, `appearance`, `increase_contrast` and the current orientation for the sweep simulator, and open the app with `agent-device open <BUNDLE_ID> --platform ios --udid <UDID> --foreground`; (2) navigate to the screen once via its inventory route; (3) for each orientation (`agent-device orientation portrait` then `landscape-left`) and each size (`xcrun simctl ui <UDID> content_size extra-extra-extra-large`, `accessibility-extra-large`, `accessibility-extra-extra-extra-large` — always the UDID, never `booted`, because two simulators are booted): take a snapshot, screenshot the top of the screen to `$SCRATCHPAD/sweep/<device>-<orientation>-<size>-<screen#>-top.png` with `--scale 0.5`, `agent-device scroll down … --settle` repeatedly until the bottom (the owner's rule: above-the-fold is not verified), screenshot the bottom, and judge the cell against D-03 / D-04 from the screenshots (the AX snapshot still reports a truncated Text's full label, so the image is the verdict basis); (4) record the row: `pass`, or `finding:#N` with a written description and a new Findings entry; never interrupt the sweep to raise a finding (D-02); (5) after the last screen of a session, restore `content_size` to the recorded baseline (`large` if the baseline was `large`; the research observed `medium` on the iPhone Air — restore exactly what was read) and orientation to portrait. Evidence rules: screenshots only under the scratchpad (any `/tmp` path), never under the repo; findings get a written description, never a filename; before/after images for each finding are sent to the owner in chat (D-33) by stating the scratchpad path and describing the image. Resumability: a session starts at the first `pending` or `re-verify` row in key order; partial progress is committed after each screen group with `docs(16): sweep <device> <group> rows`. Forbidden on the sweep UDIDs: `xcrun simctl erase`, `xcrun simctl uninstall`, `agent-device settings clear-app-state`, and any `xcodebuild test` destination (D-09). Add the D-07 note (no `xSmall`, no Bold Text — the rule is about information lost when text grows). Commit the file with `docs(16): add sweep verdict table skeleton` after confirming `git status --porcelain` shows no image files.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "^## Protocol" "$f"; grep -c "never .booted.\|never \`booted\`" "$f"; grep -c "scroll" "$f"; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " 16-SWEEP.md` prints `9`.
    - `grep -c "content_size extra-extra-extra-large" 16-SWEEP.md` ≥ 1 and `grep -c "accessibility-extra-large" 16-SWEEP.md` ≥ 2 (both the AX3 and AX5 tokens are present).
    - `grep -ci "scroll" 16-SWEEP.md` ≥ 1 and the protocol names scroll-to-bottom as mandatory.
    - `grep -c "clear-app-state" 16-SWEEP.md` ≥ 1 (forbidden list present).
    - `git log -1 --format=%s` equals `docs(16): add sweep verdict table skeleton` and `git show --stat HEAD` lists only `16-SWEEP.md`.
    - `git ls-files | grep -Ei '\.(png|jpe?g|heic|gif)$' | grep -c "16-dynamic-type-accessibility"` prints `0`.
  </acceptance_criteria>
  <done>The sweep protocol is written once, in the owner's literal terms, and the skeleton is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scratchpad → repository | Screenshots of real gallery content at AX5 must never cross into the public repo. |
| Planning doc → owner | The verdict table is the only evidence the owner signs; it must be complete and keyed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | `16-SWEEP.md` commits | high | mitigate | Text-only rows; screenshot root is the scratchpad; `git status --porcelain` image check in every docs-commit acceptance criterion (D-32). |
| T-16-06 | Repudiation | verdict traceability | low | mitigate | Rows keyed by (screen, device, orientation, size); findings numbered; owner signature recorded in plan 16-12. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. `16-SWEEP.md` has nine `## ` sections, 12 `pending` matrix rows per in-scope screen, 5 D-13 rows, the D-04 checklist rows, and a protocol section naming the three `content_size` tokens, scroll-to-bottom, the restore rule and the forbidden list.
2. No image file tracked or untracked under the repo.
3. One docs commit touching only `16-SWEEP.md`.
</verification>

<success_criteria>
- Inventory re-derived (D-12), scoped per D-11, matrix per D-05/D-06/D-10, D-13 rows and D-04 checklist registered, protocol in the owner's literal terms, evidence rules per D-32/D-33.
- The sweep is resumable: any later session can start from the first `pending` row.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-02-SUMMARY.md` when done, listing the inventory row count, any surfaces added or dropped relative to RESEARCH (with reasons), and the commit hash.
</output>
