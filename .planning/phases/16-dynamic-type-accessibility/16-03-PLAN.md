---
phase: 16-dynamic-type-accessibility
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: false
requirements: [A11Y-01]

must_haves:
  truths:
    - "The owner signs in by hand, once per sweep simulator, through the app's own WKWebView login; the agent never handles a credential — no cookie value, member id, pass hash or igneous value passes through a prompt, an env file, shell history, or any artifact (D-09)."
    - "The `EHPANDA_AUTOMATION_IPB_MEMBER_ID` / `IPB_PASS_HASH` / `IGNEOUS` launch seam is never proposed, referenced as an option, or used — it was rejected on credential-exposure grounds (D-09)."
    - "The owner names the sweep simulator UDID(s) and the bundle id that holds the login (`app.ehpanda` vs `app.ehpanda.personal`); both are recorded in `16-SWEEP.md § Infrastructure` and every later command addresses that UDID explicitly — never `booted` (D-09, D-08)."
    - "The logged-in simulators are phase infrastructure: from this plan on, no plan erases, resets, uninstalls, `clear-app-state`s, or uses them as an `xcodebuild test` destination; UI tests run on `SPARE_UDID` only (D-09)."
    - "The sweep runs on the Simulator, not the physical device, because `xcrun simctl ui <UDID> content_size` and `agent-device orientation` make the 12-pass matrix scriptable (D-08)."
    - "Pre-flight confirms research assumption A1 (a running EhPanda re-lays out live on `content_size` changes without relaunch) and A6 (XXL = `extra-extra-extra-large`, iOS `xxxLarge`, slider 7) on the real sweep simulator before any matrix row is walked; the outcome is recorded in `16-SWEEP.md` (D-05)."
    - "Idempotency: the simulator baseline (`content_size`, `appearance`, `increase_contrast`, orientation) is read and recorded before the first change and restored exactly after pre-flight, so a re-run starts from the same state."
    - "The pre-flight screenshot is written under the persistent evidence root `$HOME/Library/Caches/ehpanda-phase16/preflight/` named in `16-SWEEP.md § Protocol`, never under the repo; no credential vocabulary appears in § Infrastructure (the inventory row for the Account screen may legitimately name its cookie-state rows — that is layout vocabulary, not a credential) (D-09, D-32)."
    - "Concurrency: one simulator action at a time; pre-flight drives one simulator at a time and never overlaps with an `xcodebuild` invocation."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Infrastructure header filled: IPHONE_UDID, IPAD_UDID, BUNDLE_ID, baselines, pre-flight results"
      contains: "BUNDLE_ID"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      to: "xcrun simctl ui <UDID>"
      via: "every sweep plan reads IPHONE_UDID / IPAD_UDID / BUNDLE_ID from the header"
      pattern: "IPHONE_UDID"
  prohibitions:
    - statement: "Never erase, reset, uninstall, `clear-app-state`, or use as an `xcodebuild test` destination any simulator the owner logged in on; those UDIDs are phase infrastructure (D-09)."
      status: resolved
      verification: judgment
    - statement: "Never propose, reference as an alternative, or use the `EHPANDA_AUTOMATION_IPB_MEMBER_ID` / `IPB_PASS_HASH` / `IGNEOUS` credential seam; never let a credential value touch the agent (D-09)."
      status: resolved
      verification: judgment
    - statement: "Never address a simulator as `booted` — two simulators are booted and `booted` silently resolves to either; always pass the UDID (RESEARCH Pitfall 1)."
      status: resolved
      verification: judgment
---

<objective>
Open the account-gated half of the app to the sweep without the agent ever touching a credential (D-09), and resolve RESEARCH Open Question 1 (which simulator and which bundle id hold the login) before any matrix row is walked. The owner signs in by hand on the sweep simulator(s); the agent records the UDIDs and bundle id as phase infrastructure, reads and restores the simulator baseline, and runs a two-minute pre-flight that confirms live re-layout on `content_size` changes (A1) and the XXL token (A6).

Purpose: D-09 is the one legitimate "the agent must not do this" step in round 1 — credential entry — and everything after it depends on knowing exactly which simulator to drive.
Output: `16-SWEEP.md § Infrastructure` filled and committed; a confirmed, restored sweep environment.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (infrastructure header filled here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (`Color.Resolved.relativeLuminance`, `Color.contrastRatio(_:_:)`, `Color.contrastingForeground(in:)`); adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner hand-login on the sweep simulator(s) and naming of UDID + bundle id</name>
  <action>Stop and hand over to the owner. Do not launch the app, do not open the login screen, do not touch any cookie or env var. The agent's only action before this checkpoint is to print the list below so the owner can choose; the agent must never see or relay a credential (D-09).</action>
  <what-built>Nothing yet — this is the D-09 credential boundary. Plans 16-01 (lint rules) and 16-02 (`16-SWEEP.md` skeleton) are committed; the sweep needs a logged-in session on the simulator(s) it will drive.</what-built>
  <how-to-verify>
    The booted simulators right now (from `xcrun simctl list devices booted`):
    1. iPhone Air, iOS 26.5 — `ADE09605-A44E-4F00-BE12-235970217355`
    2. iPad Pro 11-inch (M5), iOS 26.5 — `8250D97E-9AB0-42FD-99DB-07B0094BF8C7`
    Both carry `app.ehpanda` and `app.ehpanda.personal` (the `$(BUNDLE_ID_SUFFIX)` variant). The spare iPhone 17e `88B217DA-A166-4BAD-820D-DE13B1C4EB54` is reserved for UI tests and will never hold a login.

    Please:
    1. On the iPhone Air simulator, open the EhPanda build you want swept (say which bundle id) and sign in through the app's own login (Setting › Account › Login, WKWebView). Leave the session in place.
    2. D-10 makes iPad a first-class device with its own layouts (Setting and gallery detail are presented modally on the pad idiom), and its login-gated screens (Favorites, Watched, Archives, Torrents, EhSetting, FolderManager, live Detail/Comments/Reading) need a session too. Either sign in on the iPad Pro 11 simulator as well (preferred), or tell me the iPad rows for login-gated screens should be recorded as `blocked: no iPad session` — they will then be surfaced in the findings report rather than silently skipped.
    3. Do not erase or reset either simulator from now until the phase closes; the agent will not either.
  </how-to-verify>
  <resume-signal>Reply with exactly: `IPHONE_UDID=<udid> IPAD_UDID=<udid or none> BUNDLE_ID=<app.ehpanda|app.ehpanda.personal>` (for example `IPHONE_UDID=ADE09605-A44E-4F00-BE12-235970217355 IPAD_UDID=8250D97E-9AB0-42FD-99DB-07B0094BF8C7 BUNDLE_ID=app.ehpanda`). If the iPad has no session, write `IPAD_UDID=8250D97E-9AB0-42FD-99DB-07B0094BF8C7 IPAD_LOGIN=none` so the iPad is still swept for the non-gated screens.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the infrastructure, read the baselines, run the pre-flight, restore, commit</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` § Infrastructure and § Protocol — the placeholders to fill and the restore rule.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Round-1 verification mechanics" — `content_size` token table, "Does the app pick up `content_size` live?" (A1), orientation read-back, Pitfalls 1–3; § Assumptions Log A1 and A6.
    - `$HOME/.claude/skills/agent-device/SKILL.md` — `open --platform ios --udid <UDID> --foreground`, `snapshot`, `screenshot --out`, `orientation`.
  </read_first>
  <action>
Fill `16-SWEEP.md § Infrastructure` with the owner's `IPHONE_UDID`, `IPAD_UDID`, `BUNDLE_ID` (and `IPAD_LOGIN=none` if given — then add a note that iPad rows of login-gated screens are recorded `blocked: no iPad session` and surfaced in plan 16-10's report). For each sweep simulator, read and record the baseline before changing anything: `xcrun simctl ui <UDID> content_size`, `xcrun simctl ui <UDID> appearance`, `xcrun simctl ui <UDID> increase_contrast`, and the orientation from `agent-device snapshot` after `agent-device open <BUNDLE_ID> --platform ios --udid <UDID> --foreground` (the header tag shows the orientation). Pre-flight on the iPhone only: with the app in the foreground on Home, switch `xcrun simctl ui <IPHONE_UDID> content_size accessibility-extra-extra-extra-large`, wait two seconds, `mkdir -p "$EVIDENCE_ROOT/preflight"` (the `EVIDENCE_ROOT` from § Infrastructure) then `agent-device screenshot --out "$EVIDENCE_ROOT/preflight/ax5.png" --scale 0.5`, and confirm from the image that the app re-laid out without a relaunch (A1 confirmed) — if it did not, record `A1: relaunch required` and note that every sweep cell will use `agent-device open … --relaunch` after a size change. Then switch to `extra-extra-extra-large` and confirm it is visibly smaller than AX5 but larger than the baseline (A6: XXL = `xxxLarge`, slider 7); record both outcomes under `### Pre-flight` in the Infrastructure section. Confirm the login is live by opening the Favorites tab once and recording only `login: present` or `login: absent` — record no account detail, no username, nothing from the page. Restore `content_size`, `appearance`, `increase_contrast` and orientation to the recorded baselines on every simulator touched, and re-read them to prove it. Commit with `docs(16): record sweep infrastructure and pre-flight` after the `git status --porcelain` image check. Leave `$EVIDENCE_ROOT/preflight/ax5.png` in place — it is outside the repo and is the first A1 evidence.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "IPHONE_UDID = \|IPHONE_UDID=" "$f"; grep -c "BUNDLE_ID = app.ehpanda\|BUNDLE_ID=app.ehpanda" "$f"; grep -c "### Pre-flight" "$f"; grep -ci "IPB_MEMBER_ID\|PASS_HASH\|IGNEOUS" "$f"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `16-SWEEP.md § Infrastructure` contains the owner-supplied `IPHONE_UDID`, `IPAD_UDID`, `BUNDLE_ID` values and, per simulator, the recorded baseline `content_size`, `appearance`, `increase_contrast`, orientation.
    - `### Pre-flight` records `A1: live re-layout confirmed` or `A1: relaunch required`, and `A6: XXL = extra-extra-extra-large confirmed`.
    - `grep -ci "IPB_MEMBER_ID\|PASS_HASH\|IGNEOUS" 16-SWEEP.md` prints `0`; `awk '/^## Infrastructure/,/^## Verdict rule/' 16-SWEEP.md | grep -ci "cookie"` prints `0` (no credential vocabulary in the infrastructure header — the grep is scoped to § Infrastructure because inventory row #29, Setting › Account, legitimately describes its cookie-state rows as layout).
    - After restore, `xcrun simctl ui <IPHONE_UDID> content_size` prints the recorded baseline value and `xcrun simctl ui <IPHONE_UDID> appearance` prints the recorded baseline value.
    - `ls "$HOME/Library/Caches/ehpanda-phase16/preflight/ax5.png"` succeeds; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
    - `git log -1 --format=%s` equals `docs(16): record sweep infrastructure and pre-flight`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>The sweep environment is named, baselined, pre-flighted and restored; the credential boundary was never crossed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner → simulator data container | The login session lives only in the simulator; the agent drives the app but never reads, copies or exports the session. |
| Agent → planning artifact | The infrastructure header must carry UDIDs and a bundle id, never a credential or account detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Information disclosure | credentials via launch env / shell history / artifacts | high | mitigate | Hand login only; the IPB/IGNEOUS seam is never referenced; the artifact records `login: present` and nothing else; acceptance greps the artifact for credential vocabulary. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulators | medium | mitigate | Forbidden-command list recorded; UI tests pinned to `SPARE_UDID`; every command addresses a UDID, never `booted`. |
| T-16-08 | Tampering | simulator left in a changed state | low | mitigate | Baselines read first and restored with a read-back after pre-flight. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Owner checkpoint resumed with the UDID/bundle-id signal.
2. Infrastructure header filled; pre-flight outcomes A1 / A6 recorded; baselines restored and re-read.
3. No credential vocabulary in the artifact; no image in the repo; docs commit landed.
</verification>

<success_criteria>
- D-09 honoured exactly: the owner logged in by hand; the agent touched no credential and never mentioned the rejected seam.
- Open Question 1 resolved in the artifact; the sweep plans can address the simulators by UDID and the app by bundle id.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-03-SUMMARY.md` when done, recording the UDIDs, bundle id, baselines, pre-flight outcomes and the commit hash — and nothing about the account.
</output>
