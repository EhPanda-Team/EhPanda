---
phase: 16-dynamic-type-accessibility
plan: 16
type: execute
wave: 14
depends_on: ["16-15"]
files_modified:
  - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift
  - AppPackage/Sources/DetailFeature/DetailView+CommentCells.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift
  - AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift
  - AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "Every icon-only control in DetailFeature carries a concise VoiceOver label from a catalog key — the favorites `Menu` label (heart), the download progress ring's centre glyph (hidden, the button is already labelled), the FolderManager toolbar/row glyphs — and every decorative glyph next to text (coin glyphs beside archive costs, the page-count glyph, vote thumbs beside a score) is hidden or combined with its text, so nothing is announced twice or not at all (D-20)."
    - "State is expressed as traits or values, never as label text: a voted comment exposes its vote as an `accessibilityValue` from a catalog key; the favourited state stays the existing two-`Label` swap (D-20)."
    - "Custom tappables become native controls: the Archives option cell is a `Button` with `.disabled`; comment link runs are exposed as named `accessibilityAction`s (one per link, from the `accessibility.open_link` key) so VoiceOver and Voice Control can follow them; the comment-cell `.contextMenu` items get explicit `accessibilityAction`s only if the owner's `CONTEXTMENU=not-exposed` observation says iOS 26 does not surface them already (D-20, D-22)."
    - "Voice Control: every DetailFeature interactive element is a `Button` / `Menu` / `Toggle` / link action with a name equal to its visible text or its catalog label; no hardcoded accessibility string exists (the `accessibility_hardcoded_string` rule errors on one) (D-30)."
    - "New `accessibility.*` keys in `DetailFeature/Resources/Localizable.xcstrings` carry all six locales; numeric arguments, if any, are named `%#@variable@` substitutions; string arguments positional (CLAUDE.md catalog rules)."
    - "No control with visible text receives an extra `accessibilityLabel` (over-labelling desynchronises Voice Control — RESEARCH Pitfall 7); labels land only on icon-only and custom elements (D-20)."
    - "Labels are not rendered, so this plan changes no layout and adds no screen to the D-25 list (D-24)."
    - "Each edited control is verified in the live accessibility tree (`agent-device snapshot -i` on the logged-in simulator after an install-over) — label, trait and value as expected — and the lint build is green with no suppression (D-19)."
    - "Empty: a comment with no vote exposes no vote value (the modifier is applied conditionally, never with an empty string — the `accessibility_empty_string` rule errors on one)."
  artifacts:
    - path: "AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings"
      provides: "accessibility.add_to_favorites, accessibility.voted_up, accessibility.voted_down, accessibility.open_link, accessibility.folder_* keys, six locales each"
      contains: "accessibility.open_link"
    - path: "AppPackage/Sources/DetailFeature/Comments/CommentsView.swift"
      provides: "named link actions on comment cells"
      contains: "accessibilityAction"
  key_links:
    - from: "AppPackage/Sources/DetailFeature/*.swift"
      to: "AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings"
      via: "generated LocalizedStringResource symbols (`.accessibilityAddToFavorites` …) — never a literal"
      pattern: "accessibility"
  prohibitions:
    - statement: "Never add an `accessibilityLabel` to a control that already has visible text (over-labelling breaks Voice Control name matching)."
      status: resolved
      verification: judgment
    - statement: "Never pass a string literal or a `Text(` to an accessibility modifier; catalog keys only (D-30; lint-enforced)."
      status: resolved
      verification: test
    - statement: "Never encode state in a label string (\"Favorited\" / \"Not favorited\"); use traits or values (D-20)."
      status: resolved
      verification: judgment
---

<objective>
The VoiceOver / Voice Control pass over DetailFeature (screens #14–#20, #23): label the genuinely icon-only controls from catalog keys, hide or combine decorative glyphs, express vote state as a value, turn the archive option cell into a `Button`, expose comment links as named actions, and add context-menu actions only if the owner's device observation says they are not already exposed.

Purpose: criteria 7 and 8 for the detail surfaces — every interactive element reachable, correctly announced and actuatable by name.
Output: seven DetailFeature view files edited, new catalog keys in six locales, AX-tree verification recorded.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); DetailFeature accessibility semantics (this plan: labels, hidden glyphs, vote values, archive `Button`, link actions); `OSLogEntryLog.Level.symbol` (plan 16-22); further semantics (plans 16-17 … 16-19) and Reduce Motion gating (plans 16-20, 16-21).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests` (plan 16-23).
- String-catalog keys: DetailFeature `accessibility.add_to_favorites`, `accessibility.voted_up`, `accessibility.voted_down`, `accessibility.open_link`, FolderManager keys (this plan); others per plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voiceover-swiftui.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voice-control.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, stats/subviews, comment cells, Comments view — labels, hidden glyphs, vote values, link actions</name>
  <files>AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift, AppPackage/Sources/DetailFeature/DetailView+CommentCells.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift, AppPackage/Sources/DetailFeature/Comments/CommentsView.swift, AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - `$HOME/.claude/skills/swift-accessibility-skill/SKILL.md` (First-Draft Rules; Principles — every modifier needs a semantic reason; state is a trait) and `references/voiceover-swiftui.md`, `references/voice-control.md`.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Round-2 VoiceOver / Voice Control inventory" — the classified `Image(systemSymbol:)` table and the 10 custom tappables; re-inventory at HEAD because the owner's round-1 commits moved lines.
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions — `CONTEXTMENU=exposed|not-exposed`.
    - `AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` — `favoriteButton` (the `Menu` whose label is a bare `Image(systemSymbol: .heart)`; the overlaid `Label(.favorited, systemSymbol: .heartFill)` is the labelled state), the download button (`.accessibilityLabel(downloadButtonAccessibilityLabel)` already present) and its progress overlay `Image(systemSymbol: centerSymbol)`.
    - `AppPackage/Sources/DetailFeature/DetailView+CommentCells.swift` — the vote thumb `Image` + score + date row; `CommentButton` (already a `Label` with text — no label needed).
    - `AppPackage/Sources/DetailFeature/DetailView+Subviews.swift` — the stats strip and the rating/similar-gallery buttons (already `Label`s with text), the `photoOnRectangleAngled` page glyph beside the page count.
    - `AppPackage/Sources/DetailFeature/Comments/CommentsView.swift` — the per-comment vote glyph, the `.onTapGesture` link runs (`LinkedText`), the comment `.contextMenu`, the compose sheet entry.
    - `AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings` — existing `accessibility.*` keys (plain and with `%#@variable@` substitutions) as the template; `AppPackage/Sources/GalleryListComponents/DownloadBadgeLabel.swift` lines 12–58 — combine-and-label house pattern.
  </read_first>
  <action>
Re-inventory DetailFeature at HEAD (`grep -n "Image(systemSymbol" …`, `grep -n "onTapGesture\|contextMenu" …`) and apply, per site class: (a) favorites `Menu` label → replace the bare heart image with `Label(.accessibilityAddToFavorites, systemSymbol: .heart).labelStyle(.iconOnly)` (new key `accessibility.add_to_favorites`, six locales) so VoiceOver reads it and Voice Control lists it; keep the overlaid favourited `Label` untouched (state by swap, not by label). (b) progress-ring centre glyph → `.accessibilityHidden(true)` (the enclosing button already carries `downloadButtonAccessibilityLabel`). (c) comment vote thumbs (both files) → hide the glyph and put the state on the cell: `.accessibilityValue(.accessibilityVotedUp)` / `.accessibilityVotedDown` applied only when `votedUp` / `votedDown` is true (conditional modifier; never an empty string), keys `accessibility.voted_up` / `accessibility.voted_down`; the score and date stay readable as text. (d) the page-count glyph beside the count → `.accessibilityHidden(true)`, or `.accessibilityElement(children: .combine)` on the HStack if the count is not otherwise read. (e) comment link runs in `CommentsView` → for each link in the comment expose `.accessibilityAction(named: .accessibilityOpenLink, …)`; if a comment has several links, append the link's host to the name via a positional `%@` key `accessibility.open_link_to` (`Open link to %@`) so actions are distinguishable; keep the tap gesture for sighted users. (f) comment `.contextMenu` items → only if `CONTEXTMENU=not-exposed`, mirror each item as `.accessibilityAction(named: <the same LocalizedStringResource the menu button uses>, …)`; if `exposed`, add nothing and record that in the summary. (g) the reader/compose entry points that are `Label`s with text get nothing (no over-labelling). Every new key: `extractionState: manual`, all six locales filled with real translations in the house style (match the existing `accessibility.*` wording per locale), no bare numeric specifiers. Build with the plugin; commit `feat(16-16): VoiceOver semantics for detail and comments`.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3; python3 -c "import json,sys; d=json.load(open('AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings'))['strings']; ks=[k for k in d if k.startswith('accessibility.')]; bad=[k for k in ks if set(d[k].get('localizations',{}))!={'en','de','ja','ko','zh-Hans','zh-Hant'}]; print(len(ks), 'keys;', 'missing locales:', bad)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityAddToFavorites" AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` ≥ 1; `grep -c "accessibilityHidden(true)" AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` ≥ 1.
    - `grep -c "accessibilityVotedUp\|accessibilityVotedDown" AppPackage/Sources/DetailFeature/DetailView+CommentCells.swift` ≥ 1 and the same for `Comments/CommentsView.swift`.
    - `grep -c "accessibilityAction(named: .accessibilityOpenLink" AppPackage/Sources/DetailFeature/Comments/CommentsView.swift` ≥ 1.
    - The catalog check prints `missing locales: []` and the new keys `accessibility.add_to_favorites`, `accessibility.voted_up`, `accessibility.voted_down`, `accessibility.open_link` (and `accessibility.open_link_to` if used) are present.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; `grep -rc "swiftlint:disable" AppPackage/Sources/DetailFeature | grep -v ":0" | wc -l` prints `0`.
    - No `.accessibilityLabel(` was added to a `Button` / `Label` that has visible text (summary lists every modifier added with its semantic reason).
  </acceptance_criteria>
  <done>Detail header, subviews and comments announce correctly and expose links as actions.</done>
</task>

<task type="auto">
  <name>Task 2: Archives, Torrents, FolderManager — native controls and labels</name>
  <files>AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift, AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift, AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift, AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - `AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift` — the coin glyphs (`:133`, `:140` at research HEAD) beside GP/credit numbers; the archive option cell with `.onTapGesture` guarded by `isDisabled` (`:252`).
    - `AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift` — the four symbol+text pairs (`:81–:102`); verify they read as one element each (combine if VoiceOver would read the glyph name).
    - `AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift` — the two icon-only controls (`:81`, `:90`).
    - `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` lines 391–413 — `DownloadListRow` modifier triple, the fallback when a `Button` conversion would change appearance.
  </read_first>
  <action>
Archives: hide the coin glyphs (`.accessibilityHidden(true)`) or combine each glyph+number `HStack` (`.accessibilityElement(children: .combine)`) so the number is read with its unit; convert the archive option cell to a `Button(action:)` with `.buttonStyle(.plain)` and `.disabled(isDisabled)` keeping the same layout, colours and the colour animation (if a `Button` changes the rendered appearance at `.large`, fall back to the `DownloadListRow` triple: `.contentShape(.rect)` + `.accessibilityAddTraits(.isButton)` + `.accessibilityLabel(<the option's visible title resource>)`; say which in the summary). Torrents: wrap each symbol+text pair as a combined element if the snapshot shows the symbol name being read; otherwise leave (record the check). FolderManager: give the two icon-only controls `Label(.key, systemSymbol:).labelStyle(.iconOnly)` with new keys (`accessibility.folder_create` / `accessibility.folder_rename` or whatever the controls do — name the keys after the action, six locales). Build; install-over on `IPHONE_UDID` (`xcodebuild build … -destination 'platform=iOS Simulator,id=<IPHONE_UDID>'` then `xcrun simctl install <IPHONE_UDID> <app path>`; never uninstall/erase) and run `agent-device open <BUNDLE_ID> --platform ios --udid <IPHONE_UDID> --foreground` + `agent-device snapshot -i` on Detail, Comments, Archives, Torrents and FolderManager to confirm each edited control shows the expected label / trait / value in the AX tree; record the snapshot lines in the summary. Commit `feat(16-16): VoiceOver semantics for archives and folders`.
  </action>
  <verify>
    <automated>grep -c "onTapGesture" AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift; grep -c "Label(.accessibility" AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onTapGesture" AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift` prints `0` (converted to `Button`) or the fallback triple is present with the reason recorded.
    - `grep -c "Label(.accessibility" AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift` ≥ 2 (or the equivalent `.accessibilityLabel(.accessibilityFolder…)` on each control).
    - `grep -c "accessibilityHidden(true)\|children: .combine" AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift` ≥ 1.
    - The summary contains `agent-device snapshot -i` excerpts showing the favorites menu label, a voted comment's value, the archive option as a button, and the FolderManager labels.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; the DetailFeature catalog check (Task 1's python one-liner) prints `missing locales: []`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>Every DetailFeature interactive element is a labelled native control in the live accessibility tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View code → assistive tech | Labels and values come only from catalog keys; no literal can drift from visible text. |
| Simulator install → owner's data container | Install-over only; snapshots are text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-16 | Tampering (accidental) | Voice Control name ≠ visible text | medium | mitigate | No labels on text-bearing controls; `accessibility_hardcoded_string` + `accessibility_text_argument` rules; catalog keys with six locales. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | Install-over by UDID; no uninstall/erase; AX snapshots only. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps per task; catalog six-locale check; lint build green; no suppression.
2. AX-tree snapshot excerpts for each edited control; two commits.
</verification>

<success_criteria>
- DetailFeature surfaces meet criteria 7 and 8: icon-only controls labelled, decorative glyphs hidden, state as traits/values, custom tappables native, links and (if needed) context-menu items as actions, all strings from the catalog (D-20, D-30).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-16-SUMMARY.md` when done: every modifier added with its reason, new keys, the CONTEXTMENU outcome, snapshot excerpts, commit hashes.
</output>
