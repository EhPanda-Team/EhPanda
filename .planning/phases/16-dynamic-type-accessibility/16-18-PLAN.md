---
phase: 16-dynamic-type-accessibility
plan: 18
type: execute
wave: 16
depends_on: ["16-17"]
files_modified:
  - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift
  - AppPackage/Sources/ReadingFeature/Resources/Localizable.xcstrings
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "The reader's control panel is fully announced and actuatable: the dual-page and auto-play `Menu` labels (bare `Image`s today) become `Label(.key, systemSymbol:).labelStyle(.iconOnly)` so they have VoiceOver names and Voice Control names; the checkmark-style menu items become a `Picker` (auto-play policy, single selection) and `Toggle`s (dual page / except cover) inside the menus so selection is announced as a trait instead of a hand-drawn checkmark — at `.large` appearance parity (same checkmark rendering) verified by screenshot (D-20, D-30)."
    - "The page slider carries a label and a value from catalog keys: `accessibility.page_slider` and `accessibility.page_of` with named `%#@current@` / `%#@total@` substitutions (labeled numeric arguments per CLAUDE.md), so VoiceOver reads 'Page, 12 of 120' and adjusts by swipe (D-20)."
    - "Page turns, pinch zoom and the hidden-panel gestures have assistive alternatives: `.accessibilityAction(named: .accessibilityNextPage)` / `.accessibilityPreviousPage` on the reader element and `.accessibilityZoomAction` for magnification; native paging `ScrollView` scroll actions are verified rather than duplicated (Apple's VoiceOver criterion 5 and Voice Control criterion 2) (D-20, D-31)."
    - "Showing the control panel moves VoiceOver focus into it (`@AccessibilityFocusState` on the close button when `showsPanel` becomes true) so a custom overlay behaves like a modal surface; dismissal returns focus to the page (D-20)."
    - "If the panel hides on a timer, the auto-hide is suspended while VoiceOver or Voice Control is running (a narrow coordination exception the skill allows: extending transient timeouts while assistive tech is active) — or, if there is no timer-driven hide, that is recorded; nothing else keys off `isVoiceOverRunning` (D-20)."
    - "Actions and labels render nothing; the only visible risk is the menu-item conversion, which must be pixel-parity at `.large` — verified by before/after screenshots in chat — so no D-25 row is added unless parity broke and was kept deliberately (D-24, D-33)."
    - "The reader's `.accessibilityElement(children: .contain)` (kept for the `reading_view` identifier) still surfaces the children and the new actions (RESEARCH Pitfall 13), verified in the AX snapshot."
    - "No hardcoded accessibility string; no over-labelling of `Label`-backed buttons (close, Live Text, retry) (D-30)."
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/Resources/Localizable.xcstrings"
      provides: "accessibility.next_page, accessibility.previous_page, accessibility.page_slider, accessibility.page_of (named substitutions), six locales"
      contains: "accessibility.page_of"
    - path: "AppPackage/Sources/ReadingFeature/ReadingView.swift"
      provides: "named page-turn actions, zoom action, panel focus"
      contains: "accessibilityAction"
  key_links:
    - from: "AppPackage/Sources/ReadingFeature/ReadingView.swift"
      to: "AppPackage/Sources/ReadingFeature/Support/GestureHandler.swift / PageHandler.swift"
      via: "the named actions call the same page-turn / zoom entry points the gestures call — no second code path"
      pattern: "accessibilityAction"
  prohibitions:
    - statement: "Never change core reader behaviour or layout based on `UIAccessibility.isVoiceOverRunning`; the only allowed coordination is suspending a transient auto-hide timer while assistive tech is active."
      status: resolved
      verification: judgment
    - statement: "Never pass a string literal or `Text(` to an accessibility label / value / hint / input-label modifier (D-30; lint-enforced)."
      status: resolved
      verification: test
    - statement: "Never gate the page indicator's `.contentTransition(.numericText())` or the title crossfade on Reduce Motion in this or any plan (D-29)."
      status: resolved
      verification: judgment
---

<objective>
The VoiceOver / Voice Control pass over the reader (screens #24–#26): name the icon-only menus, turn hand-drawn checkmark items into `Picker` / `Toggle`s, label and value the page slider with labeled numeric substitutions, add named page-turn and zoom actions for the gesture-only interactions, and move focus into the control panel when it appears.

Purpose: reading is the app's core task; the Nutrition Label requires it to work end-to-end under VoiceOver and Voice Control, including gesture alternatives.
Output: `ControlPanel.swift`, `ReadingView.swift`, `ReadingView+Gestures.swift` edited; four catalog keys in six locales; parity and AX-tree evidence recorded.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); Detail / Setting semantics (plans 16-16, 16-17); reader semantics (this plan: menu labels, `Picker` / `Toggle` menu items, slider label + value, named page actions, zoom action, panel focus); `OSLogEntryLog.Level.symbol` in `SettingFeature` (plan 16-22); D-28 colour fixes + rating-star decision (plan 16-23); shared-component semantics (plan 16-19); Reduce Motion gating (plans 16-20, 16-21).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests` (plan 16-24).
- String-catalog keys: ReadingFeature `accessibility.next_page`, `accessibility.previous_page`, `accessibility.page_slider`, `accessibility.page_of` (this plan); others per plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voiceover-swiftui.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voice-control.md
@$HOME/.claude/skills/swift-accessibility-skill/references/semantic-structure.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Control panel — menu labels, `Picker` / `Toggle` items, slider label + value, panel focus</name>
  <files>AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift, AppPackage/Sources/ReadingFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - `AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift` — lines 160–245 (upper panel: close `Label`, page indicator `Text(title)` with `reading_page_indicator`, Live Text `Label`, the dual-page `Menu` with `Image(.rectangleSplit2x1)` label and two checkmark `Button`s, the auto-play `Menu` with `Image(.timer)` label and `ForEach(AutoPlayPolicy.allCases)` checkmark buttons, `ToolbarFeaturesMenu`), lines 255–340 (lower panel: `Slider(value: $sliderValue, in: range, onEditingChanged:)`, reversed rotation, preview), lines 85–105 (the panel offsets / opacity / `.disabled(!showsPanel)`).
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Don't Hand-Roll" (menu selection → `Picker` inside `Menu`; verify parity), § "Reader (screen #24) specifics", § "Focus and announcements".
    - `AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings` key `accessibility.downloading` — the named-substitution template (`%#@completed@ of %#@total@`, per-locale `substitutions` with `argNum`, `formatSpecifier: lld`, `other`-only plural for ja/ko/zh; `en`/`de` category sets equal).
    - `$HOME/.claude/skills/swift-accessibility-skill/references/semantic-structure.md` — focus management for custom overlays.
  </read_first>
  <action>
Menus: replace the dual-page `Menu` label with `Label(.dualPageMode, systemSymbol: .rectangleSplit2x1).labelStyle(.iconOnly)` keeping the `.symbolVariant(enableDualPageMode ? .fill : .none)` and font, and the auto-play `Menu` label with `Label(.autoPlay, systemSymbol: .timer).labelStyle(.iconOnly)` (both resources already exist in the catalog as the menus' visible headings). Inside the dual-page menu replace the two checkmark `Button`s with `Toggle(.dualPageMode, isOn: $setting.enableDualPageMode)` and `Toggle(.exceptTheCover, isOn: $setting.exceptCover).disabled(!setting.enableDualPageMode)`; inside the auto-play menu replace the `ForEach` of checkmark buttons with `Picker(selection: $autoPlayPolicy) { ForEach(AutoPlayPolicy.allCases) { Text(policy.value).tag(policy) } } label: { Text(.autoPlay) }` with `.pickerStyle(.inline)` (keep the `.secondary` heading `Text(.autoPlay)` only if the inline picker's own label does not render it — check both, keep parity). Take `--scale 1` before/after screenshots of both open menus at `content_size large` on `IPHONE_UDID` (install-over first, per the `16-SWEEP.md § Protocol › Install-over` rule (build by UDID into `$EVIDENCE_ROOT/DerivedData`, `plutil -extract CFBundleIdentifier raw` on the built `Info.plist` must equal `BUNDLE_ID` before `xcrun simctl install <UDID>`; never uninstall / erase / clear-app-state)) and confirm the checkmark rendering is identical; if SwiftUI renders the `Picker`/`Toggle` differently, revert that menu to buttons and instead add `.accessibilityAddTraits(.isSelected)` to the selected button and `.accessibilityHidden(true)` to its checkmark image — record which path per menu. Slider: `.accessibilityLabel(.accessibilityPageSlider)` and `.accessibilityValue(.accessibilityPageOf(current: Int(sliderValue), total: pageCount))` — new keys `accessibility.page_slider` (plain) and `accessibility.page_of` with named `%#@current@` / `%#@total@` substitutions in all six locales (copy the `accessibility.downloading` substitution structure; `en`/`de` plural sets equal; `ja`/`ko`/`zh-*` `other`-only); the generated call is `.accessibilityPageOf(current:total:)`. Also give the page indicator `Text(title)` no extra label (it is visible text) but make sure it is not read twice with the slider. Focus: add `@AccessibilityFocusState private var isCloseButtonFocused: Bool`, attach `.accessibilityFocused($isCloseButtonFocused)` to the close button, and set it true when `showsPanel` flips to true (via `.onChange(of: showsPanel)` — the `lifecycle_modifiers` rule bans `onAppear`/`task`, not `onChange`; wrap the single-line closure per `single_line_trailing_closure`). Build; commit `feat(16-18): reader control panel semantics`.
  </action>
  <verify>
    <automated>f=AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift; grep -c "accessibilityPageSlider\|accessibilityPageOf" "$f"; grep -c "accessibilityFocused" "$f"; grep -c "Label(.dualPageMode, systemSymbol\|Label(.autoPlay, systemSymbol" "$f"; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Label(.dualPageMode, systemSymbol\|Label(.autoPlay, systemSymbol" …/ControlPanel.swift` prints `2`; `grep -c "accessibilityPageSlider" …/ControlPanel.swift` ≥ 1; `grep -c "accessibilityPageOf(current:" …/ControlPanel.swift` ≥ 1; `grep -c "accessibilityFocused" …/ControlPanel.swift` ≥ 1.
    - `accessibility.page_of` in the ReadingFeature catalog has `substitutions` `current` and `total` in every locale, `en` and `de` with equal plural category sets, `ja`/`ko`/`zh-Hans`/`zh-Hant` `other`-only; no bare `%lld` in any outer value (python check: every locale's `stringUnit.value` contains `%#@current@` and `%#@total@`).
    - Menu-item path recorded per menu (Picker/Toggle with parity screenshots, or selected-trait fallback with the reason); before/after screenshot paths in the summary.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable`.
  </acceptance_criteria>
  <done>The panel's menus and slider announce names, values and selection; focus lands in the panel when it opens.</done>
</task>

<task type="auto">
  <name>Task 2: Reader gestures — named page actions, zoom action, auto-hide coordination, verification</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift, AppPackage/Sources/ReadingFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - `AppPackage/Sources/ReadingFeature/ReadingView.swift` lines 75–120 (the `.accessibilityElement(children: .contain)` + `reading_view` identifier, the gesture attachments, the paging `ScrollView` / `scrollPosition`), lines 160–180 (`ControlPanel` construction), lines 270–300 (slider preview / `showsSliderPreview`), and the page-jump site (`withAnimation { scrollPositionID = … }` ~351).
    - `AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift` — `tapGesture` (single tap zones → `gestureHandler.onSingleTapGestureEnded` with an RTL-corrected offset), `magnificationGesture`, `dragGesture`, `controlPanelDismissGesture`.
    - `AppPackage/Sources/ReadingFeature/Support/GestureHandler.swift` and `PageHandler.swift` — the page-turn and zoom entry points the gestures call (the named actions must call the same ones).
    - `AppPackage/Sources/ReadingFeature/Support/AutoPlayHandler.swift` — whether anything hides the panel on a timer.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Reader (screen #24) specifics" and § Pitfall 13.
  </read_first>
  <action>
On the reader container (inside the `.contain` element so the actions surface on the page element), add `.accessibilityAction(named: .accessibilityNextPage) { <the same call the trailing tap zone makes> }` and `.accessibilityAction(named: .accessibilityPreviousPage) { <the leading-zone call> }` — new keys `accessibility.next_page` / `accessibility.previous_page` (six locales); respect the reading direction exactly as `onSingleTapGestureEnded` does (reuse its RTL-corrected path, do not re-derive direction). Add `.accessibilityZoomAction { action in … }` mapping `.zoomIn` / `.zoomOut` to the magnify handler's enter/exit-zoom entry points (or `gestureHandler.onDoubleTapGestureEnded` at the action point if double-tap is the zoom toggle). Verify with `agent-device snapshot -i --actions` on `IPHONE_UDID` (install-over per the same § Protocol rule) that the reader element lists the two named actions and that the paging `ScrollView` already exposes scroll actions (if it does not, add `.accessibilityScrollAction` delegating to the same page-turn calls). Auto-hide: if `AutoPlayHandler` or the panel hides on a timer, suspend that timer while `UIAccessibility.isVoiceOverRunning || UIAccessibility.isVoiceControlRunning`-equivalent is true — prefer SwiftUI's `@Environment(\.accessibilityVoiceOverEnabled)` read — with a doc comment that this is the narrow "extend transient timeouts" exception and changes no other behaviour; if nothing auto-hides, record `no timer-driven hide` in the summary. Confirm `.contentTransition(.numericText())` on the page indicator and the title crossfade are untouched. Build; run `ReadingFeatureTests` on the spare simulator; commit `feat(16-18): reader gesture alternatives for assistive tech`.
  </action>
  <verify>
    <automated>grep -c "accessibilityNextPage\|accessibilityPreviousPage" AppPackage/Sources/ReadingFeature/ReadingView.swift AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift; grep -c "accessibilityZoomAction" AppPackage/Sources/ReadingFeature/ReadingView.swift AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift; xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54' -only-testing:ReadingFeatureTests 2>&1 | grep -E "passed|failed|error:" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityAction(named: .accessibilityNextPage\|accessibilityAction(named: .accessibilityPreviousPage"` over the two reader files prints `2` in total; `grep -c "accessibilityZoomAction"` ≥ 1.
    - The summary has an `agent-device snapshot -i --actions` excerpt listing the two named actions on the reader element, and states whether scroll actions were native or added.
    - `grep -c "numericText" AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift` is unchanged from before the plan (recorded) — the numeric transition stays.
    - `ReadingFeatureTests` passes on the spare simulator; `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; the ReadingFeature catalog passes the six-locale check; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>Reading works end-to-end without gestures: page turns, zoom and the panel are reachable by VoiceOver and Voice Control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assistive actions → page/zoom handlers | Named actions call the existing handlers; no second code path that could desynchronise the page index. |
| Simulator install → owner's data container | § Protocol install-over rule only (`plutil` bundle-id check before `simctl install`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-17 | Tampering (accidental) | page-index drift via a parallel action path | medium | mitigate | Actions delegate to `GestureHandler` / `PageHandler` entry points; `ReadingFeatureTests` green. |
| T-16-18 | Tampering (accidental) | menu-item conversion changes `.large` appearance | medium | mitigate | Before/after screenshots at `large`; fallback to selected-trait path on any difference. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | § Protocol install-over rule by UDID (bundle id checked); no uninstall/erase. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps; catalog named-substitution check; parity screenshots for the menus; lint build green.
2. AX snapshot with `--actions` showing the named actions; `ReadingFeatureTests` green on the spare simulator; two commits.
</verification>

<success_criteria>
- The reader meets criteria 7 and 8: named menus, selection as traits, slider with labeled numeric value, gesture alternatives, panel focus, no hardcoded strings (D-20, D-30).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-18-SUMMARY.md` when done: per-site changes with reasons, the menu path chosen with screenshot paths, the auto-hide finding, snapshot excerpts, commit hashes.
</output>
