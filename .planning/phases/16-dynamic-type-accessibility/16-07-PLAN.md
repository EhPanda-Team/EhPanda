---
phase: 16-dynamic-type-accessibility
plan: 07
type: execute
wave: 6
depends_on: ["16-06"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every in-scope Group A surface (#1–#13) has six iPad rows (portrait + landscape × XXL / AX3 / AX5) that are no longer `pending`; the iPad is walked as its own device, never derived from the iPhone rows — `isRegularWidthPad` routes detail and setting surfaces to different layouts (Phase 5), so its AX5 failure modes are different (D-10)."
    - "The iPad sweep runs on `IPAD_UDID` from `16-SWEEP.md § Infrastructure`; login-gated surfaces (#5 Watched, #8 Favorites, #13 FolderManager) are walked through the owner's iPad session, or recorded `blocked: no iPad session` if the owner declared `IPAD_LOGIN=none` — never by any credential seam (D-09)."
    - "Verdicts follow the owner's rule and the strict truncation reading; a D-04 checklist row already `fine` from the iPhone may be escalated to `finding:#N` by an iPad cell but never downgraded (D-03, D-04)."
    - "The two D-13 rows that render in Group A (hero-carousel title, Favorites trailing glyph) get an iPad `Observed` note alongside the iPhone one (D-13)."
    - "Findings are recorded and the sweep moves on; screenshots stay in the scratchpad; before/after paths go to the owner in chat (D-02, D-32, D-33)."
    - "Three sizes only, AX5 the maximum, large end only (D-05, D-06, D-07)."
    - "Idempotency: the iPad baseline `content_size` / orientation read in plan 16-03 is restored and read back at the end of the session; concurrency: one simulator at a time — the iPhone simulator is not driven during this plan."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Group A × iPad matrix rows filled; Findings entries; D-04 and D-13 rows updated"
      contains: "iPad"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Matrix (iPad rows)"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Findings"
      via: "every iPad `finding:#N` references a numbered entry; a finding shared with the iPhone reuses the number and adds the iPad cells"
      pattern: "finding:#"
  prohibitions:
    - statement: "No screenshot or any image file is ever added to the repository (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow code and no reflow work order during the sweep (D-01)."
      status: resolved
      verification: judgment
    - statement: "Never address the simulator as `booted`; never erase / uninstall / clear-app-state the iPad sweep simulator (D-09)."
      status: resolved
      verification: judgment
---

<objective>
Walk Group A (#1–#13) on the iPad Pro 11 simulator: six cells each, scroll-to-bottom, judged against D-03 / D-04 with the iPad's own layouts (regular width in both orientations; Detail presented modally from tab roots; Setting modal) — not a re-run of the iPhone.

Purpose: App Store Connect answers the Larger Text label per device; the iPad half of D-10 is first-class evidence.
Output: 78 iPad rows non-`pending`, findings numbered (new or extended to iPad cells), D-13 rows #2/#8 observed on the iPad.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (rows filled here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iPad × Group A surfaces #1–#7</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Infrastructure (`IPAD_UDID`, `IPAD_LOGIN`, iPad baselines), § Protocol, § Inventory rows #1–#7, the iPad § Matrix rows, the iPhone verdicts for the same rows (to reuse finding numbers), § D-13 row "hero-carousel title truncation".
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 1–7; § "Round-1 verification mechanics" (orientation read-back; iPad landscape flips `isRegularWidthPad` layouts).
    - `AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift` lines 26–40 — on the pad idiom the Setting tab presents modally (`deviceClient.deviceType() == .pad` → `presentSetting`) and gallery detail presents modally from tab roots (`presentGalleryDetail`, Phase 5); the iPad Pro 11 is regular width in both orientations. Read to know which presentation to expect; do not edit.
  </read_first>
  <action>
Start a session on the iPad per § Protocol: read and note `xcrun simctl ui <IPAD_UDID> content_size` / orientation (must equal the recorded iPad baseline), `agent-device open <BUNDLE_ID> --platform ios --udid <IPAD_UDID> --foreground`. Walk #1–#7 exactly as plan 16-04 Task 1 did on the iPhone, with the iPad specifics: the Home hero carousel and sections use the wider layout — judge the carousel title at AX5 in both orientations and add an iPad line to the D-13 row's `Observed` column; Frontpage / Popular / Watched / History / Toplists lists render more columns or wider cells — judge the cell text (uploader, stats, category token) at AX5; Watched (#5) per the iPad session (`blocked: no iPad session` on all six rows if `IPAD_LOGIN=none`). Sizes via `xcrun simctl ui <IPAD_UDID> content_size extra-extra-extra-large` / `accessibility-extra-large` / `accessibility-extra-extra-extra-large`, orientations via `agent-device orientation portrait` / `landscape-left`; screenshots at `--scale 0.5` under `$SCRATCHPAD/sweep/ipad-<orientation>-<size>-<screen#>-{top,bottom}.png`; scroll to the bottom; judge; record; move on. When an iPad cell shows the same defect an iPhone finding already describes, reuse the finding number and append the iPad cells to its `Cells affected`; a defect unique to the iPad gets a new number. Escalate D-04 rows if needed (never downgrade). Commit with `docs(16): sweep iPad group A rows 1-7` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 1 2 3 4 5 6 7; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPad" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 1–7, the six iPad matrix rows are non-`pending` (`blocked: no iPad session` allowed only on #5 and only when § Infrastructure records `IPAD_LOGIN=none`).
    - The D-13 row "hero-carousel title truncation" has an iPad `Observed` line.
    - Every iPad `finding:#N` references an existing or new § Findings entry whose `Cells affected` includes the iPad cells.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPad group A rows 1-7` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>#1–#7 verified on the iPad at six cells.</done>
</task>

<task type="auto">
  <name>Task 2: iPad × Group A surfaces #8–#13 and restore</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Protocol, § Inventory rows #8–#13, the iPad § Matrix rows, the iPhone verdicts for the same rows, § D-13 row "Favorites trailing-glyph clip", § D-04 rows for the list cells.
    - `AGENTS.md` § "Download folders are user-owned" — never delete a download the sweep did not create.
  </read_first>
  <action>
Continue on the iPad. Favorites (#8) with index/sort menus (iPad session or `blocked: no iPad session`), and add an iPad line to the D-13 row "Favorites trailing-glyph clip" `Observed` column; Search root (#9) and results (#10); Downloads (#11) — if no download exists on the iPad, start one small download (never delete existing ones); Inspector sheet (#12) — on the iPad it is a wider sheet, judge to the bottom; FolderManager (#13) per the session. Same six-cell procedure; reuse finding numbers for shared defects, new numbers for iPad-only defects; escalate D-04 rows as needed. Restore `xcrun simctl ui <IPAD_UDID> content_size <iPad baseline>` and `agent-device orientation portrait`, read back, and commit with `docs(16): sweep iPad group A rows 8-13` after the image check. The summary lists the findings opened or extended in this plan (numbers, one-liners, scratchpad paths — D-33), `blocked` rows with reasons, and the sweep-created iPad download folder name if any.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 8 9 10 11 12 13; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPad" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 8–13, the six iPad matrix rows are non-`pending` (`blocked: no iPad session` allowed only on #8 and #13 when `IPAD_LOGIN=none`).
    - The D-13 row "Favorites trailing-glyph clip" has an iPad `Observed` line (or `blocked` with the reason).
    - `xcrun simctl ui <IPAD_UDID> content_size` prints the recorded iPad baseline after the restore.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPad group A rows 8-13` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>Group A verified on the iPad; simulator restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator screenshots → scratchpad → chat | Gallery content at AX5; scratchpad only. |
| Agent → owner's iPad simulator | Drive only; no erase, reinstall, credential access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | gallery-content screenshots | high | mitigate | Scratchpad-only; written descriptions; image check before each docs commit. |
| T-16-03 | Denial of service (phase infrastructure) | iPad logged-in simulator | medium | mitigate | UDID-addressed; forbidden list; baseline restored and read back. |
| T-16-09 | Tampering | owner's download folders | medium | mitigate | Never delete a download the sweep did not create. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. All 78 iPad Group A rows non-`pending`; findings numbered or extended.
2. D-13 rows #2/#8 carry iPad observations; D-04 rows escalated where needed.
3. iPad simulator restored and read back; no image in the repo; two docs commits.
</verification>

<success_criteria>
- Group A verified on the iPad as a first-class device under D-03 / D-04 at all six cells.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-07-SUMMARY.md` when done: findings opened/extended, D-13 iPad observations, blocked rows, commit hashes.
</output>
