# Phase 16 — deferred items

Out-of-scope discoveries logged by executors (deviation-rule scope boundary). Not fixed by the plan that found them.

## Found during 16-16

- `AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings` is not strict JSON: the last string entry is followed by a trailing comma before `"version"` (`json.load` fails at line 932). Xcode's catalog compiler tolerates it and the build is green, but any script that reads the catalogs (the six-locale check, the D-30 structural guard) must skip or repair this file first. Plan 16-19 lists this catalog among its files and is the natural place to normalise it while adding keys.
- `AppPackage/Sources/DetailFeature/Resources/Localizable.xcstrings`: the existing `vote_up` / `vote_down` keys carry English (`Vote up` / `Vote down`) in `zh-Hant` while every other locale is translated. Pre-existing; not touched by 16-16 (which only added keys). A translation pass over the DetailFeature catalog should fix both.

## Found during 16-17

- `AppPackage/Sources/HomeFeature`: on the Home tab the Frontpage / Toplists thumbnail grid cells are `Button`s with an **empty** accessibility label (`sim-use describe-ui`: eight `Button|''` entries at 87×120 pt beside the labelled hero-carousel and Toplists buttons). Out of this plan's scope (Setting / DateSeek); belongs to the Home VoiceOver plan.
- `Setting › General › App Activity Logs` on the logged-out simulator shows a run of `Error` / `Parser` entries `Text rating failed to parse: AppModels.AppError.parseFailed` (six identical lines at 23:20:54 after the install-over launch). Not an accessibility item and not touched; a parser owner should look at the rating text path.
- `AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift`: with no session the cookie rows show the placeholder `None` and `CookieValue.isInvalid` is `false`, so the glyph (and now the value) read **Valid** for an empty cookie. Pre-existing semantics of `isInvalid`; the value only mirrors what the glyph already claims. Whether an empty cookie should read as "valid" is an owner question.
- `AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift:66`: the tags-extension warning glyph is hidden per the plan, so the "translations are empty" fact it conveys sighted users now reaches assistive technologies only through the footer text (if any). A value on the toggle row would carry it; plan 16-22 (Differentiate Without Color, same site) is the natural place to decide.

## Found during 16-18

- `AppPackage/Sources/ReadingFeature/Support/ReadingToolbar.swift` + `Support/ControlPanel.swift`: with the panel shown the reader exposes **two** buttons named `Close` (the native toolbar's `topBarLeading` item and the lower panel's glass button — live: `AXButton "Close"` at (20,51) and at (173,690)). Both are correct on their own; together, Voice Control "Tap Close" has to disambiguate ("Which Close?"). Pre-existing design of the owner's round-1 toolbar; out of this plan's scope (no plan item covers the pair). An owner call whether the lower button should carry a distinguishing input label or go.
- `AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift`: the slider row's two end labels (`1` and `112`, `Text(…, format: .number)`) are separate `StaticText` stops beside the slider (live entries). Now that the slider itself announces `Page, 1 of 112`, they are redundant stops for VoiceOver; the plan did not ask to hide them and hiding visible text is a semantic call, so they were left. Owner's call (a `Slider(value:in:label:minimumValueLabel:maximumValueLabel:)` would fold them into the control, at a layout cost).

## Found during 16-19

- `AppPackage/Sources/DetailFeature/DetailView+Subviews.swift` `ActionSection`: the user-rating `RatingView` (shown after "Rate") is driven by a `DragGesture` only. With 16-19's single-element `RatingView` it now announces `Rating, 2.5 out of 5` correctly, but the drag has no assistive equivalent (no `accessibilityAdjustableAction`), so VoiceOver / Voice Control users cannot rate. DetailFeature site (16-16 did not cover it because the research classed `RatingView` as read-only); an owner call for a follow-up.

## Found during 16-20

- `AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` download button: in the Reduce-Motion-OFF recording of a download start (`spin-off.mp4`, 10 fps contact sheet) the header alternated **ring → rotated glyph → ring** over about 0.4 s right after the folder was chosen, i.e. `downloadBadge` appears to flip non-nil → nil → non-nil once during the queued/active hand-off, so the "preparing" state showed twice. Cosmetic and pre-existing (the plan only changed which animation the state selects); a downloads owner should look at the badge publication around enqueue.
- Home tab on the logged-out simulator: the first snapshot of the session showed a section rendering `questionmark.circle.fill` / `Unknown Error` / `An unknown error occurred. Please try again later.` / `Retry` below Toplists (most likely the login-gated Watched section surfacing a generic error instead of a sign-in notice). Not an accessibility item and not touched.

## Found during 16-21

- `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` `DownloadInspectorValidationActionLabel`: the private computed property `progressAnimation` (`reduceMotion ? nil : .easeInOut(duration: 0.2)`) is declared but referenced nowhere — the spinner beside "Validate image data" is animated by the body-form `.animation(.default) { $0.visible(isValidating) … }` alone, so the label's `reduceMotion` parameter reached nothing until this plan's transition gate. Pre-existing dead code; a downloads owner should either wire it (replace the `.default` with it) or delete the property and, if it goes, the parameter. Not touched here: the plan gates the transition only and pins the inspector's predicate count.
- `AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` `DownloadInspectorValidationActionLabel`: the spinner's `.transition(…)` (now `reduceMotion ? .opacity : .opacity.combined(with: .scale(scale: 0.85))`) sits on a view whose visibility is `visible(isValidating)`, i.e. an `opacity` toggle — the `ProgressView` is never inserted or removed, so no transition (scale or opacity) ever plays. Observed in both Reduce Motion states on the simulator: the spinner appears in its first 30 fps frame at full size and opacity, stays for three frames (validation of 112 pages takes ≈100 ms) and vanishes in one; the body-form `.animation(.default)` fade is not visible either at that lifetime. Pre-existing; the gate is source-correct but inert. A downloads owner deciding the fate of `progressAnimation` above should decide this with it (an `if isValidating { ProgressView() }` would make the transition real; or drop the transition).

## Found during 16-22

- ~~`AppPackage/Sources/DetailFeature` `LinkColoredText` (comment link runs): the `.underlineStyle(.single)` link carrier attributed to 16-22 by `16-CONTRAST-AUDIT.md § Decisions › D28 = ok` was in no plan.~~ **Resolved by 16-23** (`0d9945e0`, `4d2acc70`): the underline is drawn on the detected runs in `LinkColoredText` and on the parsed `.linkedText` / `.singleLink` runs in `CommentsView`, together with the `comment-link` colour; measured in `16-CONTRAST-AUDIT.md § 16-23 result (contrast)`.

## Found during 16-23

- `AppPackage/Sources/DownloadsFeature/DownloadsView.swift` swipe actions: on iOS 26 the system draws each swipe action's *text* label ("Pages", "Move", "Delete") in its own secondary gray below the tinted disc — rendered `#85858B` on `#F2F2F7`, 3.29:1 in light (dark 6.36, light+IC 5.33, dark+IC 8.48). The app has no lever over that text (the `.tint` colours only the disc); the audit's swipe rows measured the glyph on the disc, which 16-23 fixed. A platform matter to record beside `secondary-meta` in the Nutrition Label recommendation (16-26), not a code item.
- `AppPackage/Sources/DetailFeature/DetailView+CommentCells.swift` metadata row: the comment score (`+471`) on the gray-5 preview card shares the basis the audit measured for the date (`.secondary` on `#E5E5EA`, 3.13:1 in light) but was not a § Findings row; 16-23 promoted the date only, as the `D28 = ok` bullet names. Whether the score should follow is an owner call under `secondary-meta`.
- `App/Assets.xcassets/AccentColor.colorset` carries light and dark entries only; the `CommentLink` colorset added by 16-23 copies the accent's rendered dark (`#96D35F`) and Increase Contrast (`#2A4015` / `#E1FFC6`) values so that the variants that already passed change by nothing, and its doc comment says an accent change must be mirrored there. If the accent is ever re-authored, that colorset is the one place to update.

## Found during 16-24

- `AppPackage/Sources/HomeFeature` Toplists section on the stubbed (offline) Home: the four ranking rows render placeholder galleries whose title is `......` and whose rank reads `1`, so VoiceOver announces "1, ……, ……" for each; the audit reports the placeholder text as a contrast failure under the tab bar (candidate E-2). A Home owner should decide whether unloaded toplist rows carry a loading label or are hidden from accessibility until they load. Not touched: the audit plan fixes only what the audit names as app-owned and fixable without a visible change.
- `AppPackage/Sources/AppComponents/StateViews.swift` (`NotLoginView`, `ErrorView`), `DownloadsView.swift`, `FolderManagerView.swift`: the symbol `ContentUnavailableView` draws stays exposed under its raw SF Symbol name (`person.crop.circle.badge.questionmark.fill`, `rectangle.and.text.magnifyingglass`) whatever the app does to the `Label` — `.accessibilityHidden(true)` on the label's icon in the title-and-symbol form and on the `Image` in the closure form were both verified ineffective against the exposed hierarchy (the same check shows other hides working). Both attempts reverted; the report is the `ContentUnavailableView.symbol` system-owned exclusion. If the placeholders are ever rebuilt without `ContentUnavailableView`, the symbol should be decorative.
- `AppPackage/Sources/SettingFeature/SettingView.swift` `SettingRowStyle`: the audit's "Text clipped" report on the Setting root row "Appearance" does not come from the `.clipShape` on the row background — replacing it with `.background(_:in:)` changed nothing (diag-3). Tried and reverted; the report is in candidate E-8.
- `AppPackage/Sources/DetailFeature/Comments/CommentsView.swift`: opening a comment link scrolls the linked cell with `proxy.scrollTo(id, anchor: .top)`, which lands the cell under the navigation bar on iOS 26 (the audit reports its author, date and body as contrast failures through the bar — candidate E-2, `surface-Detail › Comments`). An anchor below the bar (or `.contentMargins`) is a visible change for the Detail owner to decide.
- `EhPandaUITests` History fixture: under the stub the History screen renders its parse-error state ("Parse Error" / "A parsing error occurred. Please try again later." / "Retry"), so `testHistoryAudit` audits that state, not a history list. A fixture that seeds one history record would make the list the audited surface.
- `AppPackage/Tests/FavoritesFeatureTests/LoginReturnObservationTests.swift` `cookieLoginTriggersReloadWithoutAViewCallback`: failed once in the full package run on the iPhone 17e simulator during 16-24 (`$HOME/Library/Caches/ehpanda-phase16/round2/audit/package-tests-1.log`) — after the "refreshed" cookie import the store received a second `.loginSucceeded` / `.fetchGalleries` pair the test says must not happen. FavoritesFeature is untouched by this plan, and the full suite re-run on the same simulator passed (`package-tests-2.log`, 1,060 tests), so it is a timing flake in the cookie-observation echo guard — the second import lands while `didLogin` has not yet flipped for the first. A Favorites owner should make the guard deterministic (or serialise the test), per the existing `DidLoginKeyTests` note.
- `AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift` (`actionButtons`' inner `ViewThatFits`, the two glass-styled `Menu`s), inside the Detail sheet presented from `AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift`: on iPadOS 27.0 pre-release (24A434) only, presenting Gallery Detail intermittently pins EhPanda's main thread at about 100 % CPU for minutes. Evidence root `$HOME/Library/Caches/ehpanda-phase16/round2/audit/`: `DeepLinkSchemeUITests.testCommentLinkColdLaunchScrollsToComment` and `DeepLinkEntryUITests.testCommentLinkTapPushesLinkedGallery` failed their first attempt at 171.9 s (`ios27/full-ipad27.log`), and `ShareSheetUITests.testShareSheetHandoffLandsOnDetail` failed two of three attempts (`share-fix/share-ipad1.log`, CPU in `share-fix/share-ipad1.monitor`). Not observed on iPadOS 26.5 (`ipad265-recheck/full-ipad265.log`: 41 / 41 first try) or on iPhone. The follow-up investigation (`rootcause-ios27-comments/followup-20260914/reports/INVESTIGATION.md`, `VALIDATION.md`) finds a SwiftUI / AttributeGraph regression in the iPadOS 27.0 pre-release runtime: a static standalone reproduction (`followup-20260914/harness/CompactRepro.swift`) stalls on 27.0 when the sheet content is laid out at 0×0 with a glass-styled `Menu` inside the header's `ViewThatFits`, and settles on 26.5 with the same binary; building with Xcode 27.0 RC does not change it. In the reproduction a `.bordered` Menu avoids it, and so does removing the inner `ViewThatFits` (`followup-20260914/reports/result-index.json`: `zero-noinner` 2 / 20, `ext-two-noinner` 2 / 20 pinned readings). The earlier V1 app experiment (`rootcause-ios27-comments/patches/rc27-v1-flatten-inner-viewthatfits.patch`) is invalid evidence: its build used bundle id `app.ehpanda` while the harness launched `app.ehpanda.personal` (`followup-20260914/reports/prior-v1-bundle-evidence.txt`). Candidate avoidances, neither applied: (a) `followup-20260914/patches/proposed-ipados27-menu-avoidance.patch`, bordered style for the two header menus on iPad with iOS 27 or later — verified by 12 hermetic cold launches without a stall and the three UI tests passing first try on iPadOS 27 and on iPhone iOS 26.5, but it is a visible change behind an OS and device gate, adds `DeviceClient` to DetailFeature, and iPhone on iOS 27 was not tested with the reproduction; (b) replacing the action buttons' inner `ViewThatFits` with an explicit width-based arrangement read from a stable container, keeping glass (precedent: `DetailView+Subviews.swift` chooses the tag cloud's arrangement by an explicit size read), not yet verified in the app. Owner decision (2026-09-15): "iPad 改回 26.5，27 的問題先記進 deferred" (move the iPad back to 26.5; defer the 27 problem) and "但是注意現在的 27 還是 beta" (note that 27 is still beta) — deferred; the 16-24 gate runs on iPadOS 26.5. Because iPadOS 27 is still beta, re-run `CompactRepro.swift` (zero-size and `NORMAL=1`) and the three UI tests on each new iPadOS 27 seed and on the release build before choosing a fix; if it still reproduces, the owner submits the Feedback draft (`followup-20260914/reports/FEEDBACK-DRAFT.md`, `Apple-Feedback-Attachments.zip`; not submitted).

## Found during 16-25

- `AppPackage/Sources/DetailFeature/DetailView.swift` (hermetic fixture Detail on the iOS 26.5 Simulator, `cc05aca6`, finding W-8 in `16-SWEEP.md § Round-2 walkthrough (16-25) › Findings`): once, about 1 s after VoiceOver focus reached the uploader with no key pressed, VoiceOver logged `Screen Changed` and moved focus back to `More` (`$HOME/Library/Caches/ehpanda-phase16/round2/walkthrough/transcripts/f3-detail-walk.txt`, step 002). Two reproduction attempts, one per launch route, each idle for 45 s after three steps, logged no screen change (`w-detail-screenchange-2.txt`, `w-detail-screenchange-3.txt`). With one occurrence and no known trigger there is nothing to fix yet; if it recurs on a device, record what changed on screen at that moment (a late layout, the download state reading in, an image arriving). **Update (2026-09-16, orchestrator ruling R13): reproduced and moved to `fix`.** The listening recordings reproduced it in 2 of 4 Detail launches (`$HOME/Library/Caches/ehpanda-phase16/round2/walkthrough/listen/audio/run1/L-2.vot.log`: focus on the uploader at 20.09 s, `Screen Changed` at 20.86 s, focus `More` at 21.64 s; `run1/L-5.vot.log`: 17.88 s, 18.65 s, 19.43 s), about 0.77 s after focus reaches the uploader. W-8 in `16-SWEEP.md § Round-2 walkthrough (16-25) › Findings` now carries route `fix`; this entry is no longer deferred.
- `AppPackage/Sources/SearchFeature/SearchRootView+Keywords.swift:118` (finding W-24): the recent keyword's icon-only `Delete` glyph (`xmark`, `.imageScale(.small)`, `.foregroundStyle(.secondary)`) rendered pale green on the light Search root and nearly disappears in the software grayscale conversion (`$HOME/Library/Caches/ehpanda-phase16/round2/walkthrough/display/F2/grayscale-software.png`). VoiceOver and Voice Control both name it `Delete`, so it is reachable without seeing it. Any change to its colour or weight is a visible change; an owner call after the visible-change review (`16-CONTRAST-AUDIT.md § Visible-change review (2026-09-15)`), not part of 16-25.
- `AppPackage/Tests/FavoritesFeatureTests/LoginReturnObservationTests.swift` `cookieLoginTriggersReloadWithoutAViewCallback`: recurred during 16-25 Task 6. It failed in a whole-`FeatureTests`-plan run on `GATE_IPHONE` with "the store received 2 unexpected actions" (`.loginSucceeded`, `.fetchGalleries(keyword:sortOrder:)`) — `$HOME/Library/Caches/ehpanda-phase16/round2/walkthrough/audit/fix-vo2-featuretests.log` — and passed when the same target was re-run with `AppToolsTests` alone on the same tree and the same simulator (`$HOME/Library/Caches/ehpanda-phase16/round2/walkthrough/audit/fix-vo2-favorites-only.log`, `TEST SUCCEEDED`). Reading: cross-target pollution of the process-global `@Shared(.didLogin)` ref cache within one invocation, the same echo-guard race the `## Found during 16-24` entry above describes. Not caused by the 16-25 `visible(_:)` change (`fix(16-25)` `26625a78`): that is a SwiftUI `View` extension, and no reducer test evaluates a view body. The `HomeFeatureTests` twin of the same test name passed in the same failing run, which is what a per-invocation race predicts and a source defect would not. No fix attempted in 16-25 (orchestrator ruling D-D, 2026-09-16: record only); the owner call recorded under `## Found during 16-24` stands — make the echo guard deterministic, or inject rather than serialise.
- **Supersession note (2026-09-16):** the earlier cross-target-pollution reading is not proven and is superseded by the direct W-22 gate evidence. The failure recurred in the `AppToolsTests` + `FavoritesFeatureTests` invocation (`$HOME/Library/Caches/ehpanda-phase16/round2/walkthrough/vo22/production-gates/module-app-favorites.log`, `module-app-favorites.xcresult`): 18 cases, 17 passed, 1 failed, 0 Repetition. The log records the same two unexpected actions at `store.finish()`. `CookieClient.importAutomationCookies` first removes auth cookies at `AppPackage/Sources/CookieClient/CookieClient.swift:133–147`, then writes them back through the subsequent `setCookie` calls; `FavoritesReducer.onPresented` subscribes at `AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift:124–138` and sends `loginSucceeded` on a false→true observation at `:128–136`, with `:144–146` forwarding `fetchGalleries`. This supports a true→false→true echo timeline from the helper, but the log does not record `didLogin` values, so that timeline remains code-supported inference rather than runtime-proven state. W22's view-only branch is not instantiated by this reducer TestStore. The failed gate remains failed with no rerun and no test changes; generic build, W22 AFTER work and the closing full-FeatureTests run continue separately. The same failure in AppTools + Favorites defeats isolation-as-guarantee as an explanation.
