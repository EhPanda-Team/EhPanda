---
phase: 16-dynamic-type-accessibility
plan: 17
type: execute
wave: 15
depends_on: ["16-16"]
files_modified:
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift
  - AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift
  - AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift
  - AppPackage/Sources/SettingFeature/SettingView.swift
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift
  - AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings
  - AppPackage/Sources/DateSeekFeature/Resources/Localizable.xcstrings
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "The three unlabeled custom toggles/selectables in SettingFeature become real controls: `ExcludeToggle` (EhSetting excluded-languages grid — the worst VoiceOver blocker found: no label, no trait, no value) gets `.accessibilityRepresentation { Toggle(isOn:) … }` labelled `<language> <category>` from a catalog key; the Laboratory feature cell gets the same toggle representation; the App Icon row becomes a `Button` with `.isSelected` and a hidden checkmark (D-20)."
    - "The Setting root row (`.onTapGesture` + `onLongPressGesture` pressing-state) becomes a `Button(.plain)`-backed row (or carries `.isButton` if the pressed-state background cannot be reproduced by a `Button`), so it appears under Voice Control 'Show names' with its visible title (D-20, D-30)."
    - "Decorative or duplicated glyphs are hidden or given a value: the cookie valid/invalid icon exposes the validity as an `accessibilityValue` from a catalog key (shape already differs), the General warning glyph beside its text is hidden, the activity-log level dot carries `log.level.title` as its label (the visible glyph change for Differentiate Without Color lands in plan 16-22), the Date Seek buttons — already `Button`s with text — are verified, not over-labelled (D-20)."
    - "Opacity-hidden placeholders (`ExcludeToggle … .opacity(0)` in the first row) are also `.accessibilityHidden(true)` so VoiceOver does not land on an invisible control."
    - "All new strings are catalog keys in six locales; the two string arguments of the exclude-toggle label are positional `%@`; no hardcoded accessibility string (D-30)."
    - "State is a trait or a toggle value, never label text; no control with visible text gets an extra label (D-20)."
    - "Labels and representations are not rendered; this plan moves no layout and adds no D-25 screen (D-24)."
    - "Each edited control is verified in the live AX tree via `agent-device snapshot -i` after an install-over on `IPHONE_UDID`; lint build green with no suppression (D-19)."
  artifacts:
    - path: "AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift"
      provides: "ExcludeToggle with accessibilityRepresentation Toggle"
      contains: "accessibilityRepresentation"
    - path: "AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings"
      provides: "accessibility.exclude_language, accessibility.cookie_valid, accessibility.cookie_invalid (six locales)"
      contains: "accessibility.exclude_language"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift"
      to: "AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings"
      via: "`.accessibilityExcludeLanguage(String(localized: title), String(localized: category.value))` positional `%@` key"
      pattern: "accessibilityExcludeLanguage"
  prohibitions:
    - statement: "Never add an `accessibilityLabel` to a control that already has visible text (over-labelling)."
      status: resolved
      verification: judgment
    - statement: "Never pass a string literal or a `Text(` to an accessibility label / value / hint / input-label modifier (D-30; lint-enforced)."
      status: resolved
      verification: test
    - statement: "Never suppress a lint rule; resolve at the root (D-19)."
      status: resolved
      verification: judgment
---

<objective>
The VoiceOver / Voice Control pass over SettingFeature and DateSeek (screens #28–#38, #41): give the three custom toggles/selectables real semantics via `accessibilityRepresentation` / `Button` + `.isSelected`, make the Setting root row a button, hide the invisible placeholder toggles, express cookie validity as a value, label the log-level dot, and verify the rest are already native.

Purpose: criteria 7 and 8 for the settings surfaces; `ExcludeToggle` is the highest-priority VoiceOver blocker in the app.
Output: eight view files edited, new keys in two catalogs, AX-tree verification recorded.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md`, `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); DetailFeature semantics (plan 16-16); SettingFeature + DateSeek semantics (this plan: `ExcludeToggle` / Laboratory cell representations, `AppIconRow` + Setting row buttons, cookie value, log-level label); `OSLogEntryLog.Level.symbol` in `SettingFeature` (plan 16-22); D-28 colour fixes + rating-star decision (plan 16-23); further semantics (plans 16-18, 16-19) and Reduce Motion gating (plans 16-20, 16-21).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests` (plan 16-24).
- String-catalog keys: SettingFeature `accessibility.exclude_language`, `accessibility.cookie_valid`, `accessibility.cookie_invalid` (this plan); others per plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-PATTERNS.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voiceover-swiftui.md
@$HOME/.claude/skills/swift-accessibility-skill/references/voice-control.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom toggles and selectables — `ExcludeToggle`, Laboratory cell, App Icon row, Setting root row</name>
  <files>AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift, AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift, AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift, AppPackage/Sources/SettingFeature/SettingView.swift, AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - `$HOME/.claude/skills/swift-accessibility-skill/SKILL.md` First-Draft Rules (custom toggle → `accessibilityRepresentation`; `onTapGesture` on non-Button → button semantics) and `references/voiceover-swiftui.md` § representation / traits.
    - `AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift` lines 115–190 — `ExcludedLanguagesSection` header row (`EhSetting.ExcludedLanguagesCategory.allCases` column titles), `ExcludeRow(title:bindings:isFirstRow:)` (`ExcludeToggle(isOn: binding).opacity(isFirstRow && offset == 0 ? 0 : 1)`), and `ExcludeToggle` (`Color.clear.overlay { Image(nosign|circle) }.onTapGesture { withAnimation { isOn.toggle() }; haptic }`).
    - `AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift` lines 45–80 — the feature cell (tint vs `.secondary`, `.onTapGesture`, `withAnimation`).
    - `AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift` lines 100–160 — `AppIconRow` (`.contentShape(.rect).onTapGesture { $setting.withLock(…) }`, the checkmark `Image` at the trailing edge, the 60-pt icon).
    - `AppPackage/Sources/SettingFeature/SettingView.swift` lines 90–125 — the row `Label` + `.onTapGesture { tapAction(rowType) }` + `.onLongPressGesture(pressing:)` that drives `isPressing` for the background.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Pattern 3: Custom tappable → real control" (the per-site idiom table) and § Anti-Patterns.
    - `AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings` — existing key style; CLAUDE.md catalog rules (string args positional `%@`).
  </read_first>
  <action>
`ExcludeToggle`: extend its init to take the row's language `title: LocalizedStringResource` and the column `category: EhSetting.ExcludedLanguagesCategory` (pass them from `ExcludeRow`, which gets the categories from `allCases` by `offset`), keep the rendered `Color.clear.overlay { Image … }` and the tap/haptic behaviour, and add `.accessibilityRepresentation { Toggle(isOn: $isOn) { Text(.accessibilityExcludeLanguage(String(localized: title), String(localized: category.value))) } }` — new key `accessibility.exclude_language` with two positional `%@` arguments (e.g. `en` "Exclude %1$@ %2$@" rendering "Exclude Japanese Translated"), six locales; the representation gives label, on/off value, toggle trait and a Voice Control name in one move. In `ExcludeRow`, the first-row placeholder (`opacity(0)`) also gets `.accessibilityHidden(true)` (opacity does not remove it from the AX tree). Laboratory feature cell: same representation (`Toggle(isOn:)` bound to the feature flag, label = the cell's visible title resource), keeping the tinted/gray rendering and `withAnimation` toggle untouched. `AppIconRow`: replace `.onTapGesture` with `Button(action:)` + `.buttonStyle(.plain)` keeping `$setting.withLock(…)`, add `.accessibilityAddTraits(isSelected ? .isSelected : [])`, and `.accessibilityHidden(true)` on the trailing checkmark image (selection is the trait). Setting root row: replace `.onTapGesture` with a `Button(action: { tapAction(rowType) })` + `.buttonStyle(.plain)` and reproduce the pressed background via a `ButtonStyle` that reads `configuration.isPressed` (so the `onLongPressGesture` pressing hack can go), OR — if the pressed-state parity at `.large` cannot be reproduced exactly — keep the gestures and add `.accessibilityAddTraits(.isButton)` (the row's `Label` already supplies the name); record which path in a doc comment and the summary. Build; commit `feat(16-17): native semantics for settings toggles and rows`.
  </action>
  <verify>
    <automated>grep -c "accessibilityRepresentation" AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift; grep -c "isSelected" AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityRepresentation" …/EhSettingView+Sections3.swift` ≥ 1 and `…/LaboratorySettingView.swift` ≥ 1; `grep -c "accessibilityHidden(true)" …/EhSettingView+Sections3.swift` ≥ 1 (the placeholder).
    - `grep -c "onTapGesture" …/AppearanceSettingView.swift` prints `0`; `grep -c "isSelected" …/AppearanceSettingView.swift` ≥ 1; the checkmark image is hidden.
    - `…/SettingView.swift` has either `Button(` for the row with no `onTapGesture`, or `.accessibilityAddTraits(.isButton)` with the recorded reason.
    - `accessibility.exclude_language` exists in the SettingFeature catalog with six locales and two `%@` arguments (`python3` check as in plan 16-16 adapted to this catalog prints `missing locales: []`).
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; no `swiftlint:disable` added.
  </acceptance_criteria>
  <done>The unlabeled custom toggles are real toggles; selection is a trait; rows are buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Account, General, Activity Logs, Date Seek — values, hidden glyphs, verification</name>
  <files>AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift, AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift, AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift, AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings, AppPackage/Sources/DateSeekFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - `AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift` lines 160–185 — the cookie `TextField` + validity `Image(xmarkCircle|checkmarkCircle)`.
    - `AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift` around line 52 — the warning glyph next to text.
    - `AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift` lines 205–230 — `AppActivityLogRow` (`Image(systemSymbol: .circleFill).foregroundStyle(log.level.color)`), and `AppPackage/Sources/AppModels/Support/AppActivityLog.swift` lines 72–95 (`Level.color`, `Level.title: LocalizedStringResource`).
    - `AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift` lines 95–130 — `SeekButton` (already a `Button` with `Text(title)` — nothing to add) and the picker rows.
  </read_first>
  <action>
Account: hide the validity glyph from VoiceOver and put the state on the `TextField` row as a conditional `.accessibilityValue(.accessibilityCookieValid)` / `.accessibilityCookieInvalid` (new keys, six locales) — applied to the `HStack` combined with `.accessibilityElement(children: .combine)` only if the field's own label survives combining (verify in the snapshot; otherwise keep the field separate and hide only the glyph). General: `.accessibilityHidden(true)` on the warning glyph beside its text. Activity Logs: `.accessibilityLabel(log.level.title)` on the level dot (a `LocalizedStringResource` from `AppModels` — no literal), leaving the glyph itself for plan 16-22's DWC change. Date Seek: confirm `SeekButton` reads as `Button, <title>` and the picker rows are native; add nothing unless the snapshot shows an unlabeled element. Build; install-over on `IPHONE_UDID` per the `16-SWEEP.md § Protocol › Install-over` rule (build by UDID into `$EVIDENCE_ROOT/DerivedData`, `plutil -extract CFBundleIdentifier raw` on the built `Info.plist` must equal `BUNDLE_ID` before `xcrun simctl install <UDID>`; never uninstall / erase / clear-app-state) and run `agent-device snapshot -i` on Setting root, Appearance (App Icon picker), Laboratory, EhSetting › excluded languages, Account, General, Activity Logs and the Date Seek picker; record the AX lines showing: a `Toggle`-typed element with the `Exclude <language> <category>` label and an on/off value; the Laboratory toggle; the App Icon row as a selected button; the Setting rows as buttons; the cookie row's value; the log dot's level label. Commit `feat(16-17): accessibility values and hidden glyphs in settings`.
  </action>
  <verify>
    <automated>grep -c "accessibilityCookieValid\|accessibilityCookieInvalid" AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift; grep -c "accessibilityLabel(log.level.title)" AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift; xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityCookieValid\|accessibilityCookieInvalid" …/AccountSettingView.swift` ≥ 1; `grep -c "accessibilityHidden(true)" …/GeneralSettingView.swift` ≥ 1; `grep -c "accessibilityLabel(log.level.title)" …/AppActivityLogsView.swift` prints `1`.
    - Both catalogs pass the six-locale check for every `accessibility.*` key (`missing locales: []`).
    - The summary contains `agent-device snapshot -i` excerpts for the eight screens listed, including a toggle element whose label starts with the localized "Exclude" wording and carries an on/off value.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`.
  </acceptance_criteria>
  <done>Every SettingFeature and DateSeek interactive element is labelled, stateful by trait/value, and verified in the live AX tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View code → assistive tech | Catalog keys only; representation toggles keep visual parity. |
| Simulator install → owner's data container | § Protocol install-over rule only (`plutil` bundle-id check before `simctl install`); AX snapshots are text; the Account screen's cookie values are never recorded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Information disclosure | Account screen during snapshot verification | high | mitigate | Record only the validity value line; never copy cookie text from the snapshot into any artifact. |
| T-16-16 | Tampering (accidental) | Voice Control name ≠ visible text | medium | mitigate | Representations labelled from the same resources as the visible text; lint rules; no labels on text-bearing controls. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in simulator | medium | mitigate | § Protocol install-over rule by UDID (bundle id checked); no uninstall/erase. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. Source greps; six-locale checks; lint build green; no suppression.
2. AX snapshot excerpts for all eight screens; two commits.
</verification>

<success_criteria>
- SettingFeature and DateSeek meet criteria 7 and 8: custom toggles are toggles, selection is a trait, rows are buttons, decorative glyphs hidden, values from the catalog (D-20, D-30).
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-17-SUMMARY.md` when done: per-site change list with reasons, the Setting-row path chosen, new keys, snapshot excerpts (no cookie text), commit hashes.
</output>
