---
phase: 16-dynamic-type-accessibility
plan: 12
type: execute
wave: 11
depends_on: ["16-11"]
files_modified:
  - .swiftlint.yml
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: false
requirements: [A11Y-01]

must_haves:
  truths:
    - "`minimumScaleFactor` is gone from the source tree (5 sites → 0) and the fourth D-16 rule, `no_minimum_scale_factor`, is live at error severity in the root `.swiftlint.yml`, landed only after the count reached 0 so no build was ever broken by it (D-14, D-16)."
    - "The rule is proven with a positive probe (fires on the banned modifier form) and a comment/string negative (silent in comments and string literals) against the 0.65.0 binary, and the plugin build is green (D-16, D-19)."
    - "Round 1 closes with the owner's signature on the completed `16-SWEEP.md`: every matrix row non-pending, every finding closed, every D-13 item dispositioned, both devices, both orientations, XXL / AX3 / AX5, including the authenticated content screens — the D-03 gate carried over from Phase 10 (criterion 5) (D-03, D-05, D-10)."
    - "Round 2 does not start before this signature: every round-2 plan `depends_on` this plan, so the assistive-technology work lands on settled layout (D-23)."
    - "Default-size parity held through round 1: the owner's signature covers the `.large` half of D-15 as verified in plan 16-11 (D-15)."
    - "The signature is recorded as text in `16-SWEEP.md § Owner sign-off` — no image enters the repo (D-32)."
    - "Idempotency: re-running the standalone lint after the rule lands yields the same counts (0 for all five rules)."
  artifacts:
    - path: ".swiftlint.yml"
      provides: "`no_minimum_scale_factor` custom rule"
      contains: "no_minimum_scale_factor"
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "`## Owner sign-off` section with the signature text and date"
      contains: "Owner sign-off"
  key_links:
    - from: ".swiftlint.yml"
      to: "AppPackage/Sources (all modules)"
      via: "the plugin errors on any future shrink-to-fit; the ban is self-enforcing"
      pattern: "no_minimum_scale_factor"
  prohibitions:
    - statement: "Never use `minimumScaleFactor` anywhere in the tree — banned outright, not grandfathered (D-14)."
      status: resolved
      verification: test
    - statement: "Never suppress or disable a lint rule without the owner's explicit permission (D-19)."
      status: resolved
      verification: judgment
    - statement: "No round-2 code is written before the owner signs the round-1 table (D-23)."
      status: resolved
      verification: judgment
---

<objective>
Close round 1: land the last D-16 rule now that the owner's removals have driven `minimumScaleFactor` to zero, prove it, and collect the owner's signature on the completed verdict table — the criterion-5 gate that Phase 10 deferred here. Every round-2 plan depends on this gate (D-23).

Purpose: the lint rule makes D-14 permanent; the signature makes round 1 done.
Output: `no_minimum_scale_factor` live and green; `16-SWEEP.md § Owner sign-off` recorded.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (this plan).
- Planning docs: `16-SWEEP.md` (sign-off recorded here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-25).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.swiftlint.yml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land `no_minimum_scale_factor` at zero, probe it, build green, commit</name>
  <files>.swiftlint.yml</files>
  <read_first>
    - `.swiftlint.yml` — the `custom_rules` block as landed by plan 16-01 (the three D-16 rules between `no_case_check_property` and `no_nslock`); copy their shape.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "SwiftLint rules" — the validated regex `\.minimumScaleFactor\s*\(` and YAML for this rule; § Pitfall 4.
    - `.planning/phases/16-dynamic-type-accessibility/16-01-SUMMARY.md` — the probe-table format and the standalone binary path.
  </read_first>
  <action>
First prove the precondition: `grep -rn "minimumScaleFactor" AppPackage/Sources App ShareExtension | wc -l` must print `0`; if not, stop and report the live sites to the owner (the round is not clear — D-14 target is 0, and landing the rule now would break every build). Then add `no_minimum_scale_factor` to `custom_rules` in alphabetical position (after `no_geometry_reader`, before `no_nslock`): regex `'\.minimumScaleFactor\s*\('`, `excluded_match_kinds` `comment` / `doccomment` (with the spelling-trap comment) / `string`, `severity: error`, message in the house voice naming Phase 16 D-14 and the fix (reflow — wrap, `ViewThatFits`, or stack at accessibility sizes). Probe in a throwaway scratchpad file: positive `.minimumScaleFactor(0.5)` fires; negatives — the same token inside a `//` comment and inside a string literal — stay silent. Run the standalone `swiftlint lint --strict --no-cache --config .swiftlint.yml AppPackage/Sources App ShareExtension EhPandaUITests AppPackage/Tests` (0 violations, no config warnings), then `xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator'` and `xcodebuild build-for-testing -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,id=88B217DA-A166-4BAD-820D-DE13B1C4EB54'` (sequentially; spare simulator only). Commit `.swiftlint.yml` alone: `feat(16-12): ban minimumScaleFactor via lint`. Record the probe table and both build results in the summary.
  </action>
  <verify>
    <automated>grep -rn "minimumScaleFactor" AppPackage/Sources App ShareExtension | wc -l; grep -c "no_minimum_scale_factor:" .swiftlint.yml; SWIFTLINT=$(ls $HOME/Library/Developer/Xcode/DerivedData/EhPanda-*/SourcePackages/artifacts/swiftlintplugins/*/macos/swiftlint | head -1); "$SWIFTLINT" lint --strict --no-cache --config .swiftlint.yml AppPackage/Sources App ShareExtension EhPandaUITests AppPackage/Tests 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "minimumScaleFactor" AppPackage/Sources App ShareExtension | wc -l` prints `0` before and after the rule lands.
    - `grep -c "no_minimum_scale_factor:" .swiftlint.yml` prints `1`, positioned between `no_geometry_reader:` and `no_nslock:` (`grep -n "no_geometry_reader:\|no_minimum_scale_factor:\|no_nslock:" .swiftlint.yml` prints ascending line numbers in that order).
    - Standalone strict lint: `Done linting! Found 0 violations`; probe table: positive fired, comment and string negatives silent.
    - `xcodebuild build … -scheme EhPanda` → `** BUILD SUCCEEDED **`; `build-for-testing … FeatureTests` → `** TEST BUILD SUCCEEDED **`.
    - `git show --stat HEAD` lists only `.swiftlint.yml`; message `feat(16-12): ban minimumScaleFactor via lint`.
  </acceptance_criteria>
  <done>All four D-16 rules are live at error severity; the tree is at 0 for each.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner-signed UAT of the completed round-1 verdict table (criterion 5)</name>
  <action>Present the completed `16-SWEEP.md` to the owner for signature; this is the D-03 device gate Phase 10 deferred (10-UAT.md test 7, `skipped`). Round 2 (plans 16-13 onward) is blocked on this signature (D-23).</action>
  <what-built>Round 1 complete: the 12-cell matrix walked on both simulators (iPhone Air + iPad Pro 11 × portrait + landscape × XXL / AX3 / AX5) over the re-derived inventory including the authenticated content screens; every finding fixed-and-re-verified or owner-accepted; the five D-13 items dispositioned; `.large` parity checked per fix; `minimumScaleFactor` at 0 with all four lint rules live.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md`. Check § Matrix has no `pending` / `re-verify` row, § Findings has no `open` entry, § D-13 shows five dispositions, § Round-1 report › Round-1 closure has the counts.
    2. Spot-check on your logged-in simulator any screen you care about: `xcrun simctl ui <UDID> content_size accessibility-extra-extra-extra-large`, open the screen, scroll to the bottom, confirm nothing provides less information than at `large`; then `xcrun simctl ui <UDID> content_size large` to restore.
    3. Confirm the default-size appearance of the four former shrink sites (Detail header category label, comment cells, Comments header, list cell stats) matches what you expect at `large`.
    4. If anything is not right, describe it — the agent reopens the finding and returns to the re-verification loop; nothing is accepted on your behalf.
  </how-to-verify>
  <resume-signal>Type `approved` (optionally followed by a signature line to record verbatim, e.g. `approved — signed <name>, <date>`) to close round 1, or describe the issue to reopen.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the signature and mark round 1 closed</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Round-1 report › Round-1 closure.
    - `.planning/phases/10-ui-polish/10-UAT.md` test 7 — the deferred D-03 gate this signature closes (read only; do not edit that file).
  </read_first>
  <action>
Append `## Owner sign-off` to `16-SWEEP.md` with: the owner's resume text verbatim, the date, the HEAD commit hash the signature covers, the statement that this closes Phase 10's 10-UAT.md test 7 (D-03 device gate) and ROADMAP Phase 16 criterion 5, and the lint state (`no_minimum_scale_factor` live, count 0). Commit with `docs(16): record round-1 owner sign-off` after the image check. The summary states that round 2 may begin.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; grep -c "^## Owner sign-off" "$f"; git log -1 --format=%s; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Owner sign-off" 16-SWEEP.md` prints `1`, and the section contains the word `approved`, a date, and a 7–40 hex commit hash.
    - `git log -1 --format=%s` equals `docs(16): record round-1 owner sign-off`.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>Round 1 is signed and closed; round 2 is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → build | The last rule must land only at count 0 or every module's build breaks. |
| Owner → artifact | The signature is text in the table; no image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Denial of service | every module's build | medium | mitigate | Precondition grep = 0 before the rule lands; strict standalone lint + two builds before commit. |
| T-16-04 | Tampering (accidental) | rule silently inert | medium | mitigate | Positive/negative probe; stderr checked for config warnings. |
| T-16-06 | Repudiation | round-1 closure without owner signature | medium | mitigate | Blocking human-verify checkpoint; signature recorded verbatim with the covered commit hash. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. `minimumScaleFactor` count 0; rule landed, probed, strict lint 0, both builds green; one commit touching `.swiftlint.yml`.
2. Owner `approved`; sign-off section committed.
</verification>

<success_criteria>
- Criterion 6 complete (5 → 0 and four error-level rules); criterion 5 complete (owner-signed UAT across every screen including authenticated ones); round 2 unblocked under D-23.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-12-SUMMARY.md` when done: probe table, build results, the owner's signature text, commit hashes.
</output>
