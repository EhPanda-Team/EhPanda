---
phase: 16-dynamic-type-accessibility
plan: 09
type: execute
wave: 8
depends_on: ["16-08"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every in-scope Group C surface (#28–#42) has six iPad rows that are no longer `pending`; on the iPad the Setting tab presents modally and its pushed screens live inside that modal, so the cells are judged on the iPad's own presentation (D-10)."
    - "Scope per D-11 holds on the iPad: native Login form and EhSetting sections in; WKWebView pages, Cloudflare challenge, share sheet and photo picker out (D-11)."
    - "After this plan the whole 12-cell matrix is walked: no row in § Matrix is `pending` on either device; every D-04 checklist row is dispositioned (`fine` / `finding:#N`) or carries a stated reason; every D-13 row has iPhone and iPad `Observed` lines (D-04, D-10, D-13)."
    - "Verdicts follow the owner's rule and the strict truncation reading (D-03, D-04); findings are recorded and the sweep moves on (D-02); screenshots stay under the evidence root (`$HOME/Library/Caches/ehpanda-phase16/`) and before/after paths go to the owner in chat (D-32, D-33)."
    - "Three sizes only, AX5 the maximum, large end only (D-05, D-06, D-07)."
    - "Idempotency: the iPad baseline is restored and read back; both sweep simulators end the round-1 walk at their recorded baselines (D-08, D-09)."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md"
      provides: "Group C × iPad matrix rows filled; the full 12-cell matrix walked; D-04 rows all dispositioned"
      contains: "iPad"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Matrix"
      to: ".planning/phases/16-dynamic-type-accessibility/16-SWEEP.md § Findings"
      via: "no `pending` row remains; every `finding:#N` references a numbered entry"
      pattern: "finding:#"
  prohibitions:
    - statement: "No screenshot or any image file is ever added to the repository (D-32)."
      status: resolved
      verification: judgment
    - statement: "The agent writes no reflow code and no reflow work order during the sweep (D-01)."
      status: resolved
      verification: judgment
    - statement: "The sweep never logs out, clears the cache, deletes the EhSetting profile or leaves a persisted setting changed (D-09 infrastructure discipline)."
      status: resolved
      verification: judgment
---

<objective>
Walk Group C (#28–#42: the modal Setting stack, its pushed screens, the Filters / Quick Search / Date Seek sheets and the error surface + toasts) on the iPad Pro 11 simulator at six cells each, completing the 12-cell matrix for every in-scope surface.

Purpose: close the walk so plan 16-10 can report the complete findings list to the owner (D-02 — "once every page has been scanned").
Output: 90 iPad rows non-`pending`; no `pending` row anywhere in § Matrix; all D-04 rows dispositioned; all D-13 rows observed on both devices.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md` (matrix completed here), `16-CONTRAST-AUDIT.md` (plan 16-13), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper; adaptive `CategoryLabel` / `CategoryCell`; `OSLogEntryLog.Level.symbol`; accessibility semantics and Reduce Motion gating across round-2 view files.
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests`, `AccessibilityAuditUITests`.
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iPad × Group C surfaces #28–#35 (the modal Setting stack, Account, Login native, General, Activity Logs, Appearance, Reading setting, Download setting)</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Infrastructure, § Protocol, § Inventory rows #28–#35, the iPad § Matrix rows, the iPhone verdicts, § D-04 rows mapped to these screens.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § Screen inventory rows 28–35.
    - `AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift` lines 26–40 — the pad idiom presents Setting modally (`presentSetting`); read to know the expected presentation; do not edit.
  </read_first>
  <action>
Start the iPad session per § Protocol on `IPAD_UDID` / `BUNDLE_ID`. Tap the Setting tab — on the iPad it presents the Setting stack modally; judge the modal root (#28) and then each pushed screen inside the modal: Account (#29 — logout confirmation opened and cancelled), Login native form (#30 — no submission, no WebView, no challenge), General (#31 — confirmations opened and cancelled; 50-pt fixed width judged), Activity Logs (#32 + detail sheet), Appearance (#33 + App Icon picker), Reading setting (#34), Download setting (#35). Six cells each (`xcrun simctl ui <IPAD_UDID> content_size …`, `agent-device orientation …`, screenshots at `--scale 0.5` under `$EVIDENCE_ROOT/sweep/ipad-…`, scroll to the bottom, judge, record, move on); the modal's own chrome (dismiss control, title) is part of every cell. Reuse or add finding numbers; escalate D-04 rows where an iPad cell fails. Commit with `docs(16): sweep iPad group C rows 28-35` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; for n in 28 29 30 31 32 33 34 35; do printf "%s " "$n"; grep -E "^\| *$n *\| .*iPad" "$f" | grep -c "| pending"; done; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - For each screen # in 28–35, the six iPad matrix rows are non-`pending`.
    - The summary states no logout, no cache clear, no login submission occurred on the iPad.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPad group C rows 28-35` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>#28–#35 verified on the iPad's modal Setting stack at six cells.</done>
</task>

<task type="auto">
  <name>Task 2: iPad × Group C surfaces #36–#42, matrix completeness check, restore</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md` — § Protocol, § Inventory rows #36–#42, the iPad § Matrix rows, the iPhone verdicts, § D-04 checklist (all rows), § D-13 rows (all five).
    - `EhPandaUITests/Support/UITestConstants.swift` — `malformedURL` shape for raising the error surface.
  </read_first>
  <action>
Continue on the iPad. Laboratory (#36), About (#37), EhSetting native sections (#38 — 200-pt fixed-width controls judged at AX5; delete-profile confirmation opened and cancelled; no web page), Filters sheet (#39 — on the iPad the category grid uses the 100-pt adaptive columns; judge `CategoryCell` names at AX5), Quick Search sheet + editor (#40 — throwaway item only if needed, then removed), Date Seek picker (#41), Error surface + toasts (#42 — raise the error sheet with `ehpanda://attacker.example/g/3103480/0000000000/` via `agent-device open`, raise a toast, judge both). Six cells each per § Protocol; reuse or add finding numbers; escalate D-04 rows where needed. Then the completeness check: `grep -c "| pending" 16-SWEEP.md` over § Matrix must be 0; every D-04 row must be non-`pending` (if one is still `pending` because its surface was `blocked`, set it to `blocked: <reason>`); every D-13 row must have both an iPhone and an iPad `Observed` line (or `blocked` with the reason). Restore `xcrun simctl ui <IPAD_UDID> content_size <iPad baseline>` and `agent-device orientation portrait`, read back, and also read back the iPhone's `content_size` to confirm both sweep simulators rest at their baselines. Commit with `docs(16): sweep iPad group C rows 36-42` after the image check. Summary: findings opened/extended in this plan, the total finding count across the matrix, the list of `blocked` / `n/a` rows with reasons, and an explicit statement that the matrix is fully walked.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md; awk '/^## Matrix/,/^## Findings/' "$f" | grep -c "| pending"; awk '/^## D-04 checklist/,/^## D-25/' "$f" | grep -c "| pending"; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Matrix/,/^## Findings/' 16-SWEEP.md | grep -c "| pending"` prints `0` — the whole 12-cell matrix is walked.
    - `awk '/^## D-04 checklist/,/^## D-25/' 16-SWEEP.md | grep -c "| pending"` prints `0` — every D-04 row is dispositioned or `blocked` with a reason.
    - Each of the five D-13 rows contains both the strings `iPhone` and `iPad` in its `Observed` column (or `blocked` with a reason).
    - `xcrun simctl ui <IPAD_UDID> content_size` and `xcrun simctl ui <IPHONE_UDID> content_size` both print their recorded baselines.
    - `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): sweep iPad group C rows 36-42` exists; `grep -c "/Users/" 16-SWEEP.md` prints `0`.
  </acceptance_criteria>
  <done>The 12-cell matrix is complete on both devices; both simulators are at baseline; the findings list is ready to be reported in full.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator screenshots → evidence root → chat | Settings/Account screenshots; evidence root only; no account values in the table. |
| Agent → persisted settings / session | Destructive confirmations cancelled; throwaway items removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | screenshots incl. Account screen | high | mitigate | Evidence-root only; image check before each docs commit; no cookie/account values recorded. |
| T-16-03 | Denial of service (phase infrastructure) | logged-in sessions on both simulators | medium | mitigate | Logout / delete-profile cancelled; no login submission; both baselines restored and read back. |
| T-16-11 | Tampering | owner's persisted settings | low | mitigate | Throwaway Quick Search item removed; nothing else left changed. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. No `pending` row in § Matrix; no `pending` row in § D-04 checklist; every D-13 row observed on both devices.
2. Both sweep simulators at baseline (read back); no image in the repo; two docs commits.
</verification>

<success_criteria>
- The full D-10 matrix (iPhone + iPad × portrait + landscape × XXL / AX3 / AX5) is walked under D-03 / D-04 for every in-scope surface, ready for the complete findings report.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-09-SUMMARY.md` when done: findings opened/extended, total finding count, blocked / n/a rows, matrix-complete confirmation, commit hashes.
</output>
