---
phase: 16-dynamic-type-accessibility
plan: 13
type: execute
wave: 12
depends_on: ["16-12"]
files_modified:
  - .planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md
autonomous: false
requirements: [A11Y-02]

must_haves:
  truths:
    - "Sufficient Contrast and Differentiate Without Color are audit-first: this plan measures what exists and records the numbers before any build task for those two axes runs; no colour or glyph is changed here (D-20)."
    - "The 84 category variants are re-measured from the colorset JSON (three component encodings normalised): white-text failures, best-of-black/white ratios (expected 84/84 ≥ 4.5:1, worst 4.62:1 ExHentai Game CG light, 47 flips, crossover L = 0.1791), so D-26's numbers are reproduced before the adaptive text lands (D-26)."
    - "The 40 Increase Contrast variants are compared with their standard siblings on best-of contrast (expected 19/40 lower) and a concrete re-authored value table is proposed for the owner; the decision is the owner's because D-26 froze all 84 variants byte-identical while D-27 calls for re-authoring — the conflict is surfaced with the numbers, never resolved silently (D-26, D-27, D-22)."
    - "Non-category colours are measured on their merits against 4.5:1 text / 3:1 non-text in light, dark and Increase Contrast — rating stars, offline notice, toast text on glass, swipe-action tints, tag chips, comment links, the Read button, NewDawn — from rendered pixels, not assumptions (D-28)."
    - "Differentiate Without Color is audited over the RESEARCH table (log-level dots colour-only = fail; filter selection and laboratory toggles = weak) with a grayscale-desaturated screenshot check; the verdicts and the proposed non-colour carriers are recorded (D-20)."
    - "Anything that would block a Nutrition Label claim or require a visible change beyond D-26's badge-text authorisation — rating-star colour (A5), the `CategoryCell` excluded-state treatment (Open Question 6), HC re-authoring (D-27), each D-28 colour fix — goes back to the owner as a decision with the measurements in hand; the bar is never bent (D-22)."
    - "Research Open Question 2 (do `.contextMenu` items already surface as VoiceOver actions on iOS 26?) is answered by a device observation the owner performs at the checkpoint, before any duplicate `accessibilityAction` is added (D-31, D-22)."
    - "Idempotency: `appearance` and `increase_contrast` on any simulator used for measurement are restored to the recorded baselines and read back."
  artifacts:
    - path: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      provides: "Category table (84 rows), HC comparison + proposal (40 rows), non-category measurements, DWC table, decisions section"
      contains: "4.62"
  key_links:
    - from: ".planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md"
      to: "App/Assets.xcassets/Category/Colors/{E-Hentai,ExHentai}/*.colorset/Contents.json"
      via: "every category number is computed from the live JSON, not copied from RESEARCH"
      pattern: "Contents.json"
  prohibitions:
    - statement: "No category background colour is moved in this plan or any other without the owner's explicit decision; the 44 standard variants are frozen regardless (D-26)."
      status: resolved
      verification: judgment
    - statement: "Never silently accept an unclaimable Nutrition Label category or bend the bar; reopen the blocking decision with the numbers (D-22)."
      status: resolved
      verification: judgment
    - statement: "No screenshot or image file enters the repository; measurement screenshots live under the evidence root `$HOME/Library/Caches/ehpanda-phase16/` named in `16-SWEEP.md § Protocol`, never the repo (D-32)."
      status: resolved
      verification: judgment
---

<objective>
Run the audit-first half of round 2 (D-20): reproduce the category contrast numbers from the live colorsets, compare and propose re-authored Increase Contrast variants (D-27), measure every non-category colour in light / dark / Increase Contrast from rendered pixels (D-28), audit Differentiate Without Color with a grayscale check, and bring the owner the decisions that would otherwise be taken silently — rating-star colour, the `CategoryCell` excluded state, HC re-authoring, each D-28 fix — plus the Open Question 2 device observation.

Purpose: measure before building; surface every visible change for authorisation; answer the research's open questions explicitly (D-22).
Output: `16-CONTRAST-AUDIT.md` with the numbers and the owner's recorded decisions.
</objective>

## Artifacts this phase produces

- SwiftLint custom rules in the root `.swiftlint.yml`: `no_dynamic_type_size_modifier`, `no_geometry_reader`, `no_fixed_system_font_size`, `accessibility_hardcoded_string` (plan 16-01); `no_minimum_scale_factor` (plan 16-12).
- Planning docs: `16-SWEEP.md`, `16-CONTRAST-AUDIT.md` (this plan creates it), `16-NUTRITION-LABEL.md` (plan 16-26).
- Swift: `Color+Contrast.swift` helper (plan 16-14); adaptive `CategoryLabel` / `CategoryCell` (plan 16-15); `OSLogEntryLog.Level.symbol` (plan 16-22, in `SettingFeature`); D-28 colour fixes and the rating-star decision (plan 16-23); accessibility semantics (plans 16-16 … 16-19) and Reduce Motion gating (plans 16-20, 16-21).
- Tests: `ColorContrastTests`, `CategoryColorsetInvariantTests` (plan 16-14), `AccessibilityAuditUITests` (plan 16-24).
- String-catalog keys `accessibility.*` in the touched modules' `Localizable.xcstrings`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-dynamic-type-accessibility/16-CONTEXT.md
@.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md
@.planning/phases/16-dynamic-type-accessibility/16-SWEEP.md
@$HOME/.claude/skills/swift-accessibility-skill/SKILL.md
@$HOME/.claude/skills/swift-accessibility-skill/references/display-settings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure — category variants, HC comparison + proposal, non-category colours, Differentiate Without Color</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - `$HOME/.claude/skills/swift-accessibility-skill/SKILL.md` and `references/display-settings.md` — the WCAG formula, 4.5:1 / 3:1 thresholds, light + dark + Increase Contrast + Reduce Transparency test procedure, Differentiate Without Color guidance.
    - `.planning/phases/16-dynamic-type-accessibility/16-RESEARCH.md` § "Contrast (focus area 8 / D-26–D-28)" (formula, re-measured numbers, colorset facts with the three encodings, the second white-on-category site, non-category inventory) and § "Differentiate Without Color audit (focus area 9 / D-20)".
    - `App/Assets.xcassets/Category/Colors/E-Hentai/Manga.colorset/Contents.json` and `App/Assets.xcassets/Category/Colors/ExHentai/Cosplay.colorset/Contents.json` — one hex-encoded and the decimal-encoded example; confirm the `appearances` keys (`luminosity: dark`, `contrast: high`).
    - `AppPackage/Sources/AppComponents/CategoryView.swift` — `CategoryLabel` (`.foregroundStyle(.white)`, `.footnote.bold()`) and `CategoryCell` (`.white` on `color.opacity(isFiltered ? 0.3 : 1)`).
    - `AppPackage/Sources/AppComponents/RatingView.swift`; `AppPackage/Sources/DetailFeature/DetailView.swift` around line 281 (offline notice `.orange`); `AppPackage/Sources/SystemNotification/ToastMessageView.swift` (secondary text on glass); `AppPackage/Sources/DownloadsFeature/DownloadsView.swift` swipe-action `.tint(`s; `AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift` lines 206–216 (level dot) and `AppPackage/Sources/AppModels/Support/AppActivityLog.swift` lines 72–82 (`Level.color`); `AppPackage/Sources/SettingFeature/Components/LaboratorySettingView.swift` lines 50–75.
  </read_first>
  <action>
Write `16-CONTRAST-AUDIT.md` with four measured sections and one empty decisions section:

1. `## Category variants (D-26)` — a script in the scratchpad (Python 3 or a `swift` script; never committed) walks the 22 `Contents.json` files, normalises the three component encodings (`"0x11"` hex byte, `"0.910"` float, `"163"` decimal byte — assert `color-space == "srgb"`), linearises (`c ≤ 0.04045 ? c/12.92 : ((c+0.055)/1.055)^2.4`), computes L = 0.2126R + 0.7152G + 0.0722B, and tabulates per variant (host, category, appearance ∈ light/dark/light+HC/dark+HC): L, ratio vs white, ratio vs black, best-of, chosen text (black if L > 0.1791 else white). Record the totals: variants (84), white failures (45), best-of failures (0), worst best-of (4.62 ExHentai/Game CG/light), flips to black (47), and the crossover (L = 0.17913, ratio 4.583). If any total differs from RESEARCH, say so and explain.
2. `## Increase Contrast variants (D-27)` — for each of the 40 HC variants: standard-sibling best-of vs HC best-of, `lower`/`higher`; count (expected 19/40 lower). Then a proposal table: for each `lower` variant, a re-authored sRGB value that keeps the hue (scale the linear channels uniformly away from the crossover — lighter when L > 0.1791, darker otherwise) until its best-of ≥ the standard sibling's best-of, with the resulting ratio; state explicitly that adopting it changes 40 HC entries' bytes while the 44 standard entries stay frozen, and that D-26's "84 byte-identical" and D-27's "re-authored" cannot both hold — the owner decides.
3. `## Non-category colours (D-28)` — measure rendered pixels: on `IPHONE_UDID` (read and record `appearance` / `increase_contrast` baselines first), for each of `appearance light` / `appearance dark` × `increase_contrast disabled` / `enabled` (`xcrun simctl ui <IPHONE_UDID> …`), open the relevant screens and take `--scale 1` screenshots to `$EVIDENCE_ROOT/round2/contrast/…` (the persistent evidence root from `16-SWEEP.md § Infrastructure`; `mkdir -p` first); sample foreground and background pixels with a scratchpad script (Python with a PNG reader, or a `swift` script using CoreGraphics) and compute ratios for: rating stars (`.yellow` on the list/detail background — non-text, 3:1; note the numeric rating beside them), offline notice text (`DetailView.swift` ~281, `.orange` `.subheadline.semibold` — text, 4.5:1; reachable by toggling Wi-Fi off in the simulator or by noting the colour pair from source if not triggerable), toast title / subtitle on the glass background (`ToastMessageView`), swipe-action label text on the teal / orange / indigo / red tints (Downloads row), tag chips (`.primary` on `Color(.systemGray5)`), comment link runs (`LinkedText` — underline/weight vs colour-alone), the Read button white on `.glassProminent` accent, NewDawn white on its drawn canvas (if presentable; else from source colours). Tabulate site | role (text / non-text) | light | dark | light+IC | dark+IC | threshold | verdict, and list the proposed fix for each failing site (the fix is a colour/weight change — a visible change that the owner can veto at the checkpoint). Restore `appearance` and `increase_contrast` to the baselines and read them back.
4. `## Differentiate Without Color (D-20)` — reproduce the RESEARCH table with a measured grayscale check: desaturate the relevant screenshots under `$EVIDENCE_ROOT/round2/contrast/` (`sips -s format png --matchTo '/System/Library/ColorSync/Profiles/Generic Gray Profile.icc'` or the script) and judge whether each state still reads: activity-log level dots (expected fail — propose per-level symbols: `circle.fill` for debug/info/notice, `exclamationmark.circle.fill` for error, `xmark.octagon.fill` for fault, plus the level title as the accessibility label), `CategoryCell` excluded (opacity 0.3 — measure the luminance difference in grayscale; propose the non-colour cue options), Laboratory feature on/off (tint vs `.secondary` + background — measure), plus the already-passing rows (download badge, detail download button, favourited heart, rating, vote thumbs, cookie validity, toast, offline notice, swipe actions).
5. `## Decisions` — empty, with the four decision slots (stars, CategoryCell excluded state, HC re-authoring, D-28 fixes) and the Open Question 2 slot to be filled by Task 3.

Commit with `docs(16): round-2 contrast and colour audit` after the image check.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "^## " "$f"; grep -c "4.62" "$f"; grep -c "0.1791" "$f"; xcrun simctl ui "$(grep -o 'IPHONE_UDID *= *[A-F0-9-]*' .planning/phases/16-dynamic-type-accessibility/16-SWEEP.md | head -1 | sed 's/.*= *//')" appearance; git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " 16-CONTRAST-AUDIT.md` prints `5`; the category table has 84 data rows (`grep -c "| light\|| dark" 16-CONTRAST-AUDIT.md` over § Category variants ≥ 84); the HC section lists 40 comparisons and a proposal row for every `lower` variant.
    - The totals line reads variants 84 / white failures 45 / best-of failures 0 / worst 4.62 / flips 47 / crossover 0.1791 — or a stated, explained deviation.
    - The non-category table has at least 8 site rows, each with four measured ratios (light, dark, light+IC, dark+IC), a threshold and a verdict; failing sites carry a proposed fix.
    - The DWC table has a verdict for every RESEARCH row, with proposed carriers for the failing/weak rows.
    - `xcrun simctl ui <IPHONE_UDID> appearance` and `… increase_contrast` print the recorded baselines after the task.
    - `grep -c "/Users/" 16-CONTRAST-AUDIT.md` prints `0`; `git status --porcelain | grep -Ei '\.(png|jpe?g|heic|gif)$' | wc -l` prints `0`; the commit `docs(16): round-2 contrast and colour audit` exists.
  </acceptance_criteria>
  <done>Every colour decision the phase needs is backed by a measured number in a committed text artifact.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Owner decisions — rating stars, CategoryCell excluded state, HC re-authoring, D-28 fixes; plus the context-menu device observation</name>
  <action>Stop and present the audit numbers to the owner; make no colour or code change until the resume line arrives (D-22).</action>
  <decision>Four colour/visibility decisions that D-22 says must not be taken silently, each presented with the measured numbers from `16-CONTRAST-AUDIT.md`, plus one two-minute device observation (Open Question 2).</decision>
  <context>D-26 authorised exactly one visible change (badge text colour). Everything below is a further visible change or a Nutrition Label claim boundary: (1) `.yellow` rating stars measure ≈ 1.3:1 on white — as non-text they fail 3:1 unless judged decorative duplicates of the numeric rating beside them (research A5); (2) the Filters `CategoryCell` excluded state is opacity-0.3 — luminance-only, weak under grayscale (research OQ6); (3) the 40 Increase Contrast colorset variants are less contrasty than their standard siblings in 19/40 best-of cases — D-27 says re-author, D-26 says byte-identical; (4) each D-28 non-category failure has a proposed colour/weight fix that changes appearance at `.large`. Also: whether SwiftUI `.contextMenu` items already appear as VoiceOver actions on iOS 26 decides whether plans 16-16/16-19 add explicit `accessibilityAction`s or would be duplicating.</context>
  <options>
    <option id="stars-a">
      <name>STARS=A — stars stay `.yellow`; the numeric rating text is the informational carrier; the star group gets a VoiceOver label + value</name>
      <pros>No visible change; Sufficient Contrast is claimed on the numeric text; VoiceOver reads "Rating, 4.5 out of 5".</pros>
      <cons>The star glyphs themselves remain below 3:1 as non-text; the Nutrition Label recommendation must state that the stars are decorative duplicates.</cons>
    </option>
    <option id="stars-b">
      <name>STARS=B — darken the star colour in light mode to a ≥ 3:1 yellow/amber (keep `.yellow` in dark), measured value in the audit</name>
      <pros>Stars pass 3:1 as non-text in both schemes; no caveat in the recommendation.</pros>
      <cons>A visible change at `.large` on every list cell and the Detail header; parity constraint crossed a second time.</cons>
    </option>
    <option id="cell-a">
      <name>CATEGORYCELL=A — keep opacity 0.3 for excluded; add adaptive text (D-26), `Button` semantics and `.isSelected` for included; rely on the measured grayscale luminance difference</name>
      <pros>No visible change; VoiceOver/Voice Control fixed by semantics; grayscale difference measured in the audit (state the number).</pros>
      <cons>Differentiate Without Color relies on luminance only — claimable only if the audit's grayscale number is clearly readable.</cons>
    </option>
    <option id="cell-b">
      <name>CATEGORYCELL=B — add a visible non-colour cue for excluded (strike-through text, or a `nosign` glyph overlay, or a dashed outline), agent's pick among the three unless you name one</name>
      <pros>Unambiguous under grayscale; Differentiate Without Color claimable without caveat.</pros>
      <cons>A visible change at `.large` in the Filters sheet; the screen joins the D-25 re-sweep list.</cons>
    </option>
    <option id="hc-a">
      <name>HC=A — adopt the proposed re-authored values for the HC variants that are `lower` (≤ 40 entries); the 44 standard variants stay byte-identical; the invariant test pins the two groups separately</name>
      <pros>Increase Contrast never yields less contrast (D-27 fulfilled); brand (standard) colours untouched.</pros>
      <cons>HC bytes change; D-26's "84 byte-identical" narrows to "44 standard byte-identical + 40 HC re-pinned"; visible only to users with Increase Contrast on.</cons>
    </option>
    <option id="hc-b">
      <name>HC=B — leave all 84 variants byte-identical; record the 19/40 residual as a documented should-fix not done</name>
      <pros>D-26 holds literally; zero asset change.</pros>
      <cons>D-27 not fulfilled; the recommendation must note that Increase Contrast does not reduce badge contrast below 4.5:1 but does reduce it relative to standard on 19 variants.</cons>
    </option>
    <option id="d28-ok">
      <name>D28=ok — apply every proposed D-28 fix listed in the audit (offline-notice orange, toast text on glass, swipe-action text, etc., as measured)</name>
      <pros>Every measured failure is fixed; Sufficient Contrast claimable across the app.</pros>
      <cons>Each is a small visible change at `.large`; veto any item by name instead (`D28=veto:<site>,<site>`).</cons>
    </option>
  </options>
  <resume-signal>Also do the Open Question 2 check on `Owner-iPhone-Test` with VoiceOver on: focus a tag cell in a gallery's tag cloud (it has a `.contextMenu`), swipe up/down and say whether the context-menu items appear under "Actions". Then reply on one line: `STARS=A|B CATEGORYCELL=A|B[:strike|nosign|outline] HC=A|B D28=ok|veto:<sites> CONTEXTMENU=exposed|not-exposed`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the decisions and the Open Question 2 answer</name>
  <files>.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md</files>
  <read_first>
    - `.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md` § Decisions — the five slots.
  </read_first>
  <action>
Fill `## Decisions` with the owner's resume line verbatim and one bullet per slot stating what the downstream plans will do: STARS (16-19 labels the group; 16-23 changes the light-mode colour only under B), CATEGORYCELL (16-15 builds A or B with the named cue), HC (16-15 edits the HC entries and re-pins the HC hash only under A; the standard-44 pin never changes), D28 (16-23 applies the non-vetoed fixes; 16-22 adds the DWC glyphs), CONTEXTMENU (16-16 / 16-19 add explicit `accessibilityAction`s only if `not-exposed`). Add the `D-25 re-sweep candidates` list: every screen that gains a visible element under the chosen options (Filters sheet under CATEGORYCELL=B; list cells + Detail header under STARS=B; Activity Logs for the level glyphs; any D-28 site whose fix changes size — text-colour-only changes move no layout and are excluded per D-24). Commit with `docs(16): record round-2 colour decisions`.
  </action>
  <verify>
    <automated>f=.planning/phases/16-dynamic-type-accessibility/16-CONTRAST-AUDIT.md; grep -c "STARS=\|CATEGORYCELL=\|HC=\|D28=\|CONTEXTMENU=" "$f"; grep -c "D-25 re-sweep candidates" "$f"; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `## Decisions` contains all five tokens `STARS=`, `CATEGORYCELL=`, `HC=`, `D28=`, `CONTEXTMENU=` with values, and one downstream-plan bullet per slot.
    - `D-25 re-sweep candidates` lists the screens implied by the chosen options (possibly empty with the reason).
    - `git log -1 --format=%s` equals `docs(16): record round-2 colour decisions`; `grep -c "/Users/" 16-CONTRAST-AUDIT.md` prints `0`.
  </acceptance_criteria>
  <done>Every round-2 colour and DWC build task has an owner decision to implement against; Open Question 2 is answered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measurement screenshots → evidence root | Contrast sampling uses rendered screenshots of real content; evidence root (`$HOME/Library/Caches/ehpanda-phase16/`) only. |
| Agent proposal → owner decision | Visible changes and claim boundaries are decided by the owner, never silently by the agent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information disclosure | measurement screenshots | high | mitigate | Evidence-root only; image check before each docs commit. |
| T-16-05 | Tampering | category background bytes | medium | mitigate | This plan edits no colorset; HC re-authoring happens only under HC=A in plan 16-15 with the standard-44 pin unchanged. |
| T-16-14 | Repudiation | a Nutrition Label claim resting on an unmeasured colour | medium | mitigate | Every site has four measured ratios in a committed table; decisions recorded verbatim. |
| T-16-03 | Denial of service (phase infrastructure) | sweep simulator used for appearance switching | medium | mitigate | Baselines read first, restored and read back; no erase/reinstall. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
1. `16-CONTRAST-AUDIT.md` with the five sections, 84 category rows, 40 HC comparisons + proposals, ≥ 8 non-category sites × 4 ratios, DWC verdicts; simulator appearance restored.
2. Decision checkpoint resumed with the five tokens; decisions and D-25 candidates recorded and committed.
</verification>

<success_criteria>
- Audit-first honoured for Sufficient Contrast and Differentiate Without Color (D-20); D-26/D-27 numbers reproduced; D-28 measured; every visible change and claim boundary decided by the owner with numbers (D-22); Open Question 2 answered on device.
</success_criteria>

<output>
Create `.planning/phases/16-dynamic-type-accessibility/16-13-SUMMARY.md` when done: the totals, the failing non-category sites, the DWC fails, the owner's decision line verbatim, commit hashes.
</output>
