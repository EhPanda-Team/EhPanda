---
phase: 12-cloudflare-login-restoration
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request.swift
  - AppPackage/Sources/NetworkingFeature/Request+Account.swift
  - AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift
autonomous: true
requirements: [C2, C4]
must_haves:
  truths:
    - "Any HTTP response can be classified as a Cloudflare challenge purely from its status code and cf-mitigated header — no host or setting conditionals (D-05, D-08, C2)"
    - "A login POST built with a clearance pair carries an explicit Cookie header with the clearance value and the solving web view's exact User-Agent, and shared-jar cookie handling is disabled so those headers are authoritative (D-04, D-07, C4)"
    - "A login POST built without a clearance pair is constructed exactly as today — the no-wall path is unchanged (C2)"
  artifacts:
    - path: "AppPackage/Sources/NetworkingFeature/Request.swift"
      provides: "Generic response-driven challenge classifier"
      contains: "func isCloudflareChallenge"
    - path: "AppPackage/Sources/NetworkingFeature/Request+Account.swift"
      provides: "LoginRequest clearance-carrying variant with explicit Cookie/User-Agent headers"
      contains: "clearance"
    - path: "AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift"
      provides: "Classifier matrix + stubbed header assertions for both LoginRequest variants"
      min_lines: 60
  key_links:
    - from: "AppPackage/Sources/NetworkingFeature/Request+Account.swift"
      to: "AppPackage/Sources/AppModels/Support/CloudflareClearance.swift"
      via: "optional CloudflareClearance parameter on LoginRequest"
      pattern: "CloudflareClearance"
    - from: "AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift"
      to: "AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift"
      via: "makeStubbedSession + receivedRequests header assertions"
      pattern: "makeStubbedSession"
---

<objective>
Add the two NetworkingFeature seams the challenge flow rides on: the generic per-response
challenge classifier (D-05) and the clearance-carrying `LoginRequest` variant whose explicit
Cookie + User-Agent headers are authoritative on the retried POST (D-04, D-07, C4) — with the
no-clearance path byte-identical to today (C2's no-wall case). Both land with their tests.

Purpose: detection is the phase's entry point and the retried POST is its exit point; both are
pure request-layer concerns that the reducer (12-04) merely consumes.
Output: `isCloudflareChallenge(_:)`, extended `LoginRequest`, and
`CloudflareChallengeDetectionTests` covering C2 classification and C4 header carriage offline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-cloudflare-login-restoration/12-CONTEXT.md
@.planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md
@.planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md
@.planning/phases/12-cloudflare-login-restoration/12-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 12 (producing plan in parentheses):

- `AppError.cloudflareChallengeFailed`; xcstrings keys `app_error.cloudflare_challenge_failed`, `app_error.cloudflare_challenge_failed_description`, `app_error.cloudflare_challenge_solution`; `CloudflareClearance` (`cookieValue`, `userAgent`); `SharedKey.cloudflareClearance` (12-01)
- `isCloudflareChallenge(_:)` — public free function in NetworkingFeature/Request.swift (12-02)
- `LoginRequest.init(username:password:clearance:urlSession:)` — new optional `clearance: CloudflareClearance?` parameter (12-02)
- `CloudflareChallengeDetectionTests` test suite (12-02)
- `ChallengeWebView`; `LoginClient` + `LoginClientKey` + `DependencyValues.loginClient` (12-03)
- `LoginReducer` additions: `Destination.challenge(URL)`, `Destination.errorInfo(ErrorInfo)`, `Action.challengeDetected`, `Action.challengeClearanceCaptured(CloudflareClearance)`, `Action.cancelChallenge`, `Action.presentErrorInfo(ErrorInfo)`, `Action.toast(PresentationAction<Never>)`, `State.challengeRounds`, `State.toast`, `State.cloudflareClearance` (12-04)
- `LoginChallengeFlowTests` test suite (12-05)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generic Cloudflare challenge classifier (D-05) with classification matrix tests</name>
  <files>AppPackage/Sources/NetworkingFeature/Request.swift, AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request.swift (helper placement beside `mapAppError`; the file's free-function/extension conventions)
    - AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift (suite conventions, imports, naming)
    - .planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md §"Pattern 1" (the classifier semantics and the Cloudflare cf-mitigated citation)
  </read_first>
  <behavior>
    - Test 1: status 403 with header field `cf-mitigated: challenge` → true
    - Test 2: status 403 with the header value in different letter case (e.g. "Challenge") → true (value compared case-insensitively)
    - Test 3: status 403 without the header, or with a different header value → false
    - Test 4: status 200 or 503 carrying `cf-mitigated: challenge` → false (403 is required)
    - Test 5: nil response, and a non-HTTP `URLResponse` → false
  </behavior>
  <action>
    In Request.swift add a public top-level free function
    `isCloudflareChallenge(_ response: URLResponse?) -> Bool` (free function, not a `Request`
    extension member, so SettingFeature's reducer can call it without a request instance — the
    D-05 "generic helper" contract; classification needs only the response because the
    `cf-mitigated` header is Cloudflare's sanctioned signal, so any `(data, response)` call site
    can pass its response). Implementation per RESEARCH Pattern 1: cast to `HTTPURLResponse`,
    require `statusCode == 403`, read `value(forHTTPHeaderField: "cf-mitigated")` (header-name
    lookup is already case-insensitive) and compare the value to "challenge" with
    `caseInsensitiveCompare`. Zero host, path, or settings conditionals — detection is purely
    response-driven (D-08, C2). Doc-comment the function citing the 403 + cf-mitigated contract
    and that only the login flow wires it to UI this phase.

    Create `AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift`
    with a Swift Testing suite covering the behavior matrix above using directly-constructed
    `HTTPURLResponse(url:statusCode:httpVersion:headerFields:)` fixtures — no network, no stub
    session needed for classification.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:NetworkingFeatureTests/CloudflareChallengeDetectionTests</automated>
  </verify>
  <acceptance_criteria>
    - Request.swift contains `public func isCloudflareChallenge(_ response: URLResponse?) -> Bool`
    - `test "$(grep -v '^[[:space:]]*//' AppPackage/Sources/NetworkingFeature/Request.swift | grep -c 'bypassSNIFiltering')" -eq 0` passes (no domain-fronting conditional in the classifier file, D-08)
    - The five behavior tests exist and pass via the targeted `xcodebuild test` command
  </acceptance_criteria>
  <done>Classifier compiles as a public free function, the full classification matrix passes, and the helper contains no host/setting branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clearance-carrying LoginRequest variant (D-04, D-07, C4) with stubbed header assertions</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Account.swift, AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift (current LoginRequest — the shape to preserve verbatim on the nil-clearance path)
    - AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift (StubScript/StubStep/makeStubbedSession/receivedRequests API)
    - .planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md §"Pattern 3" and §"Pitfall 4" (authoritative-header semantics and the shared-jar caveat)
  </read_first>
  <behavior>
    - Test 1: `LoginRequest(username:password:clearance:urlSession:)` with a `CloudflareClearance(cookieValue: "abc", userAgent: "UA-X")` records a request whose `Cookie` header equals "cf_clearance=abc" and whose `User-Agent` header equals "UA-X" (assert via `handle.receivedRequests` `value(forHTTPHeaderField:)`)
    - Test 2: the same request has `httpShouldHandleCookies == false` on the recorded request
    - Test 3: `LoginRequest(username:password:urlSession:)` without a clearance records a request with nil `Cookie` and nil `User-Agent` explicit header fields, method POST, and the existing url-encoded Content-Type — the no-wall construction is unchanged
    - Test 4: both variants POST to `Defaults.URL.login`
  </behavior>
  <action>
    Extend `LoginRequest` with a `clearance: CloudflareClearance?` stored property, defaulted to
    nil in the init (parameter order: username, password, clearance, urlSession), preserving the
    existing memberwise style. In `response()`, keep the params dictionary, URL, method, body,
    and `setURLEncodedContentType()` exactly as they are; then, only when `clearance` is non-nil
    (D-04, D-07): set `request.httpShouldHandleCookies = false` (stop the shared jar injecting or
    overwriting — the explicit header must be authoritative), set the `Cookie` header field to
    "cf_clearance=" + `clearance.cookieValue`, and set the `User-Agent` header field to
    `clearance.userAgent` (the exact solving web view's UA — Cloudflare binds the clearance to
    it; per D-07 this UA applies to this retried login POST only, never to general app traffic).
    Add a code comment noting the Pitfall-4 rationale: login credentials arrive via Set-Cookie on
    the response (consumed downstream by `setCredentials`), not via sent cookies, so suppressing
    the jar drops nothing the POST needs; the returned `HTTPURLResponse` still carries Set-Cookie
    regardless of the flag. Import AppModels is already present in the file.

    Extend CloudflareChallengeDetectionTests.swift with the four behavior tests above using
    `makeStubbedSession` + a `StubScript` for `Defaults.URL.login` (a single 200 step suffices —
    these tests assert the outbound request, not the response), injecting the stubbed session via
    the request's `urlSession` parameter, and calling `handle.tearDown()` at the end of each test.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:NetworkingFeatureTests/CloudflareChallengeDetectionTests</automated>
  </verify>
  <acceptance_criteria>
    - Request+Account.swift's LoginRequest has a `clearance: CloudflareClearance?` property and its `response()` sets `Cookie` and `User-Agent` header fields only inside a non-nil-clearance branch
    - The recorded no-clearance request carries neither explicit header (Test 3 passes) — proving the C2 no-wall path is construction-identical
    - The recorded clearance request carries both headers with the exact expected values (Test 1 passes) and `httpShouldHandleCookies == false` (Test 2 passes)
    - `xcodebuild test ... -only-testing:NetworkingFeatureTests/CloudflareChallengeDetectionTests` exits 0; full NetworkingFeatureTests target also green
  </acceptance_criteria>
  <done>Both LoginRequest variants construct provably correct requests offline, and the whole NetworkingFeatureTests target passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Cloudflare edge | Response status/headers are untrusted input crossing into classification |
| Shared cookie jar → login POST | Jar contents must not silently override the explicit clearance header |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Spoofing | `isCloudflareChallenge` header parsing | medium | mitigate | Treat headers as untrusted: exact status check (403) + exact value match (case-insensitive compare against "challenge"); never evaluate page content (ASVS V5) |
| T-12-05 | Tampering | Shared-jar cookie interference on the retried POST | high | mitigate | `httpShouldHandleCookies = false` on the clearance variant + explicit Cookie header (D-04); test asserts the flag and header on the recorded request |
| T-12-06 | Information Disclosure | Clearance value in logs/diagnostics | high | mitigate | No logging added in this plan's request paths; the Phase 8 cookie-logging static gate (scripts/check-cookie-logging.sh, verified in 12-06) covers cf_clearance like any cookie value |
| T-12-07 | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Targeted suite: `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:NetworkingFeatureTests/CloudflareChallengeDetectionTests` green
- Whole target: `-only-testing:NetworkingFeatureTests` green (existing baseline suites unaffected — LoginRequest's nil-clearance construction is unchanged)
</verification>

<success_criteria>
- Classification is response-driven with zero host/setting conditionals (C2, D-05, D-08)
- Retried-POST header carriage is proven offline via recorded requests (C4, D-04, D-07)
- No-wall LoginRequest construction is unchanged (C2); all NetworkingFeatureTests green
</success_criteria>

<output>
Create `.planning/phases/12-cloudflare-login-restoration/12-02-SUMMARY.md` when done
</output>
