---
phase: 12-cloudflare-login-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/AppModels/Support/AppError.swift
  - AppPackage/Sources/AppModels/Resources/Localizable.xcstrings
  - AppPackage/Sources/AppModels/Support/CloudflareClearance.swift
  - AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift
  - AppPackage/Tests/AppModelsTests/AppErrorStructuredTests.swift
autonomous: true
requirements: [C5]
must_haves:
  truths:
    - "AppError has a dedicated cloudflareChallengeFailed case with its own localized description, alert text, and a recovery suggestion steering the user to the in-app web login and manual cookie entry (D-10)"
    - "A (cookieValue, userAgent) clearance pair can be held in an in-memory shared key that resets to nil on every app launch (D-06, C5)"
    - "The new error case is non-retryable and its description is distinct from every existing case (AppError.id is the description)"
  artifacts:
    - path: "AppPackage/Sources/AppModels/Support/AppError.swift"
      provides: "cloudflareChallengeFailed case wired into all five members (case list, isRetryable, localizedDescription, alertText, solution)"
      contains: "case cloudflareChallengeFailed"
    - path: "AppPackage/Sources/AppModels/Support/CloudflareClearance.swift"
      provides: "Named Sendable pair type for the captured clearance cookie value + solving web view's User-Agent"
      contains: "struct CloudflareClearance"
    - path: "AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift"
      provides: "InMemoryKey session holder for the clearance pair, defaulting to nil"
      contains: "cloudflareClearance"
    - path: "AppPackage/Sources/AppModels/Resources/Localizable.xcstrings"
      provides: "Three new app_error.cloudflare_challenge_* keys localized in all six catalog locales"
      contains: "app_error.cloudflare_challenge_failed"
  key_links:
    - from: "AppPackage/Sources/AppModels/Support/AppError.swift"
      to: "AppPackage/Sources/AppModels/Resources/Localizable.xcstrings"
      via: "String(localized:) with the generated appErrorCloudflareChallenge* symbols"
      pattern: "appErrorCloudflareChallenge"
    - from: "AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift"
      to: "AppPackage/Sources/AppModels/Support/CloudflareClearance.swift"
      via: "InMemoryKey<CloudflareClearance?> value type"
      pattern: "InMemoryKey<CloudflareClearance\\?>"
---

<objective>
Lay the AppModels foundation for the Cloudflare login restoration: the dedicated
`AppError.cloudflareChallengeFailed` failure case with its localized strings (D-10), and the
`CloudflareClearance` value type plus its launch-resetting in-memory shared key that holds the
session-lifetime (clearance, User-Agent) pair (D-06, ROADMAP Phase 12 criterion C5).

Purpose: every later plan (request variant, web-view wrapper, reducer flow) consumes these two
types. Landing them first keeps each subsequent plan a pure consumer.
Output: extended `AppError`, three new `.xcstrings` key families, new `CloudflareClearance`
struct, new `.cloudflareClearance` shared key, and extended `AppErrorStructuredTests`.

Note (visible skip, recorded per planning directive): the spec-less probe fallback was skipped —
this phase has no requirement IDs to probe. Plan scope derives from ROADMAP §Phase 12's five
success criteria (C1–C5) and CONTEXT.md decisions D-01…D-11; `requirements` frontmatter uses the
C1–C5 criterion labels.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cloudflare-login-restoration/12-CONTEXT.md
@.planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md
@.planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols created across Phase 12 (producing plan in parentheses):

- `AppError.cloudflareChallengeFailed` — new enum case (12-01)
- `Localizable.xcstrings` keys `app_error.cloudflare_challenge_failed`, `app_error.cloudflare_challenge_failed_description`, `app_error.cloudflare_challenge_solution` → generated symbols `.appErrorCloudflareChallengeFailed`, `.appErrorCloudflareChallengeFailedDescription`, `.appErrorCloudflareChallengeSolution` (12-01)
- `CloudflareClearance` struct with `cookieValue: String`, `userAgent: String` (12-01)
- `SharedKey.cloudflareClearance` — `InMemoryKey<CloudflareClearance?>` (12-01)
- `isCloudflareChallenge(_:)` — public free function in NetworkingFeature (12-02)
- `LoginRequest.init(username:password:clearance:urlSession:)` — new optional `clearance` parameter (12-02)
- `ChallengeWebView` — `UIViewControllerRepresentable` WKWebView wrapper with cookie-store observer + UA readout (12-03)
- `LoginClient` struct + `LoginClientKey` + `DependencyValues.loginClient` (12-03)
- `LoginReducer` additions: `Destination.challenge(URL)`, `Destination.errorInfo(ErrorInfo)`, `Action.challengeDetected`, `Action.challengeClearanceCaptured(CloudflareClearance)`, `Action.cancelChallenge`, `Action.presentErrorInfo(ErrorInfo)`, `Action.toast(PresentationAction<Never>)`, `State.challengeRounds`, `State.toast`, `State.cloudflareClearance` (`@Shared`) (12-04)
- Test files: `AppPackage/Tests/NetworkingFeatureTests/CloudflareChallengeDetectionTests.swift` (12-02), `AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift` (12-05)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AppError.cloudflareChallengeFailed with localized strings (D-10)</name>
  <files>AppPackage/Sources/AppModels/Support/AppError.swift, AppPackage/Sources/AppModels/Resources/Localizable.xcstrings, AppPackage/Tests/AppModelsTests/AppErrorStructuredTests.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Support/AppError.swift (the enum and all five member switches being extended)
    - AppPackage/Tests/AppModelsTests/AppErrorStructuredTests.swift (existing suite conventions to extend)
    - .planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md §"AppModels/Support/AppError.swift" (the authenticationRequired analog threading all five members)
    - CLAUDE.md (repo root) — localized-key conventions (labeled format arguments, non-translated-key rules; note the new keys carry no format arguments, so no substitutions are needed)
  </read_first>
  <behavior>
    - Test 1: `AppError.cloudflareChallengeFailed.isRetryable` is false
    - Test 2: `localizedDescription`, `alertText` are non-empty and `solution`/`recoverySuggestion` are non-nil for the new case
    - Test 3: the new case's `localizedDescription` differs from every other case's (`AppError.id` uniqueness — build the full case list and assert distinct ids)
  </behavior>
  <action>
    Implement D-10. In AppError.swift add `case cloudflareChallengeFailed` to the enum (no
    associated value — per Phase 9 conventions, per-incident diagnostics travel in
    `ErrorInfo.context`, not the enum). Thread it through all five members exactly like the
    `authenticationRequired` analog: add it to the non-retryable arm of `isRetryable`;
    `localizedDescription` returns `String(localized: .appErrorCloudflareChallengeFailed)`;
    `alertText` returns `String(localized: .appErrorCloudflareChallengeFailedDescription)`;
    `solution` returns `String(localized: .appErrorCloudflareChallengeSolution)` (the
    `LocalizedError` conformance then exposes it as `recoverySuggestion` for free).

    In Localizable.xcstrings add three keys following the existing `app_error.authentication_*`
    entry shape (`extractionState: manual`, `stringUnit.state: translated`, all six locales: en,
    de, ja, ko, zh-Hans, zh-Hant):
    - `app_error.cloudflare_challenge_failed` — en: "Cloudflare Verification Failed"
    - `app_error.cloudflare_challenge_failed_description` — en: "Cloudflare's verification couldn't be completed after repeated attempts."
    - `app_error.cloudflare_challenge_solution` — en: "Sign in with the web login (globe button on the login screen), or enter your cookies manually in Account Settings." (D-10: steer to the two working alternatives, not a generic try-again)
    Translate the de/ja/ko/zh-Hans/zh-Hant values matching the tone of the neighboring
    `app_error.authentication_*` family. No numeric or string format arguments — plain strings,
    so the AGENTS.md substitution rules impose no extra structure.

    Extend AppErrorStructuredTests.swift with the behavior tests above, following the file's
    existing Swift Testing conventions.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppModelsTests/AppErrorStructuredTests</automated>
  </verify>
  <acceptance_criteria>
    - AppError.swift contains `case cloudflareChallengeFailed` and the case appears in exactly one arm of `isRetryable` (the `return false` arm), and in the `localizedDescription`, `alertText`, and `solution` switches
    - `python3 -c "import json; d=json.load(open('AppPackage/Sources/AppModels/Resources/Localizable.xcstrings')); ks=['app_error.cloudflare_challenge_failed','app_error.cloudflare_challenge_failed_description','app_error.cloudflare_challenge_solution']; assert all(sorted(d['strings'][k]['localizations'].keys())==['de','en','ja','ko','zh-Hans','zh-Hant'] for k in ks)"` exits 0
    - The en value of `app_error.cloudflare_challenge_solution` names both alternatives (web login and manual cookie entry)
    - `xcodebuild test ... -only-testing:AppModelsTests/AppErrorStructuredTests` exits 0 with the three new behavior tests present and passing
  </acceptance_criteria>
  <done>The new case compiles into all five AppError members, the three keys exist in all six locales, and the extended AppModelsTests suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add CloudflareClearance value type and its in-memory session key (D-06)</name>
  <files>AppPackage/Sources/AppModels/Support/CloudflareClearance.swift, AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift (the `greeting` and `privacyMaskBlur` InMemoryKey declarations and their doc-comment style — the Phase 7 precedent)
    - .planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md §"Session holder" (key shape + labeled-tuple lint note)
  </read_first>
  <action>
    Create `AppPackage/Sources/AppModels/Support/CloudflareClearance.swift`: a public struct
    `CloudflareClearance: Equatable, Hashable, Sendable` with `public let cookieValue: String`
    (the captured clearance cookie's value) and `public let userAgent: String` (the exact
    User-Agent of the web view that solved the challenge — Cloudflare binds the clearance to it),
    plus a public memberwise init. A named struct, not a tuple — the `labeled_tuple_elements`
    lint rule is at error and three consumers read the fields. Doc-comment the type: the pair
    travels together, lives only for the app session, and is deliberately never persisted
    (ROADMAP Phase 12 criterion 5).

    In AppSharedKeys.swift add, beside the `greeting`/`privacyMaskBlur` precedents, an extension
    `SharedKey where Self == InMemoryKey<CloudflareClearance?>.Default` with
    `public static var cloudflareClearance` returning `Self[.inMemory("cloudflareClearance"), default: nil]`.
    Doc-comment per D-06: written by the login flow when a challenge web view captures the pair,
    read to proactively attach it to later login POSTs in the same session, replaced whenever a
    retry comes back challenged; in-memory strategy means it resets to nil on every launch with
    zero cleanup code — the literal reading of "never persisted across launches".
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'generic/platform=iOS Simulator'</automated>
  </verify>
  <acceptance_criteria>
    - CloudflareClearance.swift exists and contains `public struct CloudflareClearance`, `public let cookieValue: String`, `public let userAgent: String`
    - AppSharedKeys.swift contains a `cloudflareClearance` key whose declaration uses `InMemoryKey<CloudflareClearance?>` (source assertion: the key is in-memory, not appStorage — this is the C5 no-persistence guarantee)
    - `xcodebuild build` exits 0 (SwiftLint runs at error severity inside the build)
  </acceptance_criteria>
  <done>Both types compile, the key is in-memory with a nil default, and the build (including lint) is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → persisted storage | The clearance pair must never cross from memory into any persistence layer |
| Error surface → user | Localized failure strings are user-visible; they must not embed secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | `.cloudflareClearance` shared key | high | mitigate | Key declared with the `InMemoryKey` strategy only (never `appStorage`/`fileStorage`); acceptance criterion asserts the declaration; resets to nil each launch |
| T-12-02 | Information Disclosure | AppError strings / ErrorInfo | medium | mitigate | New case carries no associated value; description/solution strings are static localized text with no runtime interpolation, so no cookie value can leak through the toast or ErrorInfoView |
| T-12-03 | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A — first-party frameworks only) |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppModelsTests` green
- Build clean under SwiftLint-as-error (build plugin)
- Catalog locale audit (python assertion above) passes for all three new keys
</verification>

<success_criteria>
- `AppError.cloudflareChallengeFailed` fully threaded (5 members) with distinct, localized, alternative-steering strings (D-10)
- `CloudflareClearance` + `.cloudflareClearance` in-memory key compile and are consumable from other AppPackage modules (D-06, C5)
- Extended AppModelsTests green; no lint violations
</success_criteria>

<output>
Create `.planning/phases/12-cloudflare-login-restoration/12-01-SUMMARY.md` when done
</output>
