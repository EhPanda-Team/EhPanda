---
phase: 12-cloudflare-login-restoration
plan: 05
type: execute
wave: 5
depends_on: [12-04]
files_modified:
  - AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift
autonomous: true
requirements: [C2, C3, C5]
must_haves:
  truths:
    - "The no-wall path is proven: a non-challenged response reaches loginDone with no challenge destination and no toast (C2)"
    - "The bounded state machine is proven offline: two challenge presentations, then the third challenge yields loginDone(.failure(.cloudflareChallengeFailed)) with the persistent error toast set (D-09, D-10, D-11, C5)"
    - "Capture auto-dismisses and retries with the captured pair; a held session pair is proactively attached and replaced on re-challenge (C3, C4, D-06)"
    - "Mid-challenge dismissal is silent: loginState returns to .idle with no retry and no toast, proven by exhaustive TestStore assertions (D-02)"
  artifacts:
    - path: "AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift"
      provides: "Exhaustive TestStore coverage of the challenge state machine via stubbed LoginClient"
      min_lines: 150
  key_links:
    - from: "AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift"
      to: "AppPackage/Sources/SettingFeature/Login/LoginReducer.swift"
      via: "TestStore(initialState:reducer: LoginReducer.init) with overridden loginClient"
      pattern: "LoginReducer.init"
---

<objective>
Prove the 12-04 state machine offline: an exhaustive `TestStore` suite driving every criterion
that does not require the live wall — no-wall pass-through (C2), presentation and auto-dismiss
(C3, D-01), bounded rounds and the dedicated failure (D-09/D-10/D-11, C5), silent cancel (D-02),
and session-pair proactive attach + replacement (D-06) — through the stubbed `LoginClient` seam.

Purpose: after this plan, the only unproven link is the live Cloudflare interaction itself, which
is exactly what the 12-06 owner UAT covers (RESEARCH: owner UAT is the authoritative C1 gate).
Output: `LoginChallengeFlowTests.swift`, plus a green run of the affected targets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-cloudflare-login-restoration/12-CONTEXT.md
@.planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md
@.planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md
@.planning/phases/12-cloudflare-login-restoration/12-04-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 12 (producing plan in parentheses):

- `AppError.cloudflareChallengeFailed`; xcstrings keys `app_error.cloudflare_challenge_failed`, `app_error.cloudflare_challenge_failed_description`, `app_error.cloudflare_challenge_solution`; `CloudflareClearance` (`cookieValue`, `userAgent`); `SharedKey.cloudflareClearance` (12-01)
- `isCloudflareChallenge(_:)`; `LoginRequest.init(username:password:clearance:urlSession:)`; `CloudflareChallengeDetectionTests` (12-02)
- `ChallengeWebView`; `LoginClient` + `LoginClientKey` + `DependencyValues.loginClient` (12-03)
- `LoginReducer` additions: `Destination.challenge(URL)`, `Destination.errorInfo(ErrorInfo)`, `Action.challengeDetected`, `Action.challengeClearanceCaptured(CloudflareClearance)`, `Action.cancelChallenge`, `Action.presentErrorInfo(ErrorInfo)`, `Action.toast(PresentationAction<Never>)`, `State.challengeRounds`, `State.toast`, `State.cloudflareClearance` (12-04)
- `LoginChallengeFlowTests` test suite (12-05)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Challenge state-machine TestStore suite — pass-through, rounds, capture, session pair</name>
  <files>AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift</files>
  <read_first>
    - AppPackage/Tests/SettingFeatureTests/AccountSettingReducerTests.swift (suite conventions: @MainActor on members not the type, private makeStore factory with overridable dependencies, exhaustive assertions, TestStore(initialState:reducer: Reducer.init) shorthand, CookieClient.testing usage)
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (the state machine under test — exact action names, guard conditions, effect shapes)
    - AppPackage/Sources/SettingFeature/Login/LoginClient.swift (the endpoint signature being stubbed)
  </read_first>
  <behavior>
    - Test 1 (C2 no-wall): stub loginClient returns a plain 200 HTTPURLResponse; CookieClient.testing with credentials so didLogin is true; send .login (assert loginState .loading, challengeRounds 0, focus cleared), receive .loginDone(.success) — destination never becomes .challenge, no toast; dismissal effect runs (override \.dismiss)
    - Test 2 (C3/D-01/D-03 presentation): stub returns a 403 response with the cf-mitigated challenge header; send .login, receive .challengeDetected — destination == .challenge(Defaults.URL.login), challengeRounds == 1, loginState still .loading
    - Test 3 (C3/C4/D-06 capture + retry): continue from a presented challenge; script the stub so the next call returns 200; send .challengeClearanceCaptured(pair) — destination nil, shared cloudflareClearance == pair, then receive .loginDone(.success); assert (via a LockIsolated recording of stub invocations) that the second call received exactly the captured pair
    - Test 4 (D-09/D-10/D-11/C5 bound): script the stub to return the challenged 403 three times; walk login → challengeDetected (rounds 1) → captured → challengeDetected (rounds 2) → captured → receive .loginDone(.failure(.cloudflareChallengeFailed)) — toast == .error(ErrorInfo(error: .cloudflareChallengeFailed, context: matching the reducer's rows)), loginState == .failed(.cloudflareChallengeFailed), destination nil
    - Test 5 (D-06 proactive attach): seed the shared holder with a pair before store creation (write through @Shared(.cloudflareClearance) in the test); send .login; assert the stub's first invocation received the held pair
    - Test 6 (hardening): send .challengeClearanceCaptured with no challenge destination presented — state unchanged, no effect received
  </behavior>
  <action>
    Create the suite following AccountSettingReducerTests conventions. Build a private
    `makeStore` factory taking a scripted LoginClient stub (a `LockIsolated` array of
    HTTPURLResponse-or-error steps plus a `LockIsolated` invocation log recording the clearance
    argument per call), a CookieClient (use `CookieClient.testing` variants to control
    `didLogin`), a no-op HapticsClient override, and a `\.dismiss` override for success paths.
    Construct challenged responses as `HTTPURLResponse(url: Defaults.URL.login, statusCode: 403,
    httpVersion: nil, headerFields: ["cf-mitigated": "challenge"])` and pass-through responses as
    plain 200s — the reducer's classifier consumes them exactly as production would.

    Exhaustive TestStore semantics throughout (no `withExhaustivity(.off)` — the Phase 7
    precedent): every state mutation asserted in the `send`/`receive` trailing closures, and
    `store.finish()` closing each case. For the shared holder, assert through the state's
    `cloudflareClearance` property; note `@Shared` equality participates in State equality, so
    seed the key before constructing the initial state in Test 5.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests/LoginChallengeFlowTests</automated>
  </verify>
  <acceptance_criteria>
    - LoginChallengeFlowTests.swift exists with at least the six behavior tests above, each passing
    - Test 4 walks two full challenge rounds and asserts the third yields `.cloudflareChallengeFailed` with the toast set — the D-09 bound proven end-to-end in the reducer
    - Test 3 and Test 5 assert the stub's received clearance arguments (proving D-06 attach and replacement at the seam)
    - `xcodebuild test ... -only-testing:SettingFeatureTests/LoginChallengeFlowTests` exits 0
  </acceptance_criteria>
  <done>The state machine's happy path, bound, capture/retry, and session-pair semantics are all proven offline with exhaustive assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancel-silence and failure-surface presentation tests + affected-target gate</name>
  <files>AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift</files>
  <read_first>
    - AppPackage/Tests/SettingFeatureTests/LoginChallengeFlowTests.swift (the suite built in Task 1 — extend it)
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (the .destination(.dismiss) and .cancelChallenge handlers, .presentErrorInfo)
  </read_first>
  <behavior>
    - Test 7 (D-02 swipe-down): from a presented challenge with loginState .loading, send .destination(.dismiss) — loginState == .idle, destination nil; store.finish() proves NO further actions (no retry, no toast — exhaustivity is the proof of silence)
    - Test 8 (D-02 toolbar cancel): from a presented challenge, send .cancelChallenge — destination nil, loginState .idle, silent; a subsequent .login starts a fresh attempt with challengeRounds reset to 0
    - Test 9 (D-11 tap-through): with a toast set, send .presentErrorInfo(errorInfo) — destination == .errorInfo(errorInfo)
    - Test 10 (existing-path regression): the web-login flow is untouched — send .presentWebView(url) → destination == .webView(url); send .destination(.dismiss) with loginState .idle → no state change beyond the destination nil, no cancel side effects
  </behavior>
  <action>
    Extend the suite with the four behavior tests above, reusing the Task 1 factory. Test 7 is
    the load-bearing D-02 proof: the exhaustive store receiving zero actions after the dismissal
    IS the "no retry fires, no error toast presents" assertion. Test 10 pins the pre-existing
    webView destination semantics so the challenge handling cannot have widened the dismiss
    handler's blast radius.

    Then run the two affected targets in full as the plan gate: SettingFeatureTests and
    NetworkingFeatureTests.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests -only-testing:NetworkingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - Tests 7–10 exist and pass; Test 7 asserts silence via exhaustive TestStore completion (no receive after the dismissal send)
    - Full SettingFeatureTests and NetworkingFeatureTests targets exit 0
  </acceptance_criteria>
  <done>Cancel semantics, tap-through, and web-login regression coverage are green, and both affected targets pass in full.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → suite | Synthetic responses/credentials only; no live secrets in test code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-19 | Information Disclosure | Real credentials or clearance values in fixtures | medium | mitigate | Synthetic fixture values only (the Phase 8 08-11 precedent); no live cookie stores touched — LoginClient stubbing never reaches the network |
| T-12-20 | Tampering | Cross-test shared-state pollution via the in-memory key | low | mitigate | Each test seeds/asserts its own @Shared value; TCA TestStore isolates dependencies per store |
| T-12-21 | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests -only-testing:NetworkingFeatureTests` green
- All ten behavior tests present and passing with exhaustive TestStore semantics
</verification>

<success_criteria>
- Every automatable phase criterion (C2, C3, C5, D-02, D-06, D-09, D-10, D-11) has a passing reducer-level test
- The only remaining unproven link is live Cloudflare behavior — explicitly deferred to the 12-06 owner UAT (C1)
</success_criteria>

<output>
Create `.planning/phases/12-cloudflare-login-restoration/12-05-SUMMARY.md` when done
</output>
