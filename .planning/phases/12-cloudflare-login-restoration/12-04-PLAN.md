---
phase: 12-cloudflare-login-restoration
plan: 04
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift
  - AppPackage/Sources/SettingFeature/Login/LoginView.swift
autonomous: true
requirements: [C2, C3, C4, C5]
must_haves:
  truths:
    - "A challenged login POST immediately presents the challenge sheet; a non-challenged response proceeds through the existing loginDone path with no extra UI (D-01, C2)"
    - "The moment the pair is captured the sheet auto-dismisses and the login POST retries carrying clearance + exact UA, flowing into unchanged setCredentials/didLogin handling (C3, C4)"
    - "At most 2 challenge presentations per attempt; a third challenge fails through AppError.cloudflareChallengeFailed presented as a persistent tappable toast opening ErrorInfoView (D-09, D-10, D-11, C5)"
    - "Dismissing the sheet mid-challenge silently aborts: loginState returns to .idle, no retry, no toast (D-02)"
    - "loginState stays .loading across detect → solve → retry, so the existing chevron/spinner treatment spans the whole flow with no view change to the button (D-03)"
    - "A later login in the same session proactively attaches the held pair; a re-challenged retry replaces it (D-06)"
  artifacts:
    - path: "AppPackage/Sources/SettingFeature/Login/LoginReducer.swift"
      provides: "Challenge state machine: destination cases, bounded rounds, session holder, cancel semantics, toast"
      contains: "challengeDetected"
    - path: "AppPackage/Sources/SettingFeature/Login/LoginView.swift"
      provides: "Challenge sheet with cancellationAction toolbar + privacyMask, error toast, ErrorInfoView sheet"
      contains: "ChallengeWebView"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/Login/LoginReducer.swift"
      to: "AppPackage/Sources/SettingFeature/Login/LoginClient.swift"
      via: "@Dependency(\\.loginClient) driving both the initial and retried POST"
      pattern: "loginClient"
    - from: "AppPackage/Sources/SettingFeature/Login/LoginReducer.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request.swift"
      via: "isCloudflareChallenge classification after each fetch"
      pattern: "isCloudflareChallenge"
    - from: "AppPackage/Sources/SettingFeature/Login/LoginView.swift"
      to: "AppPackage/Sources/SettingFeature/Components/ChallengeWebView.swift"
      via: "challenge sheet content sending challengeClearanceCaptured"
      pattern: "challengeClearanceCaptured"
---

<objective>
Wire the complete challenge flow into the login feature: LoginReducer gains the challenge
destination, bounded-retry state machine, session-holder writes, cancel semantics, and the
exhausted-retries failure surface (D-01, D-02, D-06, D-09, D-10, D-11); LoginView gains the
challenge sheet (cancellationAction toolbar, privacy mask), the persistent error toast, and the
ErrorInfoView detail sheet — while the login button and the existing web-login sheet stay
untouched (D-03).

Purpose: this is the phase's core deliverable — after this plan the app detects, clears, and
replays through the wall end-to-end.
Output: edited LoginReducer.swift + LoginView.swift, building clean; state machine covered by
tests in 12-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-cloudflare-login-restoration/12-CONTEXT.md
@.planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md
@.planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md
@.planning/phases/12-cloudflare-login-restoration/12-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 12 (producing plan in parentheses):

- `AppError.cloudflareChallengeFailed`; xcstrings keys `app_error.cloudflare_challenge_failed`, `app_error.cloudflare_challenge_failed_description`, `app_error.cloudflare_challenge_solution`; `CloudflareClearance` (`cookieValue`, `userAgent`); `SharedKey.cloudflareClearance` (12-01)
- `isCloudflareChallenge(_:)`; `LoginRequest.init(username:password:clearance:urlSession:)`; `CloudflareChallengeDetectionTests` (12-02)
- `ChallengeWebView`; `LoginClient` + `LoginClientKey` + `DependencyValues.loginClient` (12-03)
- `LoginReducer.Destination.challenge(URL)`, `LoginReducer.Destination.errorInfo(ErrorInfo)`, `LoginReducer.Action.challengeDetected`, `LoginReducer.Action.challengeClearanceCaptured(CloudflareClearance)`, `LoginReducer.Action.cancelChallenge`, `LoginReducer.Action.presentErrorInfo(ErrorInfo)`, `LoginReducer.Action.toast(PresentationAction<Never>)`, `LoginReducer.State.challengeRounds`, `LoginReducer.State.toast`, `LoginReducer.State.cloudflareClearance` (12-04)
- `LoginChallengeFlowTests` test suite (12-05)

<tasks>

<task type="auto">
  <name>Task 1: LoginReducer challenge state machine (D-01, D-02, D-06, D-09, D-10, D-11)</name>
  <files>AppPackage/Sources/SettingFeature/Login/LoginReducer.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (the file being rewritten — Destination, CancelID.login, the .login effect, the .loginDone downstream that must stay unchanged per C4)
    - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingReducer.swift (the Phase 9 toast pattern: `@Presents var toast: AppAlertState<Never>?`, `case toast(PresentationAction<Never>)`, `.ifLet(\.$toast, action: \.toast)`)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (ErrorInfo construction with Context rows — the `.setToast(.error(errorInfo))` shape)
    - AppPackage/Sources/AppModels/Support/AppError+Context.swift (ContextKey whitelist + AnyHashableBox literal conformances)
    - .planning/phases/12-cloudflare-login-restoration/12-PATTERNS.md §"LoginReducer" (destination/haptics/ifLet composition and the child-reducer-shorthand lint notes)
  </read_first>
  <action>
    Extend LoginReducer (folding the flow in rather than a child feature — the surface is one
    destination, one counter, and four actions; a child reducer would add composition without
    isolation. The `Feature`-suffix rule applies only to new reducers, and none is created).

    Destination: add `@ReducerCaseIgnored case challenge(URL)` and `@ReducerCaseIgnored case
    errorInfo(ErrorInfo)` beside `.webView`; the file-closing `Destination.State/Action`
    Equatable/Sendable extensions must keep compiling.

    State: add `public var challengeRounds = 0` (challenge-surface presentations this attempt),
    `@Presents public var toast: AppAlertState<Never>?`, and `@Shared(.cloudflareClearance)
    public var cloudflareClearance: CloudflareClearance?` (the D-06 session holder; declare per
    the repo's @Shared-in-State idiom — see how SettingReducer declares `@Shared(.setting)`).

    Action: add `challengeDetected`, `challengeClearanceCaptured(CloudflareClearance)`,
    `cancelChallenge`, `presentErrorInfo(ErrorInfo)`, and `toast(PresentationAction<Never>)`.

    Dependencies: add `@Dependency(\.loginClient) private var loginClient`.

    Effect factoring: extract the fetch effect into one private helper used by both `.login` and
    `.challengeClearanceCaptured` — it captures username/password and the current
    `cloudflareClearance` pair, calls `loginClient.login(username, password, pair)` inside `do
    throws(AppError)`, then: if `isCloudflareChallenge(response)` send `.challengeDetected`,
    else send `.loginDone(.success(response))`; catch sends `.loginDone(.failure(error))`. Keep
    `.cancellable(id: CancelID.login)` on it. (A challenged 403 is a successful transport
    response — `fetch`'s 4-attempt loop returns it on attempt 1; challenge rounds are a distinct
    reducer-level concept, D-09.)

    Handlers:
    - `.login`: keep the existing guard, focus clear, `loginState = .loading`, and soft-feedback
      haptic merge; additionally reset `challengeRounds = 0` (the D-09 bound is per attempt) and
      swap the inline LoginRequest call for the shared fetch-effect helper — which proactively
      attaches the held session pair when one exists (D-06): an unexpired clearance skips the
      wall entirely; an expired one comes back challenged and re-runs the flow.
    - `.challengeDetected`: if `challengeRounds >= 2`, return
      `.send(.loginDone(.failure(.cloudflareChallengeFailed)))` (a third challenge fails the
      attempt, D-09). Otherwise increment `challengeRounds` and set `state.destination =
      .challenge(Defaults.URL.login)` — present immediately, no hidden pre-attempt (D-01);
      `loginState` stays `.loading` (D-03). `Defaults.URL.login` is the exact challenged
      endpoint (AppTools import needed).
    - `.challengeClearanceCaptured(let pair)`: guard `case .challenge = state.destination` else
      return .none (ignore stray captures). Write the pair into the shared holder via the
      projected `$cloudflareClearance` (replacing any prior pair, D-06), set `state.destination
      = nil` (the surface auto-dismisses the moment the pair lands — covers interactive and
      zero-interaction walls, C3), and return the shared fetch-effect helper (the retry now
      carries clearance + exact UA, C4). `loginState` remains `.loading` throughout (D-03).
    - `.cancelChallenge` (toolbar button): set `state.destination = nil`, `state.loginState =
      .idle`, and return `.cancel(id: CancelID.login)`. Silent — no toast, no retry (D-02).
    - `.destination(.dismiss)` (swipe-down): when the presented case is `.challenge` (inspect
      `state.destination` — still populated while handling the dismissal) and `loginState ==
      .loading`, apply the same silent-abort semantics: `loginState = .idle`, return
      `.cancel(id: CancelID.login)`. Every other destination action keeps returning `.none`
      (the web-login sheet's behavior is untouched).
    - `.loginDone`: keep the existing body verbatim — didLogin check, success/error notification
      haptics, dismiss-on-success, `setCredentials(response:)` (the unchanged C4 downstream).
      Add one branch: when the result is `.failure(.cloudflareChallengeFailed)`, set
      `state.loginState = .failed(.cloudflareChallengeFailed)` (instead of the generic
      `.failed(.unknown)`) and set `state.toast = .error(ErrorInfo(error:
      .cloudflareChallengeFailed, context: [.action: "Login", .statusCode: 403]))` — the
      persistent tappable failure toast (D-10/D-11; `AppAlertState.error(_:)` is the
      `autoHide: false` variant). The existing `.error` notification haptic in that branch is
      the D-11 "existing error notification haptic" — keep it. Never place the clearance value
      in ErrorInfo context, state, or logs.
    - `.presentErrorInfo(let errorInfo)`: set `state.destination = .errorInfo(errorInfo)`.
    - `.toast`: return `.none`.

    Composition: add a second `.haptics(unwrapping: \.destination, case: \.challenge,
    hapticsClient:)` beside the existing webView one (sheet-presentation haptic parity), and
    `.ifLet(\.$toast, action: \.toast)` after the destination `.ifLet`. No domain-fronting
    conditionals anywhere in this file (D-08 — the flow is purely response-driven).
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'generic/platform=iOS Simulator' && xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - LoginReducer.swift contains `case challenge(URL)`, `case errorInfo(ErrorInfo)`, `challengeRounds`, `@Shared(.cloudflareClearance)`, `@Presents public var toast`, `challengeDetected`, `challengeClearanceCaptured`, `cancelChallenge`, `presentErrorInfo`, and `isCloudflareChallenge`
    - The `.loginDone` didLogin/setCredentials/dismiss logic is textually unchanged apart from the added cloudflareChallengeFailed branch (C4)
    - `test "$(grep -v '^[[:space:]]*//' AppPackage/Sources/SettingFeature/Login/LoginReducer.swift | grep -c 'bypassSNIFiltering')" -eq 0` passes (D-08)
    - Existing SettingFeatureTests target stays green (no behavior change to sibling reducers)
  </acceptance_criteria>
  <done>The full detect → present → capture → retry → bounded-fail state machine compiles, existing login behavior paths are preserved, and the SettingFeatureTests target passes.</done>
</task>

<task type="auto">
  <name>Task 2: LoginView challenge sheet, error toast, ErrorInfoView sheet (D-01, D-02, D-03, D-11)</name>
  <files>AppPackage/Sources/SettingFeature/Login/LoginView.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Login/LoginView.swift (the existing webView sheet + privacyMask pattern to mirror; the login Button block that must NOT change)
    - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift (the Phase 5 cancellationAction convention: untitled `Button(role: .cancel)` in a `ToolbarItem(placement: .cancellationAction)` inside a NavigationStack)
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (the ErrorInfoView sheet + `.toast(_:onErrorTap:)` wiring to replicate at this owning view)
    - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift (the `.toast($store.scope(\.$toast, action: \.toast))` scoping shape inside SettingFeature)
  </read_first>
  <action>
    Three additions to LoginView; zero changes to the login Button block — `loginState ==
    .loading` spanning the whole flow makes the existing chevron-clearing + ProgressView overlay
    read as a spinner for the entire detect → solve → retry span (D-03; verify by leaving those
    lines untouched).

    1. Challenge sheet, directly after the existing webView sheet: `.sheet(item:
       $store.destination.challenge, id: \.absoluteString)` presenting a `NavigationStack`
       containing `ChallengeWebView(url:onClearance:)` where onClearance sends
       `.challengeClearanceCaptured($0)`. Inside the NavigationStack attach a `.toolbar` with
       `ToolbarItem(placement: .cancellationAction)` holding an untitled `Button(role: .cancel)`
       that sends `.cancelChallenge` (D-02 — the reducer, not a bare environment dismiss, owns
       the abort; swipe-down stays enabled and lands on the same silent-abort handling). Apply
       `.privacyMask()` to the sheet root exactly like the webView sheet (this is a new runtime
       presentation root — the Phase 7 inventory is reconciled in 12-06). Mirror the webView
       sheet's `.ignoresSafeArea(edges: .bottom)` treatment so the challenge page fills the
       sheet.
    2. Error toast at the view root: `.toast($store.scope(\.$toast, action: \.toast),
       onErrorTap: { store.send(.presentErrorInfo($0)) })` — the persistent tappable failure
       toast (D-11). LoginView gains no inline error text.
    3. ErrorInfo detail sheet: `.sheet(item: $store.destination.errorInfo)` presenting
       `ErrorInfoView(errorInfo:)` with `.privacyMask()` — the TabBarView shape, owned here per
       the Phase 9 per-site convention (ErrorInfo is Identifiable, so no explicit id key path is
       needed). SystemNotification and AppComponents imports: AppComponents is already imported;
       add the SystemNotification import if the toast modifier requires it (AccountSettingView
       shows the needed import set), keeping `sorted_imports` order.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'generic/platform=iOS Simulator'</automated>
  </verify>
  <acceptance_criteria>
    - LoginView.swift contains a `$store.destination.challenge` sheet whose content includes `ChallengeWebView`, a `ToolbarItem(placement: .cancellationAction)` with `Button(role: .cancel)`, and `.privacyMask()`
    - LoginView.swift contains `.toast(` with an `onErrorTap` closure sending `presentErrorInfo`, and a `$store.destination.errorInfo` sheet presenting `ErrorInfoView` with `.privacyMask()`
    - The login Button block (Label, overlay ProgressView, glassEffect) is byte-identical to before (D-03: git diff for LoginView.swift shows no hunk touching that block)
    - The existing webView sheet block is unchanged
    - `xcodebuild build` exits 0
  </acceptance_criteria>
  <done>The challenge sheet, toast, and ErrorInfo sheet render from reducer state, both new presentation roots carry the privacy mask, and the login button and web-login sheet are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reducer effects → Cloudflare edge | Untrusted responses classified per-response (no host trust) |
| Challenge sheet → App Switcher snapshot | New presentation roots must not leak content when backgrounded |
| Toast/ErrorInfoView → user | Diagnostics must not disclose the clearance value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-14 | Information Disclosure | Challenge + ErrorInfo sheets in App Switcher | high | mitigate | `.privacyMask()` on both new sheet roots (acceptance criteria assert both); inventory reconciliation in 12-06 |
| T-12-15 | Information Disclosure | cf_clearance in ErrorInfo context or logs | high | mitigate | Context whitelist rows only (`.action`, `.statusCode`); no clearance value in state dumps beyond the typed holder; Phase 8 logging gate re-run in 12-06 |
| T-12-16 | Denial of Service (self-inflicted) | Unbounded re-challenge loop | medium | mitigate | `challengeRounds` hard bound of 2 presentations per attempt, then typed failure (D-09/D-10) |
| T-12-17 | Elevation of Privilege | Challenge UI presented over stale attempt state | low | mitigate | Rounds reset on every `.login`; stray `challengeClearanceCaptured` ignored unless the challenge destination is presented |
| T-12-18 | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- `xcodebuild build` + `-only-testing:SettingFeatureTests` green
- Grep gates: reducer has no domain-fronting conditional (D-08); both new sheets carry `.privacyMask()`
- Diff inspection: login Button block and webView sheet untouched; `.loginDone` downstream unchanged apart from the added failure branch
</verification>

<success_criteria>
- Detect → present → capture → auto-dismiss → retry → unchanged credential handling compiles end-to-end (C2, C3, C4)
- Bounded 2-presentation state machine with silent cancel and persistent-toast failure surface (D-02, D-09, D-10, D-11, C5)
- Spinner spans the whole flow with zero login-button changes (D-03)
</success_criteria>

<output>
Create `.planning/phases/12-cloudflare-login-restoration/12-04-SUMMARY.md` when done
</output>
