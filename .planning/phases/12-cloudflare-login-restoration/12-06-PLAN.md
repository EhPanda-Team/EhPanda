---
phase: 12-cloudflare-login-restoration
plan: 06
type: execute
wave: 6
depends_on: [12-05]
files_modified:
  - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md
autonomous: false
requirements: [C1, C5]
must_haves:
  truths:
    - "The Phase 7 privacy-mask inventory accounts for both new presentation roots one-to-one, and its derived counts reconcile (RESEARCH Pitfall 5)"
    - "Static gates hold: no shared-jar write in the challenge wrapper, no domain-fronting conditional in the challenge flow, the cookie-logging gate passes, the clearance shared key is in-memory, and the full suite is green under SwiftLint-as-error"
    - "Username/password login succeeds end-to-end against the live Cloudflare-fronted forums host, owner-verified on device/simulator (C1 — the phase's authoritative gate)"
  artifacts:
    - path: ".planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md"
      provides: "Updated durable coverage contract: 41 runtime roots including the login challenge sheet and login error-info sheet"
      contains: "ChallengeWebView"
  key_links:
    - from: ".planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md"
      to: "AppPackage/Sources/SettingFeature/Login/LoginView.swift"
      via: "ROOT rows mapping each new sheet to its sole privacyMask application file:line"
      pattern: "LoginView.swift"
---

<objective>
Close the phase: reconcile the Phase 7 privacy-mask coverage contract with the two new
presentation roots, run the phase's static and full-suite gates, and put the live end-to-end
login (C1) — the one criterion no automated test can prove (RESEARCH Pitfall 2: the clearance is
also TLS-bound, so the owner-driven live pass is the authoritative gate) — in front of the owner
as a blocking verification checkpoint.

Purpose: C1 is the phase goal; everything before this plan is machinery. This plan proves it or
surfaces the TLS-binding fallback signal for gap closure.
Output: updated 07-PRIVACY-MASK-INVENTORY.md, a green phase gate, and an owner-signed live login
verification.

Note (visible skip, recorded): the spec-less probe fallback was skipped for this phase — no
requirement IDs exist to probe; the verification below derives from ROADMAP criteria C1–C5 and
CONTEXT decisions D-01…D-11.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-cloudflare-login-restoration/12-CONTEXT.md
@.planning/phases/12-cloudflare-login-restoration/12-RESEARCH.md
@.planning/phases/12-cloudflare-login-restoration/12-05-SUMMARY.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md
</context>

## Artifacts this phase produces

Symbols created across Phase 12 (producing plan in parentheses):

- `AppError.cloudflareChallengeFailed`; xcstrings keys `app_error.cloudflare_challenge_failed`, `app_error.cloudflare_challenge_failed_description`, `app_error.cloudflare_challenge_solution`; `CloudflareClearance` (`cookieValue`, `userAgent`); `SharedKey.cloudflareClearance` (12-01)
- `isCloudflareChallenge(_:)`; `LoginRequest.init(username:password:clearance:urlSession:)`; `CloudflareChallengeDetectionTests` (12-02)
- `ChallengeWebView`; `LoginClient` + `LoginClientKey` + `DependencyValues.loginClient` (12-03)
- `LoginReducer` additions: `Destination.challenge(URL)`, `Destination.errorInfo(ErrorInfo)`, `Action.challengeDetected`, `Action.challengeClearanceCaptured(CloudflareClearance)`, `Action.cancelChallenge`, `Action.presentErrorInfo(ErrorInfo)`, `Action.toast(PresentationAction<Never>)`, `State.challengeRounds`, `State.toast`, `State.cloudflareClearance` (12-04)
- `LoginChallengeFlowTests` test suite (12-05)
- Updated `07-PRIVACY-MASK-INVENTORY.md` (docs artifact, 12-06)

<tasks>

<task type="auto">
  <name>Task 1: Privacy-mask inventory reconciliation + phase static gates + full suite</name>
  <files>.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md (the whole durable contract: ROOT-row format, derived-count sentences, the source-presentation reconciliation section and its preview-only exclusions)
    - AppPackage/Sources/SettingFeature/Login/LoginView.swift (the two new sheet presentations and their privacyMask line numbers, as landed in 12-04)
  </read_first>
  <action>
    Inventory update (RESEARCH Pitfall 5): append two ROOT rows following the existing format —
    one for the login challenge sheet (presentation modifier and sole privacyMask application at
    their LoginView.swift file:line) and one for the login error-info sheet. Update every derived
    count the document states: 39 runtime roots → 41, 38 production modal roots → 40, and the
    source-presentation reconciliation total (41 presentation modifiers → 43, preview-only
    exclusions unchanged) — read the document's own derivation sentences and keep them internally
    consistent rather than pattern-replacing numbers.

    Then run the phase's static gates from the repo root and record each result in the SUMMARY:
    1. Shared-jar gate (D-04): `test "$(grep -v '^[[:space:]]*//' AppPackage/Sources/SettingFeature/Components/ChallengeWebView.swift | grep -c 'HTTPCookieStorage')" -eq 0`
    2. Response-driven gate (D-08): `test "$(grep -v '^[[:space:]]*//' AppPackage/Sources/SettingFeature/Login/LoginReducer.swift AppPackage/Sources/SettingFeature/Login/LoginClient.swift AppPackage/Sources/SettingFeature/Components/ChallengeWebView.swift AppPackage/Sources/NetworkingFeature/Request.swift | grep -c 'bypassSNIFiltering')" -eq 0`
    3. Cookie-logging gate (Phase 8, covers cf_clearance): `./scripts/check-cookie-logging.sh` exits 0
    4. No-persistence gate (C5): the `cloudflareClearance` key declaration in AppSharedKeys.swift uses `InMemoryKey` (source assertion), and `test "$(grep -rc 'cloudflareClearance' AppPackage/Sources --include='*.swift' -l | wc -l)" -gt 0` with none of the consuming declarations using appStorage or fileStorage for it (inspect the grep -rn output and record it)
    5. Full suite: `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'` — all targets green (the parallel post-Phase 11 suite), which also proves SwiftLint-as-error across sources and tests
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - 07-PRIVACY-MASK-INVENTORY.md contains two new ROOT rows referencing LoginView.swift (one naming ChallengeWebView's sheet, one the error-info sheet) and states 41 runtime roots
    - Gates 1–4 pass with the exact commands above; outputs recorded in the SUMMARY
    - Full-suite xcodebuild test exits 0
  </acceptance_criteria>
  <done>The durable privacy contract reconciles one-to-one with runtime roots, every static gate holds, and the whole suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner live end-to-end login through the real Cloudflare wall (C1)</name>
  <files>none — verification only</files>
  <action>
    Run the pre-check curl below, report its result, then present this checkpoint to the owner
    with the numbered steps from how-to-verify and pause for the resume signal. Do not proceed or
    mark the phase complete without an explicit owner response; on a failure report, record the
    exact step and symptom in the SUMMARY for gap closure (especially the step-5 repeated-challenge
    TLS-binding signal).
  </action>
  <what-built>
    The complete challenge flow: response-driven detection on the login POST (D-05), an
    immediately-presented in-app challenge sheet with silent cancel (D-01/D-02), in-memory
    (clearance, exact-UA) capture from a non-persistent web view store (D-04/D-06), a retried
    POST carrying explicit Cookie + User-Agent headers into unchanged setCredentials/didLogin
    handling (C4/D-07), bounded to 2 challenge rounds with a dedicated tappable failure toast →
    ErrorInfoView on exhaustion (D-09/D-10/D-11). All reducer/request behavior is already
    machine-verified; this checkpoint is the live-wall proof no automated test can give
    (the clearance is TLS-fingerprint-bound — RESEARCH Pitfall 2/Open Question 1).
  </what-built>
  <how-to-verify>
    Pre-check (Claude runs this and reports before you start): `curl -sI 'https://forums.e-hentai.org/index.php?act=Login' | grep -i 'HTTP\|cf-mitigated'` — expect 403 + cf-mitigated: challenge (re-confirming the 2026-07-20 evidence; if the wall is gone, the no-wall path is what you'll see and criterion C2 still applies).

    On a device or the iPhone Air simulator with real credentials, from a logged-out state:
    1. Cancel path first (keeps you logged out): Setting → Account → Login → enter username/password → tap the login button. When the challenge sheet appears, tap the Cancel toolbar button. Expect: sheet closes, spinner stops (chevron returns), NO error toast, app idle. Repeat once using swipe-down instead — same silent result.
    2. Full login: tap the login button again. Expect: the challenge sheet presents immediately; if the wall auto-passes it shows ~1–2s and closes itself; if interactive, complete it and the sheet closes the moment it passes. The spinner runs the whole time. Login then completes: the login screen pops and your account shows as signed in (criteria C1/C3/C4).
    3. Privacy mask: during a challenge presentation, swipe to the App Switcher. Expect: the challenge sheet content is blurred/masked.
    4. Session reuse (D-06, only if convenient): log out and log in again in the same app run — the wall should be skipped entirely (held clearance attached proactively) or, if expired, re-present and still succeed.
    5. Failure surface (only observed if Cloudflare rejects the URLSession retry — the TLS-binding risk): after two challenge rounds a persistent "Cloudflare Verification Failed" toast appears; tapping it opens the detail view whose suggested solution points at web login / manual cookie entry. If you hit this on every attempt, the TLS-binding fallback (replaying the POST through the web view) becomes gap-closure work — report it.
  </how-to-verify>
  <resume-signal>Type "approved" if live login succeeded end-to-end (steps 1–3), or describe exactly which step failed and what you saw (especially step 5's repeated-challenge signal)</resume-signal>
  <verify>
    <automated>curl -sI 'https://forums.e-hentai.org/index.php?act=Login' | grep -ci 'cf-mitigated'</automated>
    <human-check>Owner confirms steps 1–3 (silent cancel, live login success, App Switcher mask); steps 4–5 as applicable</human-check>
  </verify>
  <acceptance_criteria>
    - Pre-check result recorded (wall present: 403 + cf-mitigated; or wall absent: noted, C2 no-wall path applies)
    - Owner resume signal received and recorded verbatim in the SUMMARY
  </acceptance_criteria>
  <done>Owner has signed off the live C1 pass, or a precise failure report exists routing to gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → live Cloudflare edge | Real challenge/clearance exchange under production TLS |
| App Switcher snapshot → OS | New roots must render masked when backgrounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-22 | Information Disclosure | Unmasked new presentation roots | high | mitigate | Inventory reconciliation maps each new root to its sole privacyMask site; owner step 3 verifies the live snapshot |
| T-12-23 | Information Disclosure | Cookie values reaching logs | high | mitigate | check-cookie-logging.sh gate re-run against the final tree (covers cf_clearance per the Phase 8 taint rules) |
| T-12-24 | Information Disclosure | Clearance persisted anywhere | high | mitigate | No-persistence gate: in-memory key assertion + non-persistent web view store (12-03 gate) + fresh-launch behavior implicitly re-proven by the owner's logged-out start |
| T-12-25 | Denial of Service (self-inflicted) | UA/TLS mismatch causing permanent re-challenge | medium | mitigate | Bounded rounds + clean failure already machine-verified; owner step 5 is the live detector; fallback path pre-identified (POST via web view) for gap closure |
| T-12-26 | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Full suite green; gates 1–4 recorded
- Inventory internally consistent (derived counts match its own ROOT rows)
- Owner approval of the live C1 pass (or a precise failure report routing to gap closure)
</verification>

<success_criteria>
- All five ROADMAP Phase 12 criteria accounted for: C2–C5 by the 12-02/12-05 automated suites and this plan's gates, C1 by the owner-signed live pass
- Privacy-mask durable contract updated (39 → 41 roots) with no orphan or duplicate mask sites
</success_criteria>

<output>
Create `.planning/phases/12-cloudflare-login-restoration/12-06-SUMMARY.md` when done
</output>
