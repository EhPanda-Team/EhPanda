---
phase: 02-native-masonry-grid-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/GalleryListComponents/MasonryLayout.swift
  - AppPackage/Package.swift
  - AppPackage/Tests/GalleryListComponentsTests/MasonryLayoutTests.swift
  - AppPackage/Tests/GalleryListComponentsTests/.swiftlint.yml
  - AppPackage/Tests/FeatureTests.xctestplan
autonomous: true
requirements: [DEP-04]

must_haves:
  truths:
    - "MasonryLayout.columnCount(for:) returns max(2, floor((w+15)/(185+15))) for finite positive widths and clamps to 2 for degenerate widths — nil/0/negative/infinite/NaN (D-20, D-24, D-25, D-32)"
    - "MasonryLayout.cellWidth divides leftover space exactly as (w − 15·(N−1)) / N with fixed 15pt spacing and no rounding (D-21, D-28)"
    - "MasonryLayout.masonryPlan places items in data order into the leftmost shortest column with an exact CGFloat first-minimum scan and reports height max(0, tallest − 15) (D-26, D-27)"
    - "The pure functions are internal (not private, not public) so @testable import GalleryListComponents reaches them, while MasonryLayout itself stays module-internal, never public (D-35)"
    - "The GalleryListComponentsTests target exists and its Swift Testing suite passes via the AppPackage-Package scheme"
  artifacts:
    - path: "AppPackage/Sources/GalleryListComponents/MasonryLayout.swift"
      provides: "struct MasonryLayout: Layout + struct MasonryPlan + pure columnCount/cellWidth/masonryPlan + Cache"
      contains: "func columnCount"
    - path: "AppPackage/Tests/GalleryListComponentsTests/MasonryLayoutTests.swift"
      provides: "Swift Testing parity suite for the pure functions"
      contains: "@Suite"
    - path: "AppPackage/Tests/GalleryListComponentsTests/.swiftlint.yml"
      provides: "parent_config link for the new test target"
      contains: "parent_config"
    - path: "AppPackage/Package.swift"
      provides: "Module case galleryListComponentsTests + its .testTarget"
      contains: "galleryListComponentsTests"
  key_links:
    - from: "MasonryLayout.sizeThatFits / placeSubviews"
      to: "columnCount(for:) / cellWidth(containerWidth:columns:) / masonryPlan(...)"
      via: "thin Layout conformance delegates all arithmetic to the internal pure functions"
      pattern: "Self\\.columnCount|Self\\.masonryPlan"
    - from: "GalleryListComponentsTests target"
      to: "GalleryListComponents module"
      via: ".testTarget dependency + @testable import"
      pattern: "@testable import GalleryListComponents"
---

<objective>
Stand up the missing `GalleryListComponentsTests` test target and land the pure, unit-testable
core of the masonry layout — `MasonryLayout.columnCount(for:)`, `cellWidth(containerWidth:columns:)`,
and the `masonryPlan(...)` placement planner — plus a `MasonryLayout: Layout` thin conformance that
delegates to them. Lock the arithmetic with a Swift Testing suite.

Purpose: This is the safe, spike-independent foundation. The pure functions carry every parity
assertion (column-count adaptive rule, degenerate-width clamp, leftmost-shortest-column placement,
exact `cellWidth` division) and are correct regardless of the SR-1 spike outcome. The `Layout`
conformance exists so Plan 02's spike can wire a real `MasonryLayout` into the call site.

Output: `MasonryLayout.swift` (new), `MasonryLayoutTests.swift` (new), the new test target's
`.swiftlint.yml`, and the `Package.swift`/`FeatureTests.xctestplan` wiring for the target.

Implements DEP-04 pure-core acceptance (D-20, D-21, D-24, D-25, D-26, D-27, D-28, D-29, D-32, D-35).
</objective>

<assumption_delta_decision>
- **Primary noun:** column-derivation policy (how the grid's column count is derived).
- **Decision:** `no-change`.
- **Rationale:** This phase is a behavior-preserving replacement of the third-party `WaterfallGrid`
  with an app-owned `Layout`. The one genuine derivation change — device/screen-based column count
  (`DeviceUtil.isPadWidth` + `UIScreen.main`) → container-width-based `columnCount(for: proposal.width)`
  — is already explicitly owner-locked and scoped by D-20 and D-35 (kept as a private, documented
  masonry-layout policy so Phase 5 can ratify or replace it). No new identity/case is introduced; no
  singular→plural, required→optional, or derived→chosen transition occurs. The plan:pre detector
  keyword-fired on "alongside" (phase scheduling — "may run alongside Phase 1") and "custom" (app-owned
  vs third-party `Layout`); neither is an identity transition. No identity-model question is raised for
  the user.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-masonry-grid-swap/02-CONTEXT.md
@.planning/phases/02-native-masonry-grid-swap/02-RESEARCH.md
@.planning/phases/02-native-masonry-grid-swap/02-PATTERNS.md
@.planning/phases/02-native-masonry-grid-swap/02-VALIDATION.md

# Source-of-truth analogs
@AppPackage/Sources/AppComponents/TagCloudView.swift
@AppPackage/Tests/ImageColorsTests/ImageColorsParityTests.swift
@AppPackage/Tests/ImageColorsTests/.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MasonryLayout.swift with the pure core and thin Layout conformance</name>
  <files>AppPackage/Sources/GalleryListComponents/MasonryLayout.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/TagCloudView.swift — `FlowLayout: Layout` precedent (lines 32-92): the two required method signatures (`sizeThatFits`/`placeSubviews`), the `cache: inout` parameter, the degenerate-proposal `maxWidth.isFinite ? … : .greatestFiniteMagnitude` idiom (line 75), and the `for (index, subview) in subviews.enumerated()` + `subview.place(at:proposal:)` placement loop (lines 60-68).
    - AppPackage/.build/checkouts/WaterfallGrid/Sources/WaterfallGrid/WaterfallGrid.swift (lines 69-95) — the parity baseline: `alignmentsAndGridHeight` (leftmost-shortest via `heights.min()` + `firstIndex(of:)`, exact `CGFloat` ==) and `columnWidth` math. NOTE: the baseline force-unwraps `heights.min()!`/`heights.max()!` — the app forbids this (see acceptance).
    - .planning/phases/02-native-masonry-grid-swap/02-RESEARCH.md — §Architecture Patterns (Pattern 1 & 2) for the exact function shapes and the within-pass cache rule.
    - .swiftlint.yml (repo root) — `force_unwrapping`/`force_try` are error; `line_length`/`file_length` 120/1000 error.
  </read_first>
  <action>
    Create `struct MasonryLayout: Layout` in the `GalleryListComponents` module (import SwiftUI). Keep
    the type module-internal — never `public` (D-35: a private, documented masonry-layout policy, not an
    app-wide breakpoint system). Add a doc comment stating the constants are frozen only after the SR-1
    spike sign-off (D-23) and that N is derived solely from the proposed container width (D-20 — no
    `UIScreen`/`DeviceUtil`/size-class/idiom read).

    Constants (internal static): `spacing: CGFloat = 15` (D-21 fixed inter-item/inter-column spacing),
    `minCellWidth: CGFloat = 185` (D-20 `m`, a design knob per D-23), `minColumns = 2` (D-25 clamp; no
    max clamp).

    Pure functions — declare all three `internal static` (NOT private, NOT public) so the test target's
    `@testable import` reaches them (per 02-PATTERNS.md note; the pure-function seam of RESEARCH Pattern 2):
      - `func columnCount(for width: CGFloat) -> Int`: guard `width.isFinite && width > 0` else return
        `minColumns` (D-32); otherwise `max(minColumns, Int((width + spacing) / (minCellWidth + spacing)))`.
        `Int(_:)` truncates toward zero == floor for positive input. No hysteresis (D-24).
      - `func cellWidth(containerWidth w: CGFloat, columns n: Int) -> CGFloat`: return
        `(w - spacing * CGFloat(n - 1)) / CGFloat(n)` — exact division, no rounding (D-21, D-28).
      - `func masonryPlan(heights: [CGFloat], columns n: Int, cellWidth: CGFloat, spacing: CGFloat) -> MasonryPlan`:
        maintain `var columnHeights = Array(repeating: CGFloat.zero, count: n)`; for each height in data
        order, pick the target column with a strict first-minimum scan (`for i in 1..<n where columnHeights[i] < columnHeights[c] { c = i }`
        starting `c = 0`) so exact `CGFloat` ties keep the leftmost column (D-26 — do NOT use `<=` or a
        tolerance compare); append origin `CGPoint(x: CGFloat(c) * (cellWidth + spacing), y: columnHeights[c])`;
        then `columnHeights[c] += height + spacing`. Report total size height
        `max(0, (columnHeights.max() ?? spacing) - spacing)` — use `?? spacing`, NOT `!` (D-27; force_unwrapping
        is an error), and width `CGFloat(n) * cellWidth + CGFloat(n - 1) * spacing`.

    Add `struct MasonryPlan: Equatable { var origins: [CGPoint]; var size: CGSize }`.

    `Layout` conformance (thin — delegates to the pure functions):
      - `struct Cache { var proposalWidth: CGFloat?; var plan: MasonryPlan? }` + `makeCache(subviews:) -> Cache`.
        Use the cache as a within-pass memo only — never a cross-pass height store (D-29; `updateCache` is
        not called on pure subview-size changes, so cached heights would go stale on image load).
      - `sizeThatFits(proposal:subviews:cache:)`: guard finite positive `proposal.width` else return a safe
        zero-height size (D-32); compute `n = Self.columnCount(for: w)`, `cellW = Self.cellWidth(containerWidth: w, columns: n)`;
        measure `heights = subviews.map { $0.sizeThatFits(.init(width: cellW, height: nil)).height }` AFTER n
        and cellW are fixed (D-29 — measurement never feeds back into n); build the plan, store `(w, plan)` in
        the cache, return `CGSize(width: w, height: plan.size.height)`.
      - `placeSubviews(in:proposal:subviews:cache:)`: reuse `cache.plan` iff `cache.proposalWidth == w`, else
        recompute; place each subview at `bounds.origin + plan origin` with proposal `.init(width: cellW, height: nil)`.
        Prefer `zip(subviews, plan.origins)` over index subscripting for forward-compatibility with Phase 11's
        `unchecked_subscript_index_access` rule (currently disabled). Add no outer padding (D-21).

    The Layout type is NOT wired into any call site in this plan — it is exercised only by the tests
    (Task 2) and wired live by Plan 02's spike.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/GalleryListComponents/MasonryLayout.swift` exists and declares `struct MasonryLayout: Layout`.
    - The file contains `func columnCount`, `func cellWidth`, `func masonryPlan`, and `struct MasonryPlan`.
    - `grep -c '!' ` on the parity math shows no force-unwrap of `.min()`/`.max()`: the file contains `?? spacing` and neither `.max()!` nor `.min()!` nor `try!`.
    - `MasonryLayout` is not declared `public` (module-internal, D-35); the three pure functions are `internal static` (default access).
    - `xcodebuild build -scheme AppPackage-Package` exits 0 (compiles clean in GalleryListComponents, no SwiftLint error).
  </acceptance_criteria>
  <done>MasonryLayout.swift compiles as a module-internal Layout with internal pure functions and no force-unwrap; the package builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the GalleryListComponentsTests target and its parity suite</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/GalleryListComponentsTests/.swiftlint.yml, AppPackage/Tests/GalleryListComponentsTests/MasonryLayoutTests.swift, AppPackage/Tests/FeatureTests.xctestplan</files>
  <read_first>
    - AppPackage/Package.swift — the `Module` enum test-target block (lines 113-125, add a case alongside `tagTranslationFeatureTests`) and the `.testTarget(module: .imageColorsTests, dependencies: [.module(.imageColors)], plugins: swiftLintPlugins)` entry (lines 961-967, the closest analog) and the final `tagTranslationFeatureTests` test target (lines 977-984, append after it inside the array closing at line 985).
    - AppPackage/Tests/ImageColorsTests/.swiftlint.yml — the exact one-line `parent_config: ../../../.swiftlint.yml` to copy verbatim.
    - AppPackage/Tests/ImageColorsTests/ImageColorsParityTests.swift (lines 1-30) — the `import Testing` + `@Suite struct` + `@Test`/`#expect` idiom.
    - AppPackage/Tests/FeatureTests.xctestplan — the `testTargets` array; every existing test target is listed (add `GalleryListComponentsTests` to keep the app-scheme plan complete, matching convention).
    - .planning/phases/02-native-masonry-grid-swap/02-RESEARCH.md — §Code Examples has the concrete Swift Testing table and expected values (335→2, 408→2, 710→3, 790→4, 990→5, 1040→5, 1140→5, 1336→6, 320→2) and the placement/degenerate cases.
  </read_first>
  <action>
    Wire the new test target in `AppPackage/Package.swift`:
      - Add `case galleryListComponentsTests = "GalleryListComponentsTests"` to the `Module` enum's
        "Test targets" group.
      - Add a `.testTarget(module: .galleryListComponentsTests, dependencies: [.module(.galleryListComponents)], plugins: swiftLintPlugins)`
        entry inside the test-targets array (append after the `tagTranslationFeatureTests` entry; add the
        preceding comma).

    Create `AppPackage/Tests/GalleryListComponentsTests/.swiftlint.yml` containing exactly
    `parent_config: ../../../.swiftlint.yml` (project rule: every new module/test target carries a
    parent_config pointing three levels up to the package-root config).

    Add `GalleryListComponentsTests` to `AppPackage/Tests/FeatureTests.xctestplan`'s `testTargets` array
    (same object shape as the other entries: `containerPath` `container:AppPackage`, `identifier` and
    `name` `GalleryListComponentsTests`) so the app-scheme plan stays complete alongside every other target.

    Create `AppPackage/Tests/GalleryListComponentsTests/MasonryLayoutTests.swift`:
    `import Testing` + `import Foundation` (+ `import CoreGraphics` for `CGFloat`/`CGPoint`) +
    `@testable import GalleryListComponents` (the pure functions are internal, so `@testable` is required,
    unlike the `ImageColors` analog's plain public import). Author these Swift Testing cases:
      - A `@Test(arguments: [...] as [(CGFloat, Int)])` table over the sign-off widths asserting
        `MasonryLayout.columnCount(for: width) == expected` for the m=185, s=15, min-2 formula:
        335→2, 408→2, 710→3, 790→4, 990→5, 1040→5, 1140→5, 1336→6, 320→2. These assert the pure
        formula's actual output — e.g. `columnCount(for: 990) == 5` because `floor((990+15)/200) = 5`.
        The widths are illustrative content-width estimates; Plan 02's spike measures real `proposal.width`
        and any adjustment is a one-constant change to `m` (D-23). NOTE: the owner's CONTEXT "Expected Column
        Counts" note labels ~990pt "13-inch iPad portrait → 4"; whether the real measured 13" portrait content
        width yields 4 or 5 columns (and any `m` adjustment) is a Wave-2 spike sign-off item (D-23), NOT a
        Wave-1 assertion — the Wave-1 test asserts the arithmetic truth (990 → 5).
      - `degenerateWidthsClampToMin`: `columnCount(for:)` returns 2 for `0`, a negative width, `.infinity`,
        and `.nan` (D-32).
      - `cellWidthExactDivision`: `cellWidth(containerWidth:columns:)` returns the exact `(w − 15·(N−1))/N`
        with no rounding for a representative width (D-21, D-28).
      - `placementIsLeftmostShortestColumn`: for `masonryPlan(heights: [100, 100, 100], columns: 2, cellWidth: 160, spacing: 15)`
        assert origins x == `[0, 175, 0]`, origins y == `[0, 0, 115]`, and `plan.size.height == 215`
        (D-26 leftmost tie + D-27 `max(0, tallest − 15)` height).
    Keep every line ≤ 120 chars; no `!`/`try!`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:GalleryListComponentsTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'galleryListComponentsTests' AppPackage/Package.swift` >= 2 (Module case + .testTarget).
    - `AppPackage/Tests/GalleryListComponentsTests/.swiftlint.yml` contains `parent_config: ../../../.swiftlint.yml`.
    - `MasonryLayoutTests.swift` contains `@Suite` and `@testable import GalleryListComponents`.
    - `grep -c 'GalleryListComponentsTests' AppPackage/Tests/FeatureTests.xctestplan` >= 1.
    - `xcodebuild test … -only-testing:GalleryListComponentsTests` exits 0 (all pure-function cases pass).
  </acceptance_criteria>
  <done>The GalleryListComponentsTests target exists, is registered in the package + app-scheme plan, carries its parent_config, and its parity suite is green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols/files introduced this phase (downstream drift verification must EXCLUDE these as expected new work):
- `struct MasonryLayout: Layout` — module-internal (GalleryListComponents)
- `struct MasonryPlan: Equatable` (origins/size)
- `MasonryLayout.columnCount(for:)`, `MasonryLayout.cellWidth(containerWidth:columns:)`, `MasonryLayout.masonryPlan(heights:columns:cellWidth:spacing:)`, `MasonryLayout.Cache`
- Files: `AppPackage/Sources/GalleryListComponents/MasonryLayout.swift`, `AppPackage/Tests/GalleryListComponentsTests/MasonryLayoutTests.swift`, `AppPackage/Tests/GalleryListComponentsTests/.swiftlint.yml`
- `Package.swift` `Module` case `galleryListComponentsTests` + its `.testTarget`; `FeatureTests.xctestplan` entry `GalleryListComponentsTests`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI layout engine → MasonryLayout | The layout engine hands `proposal` widths (including degenerate `nil`/0/∞/NaN probes) to `sizeThatFits`. No external/untrusted input crosses here — widths originate from the OS layout pass over already-fetched, already-trusted gallery data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | `MasonryLayout.columnCount(for:)` / `sizeThatFits` on degenerate proposal width | low | mitigate | D-32 defensive handling: clamp N to `minColumns` and return a safe zero-height size for non-finite/non-positive widths. This is a robustness control, not a security boundary. Covered by `degenerateWidthsClampToMin`. |
| T-02-SC | Tampering | package installs (npm/pip/cargo/SwiftPM) | n/a | accept | This phase installs NO packages; it only adds app-owned Swift. No new supply-chain surface, no `[ASSUMED]`/`[SUS]` packages. |

**No new threats above the WaterfallGrid baseline.** This phase re-implements a client-side rendering
layout over already-fetched gallery data at ASVS L1. It adds no network calls, no user text input, no
persistence, no auth, and no secrets; the attack surface is unchanged. The only defensive control (D-32)
is robustness, not security.
</threat_model>

<verification>
- `xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'` builds clean (SwiftLint plugin passes: no force-unwrap, ≤120-char lines).
- `xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:GalleryListComponentsTests` is green.
- Run exactly ONE `xcodebuild test` invocation at a time (project memory: overlapping invocations wedge testmanagerd).
</verification>

<success_criteria>
- MasonryLayout.swift exists with a module-internal `Layout` conformance delegating to internal pure functions; no force-unwrap.
- The GalleryListComponentsTests target builds and its Swift Testing suite passes (column-count table, degenerate clamp, exact cellWidth, leftmost-shortest placement).
- No call site references MasonryLayout yet (wired by Plan 02); the phase's existing behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-native-masonry-grid-swap/02-01-SUMMARY.md` when done.
</output>
