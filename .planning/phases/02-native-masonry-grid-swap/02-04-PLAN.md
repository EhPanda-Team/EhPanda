---
phase: 02-native-masonry-grid-swap
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Package.resolved
autonomous: false
requirements: [DEP-04]

must_haves:
  truths:
    - "WaterfallGrid is absent from AppPackage/Package.swift (package declaration, product alias, app-target dependency, galleryListComponents-target dependency) and from AppPackage/Package.resolved (SR-4)"
    - "The full package builds and the whole test suite is green after the dependency is removed"
    - "The AboutView WaterfallGrid acknowledgement disposition is an explicit owner decision — kept per Phase 1 precedent unless the owner sweeps — not a silent drop"
  artifacts:
    - path: "AppPackage/Package.swift"
      provides: "manifest with all four WaterfallGrid lines removed and trailing commas repaired"
    - path: "AppPackage/Package.resolved"
      provides: "regenerated pin set with the waterfallgrid entry dropped"
  key_links:
    - from: "AppPackage/Package.swift"
      to: "AppPackage/Package.resolved"
      via: "swift package resolve regenerates the pin set after the dependency is removed"
      pattern: "waterfallgrid"
---

<objective>
Remove the WaterfallGrid dependency from the project now that no source references it (Plan 03 dropped
the import and swapped the call site). Delete the four manifest lines, regenerate `Package.resolved`, and
prove zero residual references. Then make an explicit owner decision on the `AboutView` acknowledgement
row rather than silently dropping it.

Purpose: This completes DEP-04 Success Criterion 4 (the library is removed from the dependency set) and
closes RESEARCH Open Question Q1 (the acknowledgement row) with an owner decision instead of a silent drop.

Output: a `Package.swift`/`Package.resolved` with no WaterfallGrid, a green full suite, and a recorded
acknowledgement-row decision.

Implements DEP-04 removal (SR-4) — no `UIScreen`/`DeviceUtil` reads were introduced (D-20), and the
library's deprecated `UIScreen.main` orientation read dies with it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-masonry-grid-swap/02-RESEARCH.md
@.planning/phases/02-native-masonry-grid-swap/02-PATTERNS.md

# Files touched
@AppPackage/Package.swift
@AppPackage/Sources/SettingFeature/Components/AboutView.swift
</context>

<!-- planner-discipline-allow: WaterfallGrid -->
<!-- planner-discipline-allow: waterfallgrid -->

<tasks>

<task type="auto">
  <name>Task 1: Remove the WaterfallGrid dependency from Package.swift and regenerate Package.resolved</name>
  <files>AppPackage/Package.swift, AppPackage/Package.resolved</files>
  <read_first>
    - AppPackage/Package.swift — the four lines to remove: the `.package(url: ".../WaterfallGrid", from: "1.0.0")` declaration (line 23), the `static let waterfallGrid` product alias (line 49), the `.targetDependency(.waterfallGrid)` in the app/AppFeature target (line 301, currently the last entry — repair the trailing comma on line 300), and the `.targetDependency(.waterfallGrid)` in the `galleryListComponents` target (line 482, currently the last entry — repair the comma on line 481).
    - AppPackage/Package.resolved — the `waterfallgrid` pin (lines 266-272) that must drop after `swift package resolve`.
    - .planning/phases/02-native-masonry-grid-swap/02-PATTERNS.md — the Package.swift edit section (exact lines + comma-repair notes).
    - Project memory: bare `swift build`/`swift test` fails for this package, but `swift package resolve` is the supported way to regenerate `Package.resolved`; final validation is via `xcodebuild`.
  </read_first>
  <action>
    Remove all four WaterfallGrid manifest lines named above. <!-- planner-discipline-allow: WaterfallGrid -->
    After deleting each `.targetDependency` line that was the last element of its `dependencies` array,
    remove the now-trailing comma on the preceding line so the array literal stays valid.

    Regenerate the resolved pin set by running `swift package resolve` against the AppPackage manifest
    (use the package path — e.g. `swift package resolve --package-path AppPackage` — since the shell cwd
    resets between commands); confirm the `waterfallgrid` entry is gone from `AppPackage/Package.resolved`.
    Then build + run the full suite via `xcodebuild` to prove the removal is clean.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WaterfallGrid' AppPackage/Package.swift` returns 0.
    - `grep -ci 'waterfallgrid' AppPackage/Package.resolved` returns 0.
    - `grep -rc 'import WaterfallGrid' AppPackage/Sources/` returns 0 (no residual source import).
    - `xcodebuild test -scheme AppPackage-Package` (full suite, including GalleryListComponentsTests) exits 0.
  </acceptance_criteria>
  <done>WaterfallGrid is absent from Package.swift and Package.resolved, no source imports it, and the full suite builds green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: AboutView acknowledgement-row disposition (owner decision)</name>
  <action>Manual decision only — no automated edit unless remove-ack is chosen. Present keep vs remove for the AboutView acknowledgement row; default is keep, matching Phase 1 precedent (which kept removed-dep acknowledgement rows). If remove-ack is chosen, delete the AboutView `.init(...)` entry and prune the two Constant.xcstrings keys across all locales, then re-run the full suite.</action>
  <decision>Keep or remove the WaterfallGrid acknowledgement row in AboutView.</decision>
  <context>
    `AboutView.swift` (lines 165-168) renders a WaterfallGrid acknowledgement via
    `.Constant.acknowledgementWaterfallGrid` + `…Link`. Phase 1 set a precedent: it removed
    SwiftCommonMark and UIImageColors as dependencies but KEPT their acknowledgement rows (AboutView still
    lists SwiftCommonMark, UIImageColors, and SwiftUIPager). This is a 1-line localized-string courtesy
    decision, surfaced explicitly rather than dropped silently (RESEARCH Open Question Q1).
  </context>
  <options>
    <option id="keep-ack">
      <name>Keep the acknowledgement row (recommended)</name>
      <pros>Matches Phase 1 precedent; preserves MIT attribution courtesy; zero code change (AboutView.swift and Constant.xcstrings untouched).</pros>
      <cons>AboutView lists a library no longer bundled (consistent with the other removed-dep rows).</cons>
    </option>
    <option id="remove-ack">
      <name>Remove the acknowledgement row (owner sweep)</name>
      <pros>AboutView reflects only currently-bundled dependencies.</pros>
      <cons>Diverges from Phase 1 precedent; requires editing AboutView.swift (delete the `.init(...)` entry) and pruning the `acknowledgementWaterfallGrid`/`acknowledgementWaterfallGridLink` keys from Constant.xcstrings across every locale. If chosen, add AboutView.swift + the xcstrings file to this task's edits and rebuild the full suite.</cons>
    </option>
  </options>
  <resume-signal>Select: keep-ack (no change), or remove-ack (then delete the AboutView entry + the two Constant.xcstrings keys across all locales and re-run the full suite).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
See 02-01-PLAN.md for the full new-symbol list. This plan removes the WaterfallGrid dependency (no new
symbols) and records the acknowledgement-row decision. If remove-ack is chosen, the
`acknowledgementWaterfallGrid`/`…Link` Constant keys and the AboutView row are deleted (an expected removal,
not drift).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supply chain → build | The manifest declares third-party package dependencies. This plan REDUCES that boundary by removing WaterfallGrid; no new package crosses it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | package dependency set (SwiftPM) | low | mitigate | This plan removes a dependency and regenerates `Package.resolved`; it installs nothing. Net reduction of supply-chain surface. No `[ASSUMED]`/`[SUS]` packages introduced. |

**No new threats above the WaterfallGrid baseline** — the change is a net removal from the dependency set
at ASVS L1; no network, input, persistence, auth, or secret surface is added.
</threat_model>

<verification>
- `grep -c 'WaterfallGrid' AppPackage/Package.swift` == 0 and `grep -ci 'waterfallgrid' AppPackage/Package.resolved` == 0 (SR-4).
- `grep -rc 'import WaterfallGrid' AppPackage/Sources/` == 0.
- Full suite green: `xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'`.
- AboutView acknowledgement disposition recorded as an explicit owner decision.
- Run exactly ONE `xcodebuild test` at a time.
</verification>

<success_criteria>
- WaterfallGrid is removed from Package.swift and Package.resolved and no source imports it (DEP-04 SR-4).
- The full test suite builds green after removal.
- The AboutView acknowledgement row disposition is an explicit owner decision, not a silent drop.
</success_criteria>

<output>
Create `.planning/phases/02-native-masonry-grid-swap/02-04-SUMMARY.md` when done — record the SR-4 grep
results and the acknowledgement-row decision.
</output>
