---
phase: 02-native-masonry-grid-swap
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - AppPackage/Sources/GalleryListComponents/MasonryLayout.swift
  - AppPackage/Sources/GalleryListComponents/GenericList.swift
autonomous: true
requirements: [DEP-04]

must_haves:
  truths:
    - "The .thumbnail branch renders MasonryLayout { ForEach(galleries) … }.animation(nil, value: galleries) as one eager row inside the existing List(.plain), preserving the notice Section, the .refreshable, and — per D-36 (amends D-30) — the auto-load footer WITHIN the masonry row (a VStack of the grid + FetchMoreFooter) plus the onScrollPhaseChange/onScrollGeometryChange auto-pagination guards; the temporary GenericList OSLogExt trigger-diagnostic (import + logger + logger.debug) is removed like the MasonryLayout width log (D-30, D-31, D-21, D-36)"
    - "columnsInPortrait and columnsInLandscape are deleted from GenericList.swift and no view reads UIScreen/DeviceUtil to derive the column count (D-20, D-34)"
    - "DeviceUtil.isPadWidth itself and its five other consumers are NOT modified (D-34 scope fence — that is Phase 5)"
    - "GenericList.swift no longer references WaterfallGrid, the temporary spike logging is removed from MasonryLayout.swift, and the full test suite is green"
  artifacts:
    - path: "AppPackage/Sources/GalleryListComponents/MasonryLayout.swift"
      provides: "finalized synchronous Layout at the frozen m, no temporary logging, doc-commented policy (D-33, D-35)"
    - path: "AppPackage/Sources/GalleryListComponents/GenericList.swift"
      provides: "production .thumbnail swap; import + legacy column reads removed"
  key_links:
    - from: "GenericList .thumbnail branch"
      to: "MasonryLayout"
      via: "production call-site swap (no WaterfallGrid reference remains in the file)"
      pattern: "MasonryLayout \\{"
---

<objective>
Commit the production swap now that the SR-1 spike has passed and `m` is frozen. Remove the temporary
spike instrumentation, finalize `MasonryLayout` as the clean synchronous `Layout`, and complete the
`GenericList.swift` swap: drop `import WaterfallGrid`, delete the legacy `columnsInPortrait`/`columnsInLandscape`
reads (D-34), and keep the grid as one eager row inside the existing `List(.plain)` with animation
suppression, preserving the notice `Section`, fetch-more footer, and `.refreshable` (D-30/D-31/D-21).

Purpose: This is the behavior-preserving replacement itself. The synchronous `Layout` also sheds
WaterfallGrid's first-layout opacity flash and async placement hop (D-33, a strictly-beneficial deviation).

Output: finalized `MasonryLayout.swift` and a `GenericList.swift` with no WaterfallGrid reference. The
WaterfallGrid *dependency* is still declared in `Package.swift` at this point — it is removed in Plan 04
after this plan proves the swap builds and the suite is green.

Implements DEP-04 call-site swap (D-20, D-21, D-30, D-31, D-33, D-34, D-35).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-masonry-grid-swap/02-CONTEXT.md
@.planning/phases/02-native-masonry-grid-swap/02-RESEARCH.md
@.planning/phases/02-native-masonry-grid-swap/02-PATTERNS.md

# Files touched
@AppPackage/Sources/GalleryListComponents/GenericList.swift
@AppPackage/Sources/GalleryListComponents/MasonryLayout.swift
@AppPackage/Sources/AppTools/DeviceUtil.swift
</context>

<!-- planner-discipline-allow: WaterfallGrid -->

<tasks>

<task type="auto">
  <name>Task 1: Finalize MasonryLayout.swift — remove spike logging, lock at frozen m, document the policy</name>
  <files>AppPackage/Sources/GalleryListComponents/MasonryLayout.swift</files>
  <read_first>
    - AppPackage/Sources/GalleryListComponents/MasonryLayout.swift — the file including the temporary `logger`/`proposal.width` instrumentation added in Plan 02.
    - .planning/phases/02-native-masonry-grid-swap/02-02-SUMMARY.md — the frozen `m` value and whether the columnCount test table needs re-baselining to measured widths.
    - .planning/phases/02-native-masonry-grid-swap/02-RESEARCH.md — §Architecture Patterns (the final synchronous shape), Anti-Patterns (no cross-pass height cache, no `<=` tie, no rounding, no padding).
  </read_first>
  <action>
    Remove the temporary `os.Logger` instance and the `proposal.width` log line from `sizeThatFits`.
    Set `minCellWidth` to the value frozen in the Plan 02 sign-off (default 185 unless the owner chose
    adjust-m — D-23). If `m` changed, update the `columnCount(for:)` expected values in
    `AppPackage/Tests/GalleryListComponentsTests/MasonryLayoutTests.swift` to the measured widths so the
    suite still asserts the real bands (this is the only case where the test file is touched here).

    Confirm the finalized layout is the clean synchronous form: `sizeThatFits`/`placeSubviews` delegate
    to the internal pure functions; the `Cache` is a within-pass memo only (D-29); no `UIScreen`/`DeviceUtil`/
    size-class read (D-20); no outer padding (D-21). Ensure doc comments state the design intent that is
    otherwise non-obvious: the constants are a private, documented masonry-layout policy (D-35, liftable
    unchanged by Phase 6), and the synchronous pass deliberately removes WaterfallGrid's opacity flash and
    async hop (D-33). Keep the type module-internal (never `public`) and the pure functions `internal`.
    No `!`/`try!`; lines ≤ 120.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:GalleryListComponentsTests</automated>
  </verify>
  <acceptance_criteria>
    - `MasonryLayout.swift` `sizeThatFits` no longer contains a `logger`/`proposal.width` log line (grep for `Logger(` in the file returns 0).
    - `minCellWidth` equals the frozen value from 02-02-SUMMARY (default 185).
    - The file contains a doc comment referencing the private policy (D-35) and the synchronous no-flash rationale (D-33).
    - No `UIScreen`/`DeviceUtil` token appears in `MasonryLayout.swift`.
    - `xcodebuild test … -only-testing:GalleryListComponentsTests` exits 0.
  </acceptance_criteria>
  <done>MasonryLayout is the finalized synchronous Layout with no instrumentation, frozen constants, documented policy, and a green pure-function suite.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the GenericList.swift production swap and delete the legacy column reads</name>
  <files>AppPackage/Sources/GalleryListComponents/GenericList.swift</files>
  <read_first>
    - AppPackage/Sources/GalleryListComponents/GenericList.swift — line 6 import; `WaterfallList.columnsInPortrait`/`columnsInLandscape` (lines 160-165) to delete; the candidate call site wired in Plan 02; the `List { … }.listStyle(.plain)` + notice `Section` (196-200) + fetch-more footer (218-234) to preserve.
    - AppPackage/Sources/AppTools/DeviceUtil.swift — confirm `isPadWidth` (lines 13-15) is NOT modified this phase (D-34 scope fence; five other consumers stay untouched).
    - .planning/phases/02-native-masonry-grid-swap/02-PATTERNS.md — the GenericList edit section (import to drop, column reads to delete, structure to preserve).
  </read_first>
  <action>
    Finalize the swap in `GenericList.swift`:
      - Delete the module import on line 6 so no reference to the removed library remains (Plan 04 removes
        the package dependency next). <!-- planner-discipline-allow: WaterfallGrid -->
      - Delete the `columnsInPortrait` and `columnsInLandscape` computed vars in `WaterfallList`
        (lines 160-165) — the two legacy reads die with the component (D-34). Do NOT touch
        `DeviceUtil.isPadWidth` itself or its five other consumers (ControlPanel, CategoryView,
        ArchivesView, PreviewsView, `Defaults+Runtime`) — that is Phase 5 / UIARCH-01 scope.
      - Confirm the `.thumbnail` row is the production form:
        `MasonryLayout { ForEach(galleries) { gallery in <Button/GalleryThumbnailCell/borderless block> } }.animation(nil, value: galleries)`
        (D-31). Preserve the inner `Button { navigateAction?(gallery) } label: { GalleryThumbnailCell(...) .tint(.primary).multilineTextAlignment(.leading) }.buttonStyle(.borderless)` verbatim.
      - **D-36 (amends D-30): the manual chevron footer is GONE.** Preserve the auto-load pagination
        implemented in the spike (commit `08e7f7ef`) exactly as-is: the `FetchMoreFooter` lives INSIDE the
        masonry row (`VStack(spacing: 0) { MasonryLayout {…}.animation(nil, value: galleries); if let pageNumber, pageNumber.hasNextPage() { FetchMoreFooter(...) } }`),
        the `onScrollPhaseChange`/`onScrollGeometryChange` trigger + the `isUserScrolling`/`lastAutoFetchCount`
        `@State` guards + the `fetchMoreThreshold` constant all stay. Do NOT reinstate the chevron `Button`
        and do NOT move the footer back out to a sibling `List` row (that re-introduces the bottom-pinning
        fetch loop — see D-36). Keep the enclosing `List { … }.listStyle(.plain)`, the notice `Section`,
        and the ancestor `.refreshable`/`.animation(.default, value: galleries)` unchanged (D-30). Add NO
        `.listRowInsets`/outer padding — outer padding stays the List row insets (D-21).
      - Remove the temporary spike diagnostics that live in `GenericList.swift` (added in Plan 02 alongside
        the wiring): delete `import OSLogExt`, the file-top `private let logger = Logger(...)`, and the
        `logger.debug("fetchMore auto-trigger: …")` line in the `onScrollGeometryChange` action — this
        mirrors removing MasonryLayout's `proposal.width` log. Keep the auto-load trigger logic itself.
    The `WaterfallList` struct name may stay as-is (internal; renaming is out of scope — the type is
    replaced in Phase 6's decomposition). Optionally add a brief doc comment noting the grid is now the
    app-owned MasonryLayout.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WaterfallGrid' AppPackage/Sources/GalleryListComponents/GenericList.swift` returns 0 (no import, no call).
    - `grep -c 'columnsInPortrait\|columnsInLandscape' AppPackage/Sources/GalleryListComponents/GenericList.swift` returns 0.
    - `GenericList.swift` contains `MasonryLayout {` and `.animation(nil, value: galleries)` in the `.thumbnail` branch, and still contains `.listStyle(.plain)` and the auto-load `FetchMoreFooter` inside the masonry `VStack` row (D-36).
    - Auto-load preserved (D-36): `GenericList.swift` still contains `onScrollGeometryChange`, `onScrollPhaseChange`, and `lastAutoFetchCount`; it contains NO chevron `Button`/`.chevronDown` in the thumbnail branch.
    - Spike diagnostics gone: `grep -c 'OSLogExt\|logger' AppPackage/Sources/GalleryListComponents/GenericList.swift` returns 0 (import + logger + debug line removed).
    - `git diff AppPackage/Sources/AppTools/DeviceUtil.swift` is empty (DeviceUtil untouched, D-34).
    - `xcodebuild test -scheme AppPackage-Package` (full suite) exits 0.
  </acceptance_criteria>
  <done>The thumbnail grid renders through MasonryLayout with the legacy column reads gone, no WaterfallGrid reference in the file, structure/refresh preserved, DeviceUtil untouched, and the full suite green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See 02-01-PLAN.md for the full new-symbol list. This plan finalizes `MasonryLayout` (no new symbols) and
edits `GenericList.swift` (removes the import + `columnsInPortrait`/`columnsInLandscape`). Downstream
drift verification must treat the removed legacy column reads and the removed library import as expected.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI layout engine → MasonryLayout | Same boundary as Plans 01-02; the production swap changes the layout implementation, not the data source. Gallery data is already-fetched and already-trusted; no new input boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | `MasonryLayout` degenerate proposal width | low | mitigate | D-32 clamp retained in the finalized layout (covered by the pure-function suite). Robustness, not security. |
| T-02-SC | Tampering | package installs | n/a | accept | No packages installed this plan; the WaterfallGrid dependency removal is Plan 04. |

**No new threats above the WaterfallGrid baseline.** The swap replaces one client-side rendering layout
with another over the same trusted data at ASVS L1; no network, input, persistence, auth, or secret
surface is added.
</threat_model>

<verification>
- Full suite green: `xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16 Pro'`.
- `grep -c 'WaterfallGrid' AppPackage/Sources/GalleryListComponents/GenericList.swift` == 0.
- `columnsInPortrait`/`columnsInLandscape` are gone; `DeviceUtil.swift` is unchanged (D-34).
- Run exactly ONE `xcodebuild test` at a time.
</verification>

<success_criteria>
- The `.thumbnail` grid renders via MasonryLayout with animation suppression, structure parity, and no WaterfallGrid reference in GenericList.swift.
- The legacy `columnsInPortrait`/`columnsInLandscape` reads are deleted; DeviceUtil is untouched.
- MasonryLayout is finalized (no spike instrumentation) at the frozen `m`; the full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/02-native-masonry-grid-swap/02-03-SUMMARY.md` when done.
</output>
