---
phase: 15-continued-background-downloads
plan: 44
type: execute
wave: 44
depends_on: ["15-43"] # File overlap on DownloadClient+Manager.swift (15-43 may correct trust-set docs there) and the corrected sentences must describe post-15-42/15-43 source; xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/BackgroundProcessingClient/ContinuedTaskScheduling.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-24]
requirements: [SC1, SC2]

must_haves:
  truths:
    - "No load-bearing claim about `schedulableDownloads()` states something source contradicts: both corrected sites say what is actually SHARED (the `isSchedulableDownload` predicate), name where the scheduler's own read lives (`scheduleNextIfNeededCore`'s scoped `queueStore.gids` + `indexedDownloads(gids:)` selection through `nextQueuedDownload` / `nextUnqueuedSchedulableDownload`), and record why the divergence — the active-gallery union the scoped read does not carry — is currently inert (`scheduleNextIfNeededCore` returns at `guard activeTask == nil` whenever an active gallery exists), each sentence derived from a fresh enumeration quoted in the summary. This is the FIFTH consecutive round of a corrected comment carrying a claim source contradicts, and the second in a row written by the previous round's own correction work — so no corrected sentence may be written from the gap record's wording."
    - "The claim is owned, not merely corrected: a `schedulableDownloads()` call-site census joins `DownloadSourceInventoryTests` on the file's own established pattern (fragment-assembled token, comment lines and the declaration excluded, per-file table plus joined total, known-member guard), so the next divergence between the read authority's callers and the docs that cite them fails a build instead of waiting for a review round."
    - "The device-proven registration exemption is recorded at the site: a note beside `ContinuedTaskScheduling.live.register`, in the shape of the existing App/Info.plist UIBackgroundModes note, states that per-session identifiers under the bundle-scoped continued-processing wildcard make pre-launch registration structurally impossible, that post-launch registration at first session start was accepted on physical iOS 26 hardware, and names 15-UAT.md test 1 as the run that showed it — so the next reader does not re-open WR-02's question from the same absence."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift"
      provides: "schedulableDownloads()'s header corrected: the shared predicate named, the scheduler's own read located, the inert divergence explained, the census test pointed at"
      contains: "isSchedulableDownload"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      provides: "The G-15-8 paragraph's reader list corrected to the three actual callers plus the scheduler's same-predicate scoped read"
      contains: "schedulingBlockedGalleryCounts"
    - path: "AppPackage/Sources/BackgroundProcessingClient/ContinuedTaskScheduling.swift"
      provides: "The device-verified post-launch registration note beside live.register"
      contains: "15-UAT"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      provides: "The schedulableDownloads() call-site census with known-member guard and fragment tokens"
      contains: "testSchedulableDownloadsCallSitesMatchTheRecordedCensus"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift"
      via: "the census fails when a schedulableDownloads() call site is added or removed, so the corrected caller list cannot silently rot the way the one-authority sentence did twice"
      pattern: "schedulableDownloads"
    - from: "AppPackage/Sources/BackgroundProcessingClient/ContinuedTaskScheduling.swift"
      to: "App/Info.plist"
      via: "the registration note names the bundle-scoped wildcard entry and mirrors the Info.plist note's shape: a device-verified rationale recorded where the next editor will look"
      pattern: "continued"
  prohibitions:
    - statement: "Must NOT change any production behavior: every edit in Sources is comment-only, proven by a diff that touches no executable line in any Sources file."
      status: pending
      verification: automated
    - statement: "Must NOT plan or make a registration-timing change: WR-02's runtime concern is REFUTED by the device UAT record and that judgement is recorded in 15-VERIFICATION.md's round-13 scope note precisely so this round does not relitigate it — the remedy is the note, nothing else."
      status: pending
      verification: automated
    - statement: "Must NOT correct any sentence from the gap record's wording alone: each correction is derived from a fresh enumeration of the source it describes, quoted in the summary."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression, and must NOT weaken or delete an existing case."
      status: pending
      verification: automated
---

<objective>
Close G-15-24 (warning, doc-vs-source group — the FIFTH consecutive round in which a corrected
comment carries a claim source contradicts, this time written by round 13's own correction plan).
Verified by enumeration at the round-13 HEAD:

- **WR-01 — the one-authority claim.** `schedulableDownloads()` has exactly three call sites —
  `schedulableSnapshot()` (`+ContinuedSession.swift:122`), `pauseAllSchedulable` (`:357`), and
  `hasPendingWork()` (`+PendingWork.swift:14`) — and `scheduleNextIfNeededCore`
  (`+Scheduling.swift:38-53`) is NOT among them: it takes `queueStore.gids`, performs its own
  `indexedDownloads(gids: queuedGIDs)` read, and applies `isSchedulableDownload` through
  `nextQueuedDownload` / `nextUnqueuedSchedulableDownload`. Two docs assert the scheduler reads
  through the function anyway: the header at `+PendingWork.swift:17-20` and the G-15-8
  paragraph's parenthetical at `+Manager.swift:387-388`, the latter written by plan 15-40's own
  WR-01 correction. What is genuinely shared is the PREDICATE, not the read scope, and the
  divergence is real: the active-gallery union at `+PendingWork.swift:44-47` is exactly what the
  scheduler's scoped read does not carry, inert today only because `scheduleNextIfNeededCore`
  returns at `guard activeTask == nil` whenever an active gallery exists.
- **WR-02 — refuted as a defect, kept as a note.** The mechanical half is true (`BGTaskScheduler`
  appears in exactly one file, its `register` closure is reached only from
  `ContinuedProcessingSession.start`, `AppDelegate` registers nothing), but 15-UAT.md test 1
  reads `result: pass` on physical iOS 26 hardware with pages landing well past the deleted
  grace window — reachable only if the post-launch registration was honoured and the task
  launched. The design is device-proven. What survives is the reviewer's own remedy: a
  device-verified note beside `ContinuedTaskScheduling.live.register`, in the shape of the
  existing App/Info.plist UIBackgroundModes note, because a per-session UUID identifier makes
  pre-launch registration structurally impossible and that exemption is load-bearing for the
  whole design.

Purpose: the reasoning surface future scheduling and session fixes stand on states only what
source satisfies (SC1's queue lifetime and SC2's card both reason from these docs), and the
corrected caller list is owned by a census that breaks on drift.
Output: two corrected authority sites, the registration note, the new census case, and a green
full run.
</objective>

## Artifacts this plan produces

- **New test case:** `testSchedulableDownloadsCallSitesMatchTheRecordedCensus` in
  `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift`, with its expected
  per-file table and joined-total constants (names following the file's existing
  `expectedSchedulingBlockCallSites` / `expectedSchedulingBlockCallTotal` idiom) and a
  fragment-assembled detection token for the function name.
- **Doc corrections** at the two authority sites; **new doc note** beside
  `ContinuedTaskScheduling.live.register`. No new production symbols.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-UAT.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two authority claims and record the device-verified registration note, from fresh enumerations</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift, AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/BackgroundProcessingClient/ContinuedTaskScheduling.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-24 entry WHOLE and the round-13 `verification_scope_note`'s WR-02 judgement (recorded so it is not relitigated)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` — the WHOLE file AS 15-42/15-43 LEFT IT: the header claim at `:17-20`, the active-gallery union and its WR-01 paragraph (`:22-41`), and the body (`:42-52`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `scheduleNextIfNeededCore` (`:38-53` region): enumerate for yourself what it reads and where `isSchedulableDownload` enters — the correction is derived from this enumeration, not from the gap text
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the G-15-8 paragraph on `schedulingBlockedGalleryCounts` (`:376-399` region) as the previous plans left it
    - `AppPackage/Sources/BackgroundProcessingClient/ContinuedTaskScheduling.swift` — the WHOLE file (135 lines): where `live.register` sits and what its closure does
    - `AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift` — where the per-session identifier is minted and where `register` is reached from (the `:141-147` region the verification cites) — re-derive both claims
    - `App/Info.plist` — the `BGTaskSchedulerPermittedIdentifiers` entry (confirm the exact bundle-scoped wildcard value) and the UIBackgroundModes note at `:160-165` (the SHAPE the new note follows: a device-verified rationale with asymmetric failure modes, recorded at the site)
    - `.planning/phases/15-continued-background-downloads/15-UAT.md` — test 1's record (result, device, what was observed): the note cites it, so read what it actually says
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**Fresh enumerations first.** Grep `schedulableDownloads()` across `AppPackage/Sources` and
record every match classified as declaration, call site, or doc mention. Grep `BGTaskScheduler`
across `AppPackage/Sources` and `App/` and record the single-file claim's verdict. Trace
`register` from `ContinuedTaskScheduling.live` to its one caller and record where the identifier
it registers is minted. Read 15-UAT.md test 1's result line. Every corrected or new sentence
below derives from these enumerations, quoted in the summary.

**Site 1 — `+PendingWork.swift` header.** Rewrite the one-authority header so it claims only
what holds: this function is the shared READ for the pending-work gate, the session snapshot,
and the expiration sweep (name the three call sites the enumeration found); what the SCHEDULER
shares with it is the `isSchedulableDownload` PREDICATE, not this read —
`scheduleNextIfNeededCore` performs its own scoped read (`queueStore.gids`, then
`indexedDownloads(gids:)`, predicate applied through `nextQueuedDownload` /
`nextUnqueuedSchedulableDownload`). State the divergence and why it is inert: the active-gallery
union below widens THIS read in exactly the way the scheduler's scoped read is not widened, and
that cannot bite today because `scheduleNextIfNeededCore` returns at its `guard activeTask ==
nil` whenever an active gallery exists — with the standing re-validation note that removing that
guard re-opens the question. Point at `DownloadSourceInventoryTests` as the census that owns the
caller list. Keep the WR-01 union rationale paragraph (it is true and load-bearing); only the
authority claims change.

**Site 2 — `+Manager.swift` G-15-8 paragraph.** Replace the single-authority parenthetical with
the derived statement: a gid left blocked is invisible to every `schedulableDownloads()` reader
— the pending-work gate, the session snapshot, and the expiration sweep — and the scheduler's
own scoped read applies the same predicate, so a blocked gid is skipped there too. The
surrounding release-then-converge rule stays untouched.

**Site 3 — the registration note.** Beside `ContinuedTaskScheduling.live.register` (on the
`live` value or the `register` closure, whichever reads better at the site), write the
device-verified note in the Info.plist note's shape: registration happens post-launch, at first
session start, and CANNOT be moved to launch — the identifier is minted per session (name where,
from the trace) under the bundle-scoped continued-processing wildcard in
`BGTaskSchedulerPermittedIdentifiers` (name the exact entry read from App/Info.plist), so no
concrete identifier exists to register before a session starts. Record the device evidence: the
post-launch registration design was accepted on physical iOS 26 hardware — 15-UAT.md test 1,
`result: pass`, with pages continuing to land past the deleted grace window, an outcome
reachable only if the registration was honoured and the task launched. Close with the
asymmetry, mirroring the Info.plist note: moving registration earlier is structurally
impossible, and re-raising the lazy-registration concern without new device evidence re-opens a
question this record already answers.

**Then:** no test run in this task — every edit is comment-only; Task 2 runs everything once.
  </action>
  <verify>
    <automated>git diff --stat -- AppPackage/Sources</automated>
  </verify>
  <acceptance_criteria>
    - Site 1: `grep -c 'all select through this' AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` outputs `0`, the rewritten header names the three call sites and the scheduler's own read location, and the inert-divergence sentence carries the `activeTask` guard reason (quoted in the summary beside the enumeration that derived it).
    - Site 2: `grep -c 'the scheduler all read' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `0` and `grep -c 'single authority' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `0`; the replacement is quoted in the summary.
    - Site 3: the note exists beside `live.register` — `grep -c '15-UAT' AppPackage/Sources/BackgroundProcessingClient/ContinuedTaskScheduling.swift` outputs at least `1` — and names the wildcard entry verbatim as read from App/Info.plist, the per-session minting site, and the device result (quoted in the summary).
    - The enumerations are in the summary: the classified `schedulableDownloads()` match list, the `BGTaskScheduler` single-file verdict, the register-caller trace, and the UAT test-1 reading.
    - Comment-only proof: the task's `git diff` touches no executable line in any Sources file (stated from the diff, quoted in the summary).
  </acceptance_criteria>
  <done>Both authority sites read true against fresh enumerations, the registration exemption is recorded at the site with its device evidence, and no correction was written from the gap record's wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the corrected caller list with a schedulableDownloads() call-site census</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the WHOLE file: the scanner (`callSiteCount` excludes comment lines and the `func`-prefixed declaration line; `executableLines`; fragment-assembled tokens), the known-member guard, and the per-file-table-plus-joined-total assertion idiom the new census must reuse verbatim
    - `AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` and `DownloadClient+ContinuedSession.swift` — every `schedulableDownloads()` call site AS the previous plans left them, enumerated fresh
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <behavior>
    - `testSchedulableDownloadsCallSitesMatchTheRecordedCensus` — walks
      `AppPackage/Sources/DownloadClient` with the file's existing scanner, counts call
      occurrences of the fragment-assembled function token per file (declaration and comment
      lines excluded exactly as the existing censuses exclude them), and asserts equality
      against a named per-file table plus a separately-counted joined total, behind the existing
      known-member guard. The expected census is derived fresh from source at execution time —
      the verification's enumeration (one call in the pending-work gate, two in the
      continued-session file) is the reconciliation target, and a difference is a finding to
      record, not a number to copy.
  </behavior>
  <action>
**Step 1 — the census case.** Add the token (fragment-assembled, following the file's
`schedulingBlockCallToken` idiom), the expected per-file table and joined-total constants, and
the test case reusing `callSiteCount` unchanged. The table's doc comment states what the census
owns: the caller list the corrected `+PendingWork.swift` header and the G-15-8 paragraph now
carry, and the way it rots — a new reader appearing (or the scheduler gaining this read) without
those docs being re-derived.

**Step 2 — reconcile against Task 1's docs.** If the fresh enumeration disagrees with the
sentences Task 1 wrote, fix the DOC and record the discrepancy — the census asserts source, and
the doc must describe source.

**Step 3 — falsifiability and green.** The census asserts current source, so an honest RED is
not available; record falsifiability bookkeeping-style in the summary: name the edit that flips
it (the scheduler calling the shared read, a fourth caller appearing, a caller being removed)
and why the per-file equality catches each. Run the inventory suite targeted, then ONE full
FeatureTests run. Never overlap invocations.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The case exists: `grep -c 'testSchedulableDownloadsCallSitesMatchTheRecordedCensus' AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` outputs `1`.
    - The detection token is fragment-assembled so the suite does not match its own strings, and the case runs behind the existing known-member guard (source inspection, quoted).
    - The summary records the derived census, its reconciliation against the Task 1 docs, and the falsifiability bookkeeping.
    - The targeted run and one full FeatureTests run exit `0`, each a single invocation; `wc -l` for the inventory file recorded and well under `1000`.
  </acceptance_criteria>
  <done>The corrected caller list is owned by a census that fails the build when it drifts, derived fresh from source and reconciled against the corrected docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Written premise → future fix | The authority docs are the reasoning surface scheduling-liveness and card fixes stand on; a false shared-read claim steers the next fix into widening or narrowing the wrong read (the recorded five-round generator) |
| Doc note → system contract | The registration note records a system-scheduler contract fact (post-launch registration honoured) that code cannot assert; a wrong note would license a real defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-44-01 | Tampering | A future edit trusting the one-authority claim widening schedulableDownloads() expecting the scheduler to follow, or scoping a fix to it believing the scheduler reads it | medium | mitigate | Corrections derived from fresh enumerations; the call-site census fails on drift; the inert-divergence reason names its guard |
| T-15-44-02 | Repudiation | The registration note overstating what the UAT run proved | low | mitigate | The note cites 15-UAT.md test 1 verbatim facts only (result, device, observed landing past the grace window) read fresh from the record |
| T-15-44-03 | Denial of Service | A well-meaning future change moving registration pre-launch against the per-session identifier design, terminating the app on a second handler | medium | mitigate | The note states the structural impossibility and the wildcard entry; the scope note's refutation of WR-02 is cited so the timing is not "fixed" |
</threat_model>

<verification>
- Task 1 is comment-only (git diff verify); Task 2 runs the targeted inventory suite plus the
  plan's single full FeatureTests run. xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from G-15-24's grouped items and the SC labels its sites back (SC1 queue lifetime, SC2 card
  authority).
- <assumption_delta_decision>no-change — comment corrections, a doc note, and a source-census
  test; no identity model changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is added or changed — the note documents the existing
  `register` surface; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan; the note cites
  test 1's already-recorded pass, which is a different test.
</verification>

<success_criteria>
- G-15-24 closed: both authority sites read true against fresh enumerations quoted in the
  summary, the caller list is pinned by a drift-failing census, the device-verified registration
  note stands beside live.register in the established note shape, no production behavior moved,
  and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-44-SUMMARY.md` when done, including
the before/after sentences with their deriving enumerations, the registration-note text with its
UAT citation, the derived census with its reconciliation, and the falsifiability bookkeeping.
</output>
