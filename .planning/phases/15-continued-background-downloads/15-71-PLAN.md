---
phase: 15-continued-background-downloads
plan: 71
type: execute
wave: 71
depends_on: ["15-70"]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
autonomous: true
gap_closure: true
gap_ids: [WR-02]
requirements: [SC3]

must_haves:
  truths:
    - "No act of the app destroys a page file without the same act durably reconciling the record that claims it: materializeRepairSeed's source page scan no longer deletes anything, so the source folder's manifest and its page files agree after an interrupted repair."
    - "Whichever folder deduplicatedDownloadIndex selects after an interrupted repair-with-rename is not a folder claiming pages the app deleted."
    - "The entitlement rule is a censused invariant, not a per-site comment: every surviving discardingRejected: true production site carries a recorded per-site verdict, and a new unentitled site moves a table before it ships."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      provides: "Non-mutating source page scan in materializeRepairSeed"
      contains: "materializeRepairSeed"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      provides: "discardingRejected entitlement census with per-site dispositions"
      contains: "discardingRejected"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift"
      provides: "Interrupted repair-with-rename regression over an all-refused source"
      contains: "func test"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      via: "repairSeed hands materializeRepairSeed the gallery's currently indexed folderURL — the premise that makes source-side deletion a record/disk divergence on every non-completing run"
      pattern: "repairSeed"
    - from: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift"
      via: "deduplicatedDownloadIndex resolves duplicate gids by displayDate, so the surviving source folder's truthfulness decides what the app displays"
      pattern: "deduplicatedDownloadIndex"
---

<objective>
Close verification gap 2 (SC3; review WR-02) — the one production site failing the round's own entitlement test ("an act may delete only if the same act durably blanks the record for the page it destroyed"). `materializeRepairSeed`'s source page scan (DownloadStore+Operations.swift:168-172) passes `discardingRejected: true` and deletes refused page files inside the gallery's CURRENTLY INDEXED folder — `repairSeed` returns `download.folderURL` (ExecutionSupport.swift:820-838) — while writing nothing to that folder's manifest. The destination's reconciliation blanks the COPY; the source goes on claiming pages the app deleted. On a non-completing run (failure, cancellation, termination), `deduplicatedDownloadIndex` picks by `displayDate` (Persistence.swift:59-71), a child `removeItem` bumps the parent directory's mtime, and on the all-refused shape the lying source can win the index — exactly the record/disk divergence CLAUDE.md's manifest-SSOT rule forbids, created by the app, marked by nothing.

The fix takes the review's recommended arm: the NON-MUTATING default at the source scan (strictly simpler; the destination's own reconciliation records the absence; the cost is an orphan zero-byte file, the already-known IN-03 unswept population). The entitlement test is then applied to EVERY surviving `discardingRejected: true` site with a per-site verdict, and the rule becomes a censused invariant.

Purpose: SC3's record-convergence half — no app act leaves a record claiming a page the same act destroyed.
Output: a read-only source scan, per-site entitlement verdicts in code docs, an entitlement census, and an interrupted-repair regression.
</objective>

## Artifacts this phase produces

- Revised `materializeRepairSeed` (DownloadStore+Operations.swift): the source page scan takes the non-mutating default (the `discardingRejected: true` argument at line 171 is deleted); the entitlement comment block at 151-167 is rewritten to state the verdict — the source folder is the indexed record's own folder, so nothing on this route is entitled to delete inside it — replacing the conceded open question at 166-167.
- Re-derived entitlement verdicts (doc comments) at the two surviving `discardingRejected: true` production sites: the cover scan inside `materializeRepairSeed` (Operations.swift:139-149) and the cover resolution in `prepareWorkingSeed` (ExecutionSupport.swift:381-385) — both entitled because a cover carries no recorded hash, so removal has nothing to diverge from. (ExecutionSupport.swift is doc-referenced only; no source change lands there in this plan.)
- New census in `DownloadSourceInventoryTests`: `testDiscardingRejectedSitesMatchTheEntitlementCensus` — counts `discardingRejected: true` call sites over `clientModuleFiles(in:)`, pinned to exactly the two cover sites with a failure message stating the entitlement test, so a third site cannot ship without a classified verdict. Token assembled from fragments per the suite's own anti-grep convention.
- New regression in `DownloadCoordinatorRepairSeedTests`: an interrupted repair-with-rename over an all-refused source asserting source-side record/disk agreement and index-winner truthfulness.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-66-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — stage the interrupted repair-with-rename divergence</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift` — all seven existing cases (482 lines): the staging idiom (real store over a temp root, manifests via `storage.writeManifest`, zero-byte page files), and which cases drive `materializeRepairSeed` through production entry points vs directly.
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `materializeRepairSeed` (121-194): the copy loop, the cover scan, the source page scan, and what `discardingRejected: true` deletes today.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `repairSeed` (819-838): the mode/pageCount admission that makes the source the indexed folder, and how a rename (destination folder path differing from source) arises from a payload title change.
    - `AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift` — `deduplicatedDownloadIndex` (59-71): the displayDate arbitration the second assertion pins.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadReadPathNonMutationTests.swift` — the 15-66 both-sides assertion idiom (file bytes + relaunched coordinator's persisted reading) this regression mirrors on the source folder.
  </read_first>
  <behavior>
    - Stage: a completed-claiming gallery record whose folder holds only refuted page files (zero-byte), plus a repair payload whose gallery detail carries a CHANGED title so the working folder path differs from the source (the rename shape). Drive the seed materialization through the production repair path and interrupt before completion (the run fails or is cancelled after seeding — pick the existing suite's interruption idiom), so `removeSupersededFolders` never sweeps.
    - Assert, post-interruption: (1) every page the SOURCE folder's manifest claims has its file still present on disk — the app deleted nothing it did not reconcile; (2) rebuild the coordinator over the same root (relaunch idiom) and assert the indexed record for the gid — whichever folder `deduplicatedDownloadIndex` selected — does not claim any page whose file is absent in that folder.
    - Pre-fix this discriminates: the source scan deletes the zero-byte files while the source manifest still claims them, so assertion (1) fails; and on the all-refused shape the deletions bump the source's mtime so the lying source can win the index, failing (2). The case doc states the pre-fix observable and why this shape (interrupt + rename + all-refused) is the only one that can observe it.
  </behavior>
  <action>
Add the case to `DownloadCoordinatorRepairSeedTests` (482 lines — room within the 1000-line limit; add local helpers here, never to DownloadFeatureTestHelpers.swift at 992/1000). Reuse the suite's existing staging helpers; the all-refused population is zero-byte files at every claimed page's relative path. If the suite's existing entry points cannot produce the rename-shaped destination, derive the working folder path the way `prepareWorkingSeed`'s callers do and drive `storage.materializeRepairSeed` at the same call shape production uses, documenting in the case why the staging is production-faithful (rule: contract-faithful staging — the arguments must be the ones `repairSeed` would return). Bank the pre-fix failures (which files vanished, which folder won the index) for the summary.
  </action>
  <verify>
    <automated>! xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests</automated>
  </verify>
  <acceptance_criteria>
    - The new case fails pre-fix on source-side record/disk agreement; the existing seven cases stay green.
    - The staging is all-refused (no usable page is copied), the destination path differs from the source, and the run does not complete — all three conditions the gap names as jointly required.
    - The relaunch assertion goes through a rebuilt coordinator (persisted basis), not in-memory state.
  </acceptance_criteria>
  <done>The source-folder divergence is a deterministic RED observation with both the record/disk and the index-winner halves pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — non-mutating source scan, per-site verdicts, entitlement census</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift</files>
  <read_first>
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `materializeRepairSeed` (121-194): the comment block at 151-167 being rewritten and the scan call at 168-172; the cover entitlement comment at 135-138 whose reasoning the rewrite mirrors.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the census idiom: fragment-assembled tokens, `clientModuleFiles(in:)` scoping, per-file table + separately-counted joined total, `requireKnownMembers` guard, and the failure-message convention (each message instructs the next editor how to classify before touching the table).
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — the cover site at 381-385: the second surviving `true` site the census pins and whose verdict the summary records.
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — gap 2's three `missing:` bullets: per-site verdicts, the regression, and the censused invariant.
  </read_first>
  <behavior>
    - `materializeRepairSeed`'s source page scan reads without deleting: refused source files survive on disk, are not copied, and the destination's own reconciliation records their absence in the COPY — the route's only manifest writer, unchanged. The unanswered-page accounting (`unansweredPages`) is unchanged: a page the scan positively rejects is still "not selected", so the destination may treat it as absent, exactly as the existing comment at 174-177 derives.
    - Exactly two production `discardingRejected: true` sites remain — both covers, both entitled because a cover carries no recorded hash. The census pins the set per file; a new site fails the census with a message stating the entitlement test verbatim: an act may delete only if the same act durably blanks the record for the page it destroyed.
  </behavior>
  <action>
Delete the `discardingRejected: true` argument from the source page scan (the non-mutating default is the statement, matching the 15-66 convention stated at ExecutionSupport.swift:340-341). Rewrite the 151-167 comment: the verdict (this site is NOT entitled — the source is the indexed record's own folder and nothing here writes its manifest; the deletion was the divergence), why the non-mutating default is the correct arm (destination blanks the copy; the orphan zero-byte file is IN-03's known accepted population — cite IN-03 rather than re-deriving it), and that the cover scan above keeps `true` on its own no-recorded-hash entitlement. Then add the census to `DownloadSourceInventoryTests`: scope through `clientModuleFiles(in:)`, assemble the `discardingRejected: true` detection token from fragments (the suite's anti-grep convention), assert the per-file site table (`DownloadStore+Operations.swift`: 1, `DownloadClient+ExecutionSupport.swift`: 1) plus the joined total of 2, and write the failure message so the next editor must record a per-site entitlement verdict before moving the table. Record all three per-site verdicts (two covers entitled, source scan converted) in the summary as the gap's censused-invariant answer.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests</automated>
  </verify>
  <acceptance_criteria>
    - Task 1's case is green; all existing repair-seed and inventory cases green.
    - `rg -n 'discardingRejected: true' AppPackage/Sources` → exactly 2 matches, both cover sites (DownloadStore+Operations.swift and DownloadClient+ExecutionSupport.swift).
    - The census fails if a third site appears or either cover site moves files without a table edit; its failure message contains the entitlement rule.
    - The rewritten comment no longer concedes an unanswered question: `rg -n 'this round did not answer' AppPackage/Sources` → no matches.
    - No SwiftLint suppression; DownloadSourceInventoryTests stays under the 1000-line error limit (911 lines pre-change — verify headroom after the addition and split by pure move only if exceeded).
  </acceptance_criteria>
  <done>Reads never delete on the repair-seed source route; the entitlement rule is enforced by a census, and every surviving destructive read carries a recorded verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repair-seed source folder → persisted download index | The source folder is the gallery's currently indexed record; anything that mutates its files without its manifest corrupts the display basis the index serves. |
| Interrupted runs → index arbitration | On non-completing runs, duplicate folders compete by mtime-derived displayDate; the surviving folder's record must be truthful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-71-01 | Tampering | materializeRepairSeed source scan | high | mitigate | Non-mutating default; the app no longer deletes files inside a folder whose record it does not reconcile in the same act. Regression pins record/disk agreement across interruption and relaunch. |
| T-15-71-02 | Repudiation | unmarked record/disk divergence | medium | mitigate | The divergence-creating act is removed; the census message obliges a recorded verdict for any future destructive read. |
| T-15-71-03 | Information Disclosure | orphan zero-byte source files | low | accept | IN-03's known unswept population; first-writer rule keeps correctness; recorded as accepted, not fixed here. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1571DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels plus the two UAT-FU anchors, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC3 | No lost/duplicated work; manifest-SSOT | 15-71 | COVERED | The last read-that-deletes-without-reconciling is converted; source record and disk agree across interruption. |
| VERIFICATION | gap 2 bullet 1 | Entitlement test at every surviving true site, per-site verdicts | 15-71 | COVERED | Two covers entitled (verdict docs), source scan converted; verdicts recorded in summary. |
| VERIFICATION | gap 2 bullet 2 | Interrupted repair-with-rename case + index-winner assertion | 15-71 | COVERED | Task 1, both halves. |
| VERIFICATION | gap 2 bullet 3 | Censused invariant, not a per-site comment | 15-71 | COVERED | discardingRejected census with entitlement-stating failure message. |
| REVIEW | WR-02 | Same defect by review ID | 15-71 | COVERED | Review's recommended non-mutating arm taken. |

<success_criteria>
- After an interrupted repair-with-rename over an all-refused source, the source manifest and its page files agree, and the index winner claims no deleted page.
- Exactly two entitled `discardingRejected: true` sites remain, censused with per-site verdicts.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-71-SUMMARY.md` when done.
</output>
