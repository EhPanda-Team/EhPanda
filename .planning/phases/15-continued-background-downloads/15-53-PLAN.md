---
phase: 15-continued-background-downloads
plan: 53
type: execute
wave: 53
depends_on: ["15-52"] # xcodebuild serialization — one test invocation at a time on this machine — and a real file-overlap ordering that decides this plan's whole shape. `DownloadSourceInventoryTests.swift` is edited by 15-50 (which re-derives the run-proof census the promoted property moves) and by 15-52 (which adds a two-part client-double census over the scanned test target). This plan changes what that suite SCANS, and 15-49 recorded what a scan change does to a census suite: every table counts over the files the scanner returns, so a scope change silently re-bases each one — it caught a pending-page-list census that would have gone 1 to 4. Landing this before either predecessor would mean re-deriving tables that are about to move, and landing it concurrently would mean two plans re-basing one suite at once. It goes last, and it re-derives EVERY census in the file at the head its two predecessors leave behind, including the ones they added or moved.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
  # PROVISIONAL BELOW THIS LINE — the path that follows is the PRE-DERIVED candidate home for the
  # nil-generation arm's pin: this suite already owns the retirement-lifetime case and already
  # carries the only fixture seam in the target that drives a REAL `processDownload` to a real exit
  # through its `defer` (`makeStubbedURLSession` plus a stubbed transport failure), which is the
  # route the arm is reached by. The destination MUST be re-derived against the file's line budget
  # at this head and this list rewritten in the SUMMARY to what was actually modified.
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-33]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "Both items are the SAME generator one layer down — an invariant reached by omission rather than written — which is why they are grouped rather than triaged apart, and why this project's bar treats them as blocking despite their severity. One is a policy reached by type promotion instead of a branch; the other is a self-exclusion reached by not thinking of it instead of by the sibling's written decision. Neither is a defect today and both are the shape every finding in this phase's six doc-versus-source rounds started as."
    - "`isSupersededByALiveRun`'s nil-generation policy is STATED in a branch, and the direction is recorded with it: a run that cannot prove it owns the gallery's slot is treated as superseded and retires nothing, because leaving the entry to the run that DOES own the slot costs one stale proof until that run exits, while dropping a live successor's proof is the G-15-26 zero-progress card. Per this project's rule that a deliberate design carries a doc comment explaining its WHY, the absence of that sentence is what makes an intentional policy read as an oversight (SC2's retirement basis)."
    - "Whether the written branch PRESERVES today's behaviour is DERIVED and then PROVEN, not asserted. The optional promotion makes `nil` compare unequal to every generation, so the pre-derived expectation is that an explicit early return of `true` is behaviour-identical — but an equivalence claimed from the type system and never executed is exactly the class of claim this phase has spent sixteen rounds re-deriving. A case pins the arm's disposition and is shown capable of failing by an OBSERVED reading taken with the branch inverted."
    - "If the branch turns out NOT to be behaviour-identical, that is a behaviour change inside G-15-26's own guard and is dispositioned explicitly — with the changed input named, the direction assessed against the same cost asymmetry, and the change reported as a change — rather than folded in as a readability edit."
    - "The written branch is the module's own convention rather than a taste preference, and the evidence is stated as source actually holds it: the sibling predicate directly below states its policy in an explicit `if let generation` branch — and carries NO rationale doc of its own, the doc block above it belonging to `retireProvenPageWork`. So the convention evidence is the written BRANCH alone, and the asymmetry between two adjacent predicates over one optional input is the whole of the argument, quoted rather than paraphrased. The review and the gap record both describe that sibling as carrying a recorded rationale; source does not, and inheriting that phrasing into a plan would be this phase's own six-round generator reaching the artifact that is supposed to stop it."
    - "The source-inventory scanner stops policing its own prose. It excludes itself by path in the same SHAPE the sibling `DownloadLogPrivacyInvariantTests.scannedFiles()` uses — the `invariantFilePath` binding at `:259` and the enumerator `where` clause at `:270-272` — and under the same binding name, so two scanners built from one template stay readable as one pattern. The reason is AUTHORED HERE rather than copied: the sibling implements the exclusion and explains nothing, carrying no doc comment at all (`:253` is a MARK, `:254` is blank, `:256` is the function). The assembled fragments stay the first line of defence and are not weakened."
    - "The asymmetry that made this a real hazard is written down: the suite's censuses already exclude comment lines so a doc describing an inventory does not become part of it, while the prose assertion deliberately reads whole files and then includes the one file whose job is to describe the prose rule. It passes today only because the three retired phrasings live there as run-time fragments and never as prose — so the first plainly-worded maintenance edit fails it, which is a scheduled failure rather than a hypothetical one."
    - "Changing what a census suite scans is treated as the hazard 15-49 recorded, for the THIRD time in two rounds: every census in the file is re-derived at the head 15-50 and 15-52 leave behind — including the tables those two plans added or moved — with each number quoted, and any change accompanied by a rewritten doc stating what the new number pins against. A narrowed scan that silently re-bases a table would trade one unwritten invariant for a censused one that had quietly stopped meaning its doc."
    - { statement: "No criterion's verdict moves and none is claimed to. The two SC labels name the surfaces the items sit on — the retirement basis the nil arm gates, and the schedulable-work claims the inventory owns — not verdicts this plan repairs", verification: backstop }
    - { statement: "No device-observable behaviour changes if the equivalence derivation holds; 15-UAT.md test 2 remains an independent open axis this plan cannot discharge", verification: backstop }
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift"
      provides: "A nil-generation policy stated as a branch with its direction and its cost asymmetry recorded, matching the sibling predicate's convention"
      contains: "isSupersededByALiveRun"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      provides: "A scanner that excludes itself by path in the sibling's shape and under its binding name, carrying a rationale authored here because the sibling records none, and every census in the file re-derived against the narrowed scan"
      contains: "scannedFiles"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift"
      via: "the two adjacent predicates over the same optional generation now answer it the same way — with a written branch — so a reader can tell that nil was considered in both rather than falling out of the types in one"
      pattern: "generation"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      to: "AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift"
      via: "the same self-exclusion under the same binding name, so two scanners built from one template stop diverging on the one decision that decides whether a prose rule can be documented — the SHAPE is inherited, the reason is written here because the sibling records none"
      pattern: "invariantFilePath"
  prohibitions:
    - statement: "Must NOT claim the written branch is behaviour-identical without executing it: the equivalence is derived, then pinned by a case, and that case is shown capable of failing by an observed reading taken with the branch inverted."
      status: pending
      verification: automated
    - statement: "Must NOT fold a behaviour change into a readability edit: if the derivation finds any input whose disposition moves, the change is named, its direction assessed against the stale-proof-versus-dropped-proof asymmetry, and it is reported as a change."
      status: pending
      verification: automated
    - statement: "Must NOT inherit the review's and the gap record's claim that the sibling predicate carries a recorded rationale. Source shows `isActiveTaskOwner` has no doc comment — `:315` is blank and the block above it is `retireProvenPageWork`'s — so the convention evidence is the written branch alone, and every sentence of the plan's argument that touched the inherited phrasing is corrected rather than repeated."
      status: pending
      verification: automated
    - statement: "Must NOT invent a rationale for the sibling's nil arm. It is derived from an enumeration of that predicate's callers, or the failure to derive one is recorded and raised and the arm is left undocumented — a fabricated WHY is worse than an absent one and is the failure this item exists to prevent."
      status: pending
      verification: automated
    - statement: "Must NOT weaken the assembled detection fragments while adding the path exclusion: they stay the first line of defence and the exclusion is the second, with that ordering asserted from THIS suite's own tokens rather than from a sibling's account of its own."
      status: pending
      verification: automated
    - statement: "Must NOT inherit 15-REVIEW.md:443-445's quoted rationale for the sibling scanner. That quotation has NO SOURCE REFERENT: `DownloadLogPrivacyInvariantTests.scannedFiles()` carries no doc comment, and a whole-file search for the self-exclusion wording returns nothing. The gap record inherited the quote from the review, so the false premise is three artifacts deep and this plan is where it stops — the sibling supplies the exclusion's SHAPE and its binding name, and nothing else."
      status: pending
      verification: automated
    - statement: "Must NOT conflate the sibling's two nearest real docs with a self-exclusion rationale: `:45-46` and `:73-75` explain the ASSEMBLED-FRAGMENT reason — that spelling a banned shape out would make a repository grep gate match the invariant enforcing it — which is a different reason for a different mechanism and is not evidence about the path exclusion."
      status: pending
      verification: automated
    - statement: "Must NOT assume the exclusion leaves the known-member guard or any test-target-scoped census unaffected: `knownMembers` and every table that counts over the scanned test target — including the ones 15-50 and 15-52 added or moved — are re-derived at execution time and asserted at their values, with each number quoted."
      status: pending
      verification: automated
    - statement: "Must NOT narrow the scan and leave any census un-re-derived. This is the third scan-scope change to this suite in two rounds and 15-49's recorded hazard applies directly; every table in the file is re-derived and any moved number carries a rewritten doc."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression; `DownloadSourceInventoryTests.swift` starts this plan above 526 lines and grows under 15-50 and 15-52, and `DownloadContinuedSessionRunProofTests.swift` above 332, both against a 1000-line `file_length` ERROR limit, so the budget is checked BEFORE any case is written."
      status: pending
      verification: automated
---

<objective>
Close G-15-33 (warning; a hygiene PAIR). Both items were confirmed verbatim in source by the
round-16 verification. They are grouped rather than triaged apart for the reason the gap record
gives: this project's bar treats nitpicks as blocking, and both are the same generator one layer
down — **an invariant reached by omission rather than written**. Neither is a defect today. Both are
the exact shape every finding in this phase's six consecutive doc-versus-source rounds started as.

**WR-03 — a real policy reached by type promotion.** `DownloadClient+Execution.swift:308-314` reads
`guard activeTask != nil, activeGalleryID == gid else { return false }` and then
`return generation != activeTaskGeneration`, where `generation` is `Int?` and `activeTaskGeneration`
is `Int`. The comparison promotes, so `nil` compares unequal to every generation and a
generation-less run is treated as superseded — meaning it retires nothing. That is a policy, and a
defensible one, but it is reached by the types rather than by a branch, so a reader cannot tell
whether `nil` was considered or merely fell out. The arm is reachable from outside the module:
`processDownload(gid:generation:)` is `public` with `generation` defaulting to `nil` (`:9-12`). And
the sibling predicate directly below, `isActiveTaskOwner` (`:316-326`), states its policy for the
same optional input in an explicit `if let generation` branch — so the module's own convention is
the written branch, and the asymmetry between two adjacent predicates over one input is the
evidence. In a module whose docs pin five separate source censuses precisely because unwritten
invariants rot, leaving this one to an optional promotion is the same failure mode one layer down.

**One premise in the inherited record is wrong, and correcting it is part of closing this item.**
Both 15-REVIEW.md's WR-03 and the G-15-33 gap record describe that sibling as handling the input
"with an explicit branch **and a recorded rationale**". Source shows otherwise: `isActiveTaskOwner`
carries NO doc comment at all — line `:315` is blank and the doc block ending at `:302` belongs to
`retireProvenPageWork`, where it records the G-15-26 retirement argument. The sibling's evidentiary
value is therefore the written BRANCH alone, which is still sufficient — two adjacent predicates
over one optional input, one of which says what it does about `nil` and one of which does not — but
the plan must argue from that and not from a rationale that is not there. Carrying the inherited
phrasing forward would be the same doc-versus-source generator this phase has recorded in six
consecutive rounds, reaching the artifact whose job is to stop it.

**WR-04 — a scanner that polices its own prose.** `DownloadLogPrivacyInvariantTests.scannedFiles()`
removes itself from its own scan by path — the `invariantFilePath` binding at `:259` and the
enumerator `where` clause at `:270-272`. `DownloadSourceInventoryTests.scannedFiles()` (`:441-461`)
was written from the same template and has no such filter, so
`testNoScannedDocNamesTheSharedReadAsTheSchedulersSoleAuthority` polices its own prose. It passes
today only because the three retired phrasings live there as run-time fragments assembled from
pieces (`:83-85`) and never as prose — which means the one file whose whole job is to explain what
the retired claim IS cannot spell it out, and the first plainly-worded maintenance edit fails it.
That is a scheduled failure, not a hypothetical one. The gap is asymmetric with the suite's own
reasoning: its censuses exclude comment lines through `executableLines` (`:427-432`) so a doc
describing an inventory does not become part of it, while the prose assertion deliberately reads
whole files and then includes the file that must describe the prose rule.

**A second inherited premise is wrong, and this one is worse than WR-03's.** 15-REVIEW.md `:443-445`
presents a block quotation as the sibling scanner's own rationale — "Excluding this file by path is
a second line of defence behind the assembled tokens: even if a future edit spelled one out, the
scan would not read it back as a violation of itself" — and the G-15-33 gap record inherited it.
**That sentence does not exist.** `DownloadLogPrivacyInvariantTests.scannedFiles()` carries no doc
comment at all: `:253` is `// MARK: - Scanning`, `:254` is blank, `:255` opens the private
extension, `:256` is the function. A whole-file search for the wording returns nothing. The
sibling's two nearest real docs — `:45-46` and `:73-75` — record the ASSEMBLED-FRAGMENT reason, that
spelling a banned shape out would make a repository grep gate match the invariant enforcing it,
which is a different reason for a different mechanism and must not be read as the missing one.

So the sibling supplies the exclusion's SHAPE and its binding NAME, and nothing else; the rationale
is authored in this plan. This matters beyond bookkeeping: a plan that told an executor to reproduce
a rationale from a template that has none would force it either to invent prose and attribute it to
source — the exact fabrication the WR-03 half forbids one function over — or to hit an uncorrectable
acceptance criterion mid-execution. A false quotation three artifacts deep is this round's own
subject, an invariant reached by omission, occurring in the tooling that is supposed to catch it,
and the summary is required to say so rather than fix it quietly.

**What makes WR-04 more than a two-line edit here.** This changes what a census suite SCANS, for the
third time in two rounds, and 15-49 recorded exactly what that does: every table in the suite counts
over the files the scanner returns, so a scope change silently re-bases each one — that plan caught
a pending-page-list census that would have gone from one to four had the widening been left
unscoped. This plan lands at the head where 15-50 has re-derived the run-proof census and 15-52 has
added a two-part client-double census over the scanned test target, so the tables to re-derive
include theirs.

Purpose: the round's review surface closes with no invariant left standing on omission — the
retirement gate says what it does about a generation-less run, and the suite that owns this module's
written claims can finally state the claim it owns (SC2's retirement basis; SC3's schedulable-work
inventory).
Output: a written nil-generation branch with its direction recorded and its equivalence proven by an
inverted-branch reading, a self-excluding scanner in the sibling's shape whose rationale is authored
here because the sibling records none, every census in the
suite re-derived at this head, and a green full run.
</objective>

## Artifacts this plan produces

This section is the source-grounding exclusion list for the convergence pass: every symbol below is
CREATED by this plan and must not be treated as drift against the current tree. The set is
PRE-DERIVED; the SUMMARY must rewrite it to what actually landed.

- **An explicit nil-generation branch inside `isSupersededByALiveRun`** — no new symbol, a new
  control-flow path plus the doc comment stating the policy, its direction and its cost asymmetry.
- **A path-exclusion binding in `DownloadSourceInventoryTests.scannedFiles()`** — candidate name
  `invariantFilePath`, matching the sibling scanner's name exactly so two scanners built from one
  template stay readable as one pattern.
- **One new test case pinning the nil-generation arm's disposition** — candidate name
  `testAGenerationLessRunRetiresNothingWhileALiveRunOwnsTheSlot`. Its home is provisional; see the
  marker in `files_modified`.
- **Possibly a plainly-worded description of the retired claim** in the inventory suite's own doc,
  which is the capability the exclusion exists to grant. Whether to exercise it is a recorded
  decision in Task 1, not an assumption.
- **No new production types, no new public API, no new census tables.**

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1 (WR-04): Exclude the scanner from its own walk in the sibling's shape but with a rationale authored here, re-derive every census in the suite at this head, and prove the exclusion works in both directions</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-33 entry WHOLE, in particular WR-04's paragraph naming the asymmetry with the suite's own comment-line filtering, and its `truth_violated` field explaining why the pair is grouped; then the G-15-29 closure record in `re_verification`, which describes the widened walk this plan narrows and the per-census re-scoping that made the widening safe
    - `.planning/phases/15-continued-background-downloads/15-REVIEW.md` — WR-04 whole, INCLUDING the block quotation at `:443-445` presented as the sibling scanner's own rationale. That quotation has no source referent; read it knowing that, and see the read below
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` — `scannedFiles()` WHOLE (`:256-...`), the `invariantFilePath` binding at `:259` and the enumerator `where` clause at `:270-272`. Read the lines ABOVE the function too: `:253` is `// MARK: - Scanning`, `:254` is BLANK and `:255` opens the private extension, so the function carries NO doc comment and the exclusion is implemented without being explained. Confirm that by search rather than by reading alone — the self-exclusion wording appears nowhere in the file. This is the template for SHAPE and for the binding NAME, and for nothing else; the rationale is authored in this plan. Read `:45-46` and `:73-75` as well, so the assembled-fragment reason they DO record is not mistaken for the missing one — it explains a different mechanism
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the WHOLE file at the head 15-50 and 15-52 leave behind. It is NOT the file as recorded here: 15-50 re-derives the run-proof census (token, per-file table, joined total) after the property it names is promoted, and 15-52 adds a two-part client-double census over the scanned test target. Read the suite header's rationale, `clientModuleDirectory` and `downloadsTestDirectory` (`:42-43`), `scannedDirectories` and its doc (`:45-59`), `knownMembers` and its doc (`:61-70`), `repositoryRootMarkers` (`:71`), every fragment-assembled token (`:73-89`), EVERY census table with its doc and its separately counted joined total, the prose assertion `testNoScannedDocNamesTheSharedReadAsTheSchedulersSoleAuthority` and its doc (`:355-393`), `requireKnownMembers`, `clientModuleFiles(in:)` and its doc, `callSiteCount`, `mutationCount`, `executableLines` (`:427-432`), `occurrences`, `repositoryRoot()`, `repositoryRelativePath(of:under:)` and `scannedFiles()` (`:441-461`)
    - `.swiftlint.yml` — the 120-column limit and the 1000-line `file_length` ERROR limit; this file starts above 526 lines and both predecessors add to it
  </read_first>
  <action>
**Step 1 — inventory the suite as it actually is at THIS head, before touching the scanner.** Two
plans of this round have edited this file since the gap was written. List every census the file now
contains — its token, its per-file table, its joined total and the case that asserts it — and for
each one record which tree it counts over: the client module through `clientModuleFiles(in:)`, or
the whole scanned set. Only the second kind can be moved by a scanner change, and the point of the
list is to know which ones those are before the change rather than after a surprise. Include the
prose assertion and both halves of the client-double census 15-52 added. Record the list.

**Step 2 — apply the exclusion in the sibling's SHAPE, and author the reason HERE.** Bind the
invariant file's own standardized path and add it to the enumerator's `where` clause exactly as
`DownloadLogPrivacyInvariantTests.scannedFiles()` does, using the same binding name so a reader
meeting the second scanner recognises the first.

Then write the rationale, and write it as this plan's own. **The sibling has none to copy.** Its
`scannedFiles()` carries no doc comment — `:253` is a MARK, `:254` is blank, `:255` opens the
extension — and the self-exclusion wording appears nowhere in that file. 15-REVIEW.md quotes such a
sentence at `:443-445` and the G-15-33 record inherited the quote, so the premise is three artifacts
deep and stops here. Do not attribute the sentence below to the sibling, do not "reproduce" it from
the template, and do not mine `:45-46` or `:73-75` for it — those record the assembled-fragment
reason, which is a different reason for a different mechanism.

The doc's content, authored in this file's own terms: the assembled fragments stay the FIRST line of
defence and this is the SECOND, and it exists because the one file whose job is to describe the
retired claim has to be able to spell it out — a prose rule that reads its own description back is a
self-match waiting for the first plainly-worded edit. Name the asymmetry that made it a real hazard rather than a tidy-up: the
censuses already drop comment lines so a doc describing an inventory does not join it, while the
prose assertion reads whole files and then included the file that must describe the prose rule. Do
not weaken the fragments and do not relax the prose assertion's tokens.

**Step 3 — re-derive EVERY census, and treat this as the recorded hazard it is.** This is the third
scan-scope change to this suite in two rounds. For every entry in Step 1's list, re-derive its
per-file table and its joined total against the narrowed scan and assert each UNCHANGED, recording
the before and after value for each. Where a number legitimately moves — a table that counted over
the whole scanned set and counted something in this very file — do not adjust it to fit: re-derive
what it now counts, rewrite its doc to say what the new number pins against, and record both the
old and the new with the reason. Verify `knownMembers` separately and by execution rather than by
assumption: neither named member is this file today, but that is a fact about the current members
and the guard must be shown still to pass, not argued to.

**Step 4 — prove the exclusion in BOTH directions, because one reading proves only half.**
- The exclusion WORKS: place a plainly-worded instance of the retired single-authority claim in this
  file's own doc — the exact capability the exclusion exists to grant — and run the suite. The prose
  assertion MUST PASS. Quote the reading.
- The walk STILL REACHES everything else: place a temporary plainly-worded instance in a different
  scanned file, run the suite, OBSERVE the prose assertion FAIL naming that file, then remove the
  temporary instance and observe green again. Quote both readings and state that the plant was
  removed. Without this half, an exclusion that accidentally emptied or broke the walk would look
  exactly like a working one.
Take the two readings as separate runs, never overlapping, and record their order.

**Step 5 — decide whether the plainly-worded description STAYS, and record the decision.** The
exclusion's whole justification is that this file must be able to describe the claim it polices, so
leaving the description in is the natural outcome and is the DEFAULT. If it stays, state the
consequence plainly: a repository-wide grep for the retired phrasing will no longer sum to zero, and
the summary must say where the one surviving instance is, that it is inside the excluded file, and
that the live guard is the prose assertion rather than any repository grep — otherwise a later round
reads the non-zero count as a regression. If it does not stay, state why the exclusion was still
worth landing.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests</automated>
  </verify>
  <acceptance_criteria>
    - The Step-1 inventory is in the summary as a table with one row per census: token, table, joined total, asserting case, and which tree it counts over; it is derived at THIS head and visibly includes the tables 15-50 moved and 15-52 added.
    - The exclusion matches the sibling in shape and naming: both `scannedFiles()` implementations are quoted side by side in the summary, and the binding name is identical.
    - The summary records that the sibling supplies SHAPE and NAME only: it states that `DownloadLogPrivacyInvariantTests.scannedFiles()` carries no doc comment, quotes the three surrounding line numbers as evidence (`:253` MARK, `:254` blank, `:255` extension opening), and reports the whole-file search for the self-exclusion wording returning nothing.
    - The false-premise finding is ON THE RECORD, not quietly fixed: the summary states that 15-REVIEW.md `:443-445` presents a block quotation as that scanner's rationale, that no such sentence exists in the file, that the G-15-33 gap record inherited the quote from the review, and that this plan corrected the premise rather than propagating it into a fourth artifact. The two nearest real docs (`:45-46`, `:73-75`) are named as recording the assembled-fragment reason — a different reason for a different mechanism — so a later reader cannot mistake them for the missing rationale.
    - The exclusion's doc is quoted and is presented as AUTHORED HERE rather than reproduced: it states the two-lines-of-defence reason, that the fragments remain the first, and the comment-filter asymmetry that made this a scheduled failure rather than a hypothetical one. No sentence in it is attributed to the sibling.
    - Every census from Step 1 has its before and after value recorded and the two are equal, or — where one legitimately moved — its re-derivation and its rewritten doc are quoted with the old and new numbers and the reason.
    - `knownMembers` is shown still to pass by execution, with the two members named and neither being this file.
    - The exclusion is proven in BOTH directions: the pass with a plainly-worded instance inside this file, and the FAIL naming a different scanned file with a temporary plant, followed by green after its removal. All three readings are quoted, their order stated, and the plant confirmed removed by a grep.
    - The Step-5 decision is recorded. If the description stays, the summary states where the surviving instance is, that it is inside the excluded file, and that the prose assertion — not a repository grep — is the live guard.
    - No `xcodebuild` invocation overlapped another, and the file's line count after the change is recorded against the 1000-line limit.
  </acceptance_criteria>
  <done>The scanner no longer polices its own prose: it excludes itself in the sibling's shape and under the sibling's binding name, carrying a rationale authored here because the sibling records none — a finding put on the record rather than quietly worked around, since the review quoted that rationale and the gap record inherited the quote. Every census in a suite two prior plans of this round already moved has been re-derived at this head, and the exclusion has been shown both to grant the capability it exists for and to leave the rest of the walk intact.</done>
</task>

<task type="auto">
  <name>Task 2 (WR-03): State the nil-generation policy in a branch with its direction, prove the equivalence by execution rather than by types, and take the full suite green</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-33 entry's WR-03 item WHOLE and its `suggested_fix`, whose parenthesis states the direction to record; then the G-15-26 closure record in `re_verification`, which names the card this predicate's other direction produces and is the cost the policy is weighed against
    - `AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift` — `processDownload`'s signature and its `generation` default (`:9-12`), its `defer` (`:12-23`) and the comment explaining why the retirement runs ahead of the ownership clear; `retireProvenPageWork` with its WHOLE doc (`:284-306`), especially the run-that-exits-before-preparing paragraph and the overlapping-run disposition, which is the argument the branch's direction has to stay consistent with; `isSupersededByALiveRun` (`:308-314`); and `isActiveTaskOwner` (`:316-326`) — the sibling handling the same optional input with an explicit branch. Read the lines AROUND it as well as the body: `:315` is blank and it carries no doc comment of its own, which is both why the convention evidence is the branch alone and why its own nil arm is Step 2b's subject. Do not take the review's or the gap record's description of this sibling on trust; they say it carries a recorded rationale and it does not
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — `activeTaskGeneration` (`:392`) and the doc directly below it distinguishing it from the queue-intent generation, so the branch's doc names the right one
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — the generation stamp at `:57-58`, which is where a scheduled run's generation comes from and therefore what a generation-less caller is missing
    - `AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` — `testingInstallActiveTask(gid:task:)` (`:12-21`), which bumps the generation and installs ownership in one step, and `testingSetActiveGalleryID(_:)` (`:23-25`): the two seams a staging of the nil arm has available
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift` — the WHOLE file at the head 15-50 leaves behind, in particular `testAProofDoesNotOutliveItsRunIntoALaterRedo` and its real-`processDownload` staging: the stubbed transport failure, `makeStubbedURLSession` and the suspension paragraph deriving where the `defer` runs. That case is the nearest existing route to this arm and is the model for reaching it; note that 15-50 changes what the retirement retires, so the observable this case reads is not the one recorded here
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — at the head Task 1 leaves behind, to confirm whether any census counts a token this change moves in `+Execution.swift`
    - `.swiftlint.yml` — before any Swift edit; the 120-column limit and the 1000-line `file_length` ERROR limit
  </read_first>
  <action>
**Step 1 — derive the equivalence before writing the branch, and derive it as a claim that can be
wrong.** Work out what the current expression does for each input class: a non-nil generation equal
to the live one, a non-nil generation different from it, and `nil`. The optional comparison promotes
the right operand, so the pre-derived expectation is that `nil` compares unequal to every generation
and the predicate returns `true` — which an explicit early return of `true` reproduces exactly. Do
not stop at that expectation. State it as a claim, name the input class it covers, and record that
it is UNPROVEN until Step 4 executes it. This module has spent sixteen rounds discovering that a
claim derived from a written premise and never executed is precisely the class that rots; an
equivalence read off the type system is that class.

If the derivation finds ANY input whose disposition moves, stop treating this as a readability edit:
name the input, assess the direction against the cost asymmetry below, disposition it explicitly,
and report it as a behaviour change inside G-15-26's own guard rather than folding it in.

**Step 2 — write the branch, and write the direction with it.** Replace the implicit promotion with
an explicit early return for the generation-less case, positioned after the existing ownership guard
so the predicate still answers `false` for a gallery no live run owns. Then write the doc comment
this project's convention requires for a deliberate design, and make it carry the WHY rather than
the WHAT: a run that presents no generation cannot prove it owns this gallery's active slot, so it
is treated as superseded and retires nothing; that is the safe direction because leaving the entry
to the run that DOES own the slot costs one stale proof until that run exits, while dropping a live
successor's proof is the G-15-26 zero-progress card. Name the asymmetry as the reason rather than
asserting the choice. Keep the doc consistent with `retireProvenPageWork`'s own overlapping-run
paragraph, which already reasons about the same hazard from the caller's side — if the two now say
the same thing twice, say it once in the predicate and have the caller's doc point at it.

**Step 2b — decide what the sibling gets, and decide it rather than noticing it.** Reading
`isActiveTaskOwner` for Step 3 puts a second undocumented deliberate design in front of the
executor: its own nil arm requires that no task is live and that the active slot is either unclaimed
or already this gallery's, which is non-obvious and is exactly the class CLAUDE.md's deliberate-design
rule covers. It is IN SCOPE for this plan and folded in deliberately, for three reasons stated so the
scope decision is auditable: it is the same generator as WR-03 one function down, so closing one and
leaving the other is how a round hands the next round its own finding back; the file is already in
`files_modified` for WR-03, so nothing widens; and the change is doc-only, adding no branch and no
behaviour.

Derive its WHY from the callers rather than inventing one: enumerate every call of
`isActiveTaskOwner`, determine what a generation-less caller can and cannot know at each, and write
the rationale from that enumeration. If the enumeration does NOT establish a rationale — if source
genuinely does not say why the nil arm requires an idle slot — do not invent one: record that
finding explicitly, leave the arm undocumented, and raise it, because a fabricated WHY is worse than
an absent one and is the failure this whole item exists to prevent.

**Step 3 — record the convention evidence rather than arguing taste, and record it as source holds
it.** Quote the sibling predicate's explicit optional branch in the summary beside the rewritten
one. The justification is that two adjacent predicates over one optional input now answer it the
same way — a reader can tell that `nil` was considered in both — rather than a preference for
branches over promotions. State explicitly that the sibling's evidentiary value is the BRANCH alone:
it carries no rationale doc, `:315` is blank, and the doc block above it belongs to
`retireProvenPageWork`. The review and the gap record both describe it as carrying a recorded
rationale; that premise is wrong and this plan corrects it rather than repeating it, so the
summary's argument matches the quote it carries. If Step 2b lands a rationale on the sibling, say so
and date it to this plan — the sibling acquires one HERE, which is not the same as having had one.

**Step 4 — pin the arm, and prove the pin can fail.** Add one case that drives the generation-less
arm through a production route and asserts its disposition. The pre-derived staging, to be re-derived
against the head 15-50 leaves behind: install ownership for the gallery so a live run holds the slot
at some generation, then call the public `processDownload(gid:)` without a generation so its `defer`
reaches the predicate with `nil`, and assert that the run's retirement did NOT fire — the observable
being whatever 15-50's promoted store makes visible for that gallery, read through a production
push rather than a private seam. Assert the non-vacuity that keeps it honest: ownership really is
held, the generation really is absent, and the entry really existed before the exit.

Then take the sensitivity reading. Invert the new branch — return `false` for the generation-less
case instead of `true` — run the case, and OBSERVE IT FAIL. Restore the branch and observe it pass.
Quote both readings. This is the only thing that separates a pin from a case that would have been
green either way, and it is also what converts Step 1's derived equivalence into an executed one: a
case that fails on the inverted branch is a case that reaches the arm.

If no fixture can reach the arm through a production route, do not stage it through a private seam
and do not silently drop it: state the limitation explicitly and disposition it, exactly as 15-48
dispositioned the overlapping-run gating it could not stage. Check the destination file's line
budget BEFORE writing, and relocate on this phase's extension-file pattern if the case would cross
the limit.

**Step 5 — the residue sweep, before declaring the pair closed.** Four checks, each recorded.
(a) No census moved: confirm whether any table in the inventory suite counts a token this change
touches in `+Execution.swift`, and record the before and after value if one does. (b) No doc
anywhere still describes the generation-less disposition as falling out of a comparison, and no doc
in either module or the test target still describes the inventory scanner as reading every scanned
file including its own. (c) Both items of the pair are closed at their root rather than at their
symptom: state, for each, the omission that produced it and the written thing that now stands in its
place. (d) This plan created no new instance of its own generator — every invariant it introduced is
written, not implied.

**Step 6 — full green.** Run ONE full FeatureTests invocation with no filter as the plan's final
proof, and as the last invocation of the round. Record the total against the head this plan started
from and account for the movement case by case: the nil-arm case from this task, and nothing from
Task 1 unless the exclusion's proof left a case behind. Record it against 15-49's 880 as well, with
the round's whole movement summarised, since this is the last plan of the round and the next
verification will read that number.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The Step-1 derivation is recorded as a per-input-class table (equal non-nil, different non-nil, `nil`) with the current expression's result and the branch's result in each row, and it is labelled UNPROVEN until Step 4's reading; any row whose disposition moves is named and dispositioned as a behaviour change rather than absorbed.
    - The rewritten predicate is quoted in the summary beside the sibling predicate's explicit optional branch, and the doc comment is quoted in full and states the cost asymmetry — one stale proof until the owning run exits, versus dropping a live successor's proof — as the reason for the direction, not merely the direction itself.
    - The convention argument matches source: the summary states that the sibling's evidentiary value is the explicit BRANCH alone, records that it carries no rationale doc with `:315` blank and the block above it belonging to `retireProvenPageWork`, and names the review's and the gap record's contrary description as an inherited premise this plan corrected rather than repeated.
    - Step 2b is dispositioned rather than noticed: the sibling's nil arm either receives a rationale DERIVED from an enumeration of that predicate's callers — with the enumeration recorded and the new doc quoted and dated to this plan — or the failure to derive one is recorded, raised, and the arm deliberately left undocumented. No rationale is written that the caller enumeration does not support.
    - The scope decision for Step 2b is stated explicitly with its three reasons, and the summary confirms `files_modified` did not widen to accommodate it — the file was already listed for WR-03.
    - The predicate's doc and `retireProvenPageWork`'s overlapping-run paragraph do not now state the same rule twice; the summary states which one owns it and how the other refers to it.
    - The nil-arm case exists, reaches the arm through the public `processDownload` entry point with no generation, asserts its non-vacuity, and reads its observable through a production push rather than a private seam — or the fixture limitation is stated explicitly and dispositioned with no case staged through a private seam.
    - BOTH sensitivity readings are quoted: the FAIL with the branch inverted and the PASS with it restored, with the inversion described precisely enough to repeat. A case reported with only a green reading MUST NOT be described as pinning the arm.
    - The four residue checks are recorded with their results, including the per-item statement of the omission that produced it and the written thing now in its place.
    - The full FeatureTests run exits `0` in a single unfiltered invocation; its total is recorded against the starting head AND against 15-49's 880, with the round's whole movement summarised case by case.
    - No SwiftLint rule is suppressed, disabled or annotated away; `awk 'length($0)>120'` over every changed Swift file returns nothing and no changed file crosses 1000 lines.
    - No `xcodebuild` invocation in this plan overlapped another.
  </acceptance_criteria>
  <done>The generation-less disposition is a written branch carrying the asymmetry that justifies it, its equivalence with the promotion has been executed rather than read off the types, and a case that fails when the branch is inverted proves the arm is reached — so neither half of the pair is left standing on an omission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public entry point → retirement gate | `processDownload(gid:generation:)` is `public` with the generation defaulting to `nil`, so a caller outside the module reaches the gate carrying no proof of slot ownership, and the gate's answer for that input decides whether a live successor's state survives |
| Type system → policy | A behaviour reached by optional promotion is enforced by the compiler and readable by nobody: a later fix cannot tell an intended policy from an accident of the types, and neither can a reviewer |
| Guard scope → guarded claim | A scanner that reads its own file makes the claim it polices undocumentable, so the guard's scope silently decides what the codebase is allowed to say |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-53-01 | Repudiation | The generation-less policy readable only from the types, so a later fix "simplifies" it in the direction that drops a live successor's proof — the G-15-26 zero-progress card reached through a tidy-up | medium | mitigate | The policy is stated in an explicit branch with the cost asymmetry recorded as its reason, matching the sibling predicate's convention, and a case pins the arm's disposition |
| T-15-53-02 | Tampering | The written branch silently changing behaviour for some input while being presented as a readability edit inside G-15-26's own guard | high | mitigate | The equivalence is derived per input class, labelled unproven, then EXECUTED: a case that fails with the branch inverted is what converts the derivation into evidence, and any input whose disposition moves is reported as a change rather than absorbed |
| T-15-53-03 | Repudiation | The pin being green either way — a case added beside a change that would have passed without it, which is how a suite accumulates assertions that guard nothing | high | mitigate | Both sensitivity readings are required and quoted: FAIL with the branch inverted, PASS with it restored, with the inversion described precisely enough to repeat |
| T-15-53-04 | Tampering | Narrowing a census suite's scan and silently re-basing its tables — the third scan-scope change to this file in two rounds, landing on tables 15-50 moved and 15-52 added | high | mitigate | Every census is inventoried at THIS head before the change and re-derived after it with both values recorded; a number that legitimately moves carries a rewritten doc stating what it now pins against |
| T-15-53-05 | Repudiation | An exclusion that accidentally empties or breaks the walk looking exactly like a working one, so the prose rule silently stops guarding anything | high | mitigate | The exclusion is proven in BOTH directions — a pass with a plainly-worded instance inside the excluded file, and an observed FAIL naming a different scanned file with a temporary plant that is then removed |
| T-15-53-06 | Repudiation | A later round reading a newly non-zero repository grep for the retired phrasing as a regression, because the excluded file may now legitimately contain it | medium | mitigate | The Step-5 decision is recorded with the surviving instance's location, the fact that it sits inside the excluded file, and the statement that the prose assertion rather than a repository grep is the live guard |
| T-15-53-07 | Information Disclosure | Nothing user-visible is reached: the predicate is internal, the scanner is a test, and no argument is added to the card's localized key | low | accept | The residue sweep confirms the change set is one internal predicate, one test scanner and one test case |
</threat_model>

<verification>
- Between two and four `xcodebuild` invocations, strictly one at a time and never overlapping — a
  second concurrent invocation wedges `testmanagerd` on this machine and needs a reboot. Task 1
  takes the census suite targeted runs for its two-direction proof, including one EXPECTED non-zero
  exit against the temporary plant, which is the evidence rather than a failure. Task 2 takes the
  sensitivity reading with the branch inverted — also an expected non-zero exit — and then the
  single full unfiltered FeatureTests run, which must exit `0` and is the last invocation of the
  round.
- Spec-less probe fallback: visibly SKIPPED this round and recorded here as a deliberate choice —
  this phase has no requirement IDs to probe (`Requirements: None mapped`; its scope contract is the
  four success criteria), so no probe-derived edge or prohibition predicates were generated.
  `must_haves` derive from the G-15-33 record's two items and this round's standing constraints, in
  the style 15-46..15-49 established.
- <assumption_delta_decision>no-change — neither item changes an identity model. WR-03 makes an
  existing two-valued disposition explicit without adding a third case: the generation stays one
  optional integer with the same two answers, and nothing is added alongside the promotion because
  the promotion is REPLACED by the branch rather than kept beside it. WR-04 narrows a walk by one
  file and adds no second scanner, no second token set and no second rule. The round's one genuine
  delta is recorded in 15-50, where the run's proof is promoted from a membership to a quantity.
  </assumption_delta_decision>
- API coverage gate: the deterministic scan reports `detected: false` for this phase scope, so no
  coverage matrix is produced or extended and the existing `COVERAGE.md` is left untouched. No
  BackgroundTasks verb is reached by this plan at all.
- Schema-push gate: no ORM schema files exist in this repository; skipped.
- Security enforcement is active at ASVS L1 with a `high` blocking threshold. Four `high` threats
  are registered above and all four are dispositioned `mitigate` with a named mechanism and a named
  falsification; the one `accept` is `low`.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan and cannot be
  advanced by it. As the round's last plan, this summary is the right place to restate that the item
  survives the round open, on its own axis, and that closing G-15-30..G-15-33 does not discharge it.
</verification>

<success_criteria>
- G-15-33 closed at the root of both items rather than at their symptoms. WR-03: the nil-generation
  policy is a written branch positioned after the ownership guard, carrying a doc comment that gives
  the WHY as the cost asymmetry between one stale proof and a dropped live successor's proof; its
  equivalence with the optional promotion is derived per input class and then EXECUTED, with a case
  that reaches the arm through the public entry point and that has been observed failing with the
  branch inverted; any input whose disposition moved is reported as a behaviour change rather than
  absorbed; the convention argument rests on the sibling's written branch ALONE, with the inherited
  claim that it carries a recorded rationale corrected against source rather than repeated; and the
  sibling's own nil arm is dispositioned — given a WHY derived from its caller enumeration, or left
  undocumented with the failure to derive one recorded and raised. WR-04: the source-inventory scanner
  excludes itself by path in the sibling's shape and under the sibling's binding name, carrying a
  rationale AUTHORED IN THIS PLAN because the sibling records none — with the review's
  quoted-but-nonexistent rationale (`15-REVIEW.md:443-445`) and the gap record's inheritance of it
  stated as a finding in the summary rather than quietly worked around, and the sibling's two
  nearest real docs named as the different reason they actually record; the assembled fragments
  remain the first line of defence, the
  exclusion is proven in both directions, every census in the suite is re-derived at the head 15-50
  and 15-52 leave behind with each number quoted and any movement carrying a rewritten doc, and the
  Step-5 decision about the plainly-worded description is recorded with its consequence for any
  future repository grep. The full suite is green in one unfiltered invocation.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-53-SUMMARY.md` when done, including:
the census inventory taken at this head with each table's tree and asserting case; both
`scannedFiles()` implementations quoted side by side with the identical binding name; the
**false-premise finding stated explicitly** — that `15-REVIEW.md:443-445` quotes a rationale sentence
that exists nowhere in `DownloadLogPrivacyInvariantTests.swift`, that the G-15-33 record inherited
the quote, that the sibling's `scannedFiles()` carries no doc comment (`:253` MARK, `:254` blank,
`:255` extension opening) with the whole-file search returning nothing, that the sibling's `:45-46`
and `:73-75` record the assembled-fragment reason instead, and that this plan corrected the premise
rather than propagating it into a fourth artifact; the exclusion's doc quoted and identified as
authored here with the two-lines-of-defence reason and the comment-filter asymmetry; the
before and after value of every census with any movement's re-derivation and rewritten doc; the
known-member confirmation by execution; all three exclusion readings quoted in order with the plant
confirmed removed; the Step-5 decision with the surviving instance's location and the statement that
the prose assertion is the live guard; the per-input-class equivalence table with its unproven label
resolved; the rewritten predicate quoted beside the sibling's explicit branch, with the statement
that the sibling's evidentiary value is that branch alone and that the inherited recorded-rationale
claim was corrected against source; the Step-2b disposition with its caller enumeration and either
the sibling's new doc quoted and dated to this plan or the recorded failure to derive one; the
predicate's doc comment quoted in full; both sensitivity readings quoted with the inversion described; the nil-arm
case's non-vacuity assertions, or the fixture limitation with its disposition; the four
residue-sweep results including the per-item root statement; the rewritten `files_modified` set
replacing this plan's provisional candidate line; and the full-suite total against both the starting
head and 15-49's 880, with the round's whole movement summarised and the open device-UAT axis
restated.
</output>