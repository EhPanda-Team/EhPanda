---
phase: 15-continued-background-downloads
plan: 49
type: execute
wave: 49
depends_on: ["15-48"] # File overlap on DownloadSourceInventoryTests.swift (15-48 may add a census pin there, and this plan widens that suite's scan scope, which changes what every census in the file counts) and xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift
  # PROVISIONAL BELOW THIS LINE — the two paths that follow are the PRE-DERIVED candidate set for
  # the WR-05 decision: the only two suites that arm the gate, already located at
  # DownloadContinuedSessionIdentityTests.swift:33 and DownloadContinuedSessionInterleaveTests.swift:238.
  # Neither is touched if the DELETE remedy is taken (the default); at most one is touched if the
  # EXPOSE remedy is taken. This list MUST be rewritten in the SUMMARY to the set actually
  # modified, with the WR-05 criterion that put each one in or left it out.
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionIdentityTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-29]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "The retired single-authority claim is closed where it actually lives, not where the last correction happened to look: the two surviving test doc comments are rewritten from a FRESH enumeration of the shared read's callers performed in this plan, and the claim is then OWNED rather than corrected a third time — the census suite's scan is widened so the test target is inside the guard's reach, with a prose assertion that the retired phrase appears nowhere, its detection tokens assembled from fragments so a repository grep cannot match the check that enforces it."
    - "The replacement guard is FALSIFIED BEFORE IT IS TRUSTED: the widened scan and the prose assertion land BEFORE the two rewrites, and the assertion is observed failing against the still-retired docs with BOTH offending file paths named in the failure. That ordering is the only thing that can catch the guard's two silent-pass modes — a detection token mis-assembled from its fragments (unmatchable by grep by design, so invisible to inspection) and a widened scan that never reaches `DownloadContinuedSessionBasisTests.swift`, which the known-member guard cannot detect because it only proves the walk found something. Rewriting first would delete the only strings the assertion exists to catch and leave it permanently green-by-vacuity — the same shape as the scope failure that produced G-15-29."
    - "Widening the census scan is treated as the hazard it is: every existing census in that suite counts over the files it scans, so each is re-scoped to the client module explicitly while the new prose assertion runs over the whole widened set, and each pre-existing expected total is re-derived and asserted unchanged — a widened scan that silently re-baselines an existing census would replace one unowned claim with three."
    - "The dead page-progress counter is removed at its root with the emptiness the guard actually wants: the property, its increment and its assignment are deleted and the guard restated on the results collection, after a fresh grep over App, both package trees and the extension target proves no reader exists anywhere — Sources-only scoping is the exact reason the single-authority sentence survived a correction, so this derivation is deliberately wider than the module."
    - "The unreadable spy field is DECIDED rather than deferred: it is either exposed and asserted on by a named case that already arms the gate, or deleted together with both writes and the doc clause that promises a reader — with the decision's criterion recorded, deletion the default when neither arming case would assert on the parked arguments, and the gate's doc left saying only what the field can still deliver."
    - "The residue this plan itself leaves is swept before the gap is declared closed: no symbol is orphaned by the deletions, no census is left counting a scope it was not re-derived against, and no doc anywhere in either module or the test target still states the retired claim — the same sweep discipline the three preceding plans of this round carry."
    - { statement: "No device-observable behavior changes in this plan; the card's readings are unaffected and 15-UAT.md test 2 remains an independent open axis", verification: backstop }
  artifacts:
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      provides: "A widened scan whose per-census scoping is explicit, plus the prose assertion owning the retired single-authority claim across the test target"
      contains: "knownMembers"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift"
      provides: "The page-progress state without its dead counter, its guard restated on the results collection"
      contains: "results"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift"
      provides: "The progress gate carrying only the promise it can keep — either a readable parked update or no parked update at all"
      contains: "armProgressGate"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      to: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift"
      via: "the widened scan puts the test target inside the guard's reach, so a retired claim reappearing in a test doc fails a build instead of waiting for a review round"
      pattern: "repositoryRelativePath"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift"
      via: "the restored-index restoration and the early guard now read the same collection, so no second counter can drift from it"
      pattern: "restoredIndices"
  prohibitions:
    - statement: "Must NOT re-baseline an existing census while widening the scan: every pre-existing per-file table and joined total in that suite is re-derived under the new scoping and asserted unchanged, with the re-derivation recorded."
      status: pending
      verification: automated
    - statement: "Must NOT correct a doc sentence from the gap record's wording: both rewritten test doc comments are derived from a fresh caller enumeration performed in this plan, at the HEAD 15-48 leaves behind."
    - statement: "Must NOT rewrite either retired doc comment before the widened scan and the prose assertion have been landed and observed FAILING on both of them: an assertion whose first run is green has never been shown to reach the claim it owns, and the rewrite is what destroys the evidence."
      status: pending
      verification: automated
    - statement: "Must NOT scope the dead-counter reader derivation to the client module: the grep covers App, both package source and test trees and the extension target, because module-scoped derivation is the recorded generator of the very item beside it."
      status: pending
      verification: automated
    - statement: "Must NOT leave the spy's gate doc promising a reader that does not exist: whichever remedy lands, the doc states only what the field can deliver afterwards."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<!-- planner-discipline-allow: completedCount -->
<!-- planner-discipline-allow: inFlightProgressUpdate -->
<!-- planner-discipline-allow: schedulableDownloads -->

<objective>
Close G-15-29 (warning, hygiene group — grouped because this project's bar treats nitpicks as
blocking, and because its first item is the same doc-vs-source generator that has produced a
finding in six consecutive rounds). All three items confirmed verbatim in source at HEAD
6a0059d4:

- **WR-03 — the retired single-authority sentence survives in the TEST suite, where the new
  census structurally cannot see it.** `DownloadContinuedSessionBasisTests.swift:257` still names
  the shared schedulable read as the sole authority for selecting work the scheduler can run, and
  `DownloadPendingWorkTests.swift:26` echoes the same premise. Source disagrees for exactly the
  reason it disagreed in the two files 15-44 corrected: the scheduler performs its own indexed
  read and never calls that function. The guard 15-44 added cannot catch either, because
  `DownloadSourceInventoryTests` scans the single path `AppPackage/Sources/DownloadClient`
  (`:36`) and its executable-line filter drops every comment line.
- **WR-04 — the page-progress counter became dead state when 15-45 removed its last reader.**
  Four lines survive in the module: the declaration (`+PageDownload.swift:12`), the assignment to
  the results count (`:93`), the guard one line later (`:94`) and the increment (`:250`). Nothing
  reads the incremented value, so the increment is a pure dead write and the guard is a
  restatement of the results collection being non-empty. The 15-45 plan reasoned about both
  writers to justify dropping a prefix and then left behind the state that made the prefix
  misleading.
- **WR-05 — the client spy records an in-flight progress update no assertion can read.** Three
  occurrences in the whole test target — a declaration, a write and a clear — and no accessor,
  while the gate's own doc promises the call records its complete argument set before signalling
  entry. With no reader, a case parked on the gate cannot in fact inspect the parked arguments.

Purpose: the review surface for the next verification round starts clean, and the claim that has
now been wrong six rounds running becomes owned by a build rather than by a comment (SC2's
progress-reporting surfaces, SC3's schedulable-work basis).
Output: two rewritten test docs derived from a fresh enumeration, a widened census that owns the
claim without re-baselining its siblings, the dead counter removed at its root, the spy field
decided, and a green full run.
</objective>

## Artifacts this plan produces

- **A widened scan in `DownloadSourceInventoryTests`** — the scanned directory becomes a list
  that includes the downloads test target, with a known-member entry per scanned directory so a
  vacuous walk still cannot pass, and per-census scoping made explicit so each existing table
  keeps counting exactly what it counted before.
- **A new prose assertion** in that suite owning the retired single-authority claim across every
  scanned file, with detection tokens assembled from fragments on the file's established pattern.
- **Two rewritten doc comments** in `DownloadContinuedSessionBasisTests.swift` and
  `DownloadPendingWorkTests.swift`.
- **Deletions in `DownloadClient+PageDownload.swift`** — the counter property, its assignment and
  its increment, with the guard restated on the results collection.
- **A decided outcome for the spy's parked-update field** — either an accessor plus an assertion
  in a named arming case, or the field, both its writes and the doc clause deleted.
- **No new production symbols.**

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the census to own the retired claims, prove it RED against them, then rewrite both from a fresh enumeration</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-29 entry's first item WHOLE, and the G-15-24 closure record in `re_verification`, which states the shape 15-44 landed and names what it did NOT cover
    - `AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` — `hasPendingWork()` (`:10-15`) and the shared schedulable read's doc (`:17-41` and onward): the corrected shape to mirror, including its three-caller census, its explicit statement that the scheduler is not one of those callers, the scheduler's own read named, and the inert active-gallery divergence with the condition that re-opens it
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `scheduleNextIfNeededCore` and its reads (`queueStore.gids`, the indexed reads, and the predicate reached through the next-queued and next-unqueued selectors): the source of the disagreement, re-derived at THIS head rather than read from the gap record
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift` — the doc comment at the `:254-268` region carrying the retired claim, and its case, so the rewrite keeps the case's actual subject intact
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift` — the doc comment at the `:26-35` region carrying the echo
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the whole file (312 lines): the suite header's rationale, the scanned-directory constant (`:36`), the known-member guard (`:37-41`), the repository-root markers, the fragment-assembled detection tokens (`:44-49`), every per-file expected table with its separately counted joined total, the call-site and mutation counters (`:204-230`), the executable-line filter (`:231-238`), and the scanner (`:246-274`)
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**Step 1 — the enumeration, before any sentence is written.** Grep every call of the shared
schedulable read across `AppPackage/Sources` at the head 15-48 leaves behind, and classify each:
declaration, calls, doc-comment mentions. Separately derive, from the scheduler's own core, which
reads IT performs and which predicate the two genuinely share. Record both derivations. Every
sentence written in Step 2 comes from this enumeration; none comes from the gap record's wording,
and none from the corrected sentence 15-44 already landed — that one is a model for SHAPE, not a
source of facts, and it must itself be re-verified as still true at this head before it is
mirrored.

**Why the guard is built BEFORE the rewrite.** The rewrites are the only strings in the repository
that can make the new assertion fail. Landing them first would leave the assertion permanently
unfalsified: it would go green on its first run with nothing left to catch, and two silent-pass
modes would ship undetected — a detection token mis-assembled from its fragments (the file's
convention deliberately makes the token unmatchable by a repository grep, so a wrong assembly is
invisible by inspection), and a widened scan whose new directory entry never actually reaches
`DownloadContinuedSessionBasisTests.swift`. The known-member guard cannot cover the second: it
proves the walk found SOMETHING, not that it found the file carrying the claim. G-15-29 exists
because a guard's scope could not see where the claim lived; shipping the replacement guard
unfalsified would repeat that exactly. So the scan and the assertion land first, are observed RED
against the un-rewritten docs, and only then are the docs rewritten.

**Step 2 — widen the scan, treating the widening as the hazard it is.** Turn the single scanned
directory into a list that also covers the downloads test target, and add a known-member entry
for the new directory so a walk that silently found nothing still cannot pass vacuously. Then
handle the consequence before anything else: every existing census in the suite counts over the
files the scanner returns, so each must be explicitly scoped to the client module's files, and
each pre-existing per-file table and joined total must be re-derived under the new scoping and
asserted UNCHANGED. Record each re-derivation. A widened scan that quietly re-baselines an
existing census would trade one unowned claim for three, which is the residue class this round
exists to stop.

**Step 3 — own the claim.** Add a prose assertion over the whole widened set: the retired
single-authority phrasing appears in no scanned file. Assemble its detection tokens from
fragments, exactly as the suite's existing tokens are, so a repository grep counting the phrase
cannot match the check that enforces its absence. Make the assertion report the OFFENDING FILE
PATHS in its failure message, not just a count — Step 4 reads those paths as its evidence, and a
bare count cannot show that the scan reached the file carrying the claim. Write the assertion's
own doc comment to say what a failure means: source or a doc moved, and the sentence must be
re-derived before the check is changed.

**Step 4 — the honest RED, before a single sentence is rewritten.** The two retired doc comments
are still in place at this point; that is the whole design. Run the census suite once, targeted.
The new assertion MUST FAIL, and its failure MUST name BOTH offending files —
`DownloadContinuedSessionBasisTests.swift` AND `DownloadPendingWorkTests.swift`. Quote the failure
verbatim in the summary with both paths visible. That single reading falsifies both silent-pass
modes at once: a mis-assembled token would have let the assertion pass against the un-rewritten
docs, and a scan that never reached the basis suite would have named only one file. Every
pre-existing census in the suite MUST pass in this same run — the re-derivation is already done
and the widening must not have moved any of them.

If the assertion PASSES here, STOP. Do not rewrite the docs and do not proceed: re-derive whether
the token assembles to the phrase actually present in those files, and whether the walk reaches
them at all, and record the finding. If it fails naming only ONE file, the scan does not reach the
other — same stop, same re-derivation.

If for any reason the rewrites cannot be held back until after this reading, the equivalent
obligation still applies in the other direction: after rewriting, temporarily reinstate one
retired sentence, observe and quote the assertion failing on that file, then remove it again and
quote the green. A green-only assertion is not accepted either way.

**Step 5 — the two rewrites.** Rewrite the basis-suite doc comment and the pending-work doc
comment to the shape 15-44 landed: the real caller list, an explicit statement that the scheduler
is not among them, what the two actually share, and where the scheduler's own read lives. Every
sentence comes from the Step-1 enumeration. Keep each comment's original SUBJECT — both docs exist
to explain their own case, and the correction replaces a false premise rather than the
explanation. Do not import the production doc verbatim; these are test docs and must read as such.

**Step 6 — targeted proof, green.** Run the census suite and the two edited suites in ONE targeted
invocation. The assertion that was RED in Step 4 must now PASS, every re-derived census must pass
unchanged, and both edited suites must pass. Record the green beside the Step-4 RED.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests</automated>
  </verify>
  <acceptance_criteria>
    - The retired claim is gone from the test target: `grep -rc 'one authority' AppPackage/Tests` sums to `0` across all matches, and the census suite's own fragment-assembled token is confirmed not to contribute to that count.
    - Both rewritten doc comments are quoted in the summary beside the Step-1 enumeration that produced them, and each states the caller list, that the scheduler is not among them, what the two share, and where the scheduler's own read lives.
    - The Step-1 enumeration is recorded with its counts, derived at this head rather than imported, including the re-verification that 15-44's landed sentence is still true.
    - The scan is widened with a known member per directory: `grep -c 'knownMembers' AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` outputs at least `2`, and the new directory's known member is quoted.
    - Every pre-existing census is re-derived and unchanged: each per-file table and each joined total is named in the summary with its value before and after the widening, and the two values are equal for every one.
    - The new prose assertion exists and its tokens are fragment-assembled: the assertion's test name is recorded and its token construction is quoted.
    - The assertion is OBSERVED RED before either doc is rewritten, and the failure is quoted verbatim with BOTH offending paths visible in it — `DownloadContinuedSessionBasisTests.swift` and `DownloadPendingWorkTests.swift`. One path only, or no failure at all, is a STOP with the re-derivation recorded. A green-first assertion MUST NOT be reported as owning the claim: this single reading is what proves the token assembles to the phrase that is really there and that the widened scan actually reaches the file carrying it. (If the fallback ordering was taken instead, the equivalent reading is the quoted failure against one temporarily reinstated sentence, plus the quoted green after its removal, and the reason the primary ordering was not possible.)
    - The order of the two readings is recorded: the RED came from a run made while both retired doc comments were still in place, and the green came from a run after the rewrites — stated explicitly, since a RED taken after the rewrites proves nothing.
    - The Step-6 targeted census run exits `0` in a single invocation; the Step-4 run is expected to exit non-zero and its non-zero exit is the evidence, not a failure of the plan. Neither run overlaps another invocation.
  </acceptance_criteria>
  <done>Both retired claims are rewritten from a fresh enumeration and are now owned by a build-time assertion that was observed FAILING on both of them before either was touched — so the guard is known to reach where the claim lives and to detect it — with every pre-existing census re-derived and proven unmoved by the widening.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the dead page counter, decide the spy's parked update, sweep the residue, and prove the plan green</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-29 entry's second and third items WHOLE, and the G-15-25 closure record in `re_verification`, which explains how the second item became residue of that plan's own fix
    - `AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift` — the progress state's declaration (the `:10-20` region), `restoredIndices` and its surroundings (`:30-56`), `initializePageDownloadState` (`:76-104`) with its assignment and the guard one line later, and the increment in the page-outcome handler (the `:245-255` region): derive what the guard is actually protecting before restating it
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift` — the spy's private state struct (the `:155-170` region), its public accessors (`:174-190`), `armProgressGate` and its doc (`:211-228`), and the progress-update closure with the parked write and clear (the `:304-333` region)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionIdentityTests.swift` — the case arming the gate at `:33`: what it parks for and whether it would assert on the parked arguments
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift` — the case arming the gate at `:238`: the same question
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**Step 1 — the reader derivation, deliberately wider than the module.** Grep the page-progress
counter's name across `App`, `AppPackage/Sources`, `AppPackage/Tests` and `ShareExtension`, and
classify every match, excluding the three similarly-named symbols that are not it (the record's
completed page count, its display variant, and the unit count on the progress model). Record the
classification. Module-scoped derivation is precisely why the sentence in Task 1 survived a
correction, so this one is repository-wide by construction. If a reader exists anywhere, STOP:
the property is not dead and the item's remedy changes.

**Step 2 — the removal.** Delete the `completedCount` property, its assignment and its increment,
and restate the early guard on the results collection being non-empty — which Step 1's reading
proves is what the guard has always tested, since the assignment one line above it sets the
counter from that collection's count. Record the equivalence derivation beside the rewritten
guard. Nothing downstream moves.

**Step 3 — decide the parked update.** From the two arming cases, derive whether either would
assert on the parked arguments: what each parks for, and whether an assertion on the parked
values would pin anything the case does not already pin. Then take one of two remedies and record
the criterion that selected it. (a) EXPOSE: add an accessor on the spy's established
lock-and-read pattern and add the assertion to the named arming case that benefits, so the doc's
promise becomes true. (b) DELETE: remove `inFlightProgressUpdate`, both its writes and the clause
in the gate's doc that promises a reader, leaving the doc stating only the parking behaviour the
gate still delivers. Deletion is the DEFAULT and is taken when neither arming case would assert
on the parked values; expose is taken only when the derivation shows a real assertion, and then
the assertion's case is named. Either way the gate's doc afterwards says only what the gate can
deliver. The two arming suites are already pre-derived in this plan's `files_modified` under its
PROVISIONAL marker; rewrite that list in the summary to the set actually modified, with the
criterion that put each one in or left it out.

**Step 4 — the residue sweep, before declaring the gap closed.** Four checks, each recorded.
(a) Orphaned symbols: after the removals, grep each deleted name repository-wide and confirm a
zero sum; grep anything whose only purpose was to feed a deleted write and record its remaining
caller count. (b) Newly-dead state: confirm every remaining member of the page-progress struct
and of the spy's state struct has a reader, stated per member with its count — this is exactly
the class WR-04 belongs to, and this plan must not create another instance of it. (c) The same
claim surviving outside scope: grep both modules and the test target for any doc still describing
the deleted counter as live or the spy field as readable. (d) The census's own reach: confirm
Task 1's widened scan sees the files this task edited, so a future reintroduction of either
retired claim would be caught rather than reviewed.

**Step 5 — green.** Run ONE full FeatureTests invocation as the plan's final proof. Record the
total against 15-48's and explain any movement (the expose remedy adds an assertion, not a case;
the delete remedy adds none).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The Step-1 classification is in the summary covering all four scanned trees, with the three similarly-named symbols explicitly excluded, or the plan stopped on a discovered reader.
    - The counter is gone: `grep -rc 'completedCount' App AppPackage ShareExtension` sums to `0` across all matches after excluding the three similarly-named symbols, with the exclusion method stated.
    - The restated guard is quoted in the summary beside the equivalence derivation that justifies it.
    - The spy decision is recorded with its selecting criterion and its per-case derivation. If deleted: `grep -rc 'inFlightProgressUpdate' AppPackage/Tests` sums to `0` and the removed doc clause is quoted. If exposed: the accessor and the asserting case are both named and the assertion is quoted.
    - The gate's doc after the change is quoted, and it promises only what the gate delivers.
    - The residue sweep's four checks are each recorded with their grep results, including the per-member reader counts for both structs and the census-reach confirmation.
    - The full FeatureTests run exits `0` in a single invocation, with the total count recorded against 15-48's and any movement explained.
  </acceptance_criteria>
  <done>The dead counter is gone with its guard restated on what it always tested, the spy's parked update carries a deliberate recorded decision, and the residue sweep proves this plan created no new instance of either class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc claim → build guard | A load-bearing invariant stated only in a comment is correct on the day it is written and unowned forever after; the guard's SCOPE is what decides whether a drift fails a build or waits for a review round |
| Census scope → census meaning | Widening a scanned set silently changes what every table in that suite counts, so a scope change is a change to every claim the suite owns |
| Dead state → future reader | A written-but-unread counter invites a new reader that trusts a value nothing maintains, which is how the removed prefix became misleading in the first place |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-49-01 | Repudiation | The retired single-authority claim surviving a third correction because the guard's scope still cannot see where it lives | medium | mitigate | The scan is widened to the test target with a known-member guard, and a fragment-assembled prose assertion owns the claim over the whole set |
| T-15-49-06 | Repudiation | The replacement guard shipping unfalsified — a mis-assembled token or a scan that never reaches the basis suite, either of which passes silently once the rewrites remove the only matching strings | high | mitigate | The scan and the assertion land before the rewrites and the assertion is observed RED against the still-retired docs, with BOTH offending paths required in the quoted failure; one path, or no failure, stops the plan |
| T-15-49-02 | Tampering | The widening silently re-baselining the suite's existing censuses, replacing one unowned claim with three | high | mitigate | Every census is explicitly re-scoped to the client module and every pre-existing table and total is re-derived and asserted unchanged, with both values recorded |
| T-15-49-03 | Tampering | A future caller trusting the dead counter as a maintained value | low | mitigate | The property, its assignment and its increment are deleted; the guard is restated on the collection it always tested |
| T-15-49-04 | Repudiation | The gate's doc continuing to promise a parked-argument reader that does not exist, so a later case is written against a capability the spy lacks | medium | mitigate | The field is decided expose-or-delete with a recorded criterion, and the doc afterwards states only what the gate delivers |
| T-15-49-05 | Repudiation | The dead-reader derivation scoped to the module and missing a reader in another target | medium | mitigate | The grep covers App, both package trees and the extension target by construction, and a discovered reader stops the removal |
</threat_model>

<verification>
- Task 1 runs TWO targeted census invocations, strictly one at a time: the Step-4 RED reading
  (expected non-zero exit, taken while both retired docs are still in place) and the Step-6 green
  after the rewrites. Task 2 runs the plan's single full FeatureTests invocation. xcodebuild
  invocations must never overlap on this machine.
- Spec-less probe fallback: visibly SKIPPED this round and recorded here as a deliberate choice —
  the phase has no requirement IDs to probe (`Requirements: None mapped`), so no probe-derived
  edge or prohibition predicates were generated. `must_haves` derive from G-15-29's three grouped
  items and the SC labels the touched surfaces back (SC2's progress-reporting surfaces, SC3's
  schedulable-work basis).
- <assumption_delta_decision>no-change — hygiene closures and a guard-scope widening; no identity
  model changes shape. The round's one genuine delta is recorded in 15-48.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan and nothing here
  can discharge it; it remains an independent open axis under `human_verification`.
</verification>

<success_criteria>
- G-15-29 closed: both retired claims rewritten from a fresh in-plan enumeration and owned by a
  widened census whose siblings are proven unmoved and whose new assertion was observed FAILING on
  both retired sentences before either was touched, the dead page counter removed at its root
  with its guard restated on what it always tested, the spy's parked update decided with a
  recorded criterion and its doc left honest, the residue sweep's four checks recorded, and the
  full suite green in one invocation.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-49-SUMMARY.md` when done, including
the caller enumeration and both rewritten doc comments quoted beside it, the assertion's RED
reading quoted with both offending paths visible together with the green that followed the
rewrites, the before/after values of every pre-existing census under the widened scan, the
repository-wide dead-reader classification with its exclusion method, the spy decision with its
selecting criterion, the rewritten `files_modified` set replacing this plan's provisional
candidate list, and the four residue-sweep results.
</output>
