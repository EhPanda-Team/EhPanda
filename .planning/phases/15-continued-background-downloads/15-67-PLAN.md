---
phase: 15-continued-background-downloads
plan: 67
type: execute
wave: 67
depends_on: ["15-66"]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadValidationRejectionArmTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift
autonomous: true
gap_closure: true
gap_ids: [WR-01, WR-02]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "The all-or-nothing wholesale guard is measured over the COMBINED prospective blank set — positive absences plus surviving rejections — at every blanking entry point, so a per-page hold can never relax the threshold below what the guard previously refused."
    - "On the automatic repair-seed route, a positively refuted surviving file is removed only AFTER the combined guard authorizes, and its hash is blanked only when the file is verifiably gone — the same classify-authorize-remove ordering validateImageData already has, so no record claims a page over positively refuted bytes indefinitely."
    - "The doc claims that rest on rejections being deleted (PageFileScan's membership doc and the blanking loop's line 2b / guard prose) read true against source after the change."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "Combined-basis wholesale guard and seed-route classify-authorize-remove ordering"
      contains: "rejectedPageRelativePaths"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadValidationRejectionArmTests.swift"
      provides: "Mixed rejected-plus-absent regressions crossing the wholesale threshold from both sides"
      contains: "mixed"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      via: "authorized surviving rejections are removed through the store's removeRefutedPageFiles before the blanking loop runs, mirroring PersistenceNormalize's validated-record ordering"
      pattern: "removeRefutedPageFiles"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift"
      via: "one evidence rule at both blanking entry points: prospective set = positive absences ∪ positively refuted survivors, guarded before any destructive act"
      pattern: "prospectiveBlank"
---

<objective>
Close WR-01 and WR-02 (verification gap 2's evidence-ordering half). WR-01: the fourth clause at `reconcileWorkingManifestAgainstPageFiles` (ExecutionSupport.swift:704) skips rejected-surviving pages from blanking but thereby shrinks `blankedPageCount`, which the wholesale guard at line 719 compares against `manifest.completedPageCount` — a mixed rejected-plus-absent shape can now drop BELOW the all-or-nothing threshold and license blanking the guard previously refused, contradicting 15-62's "can only blank less". WR-02: `probeAssetFileContent` (DownloadStore.swift:949-957) returns a surviving rejection unconditionally, and `removeRefutedPageFiles` is reachable only from the validated-record pass — so on the automatic routes (`prepareWorkingSeed`, `blankingPass`) a positively refuted file keeps its claimed non-empty hash indefinitely, and two doc claims that rest on rejections being deleted are false.

Resolve both with ONE evidence rule, the choice the verification's option space and the AGENTS.md positive-signal invariant both point at: the seed route adopts the validated-record pass's classify-authorize-remove ordering (`PersistenceNormalize.swift:262-320` is the in-repo precedent), and the shared loop's internal guard measures over the combined positively-refuted population so no caller can relax the threshold. Positive refutations (absent, zero-byte, non-regular, positively-empty content) converge the record and the disk in the same authorized act; non-answers (unprobed, read failures) stay per-page holds exactly as today.

Purpose: the automatic repair path neither launders refuted bytes into recorded truth nor blanks under a guard the hold quietly relaxed (SC2/SC3).
Output: combined-basis guard, seed-route removal ordering, corrected docs, and mixed-shape regressions pinned from both sides of the threshold.
</objective>

## Artifacts this phase produces

- Revised `reconcileWorkingManifestAgainstPageFiles` internal guard: the wholesale comparison counts blankable absences PLUS rejected-surviving claimed pages (never unprobed holds); loop stays non-removing.
- Seed-route ordering in `prepareWorkingSeed`'s reconciliation step: non-mutating scan → derive refuted survivors (claimed ∩ `rejectedPageRelativePaths` keys ∖ `unprobedPages`) → combined guard → `storage.removeRefutedPageFiles` → fresh scan → blanking loop, all inside the EXISTING D-G7-01 bracket (no new nested bracket).
- Corrected docs: `DownloadStore.swift:96-100` (PageFileScan membership; names the content-read exit as the never-deleting exception), `DownloadClient+ExecutionSupport.swift:637-647` (line 2b) and 648-656 / 711-718 (guard basis prose).
- New regressions in `DownloadValidationRejectionArmTests` (and seed-route arcs in `DownloadCoordinatorRepairSeedTests` if the staging fits there better): mixed rejected-plus-absent shapes on both sides of the wholesale threshold.
- No new production files or public API.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-62-SUMMARY.md
@.planning/phases/15-continued-background-downloads/15-66-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the mixed rejected-plus-absent shape on both sides of the wholesale threshold</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadValidationRejectionArmTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadValidationRejectionArmTests.swift` — extend this suite; reuse its staged zero-byte fixtures, temp storage roots, and file-survival assertions (`testAWholesaleZeroBytePageRefusesWithItsFileStillOnDisk` is the refusal-side template).
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift` — the production idioms for driving a repair run's working-seed preparation; use them if the seed-route arcs need a run start.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — the loop (692-735), its fourth clause (704), the wholesale guard (719), and `prepareWorkingSeed`'s scan (324): the exact code whose pre-fix behavior the RED cases must observe.
    - `AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift` — the validated-record ordering (262-320): the behavior contract the seed route must converge to.
    - `.planning/phases/15-continued-background-downloads/15-REVIEW.md` — WR-01's concrete two-page counterexample and WR-02's asymmetry description.
  </read_first>
  <behavior>
    - REFUSAL SIDE (two claimed pages): page 1's file is zero bytes (a rejected survivor under the seed route's non-mutating scan after this plan's conversion; pre-fix, under the discarding scan, the deleting probe plus the fourth clause produce the WR-01 relaxation), page 2's file is absent. Driving the repair-seed preparation must blank NOTHING, remove NOTHING, and leave page 1's file on disk — the combined prospective count equals the completed count, so the all-or-nothing guard refuses. Pre-fix this case is RED: page 2's hash is blanked because the shrunken count passes the guard.
    - AUTHORIZED SIDE (three claimed pages): page 1 zero-byte, page 2 absent, page 3 usable. The preparation must remove page 1's refuted file, blank pages 1 and 2, and keep page 3's hash and file. Pre-fix this case is RED: page 1 keeps its claimed hash beside refuted bytes (the WR-02 laundering mirror) or is silently deleted-without-record depending on the probe's discard outcome.
    - Both cases assert the no-laundering structural pin from the existing suites: no blank-hash page keeps a file on disk, and no non-blank claimed page's file was removed.
  </behavior>
  <action>
Add the two mixed-shape arcs as a crossing fixture pair — same staging helper, differing only in the third page — so the discontinuity at the wholesale threshold is crossed by construction and each side is asserted per regime (this phase's piecewise rule). Drive them through the production repair path the seed tests already use; assert end state through public API plus direct filesystem checks under the temp root (file exists / file gone / manifest page hashes). If the RED observation for the refusal side depends on the pre-fix discarding scan deleting page 1 first, record the observed pre-fix sequence verbatim in the summary rather than adjusting the fixture to force a prettier failure. Do not touch `DownloadFeatureTestHelpers.swift` (989 of 1000 lines) — keep new staging helpers file-private, or split that file first as a pure move if sharing is unavoidable.
  </action>
  <verify>
    <automated>! xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadValidationRejectionArmTests -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests</automated>
  </verify>
  <acceptance_criteria>
    - The refusal-side case fails pre-fix by observing a blanked hash (or removed file) the combined guard must refuse.
    - The authorized-side case fails pre-fix by observing page 1's claimed hash surviving beside its refuted file after the preparation completes.
    - The two cases share staging and differ only in the crossing page, and each asserts its own regime's complete end state (hashes, files, and derived display state on a fresh coordinator).
    - Existing rejection-arm and repair-seed cases are not modified in this task.
  </acceptance_criteria>
  <done>Both WR-01's threshold relaxation and WR-02's indefinite claimed-hash-over-refuted-bytes are deterministic RED observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — combined guard basis and seed-route classify-authorize-remove ordering</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadStore.swift</files>
  <read_first>
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeed` (300-340, entirely inside one `withdrawingCountedBasisMovement` bracket), the loop and guard (692-735), and the doc blocks 637-656 / 711-718 that must be re-derived.
    - `AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift` — the precedent to mirror: `refutedPages` derivation (269-273), `prospectiveBlankPages` guard BEFORE any act (274-279), `removeRefutedPageFiles` + `unremovedPages` complement (282-293), post-removal `blankingPass` (304-320). The validated-record pass itself must NOT change.
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `removeRefutedPageFiles` (347-372): the store primitive the seed route reuses; note its `unremovedPages` return.
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `PageFileScan` membership doc (85-100) and `probeAssetFileContent` (942-957): the doc corrections' targets; `probeAssetFileContent`'s never-deleting behavior itself stays.
    - `.planning/phases/15-continued-background-downloads/15-66-SUMMARY.md` — the caller disposition table; this plan converts the seed-route site 15-66 marked as cross-referenced.
  </read_first>
  <behavior>
    - Loop guard: the wholesale comparison refuses when blankable absences PLUS rejected-surviving claimed pages together reach `manifest.completedPageCount`; unprobed holds are never counted (they are non-answers, and counting them would refuse genuine absences — the distinction the current line-3 doc already draws).
    - Seed route: the scan at the reconciliation point is non-mutating; refuted survivors are derived as claimed ∩ rejected ∖ unprobed; the combined guard runs BEFORE any removal; on authorization, `storage.removeRefutedPageFiles` removes the survivors, a fresh scan reflects the removals, and the existing loop blanks — removed refutations become positive absences, unremoved files keep hash AND file (the existing per-page protection).
    - On refusal, the disk is exactly as the scan found it: no file removed, no hash blanked, manifest handed back verbatim.
    - Everything runs inside `prepareWorkingSeed`'s existing D-G7-01 bracket; no new bracket is nested (sibling rule at ExecutionSupport.swift:262-268).
    - The validated-record pass (`PersistenceNormalize`) is byte-for-byte unchanged; its post-removal loop invocation still sees unremoved survivors as holds and its combined guard already precedes its removal.
  </behavior>
  <action>
Change the loop's guard basis: compute the rejected-surviving claimed count alongside `blankedPageCount` and refuse when their sum reaches `manifest.completedPageCount`. Convert the seed-route reconciliation to the ordering above, mirroring `PersistenceNormalize.swift:262-320` with the presence-only evidence class (no content scan on this route), reusing `storage.removeRefutedPageFiles` and taking a fresh `storage.pageFileScan` after removal; flip the seed-route scan's discarding argument to the non-mutating value and update the 15-66 cross-reference comment at that site to state the completed conversion. Verify the outer/inner guard predicates cannot disagree: the inner combined basis over the post-removal scan must be bounded by the pre-removal prospective count, and record that argument in the summary. Re-derive the three doc blocks: `DownloadStore.swift:96-100` (membership doc — name the content-read exit as the exception that never deletes, and drop the byte-for-byte claim WR-02 refuted), `ExecutionSupport.swift:637-647` (line 2b — the caller ordering now exists on BOTH routes), and 648-656 plus 711-718 (guard prose — combined positively-refuted basis; unprobed holds explicitly excluded, with the existing rationale kept). Sweep both callers of the loop (`prepareWorkingSeed`, `blankingPass`) and enumerate each with its post-change evidence ordering in the summary; if any other blanking-adjacent consumer reads `rejectedPageRelativePaths` (grep the symbol across AppPackage/Sources), give it a disposition too.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadValidationRejectionArmTests -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests</automated>
  </verify>
  <acceptance_criteria>
    - Refusal-side mixed case green: nothing blanked, nothing removed, zero-byte file intact, manifest verbatim.
    - Authorized-side mixed case green: refuted file removed and its hash blanked in the same preparation, absence blanked, usable page untouched.
    - Existing arms green unchanged: both wholesale zero-byte/non-regular refusal cases, the authorized single-page rejection case, and 15-58's content-arm and 15-56's presence-arm suites in the full run.
    - `rg -n 'removeRefutedPageFiles' AppPackage/Sources/DownloadClient` shows exactly two call sites: the validated-record pass and the seed route.
    - The three rewritten doc blocks are re-audited clause by clause against source in the summary (no claim asserted that the code does not exhibit).
    - No SwiftLint suppression or concurrency escape hatch is introduced.
  </acceptance_criteria>
  <done>One evidence rule governs both blanking entry points: combined guard before any act, removal only under authorization, blanks only where the disk verifiably agrees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| damaged/foreign files on disk → recorded hashes | Positive refutations must converge record and disk in one authorized act; a systematically wrong pipeline must hit the all-or-nothing refusal, never a quietly relaxed threshold. |
| per-page hold → wholesale guard arithmetic | A protection that shrinks the guard's numerator must not license destruction the guard previously refused. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-67-01 | Tampering | wholesale guard basis | high | mitigate | Guard measured over the combined positively-refuted population at the loop itself, so every present and future caller inherits the threshold; crossing-fixture pins both sides. |
| T-15-67-02 | Tampering | claimed hash over refuted bytes (D-SSOT-04 mirror) | high | mitigate | Seed route removes authorized survivors before blanking, matching the validated-record ordering; unremoved files keep hash and file. |
| T-15-67-03 | Denial of Service | over-eager refusal of genuine absences | medium | mitigate | Unprobed holds stay excluded from the guard basis; existing single-page and mixed authorized arms prove genuine absences still blank. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadValidationRejectionArmTests -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1567DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC2/SC3 | Honest completion claims; no lost or laundered work | 15-67 | COVERED | Record and disk converge under one guarded evidence rule on the automatic route. |
| REQ | — | No Phase 15 requirement IDs are mapped | — | EXCLUDED | SC labels are the phase contract. |
| CONTEXT | AGENTS.md manifest-SSOT | Positive evidence licenses blanking with the refuted file removed under the same guards | 15-67 | COVERED | The seed route now honors the invariant's removal clause; holds stay holds. |
| VERIFICATION | gap 2 / WR-01 | Per-page hold relaxes the wholesale threshold | 15-67 | COVERED | Combined guard basis at the loop; mixed-shape crossing fixture. |
| VERIFICATION | gap 2 / WR-02 | Surviving refutation keeps claimed hash indefinitely; two false doc claims | 15-67 | COVERED | Classify-authorize-remove on the seed route; both docs corrected and re-audited. |

<success_criteria>
- The wholesale guard cannot be relaxed by any per-page hold, at any caller, by construction.
- An authorized automatic reconciliation leaves no claimed hash beside positively refuted bytes and no blank hash beside a surviving file.
- The validated-record pass and every pre-existing rejection/content/presence arm behave byte-for-byte as before.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-67-SUMMARY.md` when done.
</output>
