---
phase: 15-continued-background-downloads
plan: 74
type: execute
wave: 74
depends_on: ["15-73"]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadDeleteConvergenceTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadOwnershipConvergenceTests.swift
  - .planning/phases/15-continued-background-downloads/15-65-SUMMARY.md
autonomous: true
gap_closure: true
gap_ids: [WR-04, WR-06, IN-01]
requirements: [SC2]

must_haves:
  truths:
    - "The bracket-nesting property is stated honestly in both source docs — sibling composition is a convention, not a type-system guarantee — and a nested withdrawal is DETECTABLE: a depth counter reports an issue (never crashes) when a bracket opens inside another."
    - "15-65-SUMMARY.md no longer claims nesting is refused by the type system; a later round reasoning from that artifact lands on the true property."
    - "DownloadSourceInventoryTests describes its own scoping truthfully from source: the unused scanner is gone and the docs name the functions the censuses actually use."
    - "The two missing-notification detectors bound their waits explicitly and short; the 10s default survives for scheduling-sensitive observers."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "Withdrawal-depth detection with reportIssue and an honest sibling-composition doc"
      contains: "reportIssue"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      provides: "Self-consistent scoping docs; no unowned declaration"
      contains: "clientDoubleTreeFiles"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      via: "advanceQueueIntentGeneration's self-bracketing runs through the depth-counting bracket, so a future synchronous nesting is detected, not assumed impossible"
      pattern: "withdrawingCountedBasisMovement"
---

<objective>
Close verification gap 5 (partial; review WR-04, WR-06, IN-01, IN-02) — the unowned invariants. (a) 15-65's SUMMARY claims sibling composition is "proved by construction … nesting is refused by the type system" (15-65-SUMMARY.md:35, 93, 125). FALSE as a general claim: `advanceQueueIntentGeneration(for:)` is a SYNCHRONOUS actor-isolated method (Manager.swift:852), and the bracket's non-escaping, non-Sendable closure inherits actor isolation, so a synchronous advance written inside a bracket body compiles today and double-withdraws silently. The factual half holds at this HEAD (all four call sites re-derived by the verifier), but nothing DETECTS a nested advance. (b) `DownloadSourceInventoryTests` declares `downloadsTestFiles(in:)` (834-836) with ZERO callers while its header (27) and scanned-set doc (65-72) name it as the censuses' scoping and never name `clientDoubleTreeFiles`, the function that decides the population — the suite built to abolish unowned doc claims contradicts itself. (c) `waitForTaskValue`'s 10s default (DownloadFeatureTestHelpers.swift:104-106) lengthens the two deliberate missing-notification detectors tenfold (DownloadDeleteConvergenceTests.swift:113, DownloadOwnershipConvergenceTests.swift:90) — carried three rounds. Gap 5's fourth item — the localized-key spelling split (IN-02) — is split into its own plan, **15-75**, at the checker's direction: it touches eight source files plus two string catalogs and deserves its own context budget.

Purpose: load-bearing invariants are enforced by construction or detected by a test — never left to a false or unowned doc sentence.
Output: honest docs + runtime nesting detection + a corrected planning artifact; a self-consistent census suite; explicit short bounds at the two detectors.
</objective>

## Artifacts this phase produces

- Withdrawal-depth detection: an actor-isolated `basisMovementDepth` counter on `DownloadCoordinator` (declared in DownloadClient+Manager.swift beside the coordinator's other state), incremented/decremented around `movement()` inside `withdrawingCountedBasisMovement` (ExecutionSupport.swift:282-295), with `reportIssue` (IssueReporting — already imported in Manager.swift; import into ExecutionSupport.swift if needed) when depth exceeds one. Never a crash; zero cost when unnested.
- New testing hook `testingProbeNestedBasisMovement()` in DownloadClient+Testing.swift (following the `testing*` precedent) that deliberately nests two brackets, plus a test pinning that the probe raises the issue (`withKnownIssue`) while ordinary sibling composition raises none.
- Re-derived docs at ExecutionSupport.swift:262-275 (the SIBLINGS-only paragraph) and Manager.swift:837-845: state that sibling composition is a review-enforced convention detectable at runtime, naming the synchronous-caller shape that could nest; delete the compile-error/type-system claim wherever it appears in source docs.
- Corrected `15-65-SUMMARY.md`: the three construction-claim sites rewritten to the true property (the four current callers are async and outside any bracket — an inspection result; a future synchronous caller could nest, now detected). Marked as a post-hoc correction with this plan's ID.
- `DownloadSourceInventoryTests`: `downloadsTestFiles(in:)` deleted; lines 27 and 65-72 rewritten FROM SOURCE naming `clientModuleFiles(in:)`, `clientDoubleTreeFiles(in:)` and `clientDoubleFiles(in:)`, saying "the two trees" for the double censuses.
- Explicit `timeout: .seconds(1)` at the two missing-notification `waitForTaskValue` call sites, each with a one-line comment tying the short bound to the detector's purpose; DownloadFeatureTestHelpers.swift untouched (992/1000 lines).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-65-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Honest nesting property — docs, detection, corrected artifact</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift, .planning/phases/15-continued-background-downloads/15-65-SUMMARY.md</files>
  <read_first>
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `withdrawingCountedBasisMovement` (282-295) and the SIBLINGS-only doc (260-281).
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — `advanceQueueIntentGeneration` (852-856), the bracket doc (837-851), the `reportIssue` precedent at 804, and where the coordinator's stored state lives (for the depth property).
    - `AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` — the `testing*` hook conventions.
    - `.planning/phases/15-continued-background-downloads/15-65-SUMMARY.md` — the three claim sites (35, 93, 125) and their surrounding derivations, which stay accurate apart from the type-system sentence.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the bracket-call-site census and its nesting note (191-209 region), whose hand-check instruction the detection supersedes.
  </read_first>
  <behavior>
    - `withdrawingCountedBasisMovement` increments a depth counter before `movement()`, decrements after (throw-safe — `defer`), and calls `reportIssue` with a message naming the SIBLINGS-only rule when the counter exceeds one at entry. Release behavior is unchanged (`reportIssue` is debug-facing); no crash on any path.
    - `testingProbeNestedBasisMovement()` nests two brackets and returns; in tests it produces exactly one reported issue, pinned with `withKnownIssue`. Every other suite run stays issue-free, which is the negative side: the whole existing suite passing IS the proof that no production path nests today.
    - Both source docs state the true property: composition as siblings is a convention the depth counter now detects; the four current callers are async and top-level (inspection, current HEAD), and a future synchronous caller inside a bracket body is the shape the counter exists for. The census's hand-check note is re-derived to point at the detection.
  </behavior>
  <action>
Implement the counter and `defer`-balanced accounting; write the reportIssue message to name the rule and the remedy (compose as siblings). Add the testing hook and its test (place the test in `DownloadSourceInventoryTests`' file only if it fits that suite's charter — otherwise a small new suite in the same new-file-free spirit is NOT needed; prefer an existing DownloadClient-facing suite such as the continued-session basis tests where the bracket already has coverage; state the placement decision in the summary). Rewrite the two source doc blocks; delete every source sentence claiming nesting is impossible at the type level. Edit 15-65-SUMMARY.md's three sites to the true claim, each edit prefixed `[Corrected by 15-74]`. Comment-text discipline: the corrected docs must not quote the false claim verbatim; describe it as "the type-system claim".
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests -only-testing:DownloadsFeatureTests/DownloadContinuedSessionBasisTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'basisMovementDepth|reportIssue' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` shows the counter and the issue report; the decrement is in a `defer`.
    - The nesting-probe test raises exactly one known issue; the full suite raises none elsewhere.
    - `rg -in 'type system|type-level' AppPackage/Sources/DownloadClient` finds no sentence claiming nesting is impossible.
    - 15-65-SUMMARY.md's three sites carry the corrected claim with the `[Corrected by 15-74]` marker.
    - No SwiftLint suppression; no crash path added (`rg -n 'fatalError|precondition' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` shows no new hit against the pre-change baseline).
  </acceptance_criteria>
  <done>The nesting invariant is honestly stated everywhere it is written and detectably enforced at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Self-consistent census suite; explicit detector bounds</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadDeleteConvergenceTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadOwnershipConvergenceTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the header sentence (27), the scanned-set doc (55-76), the census scoping functions (clientModuleFiles 825, downloadsTestFiles 834, clientDoubleTreeFiles 842-846, clientDoubleFiles), and which census calls which.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadDeleteConvergenceTests.swift` (111-116) and `DownloadOwnershipConvergenceTests.swift` (88-94) — the two waits and their existing "named failure instead of a hung suite" comments.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` (100-112) — `waitForTaskValue`'s default and its parallel-scheduling rationale; DO NOT MODIFY this file (992 of 1000 lines).
  </read_first>
  <action>
Delete `downloadsTestFiles(in:)`. Rewrite line 27's header sentence and the 65-72 scanned-set paragraph FROM SOURCE: the five production censuses scope through `clientModuleFiles(in:)`; the two double-fidelity censuses scope through `clientDoubleFiles(in:)` and `clientDoubleTreeFiles(in:)` over "the two trees" (the downloads test target plus the processing client's module); no sentence may name a function the censuses do not call. If a test-target-only census is wanted to give the deleted function an owner, do NOT add one — the gap's instruction is delete-or-own, and delete is the chosen arm (state so in the summary). At the two detector call sites, pass `timeout: .seconds(1)` with a one-line comment: this wait exists to detect a MISSING notification, so the bound is the detector's failure budget, not scheduling headroom. Leave every other `waitForTaskValue` call site and the 10s default untouched.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests -only-testing:DownloadsFeatureTests/DownloadDeleteConvergenceTests -only-testing:DownloadsFeatureTests/DownloadOwnershipConvergenceTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'downloadsTestFiles' AppPackage/Tests` → no matches (declaration and every doc mention gone).
    - Lines 27 and 65-72's replacements name exactly `clientModuleFiles(in:)`, `clientDoubleFiles(in:)`, `clientDoubleTreeFiles(in:)`.
    - `rg -nF 'timeout: .seconds(1)' AppPackage/Tests/DownloadsFeatureTests/DownloadDeleteConvergenceTests.swift AppPackage/Tests/DownloadsFeatureTests/DownloadOwnershipConvergenceTests.swift` → exactly 2 matches, at the two detector waits.
    - DownloadFeatureTestHelpers.swift is byte-identical (`git diff --stat` shows no change to it).
    - All three suites green.
  </acceptance_criteria>
  <done>The census suite describes itself truthfully from source, and a missing notification costs one second, not ten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → future rounds' reasoning | A false structural claim in a SUMMARY is an input later executors trust; correcting it is an integrity fix. |
| Bracket accounting → announced progress basis | An undetected nested withdrawal silently corrupts the monotonic floor the system card reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-74-01 | Tampering | nested withdrawal double-withdrawing the floor | high | mitigate | Depth counter + reportIssue; probe test pins detection; docs state the true property. |
| T-15-74-02 | Repudiation | false doc claims (SUMMARY, census suite) | medium | mitigate | Three SUMMARY sites corrected with markers; census docs rewritten from source; unused scanner deleted. |
| T-15-74-03 | Denial of Service | ten-second hang-detector waits | low | mitigate | Explicit 1s bounds at exactly the two detector sites; default untouched for scheduling-sensitive waits. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. Task-level gates as listed above.
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1574DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels plus the two UAT-FU anchors, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC2 | Truthful progress accounting | 15-74 | COVERED | Nested-withdrawal detection protects the floor's honesty. |
| VERIFICATION | gap 5 bullet 1 | Honest nesting docs + SUMMARY correction + detection | 15-74 | COVERED | Task 1. |
| VERIFICATION | gap 5 bullet 2 | Delete downloadsTestFiles; rewrite docs from source | 15-74 | COVERED | Task 2 (delete arm chosen and stated). |
| VERIFICATION | gap 5 bullet 3 | Keep 10s default; explicit short bounds at the two detectors | 15-74 | COVERED | Task 2. |
| VERIFICATION | gap 5 bullet 4 | One key spelling, RLocalizable majority, all 19 sites | 15-75 | COVERED | Split into its own plan (15-75) at the checker's direction — not dropped. |
| REVIEW | WR-04, WR-06, IN-01 | Same items by review ID | 15-74 | COVERED | IN-02 → 15-75. |

<success_criteria>
- A nested bracket is detected and reported, never crashes; every doc that states the property states it truthfully.
- The census suite's self-description matches its source; the two detectors bound at one second.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-74-SUMMARY.md` when done.
</output>