---
phase: 15-continued-background-downloads
plan: 75
type: execute
wave: 75
depends_on: ["15-74"]
files_modified:
  - AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings
  - AppPackage/Sources/Resources/ResourceStringSymbols.swift
  - AppPackage/Sources/Resources/Resources/Localizable.xcstrings
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+RetryHelpers.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Package.swift
autonomous: true
gap_closure: true
gap_ids: [IN-02]
requirements: [SC2]

must_haves:
  truths:
    - "DownloadClient uses ONE localized-key access spelling at all 19 key-access call sites — the RLocalizable majority — so the split IN-02 tracked (and 15-69 widened) cannot widen again."
    - "Zero translation drift: every moved key carries all six locales byte-identical to its module-local original, and continued_session.subtitle's substitution plural-category sets are unchanged per locale."
  artifacts:
    - path: "AppPackage/Sources/Resources/ResourceStringSymbols.swift"
      provides: "Hand-written RLocalizable symbols for the moved keys, labeled per the AGENTS.md shared-key rule"
      contains: "RLocalizable"
    - path: "AppPackage/Sources/Resources/Resources/Localizable.xcstrings"
      provides: "The moved keys with all six locales copied verbatim"
      contains: "download_store"
  key_links:
    - from: "AppPackage/Sources/DownloadClient"
      to: "AppPackage/Sources/Resources/Resources/Localizable.xcstrings"
      via: "every DownloadClient key access resolves through the shared RLocalizable namespace after the move"
      pattern: "RLocalizable"
---

<objective>
Close verification gap 5's fourth item (review IN-02) as its own plan, split from 15-74 at the checker's direction: the localized-key spelling split in DownloadClient — 10 `.RLocalizable.`-prefixed against 9 bare key accesses, with DownloadStore+Operations.swift using both forms eight lines apart — carried for three rounds and WIDENED by 15-69 (both keys it added took the minority bare spelling). The verification's contract: pick ONE spelling for the module — the `RLocalizable.` prefix is the majority — and apply it in a single mechanical pass across all 19 call sites, not just the two keys the last round added.

The catalog-home reality makes the pass mechanical-but-not-trivial: the bare sites' keys live in the module-local catalog (`AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings`), while `.RLocalizable.` is the shared Resources catalog's namespace — so convergence MOVES the module-local keys into the shared catalog with hand-written symbols, locales copied verbatim (they are real translations), and retires the module-local catalog once emptied.

Purpose: one spelling, one catalog home per key, zero translation drift (SC2's user-facing strings stay intact while the unowned-invariant family closes).
Output: all 19 call sites on the `.RLocalizable.` spelling, the moved keys byte-identical across six locales, and the module-local catalog retired.
</objective>

## Artifacts this phase produces

- One key spelling: the nine bare `String(localized: .key)` sites in DownloadClient converge on the `.RLocalizable.` majority. The bare sites' keys live in the module-local catalog (`AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings`), so convergence MOVES those keys into the shared Resources catalog with hand-written `RLocalizable` symbols (ResourceStringSymbols.swift), locales copied verbatim (they are real translations); the module-local catalog and its resource declaration are deleted once emptied.
- Rewritten call sites in the five bare-spelling files: DownloadStore+Operations.swift (3 sites), DownloadClient+RetryHelpers.swift (1), DownloadClient+ExecutionFetch.swift (1), DownloadClient+Folders.swift (3), DownloadClient+ContinuedSession.swift (1).
- A labeled `RLocalizable` function symbol for `continued_session.subtitle` (three named `%#@…@` substitutions) per the AGENTS.md shared-key rule.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: One localized-key spelling across all 19 DownloadClient call sites</name>
  <files>AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings, AppPackage/Sources/Resources/ResourceStringSymbols.swift, AppPackage/Sources/Resources/Resources/Localizable.xcstrings, AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift, AppPackage/Sources/DownloadClient/DownloadClient+RetryHelpers.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift, AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift, AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift</files>
  <read_first>
    - `AppPackage/Sources/Resources/ResourceStringSymbols.swift` — the `RLocalizable` namespace (LocalizedStringResource extension) and the hand-written symbol conventions for shared keys, including how existing labeled `%#@variable@` substitution symbols are declared.
    - `AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings` — the module-local catalog: every key, its six locales, and `continued_session.subtitle`'s three named substitutions with their per-locale plural category sets (en/de: {other}, {one, other}, {one, other}; ja/ko/zh-Hans/zh-Hant other-only) — copied VERBATIM, never regenerated.
    - `AppPackage/Sources/Resources/Resources/Localizable.xcstrings` — the shared catalog's key style, sorting, and locale set, which the moved keys must match.
    - `AGENTS.md` — the labeled localized-format-arguments rule (shared keys carry semantic labels hand-written in ResourceStringSymbols.swift; string arguments stay positional; substitution category coherence) and the non-translated-keys rule (not applicable here — all moved keys are translated; verify none is `shouldTranslate: false`).
    - `AppPackage/Package.swift` — the DownloadClient target's resource declaration, removed only if the module-local catalog is deleted and nothing else lives in that Resources directory.
  </read_first>
  <action>
Enumerate the 19 key-access call sites in DownloadClient (10 already `.RLocalizable.`-prefixed, 9 bare; the two `String(localized: message)` variable sites are not key accesses and are untouched). For each bare site's key, MOVE the key from the module-local catalog into the shared Resources catalog byte-for-byte (all six locales, states, and substitution structures verbatim), add the hand-written `RLocalizable` symbol following the file's conventions — for `continued_session.subtitle`, a labeled function symbol whose parameter labels are semantic per the AGENTS.md rule, matching how the module's call site passes the three counts today — and rewrite the call site to the `.RLocalizable.` spelling. Check first whether any moved key name collides with an existing shared-catalog key; on collision, re-derive whether they are the same string (merge) or different (rename the moved key with the module prefix it already carries). When the module-local catalog is empty, delete it and its directory, and remove the target's resource declaration from Package.swift if it becomes vacuous. Update any DownloadsFeatureTests assertion that resolves these keys through the module-local bundle. This is one mechanical pass: no wording, locale value, or plural category may change — the localization checks below prove it.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -nP 'String\(localized: \.(?!RLocalizable)' AppPackage/Sources/DownloadClient -g '*.swift'` → no matches (all 19 key accesses prefixed; variable-argument sites excluded by the pattern).
    - Every moved key in the shared catalog carries all six locales with values byte-identical to the module-local originals (verify with a python3 JSON comparison over the moved key set, printed True).
    - `continued_session.subtitle`'s substitution category sets are unchanged per locale (en set == de set; ja/ko/zh-Hans/zh-Hant other-only); no bare numeric specifier appears in any outer value.
    - If the module-local catalog was deleted: `ls AppPackage/Sources/DownloadClient/Resources 2>/dev/null` errors, and Package.swift carries no stale resource declaration; if any key legitimately remains, the summary derives why and the census of remaining bare accesses is zero regardless.
    - Full DownloadsFeatureTests green; the app-scheme build stays warning-free (missing-locale warnings included).
  </acceptance_criteria>
  <done>One spelling, one catalog home per key, zero translation drift — the split cannot widen again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| String catalogs → user-facing text in six locales | The move must not alter a single rendered string; drift here is a silent regression invisible to compile-time checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-75-01 | Tampering | translation drift during the key move | medium | mitigate | Byte-verbatim copy gated by JSON comparison; category-set checks; build-warning gate. |
| T-15-75-02 | Denial of Service | stale resource declaration breaking the build | low | mitigate | Package.swift declaration removed only when the directory is gone; clean app-scheme build is a gate. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1575DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels plus the two UAT-FU anchors, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| VERIFICATION | gap 5 bullet 4 | One key spelling, RLocalizable majority, all 19 sites | 15-75 | COVERED | Split from 15-74 at the checker's direction; catalog-home reality handled explicitly. |
| REVIEW | IN-02 | Same item by review ID | 15-75 | COVERED | Carried three rounds; closed here. |

<success_criteria>
- All 19 DownloadClient key accesses share the `.RLocalizable.` spelling with zero translation drift.
- The module-local catalog is retired (or its residue derived in the summary), and no bare key access remains.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-75-SUMMARY.md` when done.
</output>
