---
phase: 15-continued-background-downloads
plan: 70
type: execute
wave: 70
depends_on: ["15-69"]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFolderOperationTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFolderAdmissionTests.swift
autonomous: true
gap_closure: true
gap_ids: [CR-01, WR-01]
requirements: [SC3]

must_haves:
  truths:
    - "Every user folder the app's own listing produces is mutable from inside the app: a folder created over File Sharing whose name is not its own normalized form (double spaces, leading space, backslash, trailing dot) deletes and renames successfully, with record convergence."
    - "A destructive user-folder operation still cannot reach a filesystem target the caller did not name: every structural refusal (multi-component, dot/dot-dot, absolute path, control characters, escaping parents, symlinked child, gallery-shaped name) survives the admission relaxation."
    - "No public API reproduces the unconfined name-to-URL construction CR-02 deleted: removeFolder(relativePath:) is gone."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      provides: "Source-name admission test that agrees with the producing listing"
      contains: "confinedDirectUserFolderURL"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadFolderAdmissionTests.swift"
      provides: "The positive half of the escape catalog: listed non-normalized folders delete and rename"
      contains: "Art  Books"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadStore.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      via: "scanDownloads' promotion rule and confinedDirectUserFolderURL's admission rule describe the same set of names"
      pattern: "isGalleryFolderLikeName"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      via: "deleteFolder, renameFolder, and moveDownload consume listed names only through the store's admission boundary"
      pattern: "confinedDirectUserFolderURL"
---

<objective>
Close verification gap 1 (BLOCKER regression, SC3; review CR-01 + WR-01). The producing surface and the consuming predicate disagree about what a valid user-folder name is: `scanDownloads` (DownloadStore.swift:601-637) promotes EVERY visible non-gallery directory under the root to a user folder with no normalization filter, while `confinedDirectUserFolderURL` (DownloadStore+Operations.swift:622-640) refuses any source name failing `normalizedUserFolderName(rawName) == rawName`. With `UIFileSharingEnabled` and `LSSupportsOpeningDocumentsInPlace` over a root in `Documents/`, a Files-app-created folder named `Art  Books`, ` Photos`, `Manga\Vol1` or `Misc etc.` is listed, is a usable download destination, and is now un-deletable AND un-renamable from inside the app — the error even calls the displayed name invalid. The predicate is the side that must move: the listing describes a disk the app does not own. Also close WR-01: `removeFolder(relativePath:)` (Operations.swift:417-425) is dead public API reproducing verbatim the unconfined construction CR-02 deleted.

Purpose: SC3's record-convergence half — every listed folder is mutable, and no mutation reaches a target the caller did not name.
Output: an admission test that cannot refuse a listed name, all structural refusals kept, destinations still normalizing, a positive admission catalog beside the refusal catalog, and the dead reconstruction deleted.
</objective>

<assumption_delta_decision>
The deterministic scan (`assumption-delta scan 15`) reports `detected: true` on four lexical signals ("no fallback tier", "drain-then-second-tap", "also restores", "parameterised failure-injected") — all word-token matches inside prior-round planning prose, none a data-shape change. This round makes no singleton plural and no fixed value configurable; gap 1's change is a predicate relaxation on an existing boundary, not an assumption delta. Recorded here; no design change follows.
</assumption_delta_decision>

## Artifacts this phase produces

- Revised `confinedDirectUserFolderURL(named:)` (DownloadStore+Operations.swift): source-name ADMISSION test — non-empty, not `.`/`..`, single path component, no control characters, not gallery-shaped (`DownloadStore.isGalleryFolderLikeName`), standardized parent == root, resolved parent == root. The normalization-identity clause is deleted.
- Deleted `removeFolder(relativePath:)` (DownloadStore+Operations.swift:417-425) and its doc; `removeFolder(at:)` survives unchanged as the record-path primitive.
- Re-derived docs: `confinedDirectUserFolderURL`, `renameUserFolder`'s source-refusal bullet, `DownloadClient+Folders.swift` `renameFolder`'s early-exit comment (line 65-66), `moveDownload`'s destination-resolution comment, and the `DownloadStore.swift:92-98` "unwritable" claim (true once WR-01's function is gone).
- Revised `moveDownload` (DownloadClient+Folders.swift:200-271): the destination is a picked LISTED name, so it is admitted raw (no `normalizedUserFolderName` rewrite), and `ensureUserFolder` recreates it verbatim if vanished.
- New test file `AppPackage/Tests/DownloadsFeatureTests/DownloadFolderAdmissionTests.swift`, suite `DownloadFolderAdmissionTests`: parameterized positive catalog over `Art  Books` (two spaces), ` Photos` (leading space), `Manga\Vol1` (backslash), `Misc etc.` (trailing dot) — each staged as the folder's OWN on-disk name, asserted to list, delete, and rename successfully with three-store record convergence on the success side.
- Re-derived `whitespacePaddedAlias` expectations in `DownloadFolderOperationTests.swift`: `"  Keeper  "` beside a real `Keeper` now names a literally absent directory, so the refusal becomes `.notFound`; survival assertions unchanged.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-68-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the positive half of the admission contract</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadFolderAdmissionTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFolderOperationTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFolderOperationTests.swift` — the whole file (700 lines): the escape environment (`DownloadFolderEscapeEnvironment`), the staging helper (`stageDeleteEscapeVictims`), the disk-then-records-then-verdict assertion shape, and the `whitespacePaddedAlias` arguments (delete at 601-609, rename catalog at 420-427) whose pinned error value this plan changes.
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `scanDownloads` (601-637): the promotion rule the positive catalog mirrors; `normalizedUserFolderName` (363-380) and `normalizedFolderName` (394-431): what each staged name normalizes to, so rename destinations are predictable.
    - `AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift` — `deleteFolder` (117-180) and `renameFolder` (54-95): the record convergence the success side must assert (downloadIndex, queue store, background-task store, userFolders, observer notification).
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — fixture builders the operation tests use. This file is at 992 of the 1000-line SwiftLint error limit: add NOTHING to it; any new helper lives in the new test file.
  </read_first>
  <behavior>
    - For each of the four catalog names — `Art  Books`, ` Photos`, `Manga\Vol1`, `Misc etc.` — staged via `FileManager.createDirectory` as the folder's OWN on-disk name (never via `createUserFolder`, which would normalize): `fetchFolders()` lists it verbatim; `deleteFolder(name:)` returns `.success` and the directory is gone; a contained indexed gallery's records are cleared from all three stores (the exact convergence the refusal side asserts survival of, mirrored to the success side); `renameFolder(oldName:newName:)` with a plain new name returns `.success`, the on-disk directory moves to the normalized destination, and `fetchDownloads`/`fetchFolders` repoint. Pre-fix every one of these is RED: both mutations answer `.fileOperationFailed` (invalid folder name) before touching the disk.
    - Both sides pinned: the six-argument delete AND rename refusal catalogs in `DownloadFolderOperationTests` stay green — with ONE re-derivation: `whitespacePaddedAlias` (`"  Keeper  "` staged beside the real `Keeper`) is refused post-fix because the padded name names NOTHING on disk, so the pinned error becomes `.notFound` while the survival assertions (Keeper intact, records intact, sentinel bytes intact) stand verbatim. The case doc must state why the refusal shape changed and why the property it protects (never resolve a padded alias onto a different real folder) is unweakened: source names are never normalized, so `"  Keeper  "` can only ever mean a folder literally so named.
  </behavior>
  <action>
Create `DownloadFolderAdmissionTests.swift` with a parameterized suite over the four names (Swift Testing `@Test(arguments:)`), reusing the escape-environment construction pattern by building its own local fixture (do not grow DownloadFolderOperationTests' helpers past what the refusal catalog needs, and add nothing to DownloadFeatureTestHelpers.swift). Stage each argument's folder with a real indexed gallery inside (manifest written via `storage.writeManifest`, `reconcileDownloads`, queue and background-task store entries — mirror `stageDeleteEscapeVictims`' three-store staging including the foreign-gid `schedulerHold` pattern, and reuse its documented rationale). Assert listing, delete-with-convergence, and rename-with-repoint per behavior. For rename, derive the expected destination from `normalizedUserFolderName` of the chosen new name and assert the moved gallery record's `parentFolderName`. Update the two `whitespacePaddedAlias` expectations in DownloadFolderOperationTests to `.notFound` with the re-derived case doc. Run the gate and bank the RED observations (exact pre-fix error values) for the summary. Comment-text discipline: no test comment may contain the normalization-identity clause verbatim; describe it as "the normalization-identity clause" only.
  </action>
  <verify>
    <automated>! xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadFolderAdmissionTests</automated>
  </verify>
  <acceptance_criteria>
    - Every positive-catalog case fails pre-fix at the delete or rename admission with the invalid-folder-name refusal; the listing assertion passes pre-fix (the listing already admits these names — that asymmetry IS the gap, and the case doc says so).
    - The four staged names are byte-exact: two internal spaces, one leading space, one backslash, one trailing dot; no case stages a normalized spelling.
    - Success-side convergence asserts all three stores plus `userFolders` — the same population the refusal side asserts survival of.
    - `whitespacePaddedAlias` still refuses and everything it protects still survives; only the pinned error value changed, and its doc derives why.
    - DownloadFeatureTestHelpers.swift is untouched (992-line file; 1000-line SwiftLint error).
  </acceptance_criteria>
  <done>The admission contract is a deterministic RED observation from the positive side, with the refusal side re-pinned rather than weakened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — move the predicate to the listing's terms; delete the dead reconstruction; sweep every consumer</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift, AppPackage/Sources/DownloadClient/DownloadStore.swift, AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift</files>
  <read_first>
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `confinedDirectUserFolderURL` (609-640), `mutatingConfinedUserFolder` (592-607), `renameUserFolder` (457-509) and its source-refusal doc, `deleteUserFolder` (511-539), `createUserFolder` (541-561), `ensureUserFolder` (563-577), `removeFolder(relativePath:)` (417-425), `removeFolder(at:)` (427-455).
    - `AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift` — every caller: `createFolder` (22-40), `renameFolder` (54-95, note the early-exit comment at 65-66), `deleteFolder` (117-180), `moveDownload` (200-271), `repointRenamedUserFolder` (290-311).
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `scanDownloads` (601-637), `isGalleryFolderLikeName` (359-361), the `userFolderURL` deletion note at 92-98.
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — gap 1's four `missing:` bullets: the scope contract this task implements bullet-for-bullet.
  </read_first>
  <behavior>
    - `confinedDirectUserFolderURL` admits every name the listing can produce and refuses everything structural: empty, `.`, `..`, multi-component, control characters, gallery-shaped names (`isGalleryFolderLikeName` — the listing never promotes those to user folders, so this cannot refuse a listed name and it keeps user-folder mutations off gallery-shaped direct children), a standardized parent that is not the root, a resolved parent that is not the root. The `.typeDirectory` re-checks inside the mutation lock stand unchanged in `deleteUserFolder` and `renameUserFolder`.
    - Destinations keep normalizing where names are MINTED: `createFolder` and `renameFolder`'s NEW name (coordinator-side `normalizedUserFolderName` guards unchanged). Sources and picked destinations are ADMITTED, never rewritten: `deleteFolder`'s name, `renameUserFolder`'s `oldName`, the coordinator's existence pre-check, and `moveDownload`'s destination (a folder-picker value naming an EXISTING listed folder — normalizing it is what created the near-duplicate hazard, so the raw name flows to `confinedDirectUserFolderURL` and to `ensureUserFolder` for the vanished-folder recreation).
    - `removeFolder(relativePath:)` no longer exists; `removeGalleryFolders`' call sites pass scan-produced URLs to `removeFolder(at:)` exactly as before.
  </behavior>
  <action>
In `confinedDirectUserFolderURL`, delete the normalization-identity clause and add the control-character refusal (`rawName.unicodeScalars.contains(where: CharacterSet.controlCharacters.contains)` refuses) and the gallery-shape refusal (`!DownloadStore.isGalleryFolderLikeName(rawName)`); keep every other clause verbatim. Re-derive its doc: the boundary is an ADMISSION test agreeing with `scanDownloads`' promotion rule, and the doc must enumerate what it deliberately does NOT admit (the structural refusals above) and why normalization is the wrong test for a source (it rewrites, so it selects a folder the caller never named — keep `renameUserFolder`'s never-normalize-a-source bullet, dropping only its now-false "an accepted source must already BE its normalized spelling" sentence). Delete `removeFolder(relativePath:)` outright with its doc; confirm zero callers remain and re-derive the `DownloadStore.swift:92-98` claim so "unwritable" is true against source. In `DownloadClient+Folders.swift`: re-derive `renameFolder`'s early-exit comment (the comparison is now raw on-disk source vs normalized destination; equality still means no move is needed); change `moveDownload`'s guard to admit the raw picked name (drop the `normalizedUserFolderName(folderName)` rewrite; pass `folderName` to both `confinedDirectUserFolderURL` and `ensureUserFolder`) and re-derive its comment to name the near-duplicate hazard this closes. Sweep and record in the summary a disposition table for EVERY site that turns a folder name into a URL: deleteFolder, renameFolder source, renameFolder destination, createFolder, moveDownload destination, the coordinator existence pre-check, `repointRenamedUserFolder` (derived from the store's already-performed move — a read, unchanged), `ensureUserFolder`'s other callers (enqueue-parent path — constants `Default`/`Automation`, already admissible), and `removeFolder(at:)`'s callers. Comment-text discipline: no surviving comment may spell the deleted clause or the deleted function name verbatim.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadFolderAdmissionTests -only-testing:DownloadsFeatureTests/DownloadFolderOperationTests</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 cases green; both six-argument refusal catalogs green.
    - `rg -n 'normalizedUserFolderName\(rawName\) == rawName' AppPackage/Sources` → no matches.
    - `rg -n 'removeFolder\(relativePath' AppPackage/Sources AppPackage/Tests` → no matches (declaration, callers, and comments all gone).
    - `rg -n 'controlCharacters' AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` finds the admission clause; `rg -n 'isGalleryFolderLikeName' AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` finds the gallery-shape refusal.
    - `moveDownload` contains no `normalizedUserFolderName` call (`rg -n 'normalizedUserFolderName' AppPackage/Sources/DownloadClient/DownloadClient+Folders.swift` matches only `createFolder` and `renameFolder`'s new-name guard).
    - The summary carries the full consumer disposition table and states, per verification bullet 1, which side moved and why.
    - No SwiftLint suppression introduced; the app-scheme build stays warning-free.
  </acceptance_criteria>
  <done>The producing surface and the consuming predicate agree; listed folders are mutable; escapes still refuse; the one-line rewrite of the original defect is unwritable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Files-app-authored directory names → destructive folder mutations | Folder names created outside the app (File Sharing / open-in-place) are untrusted disk input consumed by delete/rename/move. |
| Public client API name arguments → filesystem targets | `deleteFolder`/`renameFolder`/`moveDownload` take caller strings that must never resolve outside the download root or onto an unnamed real object. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-70-01 | Elevation of Privilege | confinedDirectUserFolderURL admission relaxation | high | mitigate | The relaxation deliberately does NOT admit: multi-component paths, `.`/`..`, absolute paths, control characters, names whose standardized or resolved parent is not the root, symlinked children (lock-time `.typeDirectory` re-check), or gallery-shaped names. Both refusal catalogs re-run green beside the new positive catalog. |
| T-15-70-02 | Tampering | removeFolder(relativePath:) resurrection | high | mitigate | Deleted outright; grep gate over Sources and Tests proves no declaration, caller, or comment survives. |
| T-15-70-03 | Denial of Service | listed folder un-mutable from inside the app | medium | mitigate | Admission test derived FROM the producing listing; positive catalog pins the four File-Sharing name shapes end to end. |
| T-15-70-04 | Spoofing | padded alias resolving onto a different real folder | high | mitigate | Sources are never normalized; `whitespacePaddedAlias` re-pinned as `.notFound` with survival assertions intact. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadFolderAdmissionTests -only-testing:DownloadsFeatureTests/DownloadFolderOperationTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1570DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels plus the two UAT-FU anchors, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC3 | No lost work; record convergence | 15-70 | COVERED | Listed folders mutable again; escapes still refused; convergence asserted from the success side. |
| VERIFICATION | gap 1 bullet 1 | Which side moves, stated | 15-70 | COVERED | Predicate moves; admission test cannot refuse a listed name; structural refusals enumerated and kept. |
| VERIFICATION | gap 1 bullet 2 | All name-consuming exit paths swept | 15-70 | COVERED | Disposition table over every name-to-URL site, moveDownload's near-duplicate hazard closed. |
| VERIFICATION | gap 1 bullet 3 | Positive half of the escape catalog | 15-70 | COVERED | Four own-on-disk-name arguments, delete AND rename, success-side convergence. |
| VERIFICATION | gap 1 bullet 4 | removeFolder(relativePath:) residual | 15-70 | COVERED | Deleted; grep-gated. |
| REVIEW | CR-01, WR-01 | Same defects by review ID | 15-70 | COVERED | — |

<success_criteria>
- A folder whose own on-disk name is non-normalized lists, deletes, and renames from inside the app, with all three record stores converged.
- Every structural escape refusal from 15-68's catalog still holds; the padded-alias property is re-derived, not weakened.
- The unconfined construction is unwritable: no declaration, caller, or comment survives.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-70-SUMMARY.md` when done.
</output>
