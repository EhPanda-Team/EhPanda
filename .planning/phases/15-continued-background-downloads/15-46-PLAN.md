---
phase: 15-continued-background-downloads
plan: 46
type: execute
wave: 46
depends_on: ["15-45"] # Round continuity from the round-15 HEAD (6a0059d4) and xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift
  # PROVISIONAL BELOW THIS LINE — the four paths that follow are the PRE-DERIVED candidate set: the
  # remaining suites the Task-1 census covers (14 case-body call sites over 5 suites). Each is
  # touched only if the census marks a site inside it unfaithful; a suite the census clears stays
  # byte-unchanged. This list MUST be rewritten in the SUMMARY to the set actually modified, each
  # entry carrying the census verdict that put it in or left it out.
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-28]
requirements: [SC2]

must_haves:
  truths:
    - "The invariant is stated over the whole DOUBLE-BUILDING FAMILY, not the two cases the finding named: every payload a suite hands to a production preparation carries the `pageSelection` the production route that case drove would have put on it — enumerated at execution time by grepping every `makeStartPayload(`/`makeRepairPayload(` call site in the test target and classifying each against whether its route writes `queuedPageSelections`, rather than by repairing only the two sites the gap record cites."
    - "Faithfulness is by CONSTRUCTION, not by a hand-copied literal: the payload is produced through the same two production steps `fetchNormalizeAndDownload` uses — the selection is placed on the payload as `fetchLatestPayload(for:mode:options:pageSelection:)` places it, and then refined by the production `normalizeFetchedPayload(_:mode:rawPageSelection:)` — with the early-return guard inside that normalizer derived first, because a double that calls only the normalizer keeps a nil selection and looks repaired while remaining unfaithful."
    - "The binding between the suite's selection and the one production stored is OWNED by a named test rather than assumed: a case asserts that the coordinator's `queuedPageSelections` entry for the gid equals what the helper feeds the payload, at a moment whose production guarantee is named in the case's doc comment (which production event holds the entry in place while the assertion runs), so a later change to `retryPages`' own normalization of its indices fails a build instead of silently un-faithing every double again."
    - "Nothing about the announcement gate moves in this plan: G-15-27 is still open at its end, and both repaired refusal cases still pass — the repair is a precondition that makes 15-47's regression discriminating, not the fix, and the plan proves the distinction by recording that the suite is green both before and after the payload change."
    - "The residue this change could leave is swept before the gap is declared closed: the new helper parameter defaults so untouched call sites stay byte-identical, no helper is duplicated rather than promoted, and the two-step production shape is not restated anywhere it can go stale."
    - { statement: "On a physical iOS 26 device a selected-page retry that fetches nothing shows a card that does not climb for that gallery — an observation this plan does not change and 15-47 does", verification: backstop }
  artifacts:
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift"
      provides: "makeStartPayload carrying an optional page selection (defaulted so every existing call site is byte-unchanged), and the production-shaped payload helper that routes a case's retried indices through both production steps"
      contains: "normalizeFetchedPayload"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift"
      provides: "Both refusal cases building their payload through the production-shaped helper, and the named case pinning the helper's selection against the coordinator's stored entry"
      contains: "queuedPageSelections"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift"
      via: "the helper reproduces the fetch-then-normalize pair the production run performs, so a suite payload reaches the preparation carrying what production would have carried"
      pattern: "normalizeFetchedPayload"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+RetryHelpers.swift"
      via: "the pin asserts the coordinator entry performRetryPages stores equals the selection the helper feeds, so the double tracks the route rather than a literal"
      pattern: "queuedPageSelections"
  prohibitions:
    - statement: "Must NOT change any production source file. This plan is test-target only; a Sources diff means the announcement gate moved, which is 15-47's scope and would destroy the pre/post-fix discrimination this plan exists to enable."
      status: pending
      verification: automated
    - statement: "Must NOT repair only the two cases the gap record names: the census covers every payload-building call site in the test target and every verdict is recorded, including the sites judged already faithful and why."
      status: pending
      verification: automated
    - statement: "Must NOT copy the gap record's parameter shape without deriving it: the record writes the two selections as if the payload and the coordinator entry shared a type, and source disagrees — derive both from source before writing the signature."
      status: pending
      verification: automated
    - statement: "Must NOT duplicate a staging helper into a suite; a genuinely shared helper is promoted into DownloadFeatureTestHelpers.swift, per this phase's standing rule."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<!-- planner-discipline-allow: makeStartPayload -->
<!-- planner-discipline-allow: queuedPageSelections -->

<objective>
Close G-15-28 (warning; this phase's RECORDED GENERATOR in its choreography form). Confirmed
verbatim in source at HEAD 6a0059d4: both cases in
`DownloadContinuedSessionLedgerRefusalTests` drive the production `retryPages` route — which
stores a page selection at `DownloadClient+RetryHelpers.swift:91` — and then hand
`makeRepairPayload(for:)` to `testingPrepareWorkingSeedAnnouncingProgress`. That helper forwards
to `makeStartPayload(for:mode:)`, whose payload literal ends with the host, folder name and mode
and carries no selection at all. The double therefore cannot reach a state the production
contract not only permits but guarantees on the route it claims to model, and that is the
specific reason G-15-27 shipped green: no case in the suite has ever run the announcement gate
against a payload carrying a selection.

This plan is deliberately SEQUENCED FIRST, ahead of both blockers, on the verifier's own closing
instruction: close G-15-28 first so the regression it unblocks can fail before either fix lands.
A repair landed after the gate change would be a green assertion with nothing to discriminate.

Purpose: the suite can express the over-report before anything is fixed, and the double stays
faithful as the route evolves (SC2 — what the card reports for a selected-page retry).
Output: a production-shaped payload helper, both refusal cases rebuilt on it, a named pin binding
the helper to the coordinator entry the route writes, the full census recorded, and a green run.
</objective>

## Artifacts this plan produces

- **`makeStartPayload(for:mode:pageCountOverride:pageSelection:)`** — the existing helper gains a
  defaulted page-selection parameter so the base payload can carry a selection the way
  `fetchLatestPayload` does. The default keeps every untouched call site byte-identical.
- **A production-shaped payload helper in `DownloadFeatureTestHelpers.swift`** — name derived by
  the executor from the file's own naming voice; it takes the fixture gallery, the mode, the
  indices the case retried, and the coordinator, and returns the payload after applying BOTH
  production steps. Its doc comment states the two steps and why calling only the second one is
  insufficient.
- **New test case pinning the helper against the route** — a named case asserting the
  coordinator's stored selection entry equals what the helper feeds, with the production event
  that holds the entry in place named in its doc comment.
- **No new production symbols.** This plan does not touch `AppPackage/Sources`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the production selection chain, census every payload double, and build the faithful helper</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-28 entry WHOLE (`summary`, `detail`, `suggested_fix`) and the G-15-27 entry's `why_tests_missed_it` clause, which states what this repair has to make expressible
    - `AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift` — `fetchNormalizeAndDownload` (`:131-161`): the exact two-step order production uses, including which value it reads out of the coordinator and hands to BOTH steps
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift` — `fetchLatestPayload(for:mode:options:pageSelection:)` (`:7`—) and `normalizeFetchedPayload(_:mode:rawPageSelection:)` (`:154-186`): derive that the first step places the selection ON the payload and that the second step's equality guard RETURNS THE PAYLOAD UNCHANGED when the normalized value equals the raw one, which is why a double that only normalizes keeps whatever the base literal had
    - `AppPackage/Sources/DownloadClient/DownloadClient+RetryHelpers.swift` — `retryPages(gid:pageIndices:)` (`:44-77`) and `performRetryPages` (`:80-96`): the transform applied to the caller's indices before storage, and the storing assignment
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the `queuedPageSelections` declaration (`:348`) and `clearDownloadQueueIntent` (`:684-687`): the entry's element type, and the clear that decides whether a read-back after a settled run can still see it
    - `AppPackage/Sources/AppModels/Download/DownloadedGallery+Extensions.swift` — `DownloadRequestPayload`'s selection property (`:30`, `:41`, `:51`): its real type, which differs from the coordinator entry's
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — `makeStartPayload` (`:478-519`) and `makeRepairPayload` (`:517-519`): the helper voice and the doc-comment convention to extend
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` — the suite header's choreography-discipline paragraph and both cases' staging shape: this is the file the Step-4 pin case lands in, and the pin must read as a sibling of what is already there
    - `AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` — `testingInstallActiveTask` (`:12`) and the surrounding forwarders: candidate mechanisms for making the coordinator entry's presence deterministic
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `scheduleNextIfNeededCore`'s active-task guard (the `:180-215` region): whether an installed active task really is what keeps a queued selection unconsumed
    - `.swiftlint.yml` — the lint gates all new test code must satisfy from the start (line length 120 at error, file length 1000 at error)
  </read_first>
  <action>
**Step 1 — derive the chain, before writing any signature.** From `fetchNormalizeAndDownload`,
record in the summary: which coordinator collection production reads, the two functions it hands
that value to, and in which order. From `normalizeFetchedPayload`, record the equality guard and
its consequence — a payload arriving with no selection leaves with no selection whenever the raw
selection normalizes to itself, so the base payload MUST carry the selection for the pair to be
faithful. From `retryPages`, record the transform applied to the caller's indices before storage.
From the payload model and the coordinator declaration, record BOTH element types; they differ,
and the gap record's suggested signature conflates them — write the signature from source.

**Step 2 — the census, re-derived not imported.** Grep the whole test target for
`makeStartPayload(` and `makeRepairPayload(` and classify EVERY match into: declarations, the
forwarding call inside the repair helper, doc-comment mentions, and case-body call sites. The
derivation must agree with this enumeration: 14 case-body call sites spread over 5 suites
(`DownloadContinuedSessionBasisTests`, `DownloadContinuedSessionLedgerTests`,
`DownloadContinuedSessionReconciliationTests`, `DownloadContinuedSessionLedgerRefusalTests`,
`DownloadRepairSeedSignalPropagationTests`), plus 1 forwarding call, 2 declarations and 1
doc-comment mention. If the derived classification disagrees in either direction, STOP and record
the discrepancy — a site the round-15 grep missed is a further unfaithful double and changes this
plan's scope. Then, for each case-body site, decide faithfulness by ONE question derived from
source rather than from the case's prose: does the production route this case drives write a
`queuedPageSelections` entry for the gid before the payload is built? Record the verdict for all
14, including the sites judged already faithful and the route that makes them so.

**Step 3 — the helper.** Add a defaulted page-selection parameter to `makeStartPayload` in the
type Step 1 derived, threaded into the payload literal beside the mode, so the base payload can
carry a selection exactly as the fetch step places one; the default keeps all other call sites
byte-identical. Then add the production-shaped helper: it takes the fixture gallery, the start
mode, the page indices the case retried and the coordinator, applies the production index
transform Step 1 derived, builds the base payload carrying the result, and returns the payload
the production normalizer produces from it. Its doc comment states the two steps and why the
second alone is insufficient, and names the gap this shape exists to prevent. Do not restate the
normalizer's internal guard as a count or a line reference — state it as the rule.

**Step 4 — the pin.** Add one named case binding the helper to the route. It lands in
`AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` — NOT in
the helpers file, which holds no `@Test` and must not start holding one; the pin is a test about
the route, so it belongs beside the cases that drive the route. Drive `retryPages` with a known
index set, then assert the coordinator's stored entry equals what the helper feeds the
payload. The entry must be provably present when the assertion runs — derive the mechanism rather
than assuming one, using the Step-1 reading of `clearDownloadQueueIntent` and the Step-1 reading
of the scheduler's active-task guard, and NAME the production event that holds it in the case's
doc comment. If no mechanism makes the presence deterministic, say so in the doc comment and pin
the transform instead, stating which half is then unowned.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionLedgerTests</automated>
  </verify>
  <acceptance_criteria>
    - The Step-1 chain derivation is in the summary and names: the coordinator collection production reads, both production functions in order, the normalizer's equality guard and its consequence, the index transform applied before storage, and BOTH element types quoted from source.
    - The Step-2 census is in the summary with all 14 case-body call sites classified and a per-site faithfulness verdict, or the plan stopped on a count discrepancy with the discrepancy recorded.
    - The base helper carries the selection: `grep -c 'pageSelection' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` outputs at least `3` (the parameter, the literal member, the production-shaped helper's use), each quoted in the summary.
    - The helper routes through production: `grep -v '^[[:space:]]*//' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift | grep -c 'normalizeFetchedPayload'` outputs at least `1` — a non-comment count, because Step 3 requires the doc comment to name the second production step, and naming it writes the identifier again. The summary states WHICH occurrence is the call (quoted with its line) and counts the doc-comment mentions separately.
    - Untouched call sites are byte-identical: at Task 1's end, `git diff` over the four census suites other than `DownloadContinuedSessionLedgerRefusalTests.swift` shows zero changes, and the only change inside the refusal suite is the added pin case.
    - The pin exists, lands in the right file, and names its guarantee: `grep -c '@Test' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` outputs `0`; the new case's name and its containing file are recorded; and its doc comment names the production event holding the coordinator entry in place (quoted), or records that no such mechanism exists and which half is unowned.
    - The targeted run exits `0` in a single invocation.
  </acceptance_criteria>
  <done>The production selection chain is derived and recorded, every payload double in the test target carries a faithfulness verdict, and a helper exists that reproduces both production steps and is pinned against the route rather than against a literal.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild every unfaithful double on the helper, sweep the residue, and prove the suite green</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` — the whole file (264 lines): the suite header's choreography-discipline paragraph, `testAnAllPagesGoneRepairOfACompleteReadingRecordReportsItsWorkAndDrainsFull` (the six-index retry at `:90` and its payload build at `:104`) and `testAFailedEnumerationRepairOfACompleteReadingRecordStillEarnsSessionTrust` (the single-index retry at `:190` and its payload build at `:204`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeedAnnouncingProgress` (`:435-451`) and `pendingPageIndices` (`:796-824`): derive that today's announcement gate ignores the selection entirely, which is why threading it changes no assertion in this plan
    - The Task-1 census verdicts, for every site this task must repair beyond the two named
    - The four remaining census suites — `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift`, `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerTests.swift`, `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift` and `AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift`. Their call sites were already enumerated by the Task-1 census, so this task re-reads each site the census marked UNFAITHFUL together with the staging around it, before rebuilding it — the census verdict alone does not carry the index set the case retried. A suite whose sites the census cleared is not edited and stays byte-unchanged; these four are this task's candidate set, not its guaranteed diff
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**Step 1 — rebuild the two named cases.** Replace each case's payload build with the Task-1
production-shaped helper, passing the SAME index set that case already hands `retryPages` — read
from the case's own call rather than from the gap record, whose two literals are written in a
type source does not use. Change nothing else in either case: the stagings, the grounding
assertions, the queued-window zero assertion and every terminal assertion stay byte-identical, so
the diff is exactly the payload construction. Update each case's doc comment to state that its
payload now carries the selection its route stores, and that the announcement gate does not yet
read it — the sentence a reviewer needs in order to see this as a precondition rather than a fix.

**Step 2 — repair every other site the census marked unfaithful.** Apply the same rebuild. The
candidate set is already pre-derived — the four suites in this plan's `files_modified` under its
PROVISIONAL marker, mirrored in this task's `<files>` and `<read_first>` — and a suite the census
cleared is not edited at all; rewrite that list in the summary to the set actually modified, each
entry carrying the census verdict that put it in or left it out. If the census found
none beyond the two, record that verdict explicitly with the route that makes each remaining site
faithful — silence is what let this class survive.

**Step 3 — the residue sweep, before declaring the gap closed.** Three checks, each recorded:
(a) newly-orphaned symbols — is any helper or literal in the test target now unreferenced because
the rebuild replaced its only caller? Grep each helper the rebuilt cases stopped using and record
its remaining caller count; a zero means delete rather than leave dead. (b) the same unfaithful
CLAIM surviving outside this plan's scope — grep the whole test target for suite headers or case
docs asserting that a payload models a production route, and confirm each is now true; the
recorded generator here is a Sources-scoped or file-scoped repair leaving the same claim standing
elsewhere. (c) newly-stale prose — confirm no doc comment now restates the old selection-free
shape as if it were current.

**Step 4 — green, both sides recorded.** Run ONE full FeatureTests invocation. Record the total
test count against 15-45's baseline (872) and state the net movement. Then record the plan's
central negative result explicitly: the two rebuilt cases pass BOTH before and after the payload
change, which is the evidence that this plan repaired the double without moving the gate — and
therefore that 15-47's regression will have something to discriminate.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Both named cases build their payload through the helper: `grep -c 'makeRepairPayload' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` outputs `0`, and the two replacement calls are quoted in the summary.
    - The diff in each rebuilt case is confined to the payload construction and its doc comment: `git diff` over the file shows no change to any staging, grounding or terminal assertion, stated from the diff and quoted. The pin case Task 1 added to this same file is a NEW case rather than a rebuilt one and is excluded from this confinement — the criterion is about the two pre-existing cases only.
    - No production file changed: `git diff --stat -- AppPackage/Sources App ShareExtension` reports zero files.
    - The residue sweep's three checks are each recorded with their grep results: orphaned-helper caller counts, the payload-claim scan over the test target, and the stale-prose scan.
    - The full FeatureTests run exits `0` in a single invocation, with the test count recorded against the 872 baseline and the net movement explained by the pin case added in Task 1.
    - The before/after negative result is recorded: both rebuilt cases green on both sides of the payload change, quoted from the two runs.
  </acceptance_criteria>
  <done>Every payload double in the test target carries the selection its route stores, the change is proven not to have moved the announcement gate, and the residue sweep found and recorded what the rebuild left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test double → production contract | A double that cannot reach a state production guarantees turns a green suite into evidence of absence; this is the boundary G-15-27 crossed undetected |
| Suite-issued choreography → production choreography | A payload the suite builds and hands to a production seam substitutes the suite's ordering for production's, which the refusal file's own header forbids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-46-01 | Repudiation | A green refusal suite standing as evidence that the announcement gate is correct for selected-page retries | high | mitigate | The double is rebuilt through both production steps before any gate change, and the before/after green readings are recorded so the plan's non-effect on the gate is explicit |
| T-15-46-02 | Tampering | The helper copying a literal selection that silently drifts when `retryPages` changes its index transform | medium | mitigate | A named case pins the helper's selection against the coordinator entry the route writes, with the holding production event named |
| T-15-46-03 | Repudiation | A partial repair covering only the two cited cases, leaving the same class live in the other four suites | medium | mitigate | Task 1's census classifies all 14 call sites with a recorded per-site verdict; Task 2 repairs every one marked unfaithful |
| T-15-46-04 | Tampering | The normalizer's equality guard silently discarding the selection, so the helper looks repaired and is not | high | mitigate | Step 1 derives the guard before the signature is written; the base payload carries the selection so the guard's early return preserves it |
</threat_model>

<verification>
- Task 1 runs one targeted ledger invocation; Task 2 runs the plan's single full FeatureTests
  invocation. xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly SKIPPED this round and recorded here as a deliberate choice —
  the phase has no requirement IDs to probe (`Requirements: None mapped`), so no probe-derived
  edge or prohibition predicates were generated. `must_haves` derive from G-15-28's
  `truth_violated` clause and the SC2 label the touched surface backs.
- <assumption_delta_decision>no-change — the primary noun in this plan is the payload double, and
  its shape is unchanged; only the values it carries become production-derived. No identity model
  changes shape here. The genuine delta of this round is recorded in 15-48.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan. It is retained
  in VERIFICATION.md under `human_verification` as an independent axis; the backstop truth above
  records the device-only observation without closing it.
</verification>

<success_criteria>
- G-15-28 closed at the family: every payload double in the test target carries the selection the
  production route it models stores, produced through both production steps rather than a
  literal, pinned by a named case against the coordinator entry, with the full census and the
  residue sweep recorded and the full suite green in one invocation — and with the plan's
  non-effect on the announcement gate explicitly recorded, so 15-47's regression is
  discriminating.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-46-SUMMARY.md` when done, including
the production chain derivation, the 14-site census with per-site verdicts, the pin case's name
and its named production guarantee, the residue sweep's three results, the before/after green
readings of the two rebuilt cases, and the rewritten `files_modified` set replacing this plan's
provisional candidate list.
</output>
