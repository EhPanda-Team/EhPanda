---
phase: 15-continued-background-downloads
plan: 43
type: execute
wave: 43
depends_on: ["15-42"] # File overlap on DownloadClient+ContinuedSession.swift (15-42 reworks pauseAllSchedulable; this plan corrects trust-set docs in the same file) and xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-23]
requirements: [SC1, SC2]

must_haves:
  truths:
    - "The invariant is stated and enforced over the whole refusal FAMILY, not one branch: the run-start announcement (and the session trust it must now carry) follows 'this run has real page work to do over this record' — the working folder cannot supply the pages the manifest claims — which is true on ALL THREE refusal kinds (failed enumeration, all-unprobed, all-or-nothing residual) exactly as on the proceeding branch, so a REFUSED reconciliation over a complete-reading record can no longer produce a zero-progress run and a terminal `0 / 1 page · 0 galleries` card (SC2), and D-11's stall-expiration amplifier loses this door (SC1)."
    - "The trust admission is EXPLICIT, because an announcement alone provably changes nothing on this branch: the push's own admission is a `formUnion` sourced from `snapshot.incompleteGalleryIDs`, which by construction cannot contain a complete-reading record — re-derived from source at execution time by grepping every occurrence of `observedIncompleteSessionGIDs`, with the writer census quoted in the summary before the fix is landed."
    - "Both constraints from earlier gaps hold and are asserted, not assumed: D-G4-01's ceiling guarantee — trust is granted at the run's own preparation, never at queue time, pinned by the K=N case's queued-window assertion and by testACompleteGalleryQueuedForUpdateOpensTheCardAtZero passing byte-unchanged — and D-G7-01's withdrawal accounting, with the insert's position relative to the enclosing bracket derived and recorded (which side it lands on and why the composition stays coherent for later movements of the same gid)."
    - "The regressions drive the production route and cover the family: a K=N residual-refusal case and a failed-enumeration companion, both over a complete-reading record reached through `retryPages` → `resumeMode` exactly as the existing K=1 case is, both asserting the drain reports the gallery's real work rather than the pinned zero — and the existing K=1 case passes byte-unchanged, pinning the proceeding side the fix must not move."
    - { statement: "On a physical iOS 26 device, a repair or re-download of a gallery whose files are gone while its record reads complete climbs or holds its card honestly instead of reporting zero for the entire run", verification: backstop }
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "prepareWorkingSeedAnnouncingProgress gated on the real-page-work predicate with the explicit trust insert ahead of its push; the refuted flush-time cost sentence and the single-admission sentence corrected from fresh source enumerations"
      contains: "observedIncompleteSessionGIDs.insert"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift"
      provides: "The K=N residual-refusal regression and its failed-enumeration companion, as an extension of the ledger suite in a new file"
      contains: "extension DownloadContinuedSessionLedgerTests"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      via: "the announcement's explicit insert makes schedulableSnapshot's isSessionWork disjunct true for the refused record, so its pages enter the numerator and its departure retires at the honest record count instead of zero"
      pattern: "observedIncompleteSessionGIDs"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      via: "testingPrepareWorkingSeedAnnouncingProgress drives the production preparation under both refusal stagings, and every push asserted is production-issued"
      pattern: "testingPrepareWorkingSeedAnnouncingProgress"
  prohibitions:
    - statement: "Must NOT grant trust at queue time or weaken shouldSchedule/schedulableSnapshot: the queued window still opens at zero (D-G4-01), pinned by the K=N case's queued-window assertion and testACompleteGalleryQueuedForUpdateOpensTheCardAtZero byte-unchanged."
      status: pending
      verification: automated
    - statement: "Must NOT touch reconcileWorkingManifestAgainstPageFiles' executable lines: the three refusal exits are the round-11/12/13 defence and stay exactly as they are — the fix moves the ANNOUNCEMENT basis, never the refusal basis."
      status: pending
      verification: automated
    - statement: "Must NOT weaken, delete, or restage an existing case: the K=1 ledger case, the three reconciliation-refusal cases, and testAnAnnouncementDuringTheClientStartHopSurvivesTheSeed all pass byte-unchanged."
      status: pending
      verification: automated
    - statement: "Must NOT add the new cases to DownloadContinuedSessionLedgerTests.swift (812 lines; the two stagings would crowd the 1000-line file_length error gate) — they live in a new extension file per the 15-41 relocation pattern — and must NOT hand-issue any push: the only pushes the spy records are session-ensure starts, production convergence pushes, and the preparation's own announcement."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<objective>
Close blocker G-15-23 (SC2, and SC1 through D-11). Verified in source at the round-13 HEAD, link
by link: `reconcileWorkingManifestAgainstPageFiles` (`DownloadClient+ExecutionSupport.swift:504-547`)
has THREE refusal exits that all return the manifest unchanged — `guard pageFileScan.scanSucceeded`
(`:509`), the per-file skip leaving `blankedPageCount == 0` (`:521`), and the all-or-nothing
residual (`:530`). Over a record whose manifest claims all N pages, a refusal writes no manifest
and republishes no index record, so `prepareWorkingSeedAnnouncingProgress` (`:379-393`) finds
`workingSeed.manifest.isComplete` true and SKIPS its announcement. The trust set is only ever
grown from `snapshot.incompleteGalleryIDs` — two `formUnion` writers, no `insert` — which by
construction cannot contain a complete-reading record, and the flush path is monotone upward
(`refreshManifestPageFileHashes` only ever assigns non-empty hashes), so the record never becomes
honest during the run. The gid contributes 0 to the numerator for the entire N-page re-download
and departs untrusted, retiring zero: a terminal `0 / 1 page · 0 galleries` card over real work —
G-15-5's exact shape, surviving inside the branches the positive-signal defence built. Per D-11
the scheduler force-expires the tasks reporting least progress, and an expiration here pauses
every schedulable download.

The verifier NARROWED nothing and BROADENED the reachability: this does not depend on a Files-app
deletion — a refusal of ANY of the three kinds over a complete-reading record produces the
identical zero-progress run, `resumeMode`'s own doc names the refusal as one of exactly two
states that still route such a record to `.repair` (`+SchedulingHelpers.swift:50-61`), and the
state is self-sustaining: the record stays complete, the next run resolves `.repair` again and
reports zero again. This plan covers the family, not one branch.

Purpose: the system card reflects real work for every start mode and every refusal kind (SC2,
D-06/D-10's queue-wide basis), and the maximally-stalled reading D-11 punishes cannot be
manufactured by the coordinator's own honesty defence (SC1).
Output: the real-page-work announcement gate with its explicit trust admission, corrected docs
derived from fresh enumerations, the two family regressions RED-first in a new extension file,
and a green full run.
</objective>

## Artifacts this plan produces

- **Changed gate + new trust admission in `prepareWorkingSeedAnnouncingProgress`:** the
  announcement condition becomes the real-page-work predicate (the working seed's `existingPages`
  short of `manifest.pageCount`), and `observedIncompleteSessionGIDs.insert(payload.gallery.gid)`
  is issued ahead of the push, both gated on the live session — the trust set's first `insert`
  writer.
- **New test file:** `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift`
  — an `extension DownloadContinuedSessionLedgerTests` (suite membership, traits, and test
  identity preserved, per the 15-41 pattern) holding
  `testAnAllPagesGoneRepairOfACompleteReadingRecordReportsItsWorkAndDrainsFull` and
  `testAFailedEnumerationRepairOfACompleteReadingRecordStillEarnsSessionTrust`.
- **Possible shared helper promotion** into `DownloadFeatureTestHelpers.swift` if a staging
  helper is needed by both this file and an existing suite — never duplicated.
- **Doc corrections:** the announcing wrapper's trust-admission sentence, the D-G5-01 defence
  doc's refuted cost sentence (`+ExecutionSupport.swift:496-503` region), and any
  `observedIncompleteSessionGIDs` writer claim the insert invalidates in
  `+ContinuedSession.swift` / `+Manager.swift` — each rewritten rule-phrased from a fresh grep.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage the K=N residual refusal and the failed-enumeration companion, RED-first</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-23 entry WHOLE (detail steps 1-6, why_tests_missed_it, suggested_fix): the family definition, the two required stagings, and the constraint that the K=1 case pins the side the fix must not move
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerTests.swift` — `testARepairOfACompleteReadingRecordReportsItsWorkAndDrainsFull` (`:596-678`: the exact staging shape to mirror — fixture record 6/6, `resumeMode == .repair` grounding through the production route, `retryPages`, the queued-window zero assertion, `testingPrepareWorkingSeedAnnouncingProgress`, page-file writes + production `flushManifestPageProgress`, `settleCompletedDownload` + `scheduleNextIfNeeded` drain, the series helpers) and the suite header's choreography-discipline paragraph
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift` — `testAWholesaleScanFailureBlanksNothingWritesNothingAndWithdrawsNothing` (`:51`—: the folder-permissions `0o000` staging that makes the directory enumeration fail, and its permissions-restoring `defer` discipline — the companion reuses this shape)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — `makeQueuedCoordinator`, `writePageFiles`, `makeRepairPayload(for:)`, `lastPushedPair`, `expectTheCompletedSeriesNeverRewinds`, `expectTheFractionReachesOneOnlyAtTheDrain` (note which of these the new cases may and may NOT assert — see behavior), `waitUntil`
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeedAnnouncingProgress` (`:355-393`, the gate under test) and the three refusal exits of the reconciliation (`:509`, `:521`, `:530`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift` — `resumeMode` (`:38-69`): the (a)-refusal arrival state the stagings exercise
    - `.swiftlint.yml` — the lint gates all new test code must satisfy from the start
  </read_first>
  <behavior>
    - `testAnAllPagesGoneRepairOfACompleteReadingRecordReportsItsWorkAndDrainsFull` — the K=N
      residual refusal, expected to FAIL against current source. Mirror the K=1 case with ONE
      difference: NO page files are staged at all, so the reconciliation's residual guard refuses
      (blanking all 6 would take every claimed page). Stage the 6/6 complete-reading fixture,
      reload the index, ground the route (`resumeMode == .repair` via the production
      missing-files branch), `retryPages` over all six indices, assert the queued window still
      opens at zero (`startSubtitles.last == "0 / 6 pages · 1 gallery"` — the D-G4-01 ceiling
      assertion), then drive `testingPrepareWorkingSeedAnnouncingProgress`. Assert AFTER the
      preparation: the record is UNCHANGED at `completedPageCount == 6` (the refusal blanked and
      republished nothing) and a push carrying `"6 / 6 pages · 1 gallery"` is present — the
      announcement over the newly-trusted raw basis. Then land all six pages through the
      production flush (`writePageFiles` for the working folder + `flushManifestPageProgress`),
      settle and schedule. Terminal assertions: `lastPushedPair` reads 6/6, subtitle
      `"6 / 6 pages · 0 galleries"`, `finishSuccesses == [true]`, `rejectedProgressUpdates`
      empty, `expectTheCompletedSeriesNeverRewinds`. Do NOT assert
      `expectTheFractionReachesOneOnlyAtTheDrain` here and record why in the case's doc comment:
      a trusted complete-reading record honestly rides at its ceiling during a refused-repair run
      — the record genuinely claims N and the refusal is the defence against destroying hashes —
      so the fraction reaches one before the drain BY DESIGN on this family; the harm the case
      pins is the pinned-zero run, not the ceiling.
    - `testAFailedEnumerationRepairOfACompleteReadingRecordStillEarnsSessionTrust` — the
      directory-level refusal companion, expected to FAIL against current source. Same 6/6
      fixture WITH all six page files present. Ground the route the same way, `retryPages`, then
      drop the working folder's POSIX permissions to `0o000` (the wholesale-failure staging
      shape, with the permissions-restoring `defer` discipline) so the destination enumeration
      fails and the reconciliation refuses at `scanSucceeded`. Drive the announcing preparation.
      Assert: the record unchanged at 6, a push carrying `"6 / 6 pages · 1 gallery"` present,
      `rejectedProgressUpdates` empty. Restore permissions, settle and schedule; terminal
      assertions: `lastPushedPair` 6/6, subtitle `"6 / 6 pages · 0 galleries"`,
      `finishSuccesses == [true]` — versus the pre-fix terminal `0 / 1 page · 0 galleries`.
    - Pre-fix RED readings for both: no `"6 / 6 pages · 1 gallery"` push after the preparation,
      and a terminal `0 / 1 page · 0 galleries` drain — quoted in the summary as RED evidence.
  </behavior>
  <action>
**Step 1 — the file.** Create `DownloadContinuedSessionLedgerRefusalTests.swift` as
`extension DownloadContinuedSessionLedgerTests` (no new `@Suite` — membership, traits, and
identity preserved, the 15-41 relocation pattern applied to additions). The header doc states
why the family lives here: the ledger suite is at 812 lines against the 1000-line `file_length`
error gate, and these are ledger cases — what the SESSION reports for the run that follows a
refusal — not reconciliation cases, which assert the refusal itself and already live in
`DownloadContinuedSessionReconciliationTests`. The helpers these stagings need
(`writePageFiles`, `makeRepairPayload`, `lastPushedPair`, the series helpers) are already in the
shared helpers file; the ledger file's private manifest-patching helpers are deliberately NOT
used — record state comes only from fixture manifests, page files, and production routes. If a
genuinely shared staging helper is needed, promote it into `DownloadFeatureTestHelpers.swift`;
never duplicate.

**Step 2 — the two cases per the behavior block.** Choreography discipline is the standing
mandate: both cases ground `resumeMode == .repair` through the production route before calling
`retryPages` (the K=1 case's pattern), every push asserted is production-issued (the session
ensure, convergence pushes, and the preparation's own announcement — no direct push calls), and
the companion's permissions staging carries the restoring `defer` so a failed assertion cannot
strand an unreadable folder.

**Step 3 — the honest RED, single invocation.** Run the ledger suite once, targeted (the
extension file's cases run with the suite). Both new cases MUST fail against current source with
the pinned-zero readings; every pre-existing ledger case MUST pass unchanged in the same run.
Record the observed failure readings in the summary as RED evidence. If either new case
unexpectedly PASSES, stop: the staging did not reach its refusal exit (check the residual
arithmetic for the K=N case and the folder-permission drop for the companion) — do not proceed
to Task 2 on a green case; re-derive and record.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionLedgerTests</automated>
  </verify>
  <acceptance_criteria>
    - Both cases exist in the new file: `grep -c 'testAnAllPagesGoneRepairOfACompleteReadingRecordReportsItsWorkAndDrainsFull' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` outputs `1`, and the same for `testAFailedEnumerationRepairOfACompleteReadingRecordStillEarnsSessionTrust`.
    - The file extends the suite rather than declaring one: `grep -c 'extension DownloadContinuedSessionLedgerTests' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` outputs at least `1` and `grep -c '@Suite' ...` outputs `0`.
    - Both cases ground the production route: each asserts `resumeMode` resolves `.repair` for the staged record before `retryPages` runs (source inspection, quoted in the summary).
    - The K=N case carries the queued-window zero assertion (D-G4-01's ceiling pin) and its doc comment records why the fraction-only-at-drain helper is deliberately not asserted on this family.
    - Both cases are observed RED at this task's end with the pinned-zero readings quoted in the summary; `DownloadContinuedSessionLedgerTests.swift` is untouched (`git diff --stat` shows no change to it; `wc -l` still reports 812).
    - No direct push call in either case body: the spy's recorded updates come only from production seams (grep over the new file for push-issuing calls finds only `testingEnsureContinuedSession` and `testingPrepareWorkingSeedAnnouncingProgress`).
  </acceptance_criteria>
  <done>The refusal family has two deterministic regressions in a new extension file, both failing honestly against the announcement gate with the pinned-zero readings recorded, and the proceeding-side K=1 pin untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the announcement on real page work, admit trust explicitly, and correct the docs the fix invalidates</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift, AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — G-15-23's `suggested_fix` WHOLE: the announcement-basis rule, the two mandatory pre-fix derivations, and the two constraints from earlier gaps
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeedAnnouncingProgress` and its doc (`:355-393`), `prepareWorkingSeed` (`:296-353`: where the D-G7-01 bracket closes and what `existingPages` is assigned from), `withdrawingCountedBasisMovement` (`:268-282`: the `wasCountedBasis` test the insert changes for LATER movements), the D-G5-01 defence doc's cost paragraph (`:496-503` region)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `schedulableSnapshot` (`:121-144`: the `isSessionWork` disjunct and `incompleteGalleryIDs` construction), `reconcileRetiredSessionPages` (`:493-530`: the departure gate and the `formUnion` writer), `markContinuedSessionEnded` and `ensureContinuedSession`'s resets — every `observedIncompleteSessionGIDs` touch, AS 15-42 LEFT the file
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the `observedIncompleteSessionGIDs` declaration and its doc (the `:522` region): whether it carries a writer claim the insert invalidates
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `pageFileScan(folderURL:manifest:)` and `PageFileScan`: derive what `pages` contains so the real-page-work predicate is derived, not assumed
    - `AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift` — `resumeMode`'s (a)/(b) doc (`:50-61`): re-verify it stays true under the fix and quote it as re-verified
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <behavior>
    - Both Task 1 cases go GREEN with no change to their assertions.
    - The K=1 case, the three reconciliation-refusal cases,
      `testACompleteGalleryQueuedForUpdateOpensTheCardAtZero`, and
      `testAnAnnouncementDuringTheClientStartHopSurvivesTheSeed` all pass byte-unchanged.
    - The full FeatureTests plan is green in one invocation.
  </behavior>
  <action>
**Step 0 — the two mandatory derivations, before any edit.** (1) Grep every occurrence of
`observedIncompleteSessionGIDs` in `AppPackage/Sources` and record the census in the summary:
the declaration, the resets, the reads, and the additive writers. Confirm the verifier's claim —
both additive writers are `formUnion(snapshot.incompleteGalleryIDs)` and `incompleteGalleryIDs`
is built from `isIncomplete`, so an announcement alone cannot admit a complete-reading record.
If source disagrees, STOP and re-derive the fix. (2) Derive from `pageFileScan` what
`workingSeed.existingPages` contains, and confirm the predicate below is true on every refusal
branch over a complete-reading record, true on the proceeding branch when pages are missing, and
false exactly when the folder supplies every page a complete manifest claims.

**Step 1 — the gate and the insert.** In `prepareWorkingSeedAnnouncingProgress`, replace the
manifest-completeness announcement condition with the real-page-work predicate —
`workingSeed.existingPages.count < workingSeed.manifest.pageCount` — and, inside the same
live-session branch, issue `observedIncompleteSessionGIDs.insert(payload.gallery.gid)` BEFORE
`await pushContinuedSessionProgress(sessionID:)`, so the announcement itself carries the
newly-trusted basis. Both remain gated on `continuedSessionID` exactly as today. No other
executable line in the function moves.

**Step 2 — the bracket-side derivation (D-G7-01).** State, in the function's doc and in the
summary, which side of the enclosing `withdrawingCountedBasisMovement` bracket the insert lands
on and why the composition is coherent: it lands AFTER the bracket has closed, so the
preparation's OWN movement is measured against the pre-announcement trust state — an untrusted
complete-reading record contributed nothing and withdraws nothing, which keeps D-G4-01's ceiling
guarantee intact — while every LATER movement of the same gid in the same session finds
`wasCountedBasis` true through the trust set and withdraws its counted portion, which is correct
because from the announcement on the gallery's pages ARE in the numerator.

**Step 3 — the docs the fix invalidates, from fresh enumerations.** Correct the announcing
wrapper's trust-admission sentence: trust is now granted at exactly the places the session can
OBSERVE incompleteness or PROVE page work — the push-side `formUnion` over snapshot
incompleteness, and the run's own announcement over a record whose folder cannot supply its
claims — phrased as that rule rather than as a site count, so nothing here is a number that can
drift (if a count survives anyway, pin it in `DownloadSourceInventoryTests` on the established
pattern). Correct the D-G5-01 defence doc's cost paragraph: its closing claim that the flush path
restores the record's honesty is refuted for the refusal family — the flush path is monotone
upward, so a complete-reading record never becomes incomplete during the run — and the paragraph
must now state that the announcement's explicit admission is what covers that family. Sweep the
`observedIncompleteSessionGIDs` docs in `+ContinuedSession.swift` and `+Manager.swift` for any
writer claim the insert invalidates and correct those the same way. Re-verify `resumeMode`'s
(a)/(b) doc still reads true and quote it as re-verified. Every corrected sentence is derived
from the Step-0 census, never from the gap record's wording.

**Step 4 — green.** Run the ledger suite targeted (both new cases green, all existing ledger
cases green), then ONE full FeatureTests run. Never overlap invocations.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The insert exists ahead of the push: `grep -c 'observedIncompleteSessionGIDs.insert' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `1`, and source inspection (quoted) shows it inside the live-session branch, before the push call.
    - The old gate is gone: `grep -c 'workingSeed.manifest.isComplete' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `0`, and the new predicate reads `existingPages.count < ` against the manifest's page count (quoted).
    - The refusal mechanism is untouched: `git diff` shows no change to `reconcileWorkingManifestAgainstPageFiles`' executable lines and no change to `schedulableSnapshot`, `shouldSchedule`, or the retirement ledger.
    - The refuted cost sentence is gone: `grep -c 'catches up at flush time' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `0`; the single-admission phrasing is gone: `grep -c 'in exactly one place' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `0`; both replacements are quoted in the summary beside the Step-0 census that derived them.
    - The bracket-side derivation (which side the insert lands on and why later movements compose) is recorded in the function doc and the summary.
    - Both Task 1 cases' green flips are recorded against their RED readings; the K=1 case, the three reconciliation-refusal cases, `testACompleteGalleryQueuedForUpdateOpensTheCardAtZero`, and `testAnAnnouncementDuringTheClientStartHopSurvivesTheSeed` pass byte-unchanged.
    - The targeted run and one full FeatureTests run both exit `0`, each a single invocation.
  </acceptance_criteria>
  <done>A refused reconciliation over a complete-reading record announces its run and earns session trust, proven by both family regressions flipping green while the proceeding-side and ceiling pins never moved, with the trust-writer census and the bracket-side derivation recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Refusal defence → session accounting | The defence that protects recorded hashes (refusing destructive blanking) starves the session's trust basis, so honesty protection manufactures the stalled reading the scheduler punishes |
| Trust set → monotonic floor | Admitting a gid to the trust set changes what later basis movements withdraw; a wrong bracket-side lands over- or under-withdrawal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-43-01 | Denial of Service | A refused reconciliation producing a zero-progress run that D-11's expiration turns into a queue-wide pause | high | mitigate | Real-page-work announcement gate + explicit trust insert; two family regressions pin residual and directory refusals |
| T-15-43-02 | Tampering | Trust granted at queue time reopening D-G4-01's ceiling for queued updates/redownloads | high | mitigate | Insert lives only in the run's own preparation; queued-window zero assertion and the queued-update case pass byte-unchanged |
| T-15-43-03 | Tampering | The insert flipping wasCountedBasis for the same preparation's own movement, over-withdrawing from the floor | medium | mitigate | Insert lands after the bracket closes; the side and its composition argument are derived and recorded in doc + summary |
| T-15-43-04 | Repudiation | Corrected doc sentences written from the gap record instead of source, seeding the next drift round | medium | mitigate | Step-0 census is mandatory before any edit; every corrected sentence quotes its deriving enumeration |
</threat_model>

<verification>
- Task 1 targeted run (both new cases RED, existing ledger cases green); Task 2 targeted run
  then one full FeatureTests run. xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — the phase has no requirement IDs to
  probe; must_haves derive from G-15-23's truth_violated clause (SC2's real-progress half, SC1
  through D-11) and the phase success criteria.
- <assumption_delta_decision>no-change — the trust set gains a writer but keeps its shape and
  reset discipline; no identity model changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan: the backstop
  truth records the device-only observation without closing it. The verifier's own note stands:
  run the hardware test AFTER this gap closes, since until then a refused repair's zero card is
  a known symptom rather than new information.
</verification>

<success_criteria>
- G-15-23 closed at the family: the announcement follows real page work on all three refusal
  kinds, trust is admitted explicitly at the run's own preparation, both regressions flip
  RED→green while the K=1, ceiling, and reconciliation-refusal pins never move, the invalidated
  docs are corrected from fresh enumerations, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-43-SUMMARY.md` when done, including
the RED readings and green flips, the Step-0 trust-writer census and predicate derivation, the
bracket-side derivation, and the quoted corrected docs.
</output>
