---
phase: 15-continued-background-downloads
plan: 41
type: execute
wave: 41
depends_on: ["15-40"] # File overlap on DownloadClient+ExecutionSupport.swift (IN-04 sits in the function family 15-39/15-40 touched) and xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-21]
requirements: [SC1, SC2]

must_haves:
  truths:
    - "The four source hygiene items are closed at their roots, not suppressed: WR-06's dead disjunct is gone and the function's name matches what it does (the `.error` guard admitted iterations whose body did nothing — dead code shaped like a lost branch, and a name promising a normalization the body never performed); IN-02's `if let failedPage` arm is indented level with its two sibling arms; IN-03's `captureCachedPage` bound refuses a zero-page record with the same guard shape as the seven G-15-14 range sites (`pageCount > 0` plus the honest upper bound) instead of the max-widened bound that ADMITTED index 1 for a zero-page record — the one site the G-15-14 sweep widened rather than guarded; IN-04's selection binding stops rebuilding a `Set` from a value already typed `Set<Int>?`."
    - "The file-length headroom G-15-12 restored and round 12 consumed is restored again: `DownloadContinuedSessionBasisTests.swift` is split by MOVING a cohesive case family into a new file as an extension of the same suite (test identity and traits preserved), both files end well under the 1000-line `file_length` error gate with real headroom for future rounds, and the move is a move — case bodies land byte-identical apart from relocation mechanics, no assertion weakened, no case dropped, total test count unchanged (SC1/SC2: this suite family is the deterministic proof surface for the continuation and card criteria; its headroom is what lets the next regression land where it belongs)."
    - "Contract-faithful doubles and choreography survive the split: every moved case keeps its production-issued pushes and stagings exactly as written (the standing mandate — a fix or move must not convert a production-issued observation into a hand-issued one), and any helper the moved cases need crosses with them or is promoted to the shared helpers file, never duplicated."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift"
      provides: "WR-06 closed: the single-condition guard and a name that matches the cancellation-clearing behavior, with all call sites swept"
      contains: "shouldClearCancellationError"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift"
      provides: "IN-02 closed: the three return arms of buildInspectionPages at sibling indentation"
      contains: "buildInspectionPages"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift"
      provides: "IN-03 closed: captureCachedPage guarded against a zero-page record with the G-15-14 guard shape and a one-line class-tying comment"
      contains: "captureCachedPage"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "IN-04 closed: the pageSelection binding without the redundant Set rebuild"
      contains: "selectedIndices"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift"
      provides: "The relocated case family as an extension of DownloadContinuedSessionBasisTests, with the helpers it needs"
      contains: "extension DownloadContinuedSessionBasisTests"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift"
      provides: "The remaining case family with restored headroom and a header note pointing at the sibling file"
      contains: "DownloadContinuedSessionBasisTests"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift"
      to: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift"
      via: "extension of the same @Suite type, so every moved case keeps its suite membership, traits, and test identity"
      pattern: "DownloadContinuedSessionBasisTests"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift"
      via: "captureCachedPage's new bound joins the same G-15-14 guard class buildInspectionPages already carries, closing the sweep's one widened site"
      pattern: "pageCount > 0"
  prohibitions:
    - statement: "Must NOT rewrite, restage, weaken, or drop any moved case: the split is a relocation, proven by an unchanged total test count and by case bodies that differ only in location."
      status: pending
      verification: automated
    - statement: "Must NOT change captureCachedPage's behavior for any currently reachable input: the new guard refuses only the zero-page record the old bound wrongly admitted (reachability is closed upstream by enqueue's zero-page refusal and validateDecodedManifest's empty-page rejection — this is defense in depth, and the derivation is recorded)."
      status: pending
      verification: automated
    - statement: "Must NOT leave a caller referring to the old WR-06 name anywhere in sources or tests, and must NOT keep the dead disjunct under the new name."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<objective>
Close G-15-21 (warning; grouped because this project's bar treats nitpicks as blocking). Verified
in source at HEAD 47d23e1c:

- **WR-06** — `normalizeNeedsAttentionDownloads` (`+PersistenceNormalize.swift:32-48`) guards on
  `displayStatus == .error || shouldClearCancellationError` and then acts only under
  `shouldClearCancellationError`: the first disjunct admits an iteration whose body does nothing,
  the function is exactly equivalent to the second condition alone, and the name promises an
  `.error` normalization the body does not perform.
- **IN-02** — `buildInspectionPages` (`+PublicAPIHelpers.swift:34-42`): the `if let failedPage`
  arm's return is indented four spaces deeper than its two sibling arms.
- **IN-03** — `captureCachedPage` (`+PublicAPI.swift:286-289`) bounds its index with the
  max-widened form, ADMITTING index 1 for a zero-page record — the one page-count site the
  G-15-14 sweep widened rather than guarded. Latent (reachability closed upstream), but it would
  write a page file no manifest claims, invisible to `pageFileScan` and skipped by
  `refreshManifestPageFileHashes`.
- **IN-04** — `pendingPageIndices` (`+ExecutionSupport.swift`, the selection binding) rebuilds a
  `Set` from `payload.pageSelection`, which is already declared `Set<Int>?`
  (`AppModels/Download/DownloadedGallery+Extensions.swift:30`).
- **File-length headroom** (found by the verification itself): `DownloadContinuedSessionBasisTests.swift`
  is at 996 lines against the 1000-line `file_length` ERROR gate — the headroom G-15-12 restored,
  consumed by round 12. No future round can land a case there until it is split.

Purpose: zero hygiene residue and restored suite headroom, so the next round's regressions have
somewhere to live and the widened-not-guarded site cannot become the next G-15-14.
Output: four root-cause source fixes, the basis suite split as a pure relocation, and a green
full run with an unchanged test count.
</objective>

## Artifacts this plan produces

- **Renamed function:** the WR-06 normalizer under a name matching its cancellation-clearing
  behavior (executor names it during the caller sweep; e.g. `clearCancellationLikeDownloadErrors`),
  with every call site updated.
- **New test file:** `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift`
  — an `extension DownloadContinuedSessionBasisTests` holding the relocated case family (the
  reconciliation/probe-defence cases are the expected cohesive cluster; the executor confirms the
  cut line by helper usage) plus whichever file-private helpers travel with them.
- **Possibly promoted helpers** in `DownloadFeatureTestHelpers.swift` if a helper is needed by
  cases remaining in BOTH files after the split.
- No new production symbols beyond the rename.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-40-SUMMARY.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the four source hygiene items at their roots</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift, AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift, AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-21 entry WHOLE (each item's confirmed mechanism and suggested fix)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift` — `normalizeNeedsAttentionDownloads` (`:32-48`) and its neighbors (`normalizeInterruptedDownloads` follows — the naming family the rename must fit); then grep the repository for every caller of the old name before renaming
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift` — `buildInspectionPages` (`:6-50`: the three-arm selection and its G-15-14 guard comment, the shape IN-03's comment ties to)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift` — `captureCachedPage` (`:280-301`) and, for the guard-shape precedent, `enqueue`'s zero-page refusal (`:73` region)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `pendingPageIndices` (the selection binding and its surrounding G-15-14 doc), AS 15-39/15-40 LEFT the file
    - `AppPackage/Sources/AppModels/Download/DownloadedGallery+Extensions.swift` — `:30` region: confirm `pageSelection`'s declared type before dropping the rebuild
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadZeroPagePayloadTests.swift` — the G-15-14 suite: whether an existing staging route reaches `captureCachedPage` with a zero-page record without new production seams
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**WR-06.** Delete the dead disjunct so the loop guards on `shouldClearCancellationError` alone,
AND rename the function to name what it actually does — clearing cancellation-like errors — since
deleting the disjunct alone leaves a name promising an `.error` normalization the body never
performs (both halves of the finding are real). Sweep every caller in sources and tests with a
repository grep for the old name and update them; keep the function's placement and access as
they are. One-line doc stating what it clears and why (a cancellation-like error is interruption
residue, not a failure the user must attend to — derive the exact wording from
`isCancellationLikeAppError`'s semantics, read fresh).

**IN-02.** Re-indent the `if let failedPage` arm's return to sibling level. No other change in
the function.

**IN-03.** Replace the max-widened upper bound in `captureCachedPage` with the G-15-14 guard
shape: refuse when `download.pageCount` is not positive, keep `index >= 1`, and bound by
`download.pageCount` itself. Add the one-line comment tying it to the same class as the seven
range sites (per the gap's suggested fix). Then decide the pin: if
`DownloadZeroPagePayloadTests`' existing staging idiom can reach `captureCachedPage` with a
zero-page record WITHOUT adding a production seam, add the case there; otherwise record the
static derivation in the summary (guard shape + the two upstream closures — `enqueue`'s
zero-page refusal and `validateDecodedManifest`'s empty-page rejection) exactly as 15-34 recorded
its trap sites. Do not add a seam for this.

**IN-04.** In `pendingPageIndices`, bind the selection directly from `payload.pageSelection`
after confirming its declared type — the binding keeps the name `selectedIndices` and the
`Set`-membership use below is unchanged.

**Then:** build-level confidence only (the full run comes after Task 2 — these edits are pinned
by the existing suite). Do not run xcodebuild here; Task 2 runs everything once, keeping this
machine's one-invocation rule trivially satisfied.
  </action>
  <verify>
    <automated>git diff --stat -- AppPackage/Sources/DownloadClient</automated>
  </verify>
  <acceptance_criteria>
    - WR-06: `grep -rc 'normalizeNeedsAttentionDownloads' AppPackage` sums to `0` across all matches (old name gone everywhere), the new name appears at the definition and every former call site, and the loop's only guard is `shouldClearCancellationError` (source inspection, quoted).
    - IN-02: the three `return .init(` arms of `buildInspectionPages` share one indentation column (source inspection, quoted).
    - IN-03: `grep -c 'max(download.pageCount, 1)' AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift` outputs `0`, the new guard reads `pageCount > 0` with the honest bound (quoted), the class-tying comment exists, and the pin decision (test case or recorded derivation) is in the summary.
    - IN-04: `grep -c 'pageSelection.map(Set.init)' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `0` and the direct binding is quoted in the summary.
    - The diff touches only the four named functions and the WR-06 call sites (stated from `git diff`, quoted).
  </acceptance_criteria>
  <done>All four items are closed at their roots with call sites swept, the widened site carries the sweep's own guard shape, and each fix's proof (grep, quote, or derivation) is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Split the basis suite as a pure relocation and restore its headroom</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift` — the WHOLE file: the suite header (its charter and choreography-discipline paragraph — both files must carry the halves that apply to them), every case's topic (identify the cohesive cluster: the reconciliation/probe-defence family — wholesale scan failure, mass-partial probe failure, genuine partial loss — versus the basis/floor arithmetic family), and the `private extension` helper block at `:953`— (which helpers serve which cases — file-private helpers do not cross files)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — what is already shared, so nothing is promoted that exists or duplicated that is promotable
    - The 15-39 summary's note on any helper it already promoted (avoid a second copy)
    - `.swiftlint.yml` — the `file_length` gate both result files must clear with margin
  </read_first>
  <action>
**Step 1 — choose the cut by helper usage, then move.** Identify the case family whose move (a)
leaves both files well under the gate with real headroom and (b) minimizes helper surgery — the
reconciliation/probe-defence family is the expected cluster, but the executor confirms against
actual helper usage. Create `DownloadContinuedSessionReconciliationTests.swift` as
`extension DownloadContinuedSessionBasisTests` so every moved case keeps its suite membership,
traits, and test identity. Move the cases byte-identical: staging, assertions, doc comments, and
production-issued choreography all unchanged apart from relocation mechanics (imports, and the
helper access below).

**Step 2 — helpers travel or get promoted, never duplicated.** A file-private helper used ONLY
by moved cases moves with them (into a private extension in the new file). A helper used by
cases in BOTH files after the split is promoted once into `DownloadFeatureTestHelpers.swift`
(internal, doc kept). No helper exists twice when this task ends.

**Step 3 — headers.** The new file's header states which half of the original charter it holds
and points back; trim the original header's description to its remaining half and add the
pointer forward. Both keep the choreography-discipline paragraph if their cases rely on it.

**Step 4 — the count proof and green.** Record the FeatureTests test count from the most recent
full run (15-40's Task 2). Run ONE full FeatureTests run. The count must be unchanged by this
plan's Task 2 relative to that baseline plus any cases Task 1 added, every suite green, and
`wc -l` on both files recorded — both under the gate with real headroom (target: neither file
above ~850 lines, so the next round's cases have room where they belong).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The new file exists as an extension of the suite: `grep -c 'extension DownloadContinuedSessionBasisTests' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionReconciliationTests.swift` outputs at least `1`, and it declares no new `@Suite`.
    - Pure relocation: the full-run test count matches the recorded baseline (plus Task 1's optional zero-page pin, if added), zero failures; the summary lists each moved case name and states its body moved unchanged.
    - No helper is duplicated: for each helper the split touched, a repository grep for its name over the test target shows exactly one definition (summary lists them).
    - Headroom restored: `wc -l` on `DownloadContinuedSessionBasisTests.swift` and the new file both recorded, each at most ~850 lines, neither anywhere near 1000.
    - The full FeatureTests run exits `0` in a single invocation.
  </acceptance_criteria>
  <done>The basis suite family has real headroom again, split along its own topic seam with every case's identity, staging, and choreography intact, proven by an unchanged count and a green full run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest-unclaimed writes → download folder integrity | IN-03's widened bound could write a page file no manifest claims — invisible to the scan, skipped by hash refresh (latent; upstream-closed) |
| Test-suite surgery → proof surface | The split moves the deterministic proof surface for SC1/SC2; a lossy move would silently retire pinned invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-41-01 | Tampering | captureCachedPage writing an unclaimed page file for a zero-page record | low | mitigate | The G-15-14 guard shape closes the widened site; upstream closures recorded as defense-in-depth derivation |
| T-15-41-02 | Repudiation | A lossy suite split dropping or weakening a pinned regression unnoticed | medium | mitigate | Extension-based move preserves identity; unchanged-count proof plus per-case move listing in the summary |
| T-15-41-03 | Denial of Service | WR-06's dead branch inviting a future edit that "restores" behavior that never existed | low | mitigate | Dead disjunct deleted and the function renamed to what it does, call sites swept |
</threat_model>

<verification>
- Task 1 is build-level only; Task 2 runs the single full FeatureTests invocation for the plan.
  xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from G-15-21's grouped items and the SC labels the touched surfaces back (SC1, SC2).
- <assumption_delta_decision>no-change — hygiene closures and a test relocation; no identity
  model changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan.
</verification>

<success_criteria>
- G-15-21 closed: the four source items fixed at their roots with proofs recorded, the basis
  suite split as a verified pure relocation with both files holding real headroom, and the full
  suite green at an unchanged count.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-41-SUMMARY.md` when done, including
the WR-06 rename and its caller sweep, the IN-03 pin decision, the moved-case listing with the
helper dispositions, and both files' final line counts.
</output>
