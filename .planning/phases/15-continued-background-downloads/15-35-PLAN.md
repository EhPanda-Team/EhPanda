---
phase: 15-continued-background-downloads
plan: 35
type: execute
wave: 35
depends_on: ["15-34"] # xcodebuild serialization; the rewritten docs must describe the source as it stands after the two blocker fixes land.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-15]
requirements: [SC1, SC2]

must_haves:
  truths:
    - "No load-bearing doc comment in the session lifecycle asserts something source contradicts: the nil-client skip states the true recovery mechanism (a start-window push is dropped, not replayed — only the drain branch records reconciliation debt, and repaint comes from the next flush or convergence push), the suspension claims at all three sites carry ONE canonical wording that matches the traced source (same-actor calls that do not suspend today; an await introduced inside them reopens the window and needs its own re-validation), and the floor's writer inventory matches source exactly, teardown writer included, labeled verified exhaustive by grep. A wrong written premise is this phase's single recurring failure shape — it produced G-15-3 (the wrong suspension point led to guarding the wrong invariant) and G-15-7 (a one-mover claim where source had four) — and every premise a gap named wrong dies in this plan (SC1/SC2: these comments are the reasoning surface future fixes to liveness and progress arithmetic stand on)."
    - "Each correction is decide-one, not both: WR-01 chooses stating the truth over setting the debt flag on every skipped push (production behavior unchanged, rationale recorded), WR-02 chooses one wording applied verbatim-consistently at all three sites, WR-04 chooses naming all five writers plus the rule over dropping the count."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      provides: "Corrected nil-client skip comment (WR-01), canonical non-suspension wording at reconcileContinuedSession and pushContinuedSessionProgress, corrected ensureContinuedSession interleaving claim (WR-02)"
      contains: "do not suspend today"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      provides: "The floor's writer inventory naming all five writers including markContinuedSessionEnded's teardown zero, with the session-scoped rule and a verified-exhaustive-by-grep label (WR-04)"
      contains: "markContinuedSessionEnded"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      via: "the writer inventory on the floor's declaration names the teardown writer implemented in markContinuedSessionEnded, so the count and the closing session-scoped rule no longer contradict each other"
      pattern: "lastPushedCompletedPageCount"
  prohibitions:
    - statement: "Must NOT change any production behavior: this plan edits only comments; the diff contains no executable-line change."
      status: pending
      verification: automated
    - statement: "Must NOT leave both halves of a decide-one standing (a corrected sentence beside the old claim it corrects)."
      status: pending
      verification: automated
---

<objective>
Close G-15-15 (warning; promoted because a written premise that source contradicts is this
phase's single recurring failure shape — it produced G-15-3, where a doc naming the wrong
suspension point led to guarding the wrong invariant, and G-15-7, where a one-mover claim stood
against four movers in source). Three load-bearing doc comments assert things source does not
support, each verified against source by round 11:

- **WR-01** — the nil-client skip in `pushContinuedSessionProgress` claims the deferred
  reconcile after start recovers this update. The debt flag it depends on is set in exactly ONE
  place — the drain branch of `reconcileContinuedSession` — so every non-drain push landing in
  the start window is dropped with no debt recorded. Benign today (a drain always emits its own
  terminal push), but the comment cannot be used to reason about the window.
- **WR-02** — two functions in one file assert opposite things about whether the same call
  chain suspends: `reconcileContinuedSession`'s re-check doc says `hasPendingWork()` and its
  callees do not suspend (TRUE — traced: a shared-value read on a struct plus same-actor
  dictionary filters), while `pushContinuedSessionProgress`'s doc claims the snapshot read and
  the retirement reconcile both suspend (FALSE — same chain). A third instance sits on
  `ensureContinuedSession`, whose doc claims the liveness flag is stamped before the first
  point another caller could interleave while the guard line above it contains an await.
- **WR-04** — the floor's doc opens with a four-writer count and closes with a sentence
  describing a fifth: `markContinuedSessionEnded`'s teardown zero, which the list omits. An
  exhaustive-sounding list that is not exhaustive is what this phase lost four rounds to.

Purpose: the comments future fixes reason from say only true things, each decided one way.
Output: three corrected doc sites, zero executable changes, and a green full run.
</objective>

## Artifacts this plan produces

- **Corrected comments only** (no new symbols): the WR-01 skip rationale in
  `pushContinuedSessionProgress`; the canonical non-suspension wording at
  `reconcileContinuedSession`, `pushContinuedSessionProgress`, and `ensureContinuedSession`;
  the five-writer inventory with rule and exhaustiveness label on
  `lastPushedCompletedPageCount`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the three contradicted doc comments, each decided one way</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift, AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-15 entry WHOLE (each premise's source trace and the decide-one instruction per item)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — the nil-client skip comment inside `pushContinuedSessionProgress` (`:574-577` at planning time), the push's interleaving-dispositions paragraph (`:563-567`), `reconcileContinuedSession`'s re-check paragraph (`:399-403`, the TRUE wording the canonical sentence is derived from), `ensureContinuedSession`'s doc (`:165-191`, the synchronous-stamp claim at `:173-176`) and its guard line (`:193`, which contains the await the claim must acknowledge), the drain branch that sets the debt flag (`:416-418`), and `markContinuedSessionEnded`'s teardown zero (`:331` region — the fifth writer)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the floor's declaration doc (`:431-454`), whose writer count and closing session-scoped sentence currently contradict each other
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `withdrawingCountedBasisMovement` (the fourth writer the inventory names; read so the rewritten inventory describes it accurately, including its two call sites)
  </read_first>
  <action>
**WR-01 — state the truth at the nil-client skip (decide-one: no flag change).** Rewrite the
skip comment inside `pushContinuedSessionProgress`: a push landing while the client identity is
nil is DROPPED, not replayed — only the drain branch of `reconcileContinuedSession` records
reconciliation debt, because a dropped TERMINAL word is the one loss the next push cannot
repaint; a dropped live push is repainted by the next flush or convergence push, which recomputes
from the authoritative snapshot. Record why the flag is deliberately NOT set here: discharging a
deferred reconcile for every start-window push would run repair work for windows that need none
and would change production choreography for no observable defect. Production behavior is
untouched.

**WR-02 — one canonical wording at all three sites.** Derive the canonical sentence from the
TRUE instance on `reconcileContinuedSession`: these are same-actor calls that do not suspend
today; an await introduced inside them later reopens the window this guard closes and needs its
own re-validation — and the defensive re-checks stay under that single justification. Apply it,
adapted to each site's subject, at: (1) `reconcileContinuedSession`'s re-check paragraph (align
wording, keep meaning); (2) `pushContinuedSessionProgress`'s interleaving paragraph — delete the
claim that the snapshot read and the retirement reconcile suspend; the push's ONE real suspension
is the client seam's main-actor hop at `updateProgress`, and the ownership re-checks after the
same-actor reads are defensive under the canonical wording; (3) `ensureContinuedSession`'s doc —
replace the stamped-before-any-interleave claim with the truth: the guard line awaits
`hasPendingWork()`, which is same-actor and non-suspending today, so the stretch from the guard
through the id stamp admits no interleaving as written; a future suspension inside it would
reopen the two-starters window and needs its own re-validation.

**WR-04 — count all five writers and state the rule.** Rewrite the floor's writer inventory on
`lastPushedCompletedPageCount` in `DownloadClient+Manager.swift` to name FIVE writers: the
start-time reset, the additive seed merge, the per-push re-latch, the D-G7-01 withdrawal (one
implementation, two call sites), and `markContinuedSessionEnded`'s teardown zero. Keep the
session-scoped closing rule (cleared when a session starts and when one ends) — it now agrees
with the count instead of contradicting it. Label the list verified exhaustive by grep at this
HEAD, and perform that grep: every assignment to the property across
`AppPackage/Sources/DownloadClient` must be one of the five named writers; paste the grep output
in the summary. If the grep finds a sixth writer, that is a finding to report, not to absorb
silently.

**Verify the plan changed no behavior.** The diff for both files must contain only comment
lines. Run one full FeatureTests invocation to prove the tree still builds and passes (comment
edits inside doc examples can still break compilation; the run is the gate, not a formality).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The false suspension claim is gone: `grep -c 'can both suspend' AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` outputs `0`, and `grep -c 'do not suspend today' AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` outputs at least `3` (the canonical wording at all three sites).
    - The stamped-before-any-interleave claim is corrected: `grep -c 'before the first point another caller could interleave' AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` outputs `0`, and the rewritten `ensureContinuedSession` paragraph names the awaited guard call (quoted in the summary).
    - The recovery claim is corrected: `grep -c 'this update is recovered' AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` outputs `0`, and the rewritten skip comment names the drain branch as the only debt recorder and the next flush/convergence push as the repaint mechanism (quoted in the summary).
    - The writer inventory matches source: `grep -c 'Four writers, and no others' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `0`, the rewritten inventory names `markContinuedSessionEnded`, and the summary pastes the writer grep output showing every assignment maps to one of the five named writers.
    - Behavior unchanged: `git diff --stat` for this plan's commit shows only the two files, and the summary states from diff inspection that every changed line is a comment line.
    - The full FeatureTests run exits `0` in a single invocation.
  </acceptance_criteria>
  <done>All three contradicted premises are corrected in one decided direction each, the floor's writer inventory is exhaustive and grep-verified, no executable line moved, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
This plan edits doc comments only — no executable line, no new data flow, no API surface change,
so it has no security surface of its own. Its security relevance is indirect: accurate invariant
comments are what prevented-classes of fixes (G-15-3's wrong-guard family) stand on.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-35-01 | Repudiation (of design intent) | Wrong written premises steering a future fix into guarding the wrong invariant | low | mitigate | The three corrections themselves, each grep-gated so the false claims cannot survive |
</threat_model>

<verification>
- One full FeatureTests run (single invocation) as the build/behavior gate for a comment-only
  diff. Never overlap xcodebuild invocations.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from the G-15-15 closure condition.
</verification>

<success_criteria>
- G-15-15 closed: WR-01, WR-02, and WR-04 corrected decide-one with the false phrases gone,
  the canonical wording present at all three suspension-claim sites, the five-writer inventory
  grep-verified, and a comment-only diff proven by inspection with a green full run.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-35-SUMMARY.md` when done, including
the three rewritten paragraphs quoted, the writer grep output, and the comment-only diff
statement.
</output>
