---
phase: 15-continued-background-downloads
plan: 42
type: execute
wave: 42
depends_on: ["15-41"] # Round continuity from the round-13 HEAD the gap was derived at; xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-22]
requirements: [SC1, SC3]

must_haves:
  truths:
    - "The invariant is stated and enforced over the SNAPSHOT, not per iteration: an expiration pause may settle over a gallery only when the user's queue intent for it is exactly what it was when the expiration CHOSE that gallery. `pauseAllSchedulable` captures every gallery's queue-intent generation in the SAME synchronous stretch that captures the gid list, so a mobilizing tap landing anywhere after the sweep began advances a generation the loop has already RECORDED, the affected pause goes `.superseded`, and that arm's re-converge-and-re-ensure rescues the tap (D-07's tap keeps its session, D-11's pause-all still covers stale work; SC3's a-tap-produces-something clause, SC1 through the same door)."
    - "The regression is multi-gallery and lands the tap BEFORE the target gallery's iteration, not inside its pause: two schedulable galleries, session live, expire, hold the FIRST gallery's pause at the production `await taskToCancel?.value` suspension, mobilize the SECOND through the production `resume(gid:)` — the one mobilizer whose trailing ensure production itself issues one frame up, so the window is real rather than manufactured — release the hold, and assert the second gallery is still queued with its intent intact AND that a session was started for it BEFORE the deferred ensure is issued, which proves the loop reached the iteration and went `.superseded` rather than returning early at its session guard."
    - "Closure is derivable, not asserted: the mobilizer census is re-derived at execution time by grepping every caller of `advanceQueueIntentGeneration(for:)` in AppPackage/Sources/DownloadClient, recording each caller's ensure location and confirming none stamps `continuedSessionID` before it advances — the falsification condition the verifier stated. The census must agree with the verification's four-caller enumeration (`enqueue`, `resume`, `performRetry`, `performRetryPages`) or the discrepancy stops the plan before the fix is declared scoped."
    - { statement: "On a physical iOS 26 device, a download tap landing while an expiration sweep is pausing another gallery still starts its session and leaves the tapped download queued and running", verification: backstop }
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      provides: "pauseAllSchedulable capturing gid + generation pairs in the snapshot stretch and consuming only recorded expectations in its loop, with the doc stating the snapshot rule and the same suspension-free justification the two sibling guards record"
      contains: "pauseAllSchedulable"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift"
      provides: "The multi-gallery mid-sweep mobilization regression, tap landed before its gallery's iteration, session start discriminated from the deferred ensure"
      contains: "testAMobilizationLandingBeforeItsOwnIterationSurvivesTheExpirationSweep"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift"
      via: "ownsExpirationPause compares the CURRENT generation against the snapshot-time expectation carried in ExpirationPauseOwnership, so any post-snapshot advance forces .superseded and its rescue arm"
      pattern: "ExpirationPauseOwnership"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      via: "spy.expire() drives the production .expired arm into markContinuedSessionEnded then pauseAllSchedulable, so the sweep under test is production-issued, never hand-built"
      pattern: "expire()"
  prohibitions:
    - statement: "Must NOT widen ownsExpirationPause or add a generation guard to the user-pause side: the asymmetry is deliberate and testAUserPauseIsNeverAbandonedByAnInterleavingRetry pins it — that case and testAResumeInsideAStaleExpirationPauseSurvivesAndMobilizesTheQueue pass byte-unchanged."
      status: pending
      verification: automated
    - statement: "Must NOT describe schedulableDownloads() as an authority the SCHEDULER reads in any comment this plan writes or edits — that claim is false at this HEAD (G-15-24's standing sentence, corrected by plan 15-44); the hoist justification names only the two facts it needs: queueStore.gids is a synchronous property read and indexedDownloads(gids:) awaits nothing."
      status: pending
      verification: automated
    - statement: "Must NOT hand-issue any push or session start in the regression: the sweep is entered through spy.expire(), the mobilization through the production resume(gid:), and the deferred ensure names the production event it stands for (togglePause's trailing ensureContinuedSession) — no test-issued pushes between staging and assertions."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression, and must NOT write an unlabeled multi-element tuple type (labeled_tuple_elements is error-severity — use a labeled tuple or a small named struct for the captured pair)."
      status: pending
      verification: automated
---

<objective>
Close blocker G-15-22 (SC3, and SC1 through the same door). Verified in source at the round-13
HEAD: `pauseAllSchedulable` (`DownloadClient+ContinuedSession.swift:356-366`) snapshots the gid
list once but reads each gallery's queue-intent generation at the top of that gallery's OWN
iteration. The two guards that exist were traced and the uncovered window is exactly
`[advanceQueueIntentGeneration, ensureContinuedSession)` of a mobilizing entry point intersected
with the start of that gallery's loop iteration: the loop's session guard passes (the `.expired`
arm called `markContinuedSessionEnded` first, so `continuedSessionID` is nil), the per-iteration
read returns the ALREADY-ADVANCED generation as the expectation, `ownsExpirationPause` compares
the advanced value against itself, `commitPause`'s status gate admits the freshly enqueued
gallery, and the stale pause settles over the user's action. The design's own compensation is
bypassed: the pause never goes `.superseded`, so the rescue re-ensure never runs, and the tap's
own ensure finds the gallery blocked by the very pause undoing it — `hasPendingWork()` answers
false and no session starts. Net user-visible result: the download the user just started is not
running and no card appears, with no fallback tier (D-03) to recover it.

The verifier recorded a judgement this plan honors rather than relitigates: the review's CR-01
remedy — hoisting the generation reads into the same synchronous stretch as the gid snapshot —
is CORRECT AS WRITTEN, because `schedulableDownloads()` performs no suspending call today
(`queueStore.gids` is a synchronous property read; `indexedDownloads(gids:)` awaits nothing),
the identical justification `ensureContinuedSession` (`:177-182`) and
`pushContinuedSessionProgress` (`:569-573`) already record for their own guards.

Purpose: a qualifying tap always produces either running covered work or a superseded pause whose
rescue arm starts the session — never silence (SC1/SC3, D-07, D-11).
Output: the hoisted capture on the production sweep, the corrected pause doc, the execution-time
mobilizer census, the multi-gallery mid-sweep regression RED-first, and a green full run.
</objective>

## Artifacts this plan produces

- **Changed capture shape in `pauseAllSchedulable`:** the gid list and every gallery's
  `ExpirationPauseOwnership` are built together in the snapshot stretch; the loop consumes only
  recorded pairs. If the executor chooses a named type over a fully-labeled tuple, it is a small
  file-private struct in `DownloadClient+ContinuedSession.swift` (e.g. `ExpirationPauseTarget`
  with `gid` and `expiration` members) — no new public API.
- **New test case:** `testAMobilizationLandingBeforeItsOwnIterationSurvivesTheExpirationSweep` in
  `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift`.
- **Possible test-support addition:** a per-gallery parking task-runner control for two-gallery
  stagings (the `BlockingRunnerControl` idiom keyed by gid), added to
  `DownloadFeatureTestSupportTypes.swift` or `DownloadFeatureTestHelpers.swift` only if the local
  closure shape proves unwieldy — never duplicated.
- **Doc corrections:** `pauseAllSchedulable`'s doc paragraph tying each pause's generation to its
  own loop iteration is replaced by the snapshot rule plus the suspension-free justification.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage the mid-sweep mobilization regression, RED-first</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-22 entry WHOLE (detail, why_tests_missed_it, suggested_fix): the window definition, the two covered cases the new staging must NOT restage, and the required assertions
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift` — all three existing cases: `testAResumeInsideAStaleExpirationPauseSurvivesAndMobilizesTheQueue` (`:22-66`: the spy.expire() entry, sessionTask capture BEFORE expire, `control.cancellationObserved()` hold discipline, `waitForTaskValue` settlement) and `testWorkMobilizedInsideTheTerminalPushSurvivesTheDrain` (`:114-178`: the two-gallery `makeQueuedCoordinator` staging with an inert runner)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — `makeQueuedCoordinator` (`:359`—: galleries/queuedGIDs/client/taskRunner parameters), `makeBlockingCoordinator` and `BlockingRunnerControl` (`:651-713`: park/started/recordCancellation/release/cancellationObserved — the idiom the per-gallery runner reuses), `waitForTaskValue`, `waitUntil`
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `handleContinuedSessionEvent`'s `.expired` arm (`:305-308`) and `pauseAllSchedulable` (`:343-366`, the defect under test)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `pause(gid:expiration:)`'s `.superseded` arm (`:169-212`: the rescue re-ensure the fix must reach), `commitPause` (`:215-269`: where the held pause parks — `await taskToCancel?.value`), `resume(gid:)` (`:355-369`: advances the generation, enqueues, converges — and does NOT ensure)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift` — `togglePause` (the `:194` region): confirm the resume route's `ensureContinuedSession()` is issued one frame up, AFTER `resume(gid:)` returns — this is the production event the test's deferred ensure stands for
    - `.swiftlint.yml` — the lint gates all new test code must satisfy from the start
  </read_first>
  <behavior>
    - `testAMobilizationLandingBeforeItsOwnIterationSurvivesTheExpirationSweep` — expected to FAIL
      against current source. Stage TWO galleries via `makeQueuedCoordinator`: gallery A (the held
      one) queued and incomplete; gallery B (the mobilized one) incomplete, NOT in `queuedGIDs`,
      with an inactive record so `resume` resolves a mode for it. The task runner parks every
      scheduled run per gid: A's control records its cancellation and HOLDS (the
      `releasesOnCancellation: false` idiom), B's control releases on cancellation so the pre-fix
      sweep can complete. Choreography:
      1. Ensure the session (`testingEnsureContinuedSession`), capture the session task
         (`testingContinuedSessionTask`), assert `spy.startCount == 1`.
      2. `resume(gid: A)` and await A's control `started()` — A is running.
      3. `spy.expire()` — the production `.expired` arm runs `markContinuedSessionEnded` then
         `pauseAllSchedulable`; the sweep's first iteration parks at A's cancellation hold
         (`cancellationObserved()`).
      4. Mobilize B inside the hold: `try await manager.resume(gid: B).get()` runs to completion —
         generation advanced AFTER the sweep's snapshot, B enqueued and scheduled (B's run parks).
         Its trailing ensure is deliberately NOT yet issued: production issues it one frame up in
         `togglePause`, after the resume await returns, which is exactly the window the gap names.
      5. Release A's hold; settle the sweep with `waitForTaskValue` on the captured session task.
      6. BEFORE issuing the deferred ensure, assert the discriminator: `spy.startCount == 2` —
         only the `.superseded` arm's re-ensure can have minted it at this point, which proves the
         loop reached B's iteration and went superseded rather than returning early.
      7. Issue the deferred ensure (`testingEnsureContinuedSession`), commented as standing for
         `togglePause`'s trailing `ensureContinuedSession()`. Assert it was inert:
         `spy.startCount` still 2.
      8. Final state: `queueStore.contains(B)` is true, `queuedModes[B]` equals the mode the
         production `resume` recorded for the staged record (derive it from `resumeMode` against
         the fixture, do not hard-code blindly), `testingHasContinuedSession()` is true,
         `queueStore.contains(A) == false` (A's own pause genuinely settled — the loop ran its
         first iteration to completion), `spy.rejectedProgressUpdates` empty.
    - Expected RED readings against current source: B unqueued (`queueStore.contains(B)` false),
      `queuedModes[B] == nil`, `spy.startCount == 1`, `testingHasContinuedSession()` false — the
      tap produced nothing, exactly the gap's user-visible outcome.
  </behavior>
  <action>
**Step 1 — the per-gallery parking runner.** Build the two-gallery staging on
`makeQueuedCoordinator` with a `runScheduledDownload` closure that parks per gid using the
existing `BlockingRunnerControl` idiom — one control per gallery, A's holding its cancellation
until the test releases it, B's releasing on cancellation. Prefer two local controls captured by
the closure; promote a keyed variant into the shared support files only if the local shape cannot
express the hold/release split, and never duplicate a nontrivial helper.

**Step 2 — the case per the behavior block.** Choreography discipline is the standing mandate:
the sweep is entered only through `spy.expire()` (the production `.expired` arm), the
mobilization only through the production `resume(gid:)`, every push the spy records is
production-issued, and the deferred ensure carries a comment naming the production event it
stands for — `togglePause`'s trailing `ensureContinuedSession()`, deferred across the resume
await exactly as production defers it. Do not restage either covered case: the tap must land in a
DIFFERENT gallery than the held pause (multi-gallery), and must land before that gallery's
iteration, not inside its own pause.

**Step 3 — the honest RED, single invocation.** Run the interleave suite once, targeted. The new
case MUST fail against current source with the RED readings above; the two existing cases MUST
pass unchanged in the same run. Record the observed readings in the summary as the RED evidence.
If the new case unexpectedly PASSES, stop: the staging did not land the tap inside the
`[advance, ensure)` window ahead of B's iteration (check the hold ordering and that the resume
completed before the release) — do not proceed to Task 2 on a green case; re-derive and record.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests</automated>
  </verify>
  <acceptance_criteria>
    - The case exists: `grep -c 'testAMobilizationLandingBeforeItsOwnIterationSurvivesTheExpirationSweep' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift` outputs `1`.
    - The staging is multi-gallery with the tap ahead of its own iteration: the case stages two galleries, the mobilized gid differs from the held gid, and the resume completes before the hold is released (source inspection, quoted in the summary).
    - The sweep and the mobilization are production-issued: the case body enters the sweep only via `spy.expire()` and mobilizes only via `resume(gid:)`; a grep over the case body finds no direct `pauseAllSchedulable`, no direct push call, and exactly two `testingEnsureContinuedSession` calls — the staging ensure and the deferred ensure with its naming comment.
    - The case is observed RED at this task's end with the four RED readings quoted in the summary; both pre-existing interleave cases are observed green byte-unchanged in the same run.
    - `git diff --stat` for this task touches no file under `AppPackage/Sources`.
  </acceptance_criteria>
  <done>The uncovered window has a deterministic multi-gallery regression that fails honestly against the per-iteration read, with the session-start discriminator separating the superseded rescue from the tap's own deferred ensure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hoist the generation capture into the snapshot stretch and re-derive the mobilizer census</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — G-15-22's `suggested_fix` (the hoist, its justification, and the two things it must not touch) and the round-13 `verification_scope_note`'s CR-01 judgement (recorded so it is not relitigated)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `pauseAllSchedulable` and its doc (`:343-366`), `ensureContinuedSession`'s guard justification (`:177-182`) and `pushContinuedSessionProgress`'s interleaving dispositions (`:569-573`) — the two sibling guards whose exact justification wording the hoisted capture must stay consistent with
    - `AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` — `schedulableDownloads()` (`:42-52`): confirm for yourself that `queueStore.gids` is a synchronous property read and `indexedDownloads(gids:)` awaits nothing internally — the justification is re-derived, not copied
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `ExpirationPauseOwnership` (`:10`), `ownsExpirationPause` (`:271-278`) — read to confirm they need no change; this plan must not edit this file
    - `AppPackage/Sources/DownloadClient/DownloadClient+RetryHelpers.swift`, `DownloadClient+PublicAPI.swift` — the four `advanceQueueIntentGeneration(for:)` callers for the census
    - `.swiftlint.yml` — before any Swift edit; note `labeled_tuple_elements` (error) polices tuple TYPES with unlabeled elements
  </read_first>
  <behavior>
    - The Task 1 regression goes GREEN with no change to its assertions; both pre-existing
      interleave cases stay green byte-unchanged in the same run.
    - The full FeatureTests plan is green in one invocation with every pre-existing case
      unchanged.
  </behavior>
  <action>
**Step 1 — the hoist.** Rework `pauseAllSchedulable(expiring:)` so the single
`await schedulableDownloads()` is followed, in the same synchronous stretch, by the construction
of every gallery's `ExpirationPauseOwnership` — a list of (gid, expiration) pairs captured
together, as a fully-labeled tuple or a small file-private named struct (`labeled_tuple_elements`
is error-severity; an unlabeled positional tuple type is banned). The loop then consumes only
recorded pairs: the per-iteration `queueIntentGeneration(for:)` read and per-iteration
`ExpirationPauseOwnership` construction are gone. The loop-level session guard
(`continuedSessionID == nil || continuedSessionID == sessionID`) stays exactly where it is.
`ownsExpirationPause`, `commitPause`, and the user-pause side are untouched — the fix narrows
WHEN the expectation is read, never WHAT the comparison accepts.

**Step 2 — the doc.** Replace the doc paragraph tying each pause's generation to the moment its
own loop iteration chose it with the snapshot rule: every gallery's pause is bound to the
expiring session and to the queue-intent generation current when the SWEEP chose its targets, so
a D-07 tap landing anywhere after that capture advances a generation the sweep has already
recorded and the stale pause abandons its write through the `.superseded` rescue. Record the
hoist's justification exactly as the two sibling guards record theirs: the capture stretch is
synchronous because `schedulableDownloads()` performs no suspending call today — `queueStore.gids`
is a synchronous property read and `indexedDownloads(gids:)` awaits nothing — and an `await`
introduced there reopens the window and needs its own re-validation. Do NOT describe
`schedulableDownloads()` as an authority the scheduler reads (that standing false sentence is
G-15-24's, corrected by plan 15-44) — the justification names only the two facts above.

**Step 3 — the census, re-derived not imported.** Grep `AppPackage/Sources/DownloadClient` for
every caller of `advanceQueueIntentGeneration(for:)`. For each, record in the summary: file:line,
the suspensions between the advance and that caller's `ensureContinuedSession()` (or the frame
above that issues it), and whether any caller stamps `continuedSessionID` before advancing — the
falsification condition. Expected: the census matches the verification's enumeration (`enqueue`,
`resume` with its ensure one frame up in `togglePause`, `performRetry`, `performRetryPages`) and
no caller stamps first. A discrepancy — a fifth mobilizer, or one that stamps before advancing —
STOPS the plan before closure is declared; record it either way.

**Step 4 — green.** Run the interleave suite targeted (Task 1 case green, both existing cases
green), then ONE full FeatureTests run. Never overlap invocations.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The capture is hoisted: `grep -n 'queueIntentGeneration(for' AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` matches only inside the snapshot stretch of `pauseAllSchedulable` (line numbers quoted in the summary), and the `for` loop's body constructs no `ExpirationPauseOwnership` (source inspection, quoted).
    - The stale phrasing is gone: `grep -c 'when this loop chose it' AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` outputs `0`, and the rewritten doc records the suspension-free justification and the await-reopens-the-window note (quoted in the summary).
    - Nothing else moved: `git diff` for this task shows no change to `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` and no new `schedulableDownloads()` call site anywhere.
    - The mobilizer census table is in the summary with all callers dispositioned against the stamp-before-advance falsification condition, reconciled against the verification's four-caller enumeration, or the plan stopped on the discrepancy.
    - The Task 1 case's green flip is recorded against its RED readings; `testAUserPauseIsNeverAbandonedByAnInterleavingRetry` and `testAResumeInsideAStaleExpirationPauseSurvivesAndMobilizesTheQueue` pass byte-unchanged.
    - The targeted run and one full FeatureTests run both exit `0`, each a single invocation.
  </acceptance_criteria>
  <done>A mobilizing tap landing anywhere after the sweep's snapshot forces the affected pause to .superseded and its rescue re-ensure, proven by the multi-gallery regression flipping green while both covered-case pins never moved, with the mobilizer census re-derived and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System expiration → user queue intent | A system-initiated bulk pause can settle over a user action that arrived after the expiration was decided, silently discarding the user's intent with no error surface |
| Reentrant actor suspensions → ownership comparisons | Every suspension inside the sweep and the pause is a window in which the expectation a guard compares against can go stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-42-01 | Denial of Service | pauseAllSchedulable settling a stale pause over a just-mobilized gallery, leaving it unqueued with no session and no fallback tier (D-03) | high | mitigate | Snapshot-stretch generation capture; any post-snapshot advance forces .superseded whose arm re-converges and re-ensures; multi-gallery regression pins it |
| T-15-42-02 | Tampering | Widening ownsExpirationPause to the user-pause side, making an explicit pause revocable by a background retry | medium | mitigate | Prohibited by plan; the pinning asymmetry case must pass byte-unchanged and +Scheduling.swift shows no diff |
| T-15-42-03 | Repudiation | A hoist justification written without re-derivation becoming the next false written premise | medium | mitigate | The suspension-free claim is re-derived from schedulableDownloads()'s body and recorded with the await-reopens note, consistent with the two sibling guards |
</threat_model>

<verification>
- Task 1 targeted run (new case RED, existing cases green); Task 2 targeted run then one full
  FeatureTests run. xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — the phase has no requirement IDs to
  probe; must_haves derive from G-15-22's truth_violated clause (SC3's consistent-state and
  a-tap-produces-something halves, SC1 through the same door) and the phase success criteria.
- <assumption_delta_decision>no-change — this plan moves WHEN an existing expectation is read;
  no identity model (gid/generation/session id) changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan: the backstop
  truth records the device-only observation without closing it.
</verification>

<success_criteria>
- G-15-22 closed at the invariant: the generation expectations are captured with the snapshot,
  the multi-gallery regression flips RED→green with the superseded-rescue discriminator, both
  covered-case pins never move, the mobilizer census is re-derived and matches (or the
  discrepancy stopped the plan), the pause doc states the snapshot rule with a re-derived
  justification, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-42-SUMMARY.md` when done, including
the RED readings and green flip, the mobilizer census table with the stamp-before-advance
dispositions, the capture shape landed (labeled tuple or named struct) and why, and the quoted
corrected doc.
</output>
