---
phase: 15-continued-background-downloads
plan: 39
type: execute
wave: 39
depends_on: ["15-38"] # Round continuity from the round-12 HEAD the gap was derived at; xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadStoreRepairTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-19]
requirements: [SC3]

must_haves:
  truths:
    - "The invariant is stated and enforced over the SIGNAL across its whole route, not inside one function: a page the SOURCE probe could not answer for never becomes a positive absence at the DESTINATION. `materializeRepairSeed` derives its selection from the full `PageFileScan` instead of the collapsed `existingPageRelativePaths`, hands the source scan's `unprobedPages` (plus any selected page it skipped on a non-answer) back to its caller, `setupWorkingFolder` returns that set, and `prepareWorkingSeed` unions it into the destination `PageFileScan` before `reconcileWorkingManifestAgainstPageFiles` consumes it — so the EXISTING per-file refusal line (D-G13-01) covers laundered pages with no new refusal mechanism, and up to N-1 recorded content hashes can no longer be destroyed on a probe non-answer (SC3's no-lost-or-duplicated-work clause)."
    - "The regression crosses the two stagings existing tests each cover alone: per-file probe failure (`PartialProbeFailureFileManager` throwing the metadata read + real `0o000` modes so the content-read fallback throws for real, path fragments pinned against the production `makePageRelativePath`) AND the materialization branch (a `.repair` payload whose computed folder path differs from the fixture folder — title change — so `setupWorkingFolder` takes the seed route). It asserts no hash blanked at the destination, no `updateDownloadIndex` republish, and no floor withdrawal, and it lives in a NEW test file — `DownloadContinuedSessionBasisTests.swift` is at 996 lines against the 1000-line `file_length` error gate and gains nothing."
    - "Genuine absence stays fully blankable across the copy: the companion stages truly deleted source page files through the SAME materialization branch and asserts exactly those pages blanked at the destination, the record republished at the honest lowered count, and the pushed pair moved by exactly the counted movement (D-G7-01's withdrawal accounting) — so the fix cannot be satisfied by disabling destination blanking after a seed."
    - "Closure is derivable, not asserted: the consumer sweep is re-derived at execution time by grepping every consumer of `pageFileScan`, `existingPageRelativePaths`, and `sanitizeAssetFileIfNeeded` in AppPackage/Sources, recording each call site's disposition against the question 'can its output become the INPUT of something that blanks, in a different folder?' — the census must agree with the verification's 11-site enumeration (exactly one destructive consumer, exactly one laundering route) or the discrepancy stops the plan before the fix is declared scoped."
    - { statement: "On a physical iOS 26 device, a `.repair` gallery re-slotted to a new folder (title change) whose source pages are transiently unreadable climbs its card without stepping backwards by a laundered blank count", verification: backstop }
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      provides: "materializeRepairSeed selecting through the full PageFileScan and returning the source-side unprobed page set"
      contains: "pageFileScan"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "setupWorkingFolder handing the carried set back; prepareWorkingSeed unioning it into the destination scan ahead of the reconciliation; the false seed-route safety sentence at the reconciliation doc corrected"
      contains: "unprobedPages"
    - path: "AppPackage/Sources/DownloadClient/DownloadStore.swift"
      provides: "PageFileScan and existingPageRelativePaths docs carrying the route rule: a caller whose output feeds a destructive decision — even one step later, in a different folder — must carry the classification, not the collapse"
      contains: "PageFileScan"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift"
      provides: "The crossed regression and its genuine-absence companion, both driving the production seed route"
      contains: "testAnUnprobeableSourcePageIsNeverBlankedAcrossTheSeedCopy"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadStoreRepairTests.swift"
      provides: "Existing materializeRepairSeed call sites consuming the new return value and asserting it empty for fully-probeable stagings"
      contains: "materializeRepairSeed"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      via: "the returned unprobed set flows from the seed copy through setupWorkingFolder into the destination PageFileScan the reconciliation consumes, so the existing line-2 refusal covers laundered pages"
      pattern: "unprobedPages"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      via: "testingPrepareWorkingSeedAnnouncingProgress drives the production preparation under the crossed staging, and the announcement push it asserts is production-issued"
      pattern: "testingPrepareWorkingSeedAnnouncingProgress"
  prohibitions:
    - statement: "Must NOT take the review's remedy — throwing from materializeRepairSeed or treating an unprobeable source page as 'no seed'. The verifier traced it: the refusal falls through to createDirectory, ensureWorkingManifest writes a fresh all-empty manifest and re-indexes at 0-of-N, converting a K-page hash loss into an N-page loss plus a full D-G7-01 withdrawal — strictly worse than the defect."
      status: pending
      verification: automated
    - statement: "Must NOT hand-issue any push in the new cases: the only pushes recorded by the spy are the production announcement push of prepareWorkingSeedAnnouncingProgress and the session-ensure start — no test-issued pushes between staging and assertions."
      status: pending
      verification: automated
    - statement: "Must NOT weaken, delete, or restage an existing case to make the new ones pass — the mass-partial, genuine-partial, wholesale-failure, and repair-seed suites all pass byte-unchanged."
      status: pending
      verification: automated
    - statement: "Must NOT add anything to DownloadContinuedSessionBasisTests.swift (996 of 1000 lines), and must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<objective>
Close blocker G-15-19 (SC3). Verified in source at HEAD 47d23e1c: `materializeRepairSeed`
(`DownloadStore+Operations.swift:38-75`) selects pages through `existingPageRelativePaths` — the
collapsed scan that DISCARDS `unprobedPages` (`DownloadStore.swift:196-198`) — and re-probes each
with `sanitizeAssetFileIfNeeded`'s `Bool` forward, which is false for `.unprobeable` exactly as it
is for `.rejected`. An unprobeable SOURCE page is therefore not copied while the manifest is copied
whole; `ensureWorkingManifest` returns the copied manifest verbatim at the destination; the
destination `pageFileScan` then honestly reports the file absent (`scanSucceeded == true`,
`unprobedPages` empty), and both lines of the round-12 defence pass it through to
`reconcileWorkingManifestAgainstPageFiles`, which blanks the recorded hash, rewrites the manifest,
and re-indexes. A source-side per-file NON-ANSWER is laundered into a destination-side POSITIVE
absence: up to N-1 recorded content hashes destroyed irreversibly and the pages re-downloaded, on a
probe that established nothing. This is the sixth consecutive round lost to a fix scoped to the
branch its regression stages; the sweep and the crossed regression below are what make this one
invariant-scoped.

Purpose: SC3's no-lost-work clause and D-G13-01 ("a non-answer is never authority to destroy a
recorded hash") hold across the seed copy, not just within one folder scan.
Output: the carried-set fix on the production route, corrected route docs, the execution-time
consumer sweep with dispositions, the crossed regression plus genuine-absence companion in a new
test file, and a green full run.
</objective>

## Artifacts this plan produces

- **Changed signature:** `materializeRepairSeed(from:manifest:to:)` returns `Set<Int>` — the
  claimed pages whose source-side classification was a non-answer (the source scan's
  `unprobedPages`, plus any scan-selected page skipped at the pre-copy guard).
- **Changed signature:** `setupWorkingFolder(folderURL:shouldReuse:seedContext:)` returns
  `Set<Int>` (empty on every non-seed path), consumed by `prepareWorkingSeed`.
- **New wiring:** `prepareWorkingSeed` constructs the effective destination `PageFileScan` with
  `unprobedPages` unioned with the carried set before calling
  `reconcileWorkingManifestAgainstPageFiles`.
- **New test file:** `AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift`
  with `testAnUnprobeableSourcePageIsNeverBlankedAcrossTheSeedCopy` and
  `testAGenuinelyAbsentSourcePageIsStillBlankedAcrossTheSeedCopy`.
- **Possible shared helper promotion:** if the new file needs a staging helper currently
  file-private to the basis suite (e.g. a permissions restorer), it is promoted into
  `DownloadFeatureTestHelpers.swift` rather than duplicated — no new public production API.
- **Doc corrections:** the reconciliation doc's seed-route sentence
  (`DownloadClient+ExecutionSupport.swift:402-404`), the `PageFileScan` licensing paragraph and
  `existingPageRelativePaths` caller rule (`DownloadStore.swift:58-73, 191-195`).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage the crossed regression and its genuine-absence companion in a new file, RED-first</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-19 entry WHOLE (detail, why_tests_missed_it, suggested_fix) and the round-12 amendment's "Gate for the next round" paragraph
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift` — `testAMassPartialProbeFailureBlanksNothingWritesNothingAndWithdrawsNothing` (`:748-873`: the double + real-modes staging being crossed, its path-fragment pinning, its permissions `defer` discipline) and `testAGenuinePartialLossBlanksExactlyTheMissingPages` (`:875-948`: the companion shape); note which helpers those cases use are file-private (`private extension` at `:953`) and cannot be imported
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift` — `testRepairSeedReusesCompletedFilesWhenPageCountMatches` (`:10-71`: the title-change staging — a payload whose title differs from the staged folder, destination computed via the production `manager.folderRelativePath(for:parentFolderName:)`)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift` — `PartialProbeFailureFileManager` (`:409`—: which call it throws from, how fragments select files)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — `makeQueuedCoordinator`, `makeStartPayload`/`makeRepairPayload` (`:495`: `makeRepairPayload(for:)` keeps the gallery's title — the crossed case builds its payload from a second `SessionGallery` with the SAME gid and a DIFFERENT title), `lastPushedPair`, `writePageFiles`
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeed` (`:296-341`), `reconcileWorkingManifestAgainstPageFiles` (`:437-511`), `setupWorkingFolder` (`:562-592`), `repairSeed` (`:662-681`), `shouldReuseWorkingFolder` (`:533-555` — why a title change routes into the materialization branch)
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `materializeRepairSeed` (`:38-75`, the defect under test)
    - `.swiftlint.yml` — the lint gates all new test code must satisfy from the start
  </read_first>
  <behavior>
    - `testAnUnprobeableSourcePageIsNeverBlankedAcrossTheSeedCopy` — the crossed case, expected to
      FAIL against current source. Stage exactly as the mass-partial case: one gallery (6 pages, 4
      completed), `PartialProbeFailureFileManager` armed with fragments for pages 2-4 pinned
      against `makePageRelativePath`, page files 1-4 written in the fixture folder, index
      reloaded, session ensured (subtitle asserted "4 / 6 pages · 1 gallery"), then real `0o000`
      modes on pages 2-4 with the permissions-restoring `defer` discipline. THEN the cross: build
      the `.repair` payload from a second `SessionGallery` with the same gid and a different title,
      compute the destination through the production `folderRelativePath(for:parentFolderName:)`,
      confirm it differs from the fixture folder and does not exist, and drive
      `testingPrepareWorkingSeedAnnouncingProgress(payload:existingDownload:folderURL:)` with the
      NEW destination. Assert: the record still reads `completedPageCount == 4` (no republish); the
      manifest read from the DESTINATION folder carries non-empty hashes for exactly pages 1-4
      (nothing blanked — compare against the staged source manifest captured before the modes were
      dropped); `seed.existingPages.keys.sorted() == [1]` (only the probeable page was copied);
      exactly one progress push whose pair is 4/6 with subtitle "4 / 6 pages · 1 gallery" (no
      withdrawal — the bracket measured a zero delta); `rejectedProgressUpdates` empty.
    - `testAGenuinelyAbsentSourcePageIsStillBlankedAcrossTheSeedCopy` — the companion, expected to
      PASS both before and after the fix. Same shape over the REAL file manager: pages 3-4 deleted
      outright from the source folder, same title-changed `.repair` payload and computed
      destination. Assert: the record republishes at `completedPageCount == 2`;
      `seed.existingPages.keys.sorted() == [1, 2]`; the destination manifest keeps non-empty
      hashes for exactly pages 1-2; exactly one push whose pair is 2/6 with subtitle
      "2 / 6 pages · 1 gallery" (the counted movement withdrawn, honestly).
  </behavior>
  <action>
**Step 1 — the file and its header.** Create
`DownloadRepairSeedSignalPropagationTests.swift` as a new suite (a struct conforming to
`DownloadFeatureTestCase`, like `DownloadCoordinatorRepairSeedTests`). Its header doc states the
invariant it pins — a page the source probe could not answer for must never become a positive
absence at the destination — and why it is a new file: the basis suite sits at 996 lines against
the 1000-line `file_length` error gate. Helpers the basis cases use are file-private there; either
write the minimal local staging this suite needs or promote a genuinely shared helper (the
permissions restorer is the likely candidate) into `DownloadFeatureTestHelpers.swift` — never
duplicate a nontrivial helper in both files, and never widen the ledger suite's private
index-patch seam.

**Step 2 — the two cases per the behavior block.** Choreography discipline is the standing
mandate: record state comes only from fixture manifests, `writePageFiles`, and the production
announcing preparation; the pause/push surface is untouched; the ONLY pushes the spy records are
the session-ensure start and the preparation's own announcement. Capture the staged source
manifest (via `fixture.storage.readManifest`) BEFORE dropping the modes, so the nothing-blanked
assertion compares hash-for-hash against ground truth rather than against a re-read the modes
could distort.

**Step 3 — the honest RED, single invocation.** Run the new suite once. The crossed case MUST
fail against current source — with the destination manifest showing blanked hashes for pages 2-4
and the record republished at 1 — and the companion MUST pass. Record the observed failure
readings in the summary as the RED evidence. If the crossed case unexpectedly PASSES, stop:
either the staging did not take the materialization branch (check the computed destination
differs and did not exist) or the defect is not what the verification traced — do not proceed to
Task 2 on a green crossed case; re-derive and record.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests</automated>
  </verify>
  <acceptance_criteria>
    - Both cases exist: `grep -c 'testAnUnprobeableSourcePageIsNeverBlankedAcrossTheSeedCopy' AppPackage/Tests/DownloadsFeatureTests/DownloadRepairSeedSignalPropagationTests.swift` outputs `1`, and the same for `testAGenuinelyAbsentSourcePageIsStillBlankedAcrossTheSeedCopy`.
    - The crossed case takes the materialization branch by construction: its body computes the destination through `folderRelativePath(for:parentFolderName:)` with a payload title differing from the staged folder's, and asserts the computed path differs from the fixture folder before driving the preparation (source inspection, quoted in the summary).
    - The crossed case is observed RED at this task's end: the targeted run shows it failing with the laundered blanking (readings quoted in the summary); the companion is observed green in the same run.
    - Every push asserted is production-issued: the case bodies contain no direct push call — the spy's recorded updates come only from `testingEnsureContinuedSession` and `testingPrepareWorkingSeedAnnouncingProgress` (grep over the new file for push-issuing seams finds none besides those two).
    - `DownloadContinuedSessionBasisTests.swift` is untouched: `git diff --stat` for this task shows no change to it, and `wc -l` still reports 996.
  </acceptance_criteria>
  <done>The crossed regression exists in a new file, fails honestly against the laundering route with the companion green beside it, and both cases drive the production seed route with production-issued pushes only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry the source scan's non-answers across the copy, correct the route docs, and re-derive the consumer sweep</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadStore.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadStoreRepairTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — G-15-19's `suggested_fix` (the verifier's remedy and its prohibition of the review's) and the amendment's sweep paragraph (the 11-site census and the question each site answers)
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `materializeRepairSeed` (`:38-75`) and `linkOrCopyReadableAsset` (`:10-36`: its own internal probe-throw, which stays); the seven `sanitizeAssetFileIfNeeded` call sites in this file (`:11, 58, 72, 242, 249, 258, 301`) for the sweep
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeed` (`:296-341`: where the union lands, inside the D-G7-01 bracket), `setupWorkingFolder` (`:562-592`), the reconciliation doc (`:383-474`) and its seed-route sentence (`:402-404`)
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `PageFileScan` and its licensing doc (`:54-84`), `existingPageRelativePaths` (`:191-198`), `pageFileScan` (`:200-244`), `sanitizeAssetFileIfNeeded` / `probeAssetFile` (`:677-722`)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadStoreRepairTests.swift` — the two direct `materializeRepairSeed` call sites (`:22, :44`) that must consume the new return value
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <behavior>
    - The Task 1 crossed case goes GREEN with no change to its assertions; the companion stays
      green in the same run.
    - `DownloadStoreRepairTests` call sites assert the returned set is empty for their
      fully-probeable stagings.
    - The full FeatureTests plan is green in one invocation with every pre-existing case
      unchanged.
  </behavior>
  <action>
**Step 1 — the carried set at the copy.** Rework `materializeRepairSeed(from:manifest:to:)` to
select pages through the full `pageFileScan(folderURL:manifest:)` on the SOURCE folder and to
return `Set<Int>`: the scan's `unprobedPages`, unioned with any scan-selected page the per-page
pre-copy guard skips. The scan has already classified every selected file `.usable`, so the
pre-copy `sanitizeAssetFileIfNeeded` guard at the old `:72` can only disagree when the file
changed between scan and copy — a disagreement establishes nothing positive at the destination,
so a skipped selected page joins the returned set rather than silently vanishing (this is the
smaller of the two remedies; the alternative — dropping the redundant re-probe and letting
`linkOrCopyReadableAsset`'s internal guard throw — converts an edge skip into a whole-preparation
failure and is NOT taken unless the suite proves the guard unreachable; record whichever is
landed and why). The cover copy keeps its `Bool` guard: the cover has no per-page recorded hash,
so it is outside the signal's blast radius — say so in the function doc. A throw from the
manifest or page copy still propagates (a failed preparation is a recoverable failed download,
not destroyed state).

**Step 2 — the route.** `setupWorkingFolder` returns the set from its materialization arm and an
empty set from every other path; `prepareWorkingSeed` captures it and, after taking the
destination scan, hands `reconcileWorkingManifestAgainstPageFiles` a `PageFileScan` whose
`unprobedPages` is the destination set unioned with the carried set (construct via the public
memberwise initializer; `pages` and `scanSucceeded` pass through untouched, and `existingPages`
for the `WorkingSeed` stays the destination scan's `pages` — an uncopied page is re-fetched, never
reused). The union happens INSIDE the existing D-G7-01 bracket, before the reconciliation — no
new suspension, no new refusal mechanism: the existing line-2 refusal is what covers the
laundered pages.

**Step 3 — the docs the defect hid behind.** Correct the reconciliation doc's seed-route sentence
at `:402-404` — it currently classifies the materialization as safe on exactly the collapse this
gap exploited; it must now state that the materialization carries the source scan's non-answers
across the copy so the destination reconciliation treats them as unprobed rather than absent.
Update the line-2 description in the same doc block to name BOTH populations it now covers (the
destination listing's own unprobeables, and the carried source-side set). In `DownloadStore.swift`,
tighten the `PageFileScan` licensing paragraph and the `existingPageRelativePaths` caller rule
with the route qualifier: a caller may collapse the pairs only if its output can never become the
input of a destructive decision — in this folder or any other. Do NOT touch the all-or-nothing
sentence at `:493` (that is G-15-20's WR-04, next plan) and do NOT rewrite `probeAssetFile`'s
guard comment (WR-03, same plan) — this plan corrects only the sentences its own change
invalidates.

**Step 4 — the sweep, re-derived not imported.** Grep `AppPackage/Sources` for every call of
`existingPageRelativePaths(`, `pageFileScan(`, and `sanitizeAssetFileIfNeeded(`. For each site,
record in the summary: file:line, what consumes its answer, and the disposition — can its output
become the input of something that blanks recorded state, in a different folder? Expected: the
census matches the verification's enumeration (11 call sites; exactly one destructive consumer,
`reconcileWorkingManifestAgainstPageFiles`; exactly one laundering route, `materializeRepairSeed`,
now closed; `addingCurrentFileHashes` throws on a non-answer — recoverable; `validatePage` reports
`.missingFiles` — a re-fetch; `.redownload`/`.update` arrive at a fresh all-empty manifest — no-op
reconciliation). If the census disagrees with the verification's — a new consumer, a second
laundering route — STOP and widen the fix before declaring closure; record the discrepancy either
way.

**Step 5 — call sites and green.** Update the two `DownloadStoreRepairTests` call sites to bind
and assert the returned set (empty for their stagings — full probeability is now a stated
property of those fixtures, not an accident). Run the new suite targeted (crossed case green),
then ONE full FeatureTests run, sequentially, never overlapping invocations.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - `materializeRepairSeed` selects through the full scan and returns the set: `grep -c 'pageFileScan' AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` outputs at least `1`, and `grep -n 'existingPageRelativePaths' AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` matches only inside `addingCurrentFileHashes` (line numbers quoted in the summary — region-scoped: that caller's collapsed read is legitimately non-destructive and stays).
    - The function does not refuse the seed: `materializeRepairSeed` contains no new throw for the unprobeable population (source inspection — the only throws are the pre-existing copy/manifest propagation), and `setupWorkingFolder`'s fallthrough to `createDirectory` is unreachable from a successful materialization (quoted in the summary with the traced reason the review's remedy was rejected).
    - The union reaches the consumer: `prepareWorkingSeed` hands the reconciliation a scan whose `unprobedPages` includes the carried set (source inspection, quoted), and `reconcileWorkingManifestAgainstPageFiles` itself is UNCHANGED apart from its doc block (`git diff` shows no edit to its executable lines).
    - The seed-route safety sentence is gone: `grep -c 'passed sanitization' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `0`.
    - The sweep table is in the summary with all sites dispositioned and the census reconciled against the verification's 11-site enumeration, or the plan stopped on the discrepancy.
    - Both `DownloadStoreRepairTests` call sites consume the return value (grep shows no bare statement-position `materializeRepairSeed` call in that file).
    - The targeted run and one full FeatureTests run both exit `0`, each a single invocation; the crossed case's green flip is recorded against its Task 1 RED readings.
  </acceptance_criteria>
  <done>An unprobeable source page crosses the seed copy as an unprobeable destination page — covered by the existing refusal line, proven by the crossed regression flipping green while the genuine-absence companion never moved, with the consumer census re-derived and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem probe → recorded content hashes | A best-effort per-file probe's answer feeds a decision that irreversibly destroys locally recorded integrity state (the manifest's content hashes) |
| Source folder → destination folder (seed copy) | State derived from one folder's probe becomes the ground truth another folder's destructive scan is judged against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-39-01 | Tampering | materializeRepairSeed → reconcileWorkingManifestAgainstPageFiles | high | mitigate | Carry the source scan's non-answers across the copy and union them into the destination scan so the existing D-G13-01 refusal covers them; crossed regression pins it |
| T-15-39-02 | Tampering | The review's refuse-the-seed remedy (fresh all-empty manifest destroys N hashes instead of K) | high | mitigate | Prohibited by plan; acceptance criterion traces the createDirectory fallthrough unreachable from a successful materialization |
| T-15-39-03 | Denial of Service | Over-protection: a carried set shielding genuinely absent pages, leaving stale claims unreconciled | low | mitigate | The set is per-materialization, derived from one source scan; the genuine-absence companion pins that positive absences stay blankable |
</threat_model>

<verification>
- Task 1 targeted run (crossed case RED, companion green); Task 2 targeted run then one full
  FeatureTests run. xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — the phase has no requirement IDs to
  probe; must_haves derive from G-15-19's truth_violated clause (SC3's no-lost-work half,
  D-G13-01 across the route) and the phase success criteria.
- <assumption_delta_decision>no-change — this plan moves a per-file classification across a copy
  boundary; no identity model (gid/token/session id) changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan: the backstop
  truth records the device-only observation without closing it.
</verification>

<success_criteria>
- G-15-19 closed at the signal across the route: the crossed regression flips RED→green, the
  genuine-absence companion never moves, the consumer census is re-derived and matches (or the
  discrepancy stopped the plan), the route docs state the carried-set rule, the review's
  worse-than-the-defect remedy is provably not taken, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-39-SUMMARY.md` when done, including
the RED readings and green flip, the consumer-sweep disposition table, which pre-copy-guard remedy
landed and why, and the quoted corrected docs.
</output>
