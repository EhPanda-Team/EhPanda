---
phase: 15-continued-background-downloads
plan: 40
type: execute
wave: 40
depends_on: ["15-39"] # Real code dependency: WR-03 and WR-04 sit on the exact seam and function 15-39 reworks, so the corrected statements must describe post-15-39 source; file overlap on DownloadStore.swift and DownloadClient+ExecutionSupport.swift; xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-20]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "No load-bearing doc claim in the five named sites states something source contradicts, and each correction PREFERS a stated rule or a drift-failing check over a corrected inventory (the verifier folded IN-05's actionable half in here): WR-01's convergence-ownership claim becomes a rule over exit CATEGORIES (every `.settled` exit of `commitPause` converges inline after its release; every `.superseded` exit releases and hands convergence to `pause(gid:expiration:)`) with no site-count left to drift; WR-02's three-writer list becomes the invariant it stood for (every queue-mobilizing entry point takes no scheduling block, so any of them can land inside the pause's unbounded wait) with the underlying `blockScheduling` call-site census pinned by a drift-failing test; WR-03's existence-guard rationale stops claiming all callers hand the probe listing-derived URLs; WR-04's all-or-nothing sentence stops describing an equality the round-12 exclusion made structurally unreachable for the mixed population; IN-01's positional count is rephrased count-free. This is the fourth consecutive round in which a wrong written premise was the defect generator — it produced G-15-3, G-15-7, G-15-13, and G-15-19 — and three of these five were written by round 12's own doc corrections, so every corrected sentence must be derived from a fresh source enumeration quoted in the summary, never from the round's own records."
    - "Every inventory that SURVIVES in a doc is paired with a check that fails when it drifts, on the established `DownloadLogPrivacyInvariantTests.expectedHashMaskedCounts` pattern: the `blockScheduling` call-site census and the `lastPushedCompletedPageCount` writer census are asserted by a source-scanning test with a known-member guard against vacuous walks, and the floor doc's sentence asking the reader to re-run a grep is replaced by a pointer to that test."
    - "Every remedy is decide-one with its evidence recorded: WR-03 decides between restating the comment (classification meaningful only for listing-derived URLs) and splitting the probe for constructed-path callers, judged against the seam as 15-39 LANDED it; WR-04 decides between the verifier's comparison change (against the blankable population, with a pinning regression) and the sentence-only correction, judged by tracing which the verifier's own detail supports — a suggested_fix is a hypothesis, and if the suite goes red under the prescribed remedy the alternate lands with the counter-evidence recorded, never a bent test (SC2: these comments are the reasoning surface scheduling-liveness fixes stand on; SC3: WR-03/WR-04 sit on the destroy-recorded-state defence)."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      provides: "WR-01's rule-phrased convergence split on schedulingBlockedGalleryCounts' G-15-8 paragraph; IN-01's count-free session-state phrasing on continuedSessionTask; the floor inventory's re-run-the-grep sentence replaced by a pointer to the census test"
      contains: "schedulingBlockedGalleryCounts"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift"
      provides: "WR-02's invariant-phrased writer statement on writeSettledPauseRecord, covering enqueue(payload:) by rule rather than by enumeration"
      contains: "queue-mobilizing"
    - path: "AppPackage/Sources/DownloadClient/DownloadStore.swift"
      provides: "WR-03's decided remedy on probeAssetFile's existence guard, consistent with the 15-39 seam"
      contains: "probeAssetFile"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "WR-04's decided remedy on the reconciliation's all-or-nothing residual (true sentence, and the comparison change only if the trace supports it)"
      contains: "blankedPageCount"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      provides: "The drift-failing censuses: blockScheduling call sites and lastPushedCompletedPageCount writers, equality-asserted per file with known-member guards"
      contains: "blockScheduling"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift"
      via: "the census test fails when a blockScheduling call site is added or removed, so WR-02's rule (mobilizers take no block) cannot silently rot the way the three-writer list did"
      pattern: "blockScheduling"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      via: "the writer census pins the floor inventory the doc keeps, replacing the comment that asked the reader to re-run a grep"
      pattern: "lastPushedCompletedPageCount"
  prohibitions:
    - statement: "Must NOT change production behavior except WR-04's comparison, and only if the trace supports it AND its pinning regression lands in the same task — every other correction is comment-only, proven by a diff that touches no executable line outside that one decided site."
      status: pending
      verification: automated
    - statement: "Must NOT correct any sentence from the gap record's wording alone: each of the five corrections is derived from a fresh enumeration of the source it describes, quoted in the summary (the gap's own text was written by the same process that produced the last four drifts)."
      status: pending
      verification: automated
    - statement: "Must NOT add anything to DownloadContinuedSessionBasisTests.swift (996 of 1000 lines); WR-04's pinning regression, if taken, goes in DownloadCoordinatorRepairSeedTests.swift."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression, and must NOT weaken or delete an existing case."
      status: pending
      verification: automated
---

<objective>
Close G-15-20 (warning, promoted): five doc claims contradicted by the code they sit on, every one
written by round 12's own doc-correction work — the fourth consecutive round of this exact shape,
which produced G-15-3, G-15-7, G-15-13, and G-15-19. Verified in source at HEAD 47d23e1c:

- **WR-01** — `schedulingBlockedGalleryCounts`'s G-15-8 paragraph (`+Manager.swift:378-386`)
  claims `commitPause` is a site whose convergence its CALLERS own on every path; source: three of
  `commitPause`'s five exits converge inline (vanished-record, wrong-status, settled-success —
  `+Scheduling.swift:225-268`) and only the two `.superseded` exits delegate one frame up, which
  `commitPause`'s own inline comments say, so the file contradicts itself.
- **WR-02** — `writeSettledPauseRecord`'s doc (`+Scheduling.swift:304-321`) enumerates three
  re-cleared writers; `enqueue(payload:)` is a fourth (takes no scheduling block, calls
  `advanceQueueIntentGeneration` then `queueStore.enqueue`, reachable from a user tap while the
  pause is parked on `await taskToCancel?.value`).
- **WR-03** — `probeAssetFile`'s existence-guard comment (`DownloadStore.swift:692-695`) justifies
  `.unprobeable` by claiming callers hand it listing-derived files; seven `sanitizeAssetFileIfNeeded`
  call sites in `DownloadStore+Operations.swift` pass manifest-constructed paths, for which a
  missing file is a POSITIVE absence.
- **WR-04** — the residual guard's sentence (`+ExecutionSupport.swift:493`) still describes an
  all-claimed-pages equality; the round-12 `unprobedPages` exclusion (and now 15-39's carried set)
  makes that equality structurally unreachable for exactly the mixed population it claims to cover.
- **IN-01** — `continuedSessionTask`'s doc (`+Manager.swift:435`) carries a positional count that
  is wrong (four session-state declarations precede it, four follow).

Purpose: kill the drift generator — every corrected claim becomes a rule or gains a check that
fails when it drifts, so the fifth round of this shape has nothing to grow from.
Output: five decided corrections derived from fresh source enumerations, the census test pairing
every surviving inventory, and a green full run.
</objective>

## Artifacts this plan produces

- **New test file:** `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` —
  a source-scanning suite on the `DownloadLogPrivacyInvariantTests` pattern (repository-root
  walker, known-member guard, per-file equality tables) asserting:
  - the `blockScheduling(` call-site census over `AppPackage/Sources/DownloadClient` (expected:
    `commitPause`, `delete`, `deleteFolder`, `moveDownload`, and the DEBUG forwarder — derived
    fresh at execution time, asserted as per-file counts plus a total),
  - the `lastPushedCompletedPageCount` writer census (the five writers the floor doc names,
    asserted the same way).
- **Possible new test case** (only if WR-04's comparison change is taken):
  `testAMixedPopulationLosingEveryBlankablePageIsRefusedWholesale` (name indicative) in
  `DownloadCoordinatorRepairSeedTests.swift`.
- **Doc corrections** at the five named sites; no new production symbols.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-39-SUMMARY.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the five claims decide-one each, from fresh source enumerations</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift, AppPackage/Sources/DownloadClient/DownloadStore.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-20 entry WHOLE (each finding's confirmed mechanism and the per-item suggested_fix)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `commitPause` ENTIRE (`:215-268`: enumerate its exits yourself and classify each as converging inline or delegating up — the correction is derived from this enumeration, not from the gap text), `writeSettledPauseRecord`'s doc (`:304-321`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift` — `enqueue(payload:)` (the fourth writer: confirm it takes no scheduling block and mobilizes the queue)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the G-15-8 paragraph on `schedulingBlockedGalleryCounts` (`:378-386`), `continuedSessionTask`'s doc (`:431-436`), and the floor inventory on `lastPushedCompletedPageCount` (`:437-469` — Task 2 pins it; this task only replaces its re-run-the-grep sentence with a pointer to the census test)
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `probeAssetFile` and its existence guard (`:689-722`) AS 15-39 LEFT THEM, plus every `sanitizeAssetFileIfNeeded` call site in `DownloadStore+Operations.swift` post-15-39 (re-enumerate: 15-39 changed the seam this comment describes)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `reconcileWorkingManifestAgainstPageFiles` (`:475-511` at planning time) and its three-line defence doc, post-15-39
    - `.planning/phases/15-continued-background-downloads/15-39-SUMMARY.md` — which pre-copy-guard remedy landed (WR-03's decision must be consistent with it)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests.swift` — where WR-04's pinning regression goes if the comparison change is taken (411 lines; ample headroom)
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**WR-01.** Enumerate `commitPause`'s exits from source and rewrite the false sentence in the
G-15-8 paragraph as a rule over exit categories, with no count: every `.settled` exit converges
inline (release, then `notifyObservers()`, then `scheduleNextIfNeeded()`), and every `.superseded`
exit releases and hands convergence to `pause(gid:expiration:)`, which converges on every
`.superseded` value it receives. The paragraph's surrounding G-15-8 invariant statement stays;
only the ownership claim changes.

**WR-02.** Replace the three-writer enumeration in `writeSettledPauseRecord`'s doc with the
invariant it stood for: every queue-mobilizing entry point takes no scheduling block, so any of
them can land inside the unbounded `await taskToCancel?.value` wait, and this helper re-clears
whatever such a writer put back. Name the census the new `DownloadSourceInventoryTests` pins
(Task 2) as the thing that fails if a mobilizer ever gains a block or a block site appears. Do not
carry a corrected four-name list — the list is exactly the shape that drifted.

**WR-03.** Re-enumerate every `sanitizeAssetFileIfNeeded` caller post-15-39 and decide one:
(a) restate the existence-guard comment so it claims only what holds — the `.unprobeable`
classification of a failed existence check is meaningful for listing-derived URLs; a caller
passing a manifest-constructed path reads a collapsed `Bool` and must never feed this function's
absence answer into a destructive decision (which, post-15-39, no caller does — state that as the
licensing condition); or (b) split the probe so constructed-path callers get a positive absence
for a missing file. Decision criteria: (a) is comment-only and is the default; (b) is justified
only if the post-15-39 census shows a constructed-path caller whose answer still reaches a
destructive decision — if none does, (b) changes classification semantics at seven call sites for
no consumer, which is scope this warning does not license. Record the census and the decision.

**WR-04.** Trace the residual guard post-15-39 and decide one: (a) correct the sentence to state
the true comparison — the equality is against ALL claimed pages by design, and its coverage of
the mixed population is deliberately narrower because the per-file line already refuses the
unprobed portion, so the residual fires only on the no-unprobed shape; or (b) the verifier's
comparison change — refuse when the blanking would take every page of the BLANKABLE population
(claimed, non-empty hash, not excluded as unprobed or carried) — which re-enables the residual
for the mixed population. Decision criteria: the verifier's own detail records the current
behaviour as correct, and (b) is a behavior change; take (b) only if the trace shows the mixed
all-blankable-gone shape genuinely needs the residual refusal (the manifest-just-read argument at
the doc's line 3 applies to it), and then land the pinning regression in
`DownloadCoordinatorRepairSeedTests.swift` in this same task: a mixed staging (some pages
unprobed via the carried set or the destination scan, every remaining blankable page genuinely
absent) asserting refusal — nothing blanked, nothing written, nothing withdrawn. Under (a) no
executable line moves. Either way the sentence ends true against the code one line below it, and
the summary records the trace that decided it. If (b) is attempted and any existing case goes
red, land (a) and record the counter-evidence — never bend the existing case.

**IN-01.** Rephrase `continuedSessionTask`'s aside count-free — tie it to the section's rule
(module-internal like every other session-state declaration here, reached by suites only through
its testing forwarder) rather than to any positional count. Do not write a corrected number: a
count that nothing checks is the exact shape this gap records.

**Then:** one targeted run of the suites covering the touched sources if (b) was taken
(`DownloadCoordinatorRepairSeedTests`), otherwise proceed to Task 2 — the full run comes at its
end. Never overlap invocations.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests</automated>
  </verify>
  <acceptance_criteria>
    - WR-01: `grep -c 'the one site whose convergence' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `0`, and the rewritten paragraph states the settled/superseded split as a category rule (quoted in the summary beside the executor's own exit enumeration).
    - WR-02: the doc names no writer list; `grep -c 'queue-mobilizing' AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` outputs at least `1`, and the sentence pointing at the census test is quoted in the summary.
    - WR-03: `grep -c 'a directory listing just produced' AppPackage/Sources/DownloadClient/DownloadStore.swift` outputs `0`, the post-15-39 caller census is in the summary, and the decided remedy (with rationale) is recorded; under (b), the split's semantics are stated per caller class.
    - WR-04: `grep -c 'every one of them would go' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` outputs `0`; the summary records the trace and the decision; under (b) the pinning regression exists in `DownloadCoordinatorRepairSeedTests.swift` and the targeted run exits `0`; under (a) `git diff` shows no executable-line change in the reconciliation.
    - IN-01: `grep -c 'like the eight above' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `0`, and the replacement carries no positional count (source inspection, quoted).
    - Comment-only proof: the task's `git diff` touches no executable line outside WR-04's decided site (stated from the diff, quoted in the summary).
  </acceptance_criteria>
  <done>All five claims are true against a fresh enumeration of the source they sit on, each correction is a rule or a decided remedy with its evidence recorded, and no inventory was corrected into a new unchecked count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin every surviving inventory with a drift-failing census test</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift, AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` — the WHOLE file: the repository-root walker, the known-member guard against vacuous walks, the fragment-assembled tokens (so repository grep gates cannot match the invariant that enforces them), and the per-file equality table plus separate joined total (`expectedHashMaskedCounts` / `expectedHashMaskedTotal`) — the new suite follows this pattern structurally
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the floor inventory doc on `lastPushedCompletedPageCount` (`:437-469`): the five writers it names and the sentence that asks the reader to re-run a grep (this task replaces that sentence with a pointer to the census)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift`, `DownloadClient+Folders.swift` (or wherever `delete`/`deleteFolder`/`moveDownload` live — locate by grep), and the DEBUG forwarder in `DownloadClient+Testing.swift` — every `blockScheduling(` call site, enumerated fresh
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <behavior>
    - `testSchedulingBlockCallSitesMatchTheRecordedCensus` — walks `AppPackage/Sources/DownloadClient`,
      counts `blockScheduling(` call occurrences per file (declaration excluded — assemble the
      detection token from fragments so this suite does not match its own definition), and asserts
      equality against a named per-file table plus a separate joined total, with a known-member
      guard so a vacuous walk cannot pass. The table is derived fresh from source at execution
      time; the expected census is the five sites the round-12 verification enumerated
      (`commitPause`, `delete`, `deleteFolder`, `moveDownload`, the DEBUG forwarder) — a
      difference is a finding to record, not a number to copy.
    - `testFloorWriterAssignmentsMatchTheRecordedCensus` — same mechanism over assignments to
      `lastPushedCompletedPageCount` (all mutation forms: plain assignment and compound
      operators), asserting the per-file census behind the doc's five-writer inventory.
  </behavior>
  <action>
**Step 1 — the suite.** Create `DownloadSourceInventoryTests.swift` with a header doc stating
what it exists for: source censuses that load-bearing docs cite must fail a build when they
drift, because a corrected comment alone is how G-15-3, G-15-7, G-15-13, and G-15-19 happened
(the IN-05 fold-in). Reuse the structural pattern read from `DownloadLogPrivacyInvariantTests` —
root walker, known members, fragment-assembled tokens, per-file equality plus joined total. Keep
the scanner logic local to this file; do not couple the two suites.

**Step 2 — the two censuses per the behavior block.** Derive both expected tables by enumerating
source, then cross-check against the docs the tables back: the `blockScheduling` census against
WR-02's rewritten rule, the writer census against the floor doc's five-writer inventory. If
either enumeration disagrees with the corresponding doc as Task 1 left it, fix the DOC (and
record the discrepancy) — the census asserts source, and the doc must describe source.

**Step 3 — close the loop in the floor doc.** Replace the floor inventory's sentence that asks
the reader to re-run a grep with one naming `DownloadSourceInventoryTests` as the check that
fails when the writer set moves. The five-entry inventory itself stays — it is load-bearing and
correct — which is exactly why it now has a companion that breaks.

**Step 4 — falsifiability and green.** These censuses assert current source, so an honest RED is
not available; record falsifiability bookkeeping-style in the summary: for each census, name the
edit that would flip it (adding a `blockScheduling` call in `resume`, adding a sixth floor
writer) and why the equality catches it. Run the new suite targeted, then ONE full FeatureTests
run. Never overlap invocations.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Both census cases exist: a grep for each case name over `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` outputs `1` per name.
    - The suite refuses vacuous walks: a known-member `#require` exists for the scanned directory (source inspection, quoted).
    - The detection tokens are fragment-assembled so the suite does not match its own strings (source inspection).
    - The floor doc points at the census test and no longer asks the reader to re-run a grep: `grep -c 'Re-run that grep' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `0` and `grep -c 'DownloadSourceInventoryTests' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs at least `1`.
    - The summary records both derived censuses, their reconciliation against the Task 1 docs, and the falsifiability bookkeeping (which edit flips each census).
    - The targeted run and one full FeatureTests run exit `0`, each a single invocation; `wc -l` for the new file recorded and well under `1000`.
  </acceptance_criteria>
  <done>Every inventory a doc still carries has a census that fails the build when it drifts, derived fresh from source and reconciled against the corrected docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Written premise → future fix | Load-bearing doc claims are the reasoning surface later scheduling and data-integrity fixes stand on; a false premise steers a correct-looking fix into a defect (the recorded generator of G-15-3, G-15-7, G-15-13, G-15-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-40-01 | Tampering | A future edit trusting WR-01/WR-02's false ownership claims reproducing a stranded-block or abandoned-pause defect | medium | mitigate | Rule-phrased corrections derived from fresh exit/writer enumerations; blockScheduling census fails on drift |
| T-15-40-02 | Tampering | WR-03/WR-04's false premises steering the next probe/reconciliation edit into destroying recorded hashes | medium | mitigate | Decided remedies consistent with the 15-39 seam; under WR-04(b) a pinning regression lands with the change |
| T-15-40-03 | Denial of Service | WR-04(b) taken wrongly, refusing legitimate reconciliations and leaving stale claims | low | mitigate | Decide-one criteria require the trace; existing genuine-absence cases must stay green or the alternate lands |
</threat_model>

<verification>
- Task 1 targeted run only if WR-04(b) is taken; Task 2 targeted run plus one full FeatureTests
  run. xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from G-15-20's truth_violated clause and the SC labels its sites back (SC2, SC3).
- <assumption_delta_decision>no-change — comment corrections and source-census tests; no identity
  model changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan.
</verification>

<success_criteria>
- G-15-20 closed: all five claims read true against fresh source enumerations quoted in the
  summary, every correction is a rule or a decided remedy with evidence, every surviving
  inventory has a drift-failing census, the floor doc points at the census instead of a manual
  grep, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-40-SUMMARY.md` when done, including
the five before/after sentences with their deciding enumerations, the WR-03 and WR-04 decisions
with rationale, both censuses, and the falsifiability bookkeeping.
</output>
