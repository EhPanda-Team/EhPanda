---
phase: 15-continued-background-downloads
plan: 73
type: execute
wave: 73
depends_on: ["15-72"]
files_modified:
  - AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsReducer.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorPauseFailureTests.swift
autonomous: true
gap_closure: true
gap_ids: [WR-05]
requirements: [SC2]

must_haves:
  truths:
    - "Every failing inspector action the user can tap reports why it failed: toggleDownloadPauseDone(.failure) surfaces the error through the toast the reducer already owns, exactly as the retry and validation branches do."
    - "Every result-carrying Done action in DownloadInspectorReducer AND DownloadsReducer has a stated disposition — reports, or deliberately does not and says why — so a future silent branch cannot be added without contradicting a doc."
    - "The failure-toast mapping is named for what it now is: it serves multiple action families, not just retry."
  artifacts:
    - path: "AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift"
      provides: "Reporting pause-failure branch and the renamed actionFailureToast mapping"
      contains: "actionFailureToast"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorPauseFailureTests.swift"
      provides: "Reducer cases pinning the pause-failure toast from both sides"
      contains: "toggleDownloadPauseDone"
  key_links:
    - from: "AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift"
      to: "AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift"
      via: "the pause branch reuses the same private AppError toast mapping the retry branch renders"
      pattern: "actionFailureToast"
---

<objective>
Close verification gap 4 (SC2; review WR-05) — the branch-scoped shape this phase has re-opened every round. 15-69 fixed `retryPagesDone(.failure)` (DownloadInspectorReducer.swift:189-198) and left its sibling 25 lines below untouched: `toggleDownloadPauseDone(.failure)` is still `if case .failure = result { return .send(.loadInspection) }` (221-225) — reload and say nothing. The failure is reachable on exactly WR-04's shape: `togglePause` answers `.failure` for a gallery whose status moved out of the toggleable set between render and tap, and for a gallery deleted underneath the inspector — the row reverts and nothing tells the user why. The reducer owns the toast surface (`validateImageDataDone` writes it at 239; `retryPagesDone` at 194) and the `retryFailureToast` mapping (247-268) is directly reusable.

Per the gap's missing[], the fix is invariant-scoped, not branch-scoped: sweep EVERY `…Done(.failure)` branch in BOTH reducers, enumerate each result-carrying action with a reports-or-deliberately-silent disposition, surface the pause branch, rename the mapping to `actionFailureToast`, and add a reducer case per newly-reporting branch.

Purpose: refusals are visible and truthful at every inspector surface (SC2).
Output: a reporting pause branch, a renamed multi-family toast mapping, a two-reducer disposition table stated in code docs and the summary, and TestStore regressions from both sides.
</objective>

## Artifacts this phase produces

- Revised `toggleDownloadPauseDone` (DownloadInspectorReducer.swift:221-225): the failure arm sets `state.toast = error.actionFailureToast` before re-sending `.loadInspection`; the success arm is unchanged (`.none`).
- Renamed private helper `AppError.retryFailureToast` → `AppError.actionFailureToast` (DownloadInspectorReducer.swift:262) with its doc re-derived for multi-family use; both call sites updated.
- Disposition docs: a one-line stated disposition on every silent result-carrying failure arm in `DownloadsReducer` (`moveDownloadDone`, `toggleDownloadPauseDone`, `updateDownloadDone`, `deleteDownloadDone`, `openReadingDone`) anchored to the existing DES-3 list-level policy comment (DownloadsReducer.swift:364-366), plus the enumerated two-reducer table in the summary.
- New test file `AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorPauseFailureTests.swift`, suite `DownloadInspectorPauseFailureTests`: TestStore cases for the pause failure toast (per failure kind) and the success no-toast side.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-69-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the silent pause failure from both sides</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorPauseFailureTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorRetryTests.swift` — `makeRetryTestStore` and the 15-69 toast-assertion idiom (exact toast value, exhaustivity conventions, `@MainActor` pattern): the fixture shape this suite mirrors with a `togglePause` override instead of `retryPages`.
    - `AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift` — `toggleDownloadPause` (200-219: the `canTogglePause` guard and the analytics send), `toggleDownloadPauseDone` (221-225), the toast mapping (247-268), and `validateImageDataDone`'s assertion precedent (237-240).
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift` — `togglePause`'s refusal exits, so the staged failure kinds are the ones production actually answers (`.notFound` for a vanished gallery; the non-toggleable-status refusal's kind — read the source, do not assume).
  </read_first>
  <behavior>
    - Sending `.toggleDownloadPauseDone(.failure(error))` sets `state.toast` to the mapped error toast AND re-sends `.loadInspection` — RED pre-fix (no toast is written). Pin at least two failure kinds: a `.fileOperationFailed(message)` rendering `.error(caption: message)`, and `.notFound` rendering its `alertText` — the two arms of the mapping, so the rename cannot regress either.
    - Sending `.toggleDownloadPauseDone(.success)` sets no toast and triggers no reload — the other side, pinned green pre- and post-fix.
    - The regression discriminates: the pre-fix observable is a state transition that changes only `retryingPageIndices`-adjacent state with `toast == nil` while `.loadInspection` fires; the case doc states this.
  </behavior>
  <action>
Create `DownloadInspectorPauseFailureTests.swift` with a fixture modeled on `makeRetryTestStore` (own file — do not grow DownloadInspectorRetryTests with off-family cases, and add nothing to DownloadFeatureTestHelpers.swift at 992/1000 lines). Drive the reducer through `.toggleDownloadPauseDone` directly with both failure kinds and the success case; where the full tap path is cheap to stage (a `togglePause` dependency override that throws), add one end-to-end case proving the effect's catch arm wraps the client error into the action — the seam 15-69's retry cases cover for retry. Assert exact toast values using the mapping's own rendering rules (match the retry suite's idiom). Bank pre-fix observations.
  </action>
  <verify>
    <automated>! xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadInspectorPauseFailureTests</automated>
  </verify>
  <acceptance_criteria>
    - Failure-arm cases fail pre-fix precisely on the missing toast; the success-arm case passes pre-fix (both sides pinned).
    - Both mapping arms (payload-rendered fileOperationFailed; alertText fallback) are asserted with exact values.
    - The staged failure kinds are derived from `togglePause`'s real refusal exits, named in the case docs.
  </acceptance_criteria>
  <done>The silent pause failure is a deterministic RED observation with the success side pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — surface the branch, rename the mapping, sweep both reducers with dispositions</name>
  <files>AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift, AppPackage/Sources/DownloadsFeature/DownloadsReducer.swift</files>
  <read_first>
    - `AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift` — every result-carrying Done action: `loadInspectionDone` (88-106, reports via `loadingState = .failed`), `retryPagesDone` (189-198, reports via toast), `toggleDownloadPauseDone` (221-225, the fix site), `validateImageDataDone` (237-240, reports via toast); the mapping doc (247-261) whose retry-specific wording is re-derived.
    - `AppPackage/Sources/DownloadsFeature/DownloadsReducer.swift` — every result-carrying Done action and its current disposition evidence: `moveDownloadDone` (285-295, has a deliberate-silence comment), the DES-3 list-level policy comment (364-366), `toggleDownloadPauseDone` (353-354, NO stated disposition), `updateDownloadDone` (367-372), `deleteDownloadDone` (382-386), `openReadingDone` (312-330, reports by falling back to the remote reader).
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — gap 4's three `missing:` bullets.
  </read_first>
  <behavior>
    - Inspector: the pause failure arm writes `state.toast = error.actionFailureToast`, clears nothing else, and re-sends `.loadInspection`; the mapping is renamed with a doc naming both consuming families and why `.fileOperationFailed` renders payload-only (keep that derivation, generalized).
    - DownloadsReducer: no behavior change — every silent failure arm gains a one-line disposition comment anchored to the DES-3 policy (the observe stream is the list's feedback surface; the row's revert is the visible outcome; a failed move/update/delete is not that operation). `toggleDownloadPauseDone`'s bare `return .none` gets the disposition it has never had. `openReadingDone`'s failure arm documents that it reports by behavior (remote fallback or silent bail when the record vanished — state which and why).
    - The summary carries the full two-reducer enumeration: action, failure reachability, disposition, and where the disposition is stated in code — the table the gap demands so no future branch lands silently.
  </behavior>
  <action>
Implement per behavior. Rename `retryFailureToast` → `actionFailureToast` everywhere (`rg -n 'retryFailureToast'` must end empty — including comments; the 15-69 comment at 191-193 stays accurate since it describes the retry branch's own defect history). Keep the pause branch's ordering identical to retry's (toast, then reload) so the two siblings read as one family. Add the disposition comments in DownloadsReducer without changing any effect. Build the summary table covering BOTH reducers' complete result-carrying inventory (inspector: 4 actions; downloads: 5) with per-branch dispositions.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadInspectorPauseFailureTests -only-testing:DownloadsFeatureTests/DownloadInspectorRetryTests</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 cases green; DownloadInspectorRetryTests green (the rename touched its subject).
    - `rg -n 'retryFailureToast' AppPackage` → no matches; `rg -c 'actionFailureToast' AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift` ≥ 3 (declaration + two branch call sites).
    - `rg -nF 'state.toast' AppPackage/Sources/DownloadsFeature/DownloadInspectorReducer.swift` shows three writer sites: validation, retry failure, pause failure.
    - Every silent failure arm in DownloadsReducer carries a stated disposition (verified by reading the five Done cases); no DownloadsReducer effect changed.
    - The summary contains the nine-action disposition table.
    - No SwiftLint suppression; `Delegate`-enum and reducer conventions untouched.
  </acceptance_criteria>
  <done>Every failing tap the inspector offers reports why; every silent branch in both reducers is deliberately, documentedly silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → queue mutation result | A refused mutation must be visible and truthful; a silent revert teaches the user the app is broken or lies about state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-73-01 | Repudiation | silent pause refusal | medium | mitigate | Failure toast on the pause branch; both mapping arms pinned by TestStore cases. |
| T-15-73-02 | Information Disclosure | toast message content | low | accept | Static localized sentences or AppError alertText; no gallery identity or path embedded — the module's established pattern, unchanged. |
| T-15-73-03 | Repudiation | future silent branches | low | mitigate | Stated per-branch dispositions in both reducers plus the summary inventory; a new silent branch contradicts a doc. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadInspectorPauseFailureTests -only-testing:DownloadsFeatureTests/DownloadInspectorRetryTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1573DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels plus the two UAT-FU anchors, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC2 | Consistent, truthful download controls | 15-73 | COVERED | The last silent inspector failure reports. |
| VERIFICATION | gap 4 bullet 1 | Sweep every Done(.failure) in both reducers with dispositions | 15-73 | COVERED | Nine-action table, dispositions stated in code. |
| VERIFICATION | gap 4 bullet 2 | Rename retryFailureToast | 15-73 | COVERED | actionFailureToast, grep-gated. |
| VERIFICATION | gap 4 bullet 3 | Reducer case per newly-reporting branch | 15-73 | COVERED | Pause failure cases from both sides plus the end-to-end catch-arm case. |
| REVIEW | WR-05 | Same defect by review ID | 15-73 | COVERED | — |

<success_criteria>
- A refused pause/resume tap in the inspector shows a toast whose message matches the refusal; success stays toast-free.
- Both reducers' result-carrying actions are fully enumerated with stated dispositions.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-73-SUMMARY.md` when done.
</output>
