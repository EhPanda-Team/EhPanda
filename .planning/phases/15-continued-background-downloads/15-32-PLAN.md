---
phase: 15-continued-background-downloads
plan: 32
type: execute
wave: 32
depends_on: ["15-31"] # last by design: IN-01's note names G-15-9's landed refusal shape (15-30), the IN-03 inventory must count 15-30's new masked log, and the rename sweep overlaps files touched by 15-27..15-31. One xcodebuild at a time.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadProcessTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DetailReducerPauseAndGuardTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DetailReducerDownloadTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadObserverReadingTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/FolderManagerReducerTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadProcessCacheTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/ReadingReducerLocalTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DetailReducerMetadataUpdateTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DetailReducerMetadataTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/ReadingReducerDownloadTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadsReducerActionTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorSkipTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DetailReducerObserveTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadsReducerRefreshTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadObserverRefreshTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadImageParsingTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadAutomationTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorLoadTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-12]
requirements: [SC1, SC2]

must_haves:
  truths:
    - "No written premise in the touched surface is wrong, because a wrong written premise is a first-class defect in this phase (it produced G-15-3 and G-15-7): the dedupe rationale on the one authority states the true reason (redundant defence — the index holds one record per gid, so a duplicate scoped gid could not double pages); resumeMode's storage.validate branch carries its why-still-reachable note naming the G-15-9 refusal routes; and the dead force-unwrap in resolveSource is replaced by a guard that makes the producer's non-nil contract local instead of trapping."
    - "The log-privacy invariant has no blind spot for unclassified interpolations: every interpolation in a DownloadClient logger call carries an explicit privacy classification, enforced by a scanner check that was observed failing on the known unclassified site before that site was classified."
    - "The hash-mask count pins something derivable: the scanner asserts against a named per-file inventory of masked interpolations instead of a bare numeric threshold, so a masked log added or removed anywhere is a deliberate, visible table update — including the blanking log 15-30 added."
    - "The test-support box's name says what it is: the Mutex-backed, genuinely-checked box is LockedBox, so the name no longer advertises the unchecked-Sendable pattern this project bans at error severity; the rename is a whole-target sweep with zero behavior change."
    - "The one authority's active-gallery union has direct coverage at the pending-work seam: a schedulable active gallery absent from a non-empty persisted queue is seen by hasPendingWork, with a control assertion proving the union (not the queued remainder) is what makes it true."
    - "The 999-line file risk is dispositioned, not deferred: DownloadContinuedSessionTests.swift's headroom was restored by 15-28's relocation (recorded with the landed wc -l), this plan adds no case to it, and the new coverage lands in DownloadPendingWorkTests."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "WR-06: resolveSource's galleryURL handled by guard-let-throw (AppError.notFound), matching the producer's own guard, with a one-line contract note; no force-unwrap helper remains in the function"
      contains: "AppError.notFound"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift"
      provides: "IN-04: the dedupe sentence corrected to redundant defence with the true mechanism named (downloadIndex.values holds one record per gid; downloads(from:) deduplicates again)"
      contains: "redundant defence"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift"
      provides: "IN-01: the storage.validate branch's why-still-reachable note naming the G-15-9 refusal (failed or wholesale scan leaves a complete-reading record with missing files) and the untouched-record route"
      contains: "G-15-9"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift"
      provides: "IN-02: the request-URL interpolation explicitly classified .private"
      contains: "privacy: .private"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift"
      provides: "IN-02's scanner check for unclassified interpolations in logger calls, and IN-03's named per-file masked-interpolation inventory replacing the bare threshold"
      contains: "expectedHashMaskedCounts"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift"
      provides: "IN-05: the box renamed LockedBox with its doc stating it is Mutex-backed and genuinely checked"
      contains: "LockedBox"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift"
      provides: "IN-05: the active-gallery-union coverage with its control assertion"
      contains: "testHasPendingWorkSeesAnActiveGalleryThePersistedQueueLagsBehind"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift"
      via: "the new scanner check fails on the unclassified interpolation until it is classified — the RED-first proof that the blind spot is closed"
      pattern: "privacy"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift"
      via: "the union case pins schedulableDownloads' active-gallery read through the public hasPendingWork surface, with the control assertion isolating the union's contribution"
      pattern: "hasPendingWork"
  prohibitions:
    - statement: "Must NOT change any production behavior beyond the WR-06 guard and the IN-02 classification — IN-01/IN-04 are documentation, IN-03/IN-05 are test-side; a behavior delta anywhere else is a finding."
      status: pending
      verification: automated
    - statement: "Must NOT add any case or line to DownloadContinuedSessionTests.swift."
      status: pending
      verification: automated
    - statement: "Must NOT weaken the existing privacy invariants: the forbidden-interpolation and masked-message checks stay; the named inventory may only make the masked check stricter."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression; the rename must leave zero occurrences of the old box name in the target."
      status: pending
      verification: automated
---

<objective>
Close G-15-12 (warning, blocking under this project's perfection bar) — six confirmed hygiene
items, two of which are the exact failure shape that produced G-15-3 and G-15-7 (a written
premise that is wrong):

- **WR-06** — `resolveSource` passes the gallery URL through a force-unwrap helper into a
  non-optional parameter, while `resolvedImageSource` in the same file handles the same optional
  with a fallback. The unwrap is dead (the only producer, `fetchLatestPayload`, guards
  `AppError.notFound` on nil), but a dead trap plus two treatments of one optional is exactly
  the local-contract ambiguity this project removes. Fix per the mandate's guard option: hoist a
  guard-let throwing `AppError.notFound` — the producer's own error — with a one-line note that
  the producer guarantees non-nil and the guard makes that contract local instead of trapping.
  (The sibling's host-URL fallback is NOT copied: silently retargeting a thumbnail request is a
  behavior change; the guard preserves observable behavior exactly.)
- **IN-04** — `schedulableDownloads()`'s doc justifies its dedupe with a claim that is provably
  false (`indexedDownloads(gids:)` filters `downloadIndex.values`, which holds exactly one
  record per gid, and `downloads(from:)` deduplicates again). Correct the sentence to redundant
  defence, keeping the check.
- **IN-01** — `resumeMode`'s `storage.validate` branch is near-dead after D-G5-01 and carries no
  note saying why it survives. Post-15-30 it has a load-bearing answer: it is the route that
  still resolves `.repair` when the reconciliation REFUSES its destructive half (failed or
  wholesale scan — G-15-9), and for a complete-reading record with missing files that no
  preparation has touched this session. Write that note on the branch.
- **IN-02** — an unclassified request-URL interpolation is invisible to the invariant scanner,
  which only greps for the public classification. Classify the site explicitly `.private`, and
  add a scanner check that flags ANY unclassified interpolation in the module's logger calls —
  observed failing on the known site first — then sweep whatever else it finds.
- **IN-03** — `maskedCount >= 8` pins nothing derivable and drifts silently. Replace it with a
  named per-file inventory of masked interpolations asserted with equality, so every
  addition/removal (including 15-30's blanking log) is a visible table update.
- **IN-05** — `UncheckedBox` is Mutex-backed and genuinely checked; the name invites the banned
  unchecked-Sendable pattern. Rename it `LockedBox` across the target.
  `DownloadPendingWorkTests` has no case for the active-gallery union 15-26 added to the one
  authority — add it, with a control assertion isolating the union. The 999-line
  `DownloadContinuedSessionTests.swift` headroom item was restored structurally by 15-28's
  relocation; this plan records that disposition and adds nothing to that file.

Purpose: no wrong premise, no scanner blind spot, no underived threshold, no misleading name, no
uncovered union — the perfection-bar closure of everything round 10 confirmed.
Output: two production edits (guard, classification), four doc/test corrections, one rename
sweep, one new coverage case, and a green full run.
</objective>

## Artifacts this plan produces

- **Renamed type (test target):** `UncheckedBox` → `LockedBox`
  (declaration in `DownloadFeatureTestSupportTypes.swift`; ~19 user files re-spelled).
- **New test case:** `testHasPendingWorkSeesAnActiveGalleryThePersistedQueueLagsBehind` in
  `DownloadPendingWorkTests.swift` (plus a small extension of its private manifest helper to
  stage an incomplete and a complete record).
- **New scanner assets in `DownloadLogPrivacyInvariantTests.swift`:** an
  unclassified-interpolation check over the module's logger calls, and the
  `expectedHashMaskedCounts` named per-file inventory replacing the bare threshold.
- **Production edits:** the `resolveSource` guard (`DownloadClient+ExecutionSupport.swift`), the
  `.private` classification (`DownloadClient+ResponseValidation.swift`).
- **Doc-only edits:** the corrected dedupe sentence (`DownloadClient+PendingWork.swift`), the
  why-still-reachable note (`DownloadClient+SchedulingHelpers.swift`).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-30-SUMMARY.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The premise corrections, the classification, and the scanner upgrades observed failing first</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift, AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift, AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-12 entry WHOLE (each item's evidence, the WR-06 reachability correction, and the mandated fixes)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `resolveSource` (the unwrap inside the page-number loop) and `resolvedImageSource` (the sibling's differing treatment, referenced in the note), plus `fetchLatestPayload` in `DownloadClient+ExecutionFetch.swift` (the producer's guard the note cites)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` — the whole file: the doc sentence to correct sits on `schedulableDownloads()`; nothing executable changes
    - `AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift` — `resumeMode` whole (the branch order the note must not misstate) 
    - `AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift` — the unexpected-HTML logger call (`:262-273` region: the unclassified `requestURL` interpolation beside the already-private snippet)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` — the whole file: the fragment-assembly convention (tokens built at run time so repository grep gates cannot match the invariant), the scanned-files plumbing, and both existing checks
    - `.planning/phases/15-continued-background-downloads/15-30-SUMMARY.md` — the landed blanking log (the new masked site the IN-03 inventory must count)
  </read_first>
  <behavior>
    - New scanner check `testEveryDownloadLogInterpolationCarriesAnExplicitPrivacyClassification`:
      for each scanned module file, locate every `logger.` call span (balancing parentheses from
      the call's opening paren), find each string interpolation opening inside it, and require an
      explicit `privacy:` argument within that interpolation. RED first: observed failing on the
      known unclassified request-URL interpolation before Step 3 classifies it. Assemble any
      needed detection tokens from fragments per the file's convention.
    - IN-03 replacement: `expectedHashMaskedCounts` — a named `[String: Int]` from module file
      name to its count of hash-masked interpolations — asserted with per-file equality plus a
      total; the existing four operational-message presence checks stay. The table includes
      15-30's blanking log site.
  </behavior>
  <action>
**Step 1 — RED the scanner.** Write the unclassified-interpolation check per the behavior block
and run the targeted invariant-suite command; record verbatim the failure naming the
request-URL site (and any other unclassified sites it finds — each is in scope for Step 3's
sweep, not a reason to weaken the check). In the same edit, replace the bare `maskedCount >= 8`
with the `expectedHashMaskedCounts` inventory: build it by counting the module's hash-masked
interpolations per file at execution time (the RED run's failure output and a grep over the
scanned files are the derivation evidence to record).

**Step 2 — the premise corrections.** In `DownloadClient+PendingWork.swift`, rewrite the dedupe
sentence: the membership check is redundant defence, not load-bearing — `indexedDownloads(gids:)`
filters `downloadIndex.values`, which holds exactly one record per gid, and `downloads(from:)`
deduplicates again, so a duplicated scoped gid could not double a gallery's pages; the check
stays because a cheap invariant at the read keeps the scoped list canonical if either downstream
shape ever changes. In `DownloadClient+SchedulingHelpers.swift`, add the why-still-reachable
note on `resumeMode`'s `storage.validate` branch: after D-G5-01 most missing-file records are
blanked into `isIncomplete` before this branch runs, but it remains load-bearing for (a) a
record whose reconciliation REFUSED its destructive half — a failed or wholesale page-file scan
(G-15-9) — leaving a complete-reading record with files missing, and (b) a complete-reading
record with missing files that no preparation has touched this session; in both, this branch is
what still routes the record to `.repair`.

**Step 3 — the WR-06 guard and the IN-02 classification.** In `resolveSource`, hoist
`guard let galleryURL = payload.gallery.galleryURL else { throw AppError.notFound }` above the
page-number loop and pass `galleryURL` to the request; one doc line: the only producer
(`fetchLatestPayload`) already guards this optional with the same error, and the local guard
states that contract where it is consumed instead of trapping on it (the sibling
`resolvedImageSource`'s host fallback is deliberately not copied — retargeting a request is a
behavior change; this guard is behavior-identical). In
`DownloadClient+ResponseValidation.swift`, give the request-URL interpolation an explicit
`.private` classification (matching the unified-log default it already had implicitly). Sweep
every other site Step 1's RED run flagged: each gains an explicit classification chosen by
content (operational scalars `.public`; URLs, errors, and anything identity-adjacent
`.private` or the module's hash-mask), enumerated with dispositions in the summary.

**Step 4 — GREEN.** Run the targeted invariant-suite command: both new checks green, both
pre-existing checks green. The full run happens in Task 2.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadLogPrivacyInvariantTests</automated>
  </verify>
  <acceptance_criteria>
    - The scanner has no blind spot: `grep -c 'testEveryDownloadLogInterpolationCarriesAnExplicitPrivacyClassification' AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` outputs `1`, and the summary quotes the RED failure naming the request-URL site before its classification landed.
    - The threshold is derivable: `grep -c 'expectedHashMaskedCounts' AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` outputs at least `2` (declaration + assertion), and `grep -c 'maskedCount >= 8' AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` outputs `0` (it is `1` at planning time).
      <!-- planner-discipline-allow: maskedCount >= 8 -->
    - WR-06 closed: the extracted `resolveSource` body contains `guard let galleryURL` and no force-unwrap helper invocation — the sed-extracted region grepped for the helper's name outputs `0` while the repository-wide count elsewhere is unchanged by this plan.
      <!-- planner-discipline-allow: forceUnwrapped -->
    - IN-04 corrected: `grep -c 'redundant defence' AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` outputs at least `1`, and `grep -c 'would double that gallery' AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` outputs `0` (it is `1` at planning time) — the wrong sentence is replaced, not supplemented.
      <!-- planner-discipline-allow: would double that gallery -->
    - IN-01 noted: `grep -c 'G-15-9' AppPackage/Sources/DownloadClient/DownloadClient+SchedulingHelpers.swift` outputs at least `1`, inside a comment on the `storage.validate` branch (summary quotes the note).
    - IN-02 classified: the unexpected-HTML logger call's URL interpolation carries `privacy: .private` (summary quotes the call), and the sweep table in the summary dispositions every site the RED run flagged.
    - The targeted run exits `0` as a single invocation with all four invariant checks green.
  </acceptance_criteria>
  <done>The scanner now sees what it could not, the mask count derives from a named inventory, and every corrected sentence states a premise that is true in source — with the scanner's blind-spot closure proven RED-first.</done>
</task>

<task type="auto">
  <name>Task 2: The LockedBox rename sweep, the union coverage, the headroom disposition, and the full run</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadProcessTests.swift, AppPackage/Tests/DownloadsFeatureTests/DetailReducerPauseAndGuardTests.swift, AppPackage/Tests/DownloadsFeatureTests/DetailReducerDownloadTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadObserverReadingTests.swift, AppPackage/Tests/DownloadsFeatureTests/FolderManagerReducerTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadProcessCacheTests.swift, AppPackage/Tests/DownloadsFeatureTests/ReadingReducerLocalTests.swift, AppPackage/Tests/DownloadsFeatureTests/DetailReducerMetadataUpdateTests.swift, AppPackage/Tests/DownloadsFeatureTests/DetailReducerMetadataTests.swift, AppPackage/Tests/DownloadsFeatureTests/ReadingReducerDownloadTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadsReducerActionTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorSkipTests.swift, AppPackage/Tests/DownloadsFeatureTests/DetailReducerObserveTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadsReducerRefreshTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadObserverRefreshTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadImageParsingTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadAutomationTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadInspectorLoadTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift` — the box declaration (`:8-19`: Mutex-backed, genuinely checked — the doc line the rename adds states exactly that)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift` — the whole file (46 lines): the existing case, `writeQueuedManifest`, and `sampleManifest` (the helper to extend for an incomplete record and a complete one)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PendingWork.swift` — `hasPendingWork` (the activeTask short-circuit the staging must bypass by installing NO task) and `schedulableDownloads()` (the union under test)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` — `testingSetActiveGalleryID`, `testingSetQueuedGalleryIDs` (the staging seams)
    - `.planning/phases/15-continued-background-downloads/15-28-SUMMARY.md` — the relocation's landed `wc -l` values (the headroom disposition this plan records)
  </read_first>
  <behavior>
    - `testHasPendingWorkSeesAnActiveGalleryThePersistedQueueLagsBehind` — the IN-05 union
      coverage at the mandated location. Staging (no `BlockingRunner` needed — no task is
      installed): storage carrying TWO records — gid "210401" incomplete (schedulable by
      predicate) and gid "210402" complete (not schedulable); `reloadDownloadIndex()`;
      `testingSetQueuedGalleryIDs(["210402"])` (a non-empty persisted queue that does NOT list
      the active gallery); `testingSetActiveGalleryID("210401")` with no active task, so
      `hasPendingWork`'s activeTask short-circuit cannot answer. Assertions:
      1. `hasPendingWork() == true` — reachable only through the union: the queue-scoped read
         fetches ["210402", "210401"], and the active record passes the predicate.
      2. Control: `testingSetActiveGalleryID(nil)` → `hasPendingWork() == false` — the queued
         remainder alone contributes nothing, isolating the union as the reason for assertion 1.
      Falsifiability is structural (its doc comment says so): reverting 15-26's union makes
      assertion 1 read false; the control assertion proves the staging cannot pass vacuously.
  </behavior>
  <action>
**Step 1 — the rename sweep.** In `DownloadFeatureTestSupportTypes.swift`, rename the box type
to `LockedBox` and add one doc line: Mutex-backed and genuinely checked — named for what it is,
so the name cannot advertise the unchecked-Sendable pattern this project bans (IN-05). Re-spell
every use across the target (the files list enumerates the 19 user files found at planning
time; re-derive the list at execution time with a whole-target grep and sweep any file it adds —
a file appearing outside this plan's list is an inventory finding to report and include, never
skip). Zero behavior change: type body, generic signature, and every call site's semantics are
identical.

**Step 2 — the union coverage.** Extend `DownloadPendingWorkTests`'s private helper so it can
write an incomplete manifest and a complete one (a `completedPageCount` parameter on the
existing writer, or a sibling writer — follow the file's register), then add the case per the
behavior block with its doc comment carrying the structural-falsifiability statement and the
WR-01 lineage (the union landed in 15-26; this is its first direct coverage at the pending-work
seam, mandated by IN-05).

**Step 3 — the headroom disposition and the full run.** Run the full FeatureTests plan once
(single invocation, no other xcodebuild). Every suite green — the rename is behavior-neutral,
the new case additive; any failure is a finding. Record in the summary: the IN-05 headroom
disposition (restored structurally by 15-28's relocation, with the landed `wc -l` for
`DownloadContinuedSessionTests.swift`, and `git diff --stat` proof that THIS plan did not touch
that file), the rename's file inventory with per-file occurrence counts, and the gap-closure
statement for all six G-15-12 items with where each landed (WR-06/IN-01/IN-02/IN-04 in Task 1;
IN-03 in Task 1; IN-05 across 15-28's relocation and this task).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The rename is total: `grep -rc 'UncheckedBox' AppPackage/Tests/ --include='*.swift'` totals `0` across the target (it is ~19 files at planning time), and `grep -c 'final class LockedBox' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift` outputs `1`.
      <!-- planner-discipline-allow: UncheckedBox -->
    - The union case exists with its control: `grep -c 'testHasPendingWorkSeesAnActiveGalleryThePersistedQueueLagsBehind' AppPackage/Tests/DownloadsFeatureTests/DownloadPendingWorkTests.swift` outputs `1`, and its body contains `testingSetActiveGalleryID` twice (the set and the nil control) and two `hasPendingWork` expectations with opposite truth values.
    - No line lands in the guarded file: `git diff --stat` for this plan shows no change to `DownloadContinuedSessionTests.swift`, and the summary records its landed `wc -l` from 15-28.
    - The full FeatureTests run exits `0` as a single invocation with every suite green; `wc -l` recorded and below `1000` for `DownloadFeatureTestSupportTypes.swift` and `DownloadPendingWorkTests.swift`.
    - The summary carries the six-item closure table (item → fix → landed location → evidence).
  </acceptance_criteria>
  <done>The box's name tells the truth, the union has direct falsifiable coverage at its mandated seam, the guarded file gained nothing, and all six G-15-12 items are dispositioned with evidence in one table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coordinator → unified log | Log interpolations cross into a surface readable from device archives |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-32-01 | Information Disclosure | Unclassified log interpolations invisible to the invariant scanner (correct today only by OSLog defaults) | medium | mitigate | Explicit classification everywhere + a scanner check proven RED-first on the known site |
| T-15-32-02 | Repudiation | Underived masked-count threshold drifting silently as logs change | low | mitigate | Named per-file inventory asserted with equality |
| T-15-32-03 | Tampering | Wrong written premises inviting future bad edits (the G-15-3/G-15-7 class) | medium | mitigate | IN-01/IN-04/WR-06 premises corrected at the code, pinned by acceptance greps |
</threat_model>

<verification>
- Task 1 targeted invariant-suite run RED then GREEN; Task 2 full FeatureTests run. Never
  overlap xcodebuild invocations.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from the G-15-12 items and SC1/SC2 hygiene.
</verification>

<success_criteria>
- All six G-15-12 items closed with evidence: guard landed, notes and corrected sentences in
  place, scanner blind spot closed RED-first, threshold derivable, box renamed target-wide,
  union covered with a control, headroom disposition recorded.
- Full suite green; zero behavior change outside the WR-06 guard and the classifications.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-32-SUMMARY.md` when done, including
the six-item closure table, the scanner RED output, the classification sweep dispositions, and
the rename inventory.
</output>
