---
phase: 15-continued-background-downloads
plan: 72
type: execute
wave: 72
depends_on: ["15-71"]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSeedRecoveryTests.swift
autonomous: true
gap_closure: true
gap_ids: [WR-03]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "An authorized destructive reconciliation either completes durably or compensates and leaves a trail: every post-removal exit on the seed route re-attempts the blanking once and, when the retry also fails, logs the removed indices at error beside the masked gid."
    - "The recover-once-and-log rule has ONE implementation reached from both the validate route and the seed route — it cannot fork again."
    - "The announced progress basis never over-reports: a post-removal rescan failure cannot cause pages this pass deleted to be presumed done, so the pushed numerator's basis never exceeds the honest one."
    - "No withdrawal bracket nests: the seed route's recovery runs inside prepareWorkingSeed's existing bracket, and the validate route keeps its sibling bracket."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "authorizedReconciliationScan returning its removed page set; prepareWorkingSeed compensating all three post-removal exits; inheritedPages never presuming removed pages done"
      contains: "removedPages"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift"
      provides: "The single shared recover-once-and-log implementation, bracket supplied by caller context"
      contains: "recoveredBlanking"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadSeedRecoveryTests.swift"
      provides: "Seed-route failed-write recovery case and failed-rescan announced-basis case"
      contains: "func test"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift"
      via: "prepareWorkingSeed's post-removal exits call the same recovery implementation reconcileValidatedRecordAgainstPageFiles calls"
      pattern: "recoveredBlanking"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+WorkingManifestReconciliation.swift"
      via: "the verbatim return at scanSucceeded == false is survivable only because the caller now compensates and the announcement subtracts the removed pages"
      pattern: "scanSucceeded"
---

<objective>
Close verification gap 3 (SC2/SC3; review WR-03). 15-67 copied the validate route's classify-guard-remove-rescan-blank ORDERING into `authorizedReconciliationScan` (ExecutionSupport.swift:441-487) and dropped the compensation the validate route treats as mandatory (`recoveredBlanking`, PersistenceNormalize.swift:392-418, reached from all three post-removal exits at 304-337). The seed route has the same three exits and handles none: (1) a post-removal rescan reporting `scanSucceeded == false` makes `reconcileWorkingManifestAgainstPageFiles` return the manifest VERBATIM (WorkingManifestReconciliation.swift:210) over pages this pass just deleted; (2) the post-removal loop can refuse if the rescan's terms grew; (3) `storage.writeManifest` can throw out of `prepareWorkingSeed`, aborting the run with the deletion performed and the record unchanged — none logged. Independently, the same change re-sourced the announced basis (`scanSucceeded: reconciliationScan.scanSucceeded`, line 398), so the ONLY new false value comes from a rescan failing after a successful removal — and `inheritedPages`' pessimistic branch (657-658) then presumes done every claimed page INCLUDING the pages this pass deleted: over-reporting, the direction the announcement's own doc calls the defect.

Fix shape, per the gap's own missing[]: share the compensation (lift the helper — one implementation, as the blanking loop's own shared-implementation argument demands), sweep ALL THREE exits with dispositions in the doc, and make the removed pages structurally unable to be presumed done. The bracket law constrains the lift: `withdrawingCountedBasisMovement` composes as SIBLINGS ONLY — the seed route's recovery must run inside `prepareWorkingSeed`'s existing bracket without opening a second one, while the validate route keeps its own sibling bracket.

Purpose: destructive reconciliations compensate and leave a trail; the card never over-reports (SC2, SC3).
Output: one shared recovery implementation, three swept exits, a never-over-reporting announcement, and regressions on the two exits the gap names.
</objective>

## Artifacts this phase produces

- Revised `authorizedReconciliationScan`: returns the post-removal scan TOGETHER with the removed page set (a small module-internal result struct, e.g. `AuthorizedReconciliation` with `scan: PageFileScan` and `removedPages: Set<Int>`; empty `removedPages` on every refusal/early-return path). The `_ =` discard of `removeRefutedPageFiles`' answer is replaced by deriving `removedPages = refutedPages.subtracting(unremovedPages)` — the validate route's own expression.
- Refactored recovery: the recover-once-and-log core is extracted so BOTH routes reach one implementation. The core (fresh scan + `reconcileWorkingManifestAgainstPageFiles` + claims-check + the error log with masked gid and removed indices) opens NO bracket of its own; the validate route wraps it in its existing sibling bracket (today's `blankingPass` shape), and the seed route calls it inside `prepareWorkingSeed`'s already-open bracket. Exact factoring is the executor's derivation; the acceptance criteria pin the properties (one log site, one loop, zero new blanking paths, zero bracket nesting).
- Revised `prepareWorkingSeed`: after the blanking loop, if the reconciled manifest still claims any removed page (exits 1 and 2) or the write threw with removals performed (exit 3), the shared recovery runs once; its doc enumerates all three exits with dispositions so the family cannot be handled piecemeal again.
- Revised `WorkingSeed`: carries `removedPages`; `inheritedPages` (ExecutionSupport.swift:647-662) subtracts them in BOTH branches — a page whose file this pass removed is positively absent by construction and may never be presumed done, whatever the rescan's health. The `scanSucceeded` announcement source is thereby decided deliberately and documented: it stays sourced from the reconciliation scan (honest about enumerating), and the subtraction is what makes a false value unable to over-report.
- New test file `AppPackage/Tests/DownloadsFeatureTests/DownloadSeedRecoveryTests.swift`, suite `DownloadSeedRecoveryTests`, with a count-gated failing `FileManager` subclass fixture (precedent: `LinkFailingFileManager`, DownloadStoreRepairTests.swift:90) whose doc names exactly which call fails and on which invocation.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-67-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the un-compensated write and the over-reporting announcement</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadSeedRecoveryTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadValidationReconciliationTests.swift` — `testATransientPostRemovalRescanFailureRecoversSoTheDurableBlankStillLands` (399) and `testAnUnwritableManifestKeepsTheEntryAndTheNextValidateConvergesTheRecord` (489): the validate-route cases these two mirror, including the immutable-manifest staging at 475-516 and its cleanup-ordering note.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadStoreRepairTests.swift` — `LinkFailingFileManager` (90): the FileManager-subclass injection precedent the failing fixture follows.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeed` (309-401) and `authorizedReconciliationScan` (441-487): which storage calls run in which order, so the count-gated double fails exactly the post-removal call; `inheritedPages` (647-662): the presumed-done arithmetic the basis case pins.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadValidationRejectionArmTests.swift` — the mixed 2-page/3-page staging shapes (below-threshold removal-authorizing populations) to reuse.
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift` — how announced run bases reach the spy's pushed frames, for the basis assertion's observable.
  </read_first>
  <behavior>
    - Case A (exit 3, mirroring the validate route's unwritable-manifest case): stage a below-threshold removal-authorizing repair shape (refuted zero-byte pages under a complete-claiming manifest, count under the wholesale guard), make the manifest unwritable AFTER staging (immutable flag / posixPermissions, per the 489 precedent, with the same cleanup-before-teardown ordering), drive the repair through the production path. Pre-fix: the write throws out of `prepareWorkingSeed` with files already deleted and the record still claiming them — assert post-run (and across a rebuilt coordinator) that record and disk agree for the removed pages, which is RED pre-fix. Post-fix: the recovery re-attempts once; with the write still failing, the run fails as a recoverable failed download AND the divergence is compensated on the next converging pass — assert the post-fix state the executor derives from the shared helper's contract (the removed pages are positively absent, so any answering retry blanks exactly them), plus that the run's failure does not settle a lying record.
    - Case B (exit 1, the announced basis): build the coordinator's store over a count-gated failing FileManager whose `contentsOfDirectory` succeeds for the classification scan and fails from the post-removal rescan onward — the double's doc names the failing call (`contentsOfDirectory(at:includingPropertiesForKeys:options:)` or the exact overload `pageFileScan` reaches) and the invocation index, satisfying the contract-faithfulness rule. Drive the same removal-authorizing repair shape with a live session spy. Pre-fix: `scanSucceeded` flips false, `inheritedPages` presumes ALL claimed pages done — including the deleted ones — and the announced basis exceeds the honest sum (pin the exact pushed frame). Post-fix: the announced basis never exceeds the honest one (claimed pages minus the pages this pass removed, at most). Assert piecewise from both sides: a companion assertion (or reference to the existing 15-67 mixed-shape cases) pins the healthy-rescan regime unchanged, so the fix is pinned from both regimes.
  </behavior>
  <action>
Create `DownloadSeedRecoveryTests.swift` with its own fixture (add nothing to DownloadFeatureTestHelpers.swift — 992 of 1000 lines). Build the failing FileManager subclass count-gated and single-purpose, with a doc naming the exact production call it fails and why that is the only window that can stage exit 1 (the scan and rescan are synchronous inside one preparation — no external mutation can land between them). For Case A reuse the immutable-manifest staging idiom including its teardown-ordering comment. For Case B pin the pushed frames through the session spy exactly as the run-proof suite does — production-issued pushes only, never fixture-issued. Bank every pre-fix observation (the thrown error, the lying claims, the over-reported frame values) for the summary.
  </action>
  <verify>
    <automated>! xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSeedRecoveryTests</automated>
  </verify>
  <acceptance_criteria>
    - Case A fails pre-fix with the record claiming pages whose files the pass deleted, observed across a rebuilt coordinator.
    - Case B fails pre-fix with a pushed basis exceeding the honest sum; the healthy-rescan regime is pinned green on the other side of the discontinuity.
    - The failing double's doc names the exact call and invocation it fails; no test issues a push or a scan the production path would not issue.
    - No addition to DownloadFeatureTestHelpers.swift.
  </acceptance_criteria>
  <done>Both gap-named exits are deterministic RED observations, pinned from both regimes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — shared compensation, swept exits, never-over-reporting announcement</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift</files>
  <read_first>
    - `AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift` — `reconcileValidatedRecordAgainstPageFiles` (241-338), `blankingPass` (360-376), `recoveredBlanking` (392-418): the implementation being lifted, its bracket placement, its claims-check, and its error-log wording.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `prepareWorkingSeed` (309-401: the enclosing bracket at 314, the announcement-evidence comment at 391-397), `authorizedReconciliationScan` (441-487), `inheritedPages` (647-662), and the sibling-composition doc at 260-281 whose law the factoring must obey.
    - `AppPackage/Sources/DownloadClient/DownloadClient+WorkingManifestReconciliation.swift` — the verbatim-return exit (210) and the loop's refusal lines (234-237): exits 1 and 2's mechanics.
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — `withdrawingCountedBasisMovement`'s caller inventory context (837-856): why a nested bracket double-withdraws.
  </read_first>
  <behavior>
    - `authorizedReconciliationScan` answers with scan + removedPages; every refusal path answers empty removals. `prepareWorkingSeed` compensates: exits 1 and 2 (reconciled manifest still claims a removed page) and exit 3 (write threw with removals performed) all reach the ONE shared recover-once implementation; a second failure logs at `error` with masked gid + sorted removed indices — the identical wording family `recoveredBlanking` uses today, one implementation, one log site.
    - Bracket law holds: the shared core opens no bracket; the validate route's wrapper keeps its sibling bracket; the seed route's calls sit inside `prepareWorkingSeed`'s bracket. The doc on the shared core states which caller supplies the bracket and why nesting would double-withdraw (cite the SIBLINGS-only rule; do not restate its arithmetic).
    - `WorkingSeed.removedPages` flows to `inheritedPages`, which subtracts it in both branches; the announcement doc states the decided property: a rescan failure keeps the record's claims standing (non-answer discipline, unchanged) EXCEPT for pages this pass itself removed, whose absence is this pass's own positive knowledge — so the basis can under-report toward re-fetching but can never over-report.
    - `pendingPageIndices`/re-fetch behavior is unchanged: exits 1 and 2 remain self-healing on the run route, and the doc's exit table says so (disposition: compensate + proceed), while exit 3's disposition is compensate + propagate the failure.
  </behavior>
  <action>
Implement per behavior. Factor the recovery so `recoveredBlanking`'s claims-check + retry + error log live once (executor chooses the exact split between PersistenceNormalize and a shared location; the constraint set is: one log call site for this message family, one blanking loop, no new blanking predicate, no bracket opened inside the core). Change `authorizedReconciliationScan`'s return type to the result struct, deriving removedPages from `removeRefutedPageFiles`' unremoved answer rather than discarding it — update its doc's "deliberately discarded" paragraph (466-468) accordingly, keeping the rescan-over-derivation rule for the SCAN itself. Extend `prepareWorkingSeed`: build the seed as today, then apply the compensation rule; add `removedPages` to `WorkingSeed` and thread it into the returned seed; rewrite the three-exit disposition table into `authorizedReconciliationScan`'s (or `prepareWorkingSeed`'s) doc, naming each exit, its mechanism line, its disposition, and its test. Amend `inheritedPages` with the subtraction and re-derive its authority-ordered doc: removal-by-this-pass now ranks as positive knowledge above the failed-listing non-answer. Confirm and state in the summary that the validate route's observable behavior is unchanged (its suite is the proof — no expectation there may need edits; if any does, the factoring is wrong).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSeedRecoveryTests -only-testing:DownloadsFeatureTests/DownloadValidationReconciliationTests -only-testing:DownloadsFeatureTests/DownloadValidationRejectionArmTests -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 cases green; the validate-route reconciliation and rejection-arm suites green WITHOUT expectation edits; repair-seed suite green.
    - `rg -n 'Validation removed refuted page files|removed refuted page files the record still claims' AppPackage/Sources/DownloadClient` → exactly one log call site (the shared implementation).
    - `rg -c 'withdrawingCountedBasisMovement' AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift` shows the validate route still supplies exactly one bracket wrapper; no bracket call is added inside `prepareWorkingSeed`'s body or the shared core.
    - `rg -n 'removedPages' AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` shows the result struct, the WorkingSeed member, and the inheritedPages subtraction.
    - The three-exit disposition table exists in one doc block naming exits 1, 2, and 3 with their dispositions.
    - The summary records: the factoring shape chosen, the scanSucceeded decision and its derivation, and confirmation that no validate-route expectation moved.
  </acceptance_criteria>
  <done>One recovery rule, three swept exits, a trail on every failure, and an announcement that cannot over-report — with the bracket law intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authorized file removal → durable manifest write | Between the removal and the write, every failure path must compensate or leave a trail; silence is the threat. |
| Run-measured progress → system progress card | The announced basis feeds system UI; over-reporting masks stalls and invites expiration-driven pauses (D-11). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-72-01 | Tampering | post-removal exits leaving record claiming deleted pages | high | mitigate | Shared recover-once on all three exits; error log with masked gid + indices when the retry fails; pinned by Case A across relaunch. |
| T-15-72-02 | Repudiation | destroyed files with no trail | medium | mitigate | Single log site inherited from the validate route's implementation; grep-gated to one call site. |
| T-15-72-03 | Information Disclosure | log content | low | accept | Masked-gid identity pattern and numeric indices only — the module's established privacy pattern, unchanged. |
| T-15-72-04 | Denial of Service | over-reported basis → stalled-looking card → expiration pause-all | high | mitigate | inheritedPages subtracts this pass's removals in both branches; Case B pins the ceiling from both regimes. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadSeedRecoveryTests -only-testing:DownloadsFeatureTests/DownloadValidationReconciliationTests -only-testing:DownloadsFeatureTests/DownloadValidationRejectionArmTests -only-testing:DownloadsFeatureTests/DownloadCoordinatorRepairSeedTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1572DerivedData build`

Device note for the verifier: 15-VERIFICATION.md asks that UAT test 2 (system card fraction + cancel parity) be re-run on device after gap 3 closes, since this sits on the announced-basis path. Record that in the summary as an outstanding device item — do not attempt it from the simulator.
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels plus the two UAT-FU anchors, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC2 | Card reflects real progress | 15-72 | COVERED | Announced basis structurally cannot over-report. |
| GOAL | SC3 | No lost work; manifest-SSOT | 15-72 | COVERED | Every destructive exit compensates or leaves a trail. |
| VERIFICATION | gap 3 bullet 1 | Share the compensation, lift the helper | 15-72 | COVERED | One implementation, both routes, bracket law preserved. |
| VERIFICATION | gap 3 bullet 2 | Sweep all three exits with dispositions | 15-72 | COVERED | Exit table in the doc; tests on exits 1 and 3; exit 2's disposition recorded with its self-healing derivation. |
| VERIFICATION | gap 3 bullet 3 | scanSucceeded source decided deliberately | 15-72 | COVERED | Removed-pages subtraction decided, documented, pinned. |
| VERIFICATION | gap 3 bullet 4 | The two named cases | 15-72 | COVERED | Case A (failed write) and Case B (failed rescan). |
| REVIEW | WR-03 | Same defect by review ID | 15-72 | COVERED | — |

<success_criteria>
- A failed write after an authorized removal recovers once or logs; record/disk agreement is restored on the next answering pass, observed across relaunch.
- A failed post-removal rescan cannot raise the announced basis above the honest one; the healthy regime is unchanged.
- One recovery implementation serves both routes; no bracket nests.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-72-SUMMARY.md` when done.
</output>
