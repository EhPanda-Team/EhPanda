---
phase: 15-continued-background-downloads
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/DownloadClient/DownloadClient.swift
  - AppPackage/Package.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift
autonomous: true
requirements: [SC2, SC4]

must_haves:
  truths:
    - "The card's title and subtitle are localized keys whose only inputs are integer counts; no gallery title, tag, category or identifier is reachable from the string builder."
    - "Every new localized key carries all six catalog locales, and every numeric argument is a named substitution rather than a bare numeric format specifier in the outer value."
    - "The unimplemented test value reports an issue when any of its three endpoints is called, so an unexpected call fails a test loudly rather than silently succeeding."
    - "The download coordinator stores the session client as an injected dependency defaulting to the no-op value, so every existing test-constructed coordinator still compiles unchanged."
    - statement: "The system card renders and, where necessary, truncates the localized title and subtitle under system policy; the app declares no length limit of its own on either string."
      verification: backstop
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings"
      provides: "Neutral card title and count subtitle in six locales"
      contains: "continued_session.subtitle"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift"
      provides: "Session seam contract coverage"
      contains: "DownloadContinuedSessionTests"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift"
      provides: "Mutex-backed session client spy with a controllable event continuation"
      contains: "BackgroundProcessingClientSpy"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient.swift"
      to: "AppPackage/Sources/BackgroundProcessingClient/BackgroundProcessingClient.swift"
      via: "the live composition root injects the live session client straight into the coordinator"
      pattern: "backgroundProcessingClient"
  prohibitions:
    - "Must NOT place a gallery title, tag, category, artist, or any other content-identifying text into the system card's title or subtitle. The card renders in system UI, outside the reach of the app's privacy mask and outside the App Switcher snapshot protection."
---

<objective>
Add the neutral card strings, inject the session client into the download coordinator, and
stand up the test double the session suites will drive.

Purpose: D-09 fixes the card's content to counts only, and the repo's localized-format rules
fix how those counts must be declared. D-05 fixes the injection shape: a stored dependency on
the coordinator, like the client it replaces, not a reducer-resolved one. The spy and the first
suite land here so every later behavior plan has automated verification available the moment
it writes code.

Output: two localized keys in six locales, a coordinator that holds the session client and its
session state, and a session test suite seeded with the seam's contract coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-CONTEXT.md
@.planning/phases/15-continued-background-downloads/15-RESEARCH.md
@.planning/phases/15-continued-background-downloads/15-PATTERNS.md
@.planning/phases/15-continued-background-downloads/15-VALIDATION.md
@CLAUDE.md
@.swiftlint.yml
</context>

<assumption_delta_decision>
**Primary noun:** the continued-processing session, as the app's sole background-execution
mechanism. **Decision: promote** (full record in 15-01). The injected client is the single
background-execution seam on the coordinator; no second client is added beside it.
</assumption_delta_decision>

<!-- planner-discipline-allow: backgroundTaskClient -->
<!-- planner-discipline-allow: BackgroundTaskClientSpy -->

<tasks>

<task type="auto">
  <name>Task 1: Add the neutral card title and count subtitle to the module catalog</name>
  <files>AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings (whole file — six existing keys, all six locales each, `extractionState` manual)
    - AppPackage/Sources/DownloadsFeature/Resources/Localizable.xcstrings (the `downloaded` key — the repo's known-good named-substitution example)
    - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift (lines 1-15 — how a module-local key is consumed, with no shared-resources prefix)
    - CLAUDE.md (the labeled localized-format-argument rule and the every-locale rule)
    - .planning/phases/15-continued-background-downloads/15-PATTERNS.md (§ DownloadClient/Resources/Localizable.xcstrings)
  </read_first>
  <action>
Add exactly two keys to `AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings`,
both with `extractionState` set to `manual` and both carrying all six locales the catalog
supports: `en`, `de`, `ja`, `ko`, `zh-Hans`, `zh-Hant`.

Key `continued_session.title` is a plain per-locale `stringUnit` with no substitution, in the
same shape as the existing `download_store.manifest_missing` key. English value: `Downloading
galleries`. German: `Galerien werden geladen`. Japanese: `ギャラリーをダウンロード中`. Korean:
`갤러리 다운로드 중`. Simplified Chinese: `正在下载图库`. Traditional Chinese: `正在下載圖庫`.
This is the whole of the card's title, and it is deliberately content-free: the card renders
in system UI, outside the privacy mask, so it must never name what is being downloaded.

Key `continued_session.subtitle` carries three named substitutions — `completed`, `total`,
`galleries` — with `argNum` 1, 2 and 3 respectively and `formatSpecifier` `lld` on each. The
outer value per locale references them only through the named-substitution form, never as a
bare numeric specifier. English outer value: `%#@completed@ / %#@total@ · %#@galleries@`.
German outer value is identical in structure. For the CJK locales the outer value is
`%#@completed@ / %#@total@ · %#@galleries@` as well; only the substitution variation values
differ per language.

Substitution plural categories, which must be internally coherent: `completed` is `other`-only
in all six locales, with variation value `%arg`. `total` and `galleries` carry both `one` and
`other` in `en` and in `de` (the two sets must be equal to each other), and are `other`-only in
`ja`, `ko`, `zh-Hans` and `zh-Hant`.

Variation values: English `total` — `one` is `%arg page`, `other` is `%arg pages`; English
`galleries` — `one` is `%arg gallery`, `other` is `%arg galleries`. German `total` — `one` is
`%arg Seite`, `other` is `%arg Seiten`; German `galleries` — `one` is `%arg Galerie`, `other`
is `%arg Galerien`. Japanese `total` — `other` is `%argページ`; `galleries` — `other` is
`%arg件のギャラリー`. Korean `total` — `other` is `%arg페이지`; `galleries` — `other` is
`%arg개 갤러리`. Simplified Chinese `total` — `other` is `%arg 页`; `galleries` — `other` is
`%arg 个图库`. Traditional Chinese `total` — `other` is `%arg 頁`; `galleries` — `other` is
`%arg 個圖庫`. Every variation `stringUnit` uses state `translated`.

The generated symbols this produces are `.continuedSessionTitle` and
`.continuedSessionSubtitle(completed:total:galleries:)` — labeled integer parameters, as the
repo requires. They are module-local, so callers use them without a shared-resources prefix
and no hand-written symbol file needs editing.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone 17' -quiet</automated>
  </verify>
  <acceptance_criteria>
    - Write the structural catalog check as a throwaway script under the session scratch directory and run it with `python3`. It loads `AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings` and asserts, for both new keys: the `localizations` key set equals `{en, de, ja, ko, zh-Hans, zh-Hant}`; for the subtitle key, every locale's `substitutions` key set equals `{completed, total, galleries}` with `formatSpecifier` `lld` on each; no locale's outer `stringUnit.value` contains `%lld` or `%d`; the `plural` category sets of `total` and of `galleries` are equal between `en` and `de`; and those sets are exactly `{other}` in `ja`, `ko`, `zh-Hans` and `zh-Hant`. The script must exit `0`.
    - `python3 -c "import json;print(len(json.load(open('AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings'))['strings']))"` outputs `8`.
    - `grep -c 'continued_session.title' AppPackage/Sources/DownloadClient/Resources/Localizable.xcstrings` outputs a value of at least `1`.
    - The clean app-scheme build succeeds, proving the catalog parses and the symbols generate.
  </acceptance_criteria>
  <done>Two localized keys exist in six locales; every numeric argument is a named substitution producing a labeled integer parameter; the card's vocabulary is counts and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Inject the session client and its state into the download coordinator</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/DownloadClient/DownloadClient.swift, AppPackage/Package.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift (lines 1-20 imports, lines 290-380 stored dependencies, mutable state, initializer)
    - AppPackage/Sources/DownloadClient/DownloadClient.swift (lines 40-95 — the live composition root)
    - AppPackage/Package.swift (the `downloadClient` target dependency list)
    - .planning/phases/15-continued-background-downloads/15-PATTERNS.md (§ DownloadClient+Manager.swift; § Shared Patterns — directly-injected client)
    - .planning/phases/15-continued-background-downloads/15-RESEARCH.md (Pattern 6 § D-05 injection; § Open Questions Q2)
  </read_first>
  <action>
In `AppPackage/Package.swift`, add `.module(.backgroundProcessingClient)` to the
`downloadClient` target's dependency list, keeping the list alphabetically consistent with its
existing ordering.

In `DownloadClient+Manager.swift`, add `import BackgroundProcessingClient` in sorted position
and declare a stored dependency `public let backgroundProcessingClient: BackgroundProcessingClient`
in the same block as the other injected collaborators. Add an initializer parameter
`backgroundProcessingClient: BackgroundProcessingClient = .noop` positioned where the deleted
assertion client used to sit, and assign it in the initializer body. The `.noop` default is
load-bearing: every existing test-constructed coordinator must keep compiling untouched.

Give the property a doc comment that records the deliberate divergence from the client it
replaces: this one keeps its dependency-key registration and its `DependencyValues` accessor
so an unimplemented test value exists, yet it is still injected directly here rather than
resolved through `DependencyValues`, because download-start calls flow synchronously from user
actions into this actor and this is the only place that knows real queue progress.

Add the session state next to the other mutable coordinator state:
- `public var hasLiveContinuedSession = false`
- `public var continuedSessionTask: Task<Void, Never>?`
- `public var lastPushedCompletedPageCount = 0`

Document `hasLiveContinuedSession` explicitly: it is set synchronously, before the first
suspension point of the start path, and is never rolled back — which is what makes a single
flag sufficient here where the deleted assertion needed a two-state guard. That guard matters
because a second registration of the same session identifier terminates the app, and two live
sessions would put two cards on screen.

In `DownloadClient.swift`, pass `backgroundProcessingClient: .live` in the `DownloadCoordinator`
construction inside `live(rootURL:urlSession:fileManager:)`, in the argument position matching
the initializer.

Add no behavior in this task. Nothing calls the client yet; the wiring exists so the next plan
can be a pure behavior change.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:DownloadsFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'public let backgroundProcessingClient: BackgroundProcessingClient' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `1`.
    - `grep -c 'backgroundProcessingClient: BackgroundProcessingClient = .noop' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs `1`.
    - `grep -c 'hasLiveContinuedSession' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` outputs a value of at least `1`.
    - `grep -c 'backgroundProcessingClient: .live' AppPackage/Sources/DownloadClient/DownloadClient.swift` outputs `1`.
    - `swift package dump-package --package-path AppPackage` exits `0` and its `downloadClient` target lists the client module.
    - The quick test command exits `0` with no test-source changes required, proving the `.noop` default kept every existing coordinator construction compiling.
    - A clean app-scheme build has zero lint violations.
  </acceptance_criteria>
  <done>The coordinator holds the session client and its session state; the live composition root injects the live value; no behavior has changed yet.</done>
</task>

<task type="auto">
  <name>Task 3: Add the session client spy and the seam contract suite</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift</files>
  <read_first>
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift (whole file — the mutex-backed sendable helper types and the free-function sleep helper)
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift (the relocated blocking-coordinator fixture from plan 15-02)
    - AppPackage/Sources/BackgroundProcessingClient/BackgroundProcessingClient.swift (the three endpoint signatures)
    - .planning/phases/15-continued-background-downloads/15-PATTERNS.md (§ DownloadContinuedSessionTests.swift + spy delta)
    - .planning/phases/15-continued-background-downloads/15-VALIDATION.md (§ Wave 0 Requirements; § Per-Task Verification Map)
    - .swiftlint.yml (the banned-lock rule: use the standard-library mutex, never the Foundation lock)
  </read_first>
  <action>
Add `BackgroundProcessingClientSpy` to `DownloadFeatureTestSupportTypes.swift`, modeled on the
mutex-backed spy that plan 15-02 deleted: a `final class ... : Sendable` whose whole state
lives in a private struct behind a single `Mutex` from the `Synchronization` module (the
Foundation lock is banned at error severity).

State it records: the number of `start` calls; the title and subtitle of every `start` call;
every `updateProgress` call as an ordered array of a small named-field record holding
`completedUnitCount`, `totalUnitCount` and `subtitle` (a named record, not a tuple —
unlabeled tuple elements are banned at error severity); the number of `finish` calls and the
success flag of each; and the live event continuation.

Expose read-only computed properties for each recorded collection, plus `emit(_:)` which
yields a `BackgroundProcessingEvent` on the stored continuation, and `expire()` which yields
the expired event and then finishes the stream — mirroring the self-finishing contract of the
real client so a consuming task falls out of its loop without external cancellation.

Expose a computed `client: BackgroundProcessingClient` that vends a struct literal: `start`
records the strings, creates a stream with `AsyncStream.makeStream(of:)`, stores the
continuation, and returns the stream; `updateProgress` appends a record; `finish` records the
flag and finishes the stream.

In `DownloadFeatureTestHelpers.swift`, add a `backgroundProcessingClient: BackgroundProcessingClient = .noop`
parameter to the relocated `makeBlockingCoordinator` and forward it into the coordinator
construction, so the session suites can drive the fixture through the spy while every existing
caller keeps working. Add the `BackgroundProcessingClient` import in sorted position.

Create `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift` as a
`@Suite struct DownloadContinuedSessionTests: DownloadFeatureTestCase` and seed it with the
seam's contract coverage, which does not depend on any coordinator behavior:
- A case asserting the unimplemented test value fails loudly: construct
  `BackgroundProcessingClient()` directly, wrap a call to each of the three endpoints in
  `withKnownIssue`, and assert an issue was reported for each. This is SC4's literal
  requirement — an unexpected call must not silently succeed.
- A case asserting the no-op value's `start` returns a stream that finishes immediately with
  no elements, and that its other two endpoints complete without effect.
- A case asserting the spy itself behaves like the real contract: `start` records the strings
  and returns a live stream; `expire()` delivers exactly one expired event and then finishes,
  so a `for await` loop over the stream terminates on its own.

Keep imports sorted. Do not annotate the suite type with a main-actor attribute; annotate only
members that genuinely require it.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'final class BackgroundProcessingClientSpy' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift` outputs `1`.
    - `grep -c 'NSLock' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestSupportTypes.swift` outputs `0`.
    - `grep -c 'withKnownIssue' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift` outputs a value of at least `1`.
    - `grep -c 'backgroundProcessingClient' AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` outputs a value of at least `1`.
    - The targeted test command exits `0` with three or more cases executed.
    - The full `DownloadsFeatureTests` target is green and a clean app-scheme build has zero lint violations.
  </acceptance_criteria>
  <done>A mutex-backed spy can stand in for the session client and drive events on demand; the seam's own contract — unimplemented test value, inert no-op value, self-finishing stream — is under automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app process → system progress card | The card renders outside the app process, outside the privacy mask and outside App Switcher snapshot protection. Everything crossing this boundary is user-visible to anyone who can see the device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | card title and subtitle strings (`continued_session.title`, `continued_session.subtitle`) | high | mitigate | The two keys accept only integer counts; no string argument and therefore no path for a gallery title, tag or identifier to reach the card. Enforced structurally by the catalog shape (three `lld` substitutions, no `%@` argument) and asserted by the catalog check in Task 1; behavioral assertion on the exact strings pushed lands in plan 15-06. |
| T-15-06 | Information Disclosure | card text persisting in system UI after backgrounding | low | accept | The card carries counts only, so a shoulder-surfer learns that a download is in progress and how many pages remain — not what is being downloaded. The user initiated the download, and the card is the API's mandated affordance with no opt-out. |
| T-15-04 | Denial of Service | manifest-derived page counts feeding the subtitle | low | mitigate | Counts reach the subtitle through the existing clamped progress model in a later plan; the substitution format itself accepts any `Int` without crashing. |
| T-15-SC | Tampering | package-manager installs | low | accept | This phase installs no packages; RESEARCH.md § Package Legitimacy Audit records zero entries. |
</threat_model>

## Artifacts this phase produces

Newly created symbols, files, and configuration keys for the whole of Phase 15. The
source-grounding pass excludes these from drift verification.

**New files**
- `AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift`
- `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift`
- `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift`
- `AppPackage/Tests/DownloadsFeatureTests/BackgroundExecutionInvariantTests.swift`

**Renamed files**
- `AppPackage/Sources/DownloadClient/DownloadClient+BackgroundAssertion.swift` → `DownloadClient+PendingWork.swift`
- `AppPackage/Tests/DownloadsFeatureTests/DownloadBackgroundProcessingTests.swift` → `DownloadPendingWorkTests.swift`

**New types**
- `BackgroundProcessingEvent` (enum: `granted`, `expired`, `unavailable`)
- `ContinuedProcessingSession` (`@MainActor final class`, `shared`)
- `ContinuedSessionProgress` (struct: `progress`, `galleryCount`)
- `BackgroundProcessingClientSpy` (test double)

**New client endpoints**
- `BackgroundProcessingClient.start(_:_:)`
- `BackgroundProcessingClient.updateProgress(_:_:_:)`
- `BackgroundProcessingClient.finish(_:)`

**New coordinator members**
- `DownloadCoordinator.backgroundProcessingClient`
- `DownloadCoordinator.hasLiveContinuedSession`
- `DownloadCoordinator.continuedSessionTask`
- `DownloadCoordinator.lastPushedCompletedPageCount`
- `DownloadCoordinator.ensureContinuedSession()`
- `DownloadCoordinator.reconcileContinuedSession()`
- `DownloadCoordinator.pushContinuedSessionProgress()`
- `DownloadCoordinator.schedulableProgress()`
- `DownloadCoordinator.continuedSessionSubtitle(for:)`
- `DownloadCoordinator.handleContinuedSessionEvent(_:)`
- `DownloadCoordinator.pauseAllSchedulable()`
- `DownloadCoordinator.markContinuedSessionEnded()`
- `DownloadCoordinator.testingHasContinuedSession()`

**New localized keys and generated symbols**
- `continued_session.title` → `.continuedSessionTitle`
- `continued_session.subtitle` → `.continuedSessionSubtitle(completed:total:galleries:)`

**New configuration**
- `App/Info.plist` permitted identifier `$(PRODUCT_BUNDLE_IDENTIFIER).continued.*`

**Visibility change**
- `DownloadCoordinator.isSchedulableDownload(_:)` private → internal

**Relocated test helpers**
- `BlockingCoordinatorContext`, `makeBlockingCoordinator`, `waitUntil` moved into `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift`

<verification>
- The catalog structure check passes for both new keys across all six locales.
- The coordinator compiles with the injected client and every pre-existing construction is unchanged.
- The seam contract suite is green.
- Full `FeatureTests` plan green; clean app-scheme build with zero lint violations.
</verification>

<success_criteria>
- Card strings exist, are localized six ways, and can express nothing but counts.
- The coordinator owns an injected session client defaulting to the inert value.
- A controllable test double exists for every later behavior assertion.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-04-SUMMARY.md` when done.
</output>
