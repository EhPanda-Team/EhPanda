---
phase: 15-continued-background-downloads
plan: 37
type: execute
wave: 37
depends_on: ["15-36"] # File overlap on DownloadClient+Scheduling.swift, DownloadClient+Manager.swift, DownloadClient+Testing.swift, and the convergence suite; xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadOwnershipConvergenceTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionExpirationTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-18]
requirements: [SC2, SC4]

must_haves:
  truths:
    - "The session store establishes its request identity BEFORE handing the request over: pendingIdentifier and isAwaitingTask are set ahead of the submission call, so a launch delivered during submission adopts instead of being turned away and waited for forever — and the endSession paragraph that claimed every early-unavailable path runs before the identifier is set is rewritten to match the new ordering, including the throwing-submission arm's defensive request take-back (WR-08; the seam's whole purpose is to be driven by injected doubles, so the ordering must be correct for a synchronous launch even though the production main queue cannot deliver one mid-call)."
    - "The .superseded pause arm no longer starts a background session ensure it cannot justify: the ensure call is dropped, and the arm's comment states the observable safety argument — reaching .superseded requires either an advanced queue-intent generation (only a queue-mobilizing user action does that, and every such action already owns its ensure per D-07) or a live successor session (a non-nil mismatched session id) — covering both branches of the ownership predicate, not just the one the review sampled (WR-09)."
    - "The nil-vs-empty collapse in the cover-probe path carries a binding invariant instead of a silent assumption: the collapse site states that probe consumers may flatten a failed listing only while none acts irreversibly on the answer, and that a destructive consumer must use the surfaced-signal scan API — the exact property whose silent loss produced G-15-9 (IN-01)."
    - "The nine pieces of continued-session state are module-internal (SC4: one testable seam; a linking module can no longer read or depend on session internals), the suites reach the session task only through a testing forwarder in the DEBUG seam, and a grep proves zero production callers outside the module and zero direct test reads remain (IN-02)."
    - "No convergence case delivers a spy expiration into a deleted fixture after the case returned: the two defer orderings are restructured or the vestigial expirations dropped, decided per case from what its teardown actually needs (IN-03)."
    - "No raw error value is logged public anywhere in the background-processing module, and that is now enforced by a mechanism rather than a point-in-time audit: the submission-failure log carries the error's type public and its value private, and the log-privacy invariant suite scans the BackgroundProcessingClient module's sources under the same rules as the download client's (IN-04; D-09's neutral-card boundary extended to the module's log surface)."
  artifacts:
    - path: "AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift"
      provides: "Identity-before-submission ordering in start, rewritten endSession paragraph, type-public/value-private submission-failure log"
      contains: "pendingIdentifier"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift"
      provides: "The .superseded arm without the ensure call, with the generation-check safety argument written on it"
      contains: "ownsExpirationPause"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      provides: "The nine session-state declarations at internal access"
      contains: "hasLiveContinuedSession"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift"
      provides: "testingContinuedSessionTask() forwarder"
      contains: "testingContinuedSessionTask"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift"
      provides: "The scan extended over the BackgroundProcessingClient module with its header rationale updated"
      contains: "BackgroundProcessingClient"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift"
      to: "AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift"
      via: "the extended scan root makes the raw-error-public token a permanent suite failure in the module, so the IN-04 fix is mechanism-backed: run the extension BEFORE the log fix and the suite fails RED on the existing interpolation"
      pattern: "forbiddenInterpolations"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift"
      to: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionExpirationTests.swift"
      via: "the session-task forwarder is the one remaining route from the suites to the session task, completing the boundary G-15-11's closure drew for the mutators"
      pattern: "testingContinuedSessionTask"
  prohibitions:
    - statement: "Must NOT change any session-lifecycle behavior beyond the two named ones (identity-before-submission; the dropped superseded-arm ensure): every existing store and coordinator case passes, with assertions updated only where the WR-08 reorder changes the observable cancel bookkeeping."
      status: pending
      verification: automated
    - statement: "Must NOT weaken the privacy scan to admit the module: the extension adds no allowlist entry and no exemption; the module must pass the same rules the download client passes."
      status: pending
      verification: automated
    - statement: "Must NOT leave any of the six items undecided or half-adopted — nitpicks are blocking; each item lands exactly as dispositioned here."
      status: pending
      verification: manual
---

<objective>
Close G-15-18 (warning; six confirmed hygiene items, grouped because this project's bar treats
nitpicks as blocking and two of them are safety arguments stated in terms the code cannot
observe). All six verified in source at round 11:

- **WR-08** — `ContinuedProcessingSession.start` sets `pendingIdentifier`/`isAwaitingTask`
  AFTER `scheduling.submit`, so a launch delivered during submission is turned away by the
  adoption identity gate and then awaited forever. Unreachable in production (main-actor type,
  main-queue launches), but the seam exists to be driven by injected doubles.
- **WR-09** — the `.superseded` pause arm calls `ensureContinuedSession()` on a path reachable
  from the backgrounded expiration consumer, defended by a comment naming unobservable
  scheduler behavior. The observable bound is `ownsExpirationPause`'s generation check.
- **IN-01** — the cover-probe path collapses the surfaced enumeration failure with an
  empty-array fallback, correct only while no consumer acts irreversibly — the exact property
  whose silent loss produced G-15-9.
- **IN-02** — nine pieces of continued-session state are still `public var` after 15-28 moved
  the mutators internal, and suites read the session task directly, bypassing the seam.
- **IN-03** — two convergence cases register a spy-expiration defer before the fixture-removal
  defer, so LIFO delivers the expiration into a deleted directory, unawaited, after the case
  returned.
- **IN-04** — the submission-failure log writes a raw error value into the public log field in
  the one module the privacy invariant does not scan; accurate today only by point-in-time
  audit.

Purpose: the seam is faithful to synchronous doubles, every safety argument is stated in
observable terms, the session state boundary is complete, and the module's log privacy is
mechanism-enforced.
Output: all six items landed as dispositioned, with the privacy-scan extension proven RED
against the pre-fix log line, and a green full run.
</objective>

## Artifacts this plan produces

- **Reordered production code:** identity-before-submission in `ContinuedProcessingSession.start`
  (assignments moved ahead of the submission call; post-submission assignments removed).
- **Removed call:** `ensureContinuedSession()` gone from the `.superseded` arm of
  `pause(gid:expiration:)`, its comment rewritten around the generation check.
- **Access narrowing:** the nine session-state vars (`hasLiveContinuedSession`,
  `continuedSessionID`, `continuedClientSessionID`, `continuedSessionNeedsReconciliation`,
  `continuedSessionTask`, `lastPushedCompletedPageCount`, `retiredSessionPages`,
  `observedSchedulablePages`, `observedIncompleteSessionGIDs`) dropped to internal.
- **New DEBUG seam member:** `testingContinuedSessionTask() -> Task<Void, Never>?` in
  `DownloadClient+Testing.swift`; the four direct test reads (three in
  `DownloadContinuedSessionExpirationTests.swift`, one in
  `DownloadContinuedSessionInterleaveTests.swift`) switched to it.
- **Restructured teardown:** the two IN-03 defer orderings in
  `DownloadOwnershipConvergenceTests.swift`.
- **Changed log line:** the submission-failure log in `ContinuedProcessingSession.swift`
  carrying the error's type public and its value private.
- **Extended invariant scan:** `DownloadLogPrivacyInvariantTests` scanning
  `AppPackage/Sources/BackgroundProcessingClient` alongside the download client, header
  rationale updated, no allowlist additions.
- **Strengthened comment:** the binding probe-collapse invariant at
  `existingAssetFileURL(folderURL:prefix:)` in `DownloadStore.swift`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-28-SUMMARY.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coordinator side — drop the superseded-arm ensure, bind the probe-collapse invariant, complete the session-state boundary, and fix the teardown orderings</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift, AppPackage/Sources/DownloadClient/DownloadStore.swift, AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionExpirationTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadOwnershipConvergenceTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-18 entry WHOLE (each item's source trace, the two corrections to the review, and the decided remedies)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `pause(gid:expiration:)`'s `.superseded` arm (`:169-186` at planning time: the ensure call and the comment naming scheduler foreground validation) and `ownsExpirationPause` (`:266-273`: the generation check the rewritten comment stands on)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — the `.expired` handler ordering (teardown before pause-all, which is why the liveness guard does NOT stop the superseded-arm ensure — the round-11 correction the rewritten comment must reflect)
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — `existingAssetFileURL(folderURL:prefix:)` (`:400-407`: the collapse and its current comment) and `PageFileScan` (the surfaced-signal alternative the invariant names, including 15-33's unprobedPages)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the nine session-state declarations (`:407-495` region) and every other public member near them (drop access ONLY on the nine)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` — the seam's forwarder idiom and its carries-only-consumed rule
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionExpirationTests.swift` (`:156`, `:166`, `:342`) and `DownloadContinuedSessionInterleaveTests.swift` (`:36`) — the four direct session-task reads being switched
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadOwnershipConvergenceTests.swift` — both cases with the spy-expiration defer registered before the fixture-removal defer (`:31` and `:51` regions): what each case's teardown actually needs from the expiration, if anything
  </read_first>
  <action>
**WR-09 — drop the ensure; write the observable argument.** Remove the
`ensureContinuedSession()` call from the `.superseded` arm and rewrite the arm's comment on the
generation check: `.superseded` means `ownsExpirationPause` failed, which happens on exactly two
branches — the gid's queue-intent generation advanced (only a queue-mobilizing user action does
that, and every such action already ensures its own session per D-07), or a live successor
session exists (a non-nil, mismatched session id — minted only by a qualifying tap that owned
its own ensure). Both branches already own their ensure, so none belongs here; and the dropped
call could actually run (the expired handler tears down BEFORE pausing, so the liveness guard is
false — the round-11 correction), minting a dead identifier from a background context. The
convergence half of the arm (notify + schedule) stays byte-identical.

**IN-01 — bind the collapse invariant.** Rewrite the comment at
`existingAssetFileURL(folderURL:prefix:)`: a probe consumer may collapse a failed listing into
an empty answer only while NONE of its consumers acts irreversibly on that answer; the moment
one does, it must consume the surfaced-signal scan (`PageFileScan`: `scanSucceeded` and
`unprobedPages`) instead — G-15-9 is the recorded cost of losing this property silently. Name
the current consumers (the cover and page-file lookups) as the audited-safe set.

**IN-02 — complete the boundary.** Drop the `public` keyword from each of the nine session-state
declarations in `DownloadClient+Manager.swift` (their doc comments stay). Add
`testingContinuedSessionTask() -> Task<Void, Never>?` to the DEBUG seam (doc names its four
consumer sites per the carries-only-consumed rule) and switch the four direct reads in the two
suites to it. Then prove the boundary: grep each of the nine names over `App/`,
`ShareExtension/`, and every `AppPackage/Sources` module other than `DownloadClient` (zero
hits), and grep the session-task property name over `AppPackage/Tests` filtering out the
testing-forwarder spelling (zero hits) — paste both outputs in the summary, labeled verified
exhaustive.

**IN-03 — fix the teardown orderings.** For each of the two convergence cases: determine from
its assertions whether anything consumes the spy expiration. If nothing does, delete the
vestigial expiration defer. If the case needs the expiration to release spy or session state
before the fixture goes, restructure to explicit awaited teardown IN the case body ahead of the
removal (a defer cannot await, so the ordering cannot be fixed by reordering defers alone where
awaiting the session task matters). Record the per-case decision and its reason in the summary.

**Run targeted.** Single invocation over the expiration, interleave, and convergence suites;
every case green, with the four switched reads asserting through the forwarder.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionExpirationTests -only-testing:DownloadsFeatureTests/DownloadContinuedSessionInterleaveTests -only-testing:DownloadsFeatureTests/DownloadOwnershipConvergenceTests</automated>
  </verify>
  <acceptance_criteria>
    - The superseded arm no longer ensures: the extracted arm shows notify + schedule only, and its rewritten comment names both ownership-predicate branches and the teardown-before-pause correction (quoted in the summary).
    - The collapse invariant is bound: the extracted comment at the cover-probe collapse names the destructive-consumer rule, the surfaced-signal alternative, and G-15-9 (quoted).
    - The boundary is complete: the summary pastes the nine-name external grep (zero hits) and the tests-side session-task grep filtered of the forwarder spelling (zero hits); `grep -c 'testingContinuedSessionTask' AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` outputs `1`.
    - The teardown decisions are recorded per case with reasons, and no case delivers a spy expiration after its fixture removal (stated from source inspection, quoted orderings).
    - The targeted run exits `0` in a single invocation.
  </acceptance_criteria>
  <done>The pause arm's safety argument is observable and the unjustifiable ensure is gone, the probe collapse carries its binding rule, the session-state boundary is closed and grep-proven, and no teardown outlives its fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client side — identity before submission, the type-public error log, and the mechanism-backed privacy scan, RED first</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift, AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift</files>
  <read_first>
    - `AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift` — `start` (`:77-152`: the three early-unavailable arms, the submission call, and the post-submission identity assignments being moved), `adopt` (`:207-228`: the identity gate a mid-submission launch must now pass), `endSession` (`:250-272`) and its paragraph at `:247-249` claiming every early arm precedes the identifier (the paragraph being rewritten), and the submission-failure log at `:144`
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadLogPrivacyInvariantTests.swift` — the scan roots (`:28-30`), the fragment-assembled forbidden tokens (`:34-54`), the header rationale (`:4-11`, whose out-of-scope audit sentence for this module dies with this fix), the known-member require, and the per-file masked-count inventory (which gains no entry — the module has no hash-masked interpolation)
    - `AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift` — every case touching submissions, cancellation bookkeeping, or the awaiting state (the reorder must not silently change what an existing case asserts; if one asserts the old ordering's observable, update it deliberately and say so)
  </read_first>
  <behavior>
    - The privacy scan, extended to the module BEFORE the log fix, fails RED on the existing
      raw-error public interpolation at the submission-failure site — the mechanism catching the
      exact defect it exists for — and passes GREEN after the log carries the error's type
      public and its value private.
  </behavior>
  <action>
**Step 1 — extend the scan and watch it fail.** In `DownloadLogPrivacyInvariantTests`: add
`AppPackage/Sources/BackgroundProcessingClient` as a second scan root walked by both tests, add
a known-member require for `ContinuedProcessingSession.swift` (so an empty walk cannot pass
vacuously, matching the existing known-member idiom), and rewrite the header paragraph: the
module publishes log lines adjacent to system submission where errors flow, so it is now scanned
under the same rules — the former out-of-scope audit sentence is retired, and the
no-whole-tree-rule rationale stands for everything else. No allowlist entry, no exemption, no
masked-count change. Run the targeted suite once (single invocation): the raw-error forbidden
token fails against the submission-failure line — record the RED reading verbatim.

**Step 2 — fix the log (IN-04).** Change the submission-failure log to carry the error's type
in the public field and its value in the private field — the type (for example the scheduler's
error enum name) is the operational signal, the value may embed arbitrary system strings. Use
the established interpolation style with explicit privacy classifications on both
interpolations.

**Step 3 — identity before submission (WR-08).** Move the `pendingIdentifier = identifier` and
`isAwaitingTask = true` assignments to AFTER the registration guard passes and BEFORE the
submission call; delete the post-submission assignments. A launch delivered synchronously during
submission now passes the adoption identity gate and adopts (and adoption's own clearing of the
identifier must not be undone afterwards — nothing may assign either property after the
submission call returns). The throwing-submission arm now reaches `endSession` holding the
recorded identifier, so the take-back cancel fires for a request the scheduler never accepted —
a documented defensive no-op (cancelling an unknown identifier is harmless by the scheduler API
contract) that also covers any half-submitted state a throw leaves behind. Rewrite the
`endSession` paragraph accordingly: the no-bundle-identifier and refused-registration arms still
precede the identifier and cancel nothing; the throwing-submission arm holds it and takes it
back.

**Step 4 — run to green.** Targeted run: the privacy suite green over both roots, and the
store's lifecycle suite green — if any existing case asserted the old post-submission
assignment ordering or the old no-cancel-on-throw bookkeeping, update it deliberately and record
which and why in the summary (15-38 adds the producer-arm cases; this plan only keeps the
existing suite honest). Then one full FeatureTests run, green.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadLogPrivacyInvariantTests -only-testing:DownloadsFeatureTests/ContinuedProcessingSessionTests</automated>
  </verify>
  <acceptance_criteria>
    - The scan covers the module: the extracted scan-root declaration names both source directories, a known-member require exists for the module file, and the header paragraph no longer contains the out-of-scope audit sentence for this module (quoted in the summary).
    - **Falsifiability recorded:** the summary quotes the RED reading — the raw-error forbidden token failing against the submission-failure line — taken after the scan extension and before the log fix.
    - The log is fixed: the extracted submission-failure line shows the type interpolation classified public and the error value classified private (quoted); the scan passes with zero allowlist additions.
    - Identity precedes submission: the extracted `start` body shows both assignments between the registration guard and the submission call, and no assignment to either property after the submission call (quoted); the rewritten `endSession` paragraph distinguishes the two pre-identifier arms from the holding arm (quoted).
    - Any existing-case assertion updated for the reorder is named in the summary with its reason; both the targeted run and one full FeatureTests run exit `0`, each a single invocation; `wc -l` recorded and below `1000` for every edited file.
  </acceptance_criteria>
  <done>The store's request identity exists before anyone can launch against it, the module's log surface is scanned by the same mechanism as the download client's and passed only after the raw-error value left the public field, and the endSession contract paragraph tells the new truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DownloadClient module boundary → linking modules | Session-state exposure narrowed from public to internal (attack-surface reduction) |
| Unified log → device archive readers | The background-processing module's log lines now under mechanism-enforced privacy rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-37-01 | Information Disclosure | Raw scheduler error value in the public log field (system strings of unknown content) | medium | mitigate | Type-public/value-private log shape + the privacy scan extended over the module, proven RED against the pre-fix line |
| T-15-37-02 | Elevation of Privilege | Any linking module reading or depending on continued-session internals through the public state surface | low | mitigate | The nine declarations dropped to internal; grep-proven zero external consumers; suites route through the DEBUG seam |
| T-15-37-03 | Denial of Service | The dropped superseded-arm ensure minting a dead identifier from a background context (a wedged single-session guard) | low | mitigate | The call is removed; both ownership-predicate branches shown to own their ensure elsewhere |
</threat_model>

<verification>
- Task 1 targeted expiration/interleave/convergence run; Task 2 privacy-scan RED then GREEN plus
  one full FeatureTests run. Never overlap xcodebuild invocations.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from the G-15-18 closure condition and SC2/SC4.
</verification>

<success_criteria>
- G-15-18 closed: all six items landed exactly as dispositioned — identity before submission
  with the endSession paragraph rewritten, the superseded-arm ensure gone with an observable
  argument, the collapse invariant bound, the session-state boundary internal and grep-proven,
  the teardown orderings fixed per recorded decisions, and the module's log privacy
  mechanism-enforced with the scan's RED observed first.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-37-SUMMARY.md` when done, including
the scan RED reading, both boundary greps, the per-case teardown decisions, and every quoted
rewritten paragraph.
</output>
