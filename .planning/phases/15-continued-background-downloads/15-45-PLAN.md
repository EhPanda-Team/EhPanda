---
phase: 15-continued-background-downloads
plan: 45
type: execute
wave: 45
depends_on: ["15-44"] # File overlap on DownloadClient+ContinuedSession.swift and DownloadClient+Scheduling.swift (the WR-04 blank lines must be located after plans 15-42/15-43 shifted those files) and xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadStoreTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-25]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "The three source residues are closed at their roots, each with a derivable proof: WR-03's bound that is always the whole array is gone — `restoredIndices` builds from every result's index, with the equivalence derived from the two writers of `completedCount` (assigned to `results.count` immediately before, incremented only later) rather than asserted; WR-04's two stray blank lines before closing braces are deleted, located fresh in the post-15-42/15-43 files; WR-05's unintended trailing comma in the parameter list is deleted (SC2: the restore accounting feeds the progress basis; the residues sit on the session and scheduling surfaces this phase's proofs read)."
    - "The public dead API is DECIDED, not deferred a third time: `validPageCount(folderURL:manifest:)` and `isReadableAssetFile(at:)` each take delete-or-justify with the caller census re-derived by grep at execution time across App/, AppPackage/Sources and AppPackage/Tests; deletion is the default for a public symbol with no production caller, and whichever remedy lands, the behavior the one existing Tests caller pins — the attributes-throw probe falling back to a content read and answering true for a readable file — remains pinned by a named test afterwards, never silently dropped (SC3: the probe fallback sits on the destroy-recorded-state defence)."
    - "The suite proves the equivalence: the full FeatureTests plan is green in one invocation with every pre-existing case unchanged except the one Tests caller of a deleted symbol, whose disposition (rerouted pin or recorded justification) is quoted in the summary."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift"
      provides: "restoredIndices built from the whole results array, with the always-whole-array derivation recorded"
      contains: "restoredIndices"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift"
      provides: "clearSelectedFailedPages' parameter list without the unintended trailing comma"
      contains: "clearSelectedFailedPages"
    - path: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      provides: "The dead-API decision landed: both symbols deleted, or a kept symbol carrying its recorded reason at the declaration"
      contains: "sanitizeAssetFileIfNeeded"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadStoreTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      via: "the attributes-throw fallback pin survives the dead-API decision — rerouted through a surviving public surface or kept against a justified symbol — so the probe's fallback behavior stays owned by a test"
      pattern: "ThrowingAttributesFileManager"
  prohibitions:
    - statement: "Must NOT change any observable behavior with WR-03/WR-04/WR-05: the WR-03 rewrite is a proven equivalence (the prefix length equals the array count at that point, derived from both writers), and WR-04/WR-05 are formatting-only — the full suite passes with every pre-existing case green."
      status: pending
      verification: automated
    - statement: "Must NOT silently drop the fallback behavior the one Tests caller pins: if isReadableAssetFile is deleted, the pin is rerouted through a production-shaped public route in the same task, or the symbol is kept with its reason recorded — the summary names the surviving test either way."
      status: pending
      verification: automated
    - statement: "Must NOT keep a dead symbol without a reason written at its declaration: keep-with-no-recorded-why is the deferral this gap exists to end."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<objective>
Close G-15-25 (warning, hygiene group — grouped because this project's bar treats nitpicks as
blocking). Verified verbatim in source at the round-13 HEAD:

- **WR-03** — `restoredIndices` (`DownloadClient+PageDownload.swift:42-46`) bounds its map with a
  prefix of `progress.completedCount`, which `initializePageDownloadState` assigned
  `progress.results.count` immediately before (`:97`); the only other writer is the `+= 1` in
  `applyPageTaskOutcome` (`:254`), which runs later. The bound is therefore always the whole
  array: it reads as a deliberate restriction and is not one.
- **WR-04** — stray trailing blank lines before closing braces at `+ContinuedSession.swift:625`
  and `+Scheduling.swift:370` (positions as of the round-13 HEAD; both files shift under plans
  15-42/15-43, so they are located fresh). Confirmed unlintable here:
  `vertical_whitespace_closing_braces` is opt-in and not in the root config's `opt_in_rules`.
- **WR-05** — `clearSelectedFailedPages(gid:selectedPageIndices:)`
  (`+PublicAPIHelpers.swift:54-57`) ends its parameter list with an unintended trailing comma;
  Swift accepts it, `trailing_comma` is not configured for parameter lists, and it is the only
  occurrence in the module.
- **Public dead API**, adopted from rounds 13's out-of-scope note rather than deferred a third
  time: `validPageCount(folderURL:manifest:)` (`DownloadStore+Operations.swift:271`) has NO
  caller in `AppPackage/Sources` or `AppPackage/Tests`; `isReadableAssetFile(at:)` (`:286`) has
  none in Sources and exactly one in Tests (`DownloadStoreTests.swift:311` region), which pins
  the attributes-throw → content-read fallback of the probe through the public forwarder.

Purpose: zero hygiene residue and a deliberate dead-API decision, so the review surface for the
next verification round starts clean (SC2's restore accounting, SC3's probe-fallback defence).
Output: three root-cause fixes, the decided dead-API remedy with the pin preserved, and a green
full run.
</objective>

## Artifacts this plan produces

- **No new production symbols.** Deletions expected: `validPageCount(folderURL:manifest:)` and —
  under the default remedy — `isReadableAssetFile(at:)` from `DownloadStore+Operations.swift`.
- **Possible test change:** the one `isReadableAssetFile` caller in `DownloadStoreTests.swift`
  rerouted to pin the same fallback behavior through a surviving public surface (the executor
  derives the route; the staged `ThrowingAttributesFileManager` fixture and the
  readable-file-answers-true assertion survive whatever the route is), or unchanged if the
  symbol is kept with a recorded reason.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close WR-03, WR-04, and WR-05 at their roots</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift, AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift, AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift, AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-25 entry WHOLE (each item's confirmed mechanism and suggested fix)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift` — `restoredIndices` and its surroundings (`:30-56`), `initializePageDownloadState` (`:76-104`: the count assignment), and `applyPageTaskOutcome`'s increment (the `:254` region): derive the always-whole-array equivalence yourself from both writers before editing
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` and `DownloadClient+Scheduling.swift` — the file TAILS as plans 15-42/15-43 left them: locate each stray blank line immediately preceding a closing brace fresh, rather than by the round-13 line numbers
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift` — `clearSelectedFailedPages` (`:54-64`)
    - `.swiftlint.yml` — before any Swift edit (note `single_line_trailing_closure` polices one-line trailing closures on `map` — a key-path argument in parentheses is the shape that avoids it)
  </read_first>
  <action>
**WR-03.** In `restoredIndices`, drop the always-whole-array bound so the set is built from
every result's index — `Set(progress.results.map(\.index))` — after deriving the equivalence
from the two writers of `completedCount` (the assignment to the results count immediately
upstream, the increment only downstream). Record the derivation in the summary; the surrounding
`remainingPageIndices` filter and everything downstream are untouched.

**WR-04.** Delete the stray blank line immediately preceding the closing brace at the tail of
each of the two files, located fresh. No other whitespace change.

**WR-05.** Delete the trailing comma after the final parameter of `clearSelectedFailedPages`.
No other change in the function.

**Then:** build-level confidence only — these edits are pinned by the existing suite; Task 2
runs the plan's single full invocation.
  </action>
  <verify>
    <automated>git diff --stat -- AppPackage/Sources/DownloadClient</automated>
  </verify>
  <acceptance_criteria>
    - WR-03: `grep -c 'prefix(progress.completedCount)' AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift` outputs `0`, the rewritten binding is quoted in the summary, and the two-writer equivalence derivation is recorded.
    - WR-04: `tail -n 2` of each of the two files shows the closing brace pair with no blank line between (both tails quoted in the summary).
    - WR-05: `grep -c 'Int\],$' AppPackage/Sources/DownloadClient/DownloadClient+PublicAPIHelpers.swift` outputs `0` and the parameter list is quoted in the summary.
    - The diff touches only the three named sites (stated from `git diff`, quoted in the summary).
  </acceptance_criteria>
  <done>All three residues are closed at their roots with each proof recorded, and no observable behavior moved.</done>
</task>

<task type="auto">
  <name>Task 2: Decide the dead public API and prove the whole plan green</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadStoreTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-25 dead-API item and the round-13 `verification_scope_note`'s adoption sentence (both symbols verified dead by grep across App/, AppPackage/Sources and AppPackage/Tests)
    - `AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift` — `validPageCount` (`:271-284`) and `isReadableAssetFile` (`:286-288`), plus `sanitizeAssetFileIfNeeded` and `probeAssetFile` in `DownloadStore.swift` (what the forwarder forwards to, and which public surfaces also reach it)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadStoreTests.swift` — the one `isReadableAssetFile` caller (the `:300-314` region): what it stages (`ThrowingAttributesFileManager`), what it pins (attributes-throw → content-read fallback answers true; the file is not deleted), and what other public `DownloadStore` surface the same staging could drive to the same probe
    - `AppPackage/Sources/DownloadClient/DownloadStore.swift` — the public `DownloadStore` API surface, to derive candidate reroute targets before deciding
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**Step 1 — the census, re-derived not imported.** Grep both symbol names across `App/`,
`AppPackage/Sources`, and `AppPackage/Tests` and record every match. Expected: `validPageCount`
has its declaration only; `isReadableAssetFile` has its declaration plus the single
`DownloadStoreTests` call. A discrepancy (a caller the round-13 grep missed) STOPS the decision
and is recorded — a symbol with a production caller is not dead and leaves this plan's scope.

**Step 2 — decide-one per symbol, deletion the default.** `validPageCount`: no caller anywhere
— delete the declaration outright. `isReadableAssetFile`: decide between (a) delete, rerouting
the Tests pin in the same edit — the same `ThrowingAttributesFileManager` staging driven through
a surviving public `DownloadStore` surface that reaches the same probe fallback (derive the
route from the Step-1 reading of the public surface; the assertion stays: a readable file whose
attributes read throws still answers usable and is not deleted) — or (b) keep, writing the
reason at the declaration (the public seam through which the probe's fallback contract is
pinned, and why no production-shaped route can pin it). Criteria: (a) is the default and is
taken if any surviving public surface can drive the staging to the same observable answer; (b)
is taken only if the derivation shows none can, and then the recorded reason must say exactly
that. Either way the pinned behavior ends the task owned by a named test, and the decision with
its derivation is recorded in the summary.

**Step 3 — green.** If deleted: a repository grep for each deleted name sums to zero matches.
Run ONE full FeatureTests run — the plan's single invocation, never overlapping another.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The Step-1 census is in the summary with every match classified, or the plan stopped on a discrepancy.
    - `validPageCount` is gone: `grep -rc 'validPageCount' App AppPackage` sums to `0` across all matches.
    - `isReadableAssetFile` is decided: either `grep -rc 'isReadableAssetFile' App AppPackage` sums to `0` and the rerouted pin's test name is recorded with its staging intact (`ThrowingAttributesFileManager` still staged, the readable-file-answers-usable assertion preserved), or the symbol remains with its reason written at the declaration (quoted in the summary) and the original test unchanged.
    - The full FeatureTests run exits `0` in a single invocation, with the total test count movement (zero, or the reroute's net change) recorded against the previous full run's count.
  </acceptance_criteria>
  <done>Both dead symbols carry a deliberate, recorded decision — deleted with the fallback pin preserved, or kept with the why at the declaration — and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dead public API → future callers | A public symbol with no production caller invites new call sites that bypass the defended routes (the probe forwarder collapses the classification the D-G13-01 defence depends on) |
| Test-pin surgery → proof surface | The dead-API decision touches the one test pinning the probe's fallback contract; a careless deletion silently retires that pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-45-01 | Tampering | A future caller adopting the collapsed-Bool forwarder for a destructive decision, replaying the G-15-19 laundering class | medium | mitigate | Delete-by-default removes the surface; a kept symbol's recorded reason states the licensing condition |
| T-15-45-02 | Repudiation | The fallback pin silently dropped with the deleted forwarder | medium | mitigate | The prohibition and Task 2 criteria require a named surviving test with the staging intact, either remedy |
| T-15-45-03 | Tampering | WR-03's rewrite changing restore accounting if the equivalence were wrong | low | mitigate | The two-writer derivation is recorded before the edit and the full suite pins the behavior |
</threat_model>

<verification>
- Task 1 is build-level only; Task 2 runs the plan's single full FeatureTests invocation.
  xcodebuild invocations must never overlap on this machine.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from G-15-25's grouped items and the SC labels the touched surfaces back (SC2 restore
  accounting, SC3 probe-fallback defence).
- <assumption_delta_decision>no-change — hygiene closures and a dead-API decision; no identity
  model changes shape.</assumption_delta_decision>
- API coverage: no BackgroundTasks verb is touched; COVERAGE.md unchanged.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan.
</verification>

<success_criteria>
- G-15-25 closed: the three residues fixed at their roots with proofs recorded, both dead
  symbols decided with the caller census re-derived and the fallback pin preserved by a named
  test, and the full suite green in one invocation.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-45-SUMMARY.md` when done, including
the WR-03 equivalence derivation, both quoted file tails, the dead-API census and per-symbol
decision with its derivation, and the surviving pin's test name.
</output>
