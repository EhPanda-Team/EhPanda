---
phase: 15-continued-background-downloads
plan: 50
type: execute
wave: 50
depends_on: ["15-49"] # xcodebuild serialization — one test invocation at a time on this machine — and file overlap on DownloadSourceInventoryTests.swift, whose run-proof census (four sites, one per file) counts the very property this plan replaces, and on DownloadClient+PageDownload.swift, whose progress struct 15-49 just rewrote and whose flush sites are candidate decrement points here.
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift
  # PROVISIONAL BELOW THIS LINE — the path that follows is the PRE-DERIVED candidate for the
  # decrement-point derivation (Task 1, Derivation A): `+PageDownload.swift:99`'s direct
  # `flushManifestPageProgress` for RESTORED pages is a manifest-raising route that does NOT pass
  # through `flushDownloadProgress`, so it is either dispositioned as owing no decrement or edited.
  # This list MUST be rewritten in the SUMMARY to the set actually modified, with the derivation
  # result that put each path in or left it out.
  - AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-30]
requirements: [SC2]

must_haves:
  truths:
    - "The run's proof of page work carries a QUANTITY rather than a membership, and the basis it unlocks is the work THIS RUN has finished rather than the record's ceiling: for the refusal family — where `reconcileWorkingManifestAgainstPageFiles` returns the manifest verbatim on all three exits and the record therefore reads N-of-N for the whole re-download — the card opens at the run's real basis, climbs as pages actually land, and never credits a page the run has not fetched (SC2)."
    - "The remedy reopens NEITHER of the two gaps it is built on top of. The proof stays RUN-scoped and session-SEEDED: it is written unconditionally at the preparation, no session boundary clears it, and every session start seeds the session trust set from it inside `ensureContinuedSession`'s SYNCHRONOUS reset, ahead of the snapshot the opening subtitle is computed from (G-15-26). And it stays the SAME SINGLE `pendingPageIndices` evaluation, computed once inside `prepareWorkingSeedAnnouncingProgress` and handed onward through `PreparedWorkingRun` rather than recomputed (G-15-27) — the one-evaluation rule stays grep-pinned by the census suite."
    - "The decrement point is DERIVED, not copied. Every route that raises a gallery's `completedPageCount` during a run is enumerated at execution time from source and classified by whether it lands a page this run OWED, and the chosen point is shown to be the one point every owed page passes — with each route that does not pass it dispositioned by the direction rule (under-crediting keeps the fraction at or below truth; over-crediting is the defect). A point named by the review and adopted without that enumeration is not accepted."
    - "The departure half is closed over EVERY departure kind, not the one the finding described. Completion, failure, user pause, queued-work-item cancel, outright delete and D-11's expiration sweep through `pauseAllSchedulable` are each enumerated with the ordering between the run's own retirement and the departure's `reconcileRetiredSessionPages`, and the retired value must be the work the run actually did in BOTH orderings — the design is ordering-INSENSITIVE by construction rather than by a race that currently happens to fall the right way."
    - "Consequence 3 is closed with its own case, and it is the harm that makes this a blocker: a refusal repair PAUSED after K of N pages, alone in its session, must terminate at `K / K pages · 0 galleries` through `reconcileContinuedSession`'s drain branch rather than at `N / N` — because the drain calls `finish(clientSessionID, true)`, so an over-retirement reports a paused or expiration-swept repair to the user as a fully successful N-page completion (SC2)."
    - "The two run-proof cases that PIN the defect are corrected without inheriting a new frozen constant. Their discriminating observation moves from the OPENING — which under the corrected basis reads the same for a proof kept and a proof lost, so an opening assertion alone would be vacuous — to the PROGRESSION, and each corrected case is shown capable of failing by an OBSERVED reading taken with the G-15-26 seeding removed."
    - "The distinct arm where trust OUTLIVES the run inside one session is dispositioned in writing rather than folded in silently: `observedIncompleteSessionGIDs` keeps a gid after a failed run and its only clears are the session start's re-seed and `markContinuedSessionEnded`, so a basis correction retired with the run leaves the record's full count credited for a merely-queued gallery — the queued-window zero D-G4-01 guarantees. The chosen disposition is derived against what the snapshot-sourced `formUnion` can and cannot re-add for a record that reads COMPLETE."
    - "The two written arguments that state a rule source does not implement are rewritten to the rule the fixed source implements: `reconcileRetiredSessionPages`' doc and the refusal suite's `BY DESIGN` ceiling argument, the latter being sound only for the completed case its own staging drives and silent about the paused, deleted, cancelled and expiration-swept departures that reach the same retirement through the same line."
    - { statement: "SC1 is deliberately NOT claimed by this plan. The verification record judged that G-15-30 does not reach SC1 through D-11's stall-expiration amplifier the way its four predecessors did — the scheduler force-expires the tasks reporting the LEAST progress, so a numerator pinned HIGH is protective where the pinned-zero one was lethal. The harm is entirely on SC2's honesty axis, and no plan may claim otherwise without new evidence", verification: backstop }
    - { statement: "No device-observable change is claimed beyond the card's readings; 15-UAT.md test 2 remains an independent open axis that this plan cannot discharge", verification: backstop }
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      provides: "The run's proof carrying how many pages the run still owes, replacing the membership set, with its lifetime rule and the reason a quantity is required stated on the declaration"
      contains: "pages"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      provides: "A basis that credits the run's own finished work rather than the record's ceiling, a departure retirement that cannot over-retire in either ordering, and a corrected D-G4-01 doc"
      contains: "reconcileRetiredSessionPages"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift"
      provides: "The derived decrement point, placed so a flush that wrote nothing credits nothing"
      contains: "flushDownloadProgress"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift"
      provides: "The two lifecycle cases with their discriminator moved from the opening to the progression, plus the strictly-increasing regression"
      contains: "startSubtitles"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift"
      provides: "The paused-departure terminal-pair case closing consequence 3, and the rewritten ceiling argument"
      contains: "terminalPair"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      via: "the preparation records the run's shortfall from the SAME single pending-page evaluation it hands to the page loop, and the snapshot's basis subtracts it — so the trust granted, the work performed and the number reported cannot come apart"
      pattern: "pendingPageIndices"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      via: "the flush that persists landed pages is also what lowers the run's outstanding debt, so the pushed numerator climbs page by page — which is the liveness the push exists for"
      pattern: "pushContinuedSessionProgress"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      via: "the run's own exit retires its outstanding debt at the same point it retires its proof, and the departure retirement must reach the same value whichever of the two lands first"
      pattern: "retireProvenPageWork"
  prohibitions:
    - statement: "Must NOT move the proof back to session scope, nor add a clear of the run-scoped collection to `markContinuedSessionEnded` or to `ensureContinuedSession`'s reset: that is G-15-26 reopened, and the census suite counts every site naming the property precisely so the attempt fails a build."
      status: pending
      verification: automated
    - statement: "Must NOT recompute the pending page list anywhere: the run's shortfall is seeded from the ONE evaluation inside `prepareWorkingSeedAnnouncingProgress` and handed onward through `PreparedWorkingRun`, and the one-evaluation census must be re-derived and asserted unchanged."
      status: pending
      verification: automated
    - statement: "Must NOT adopt the review's `provenPageWorkRunShortfalls` decrement point or its `reconcileRetiredSessionPages` clamp without the execution-time derivation: the enumerated raise-routes and the enumerated departure orderings must both be recorded in the summary, and a mismatch between the derivation and the shape means the derivation wins."
      status: pending
      verification: automated
    - statement: "Must NOT close only the 100% opening: a case driving a mid-run departure of a trusted complete-reading record to the DRAIN branch, asserting the terminal pair is the work actually done, is required — the suite currently contains no case that drives any mid-run departure of such a record."
      status: pending
      verification: automated
    - statement: "Must NOT correct a pinned assertion by substituting another frozen string. Every assertion in the target that encodes the pre-fix basis is enumerated by a recorded grep and dispositioned, and the two cases the gap names are corrected by moving their discriminator to an observation a lost proof would fail — each shown capable of failing by an OBSERVED reading, not by argument."
      status: pending
      verification: automated
    - statement: "Must NOT leave the outliving-trust arm undispositioned: the summary states what happens to a gid still in `observedIncompleteSessionGIDs` after its run ends, why that is the chosen direction, and which observation re-adds it if one does."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression; `+ExecutionSupport.swift` sits at 880 lines against a 1000-line `file_length` error limit, so doc mass added there must be budgeted rather than absorbed."
      status: pending
      verification: automated
---

<!-- planner-discipline-allow: provenPageWorkRunGIDs -->
<!-- planner-discipline-allow: completedPageCount -->

<objective>
Close BLOCKER G-15-30 — a NEW over-report introduced by this round's own G-15-26/G-15-27 remedy,
and the exact INVERSE of the pinned-ZERO defect rounds 12..15 chased, reached through the fix for
it. Every mechanical link was re-derived in source at HEAD 3961698c by the round-16 verification,
its three consequences each traced rather than accepted, the reviewer's own falsification test
re-run without falsifying it, and its ordinary-route reachability verified independently.

**The mechanism.** The admission records a bare membership — `+ExecutionSupport.swift:495-501`
inserts the gid into the run's collection and into the live session's trust set — and membership
makes the record authoritative IN FULL at `+ContinuedSession.swift:159-162`, where
`isSessionWork` selects between the record's whole finished-page count and zero. For the refusal
family those two quantities are opposites: the reconciliation returns the manifest verbatim on all
three refusal exits (the scan-failure guard at `+ExecutionSupport.swift:634`, the nothing-blanked
guard at `:646`, the all-or-nothing residual at `:655`), and the flush path is monotone upward, so
the record reads N-of-N at the announcement and N at every push until the run ends. The number the
card is handed is precisely the work the run has NOT done.

**Three consequences, all in source.**

1. The card opens and stays at 100% for the whole re-download. Both new run-proof cases assert
   exactly that string as the EXPECTED opening (`DownloadContinuedSessionRunProofTests.swift:129`,
   `:212`) against a fixture whose folder holds no page file at all (`:118` asserts the seed's
   existing pages are empty, `:119` asserts the run's pending work is all six pages). The same file
   records at `:185` that the pre-preparation opening was the zero reading, so the six pages are
   credited by the announcement and by nothing else.
2. The numerator is frozen for the run. The push exists for liveness — the scheduler forcibly
   expires tasks that appear stalled and prioritises terminating the ones reporting the least
   progress (`ContinuedProcessingSession.swift:196-198`). For this family the pre-fix constant was
   zero and the post-fix constant is N. The fix moved the constant, not the stall.
3. **The provable harm, and the reason this is a BLOCKER rather than a display nitpick.** A mid-run
   departure retires the ceiling into BOTH sides of the fraction. Pause a 100-page refusal repair
   after five pages: `shouldSchedule` (`+Scheduling.swift:125-135`) fails for a paused,
   complete-reading record, so the gid departs the schedulable set; it IS in the trust set and its
   record survives, so `reconcileRetiredSessionPages` takes the trusted branch at
   `+ContinuedSession.swift:603` and retires 100, which `:683-686` adds to numerator and
   denominator alike. If it was the session's only gallery, `reconcileContinuedSession`'s drain
   branch pushes `100 / 100 pages · 0 galleries` and calls `finish(clientSessionID, true)` (`:508`,
   `:521`). A PAUSED download is reported to the user as a fully successful 100-page completion over
   five pages of work — and `pauseAllSchedulable` makes D-11's expiration sweep one of those
   departures, so this sits on the phase's own best-effort path.

`reconcileRetiredSessionPages`' own doc forbids exactly that and names the guard meant to prevent
it (`:557-564`), closing with the direction rule this change reverses (`:571-574`): under-retiring
keeps the fraction at or below truth, while over-retiring is the defect. Putting the refusal family
inside the trust set means the guard no longer holds it out. The written acknowledgement at
`DownloadContinuedSessionLedgerRefusalTests.swift:59-66` does not cover it: its argument is sound
for the COMPLETED case its own staging drives, where the terminal N/N happens to be true, and says
nothing about the paused, deleted, cancelled or expiration-swept departures.

**Reachability is settled and must NOT be re-argued.** Downloads live under `.documentsDirectory`
(`AppTools/FileUtil.swift:7-11`) and `App/Info.plist` sets both `UIFileSharingEnabled` (`:170`) and
`LSSupportsOpeningDocumentsInPlace` (`:145`), so deleting a downloaded gallery's page files through
the Files app and tapping resume reaches `.repair` through `resumeMode`'s missing-files branch and
lands in the residual refusal. That is an ordinary user route, already verified.

Purpose: the system-provided progress UI reports the work the session has actually done, at the
opening, throughout the run, and at every departure — including the departure that terminates the
session (SC2).
Output: a proof that carries the run's outstanding page debt rather than a boolean, a basis and a
retirement derived over every raise-route and every departure ordering, two corrected docs, six
re-dispositioned pinned assertions, and three new regressions — the paused terminal pair, the
strictly-increasing progression, and the queued-window arm — each shown capable of failing.
</objective>

## Artifacts this plan produces

This section is the source-grounding exclusion list for the convergence pass: every symbol below is
CREATED or RENAMED by this plan and therefore must not be treated as drift against the current
tree. The set is PRE-DERIVED; the SUMMARY must rewrite it to what actually landed.

- **A run-scoped outstanding-page-debt store on `DownloadCoordinator`**, replacing
  `provenPageWorkRunGIDs` (`DownloadClient+Manager.swift:595`). The candidate name is
  `provenPageWorkRunShortfalls`, typed as a gid-keyed integer map; the name and type are the
  executor's to confirm against the census the rename moves. The membership predicate the rest of
  the module needs becomes a presence test on that store.
- **A basis accessor on the snapshot path** in `DownloadClient+ContinuedSession.swift` — the
  per-gallery number `schedulableSnapshot` writes into `sessionCompletedPages`, computed from the
  record and the run's outstanding debt rather than from the record alone. Candidate shape: a small
  private helper named for what it answers (the session's credited pages for one download) so both
  the snapshot and the retirement read one definition.
- **A decrement at the derived point**, expected in `flushDownloadProgress`
  (`DownloadClient+Persistence.swift:201`) — the derivation in Task 1 decides.
- **A retirement of the outstanding debt** alongside `retireProvenPageWork`
  (`DownloadClient+Execution.swift:303`), and whatever the outliving-trust disposition adds there.
- **New test cases** (names are candidates; the summary records what landed):
  `testARefusalRepairPausedPartWayDrainsAtTheWorkItActuallyDid`,
  `testARefusalRepairsIntermediatePushesStrictlyIncrease`, and the queued-window arm
  `testAFailedRefusalRepairsGalleryReturnsToZeroWhileMerelyQueued`.
- **A moved census table** in `DownloadSourceInventoryTests.swift` — `runProofPropertyName`
  (`:79`), `expectedRunProofSites` (`:193-200`) and `expectedRunProofSiteTotal` (`:201`) all name
  the replaced property and must be re-derived, not edited to fit.
- **No new production types, no new public API surface, no new module.**

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the four load-bearing points from source, then land all three regressions and observe them RED against the current over-report</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-30 entry WHOLE (`detail`, `why_tests_missed_it`, `suggested_fix`, `files`), plus `re_verification.verification_scope_note`, whose closing sentences state the two gaps a fix must not reopen and the one judgement about consequence 3 that must not be relitigated
    - `.planning/phases/15-continued-background-downloads/15-REVIEW.md` — CR-01 whole, INCLUDING its "What would falsify this" paragraph; its proposed shape is a STARTING POINT and its two named points must be re-derived rather than adopted
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `schedulableSnapshot()` and the D-G4-01 doc above it (the `:80-177` region), `ensureContinuedSession` with its synchronous reset, its seed at `:246`, the snapshot at `:248` and the three post-start merges (`:268`, `:283`, `:313`), `markContinuedSessionEnded` (`:370-402`), `pauseAllSchedulable` (`:404-447`), `reconcileContinuedSession` with its drain branch (`:449-525`, especially `:499-522`), `reconcileRetiredSessionPages` with its whole doc (`:527-612`), and `pushContinuedSessionProgress` (`:614-699`) with the retired-total addition at `:683-686`
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `withdrawingCountedBasisMovement` (`:268-282`), `prepareWorkingSeed` (`:296-353`), the whole doc above `prepareWorkingSeedAnnouncingProgress` (`:355-479`) and the function itself (`:480-506`), the three refusal exits inside `reconcileWorkingManifestAgainstPageFiles` and its two `updateDownloadIndex` writes (`:660`, `:690`), and `pendingPageIndices` (`:851-879`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift` — `flushDownloadProgress` (`:183-226`) with its throttle guard, its `try flushManifestPageProgress`, its observer notify and its push; `flushManifestPageProgress` (`:228-248`) with BOTH its early returns; `updateDownloadIndex` (`:250-256`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift` — `downloadPages` (`:21-69`) including the `restoredIndices` filter at `:41-43` and the forced flush at `:56`, `initializePageDownloadState` (`:71-104`) with its DIRECT `flushManifestPageProgress` at `:99`, `collectExistingPages` (`:132-157`), `processRemainingPages` (`:159-224`) with its cadence flush at `:201`, and `applyPageTaskOutcome` (`:245-...`) where a landed page reaches `pendingResolvedPages`
    - `AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift` — `processDownload`'s `defer` (`:12-23`) and every exit of its body (`:25-57`), `completeDownload` and its `settleCompletedDownload` call (`:72`), `retireProvenPageWork` with its whole doc (`:284-306`) and `isSupersededByALiveRun` (`:308-314`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionPerform.swift` — `performDownload` (`:18-49`) and the comment at `:29-32` recording why the pending list is consumed rather than recomputed, `executePageDownloads` (`:51-98`), `finalizeBatchResult`/`finalizeDownload` and its `updateDownloadIndex` at `:178`
    - `AppPackage/Sources/DownloadClient/DownloadClient+BackgroundDownloads.swift` — the `flushManifestPageProgress` at `:193` and the enclosing background page-landing routine
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — `shouldSchedule` (`:125-135`) and `pause(gid:)` (`:158`) with its ownership/cancellation tail
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — `PreparedWorkingRun` (`:151-161`), `ProgressFlushContext` (`:213-221`), the `observedIncompleteSessionGIDs` doc (`:530-564`) and the `provenPageWorkRunGIDs` doc and declaration (`:565-595`)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift` — the WHOLE file (332 lines): the suite doc's choreography discipline, all three cases, and the third case's real-`processDownload` staging with `makeStubbedURLSession` (`:316-331`), which is the only existing seam that drives a run to a REAL exit through its `defer`
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` — the WHOLE file (473 lines): the suite doc, the `BY DESIGN` ceiling argument at `:59-66`, both refusal cases, the selected-page regression, and the `pageResults` helper at `:455`
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — `SessionGallery` (`:309`), `makeQueuedCoordinator` (`:365`), `writePageFiles` (`:419`), `makeRepairPayload` (`:536`), `makeRetriedPagesPayload` (`:560`), `expectTheFractionReachesOneOnlyAtTheDrain` (`:586`), `expectTheCompletedSeriesNeverRewinds` (`:601`), `lastPushedPair` (`:615`), `waitUntil` (`:799`)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionExpirationTests.swift` — the production `flushDownloadProgress` staging at `:380-398`, which is the established way a case lands pages through the production flush rather than through a private seam
    - `.swiftlint.yml` — before any Swift edit; note the 120-column limit and the 1000-line `file_length` ERROR limit
  </read_first>
  <action>
Nothing in this task changes production behaviour. It produces four recorded derivations and three
failing regressions. The mechanism lands in Task 2.

**Derivation A — every route that RAISES a gallery's finished-page count during a run.** Enumerate
by grep across `AppPackage/Sources/DownloadClient` every call of `updateDownloadIndex`,
`flushManifestPageProgress`, `refreshManifestPageFileHash`, `refreshManifestPageFileHashes` and
`addingCurrentFileHashes`, and classify each site by ONE question: can it land a page that THIS RUN
owed — that is, a page in the run's own `pendingPageIndices`? The pre-derived candidate set, to be
re-derived at this head rather than trusted: the flush inside `flushDownloadProgress`
(`+Persistence.swift:215`); the DIRECT flush for restored pages in `initializePageDownloadState`
(`+PageDownload.swift:99`); the background page landing (`+BackgroundDownloads.swift:193`);
`finalizeDownload`'s hash fill (`+ExecutionPerform.swift:178`); the reconciliation's own writes
(`+ExecutionSupport.swift:660`, `:690`); and the folder/capture writes in `+PublicAPI.swift`
(`:142`, `:147`, `:346`). For the restored-pages site, settle the overlap question explicitly:
`pendingPageIndices` excludes a page whose file exists and `collectExistingPages` restores a page
whose file exists, so the two sets are disjoint under a single scan — but `downloadPages` filters
`restoredIndices` out of `pendingPageIndices` at `:41-43`, which only exists because the two scans
are separated in time. State whether that residual overlap can leave a page owed forever, and which
direction the residual errs in. For the background site, state whether it can run for a gallery
whose run holds an outstanding debt.

Record the enumeration as a table: site, whether it can land an owed page, and the disposition. A
site that can land an owed page and does NOT pass the chosen decrement point is a hole, and a hole
that under-credits is accepted with its direction stated while a hole that over-credits is not.

**Derivation B — the decrement point's correctness conditions.** Whatever point Derivation A
selects, enumerate the ways it can be reached without the record actually rising, and place the
decrement so none of them credits the run. At the candidate point these are: the throttle guard
returning before anything is consumed; `flushManifestPageProgress` returning early on an empty page
list; the same function returning early when the manifest file is absent; the `try` throwing so the
pending page list is NOT cleared; and one page appearing across two flushes after a retry. Each
needs a stated disposition, and the placement must satisfy the same rule the pending-page-list
clearing already satisfies — the decrement happens exactly where the clearing happens, or the two
can drift. Also enumerate the FIVE test call sites of `flushDownloadProgress` (it is `public`) and
state whether each remains correct under the decrement, since a case that lands pages through the
production flush is how two of this plan's regressions are staged.

**Derivation C — the departure orderings, over every departure kind.** For each of: run completion
(`settleCompletedDownload`, in-body at `+Execution.swift:72`), run failure (`settleDownloadFailure`
at `+BackgroundDownloads.swift:110`, `:144` and `+Persistence.swift:171`, plus every arm of
`handleProcessDownloadError`), a user pause through `pause(gid:)`, a queued-work-item cancel, an
outright delete (no record survives — the existing branch already handles it), and D-11's expiration
sweep through `pauseAllSchedulable`, answer TWO questions and record both. (i) When the departure's
`reconcileRetiredSessionPages` runs, is the run's outstanding debt still recorded, or has
`processDownload`'s `defer` already retired it? (ii) What value does the retirement produce in EACH
of those two orderings? The acceptance bar is a property, not a shape: **the retired value must be
the work the run actually did in BOTH orderings.** If the current design can only satisfy that when
a race falls one way, the design changes until it is ordering-INSENSITIVE, and the summary states
which change bought the insensitivity. Note explicitly that a user pause and the expiration sweep
are issued from OUTSIDE the run, so their ordering against the cancelled run's `defer` is not
controlled by either call site.

**Derivation D — the outliving-trust arm, dispositioned in writing.** `retireProvenPageWork` removes
the run's entry, but nothing removes the gid from `observedIncompleteSessionGIDs`, whose only clears
are `ensureContinuedSession`'s re-seed (`:246`) and `markContinuedSessionEnded` (`:401`). So after a
FAILED refusal repair the gid keeps contributing while merely queued — the queued-window zero
D-G4-01 guarantees, lost for the rest of that session. Derive the disposition rather than choosing
one: state what the snapshot-sourced `formUnion` over `incompleteGalleryIDs` can and cannot re-add,
and note that it provably cannot re-add a record that reads COMPLETE, which is the entire reason the
run's proof exists. Then state the chosen direction and what it costs. This arm is DISTINCT from the
basis and must not be folded into it silently.

**Then land the three regressions, and observe every one of them RED.** All three fail against the
current tree, which is what makes them evidence rather than decoration. Use the two suites' own
choreography discipline without exception: record state comes only from fixture manifests,
`writePageFiles` and production routes, and every push, start, run-exit and departure asserted on is
PRODUCTION-issued.

1. **The consequence-3 case — the paused departure that drains.** A refusal-family repair over a
   complete-reading record, ALONE in its session, whose reconciliation refuses so the record reads
   N-of-N throughout. Land K of N pages through the production flush, then depart the gallery
   through a production route that reaches the DRAIN branch, and assert the terminal pair and
   subtitle are the work actually done rather than the record's ceiling — the drain calls
   `finish(clientSessionID, true)`, so this assertion is the difference between an honest terminal
   card and reporting a paused repair as a successful N-page completion. Assert non-vacuity FIRST:
   the record still reads N, the run's pending work really was N pages, and K of them really landed.
   State in the case's own doc WHICH of Derivation C's two orderings this staging exercises. If it
   exercises only one, add or extend a case reaching the other through the real-`processDownload`
   seam the third run-proof case already uses (`makeStubbedURLSession` plus a stubbed transport
   failure), so the run's own `defer` really runs; if no fixture can reach the second ordering,
   state that limitation explicitly and disposition it, exactly as 15-48 dispositioned the
   overlapping-run gating it could not stage.
2. **The strictly-increasing case.** The same family, landing pages in at least three production
   flush batches, asserting the pushed numerator STRICTLY INCREASES across the recorded updates
   rather than asserting any particular string. Assert the series has at least three distinct values
   so a two-point staging cannot pass it, and assert the final value equals the work done. This is
   the case that makes consequence 2 unrepeatable: it fails against a numerator frozen at zero and
   against one frozen at N alike, which no assertion on a single constant can do.
3. **The queued-window case for Derivation D.** A refusal repair that FAILS mid-run, after which the
   gallery is merely queued again inside the SAME session, asserting its contribution has returned
   to zero — D-G4-01's guarantee that a redo's target pages are not this session's progress.

Run the two suites in ONE targeted invocation. All three new cases MUST FAIL. Quote each failure
verbatim in the summary with the observed value beside the expected one, because those three
readings are the only evidence that the cases discriminate; a case added after the mechanism lands
has never been shown capable of failing. Every PRE-EXISTING case in both suites is expected to PASS
in this same run — this task changes no production code, so a pre-existing failure here means the
staging perturbed shared state and must be fixed before Task 2.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionLedgerTests</automated>
  </verify>
  <acceptance_criteria>
    - Derivation A is recorded as a table in the summary with one row per enumerated site, each row stating whether the site can land a page the run owed and its disposition; the grep commands that produced the enumeration are quoted, and the restored-pages overlap question is answered with its direction stated.
    - Derivation B lists every reach-without-a-record-rise condition at the chosen point with its disposition, names the placement rule that keeps the decrement and the pending-page-list clearing from drifting, and enumerates all five test call sites of `flushDownloadProgress` with a per-site verdict.
    - Derivation C is recorded as a table with one row per departure kind and TWO columns of retired value — one per ordering — and every row's two values are the work the run actually did. If any row required a design change to reach that, the change and the property it buys are named. Rows for the user pause and the expiration sweep explicitly state that their ordering against the run's `defer` is not controlled by the call site.
    - Derivation D states what the snapshot-sourced union can and cannot re-add for a complete-reading record, the chosen direction, and what it costs — in the summary and in a doc comment on the code that implements it, per the project's rule that non-obvious deliberate designs carry their WHY.
    - All three new cases exist and were OBSERVED FAILING against the unmodified production tree, with each failure quoted verbatim showing observed beside expected. A case whose first observed reading is green MUST NOT be reported as a regression for this gap.
    - The consequence-3 case reaches `reconcileContinuedSession`'s drain branch — the gallery is the session's only one and the terminal push is the drain's — and its doc names which of Derivation C's two orderings it exercises, with the other ordering either covered by a named companion case or dispositioned with the fixture limitation stated.
    - The strictly-increasing case asserts a strict increase over at least three distinct pushed values and asserts no single pinned subtitle for its intermediate readings.
    - Every pre-existing case in both suites passed in the same run; the invocation's non-zero exit is attributable ONLY to the three new cases, and the summary names which cases failed.
    - The targeted invocation did not overlap any other `xcodebuild` invocation.
  </acceptance_criteria>
  <done>The four derivations exist as recorded enumerations rather than as adopted proposals, and all three regressions — the drain-terminal pair, the strictly-increasing progression, and the queued window — have been observed failing against the current over-report, so each is known to discriminate before any production line moves.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the proof from a membership to the run's outstanding page debt, and make the basis and the retirement credit the work the run did</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift, AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift, AppPackage/Sources/DownloadClient/DownloadClient+Persistence.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift</files>
  <read_first>
    - The four derivations recorded by Task 1 — this task implements them and may not deviate from them without recording why
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — the `provenPageWorkRunGIDs` declaration and its whole doc (`:565-595`), whose three headed paragraphs (the lifetime rule, why a session boundary is not a run boundary, why the retirement is not optional) must survive the promotion with their claims re-checked rather than copied; and the `observedIncompleteSessionGIDs` doc directly above it (`:530-564`), whose closing paragraph describes what a session boundary re-derives the set FROM
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `schedulableSnapshot()` (`:154-177`) and the D-G4-01 doc above it, in particular the paragraph beginning "Each half of the predicate earns its place" (`:140-148`), which states what the trust set covers and is the sentence the promotion changes; `reconcileRetiredSessionPages` (`:575-612`) and the whole doc above it (`:527-574`)
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `withdrawingCountedBasisMovement` (`:268-282`), whose withdrawal is computed from the RECORD's raw before/after delta while `wasCountedBasis` reads the session trust set; and the bracket-composition paragraph in the preparation's doc (`:439-455`), which states which side of that bracket the admission lands on and why
    - `AppPackage/Sources/DownloadClient/DownloadClient+Execution.swift` — `retireProvenPageWork` (`:284-306`) with its overlapping-run disposition, and `isSupersededByALiveRun` (`:308-314`)
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadSourceInventoryTests.swift` — the suite header's rationale, `runProofPropertyName` (`:79`), `expectedRunProofSites` (`:193-200`) with the whole doc above it stating what the table pins against, `expectedRunProofSiteTotal` (`:201`), `pendingPageListToken` (`:78`) and the pending-list census it feeds, `clientModuleFiles(in:)` (`:462-...`), `callSiteCount`, `mutationCount` and `executableLines` (`:415-432`)
    - `.swiftlint.yml` — the 120-column limit and the 1000-line `file_length` error limit; `+ExecutionSupport.swift` is at 880 lines and `+Manager.swift` at 764
  </read_first>
  <action>
**Step 1 — promote, do not add alongside.** Replace the run-scoped membership set with a gid-keyed
count of the pages THIS RUN still owes. The set is REMOVED, not kept beside the map: two sources of
the same fact is how a later fix moves them apart, which is the generator this phase has recorded
four times. Every consumer that needed membership reads a presence test on the map instead. The
declaration's doc keeps its three headed paragraphs and gains a fourth stating why a QUANTITY is
required: membership unlocks the record's whole finished-page count, and for the refusal family that
count IS the run's remaining work, so crediting it opens the card at its ceiling, freezes the
numerator for the run, and retires the ceiling into both sides of the fraction on any mid-run
departure — the over-retirement D-G2-01 forbids in its own words.

**Step 2 — record the debt from the ONE evaluation that already exists.** In
`prepareWorkingSeedAnnouncingProgress`, the gate stays exactly what it is — the run's own pending
page list being non-empty — and the recorded value is that list's count. Nothing new is computed and
nothing is recomputed: the same evaluation still travels onward through `PreparedWorkingRun` to
`performDownload`. The session insert and the announcement push keep their current position and
their current order, and the run-scoped write stays UNCONDITIONAL, outside the live-session branch.
Re-verify the bracket-composition paragraph rather than assuming it survives: the preparation's own
movement is still measured against the pre-announcement trust state, because the debt is recorded
after `prepareWorkingSeed` has returned and closed its `withdrawingCountedBasisMovement` bracket.

**Step 3 — the basis.** `schedulableSnapshot` stops selecting between the record's whole count and
zero. Give the per-gallery number one definition, in one place, that both the snapshot and the
departure retirement read, so the opening rule and the departure rule cannot disagree about a
gallery — which is the property the existing comment at `:167-168` already claims for the summed
numerator and which this change must preserve. Derive each branch rather than transcribing the
review's expression: state why a record that reads INCOMPLETE must count raw with no subtraction
(its own count already excludes the pages this run has not written), why a trusted complete-reading
record must have the run's outstanding debt subtracted, and why the result is clamped at zero. Then
re-derive the interaction with D-G7-01: `withdrawingCountedBasisMovement` withdraws the RECORD's
raw before/after delta, so state whether the basis delta can differ from the record delta under the
clamp, and in which direction the difference errs. Record that derivation beside the code.

**Step 4 — the decrement, at the point Derivation A and B selected.** Place it so a flush that wrote
nothing credits nothing, and so it lands in the same synchronous stretch that clears the pending
resolved pages. Clamp at zero, and state why a clamp is needed rather than treating it as defensive
noise: Derivation B's retry condition is the reachable path that would otherwise drive the debt
negative. Add the doc comment explaining what the decrement is for — the pushed numerator climbing
page by page is the liveness the push exists to report, which is why it rides the flush rather than
a queue mutation, in the same terms `flushDownloadProgress`'s own doc already uses.

**Step 5 — the retirement, ordering-insensitive.** Implement Derivation C's conclusion at BOTH ends:
the run's own exit and `reconcileRetiredSessionPages`. The run's exit retires the debt at the same
point and under the same superseded-run gate as the proof, for the reason that gate already records
— a superseded predecessor must not drop a live successor's state. The departure retirement stops
retiring the record's raw count for a trusted gallery and retires the basis Step 3 defined. Whatever
Derivation D chose for the outliving-trust arm lands here too, with its direction stated in the
code. The acceptance bar is Derivation C's property: for every departure kind, the retired value is
the work the run actually did in BOTH orderings.

**Step 6 — re-derive the census the rename moves.** `DownloadSourceInventoryTests` pins the run-proof
property by name across four files. The token, the per-file table and the joined total must all be
RE-DERIVED against the promoted property at this head — enumerated by a fresh grep and recorded —
not edited until the numbers fit. If the promotion changes the site count in any file, the doc above
the table is rewritten to say what the new number pins against, since that doc's whole purpose is to
name what a moving number would mean. Re-derive the pending-page-list census in the same pass and
assert it unchanged: the one-evaluation rule is G-15-27's closure and this task must not move it.

**Step 7 — targeted proof.** Run the two suites from Task 1 plus the census suite in ONE targeted
invocation. All three regressions must now PASS, every pre-existing census must pass, and the SIX
pre-existing assertions that encode the old basis are EXPECTED to fail here — they are Task 3's
subject and must not be touched in this task. Record which cases failed and confirm the set is
exactly the enumerated six, because a seventh failure means the change reached a surface no
derivation covered.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionLedgerTests -only-testing:DownloadsFeatureTests/DownloadSourceInventoryTests</automated>
  </verify>
  <acceptance_criteria>
    - The membership set is GONE rather than kept alongside: `grep -rn 'provenPageWorkRunGIDs' AppPackage/Sources AppPackage/Tests` returns no hits outside doc comments that deliberately record the history, and every such surviving doc mention is quoted in the summary with the reason it survives.
    - The proof stays run-scoped and session-seeded: the recorded write is outside the live-session branch, no clear of the run-scoped store exists in `markContinuedSessionEnded` or in `ensureContinuedSession`'s reset, and the session start still seeds the trust set inside the SYNCHRONOUS reset ahead of the snapshot the opening subtitle is built from. Each of the three is confirmed by a quoted line, not asserted.
    - The single pending-page evaluation is intact: the pending-list census is re-derived and asserted unchanged at its recorded value, and `performDownload` still consumes `preparedRun.pendingPageIndices` rather than recomputing.
    - The basis has ONE definition read by both the snapshot and the departure retirement, and the summary quotes it once with the three derived branches (incomplete raw, trusted complete-reading minus the debt, untrusted zero) each justified in its own sentence.
    - The D-G7-01 interaction is derived, not assumed: the summary states whether the basis delta can differ from the record delta under the clamp and in which direction it errs.
    - The decrement's placement is quoted beside the pending-resolved-pages clearing it must not drift from, and every condition from Derivation B is shown to credit nothing when the record did not rise.
    - The retirement satisfies Derivation C's property: the summary re-states the per-departure table with the values now produced, and every row's two orderings agree.
    - The run-proof census is re-derived by a fresh grep whose output is quoted, and any changed number is accompanied by a rewritten doc stating what the new number pins against.
    - The targeted invocation shows all three new regressions PASSING and exactly the enumerated six pre-existing basis assertions FAILING; the failing set is listed by file and line, and any additional failure is investigated and recorded rather than absorbed.
    - No `// swiftlint:disable`, no `try?`, no `try!`, no `@unchecked Sendable`, no `nonisolated(unsafe)`; `awk 'length($0)>120'` over every changed Swift file returns nothing, and no changed file crosses 1000 lines.
  </acceptance_criteria>
  <done>The run's proof carries how many pages the run still owes, the card's numerator is the work the session has done at the opening and at every push, and every departure retires that same number in both orderings — with the census that pins the property re-derived rather than fitted.</done>
</task>

<task type="auto">
  <name>Task 3: Move the pinned cases from a constant to a discriminating observation, prove each capable of failing, correct the two docs that state the wrong rule, and take the full suite green</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift, AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift</files>
  <read_first>
    - Task 1's four derivations and Task 2's per-departure table — the rewritten docs are derived from those, never from the gap record's or the review's wording
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionRunProofTests.swift` — the WHOLE file again at the head Task 2 leaves behind, in particular the suite doc's statement of what the trio pins (`:19-38`), the first case's suspension paragraph (`:60-67`) explaining that the opening subtitle is fixed before the client start's hop, the second case's earned-then-torn-down staging (`:196-199`), and the third case's explicit treatment of its own VACUOUS pre-fix green (`:224-231`) — that paragraph is the model for how a case whose green proves nothing must be handled
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionLedgerRefusalTests.swift` — the `BY DESIGN` ceiling argument at `:59-66` WHOLE, including the sentence naming `expectTheFractionReachesOneOnlyAtTheDrain` as deliberately not asserted, and both cases' terminal assertions
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `reconcileRetiredSessionPages`' doc `:527-574` WHOLE, especially the earned-authority paragraph `:557-564` and the accepted-residual paragraph `:571-574` carrying the direction rule
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift` — `expectTheFractionReachesOneOnlyAtTheDrain` (`:586`) and `expectTheCompletedSeriesNeverRewinds` (`:601`), so the rewritten argument states accurately which of the two now applies to which case
    - `.swiftlint.yml` — before any Swift edit
  </read_first>
  <action>
**Step 1 — enumerate every assertion that encodes the pre-fix basis, by grep, before editing any of
them.** The gap record names two (`DownloadContinuedSessionRunProofTests.swift:129` and `:212`).
There are more: a recorded grep for the pushed-subtitle literals across
`AppPackage/Tests/DownloadsFeatureTests` returns six assertions in the two suites that encode the
complete-reading record's ceiling as an expected reading, and three others that encode the legitimate
queued-window zero and must NOT move. Enumerate all of them, classify each as "encodes the defect"
or "encodes a rule that still holds", and record the classification with its line anchors. Editing
only the two the gap named is the branch-scoped-fix failure this phase has recorded three times.

**Step 2 — move the discriminator; do not substitute a constant.** The two cases the gap names exist
to prove that the run's proof survives a session boundary. Under the corrected basis their OPENING
reads the same whether the proof was kept or lost — the run owes all N pages at that moment, so the
credited work is zero either way — which means an opening assertion is no longer a discriminator at
all, and replacing the ceiling string with a zero string would restore exactly the pre-G-15-26
constant while claiming to have fixed it. That is the round-16 lesson stated in its own terms:
correcting the predicate while inheriting the basis swaps a pinned zero for a pinned N, and a
regression asserting the pre-fix constant locks the bug in.

So each case's proof moves to an observation a LOST proof would fail: after the session start, land
pages through the production flush and assert the credited work RISES — which it can only do for a
gallery the session trusts, because an untrusted complete-reading record contributes zero at every
push regardless of what lands. Keep each case's original subject and staging intact; the seeding
route, the teardown route and the no-session precondition are what the two cases are about and none
of them changes. Add to each case's doc a sentence recording why the opening assertion was demoted
and what replaced it.

**Step 3 — prove each corrected case capable of failing.** An assertion that has only ever been
observed green has not been shown to reach anything. For EACH of the two corrected cases, take a
sensitivity reading: remove the G-15-26 mechanism the case exists to pin — the session start's seed
of the trust set from the run's proof — run the case, and OBSERVE IT FAIL; then restore the seed and
observe it pass. Quote both readings. This is the same discipline the third case's own doc demands
of itself at `:224-231`, and it is the only thing that separates a moved discriminator from a
re-frozen constant. If a corrected case passes with the seed removed, STOP: the replacement
observation does not discriminate and must be re-derived before anything is reported.

**Step 4 — correct the two written arguments to the rule source now implements.**

`reconcileRetiredSessionPages`' doc: the earned-authority paragraph currently says a departed gallery
inside the trust set retires its record's count and one outside retires its last observation. Rewrite
it from Task 2's implementation to say what the record is now authoritative FOR and what the run's
outstanding debt corrects it BY, keeping the paragraph's existing and still-true claims — that this
is not a departure-reason branch, that no call site classifies why a gallery left, and the direction
rule that under-retiring keeps the fraction at or below truth while over-retiring is the defect.
Where Derivation D's disposition changed what a departure sees, say so there.

The refusal suite's `BY DESIGN` ceiling argument: it is sound only for the completed case its own
staging drives, and silent about the paused, deleted, cancelled and expiration-swept departures that
reach the same retirement through the same line. Rewrite it to state the rule that now holds for the
whole family, name the case that covers the departure half — this plan's consequence-3 regression —
and state accurately which of the two series helpers applies to which case now that the fraction's
behaviour has changed. Do not import the production doc verbatim; this is a test doc and must read
as one.

Both rewrites are derived from Task 1's and Task 2's recorded derivations at this head. Neither is
copied from the gap record, the review, or from each other.

**Step 5 — sweep the residue before declaring the gap closed.** Four checks, each recorded.
(a) Orphans: grep the removed property name repository-wide and confirm the only survivors are the
history-recording doc mentions Task 2 quoted. (b) Newly-unowned claims: grep both the client module
and the test target for any doc still describing the basis as the record's own count, the trust set
as unlocking a full count, or the retirement as retiring what the record claims — this is the class
the two Step-4 rewrites belong to, and this plan must not leave a third instance. (c) Census reach:
confirm the widened inventory scan 15-49 landed covers every file this plan edited, so a
reintroduction of a corrected claim fails a build rather than waiting for a review round.
(d) Direction audit: for every accepted residual this plan created, state which direction it errs in
and confirm it is the under-crediting one.

**Step 6 — full green.** Run ONE full FeatureTests invocation as the plan's final proof. Record the
total against 15-49's recorded 880 and account for the movement case by case: three new regressions
added by Task 1, plus any case added for Derivation C's second ordering. A total that moves by an
unexplained amount is a finding, not a rounding error.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The Step-1 enumeration is in the summary with every matching assertion listed by file and line and classified as encoding the defect or encoding a rule that still holds; the grep that produced it is quoted. The two assertions the gap named are inside that list rather than being the whole of it.
    - Neither corrected case asserts a single pinned subtitle as its proof of the proof surviving: each one's discriminating assertion is a rise in credited work across production-issued pushes, and the demotion of the opening assertion is recorded in the case's own doc.
    - BOTH sensitivity readings are quoted per corrected case — the FAIL with the session-start seed removed and the PASS with it restored — with the removal described precisely enough to be repeated. A case for which only a green reading is reported MUST NOT be described as pinning anything.
    - The consequence-3 case from Task 1 now passes and its terminal assertion is the work actually done rather than the record's ceiling; the summary quotes the terminal pair and the subtitle, and states that the drain branch is what produced it.
    - Both rewritten docs are quoted in full in the summary beside the derivation that produced them, and neither reproduces the gap record's or the review's phrasing. The retirement doc still carries the not-a-departure-reason-branch claim and the direction rule; the test doc names the case covering the departure half.
    - The four residue checks are each recorded with their grep results, including the direction audit naming every accepted residual and its direction.
    - The full FeatureTests run exits `0` in a single invocation with no `-only-testing` filter, and its total is recorded against 15-49's 880 with the movement accounted for case by case.
    - No SwiftLint rule is suppressed, disabled or annotated away anywhere in this plan; a fresh lint pass over the changed files reports nothing, and no changed file crosses the 1000-line limit.
    - No `xcodebuild` invocation in this plan overlapped another.
  </acceptance_criteria>
  <done>The suite no longer encodes the defect anywhere: the two cases that pinned the ceiling now prove the proof's survival through an observation a lost proof fails, each shown capable of failing by an observed reading; the departure half is covered by its own case; the two docs state the rule source implements; and the full suite is green in one invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coordinator → system progress card | Numbers cross into system UI that renders outside the app's privacy mask and outside App Switcher snapshot protection; the card is also the user's only evidence that a backgrounded session did what it says it did |
| Manifest record → session accounting | The record is authoritative about the MANIFEST; treating it as authoritative about THIS SESSION's work is the entire class this gap belongs to, and the refusal family is where the two diverge maximally |
| Run lifetime → session lifetime | A fact about a run (this run owes these pages) read through a session-scoped collection, where a boundary on either side can retire it too early or too late |
| Departure sweep → terminal report | The last value a departing gallery contributes is also what a drained session reports as its final, successful result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-50-01 | Repudiation | The drain branch reporting a paused or expiration-swept refusal repair as a fully successful N-page completion, over a fraction of N pages of real work | high | mitigate | The departure retirement credits the work the run actually did rather than the record's ceiling, and the consequence-3 regression drives a real departure to the drain branch and asserts the terminal pair, observed RED before the mechanism landed |
| T-15-50-02 | Spoofing | The card asserting 100% completion before a byte is fetched, for a family reachable by an ordinary Files-app deletion plus a resume tap | high | mitigate | The basis subtracts the run's outstanding page debt from the record's count for a trusted complete-reading gallery, so the opening reading is the work the session has done |
| T-15-50-03 | Denial of Service | A numerator frozen for the whole run, which the scheduler reads as a stalled task and force-expires — D-11 then pauses every schedulable download | medium | mitigate | The debt is decremented at the derived point every landed page passes, so the pushed numerator climbs page by page; the strictly-increasing regression fails against a numerator frozen at either constant |
| T-15-50-04 | Tampering | A fix that closes the over-report by moving the proof back to session scope or by recomputing the pending page list, trading G-15-30 for a reopened G-15-26 or G-15-27 | high | mitigate | The run-scoped write stays unconditional and unspilled by any session boundary, the single pending-page evaluation still travels through `PreparedWorkingRun`, and both invariants are re-derived and grep-pinned by the inventory suite's censuses |
| T-15-50-05 | Repudiation | The retirement over-crediting because the run's own exit and the departure sweep race, so the fix holds only in whichever ordering the current code happens to produce | high | mitigate | Derivation C enumerates every departure kind against both orderings and the design is changed until the retired value is identical in both; the per-departure table with two ordering columns is quoted in the summary |
| T-15-50-06 | Repudiation | The trust set outliving the run, so a merely-queued gallery keeps contributing its record's full count for the rest of the session — the queued-window zero D-G4-01 guarantees | medium | mitigate | Derivation D dispositions the arm in writing, its direction is recorded in a doc comment on the implementing code, and the queued-window regression pins the outcome |
| T-15-50-07 | Repudiation | A corrected assertion that re-freezes a constant, so the suite goes green while still encoding a defect — the failure mode that produced this gap, where two passing cases pinned the 100% opening | high | mitigate | Every assertion encoding the pre-fix basis is enumerated by grep and classified; the two named cases move their discriminator to a rise in credited work; and each is OBSERVED failing with the seeding removed before it is trusted |
| T-15-50-08 | Information Disclosure | Nothing new crosses the card seam: the subtitle is built from integers alone and no gallery value is in scope on the push path | low | accept | The existing content-surface rule on `continuedSessionSubtitle` is unchanged by this plan; no new argument is added to the localized key and no new value reaches the seam |
</threat_model>

<verification>
- Three `xcodebuild` invocations, strictly one at a time and never overlapping — a second concurrent
  invocation wedges `testmanagerd` on this machine and needs a reboot. Task 1's targeted run over
  the two ledger suites is EXPECTED to exit non-zero, and that non-zero exit is the plan's primary
  evidence rather than a failure. Task 2's targeted run adds the census suite and is expected to
  exit non-zero on exactly the enumerated pre-existing basis assertions. Task 3's run is the full
  FeatureTests suite with no filter and must exit `0`.
- Both ledger extension files declare their cases inside `DownloadContinuedSessionLedgerTests`, so
  the `-only-testing` selector for the run-proof and refusal cases is that suite name; the summary
  must confirm the selected run actually executed the new cases rather than silently selecting an
  empty set.
- Spec-less probe fallback: visibly SKIPPED this round and recorded here as a deliberate choice —
  this phase has no requirement IDs to probe (`Requirements: None mapped`; its scope contract is
  the four success criteria), so no probe-derived edge or prohibition predicates were generated.
  `must_haves` are derived from the G-15-30 record itself and from this round's standing
  constraints, in the style 15-46..15-49 established.
- <assumption_delta_decision>promote — primary noun: the run's proof of page work. It changes from
  a MEMBERSHIP (`Set<String>`) to a QUANTITY per gid (how many pages the run still owes). The
  decision is **promote**, not add-alongside: the set is removed rather than kept beside the map,
  because two stores of the same fact is precisely how a later fix moves the trust granted and the
  work performed apart — the generator behind G-15-26, G-15-27 and this gap. The membership
  predicate every consumer needed becomes a presence test on the promoted store. Invariant test
  encoding the generalized intent: the strictly-increasing regression, which asserts the credited
  work is a function of pages landed rather than of any single admission event, and the inventory
  suite's re-derived census, which fails a build if a second store of the same fact appears.
  </assumption_delta_decision>
- API coverage gate: the deterministic scan reports `detected: false` for this phase scope, so no
  coverage matrix is produced or extended and the existing `COVERAGE.md` is left untouched. No
  BackgroundTasks verb is added, removed or re-dispositioned by this plan.
- Schema-push gate: no ORM schema files exist in this repository; skipped.
- Security enforcement is active at ASVS L1 with a `high` blocking threshold. Five `high` threats
  are registered above and all five are dispositioned `mitigate` with a named mechanism and a named
  regression; the one `accept` is `low`.
- The physical-device UAT re-run (15-UAT.md test 2) is NOT claimed by this plan and nothing here can
  discharge it. It remains an independent open axis under `human_verification`, and closing G-15-30
  does not advance it in either direction.
</verification>

<success_criteria>
- G-15-30 closed: the run's proof carries the pages the run still owes rather than a boolean, the
  basis and both retirements are derived over every enumerated raise-route and every departure
  ordering rather than copied from the review's shape, consequence 3 is closed by a case that drives
  a real departure to the drain branch and asserts the terminal pair is the work actually done, the
  outliving-trust arm is dispositioned in writing with its direction recorded in code, the two docs
  that state the wrong rule are rewritten from this plan's own derivations, every assertion encoding
  the pre-fix basis is enumerated and dispositioned with the two named cases moved from a constant
  to a discriminating observation and each observed failing before it is trusted, the run-proof
  census is re-derived rather than fitted, neither G-15-26 nor G-15-27 is reopened, and the full
  suite is green in one unfiltered invocation.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-50-SUMMARY.md` when done, including:
the four derivation records (the raise-route table, the decrement's correctness conditions with the
five test call sites, the per-departure table with two ordering columns, and the outliving-trust
disposition with its direction); the three RED readings quoted verbatim with observed beside
expected; the enumeration of every assertion encoding the pre-fix basis with its classification; both
sensitivity readings per corrected case; the two rewritten docs quoted in full beside the
derivations that produced them; the re-derived run-proof and pending-list censuses with their grep
output; the four residue-sweep results including the direction audit; the rewritten `files_modified`
set replacing this plan's provisional candidate line; the rewritten artifact list replacing the
pre-derived candidate names; and the full-suite total recorded against 15-49's 880 with the movement
accounted for case by case.
</output>
