---
phase: 15-continued-background-downloads
plan: 38
type: execute
wave: 38
depends_on: ["15-37"] # Real code dependency: the producer cases assert the post-WR-08 ordering and cancel bookkeeping 15-37 lands in ContinuedProcessingSession.swift (file overlap there too); xcodebuild serialization — one test invocation at a time on this machine.
files_modified:
  - AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift
  - AppPackage/Sources/BackgroundProcessingClient/BackgroundProcessingClient.swift
  - AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift
autonomous: true
gap_closure: true
gap_ids: [G-15-17]
requirements: [SC2, SC3]

must_haves:
  truths:
    - "The scheduling spy can refuse and can throw, so the production translation from a real scheduler refusal into the .unavailable event is executed by the suite instead of being hard-coded away: every .unavailable producer in ContinuedProcessingSession.start — missing bundle identifier, refused registration, throwing submission — has a case asserting the session yields exactly one unavailable event, finishes its stream, and leaves the store willing to grant the next start; the .unavailable yield plus self-finish is the SOLE mechanism releasing the coordinator's liveness flag on a refused submission (SC3's refusal half, with no fallback tier behind it), so the producer side must be exercised, not assumed (mandate: contract-faithful doubles — this is how G-15-3, G-15-4, and G-15-5 all shipped green)."
    - "Both arms of handleLaunch's nil-task path are executed: a nil launch for the awaited request ends the session unavailable, and a STALE nil launch — an old identifier's handler firing after its session ended — cannot end a live session (the identity gate the suite has never driven)."
    - "The WR-07 regression's documented consequences are its assertions: under the persisted-queue-lag staging, the expiration pause-all actually runs (production-issued through the seam forwarder that wraps pauseAllSchedulable) and the RUNNING gallery is asserted to be among the paused set — the SC2 cancel half named in the case doc is driven, not narrated (D-11: an expiration pauses every schedulable download, and the running gallery is exactly the one consuming resources)."
    - "The cancel bookkeeping asserted for the throwing-submission arm is the POST-WR-08 contract deliberately: the store recorded its identifier before submitting, so endSession takes the request back defensively — the case documents that the gap's cancels-nothing expectation was written against the pre-reorder shape and was superseded by 15-37 (a fix routing through a site with an open warning inherits it; the inherited disposition is recorded where it changed the assertion)."
  artifacts:
    - path: "AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift"
      provides: "Spy controls (refusesNextRegistration, nextSubmissionError) with the wrong always-succeeds premise comment retired; five new cases covering the three producers and both nil-launch arms"
      contains: "refusesNextRegistration"
    - path: "AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift"
      provides: "An injectable bundle identifier on the internal test initializer (defaulting to the main bundle's), making the missing-bundle-identifier arm executable"
      contains: "bundleIdentifier"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift"
      provides: "The WR-07 companion driving the expiration pause-all under the lag staging and asserting the running gallery is paused"
      contains: "testAnExpirationPausesTheRunningGalleryWhenThePersistedQueueLagsBehindIt"
    - path: "AppPackage/Sources/BackgroundProcessingClient/BackgroundProcessingClient.swift"
      provides: "The every-endpoint coverage claim verified against the new cases and corrected if it overclaims"
      contains: "BackgroundProcessingClient"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift"
      to: "AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift"
      via: "the spy's controllable refusal and thrown submission drive the exact production arms that translate scheduler outcomes into the unavailable event the coordinator's liveness release depends on"
      pattern: "nextSubmissionError"
    - from: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift"
      via: "testingPauseAllSchedulable(expiring:) wraps the production pause-all, so the companion's paused-set assertion exercises the same selection path the device expiration uses"
      pattern: "testingPauseAllSchedulable"
  prohibitions:
    - statement: "Must NOT change production behavior beyond the injectable bundle identifier on the already-internal test initializer (production keeps resolving through shared with the default): every existing case passes unchanged except where a case doc says the new controls changed its staging."
      status: pending
      verification: automated
    - statement: "Must NOT hand-issue the pause in the WR-07 companion: the pause-all must be production-issued through the seam forwarder, and the case must not pre-pause or hand-mutate the running gallery's state to make the assertion pass."
      status: pending
      verification: automated
    - statement: "Must NOT weaken or delete an existing store or basis case to make the new cases pass."
      status: pending
      verification: automated
    - statement: "Must NOT reach for a concurrency or lint escape hatch or a SwiftLint suppression."
      status: pending
      verification: automated
---

<objective>
Close G-15-17 (warning; promoted under the standing mandate for contract-faithful doubles —
test-double infidelity is how G-15-3, G-15-4, and G-15-5 all shipped green). Verified in source:
`ContinuedTaskSchedulingSpy.scheduling` returns true from register unconditionally and never
throws from submit, with a comment claiming the refusal path is the store's early-unavailable
branch — so the missing-bundle-identifier arm, the refused-registration arm, and the
throwing-submission arm of `ContinuedProcessingSession.start` never execute, and no case calls
the spy's launch with a nil task, so neither arm of `handleLaunch`'s nil path runs either. This
is more than coverage: the coordinator treats any non-nil handle as success and learns of
unavailability only through the stream, so the `.unavailable` yield plus self-finish is the sole
mechanism that releases `hasLiveContinuedSession` on a refused submission. The coordinator SIDE
of that contract is covered; the production translation from a real scheduler refusal into that
event is not. And WR-07: the persisted-queue-lag regression documents two consequences — a
frozen retirement value and `pauseAllSchedulable(expiring:)` skipping the one gallery consuming
resources, the SC2 cancel half — and asserts only a subtitle.

Purpose: the refusal translation and the stale-launch identity gate are pinned by the suite,
and the lag regression's SC2 claim is an assertion rather than prose.
Output: controllable spy outcomes, five producer/launch cases asserting the post-15-37
contract, the pause-all companion, the client doc claim verified, and a green full run.
</objective>

## Artifacts this plan produces

- **New spy controls:** `refusesNextRegistration` (one-shot, consumed only by the registration
  it refuses) and `nextSubmissionError: (any Error)?` (one-shot, thrown by the next submission
  then cleared) on `ContinuedTaskSchedulingSpy`; the always-succeeds premise comment retired.
- **New seam parameter:** `bundleIdentifier: String?` on `ContinuedProcessingSession`'s internal
  initializer, defaulting to the main bundle's identifier; `start` reads the stored value.
- **New test cases** in `ContinuedProcessingSessionTests.swift`:
  `testAMissingBundleIdentifierYieldsUnavailableAndReleasesTheStore`,
  `testARefusedRegistrationYieldsUnavailableAndReleasesTheStore`,
  `testAThrowingSubmissionYieldsUnavailableTakesItsRequestBackAndReleasesTheStore`,
  `testANilLaunchForTheAwaitedRequestYieldsUnavailable`,
  `testAStaleNilLaunchCannotEndALiveSession`.
- **New test case** in `DownloadContinuedSessionBasisTests.swift`:
  `testAnExpirationPausesTheRunningGalleryWhenThePersistedQueueLagsBehindIt`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-37-SUMMARY.md
@CLAUDE.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the spy controllable outcomes and execute every unavailable producer and both nil-launch arms</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift, AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift, AppPackage/Sources/BackgroundProcessingClient/BackgroundProcessingClient.swift</files>
  <read_first>
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — the G-15-17 entry WHOLE (why the producer side matters for liveness release, and every mandated assertion)
    - `AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift` — `start` AS 15-37 LEFT IT (identity now precedes submission; the three unavailable arms and the take-back cancel on the throwing arm), `handleLaunch` (`:195-205` region: the awaited-nil arm and the stale-identity guard), `adopt`, `endSession`, and the internal initializer the bundle-identifier parameter joins
    - `.planning/phases/15-continued-background-downloads/15-37-SUMMARY.md` — the landed WR-08 ordering and endSession paragraph (the contract the new cases assert)
    - `AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift` — `ContinuedTaskSchedulingSpy` (`:59-103`: the closure seam gaining the controls, the premise comment being retired, the launch helper), and the suite's determinism idiom (drive to completion first, drain the buffered stream after — the new cases follow it)
    - `AppPackage/Sources/BackgroundProcessingClient/BackgroundProcessingClient.swift` — the endpoint doc making the every-endpoint coverage claim (verify or correct after the cases land)
  </read_first>
  <behavior>
    - `testAMissingBundleIdentifierYieldsUnavailableAndReleasesTheStore` — store built with a nil
      bundle identifier; start returns a session; drained stream is exactly one unavailable
      event; nothing registered, nothing submitted, nothing cancelled; a second start on the same
      store returns non-nil (the store granted it).
    - `testARefusedRegistrationYieldsUnavailableAndReleasesTheStore` — arm
      `refusesNextRegistration`; drained stream exactly one unavailable event; registration was
      attempted, nothing submitted, nothing cancelled; the next start succeeds and registers a
      FRESH identifier (two distinct registered identifiers total).
    - `testAThrowingSubmissionYieldsUnavailableTakesItsRequestBackAndReleasesTheStore` — arm
      `nextSubmissionError`; drained stream exactly one unavailable event; per the post-WR-08
      contract the store recorded its identifier before submitting, so the cancelled identifiers
      equal the one just registered (the documented defensive take-back — the case doc records
      that the gap's cancels-nothing expectation predates 15-37's reorder); the next start
      succeeds.
    - `testANilLaunchForTheAwaitedRequestYieldsUnavailable` — start normally (submission
      succeeds), then deliver the spy launch for the MINTED identifier with a nil task; drained
      stream exactly one unavailable event; the awaited request is taken back; the next start
      succeeds.
    - `testAStaleNilLaunchCannotEndALiveSession` — session A starts and is finished via the
      store's finish verb (its handler stays registered forever — the production property the
      spy's keyed handlers mirror); session B starts; deliver the spy launch for A's identifier
      with a nil task; B is unaffected — no event on B's stream yet, and B's minted launch still
      adopts (deliver B's identifier with a task spy and observe the granted event), then finish
      B cleanly.
  </behavior>
  <action>
**Step 1 — controls first, watched failing where honest.** Add `refusesNextRegistration` and
`nextSubmissionError` to the spy: the register closure consumes the refusal arm ONLY when it
refuses (mirroring the one-shot discipline G-15-10's closure established for the client spy —
the arm must never be consumed by a call it did not refuse), and the submit closure throws and
clears the armed error. Retire the always-succeeds premise comment and replace it with the
controls' contract. Note the submit closure records the submission attempt before throwing or
not at all — decide and document which (the faithful reading: a submission that threw never
reached the scheduler's queue; do not record it as accepted).

**Step 2 — the bundle seam.** Add `bundleIdentifier: String?` to the internal initializer
(default: the main bundle's identifier), stored, read by start where it currently reads the
bundle directly. Production resolution through `shared` is untouched. One-line doc on the
parameter: exists so the missing-identifier arm is executable by the suite; nil in production
would mean a process without a bundle.

**Step 3 — the five cases.** Add them per the behavior block, following the suite's
drive-then-drain determinism idiom. Every case asserts the full release contract, not just the
event: stream finished (the drain loop exits), spy bookkeeping exact (registered / submitted /
cancelled lists compared whole, not sampled), and the store willing to grant the next start.
Run the targeted store suite once mid-work (single invocation) if an honest RED is available for
any case against the pre-controls spy — the three producer cases cannot compile against the
missing controls, so the recorded falsifiability here is bookkeeping-level: after landing,
temporarily inverting one assertion (locally, never committed) is NOT required; instead record
in the summary, for each case, which production arm it executes, verified by a coverage-style
argument from the arm's observable side effects (the distinct spy lists each arm leaves).

**Step 4 — the client doc claim.** Re-read the every-endpoint coverage sentence in
`BackgroundProcessingClient.swift` against the suite as it now stands; if it still overclaims
(any endpoint or arm no case drives), correct the sentence to name what is actually exercised —
a wrong written premise must not survive a plan that touches its subject.

**Step 5 — run to green.** Targeted run over the store suite: all cases green, existing cases
untouched. Full FeatureTests run happens at the end of Task 2.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/ContinuedProcessingSessionTests</automated>
  </verify>
  <acceptance_criteria>
    - The controls exist with one-shot discipline: the extracted spy shows the refusal consumed only on the refusing branch and the submission error cleared on throw (quoted in the summary), and the always-succeeds premise sentence is gone from the spy comment (`grep -c 'Registration always succeeds here' AppPackage/Tests/DownloadsFeatureTests/ContinuedProcessingSessionTests.swift` outputs `0`).
    - All five cases exist: a grep for each of the five case names over `ContinuedProcessingSessionTests.swift` outputs `1` per name.
    - Each producer case asserts the whole contract: drained events compared as a whole array, all three spy lists asserted, and a follow-up start asserted non-nil (stated from source inspection with the throwing-arm case quoting its post-WR-08 take-back rationale).
    - The bundle seam exists and production is untouched: `grep -c 'bundleIdentifier' AppPackage/Sources/BackgroundProcessingClient/ContinuedProcessingSession.swift` outputs at least `2` (parameter + read site), and `shared` still resolves with no argument.
    - The summary maps each case to the production arm it executes via that arm's distinct observable side effects.
    - The targeted run exits `0` in a single invocation.
  </acceptance_criteria>
  <done>Every unavailable producer and both nil-launch arms are executed by the suite against a spy that can refuse and throw, each case pins the full release contract, and the client's coverage claim tells the truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the expiration pause-all under the lag staging and assert the running gallery is paused</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift` — `testTheRunningGalleryStaysInTheSessionBasisWhenThePersistedQueueLagsBehindIt` (`:266-314` at planning time: the parked-runner staging, the lag step, the doc block at `:260-265` naming the pause-all consequence this companion now drives — update that doc to point at the companion for the SC2 half)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Testing.swift` — `testingPauseAllSchedulable(expiring:)` (`:94`) and `testingContinuedSessionID()` (how the companion names the live session it expires)
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — the pause path a pause-all takes per gallery (what observable state a paused RUNNING gallery leaves: cancelled active task, non-queued record, paused display status) so the assertions are derived from production semantics, not invented
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `pauseAllSchedulable`'s selection (the schedulable set unioned with the active gallery — the 15-26 union this case proves catches the lagging runner)
  </read_first>
  <behavior>
    - `testAnExpirationPausesTheRunningGalleryWhenThePersistedQueueLagsBehindIt` — the WR-07
      companion. Stage exactly as the existing lag case: running gallery (6 of 10) parked on a
      blocking runner control, queued sibling, session ensured, then the lag
      (`testingSetQueuedGalleryIDs` drops the running gid from the persisted queue while its
      task runs). Then the production-issued expiration consequence: read the live session id
      via `testingContinuedSessionID()` and await
      `testingPauseAllSchedulable(expiring: sessionID)`. Assert the RUNNING gallery is among the
      paused set: its active task was cancelled (the control records the cancellation and the
      coordinator reports no active task), and its record shows the paused shape production
      leaves (derive the exact display-status/queue assertions from the pause path read in
      read_first). Assert the queued sibling was paused too (the pause-all covers every
      schedulable download per D-11 — the set, not one member).
  </behavior>
  <action>
**Step 1 — write the companion and watch the selection matter.** Add the case per the behavior
block. Its doc states the WR-07 finding: the sibling case documents this consequence and asserts
only a subtitle; this case makes the SC2 cancel half — the expiration pausing the one gallery
actually consuming resources — the assertion itself, through the active-gallery union in the
pause-all's selection. Production-issued discipline: the pause is driven ONLY through
`testingPauseAllSchedulable(expiring:)`, which wraps the production pause-all; the case performs
no hand-pause and no hand-mutation of the running gallery's state between the lag step and the
assertions (grep-gate: the case body contains exactly one pause-related call, the forwarder).

**Step 2 — update the sibling's doc.** In the existing lag case's doc block, replace the
narrated pause-all consequence with a pointer to the companion that now drives it (the doc keeps
the retirement half it does assert).

**Step 3 — run to green.** Targeted basis run (single invocation), then one full FeatureTests
run: every suite green, no existing literal changed.
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionBasisTests</automated>
  </verify>
  <acceptance_criteria>
    - The companion exists: `grep -c 'testAnExpirationPausesTheRunningGalleryWhenThePersistedQueueLagsBehindIt' AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionBasisTests.swift` outputs `1`; its body contains `testingContinuedSessionID`, `testingPauseAllSchedulable`, and assertions on BOTH galleries' paused shapes (quoted in the summary).
    - Production-issued: the case body's only pause-related call is the forwarder (stated from source inspection; no direct pause, togglePause, or state hand-mutation between lag and assertions).
    - The sibling's doc now points at the companion for the pause-all half (quoted).
    - Both the targeted run and one full FeatureTests run exit `0`, each a single invocation; `wc -l` recorded and below `1000` for the basis suite file.
  </acceptance_criteria>
  <done>The lag regression's SC2 cancel half is a driven assertion: an expiration's pause-all provably pauses the running gallery the persisted queue forgot, alongside every schedulable sibling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Injected test seam → production lifecycle | The bundle-identifier parameter and spy controls widen only the internal test initializer's inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-38-01 | Denial of Service | An unexercised refusal translation regressing silently — a refused submission that never releases the liveness flag leaves background coverage dead for the process | medium | mitigate | The three producer cases pin the yield-and-self-finish release contract end to end |
| T-15-38-02 | Spoofing | A stale launch handler ending or adopting into a live session | low | mitigate | The stale-nil-launch case pins the identity gate; the awaited-nil case pins the honest teardown |
| T-15-38-03 | Elevation of Privilege | The bundle-identifier seam reaching production behavior | low | mitigate | Parameter on the already-internal initializer only, defaulted; shared resolves unchanged and the plan's grep confirms it |
</threat_model>

<verification>
- Task 1 targeted store-suite run; Task 2 targeted basis run plus one full FeatureTests run.
  Never overlap xcodebuild invocations.
- Spec-less probe fallback: visibly skipped this round — no requirement IDs; must_haves derive
  from the G-15-17 closure condition, SC2's cancel half, and SC3's refusal half.
</verification>

<success_criteria>
- G-15-17 closed: the spy refuses and throws on demand, all three unavailable producers and both
  nil-launch arms are executed with full release-contract assertions matching the post-15-37
  ordering, the WR-07 companion drives the pause-all and asserts the running gallery paused, the
  client doc claim is honest, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-38-SUMMARY.md` when done, including
the case-to-arm execution map, the quoted take-back rationale in the throwing-arm case, and the
companion's paused-set assertions.
</output>
