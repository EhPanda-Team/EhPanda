---
phase: 15-continued-background-downloads
plan: 65
type: execute
wave: 65
depends_on: ["15-64"]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift
autonomous: true
gap_closure: true
gap_ids: [CR-01]
requirements: [SC2]

must_haves:
  truths:
    - "Every deliberate downward mover of the credited session basis is enclosed by a D-G7-01 withdrawal bracket by construction, so the monotonic floor never sits above the honest sum after a queue-intent advance."
    - "After a same-session re-queue, the very next page of genuine work moves the pushed numerator — no frozen frames are absorbed by a stale floor."
    - "The enumerated invariant at DownloadClient+ContinuedSession.swift:230-236 — 'deliberate movers are bracketed' — is true against source, not merely asserted."
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      provides: "Self-bracketing queue-intent advance"
      contains: "withdrawingCountedBasisMovement"
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift"
      provides: "One-at-a-time keeper-page regression that discriminates the frozen-floor defect"
      contains: "oneAtATime"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      via: "advanceQueueIntentGeneration wraps its own increment in the shared withdrawal bracket, composing as a sibling with every existing bracket"
      pattern: "withdrawingCountedBasisMovement"
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift"
      to: "AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift"
      via: "sessionCreditedPages' generation-mismatch drop is measured across the advance and withdrawn from lastPushedCompletedPageCount"
      pattern: "queueIntentGeneration"
---

<objective>
Close CR-01 (verification gap 1): the 15-61 fix made `advanceQueueIntentGeneration(for:)` a deliberate DOWNWARD mover of the credited session basis — for a gallery with a complete record observed under the previous generation, `sessionCreditedPages` steps from `recorded` to 0 the instant the generation increments — and none of its four call sites is enclosed by `withdrawingCountedBasisMovement`. The stale monotonic floor then absorbs the next N pages of genuine work, freezing the system card and feeding `ContinuedTaskScheduling`'s most-stalled expiration policy a falsely stalled task. That is exactly the G-15-6/G-15-7 masking the module's own doc forbids.

Fix the property at its single choke point: the movement itself brackets its own increment, so every present and future call site is enclosed by construction. Do NOT patch the four call sites individually — that is the branch-scoped shape this phase has re-opened repeatedly.

Purpose: an honest, never-frozen pushed numerator across every queue-intent advance (SC2).
Output: a self-bracketing advance, a sibling-composition audit over all four call sites, a discriminating one-at-a-time regression, and a re-audited invariant doc.
</objective>

## Artifacts this phase produces

- Revised `advanceQueueIntentGeneration(for:)` in `DownloadClient+Manager.swift`: the increment runs inside `withdrawingCountedBasisMovement(gid:)`; public signature unchanged.
- New regression test in `DownloadContinuedSessionTests`: keeper pages land ONE AT A TIME across a re-queue and the pushed numerator is asserted to move on the FIRST one.
- Re-derived doc prose: the invariant enumeration at `DownloadClient+ContinuedSession.swift:230-236` and the stale narrative inside `testARequeuedGalleryInheritsNoPredecessorObservationCredit`'s comment (lines 640-646) rewritten to match post-fix source.
- No new production types, files, or public API.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-continued-background-downloads/15-VERIFICATION.md
@.planning/phases/15-continued-background-downloads/15-REVIEW.md
@.planning/phases/15-continued-background-downloads/15-61-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove the re-queue's floor freezes the first post-requeue pages</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift</files>
  <read_first>
    - `AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift` — modify this file; reuse its two-gallery fixtures, `BackgroundProcessingClientSpy` progress capture, blocker/parked-runner occupancy, and the existing `testARequeuedGalleryInheritsNoPredecessorObservationCredit` (lines 653-813) whose single-flush shape this new case must NOT copy — its own doc (640-646) records that landing six keeper pages in one flush "discriminates nothing".
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — `sessionCreditedPages` (237-262), `reconcileRetiredSessionPages` (765-804), and the invariant doc (215-236): the failure sequence being pinned.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `withdrawingCountedBasisMovement` (273-286) and the sibling-composition rule (262-268).
    - `.planning/phases/15-continued-background-downloads/15-VERIFICATION.md` — gap 1's exact trace: retire A at 4 pages, floor latched at 4, re-queue advances the generation unbracketed, credited sum falls by 4, floor stays, next 4 pages of keeper work move nothing.
  </read_first>
  <behavior>
    - Gallery A (small page count, e.g. 4) completes and retires while keeper gallery B holds the queue-wide session open; a push latches the floor at A's page total; the session ID and spy start count are captured.
    - A is re-queued through a real queue-mobilizing API (`retry` or `retryPages`), advancing `queueIntentGeneration`; the keeper's remaining pages then land ONE AT A TIME, each followed by its own production progress push.
    - The pushed numerator must MOVE on the FIRST keeper page landed after the re-queue. Pre-fix it cannot: the floor sits A's page total above the honest sum and absorbs exactly that many pages.
    - Session identity and spy start count remain unchanged throughout, so the fix cannot pass by minting a successor session contrary to D-06.
  </behavior>
  <action>
Add one production-shaped regression to `DownloadContinuedSessionTests` (file is at 814 lines; the addition must keep it under the 1000-line SwiftLint limit — if it cannot, split the suite by extraction before adding). Stage A's completion, retirement (`retiredSessionPages` populated via the production reconcile, observable through pushed values), and the floor latch with production pushes. Re-queue A via the production retry path. Then drive keeper B's pages individually: each landing must be production-issued (the same flush landing point every other series test uses — name the production event in the test's comment per this phase's choreography rule), and after each landing capture the next pushed `completedPageCount`. Assert the first post-requeue push exceeds the last pre-requeue push. Do not mutate `queueIntentGenerations`, `retiredSessionPages`, `runProgressBases`, `observedIncompleteSessionGenerations`, or `lastPushedCompletedPageCount` through any test accessor. Bank the failing pre-fix series (the frozen frames) in the summary.
  </action>
  <verify>
    <automated>! xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionTests</automated>
  </verify>
  <acceptance_criteria>
    - The new case fails against pre-fix production with a frozen first frame: the first post-requeue push equals the pre-requeue floor value instead of exceeding it.
    - Keeper pages land one at a time — no single flush lands two or more pages between two consecutive observed pushes in the post-requeue window.
    - The case asserts the session ID and `BackgroundProcessingClientSpy` start count are identical before and after the re-queue.
    - Every landing and push in the case is production-issued; the test names which production event produces each observed push.
  </acceptance_criteria>
  <done>The unwithdrawn-floor defect is reproducible as a deterministic RED test that a single-flush landing could not observe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — the movement brackets itself; audit every exit path and the invariant doc</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift, AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadContinuedSessionTests.swift</files>
  <read_first>
    - `AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` — `advanceQueueIntentGeneration` (823-825) is the modification target; `queueIntentGeneration(for:)` (819-821) stays as is.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift` — `withdrawingCountedBasisMovement` (273-286): `rethrows`, non-async, reads `sessionCreditedPages(gid:)` on both sides; the sibling-composition rule at 262-268 is the composition contract every call site must be audited against.
    - `AppPackage/Sources/DownloadClient/DownloadClient+RetryHelpers.swift` — call sites `performRetry` (line 37) and `performRetryPages` (line 117).
    - `AppPackage/Sources/DownloadClient/DownloadClient+Scheduling.swift` — call site `resume` (line 360).
    - `AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift` — call site `enqueue` (line 107), which runs AFTER `writeInitialManifest`'s bracket (143-157) has closed, so a self-bracketing advance composes there as a sibling.
    - `AppPackage/Sources/DownloadClient/DownloadClient+ContinuedSession.swift` — the invariant doc at 230-236 and `reconcileRetiredSessionPages`' finished-pages ledger drop at 766-771: the interplay the regression must prove is covered.
  </read_first>
  <behavior>
    - `advanceQueueIntentGeneration(for:)` wraps its increment of `queueIntentGenerations[gid, default: 0]` in `withdrawingCountedBasisMovement(gid:)`, so the credited drop from `recorded` to 0 is measured across exactly the movement and withdrawn from `lastPushedCompletedPageCount`.
    - The bracket is a no-op (delta zero) for a gallery whose record reads incomplete or that holds a live `RunProgressBasis`, so the other three call sites change no observable behavior.
    - At all four call sites the bracket composes as a SIBLING — never nested inside `prepareWorkingSeed`'s bracket or `writeInitialManifest`'s bracket.
    - The one-at-a-time regression passes; the single-flush case and every existing continued-session suite stay green.
  </behavior>
  <action>
Wrap the increment inside `advanceQueueIntentGeneration` itself with `withdrawingCountedBasisMovement(gid:)` — the review's exact fix shape — and extend the function's doc comment to state WHY: the advance is a deliberate downward mover of the quantity `sessionCreditedPages` is summed from (generation mismatch drops a complete record's observation credit to zero), so the movement carries its own D-G7-01 bracket by construction and no call site can forget it. Then perform the all-exit-path audit: enumerate all four call sites (`performRetry`:37, `performRetryPages`:117, `resume`:360, `enqueue`:107) in the summary, each with a sibling-composition proof (which enclosing brackets exist on that path and why none spans the advance). If the RED test still shows a frozen first frame after the wrap — the retirement-ledger drop at `reconcileRetiredSessionPages`:769-771 is the candidate residual mover, where a re-queued gallery's `retiredSessionPages` entry vanishes while its live credit reads zero — bracket that residual movement at ITS single choke point too, and record it as a second enumerated mover; never weaken the test to pass. Re-audit the invariant enumeration at `ContinuedSession.swift:230-236` against post-fix source and rewrite any clause that is no longer literally true. Update the stale narrative inside `testARequeuedGalleryInheritsNoPredecessorObservationCredit`'s doc comment (lines 640-646): with the withdrawal in place the floor no longer needs six pages to clear, so the "discriminates nothing" prose must be re-derived (the case itself keeps its zero-credit boundary assertions unchanged).
  </action>
  <verify>
    <automated>xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'withdrawingCountedBasisMovement' AppPackage/Sources/DownloadClient/DownloadClient+Manager.swift` finds the advance's own bracket.
    - The summary enumerates all four call sites with a sibling-composition disposition each; none nests the advance inside another bracket.
    - The one-at-a-time regression is green: the first post-requeue keeper page moves the pushed numerator.
    - The existing single-flush re-queue case, ledger, run-proof, reconciliation, expiration, and identity suites stay green in the full-suite gate.
    - The invariant doc at `ContinuedSession.swift:230-236` reads true against source after the fix (checked clause by clause in the summary).
    - No SwiftLint suppression or concurrency escape hatch is introduced.
  </acceptance_criteria>
  <done>Every queue-intent advance withdraws its own credited drop by construction, and the regression discriminates the defect it was written for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue intent → session accounting basis | A user's re-queue moves the credited basis downward while the pushed floor persists. |
| session accounting → system scheduler | `ContinuedTaskScheduling`'s most-stalled expiration policy reads the pushed series as liveness evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-65-01 | Denial of Service | continued-processing progress card | high | mitigate | Self-bracketing advance withdraws the credited drop so genuine work is never absorbed; one-at-a-time regression pins the first post-requeue frame. |
| T-15-65-02 | Tampering | `lastPushedCompletedPageCount` floor | high | mitigate | The withdrawal is measured across exactly the movement inside one synchronous, non-suspending bracket; sibling composition audited at all four call sites so no delta is withdrawn twice. |
| T-15-65-03 | Repudiation | basis-movement diagnosis | low | accept | Process-local accounting; the regression banks the pre-fix frozen series and both generation values for diagnosis. |
</threat_model>

<verification>
Run, one invocation at a time (only ONE xcodebuild test invocation may run at a time on this machine):

1. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests/DownloadContinuedSessionTests`
2. `xcodebuild test -project EhPanda.xcodeproj -scheme EhPanda -testPlan FeatureTests -destination 'platform=iOS Simulator,name=iPhone Air'`
3. `xcodebuild -project EhPanda.xcodeproj -scheme EhPanda -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/EhPandaPhase1565DerivedData build`
</verification>

## Source Coverage Audit

Spec-less probe fallback: visible skip — this phase maps no requirement IDs; the scope contract is the four SC labels, and no probe predicates are generated.

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | SC2 | System progress reflects real download progress | 15-65 | COVERED | No frozen frames after a queue-intent advance; scheduler sees honest liveness. |
| REQ | — | No Phase 15 requirement IDs are mapped | — | EXCLUDED | SC labels are the phase contract. |
| CONTEXT | D-06 | One queue-wide session | 15-65 | COVERED | Regression holds session identity constant across the re-queue. |
| CONTEXT | D-10 | Completed/total pages represent current session work | 15-65 | COVERED | The withdrawal keeps the pushed numerator equal to the honest credited sum. |
| VERIFICATION | gap 1 / CR-01 | Unbracketed downward mover masked by the stale floor | 15-65 | COVERED | Movement wrapped at its choke point; all-exit-path sibling audit; one-at-a-time discriminating regression; invariant doc re-audited. |

<success_criteria>
- `advanceQueueIntentGeneration` encloses its own movement in the D-G7-01 bracket; no call site is patched individually.
- The first page of genuine work after a same-session re-queue moves the pushed numerator.
- The invariant doc's "deliberate movers are bracketed" claim is true against source.
- Full FeatureTests and the clean app build pass with serialized xcodebuild invocations.
</success_criteria>

<output>
Create `.planning/phases/15-continued-background-downloads/15-65-SUMMARY.md` when done.
</output>
