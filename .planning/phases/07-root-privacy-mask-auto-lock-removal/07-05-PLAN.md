---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - AppPackage/Sources/SearchFeature/SearchRootView.swift
  - AppPackage/Sources/SearchFeature/SearchView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
autonomous: true
requirements: [UIARCH-04]

must_haves:
  truths:
    - "No SearchFeature or DownloadsFeature view initializer takes a blurRadius parameter (UIARCH-04)."
    - "Every SearchFeature + DownloadsFeature root/modal surface applies .privacyMask() instead of .autoBlur(radius:)."
    - "TabBarView no longer passes blurRadius to SearchRootView or DownloadsView."
    - "DownloadsView still passes blurRadius: 0 into its ReadingView fullScreenCover (ReadingView keeps its param until 07-07)."
  artifacts:
    - path: "AppPackage/Sources/SearchFeature/SearchRootView.swift"
      provides: "SearchRootView with no blurRadius param"
    - path: "AppPackage/Sources/DownloadsFeature/DownloadsView.swift"
      provides: "DownloadsView with no blurRadius param, .privacyMask() sites"
  key_links:
    - from: "AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift"
      to: "AppPackage/Sources/SearchFeature/SearchRootView.swift"
      via: "SearchRootView(store:) — no blurRadius arg"
      pattern: "SearchRootView"
---

<objective>
Remove blurRadius drilling from SearchFeature (2 view types) and DownloadsFeature (2), swap their ~9
mask sites to .privacyMask(), and drop the two TabBarView args that fed them.

Purpose: The second per-module Part-A sweep. SearchFeature and DownloadsFeature are internally
self-consistent; their top-level views (SearchRootView, DownloadsView) are consumed only by
TabBarView, whose calls drop the vestigial blurRadius: 0 argument. DownloadsView presents a reading
fullScreenCover whose ReadingView still requires a blurRadius param until 07-07, so it keeps passing
the literal 0 there while shedding its own param.
Output: Search + Downloads free of their own blurRadius; their roots masked via .privacyMask().
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@AppPackage/Sources/SearchFeature/SearchRootView.swift
@AppPackage/Sources/DownloadsFeature/DownloadsView.swift
@AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip blurRadius + swap masks across SearchFeature; drop the TabBarView SearchRoot arg</name>
  <files>AppPackage/Sources/SearchFeature/SearchRootView.swift, AppPackage/Sources/SearchFeature/SearchView.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md "Enumeration 2" rows 8-9 (SearchRootView 13/17/20 internal 87/91 preview 263; SearchView 14/18/21 preview 109) and "Enumeration 1" SearchFeature rows (SearchRootView masks 44/57; SearchView masks 46/52/64)
    - AppPackage/Sources/SearchFeature/SearchRootView.swift, AppPackage/Sources/SearchFeature/SearchView.swift
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (SearchRootView call ~54-57 passing blurRadius: 0)
  </read_first>
  <action>
    SearchRootView + SearchView: delete the blurRadius stored property, init param, and assignment in
    each; replace .autoBlur(radius: blurRadius) at SearchRootView 44/57 and SearchView 46/52/64 with
    .privacyMask(); remove the intra-module blurRadius args (SearchRootView internal passes 87/91 to
    SearchView) and the blurRadius: 0 preview args (263, 109). TabBarView: remove the blurRadius: 0
    argument from the SearchRootView(store:) call. Read the root .swiftlint.yml; keep lines <= 120.
  </action>
  <verify>
    <automated>grep -rc 'blurRadius\|autoBlur' AppPackage/Sources/SearchFeature; grep -rc 'privacyMask()' AppPackage/Sources/SearchFeature</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'blurRadius' AppPackage/Sources/SearchFeature returns 0; grep -rc 'autoBlur' returns 0.
    - grep -rc 'privacyMask()' AppPackage/Sources/SearchFeature returns 5 (SearchRoot 44/57 + Search 46/52/64).
    - TabBarView's SearchRootView call no longer contains blurRadius.
  </acceptance_criteria>
  <done>SearchFeature is blurRadius-free with .privacyMask() masks; TabBarView no longer feeds it a blur.</done>
</task>

<task type="auto">
  <name>Task 2: Strip blurRadius + swap masks across DownloadsFeature; drop the TabBarView Downloads arg</name>
  <files>AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md "Enumeration 2" rows 10-11 (DownloadsView 14/18/21 internal 29/60/78 preview 320; +Subviews 16/20/23) and "Enumeration 1" DownloadsFeature rows (masks 63/70/81 + Subviews 107)
    - AppPackage/Sources/DownloadsFeature/DownloadsView.swift (the reading fullScreenCover ~78-81: it BOTH masks the content AND calls ReadingView(blurRadius:))
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (DownloadsView call ~58-62 passing blurRadius: 0)
  </read_first>
  <action>
    DownloadsView + DownloadsView+Subviews: delete the blurRadius stored property, init param, and
    assignment in each; replace .autoBlur(radius: blurRadius) at DownloadsView 63/70/81 and Subviews
    107 with .privacyMask(); remove the intra-module blurRadius args (DownloadsView internal passes
    29/60) and the blurRadius: 0 preview arg (320). IMPORTANT exception: the reading fullScreenCover
    calls ReadingView(...); ReadingView (ReadingFeature) still requires a blurRadius param until 07-07,
    so change that ONE ReadingView call to pass the literal blurRadius: 0 (do not remove the arg — that
    happens in 07-07). TabBarView: remove the blurRadius: 0 argument from the DownloadsView(store:)
    call. Read the root .swiftlint.yml; keep lines <= 120.
  </action>
  <verify>
    <automated>grep -rc 'autoBlur' AppPackage/Sources/DownloadsFeature; grep -rc 'privacyMask()' AppPackage/Sources/DownloadsFeature; cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'autoBlur' AppPackage/Sources/DownloadsFeature returns 0.
    - grep -rc 'privacyMask()' AppPackage/Sources/DownloadsFeature returns 4 (Downloads 63/70/81 + Subviews 107).
    - No DownloadsFeature initializer declares blurRadius; the ONLY remaining blurRadius token in DownloadsFeature is the literal blurRadius: 0 passed into the ReadingView fullScreenCover call.
    - TabBarView's DownloadsView call no longer contains blurRadius.
    - The whole AppFeature graph builds green.
  </acceptance_criteria>
  <done>DownloadsFeature masks self-source via .privacyMask(); only the transitional ReadingView blurRadius: 0 remains; graph builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | Search + Downloads root/modal surfaces (search roots + filter/quick-search sheets; downloads root + its two sheets + the reading fullScreenCover) must attach .privacyMask() or they leak. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Information Disclosure | SearchFeature + DownloadsFeature root/modal surfaces | high | mitigate | All 9 module mask sites are swapped to .privacyMask(); grep asserts 0 residual .autoBlur and the exact per-module .privacyMask() counts (5 Search + 4 Downloads). The reading fullScreenCover content is masked here; ReadingView's own reader-setting/share sheets are masked in 07-07. |
| T-07-12 | Information Disclosure | reader fullScreenCover from Downloads | high | mitigate | DownloadsView:81 (the reading cover) is swapped to .privacyMask(), so a backgrounded reader launched from Downloads is masked; the transitional blurRadius: 0 passed into ReadingView is inert (ReadingView's own masks self-source once 07-07 lands). |
</threat_model>

<verification>
- SearchFeature + DownloadsFeature: 0 autoBlur; 5 + 4 = 9 .privacyMask() sites; no own blurRadius params.
- Only transitional token left in DownloadsFeature is blurRadius: 0 into ReadingView.
- TabBarView: SearchRootView + DownloadsView calls have no blurRadius; SettingView + DetailView calls still pass blurRadius: 0.
- Whole graph builds green.
</verification>

<success_criteria>
- No SearchFeature/DownloadsFeature initializer takes its own blurRadius.
- Their root/modal surfaces self-source the mask via .privacyMask().
- Build green.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-05-SUMMARY.md` when done.
</output>
