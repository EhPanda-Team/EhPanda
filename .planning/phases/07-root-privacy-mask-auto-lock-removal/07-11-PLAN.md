---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 11
type: execute
wave: 9
depends_on: []
files_modified:
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
  - AppPackage/Sources/AppComponents/ViewModifiers.swift
  - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md
autonomous: true
requirements: [UIARCH-04]
gap_closure: true

must_haves:
  truths:
    - "The Download Inspector is masked exactly once: the single mask lives on its presented NavigationStack root (DownloadsView.swift:54); the nested duplicate inside DownloadInspectorView (DownloadsView+Subviews.swift:102) is removed, so the configured blur is composed once (closes verifier truth #3 / WR-02)."
    - "A checked-in one-to-one presentation-root inventory maps each of the 39 distinct runtime roots (app root + every separately-presented modal root) to exactly one privacyMask application site, and an executable audit asserts executable-mask-count == inventory-root-count (39) so coverage cannot be satisfied by duplicates."
    - "PrivacyMaskModifier reads the accessibilityReduceMotion environment value and disables the blur animation when Reduce Motion is on, preserving the true-zero/floorless blur (D-03) and the allowsHitTesting(blur < 1) guard (D-04) (closes WR-03)."
  artifacts:
    - path: ".planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md"
      provides: "one-to-one runtime presentation-root to mask-site inventory + audit invariant"
      contains: "privacyMask"
    - path: "AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift"
      provides: "DownloadInspectorView with the duplicate mask removed"
      contains: "DownloadInspectorView"
    - path: "AppPackage/Sources/AppComponents/ViewModifiers.swift"
      provides: "Reduce-Motion-aware PrivacyMaskModifier"
      contains: "accessibilityReduceMotion"
  key_links:
    - from: ".planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md"
      to: "AppPackage/Sources"
      via: "bijective audit: executable privacyMask applications == enumerated runtime roots"
      pattern: "privacyMask()"
---

<objective>
Close GAP-3 (verifier truth #3 / WR-02) and WR-03. The Download Inspector is currently masked twice —
once on its presented NavigationStack root (DownloadsView.swift:54) and again inside
DownloadInspectorView (DownloadsView+Subviews.swift:102) — composing the configured blur twice and
inflating the coverage audit to 40 applications for 39 distinct roots. Remove the nested duplicate,
then replace the count-only audit with an explicit one-to-one presentation-root inventory that cannot
be satisfied by duplicates. While in the privacy-mask modifier, make its animation respect Reduce
Motion (WR-03).

Purpose: The count-only "40 calls" audit from 07-08 can be satisfied by a duplicate. A bijective
inventory (39 runtime roots to 39 executable masks) makes a duplicate (40) or a gap (38) a hard audit
failure, and it is the durable form of the D-16 no-content-leak guarantee.
Output: A singly-masked Download Inspector, a checked-in one-to-one root inventory + executable audit,
and a Reduce-Motion-aware PrivacyMaskModifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-VERIFICATION.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-08-SUMMARY.md
@AppPackage/Sources/DownloadsFeature/DownloadsView.swift
@AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
@AppPackage/Sources/AppComponents/ViewModifiers.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Download Inspector nested duplicate mask</name>
  <files>AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadsFeature/DownloadsView.swift lines 48-55 (the `.sheet(item: ...inspector)` presenting `NavigationStack { DownloadInspectorView(store) }.privacyMask()` — the true presentation root, whose mask at line 54 is KEPT)
    - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift lines 11 and 85-114 (`struct DownloadInspectorView`; the trailing `.privacyMask()` at line 102 — the nested duplicate to remove)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md D-16 (each root applies the mask exactly once; sheets present in separate windows)
  </read_first>
  <action>
    Delete the single `.privacyMask()` modifier applied to DownloadInspectorView's body in
    DownloadsView+Subviews.swift (line 102). DownloadInspectorView is presented only inside the
    NavigationStack at DownloadsView.swift:52, whose `.privacyMask()` at line 54 is the presented-root
    mask and covers the whole modal including nav chrome — so removing the inner one leaves the inspector
    masked exactly once. Do NOT touch DownloadsView.swift:54 (keep it), and do not remove any other
    `.privacyMask()`. Preserve the surrounding `.toast`/`.navigationTitle`/`.toolbar`/`.onAppear` chain
    order (only the `.privacyMask()` line is removed). Confirm DownloadInspectorView has no other
    presentation path that would leave it unmasked (grep confirms one call site).
  </action>
  <verify>
    <automated>grep -c 'privacyMask()' AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift; grep -c 'privacyMask()' AppPackage/Sources/DownloadsFeature/DownloadsView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'privacyMask()' AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift` returns 0 (the nested duplicate is gone).
    - `grep -c 'privacyMask()' AppPackage/Sources/DownloadsFeature/DownloadsView.swift` still returns 3 (the inspector NavigationStack, folderManager, and reader masks are intact).
    - Total executable `.privacyMask()` applications across AppPackage/Sources drops from 40 to 39.
  </acceptance_criteria>
  <done>The Download Inspector is masked exactly once, on its presented NavigationStack root.</done>
</task>

<task type="auto">
  <name>Task 2: Author the one-to-one presentation-root inventory + bijective audit</name>
  <files>.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-08-SUMMARY.md "Forty-Site Coverage Audit" table (the by-module 40-site breakdown — the starting inventory; the Download Inspector duplicate is the site removed in Task 1)
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (the app root mask + the newDawn/setting/detail app-level modal roots)
    - every source file with a `.sheet(`/`.fullScreenCover(` presentation modifier (41 modifiers total across Sources) and every `.privacyMask()` application, to reconcile presentation modifiers to distinct runtime roots to mask sites
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md D-16 (the no-content-leak audit mandate: app root + every modal root masked exactly once)
  </read_first>
  <action>
    Create a checked-in inventory enumerating the 39 distinct runtime roots (the app root plus every
    separately-presented sheet/full-screen-cover modal root) and map each to exactly one `.privacyMask()`
    application site (file + line + a short root description). Reconcile every `.sheet(`/
    `.fullScreenCover(` presentation modifier in Sources to either a masked runtime root (with its mask
    site) OR an explicit exclusion with a reason (e.g. a system share/activity sheet presenting
    `ActivityView`/`UIActivityViewController`, which renders system UI, not app content) — so no
    presentation modifier is unaccounted, explaining why the 41 presentation modifiers resolve to 38
    modal roots + 1 app root = 39 masked roots. State the bijective audit invariant: the count of
    executable `.privacyMask()` applications in AppPackage/Sources (excluding the `func privacyMask()`
    declaration in ViewModifiers.swift and the doc token in AppSharedKeys.swift) MUST equal the number of
    enumerated inventory roots, and that number is 39 — DERIVED from the root enumeration, not a
    hard-coded magic total. Include a per-file expected-mask-count table and embed the re-runnable shell
    audit. Follow the "No absolute home paths in generated docs" rule — use only repository-relative
    paths in the inventory.
  </action>
  <verify>
    <automated>MASKS=$(grep -rn 'privacyMask()' AppPackage/Sources | grep -v 'func privacyMask' | grep -v 'AppSharedKeys' | wc -l | tr -d ' '); echo "executable masks=$MASKS (must be 39)"; test -f .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md && echo "inventory present"</automated>
  </verify>
  <acceptance_criteria>
    - The inventory file exists and enumerates exactly 39 runtime roots, each mapped to one `.privacyMask()` site (file:line + description).
    - Executable `.privacyMask()` applications across AppPackage/Sources (excluding the func declaration and the AppSharedKeys doc token) == 39, and the inventory states this equality is derived from the root count (not a hard-coded literal).
    - Every `.sheet(`/`.fullScreenCover(` presentation modifier in Sources is reconciled in the inventory to a masked root or an explicit exclusion reason.
    - The inventory contains no absolute home path (`/Users/...` or `/home/...`).
  </acceptance_criteria>
  <done>A bijective 39-root ↔ 39-mask inventory exists; a duplicate (40) or a gap (38) is now a hard audit failure.</done>
</task>

<task type="auto">
  <name>Task 3: Make PrivacyMaskModifier respect Reduce Motion (WR-03)</name>
  <files>AppPackage/Sources/AppComponents/ViewModifiers.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/ViewModifiers.swift lines 9-19 (`PrivacyMaskModifier`: `@SharedReader(.privacyMaskBlur) private var blur`; `content.animation(.linear(duration: 0.1)) { $0.blur(radius: blur) }.allowsHitTesting(blur < 1)`)
    - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift line 13 and AppPackage/Sources/HomeFeature/GalleryCardCell.swift line 12 (the established `@Environment(\.accessibilityReduceMotion) private var reduceMotion` read pattern to mirror)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md D-03 (true-zero, no `max(0.00001, radius)` floor) and D-04 (keep `allowsHitTesting(blur < 1)`)
    - the swift-accessibility-skill (Reduce Motion: pass a nil animation to disable motion) and pfw-modern-swiftui skills
  </read_first>
  <action>
    Add `@Environment(\.accessibilityReduceMotion) private var reduceMotion` to `PrivacyMaskModifier`
    and gate the animation on it: when `reduceMotion` is on, apply the blur WITHOUT the linear animation
    (pass a nil animation to the scoped `.animation(_:_:)` so the blur value updates without an animated
    transition); when off, keep the existing `.linear(duration: 0.1)` scoped to the blur transform.
    Preserve D-03 (the blur value is the true, floorless `blur` from `@SharedReader(.privacyMaskBlur)`)
    and D-04 (`.allowsHitTesting(blur < 1)` unchanged). Do not reintroduce any blur floor. Prefer the
    modern SwiftUI value-scoped `.animation(reduceMotion ? nil : .linear(duration: 0.1)) { $0.blur(...) }`
    form over deprecated whole-view animation. No SwiftLint suppressions; conform to the root
    `.swiftlint.yml`.
  </action>
  <verify>
    <automated>grep -c 'accessibilityReduceMotion' AppPackage/Sources/AppComponents/ViewModifiers.swift; grep -c 'max(0.00001' AppPackage/Sources/AppComponents/ViewModifiers.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'accessibilityReduceMotion' AppPackage/Sources/AppComponents/ViewModifiers.swift` returns >= 1.
    - The `.animation(...)` is disabled (nil animation) when `reduceMotion` is true and keeps `.linear(duration: 0.1)` when false.
    - `.allowsHitTesting(blur < 1)` is retained and no `max(0.00001` blur floor is introduced (`grep -c 'max(0.00001' ...` returns 0).
    - The AppPackage-Package build succeeds with SwiftLint and zero violations.
  </acceptance_criteria>
  <done>The privacy-mask blur animation is suppressed under Reduce Motion while the true-zero blur and hit-testing guard are preserved.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols/artifacts introduced by this plan (symbols already created by 07-01…07-08 are NOT new):
- `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PRIVACY-MASK-INVENTORY.md` — the one-to-one runtime presentation-root ↔ mask-site inventory + bijective audit invariant (replaces the count-only 40-call audit).
- A new `@Environment(\.accessibilityReduceMotion) private var reduceMotion` read + Reduce-Motion branch inside `PrivacyMaskModifier`.
- No new production top-level symbols (Task 1 removes a modifier application; Task 3 adds an environment read inside the existing modifier).
</artifacts_this_phase_produces>

<verification>
- DownloadsView+Subviews.swift has 0 `.privacyMask()`; the inspector is masked once on its NavigationStack root.
- The inventory enumerates 39 roots ↔ 39 executable masks; the executable-mask count == 39 and is derived from the root enumeration.
- PrivacyMaskModifier reads `accessibilityReduceMotion` and disables the animation under Reduce Motion; no blur floor; hit-testing guard intact.
- Run xcodebuild single-invocation only — never launch two `xcodebuild` runs concurrently on this machine.
</verification>

<success_criteria>
- Verifier truth #3 flips to VERIFIED: each runtime root has exactly one mask and the audit cannot be satisfied by duplicates.
- WR-03 is resolved: the mask animation respects Reduce Motion.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-11-SUMMARY.md` when done.
</output>
