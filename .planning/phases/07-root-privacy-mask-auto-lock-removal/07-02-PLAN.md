---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - AppPackage/Sources/AppModels/Persistent/Setting.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingReducer.swift
  - AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift
autonomous: true
requirements: [UIARCH-04, UIARCH-05]

must_haves:
  truths:
    - "Setting.backgroundBlurRadius is renamed to Setting.privacyMaskIntensity, a plain var defaulting to 10 with NO didSet (D-11/D-13/D-14)."
    - "Setting.autoLockPolicy and the AutoLockPolicy enum + its value extension are gone (D-12); no VersionedSchema v2 / schemaVersion bump."
    - "AppReducer.onScenePhaseChange writes @Shared(.privacyMaskBlur) = privacyMaskIntensity on .inactive and = 0 on .active (D-05/D-07)."
    - "The became-active branch reproduces the launch greeting + conditional clipboard detection that used to ride .appLock(.unlockApp), with clipboard detection firing exactly once at cold launch (D-06, Pitfall 1)."
    - "GeneralSettingView no longer has a Security section: the auto-lock Picker is removed outright with NO replacement description or iOS-lock pointer (D-08, overriding UIARCH-05 criterion 3), the now-empty section is removed along with the passcode warning + blur Slider (D-09), and GeneralSettingReducer no longer imports/uses AuthorizationClient."
    - "AppearanceSettingView shows a Privacy Mask slider (0...100, step 10, eye/eye-slash) under the tint-color row, with a footer, bound to $setting.privacyMaskIntensity (D-10) — no reducer action added."
  artifacts:
    - path: "AppPackage/Sources/AppModels/Persistent/Setting.swift"
      provides: "privacyMaskIntensity property; AutoLockPolicy removed"
      contains: "privacyMaskIntensity"
    - path: "AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift"
      provides: "scenePhase fold writing the shared blur"
      contains: "privacyMaskBlur"
    - path: "AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift"
      provides: "the relocated Privacy Mask control"
      contains: "privacyMaskIntensity"
  key_links:
    - from: "AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift"
      to: "AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift"
      via: "onScenePhaseChange writes @Shared(.privacyMaskBlur)"
      pattern: "privacyMaskBlur"
    - from: "AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift"
      to: "AppPackage/Sources/AppModels/Persistent/Setting.swift"
      via: "Slider Binding($setting.privacyMaskIntensity)"
      pattern: "privacyMaskIntensity"
---

<objective>
The Part-B behavioral core: rename the persisted blur property and delete the auto-lock policy from
the model, fold AppLockReducer's two blur writes + the on-unlock launch effects into
AppReducer.onScenePhaseChange, remove the Security section from General settings, and relocate the
blur control to Appearance as "Privacy Mask". This is the one non-mechanical plan in the phase —
Pitfall 1 (exactly-once launch greeting/clipboard) lives here.

Purpose: After 07-01 created the mechanism, this plan makes the shared blur value LIVE (written by
the scenePhase fold) and moves the user-facing control, while removing the auto-lock policy from the
model. AppLockReducer + the AuthorizationClient module are left in place but DORMANT (no longer
sent onBecome* actions) so the whole graph keeps building green; their deletion is 07-03. Feature-
module masks stay on .autoBlur (temporarily reading a dormant 0) until their per-module swap plans.
Output: renamed model property; auto-lock policy gone; a live scenePhase blur writer; General's
Security section removed; a Privacy Mask control on the Appearance page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md
@AppPackage/Sources/AppModels/Persistent/Setting.swift
@AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
@AppPackage/Sources/AppFeature/DataFlow/AppLockReducer.swift
@AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
@AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingReducer.swift
@AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename blur property + delete AutoLockPolicy in the Setting model</name>
  <files>AppPackage/Sources/AppModels/Persistent/Setting.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Persistent/Setting.swift (init params 16-17 + assigns 41-42; the coupled didSet block 92-110; the AutoLockPolicy enum 201-211 + value extension 213-226)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Setting.swift edits" + Pitfall 2 (delete property + didSet + enum atomically)
    - AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift lines 30-49 (the whole-struct decode / no-migration comment — do NOT bump schemaVersion)
  </read_first>
  <behavior>
    - A default-constructed Setting has privacyMaskIntensity == 10.
    - Setting has no autoLockPolicy member and there is no AutoLockPolicy type.
    - privacyMaskIntensity can be set to 0 independently (no didSet resets it or any other field).
  </behavior>
  <action>
    In Setting.swift: (1) rename the init param backgroundBlurRadius (line 16, default 10) to
    privacyMaskIntensity and its assignment (line 41); (2) delete the autoLockPolicy init param (17)
    and its assignment (42); (3) replace the coupled property block 92-110 — rename
    backgroundBlurRadius to privacyMaskIntensity as a plain `public var privacyMaskIntensity: Double =
    10` with NO didSet (D-13), and delete the autoLockPolicy property + its didSet entirely, plus the
    explanatory coupling comment above them; (4) delete the AutoLockPolicy enum (201-211) and its
    value extension (213-226). Keep schemaVersion at 1 and the schemas array at [SchemaV1.self] — NO
    v2, NO migration (D-12/D-14). The old persisted backgroundBlurRadius/autoLockPolicy keys are
    ignored on decode; privacyMaskIntensity falls back to 10 — accepted pre-release. Do NOT touch the
    scale-factor didSet clamps (131-144) or any other property. This breaks AppReducer +
    GeneralSettingView references until their tasks below fix them — that is expected; the plan builds
    green only at its end.
  </action>
  <verify>
    <automated>grep -c 'AutoLockPolicy\|autoLockPolicy\|backgroundBlurRadius' AppPackage/Sources/AppModels/Persistent/Setting.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'privacyMaskIntensity' Setting.swift returns >= 2 (property + init assign).
    - grep -c 'AutoLockPolicy\|autoLockPolicy\|backgroundBlurRadius' Setting.swift returns 0.
    - grep -c 'didSet' on the privacyMaskIntensity property region returns 0 (plain var).
    - schemaVersion is still 1 and schemas is still [SchemaV1.self] (no migration added).
  </acceptance_criteria>
  <done>Setting has a plain privacyMaskIntensity (default 10) and no auto-lock policy; no schema bump.</done>
</task>

<task type="auto">
  <name>Task 2: Fold the two blur writes + launch effects into AppReducer.onScenePhaseChange (Pitfall 1)</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (onScenePhaseChange 82-136; the .appLock(.unlockApp) case 176-183; loadUserSettingsDone 280-297)
    - AppPackage/Sources/AppFeature/DataFlow/AppLockReducer.swift (the two writes being folded: onBecomeInactive sets blur; unlockApp sets blur 0 — the semantics to reproduce)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "AppReducer.swift edits" table + Pitfall 1 (the exactly-once launch trace) + Open Question 2
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md section "scenePhase -> shared-blur write"
    - the pfw-sharing skill (writing an @Shared(.inMemory) value from a reducer via withLock)
  </read_first>
  <action>
    Fold AppLockReducer's blur writes + the on-unlock launch effects into onScenePhaseChange, and stop
    driving AppLockReducer. Concretely:
    (a) Give AppReducer write access to the shared blur — hold @Shared(.privacyMaskBlur) var
    privacyMaskBlur in AppReducer.State (mirrors how shared state is held) and write it via
    state.$privacyMaskBlur.withLock per the pfw-sharing skill.
    (b) .inactive branch (110-112): remove the .send(.appLock(.onBecomeInactive(...))); instead write
    the shared blur = state.settingState.setting.privacyMaskIntensity, return .none. Blur is written
    on .inactive BEFORE .background (D-07) so the App Switcher snapshot is already masked.
    (c) .active branch (87-108): remove the threshold/backgroundBlurRadius reads and the
    .send(.appLock(.onBecomeActive(...))); write the shared blur = 0 (D-05); KEEP the existing
    appLogsPump.startPump, the "entered foreground" log, and the hasEnteredBackground reconcile.
    ADD the re-homed launch effects that used to run off .appLock(.unlockApp): .send(.setting(
    .fetchGreeting)) and, if setting.detectsLinksFromClipboard, .send(.appRoute(.detectClipboardURL))
    (D-06).
    (d) Delete the case .appLock(.unlockApp) block (176-183) now that its effects are re-homed. Keep a
    case .appLock: return .none catch-all (185-186) so AppLockReducer stays scoped-but-dormant this
    plan (deleted in 07-03).
    (e) loadUserSettingsDone (280-297): drop the threshold >= 0 / becameInactiveDate /
    .onBecomeActive block; KEEP the detectClipboardURL send (288-290) and the launch-automation
    effects. Rename the backgroundBlurRadius read (283) to privacyMaskIntensity or delete it with the
    dropped block.
    EXACTLY-ONCE (Pitfall 1 — REQUIRED trace): onScenePhaseChange returns .none until
    settingState.hasLoadedInitialSetting is true, and loadUserSettingsDone is what sets it true and
    independently runs detectClipboardURL. Trace the cold-launch ordering and ensure clipboard
    detection fires exactly once at cold launch (not once from the became-active branch AND once from
    loadUserSettingsDone). Decide the single launch owner of clipboard detection and document the
    decision in a code comment; the elected TestStore test in 07-08 guards this. Preserve the greeting
    behavior (greeting still appears on cold launch). Do NOT yet delete appLockState / the appLock
    action case / the Scope — those are 07-03 (removing them here would break TabBarView).
  </action>
  <verify>
    <automated>grep -c 'privacyMaskBlur\|privacyMaskIntensity' AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift; grep -c 'onBecomeActive\|onBecomeInactive\|unlockApp\|autoLockPolicy\|backgroundBlurRadius' AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift</automated>
  </verify>
  <acceptance_criteria>
    - The .inactive branch writes the shared blur = privacyMaskIntensity; the .active branch writes it = 0.
    - grep -c 'onBecomeActive\|onBecomeInactive\|unlockApp' AppReducer.swift returns 0 (folded/removed).
    - grep -c 'autoLockPolicy\|backgroundBlurRadius' AppReducer.swift returns 0 (model refs updated).
    - The became-active branch sends .setting(.fetchGreeting) and conditionally .appRoute(.detectClipboardURL); loadUserSettingsDone still handles launch-automation.
    - A code comment records the single owner of launch-time clipboard detection (Pitfall 1).
    - appLockState / case appLock / the Scope still exist (dormant; deleted in 07-03).
  </acceptance_criteria>
  <done>onScenePhaseChange is the sole writer of the shared blur and re-homes greeting/clipboard with exactly-once launch semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Remove General's Security section + AuthorizationClient use; add the Appearance Privacy Mask control</name>
  <files>AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift, AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingReducer.swift, AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift (Section(.security) 107-130; the onAppear .checkPasscodeSetting 155)
    - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingReducer.swift (import AuthorizationClient 5; import LocalAuthentication 1; passcodeNotSet 45; case checkPasscodeSetting 63; @Dependency authorizationClient 69; handler 145-147)
    - AppPackage/Sources/SettingFeature/AppearanceSetting/AppearanceSettingView.swift (the first header-less Section; tint-color ColorPicker at line 30)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md section "AppearanceSettingView.swift — the relocation target" (slider mechanics analog + footer analog) and "AppearanceSettingReducer.swift — no change needed"
  </read_first>
  <action>
    GeneralSettingView.swift: delete the entire Section(.security) (107-130) — the auto-lock Picker,
    the passcodeNotSet warning triangle, AND the blur VStack/Slider (the slider RELOCATES, it is not
    duplicated). Delete the .checkPasscodeSetting send from .onAppear (155); keep the
    .calculateWebImageDiskCache send.
    GeneralSettingReducer.swift: delete import AuthorizationClient (5), import LocalAuthentication (1),
    the passcodeNotSet state (45), the checkPasscodeSetting action case (63), the @Dependency
    authorizationClient (69), and its handler (145-147). Keep navigateToSystemSetting / applicationClient
    (unrelated). AuthorizationClient the MODULE still exists (deleted in 07-03) — this task only stops
    GeneralSettingReducer importing it.
    AppearanceSettingView.swift: add a Privacy Mask control directly under the tint-color row (D-10).
    Use a dedicated Section right after the first (header-less) section with a `footer:` (so the
    required footer has a native home): a VStack(alignment: .leading) containing Text(.privacyMask)
    then an HStack of Image(systemSymbol: .eye), Slider(value: Binding($setting.privacyMaskIntensity),
    in: 0...100, step: 10), Image(systemSymbol: .eyeSlash); the Section footer is Text(.privacyMaskFooter).
    Keep the slider mechanics (0...100, step 10, eye/eye-slash) exactly per the GeneralSettingView
    analog. The @Shared(.setting) is already declared at AppearanceSettingView:10; the write goes
    straight into @Shared(.setting) — add NO reducer action and NO change to AppearanceSettingReducer
    (per its own comment: a shared-state write dispatches no action). Give the slider a proper
    accessibility label per the swift-accessibility-skill (VoiceOver reads "Privacy Mask").
  </action>
  <verify>
    <automated>cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'Section(.security)\|checkPasscodeSetting\|passcodeNotSet\|AuthorizationClient\|LocalAuthentication' GeneralSettingView.swift + GeneralSettingReducer.swift returns 0.
    - grep -c 'privacyMaskIntensity' AppearanceSettingView.swift returns 1 (the Slider binding).
    - grep -c 'Text(.privacyMask)\|Text(.privacyMaskFooter)' AppearanceSettingView.swift returns 2.
    - AppearanceSettingReducer.swift is unchanged (no new action).
    - The whole AppFeature graph builds green (AppLockReducer dormant, AuthorizationClient module still present).
  </acceptance_criteria>
  <done>General has no Security section, GeneralSettingReducer no longer uses AuthorizationClient, and Appearance shows the Privacy Mask control; graph builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | The scenePhase fold is the SOLE writer of the mask value; if it fails to write on .inactive, the App Switcher snapshot leaks. |
| removed custom biometric lock -> OS per-app lock | Re-auth responsibility transfers from the deleted AuthorizationClient path to iOS built-in Require Face ID. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Information Disclosure | onScenePhaseChange blur write timing | high | mitigate | Blur is written on .inactive BEFORE .background (D-07), so the snapshot iOS captures on backgrounding is already masked. Verified by the elected TestStore test in 07-08 (.inactive -> privacyMaskBlur == privacyMaskIntensity). |
| T-07-04 | Repudiation / Denial | launch greeting + clipboard exactly-once (Pitfall 1) | medium | mitigate | Trace the cold-launch scenePhase/settings-load ordering; document + enforce a single launch owner of clipboard detection so it fires exactly once; guarded by the 07-08 TestStore test. On a primary launch path, so mitigated not accepted. |
| T-07-05 | Elevation of Privilege | removal of custom biometric auto-lock (UIARCH-05) | low | accept | iOS built-in per-app lock (Require Face ID, universal on the iOS 26 floor) owns re-auth; the privacy mask/blur is retained. Residual (no in-app re-auth) is accepted per D-08 — an intentional posture change, not a regression. Orphaned biometric entitlement is removed in 07-03. |
</threat_model>

<verification>
- Setting has a plain privacyMaskIntensity (default 10) and no AutoLockPolicy/autoLockPolicy; schemaVersion unchanged.
- AppReducer writes the shared blur on inactive (= intensity) and active (= 0); greeting/clipboard re-homed; no onBecome*/unlockApp remain.
- GeneralSettingView has no Security section; GeneralSettingReducer no longer imports/uses AuthorizationClient.
- AppearanceSettingView shows the Privacy Mask slider + footer under the tint row; AppearanceSettingReducer unchanged.
- The whole package graph builds green (AppLockReducer + AuthorizationClient module still present but dormant/unreferenced-from-GeneralSetting).
</verification>

<success_criteria>
- Model: privacyMaskIntensity (plain, default 10); AutoLockPolicy deleted; no migration.
- Fold: onScenePhaseChange is the sole shared-blur writer with re-homed launch effects, exactly-once clipboard.
- Settings: General Security section removed; Privacy Mask control on Appearance under tint, bound to privacyMaskIntensity.
- Build green.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-02-SUMMARY.md` when done.
</output>
