---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 09
type: execute
wave: 9
depends_on: []
files_modified:
  - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
  - AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift
autonomous: true
requirements: [UIARCH-04]
gap_closure: true

must_haves:
  truths:
    - "onScenePhaseChange writes the privacy mask on .inactive (= privacyMaskIntensity) and .active (= 0) and sets the background latch on .background EVEN WHEN hasLoadedInitialSetting is false, so the App-Switcher snapshot is masked on a cold-launch race (closes verifier truth #4 / CR-01)."
    - "A TestStore regression starting with hasLoadedInitialSetting = false drives .inactive then .background and asserts privacyMaskBlur is written and hasEnteredBackground is latched."
    - "The settings-dependent foreground/background side effects (greeting, clipboard, pumps, reconcile, reading flush) stay gated behind hasLoadedInitialSetting (D-05/D-06 semantics preserved)."
  artifacts:
    - path: "AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift"
      provides: "settings-independent privacy-mask writes + background latch in onScenePhaseChange"
      contains: "privacyMaskBlur"
    - path: "AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift"
      provides: "parameterizable makeStore helper + pre-settings-load regression test"
      contains: "hasLoadedInitialSetting"
  key_links:
    - from: "AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift"
      to: "AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift"
      via: "TestStore over AppReducer asserting the pre-settings shared-blur write + latch"
      pattern: "privacyMaskBlur"
---

<objective>
Close GAP-1 (the phase BLOCKER, verifier truth #4 / CR-01): make the safety-critical scene-phase
privacy writes and the background latch independent of `hasLoadedInitialSetting`, so a scene
transition before settings finish loading still masks the App-Switcher snapshot. Add the pre-settings
TestStore regression the current helper cannot express.

Purpose: This is a content-confidentiality defect. Today the `guard state.settingState
.hasLoadedInitialSetting else { return .none }` at AppReducer.swift:83 returns before the `.inactive`
mask write and the `.background` latch, leaving `privacyMaskBlur` at its launch default of `0` — the
App-Switcher snapshot can show readable content on a cold-launch race.
Output: An AppReducer whose `.inactive`/`.active` mask writes and `.background` latch always run, with
the settings-dependent side effects still gated, plus an exhaustive pre-settings regression proving it.
</objective>

<assumption_delta_decision>
Decision: **no-change**. Primary noun (the scene-phase privacy-mask model) is unchanged. The
deterministic assumption-delta detector returned `detected: true` on a single `chosen`-family signal,
but it is a false positive: it matched the word "custom" in the phase goal ("remove the **custom**
auto-lock in favor of iOS's built-in per-app lock"). This phase REMOVES a feature; nothing is
generalized, parameterized, or gaining a second variant. This gap-closure round changes no identity
model whatsoever — it fixes a reducer guard, test exhaustivity, a duplicate modifier, an accessibility
read, a Package dependency, and doc wording. No singular→plural / required→optional / derived→chosen
transition occurred. No invariant test needed.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-VERIFICATION.md
@AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
@AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the scene-phase privacy writes + background latch settings-independent</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift lines 76-138 (the `.onScenePhaseChange` case: the `state.scenePhase = scenePhase` assignment, the `guard state.settingState.hasLoadedInitialSetting else { return .none }` at line 83, and the inner switch: `.active` writes `state.$privacyMaskBlur.withLock { $0 = 0 }` + greeting/startPump/logger/clipboard/reconcile effects; `.inactive` reads `state.settingState.setting.privacyMaskIntensity` and writes it; `.background` sets `state.hasEnteredBackground = true` + pausePump/schedule/readingFlush effects)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift lines 28-34 (State: `@Shared(.privacyMaskBlur) var privacyMaskBlur`, `var scenePhase`, `var hasEnteredBackground`)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md decisions D-05, D-06, D-07 (inactive → shared blur = privacyMaskIntensity; active → 0; on-active side effects re-homed; blur on .inactive for the no-leak guarantee)
    - the pfw-composable-architecture skill (reducer structure; ordering state mutations before a guard)
  </read_first>
  <action>
    Restructure the `.onScenePhaseChange(let scenePhase)` handler so the safety-critical writes run
    BEFORE the `hasLoadedInitialSetting` guard, and only the settings-dependent side effects stay
    behind it. Keep the leading `state.scenePhase = scenePhase`. Then, unconditionally (independent of
    `hasLoadedInitialSetting`): on `.active` write `state.$privacyMaskBlur.withLock { $0 = 0 }`; on
    `.inactive` read `state.settingState.setting.privacyMaskIntensity` and write it into
    `state.$privacyMaskBlur`; on `.background` set `state.hasEnteredBackground = true`. AFTER those
    writes, keep the existing `guard state.settingState.hasLoadedInitialSetting else { return .none }`
    and, behind it, keep exactly the current settings-dependent effects unchanged: the `.active`
    greeting (`.setting(.fetchGreeting)`), `.appLogsPump(.startPump)`, the foreground logger note, the
    conditional `.appRoute(.detectClipboardURL)` (when `detectsLinksFromClipboard`), and the
    `hasEnteredBackground`-consuming `downloadClient.reconcileDownloads()` reconcile; the `.background`
    `.appLogsPump(.pausePump)`, the background logger/`backgroundProcessingClient.schedule()` run
    effect, and `readingFlushEffects(state)`. Do NOT move the latch-CONSUMING reconcile out of the gate
    (only the latch SET moves out). Preserve `default: return .none`. Result: the mask is always written
    on inactive/active and the background is always latched, while greeting/clipboard/pumps/reconcile/
    flush remain gated on loaded settings. No SwiftLint suppressions; conform to the root `.swiftlint.yml`.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - In AppReducer.swift, the `.inactive` write of `state.$privacyMaskBlur` (= `privacyMaskIntensity`), the `.active` write (= 0), and the `.background` set of `state.hasEnteredBackground = true` all appear BEFORE the `guard state.settingState.hasLoadedInitialSetting` line within the `.onScenePhaseChange` case.
    - The greeting/`fetchGreeting`, `detectClipboardURL`, `startPump`, `pausePump`, `reconcileDownloads`, and `readingFlushEffects` effects remain positioned AFTER (behind) the `hasLoadedInitialSetting` guard.
    - The AppPackage-Package build succeeds with SwiftLint enabled and zero violations (no `swiftlint:disable`).
  </acceptance_criteria>
  <done>The scene-phase mask writes and background latch are settings-independent; the settings-dependent side effects stay gated; the package builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize the test helper + add the pre-settings-load regression</name>
  <files>AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift</files>
  <behavior>
    - With hasLoadedInitialSetting = false and privacyMaskIntensity = 40: sending .onScenePhaseChange(.inactive) sets scenePhase = .inactive AND writes privacyMaskBlur = 40 (mask written before settings load).
    - Then sending .onScenePhaseChange(.background) sets scenePhase = .background AND hasEnteredBackground = true (latch set before settings load).
    - No settings-dependent effect fires on either transition while hasLoadedInitialSetting is false (the guard returns .none for effects), so the transition is provable exhaustively with no `receive`.
  </behavior>
  <read_first>
    - AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift (the full file: the `@Suite(.serialized)` struct, the two existing tests, and the private `makeStore(detectsLinksFromClipboard:privacyMaskIntensity:)` helper that hard-codes `initialState.settingState.hasLoadedInitialSetting = true`)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (the restructured `.onScenePhaseChange` from Task 1, to mirror exact state mutations)
    - the pfw-testing + swift-testing-pro skills (TestStore state-mutation closures, exhaustive assertions, `.serialized` suites) and CLAUDE.md/AGENTS.md (Swift Testing not XCTest; construct the store with `reducer: AppReducer.init` per the SwiftLint child-reducer-shorthand rule)
  </read_first>
  <action>
    Add a `hasLoadedInitialSetting: Bool = true` parameter to the private `makeStore` helper and use it
    to set `initialState.settingState.hasLoadedInitialSetting`, defaulting to `true` so the two existing
    tests are unaffected. Add one new `@Test` (e.g. `maskAndLatchAreWrittenBeforeSettingsLoad`) that
    builds a store via `makeStore(detectsLinksFromClipboard: true, privacyMaskIntensity: 40,
    hasLoadedInitialSetting: false)`, then exhaustively (NO `withExhaustivity(.off)`) sends
    `.onScenePhaseChange(.inactive)` asserting the state closure sets `scenePhase = .inactive` and
    `$privacyMaskBlur.withLock { $0 = 40 }`, then sends `.onScenePhaseChange(.background)` asserting
    `scenePhase = .background` and `hasEnteredBackground = true`, and finishes with no `receive` (the
    pre-settings guard emits no effects). Keep the store on the injected noop/test dependencies already
    used by the helper. Do not modify the two existing tests beyond the helper signature default.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AppFeatureTests/AppReducerScenePhaseTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `makeStore` accepts `hasLoadedInitialSetting: Bool = true` and applies it to `initialState.settingState.hasLoadedInitialSetting`.
    - A new test drives `.inactive` then `.background` with `hasLoadedInitialSetting: false` and asserts `privacyMaskBlur == 40` after `.inactive` and `hasEnteredBackground == true` after `.background`.
    - The new test uses no `withExhaustivity(.off)` and issues no `receive` (proving no gated effect fires pre-settings).
    - `xcodebuild test -scheme AppPackage-Package -only-testing:AppFeatureTests/AppReducerScenePhaseTests` exits 0 with all AppReducerScenePhaseTests passing.
  </acceptance_criteria>
  <done>The launch-race is now testable and green: the pre-settings inactive→background path provably writes the mask and latches background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground → app switcher / background snapshot | Untrusted observation surface: iOS captures a snapshot of app content for the App Switcher on every inactive/background transition, including on a cold-launch race before `hasLoadedInitialSetting` becomes true. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-20 | Information Disclosure | `AppReducer.onScenePhaseChange` pre-settings guard (AppReducer.swift:83) | high | mitigate | Move the `.inactive`/`.active` `privacyMaskBlur` writes and the `.background` latch OUTSIDE the `hasLoadedInitialSetting` gate so the App-Switcher snapshot is always masked (Task 1); prove it with a pre-settings TestStore regression asserting the mask write + latch (Task 2). Block-on-high: the phase cannot pass while the launch race can leave the snapshot unmasked. |
</threat_model>

<artifacts_this_phase_produces>
New symbols/artifacts introduced by this plan (symbols already created by 07-01…07-08 are NOT new):
- `makeStore(...)` gains a new `hasLoadedInitialSetting: Bool = true` parameter (test helper signature change).
- New `@Test func maskAndLatchAreWrittenBeforeSettingsLoad()` (or equivalently named) in AppReducerScenePhaseTests.swift.
- No new production symbols — Task 1 only reorders existing logic within `onScenePhaseChange`.
</artifacts_this_phase_produces>

<verification>
- AppReducer.swift: inactive/active mask writes + background latch precede the `hasLoadedInitialSetting` guard; gated effects follow it.
- AppReducerScenePhaseTests.swift: the pre-settings regression proves the mask write (40) and latch (true) with no gated effects.
- The focused suite `AppFeatureTests/AppReducerScenePhaseTests` runs green; the AppPackage-Package build is clean with SwiftLint.
- Run xcodebuild single-invocation only — never launch two `xcodebuild test`/`build` runs concurrently on this machine.
</verification>

<success_criteria>
- Verifier truth #4 flips to VERIFIED: every inactive/background transition masks content before the App-Switcher snapshot, including before settings initialization completes.
- The pre-settings launch race is covered by an automated, exhaustive TestStore regression.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-09-SUMMARY.md` when done.
</output>
