---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift
  - AppPackage/Sources/AppComponents/ViewModifiers.swift
  - AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings
autonomous: true
requirements: [UIARCH-04]

must_haves:
  truths:
    - "A shared in-memory blur value privacyMaskBlur exists and defaults to 0, readable from any module without parameter drilling (D-01)."
    - "A zero-argument .privacyMask() SwiftUI modifier exists that self-sources the blur from @SharedReader(.privacyMaskBlur) (D-01/D-02)."
    - "The modifier applies a plain blur (NO max(0.00001, ...) floor per D-03) plus the allowsHitTesting(blur < 1) guard (D-04)."
    - "Two new SettingFeature l10n keys (Privacy Mask label + footer) are present in all 6 supported locales."
  artifacts:
    - path: "AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift"
      provides: "the privacyMaskBlur @Shared(.inMemory) key"
      contains: "privacyMaskBlur"
    - path: "AppPackage/Sources/AppComponents/ViewModifiers.swift"
      provides: "PrivacyMaskModifier struct + privacyMask() func"
      contains: "PrivacyMaskModifier"
    - path: "AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings"
      provides: "privacy_mask + privacy_mask_footer keys (6 locales each)"
      contains: "privacy_mask"
  key_links:
    - from: "AppPackage/Sources/AppComponents/ViewModifiers.swift"
      to: "AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift"
      via: "PrivacyMaskModifier reads @SharedReader(.privacyMaskBlur)"
      pattern: "privacyMaskBlur"
---

<objective>
Lay the additive foundation for the root privacy mask: the shared in-memory blur value, the
self-sourcing .privacyMask() modifier that reads it, and the two new SettingFeature l10n strings for
the relocated control's label + footer.

Purpose: Everything downstream depends on these three symbols existing. The ~40 mask sites read the
modifier, the scenePhase fold (07-02) writes the shared value, and the Appearance relocation (07-02)
uses the l10n keys. This plan is deliberately PURELY ADDITIVE — the existing .autoBlur(radius:)
modifier and Setting.backgroundBlurRadius stay untouched so the whole package graph keeps building
green while the foundation lands.
Output: privacyMaskBlur shared key; PrivacyMaskModifier + .privacyMask(); privacy_mask +
privacy_mask_footer strings in 6 locales.
</objective>

## Artifacts This Phase Produces

These symbols are CREATED by Phase 7 (this plan creates the first three + the two l10n keys).
Source-grounding / drift-verification must treat them as NEW, not pre-existing:
- privacyMaskBlur — @Shared(.inMemory("privacyMaskBlur"), default: 0) key (AppSharedKeys.swift).
- PrivacyMaskModifier struct + func privacyMask() — self-sourcing mask modifier (ViewModifiers.swift).
- Setting.privacyMaskIntensity — persisted property, renamed from backgroundBlurRadius (07-02).
- privacy_mask + privacy_mask_footer — SettingFeature l10n keys (this plan).
- AppFeatureTests target + the scenePhase-fold test suite — elected unit-test target (07-08).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md
@AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift
@AppPackage/Sources/AppComponents/ViewModifiers.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the privacyMaskBlur shared in-memory key</name>
  <files>AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift (the greeting in-memory idiom at lines 68-72 is the exact analog to copy)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Self-Sourcing Modifier" (copy-ready privacyMaskBlur shape)
  </read_first>
  <action>
    Add a new @Shared(.inMemory) key privacyMaskBlur in AppSharedKeys.swift, declared exactly like
    greeting (68-72) but as an extension on SharedKey where Self == InMemoryKey of Double .Default,
    with a static var privacyMaskBlur returning Self at .inMemory("privacyMaskBlur") with default 0.
    Place it next to greeting in the "Account and preferences" region. Add a one-line doc comment
    stating WHY it is in-memory (transient scene-phase blur, reset to 0 on launch; written by
    AppReducer.onScenePhaseChange, read by .privacyMask()) per the document-deliberate-designs
    convention. The default of 0 implements D-01/D-03 (true 0 when off, no floor). Do NOT touch any
    existing key. The in-memory key string is plain (no dot or at sign).
  </action>
  <verify>
    <automated>cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'privacyMaskBlur' AppPackage/Sources/AppModels/Persistence/AppSharedKeys.swift returns >= 1.
    - The key type is InMemoryKey of Double .Default with default 0 (not nil, not a floor).
    - Build of the AppFeature scheme succeeds warning-free (SwiftLint plugin clean).
  </acceptance_criteria>
  <done>The privacyMaskBlur in-memory key exists, defaults to 0, and the graph builds green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the self-sourcing privacyMask() modifier</name>
  <files>AppPackage/Sources/AppComponents/ViewModifiers.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/ViewModifiers.swift (the current autoBlur at lines 29-33 — body kept verbatim MINUS the floor)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Self-Sourcing Modifier" (the PrivacyMaskModifier shape + pfw-sharing / pfw-modern-swiftui notes)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md section "privacyMask() self-sourcing modifier"
  </read_first>
  <action>
    Add a public struct PrivacyMaskModifier conforming to ViewModifier in ViewModifiers.swift, holding
    a @SharedReader(.privacyMaskBlur) property named blur (read-only — the modifier never writes; per
    pfw-sharing @SharedReader is a DynamicProperty), and a body(content:) applying, in order,
    blur(radius: blur) then allowsHitTesting(blur < 1) then animation(.linear(duration: 0.1), value:
    blur). This is the current autoBlur body with the value sourced from the shared reader and the
    max(0.00001, ...) floor DROPPED (D-03 — a plain blur on a true 0) while keeping the
    allowsHitTesting(blur < 1) guard (D-04) and the 0.1s linear animation. Expose a public func
    privacyMask() returning some View via modifier(PrivacyMaskModifier()) as a View extension (favor
    the ViewModifier struct over a stored-closure func per pfw-modern-swiftui now that the modifier
    owns state). KEEP the existing autoBlur(radius:) func — both coexist until every call site is
    swapped; autoBlur is deleted in 07-07. AppComponents already depends on appModels + sharing +
    composableArchitecture, so no Package.swift change is needed. Read the root .swiftlint.yml first:
    keep lines <= 120 chars and honor the custom rules.
  </action>
  <verify>
    <automated>cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'struct PrivacyMaskModifier' AppPackage/Sources/AppComponents/ViewModifiers.swift returns 1.
    - grep -c 'func privacyMask()' AppPackage/Sources/AppComponents/ViewModifiers.swift returns 1.
    - grep -c 'SharedReader(.privacyMaskBlur)' AppPackage/Sources/AppComponents/ViewModifiers.swift returns 1.
    - grep -c 'max(0.00001' AppPackage/Sources/AppComponents/ViewModifiers.swift returns 0 (D-03 floor absent).
    - autoBlur still present (coexists this plan).
    - Build succeeds warning-free.
  </acceptance_criteria>
  <done>.privacyMask() self-sources the blur, applies a floorless blur + hit-test guard, and the graph builds green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two new SettingFeature l10n keys (6 locales each)</name>
  <files>AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings (the background_blur_radius and network_description entries — the exact shape + register to copy)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md section ".xcstrings add" (analog entry + 6-locale duty)
    - CLAUDE.md / AGENTS.md (labeled localized-format + fill-every-locale rules)
  </read_first>
  <action>
    Add two keys to the SettingFeature catalog, each following the background_blur_radius entry shape
    (extractionState "manual" + a localizations map). Both are plain translatable strings — NO numeric
    args, so NO percent-substitution and NO positional percent-at; the only AGENTS.md rule that
    applies is "fill every one of the 6 supported locales" (en, de, ja, ko, zh-Hans, zh-Hant). Keys +
    English values (supply faithful translations for the other 5 locales, matching the register of
    neighboring keys): privacy_mask with en value "Privacy Mask" (the control label); and
    privacy_mask_footer with en value "Blurs the app in the App Switcher and when it moves to the
    background." (the footer under the slider). Both auto-expose as Text(.privacyMask) /
    Text(.privacyMaskFooter) once present — NO ResourceStringSymbols.swift entry is needed. Do NOT
    delete any key here (dead-key removals are 07-03). Keep the JSON valid and match the file's
    existing key ordering/formatting style.
  </action>
  <verify>
    <automated>plutil -lint AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings; grep -c 'privacy_mask' AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings</automated>
  </verify>
  <acceptance_criteria>
    - plutil -lint on the catalog reports OK (valid JSON).
    - Both keys privacy_mask and privacy_mask_footer resolve for all 6 locales — verify each with: plutil -extract strings.KEY.localizations.LOCALE raw on the file for KEY in {privacy_mask, privacy_mask_footer} and LOCALE in {en, de, ja, ko, zh-Hans, zh-Hant}.
    - No existing key was deleted (background_blur_radius, auto_lock, security still present — they leave in 07-03).
  </acceptance_criteria>
  <done>privacy_mask + privacy_mask_footer exist in all 6 locales; catalog is valid JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | The OS snapshots the UI; sensitive gallery content must be masked before it is captured. The privacy-mask value read by the modifier is the control point for this boundary. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | privacyMaskBlur default value | medium | mitigate | The shared key defaults to 0 (mask off) which is correct at foreground; the no-leak guarantee is delivered when the scenePhase fold (07-02) writes the intensity on .inactive and every root attaches .privacyMask() (07-03..07-07). This plan only creates the mechanism; full coverage is audited in 07-08. Default-0 does not leak because content is only visible while foreground. |
| T-07-02 | Tampering | package installs | low | accept | This phase adds NO external packages (it only removes the in-repo AuthorizationClient module later). No npm/pip/cargo/SPM registry install occurs, so the package-legitimacy gate is not applicable; no T-07-SC install checkpoint is required. |
</threat_model>

<verification>
- Build the AppFeature scheme (whole graph + SwiftLint plugin): warning-free.
- Both new symbols (privacyMaskBlur, PrivacyMaskModifier/privacyMask()) present via grep.
- No max(0.00001 floor introduced (D-03).
- The two l10n keys present in all 6 locales; catalog is valid JSON.
- The existing autoBlur + Setting.backgroundBlurRadius are UNCHANGED (this plan is additive).
</verification>

<success_criteria>
- privacyMaskBlur @Shared(.inMemory) key exists with default 0.
- PrivacyMaskModifier + .privacyMask() exist, floorless, with the hit-test guard.
- privacy_mask + privacy_mask_footer exist in en/de/ja/ko/zh-Hans/zh-Hant.
- The whole package graph builds green (no consumers changed yet).
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-01-SUMMARY.md` when done.
</output>
