---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppLockReducer.swift
  - AppPackage/Sources/AuthorizationClient/AuthorizationClient.swift
  - AppPackage/Sources/AuthorizationClient/.swiftlint.yml
  - AppPackage/Package.swift
  - App/Info.plist
  - App/InfoPlist.xcstrings
  - AppPackage/Sources/AppFeature/Resources/Localizable.xcstrings
  - AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings
  - AppPackage/Sources/AppModels/Resources/Localizable.xcstrings
autonomous: true
requirements: [UIARCH-04, UIARCH-05]

must_haves:
  truths:
    - "TabBarView's 4 app-root mask sites use .privacyMask() and the lock-button overlay + hosting ZStack are gone (D-05)."
    - "AppLockReducer + AppReducer.appLockState + the appLock action case + the Scope are deleted (D-05)."
    - "The AuthorizationClient module + its Package.swift enum case (72), target block (381-387), and both consumer target-deps (264, 643) are deleted; the auto-derived product goes with them (D-15)."
    - "No orphaned NSFaceIDUsageDescription remains in App/Info.plist or App/InfoPlist.xcstrings (D-15, RESEARCH Open Q1)."
    - "The dead l10n keys (auto_lock_reason, auto_lock, security, background_blur_radius, auto_lock_policy.never/.instantly) are deleted; the shared seconds/minutes keys are KEPT (D-15)."
  artifacts:
    - path: "AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift"
      provides: "app-root .privacyMask() sites, lock button removed"
      contains: "privacyMask()"
    - path: "AppPackage/Package.swift"
      provides: "AuthorizationClient target/enum/deps removed"
  key_links:
    - from: "AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift"
      to: "AppPackage/Sources/AppComponents/ViewModifiers.swift"
      via: "app-root surfaces attach .privacyMask()"
      pattern: "privacyMask()"
---

<objective>
Tear down the auto-lock machinery and swap the app-root masks. TabBarView's 4 mask sites become
.privacyMask() and its lock button + hosting ZStack are removed; AppLockReducer + its AppReducer
wiring are deleted; the whole AuthorizationClient module + its Package.swift references are removed;
the orphaned Face ID Info.plist key and all dead auto-lock l10n keys are deleted.

Purpose: After 07-02 made the shared blur live and de-referenced AuthorizationClient from
GeneralSetting, nothing but dormant wiring reads appLockState / imports AuthorizationClient. This
plan removes all of it — the leave-no-orphans mandate (D-15). TabBarView's top-level view calls keep
passing blurRadius: 0 transitionally (those params are removed per-module in 07-04..07-07); this plan
only stops TabBarView from reading store.appLockState.blurRadius so appLockState can be deleted.
Output: app root masked via .privacyMask(); AppLockReducer + AuthorizationClient gone; Package.swift,
Info.plist, and three l10n catalogs cleaned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
@AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
@AppPackage/Sources/AppFeature/DataFlow/AppLockReducer.swift
@AppPackage/Sources/AuthorizationClient/AuthorizationClient.swift
@AppPackage/Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap TabBarView app-root masks, remove the lock button, delete AppLockReducer wiring</name>
  <files>AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift, AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift, AppPackage/Sources/AppFeature/DataFlow/AppLockReducer.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (ZStack 27/81; blurRadius param passes 46/51/56/61/66/89/101/106; mask sites 74/84/92/110; lock Button 75-80)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (appLockState 25; case appLock 46; case .appLock catch-all 185-186; the Scope 318)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "TabBarView.swift edits" + "AppReducer.swift edits"
  </read_first>
  <action>
    TabBarView.swift: (1) at the 4 mask sites (74/84/92/110) replace
    .autoBlur(radius: store.appLockState.blurRadius) with .privacyMask(); (2) delete the lock Button
    (75-80) that sent .appLock(.authorize) and its .opacity(store.appLockState.isAppLocked ...);
    (3) collapse the outer ZStack (27/81) that existed only to host the lock button — the TabView
    becomes the top-level body child (this ZStack removal also serves POLISH-02 but the button removal
    is in scope here); (4) the 7 blurRadius param passes (46/51/56/61/66/89/101/106) currently read
    store.appLockState.blurRadius — since appLockState is being deleted, change each to the literal
    blurRadius: 0 (the top-level views still declare the param; it is removed with its call in
    07-04..07-07). Do NOT remove those params here.
    AppReducer.swift: delete var appLockState (25), the case appLock(AppLockReducer.Action) (46), the
    remaining case .appLock: return .none catch-all (185-186), and the
    Scope(\.appLockState, action: \.appLock, AppLockReducer.init) (318). @Shared(.privacyMaskBlur)
    from 07-02 stays.
    AppLockReducer.swift: DELETE the whole file (D-05). Note the file has an uncommitted working-tree
    edit (the pre-applied D-03 floor change) — deleting the file resolves it.
    Read the root .swiftlint.yml: the child-reducer-shorthand custom rules only govern Scope/Store
    creation, not deletion, so removing the Scope line is safe.
  </action>
  <verify>
    <automated>cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'privacyMask()' TabBarView.swift returns 4.
    - grep -c 'appLockState\|isAppLocked\|.appLock(' TabBarView.swift returns 0.
    - grep -c 'lockFill\|Image(systemSymbol: .lockFill)' TabBarView.swift returns 0 (lock button gone).
    - test -f AppPackage/Sources/AppFeature/DataFlow/AppLockReducer.swift returns non-zero (file deleted).
    - grep -rc 'appLockState\|AppLockReducer' AppPackage/Sources/AppFeature returns 0.
    - Build green.
  </acceptance_criteria>
  <done>App root is masked via .privacyMask(), the lock button/ZStack are gone, and AppLockReducer is fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the AuthorizationClient module + Package.swift refs + orphaned Face ID Info.plist key</name>
  <files>AppPackage/Sources/AuthorizationClient/AuthorizationClient.swift, AppPackage/Sources/AuthorizationClient/.swiftlint.yml, AppPackage/Package.swift, App/Info.plist, App/InfoPlist.xcstrings</files>
  <read_first>
    - AppPackage/Package.swift (Module enum case authorizationClient 72; appFeature dep .module(.authorizationClient) 264; the .target(module: .authorizationClient ...) block 381-387; settingFeature dep 643; product auto-derivation 995-1002)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Package.swift edits (exact lines)" + Runtime State Inventory (Info.plist Face ID) + Open Q1
    - App/Info.plist (NSFaceIDUsageDescription at 147) and App/InfoPlist.xcstrings (the NSFaceIDUsageDescription key at 47)
  </read_first>
  <action>
    Delete the whole AuthorizationClient module directory (AppPackage/Sources/AuthorizationClient/ —
    AuthorizationClient.swift + the module's .swiftlint.yml). In Package.swift delete: the Module enum
    case at 72, the .module(.authorizationClient) dep in the appFeature target (264), the
    .target(module: .authorizationClient ...) block (381-387 — the full span, CONTEXT's 382-385 was
    imprecise), and the .module(.authorizationClient) dep in the settingFeature target (643). There is
    NO standalone product line — products are auto-derived at 995-1002, so removing the target + enum
    case drops the library product automatically. No external deps change, so Package.resolved should
    not need regeneration; run swift package resolve only if the resolver reports drift.
    Info.plist orphan (D-15, Open Q1): NSFaceIDUsageDescription exists at App/Info.plist:147 and is
    localized in App/InfoPlist.xcstrings:47. LocalAuthentication/Face ID was used ONLY by the deleted
    AuthorizationClient, so this key is now orphaned — delete the key from App/Info.plist AND its
    entry from App/InfoPlist.xcstrings (both, so Xcode does not flag a missing/dangling localization).
    If a grep shows any OTHER Face ID / LocalAuthentication use remains, do NOT delete it and surface
    the finding instead.
  </action>
  <verify>
    <automated>cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test -d AppPackage/Sources/AuthorizationClient returns non-zero (directory deleted).
    - grep -rc 'AuthorizationClient\|authorizationClient' AppPackage/Sources AppPackage/Package.swift App ShareExtension returns 0.
    - grep -c 'NSFaceIDUsageDescription' App/Info.plist App/InfoPlist.xcstrings returns 0.
    - grep -rc 'LocalAuthentication\|LAContext' AppPackage/Sources returns 0.
    - Build green.
  </acceptance_criteria>
  <done>The AuthorizationClient module, its Package.swift refs, and the orphaned Face ID Info.plist key are gone; graph builds green.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the dead auto-lock l10n keys across the three catalogs</name>
  <files>AppPackage/Sources/AppFeature/Resources/Localizable.xcstrings, AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings, AppPackage/Sources/AppModels/Resources/Localizable.xcstrings</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Enumeration 4" (exact keys, catalogs, lines; keys to KEEP)
    - the three catalogs (AppFeature: auto_lock_reason; SettingFeature: auto_lock, background_blur_radius, security; AppModels: auto_lock_policy.instantly, auto_lock_policy.never)
  </read_first>
  <action>
    Delete these now-dead keys (their code references were removed in 07-02/07-03):
    - AppFeature/Resources/Localizable.xcstrings: auto_lock_reason (was used by AppLockReducer, deleted).
    - SettingFeature/Resources/Localizable.xcstrings: auto_lock, background_blur_radius, and security
      (the Section header — grep confirms its sole use was the removed Security section).
    - AppModels/Resources/Localizable.xcstrings: auto_lock_policy.instantly and auto_lock_policy.never
      (were used by the deleted AutoLockPolicy.value).
    KEEP the shared seconds / minutes keys (in Resources/Resources/Localizable.xcstrings) — still used
    by ReadingFeature + AppError. Do NOT touch the newly-added privacy_mask / privacy_mask_footer.
    There is NO passcode-not-set l10n key (the warning was a bare Image), so nothing to delete there.
    Before deleting security, grep-confirm no remaining Section(.security) or .security reference
    exists in SettingFeature sources. Keep each catalog valid JSON.
  </action>
  <verify>
    <automated>grep -rc 'auto_lock_reason\|"auto_lock"\|background_blur_radius\|"security"\|auto_lock_policy' AppPackage/Sources/AppFeature/Resources/Localizable.xcstrings AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings AppPackage/Sources/AppModels/Resources/Localizable.xcstrings; plutil -lint AppPackage/Sources/SettingFeature/Resources/Localizable.xcstrings</automated>
  </verify>
  <acceptance_criteria>
    - grep across the three catalogs for auto_lock_reason, auto_lock, background_blur_radius, security, auto_lock_policy returns 0 matches.
    - grep for seconds / minutes in the shared Resources catalog still returns >= 1 (KEPT).
    - plutil -lint reports OK for each edited catalog.
    - privacy_mask / privacy_mask_footer still present in the SettingFeature catalog.
  </acceptance_criteria>
  <done>All dead auto-lock l10n keys are removed; shared time keys and the new Privacy Mask keys remain; catalogs are valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | App-root surfaces (root TabView + the newDawn/setting/detail modal roots) must attach .privacyMask() or they leak. |
| removed biometric client -> OS platform | Deleting AuthorizationClient/LocalAuthentication transfers re-auth to iOS; an orphaned Face ID entitlement would misrepresent capability. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Information Disclosure | TabBarView 4 app-root mask sites | high | mitigate | All four (root TabView 74, newDawn 84, setting 92, detail 110) are swapped to .privacyMask(); the 07-08 audit greps for exactly these + confirms zero remaining .autoBlur at the app root. |
| T-07-07 | Spoofing / Elevation | orphaned NSFaceIDUsageDescription entitlement | medium | mitigate | The key is removed from App/Info.plist + App/InfoPlist.xcstrings once no LocalAuthentication use remains; grep confirms 0 Face ID references, so the app no longer advertises a biometric capability it does not use. |
| T-07-08 | Tampering | package graph after target removal | low | accept | Removing an in-repo target with no external dep change leaves Package.resolved untouched; no registry install occurs, so no supply-chain checkpoint is needed. Build-green confirms the graph is consistent. |
</threat_model>

<verification>
- TabBarView: 4 .privacyMask() sites; no appLockState/lock button/ZStack host.
- AppReducer/AppFeature: no AppLockReducer/appLockState references; AppLockReducer.swift deleted.
- AuthorizationClient module + Package.swift refs gone; graph builds green; no LocalAuthentication remains.
- No NSFaceIDUsageDescription in App/Info.plist or InfoPlist.xcstrings.
- Dead auto-lock l10n keys removed from all three catalogs; shared seconds/minutes kept; catalogs valid.
</verification>

<success_criteria>
- App root masked via .privacyMask(); custom lock UI + reducer removed.
- AuthorizationClient (module + Package.swift + Face ID entitlement) fully removed.
- Dead auto-lock l10n keys removed; shared time keys retained.
- Whole graph builds green.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-03-SUMMARY.md` when done.
</output>
