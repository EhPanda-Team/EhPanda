---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 12
type: execute
wave: 9
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [UIARCH-04, UIARCH-05]
gap_closure: true

must_haves:
  truths:
    - "ROADMAP.md Phase 7 Success Criterion 1 and REQUIREMENTS.md UIARCH-04 describe the delivered true-zero / no-floor blur behavior per locked decision D-03 (light visual check that no NavigationBar collapse occurs at blur 0), not the old floor-preservation wording (closes verifier truth #5)."
    - "ROADMAP.md Phase 7 Success Criterion 3 and REQUIREMENTS.md UIARCH-05 describe the outright removal of the auto-lock control with no in-app pointer prose per locked decision D-08 (defer to the OS-level per-app lock, which has no Settings URL/API), not the old replacement-prose wording (closes verifier truth #7)."
    - "The reconciliation is documentation-only: no code changes, no blur floor reintroduced, no pointer prose added to the UI; D-03 and D-08 are cited as the governing authority."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 success criteria reconciled to D-03 and D-08"
      contains: "D-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UIARCH-04 and UIARCH-05 acceptance wording reconciled to D-03 and D-08"
      contains: "D-08"
  key_links:
    - from: ".planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md"
      to: ".planning/ROADMAP.md"
      via: "D-03 / D-08 governing the reconciled acceptance wording"
      pattern: "D-0"
---

<objective>
Close GAP-4 (verifier truths #5 & #7): reconcile the two spec documents to the locked owner decisions.
The direction is already locked and owner-approved — this is a documentation-sync task, NOT a code
change and NOT an open design question. ROADMAP.md Phase 7 Success Criteria 1 & 3 and REQUIREMENTS.md
UIARCH-04 & UIARCH-05 still carry the OLD acceptance wording that D-03 and D-08 explicitly override.

Purpose: The verifier flagged a specification-governance conflict: the delivered code correctly follows
D-03 (true-zero blur, no floor) and D-08 (auto-lock removed outright, no replacement prose), but the
spec docs still demand the opposite. The code is already correct; only the spec docs are out of sync.
Output: ROADMAP + REQUIREMENTS acceptance wording that matches the locked decisions, citing D-03/D-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-VERIFICATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ROADMAP Phase 7 Success Criteria 1 & 3 to D-03 / D-08</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 234-239 (Phase 7 Success Criteria: criterion 1 at line 236 ends with the clause requiring the NavigationBar-collapse workaround to be kept; criterion 3 at line 238 requires swapping the auto-lock control for pointer prose)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md D-03 (NavBar-collapse workaround DROPPED — no `max(0.00001, radius)` floor; shared value is a true `0` when off; owner manually verified no navbar collapse at radius 0 on the iOS 26 stack) and D-08 (auto-lock control REMOVED outright with NO replacement description; iOS built-in per-app lock has no Settings URL/API to point to)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-VERIFICATION.md "Roadmap and Requirements Conflicts" section (the two conflicts to resolve)
  </read_first>
  <action>
    Edit ROADMAP.md Phase 7 Success Criteria in place (scoped `Edit`, not a whole-file rewrite; do not
    touch any other phase). In criterion 1, replace the trailing clause about keeping the
    NavigationBar-collapse workaround with wording that matches D-03: state there is no
    `max(0.00001, radius)` blur floor, the shared value is a true `0` when off, and a light visual check
    confirms no NavigationBar collapse at blur `0` on the current iOS 26 stack; cite D-03 as the
    governing decision. In criterion 3, replace the wording about swapping the auto-lock control for a
    pointer description with wording that matches D-08: the security-section auto-lock control is removed
    outright with no in-app replacement description, deferring re-authentication to iOS's built-in
    per-app lock (which has no Settings URL/API to point to); cite D-08. Change ONLY these two criteria's
    acceptance wording — do not alter the phase Goal, the plan list, the `[x]` completion markers, or the
    top-level Phase 7 checklist entry. This is documentation-only: no code, no floor, no pointer prose.
  </action>
  <verify>
    <automated>grep -n 'D-03\|D-08\|true .*0.*blur\|removed outright' .planning/ROADMAP.md | head; echo "old wording (must be absent):"; grep -c 'workaround preserved' .planning/ROADMAP.md; grep -c 'replaced by a description' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 7 criterion 1 no longer contains "workaround preserved"; it describes true-zero / no-floor blur behavior and a light visual check for no NavigationBar collapse, citing D-03.
    - ROADMAP.md Phase 7 criterion 3 no longer contains "replaced by a description"; it describes outright removal with no in-app pointer, deferring to the OS-level lock, citing D-08.
    - `grep -c 'workaround preserved' .planning/ROADMAP.md` returns 0 and `grep -c 'replaced by a description' .planning/ROADMAP.md` returns 0.
    - No other Phase 7 fields (Goal, plan list, `[x]` markers) or other phases are modified.
  </acceptance_criteria>
  <done>ROADMAP Phase 7 criteria 1 & 3 match the locked D-03 / D-08 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS UIARCH-04 & UIARCH-05 acceptance wording to D-03 / D-08</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 42-45 (UIARCH-04 sub-bullet at line 43 ends with the clause requiring the NavigationBar-collapse workaround to be kept; UIARCH-05 sub-bullet at line 45 requires swapping the auto-lock control for pointer prose)
    - .planning/REQUIREMENTS.md lines 108-109 (the Traceability rows marking UIARCH-04/UIARCH-05 "Complete" — leave these as-is)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md D-03 and D-08 (as in Task 1)
  </read_first>
  <action>
    Edit REQUIREMENTS.md in place (scoped `Edit`). In the UIARCH-04 acceptance sub-bullet (line 43),
    replace the trailing clause about keeping the NavigationBar-collapse workaround with D-03 wording:
    no NavigationBar-collapse blur floor — the shared value is a true `0` when off (no
    `max(0.00001, radius)`) — with a light visual check confirming no collapse at blur `0`; cite D-03. In
    the UIARCH-05 acceptance sub-bullet (line 45), replace the wording about swapping the auto-lock
    control for a pointer description with D-08 wording: the security-section auto-lock control is removed
    outright with no in-app replacement description, deferring to iOS's built-in per-app lock (which has
    no Settings URL/API to point to); keep the "background blur is retained (see UIARCH-04)" tail; cite
    D-08. Leave the `[x]` completion checkboxes on UIARCH-04/UIARCH-05 and the Traceability "Complete"
    rows unchanged. Documentation-only: no code, no floor, no pointer prose.
  </action>
  <verify>
    <automated>grep -n 'D-03\|D-08\|removed outright\|true .*0' .planning/REQUIREMENTS.md | head; echo "old wording (must be absent):"; grep -c 'workaround preserved' .planning/REQUIREMENTS.md; grep -c 'replaced by a description' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md UIARCH-04 acceptance no longer contains "workaround preserved"; it describes true-zero / no-floor behavior with a light visual check, citing D-03.
    - REQUIREMENTS.md UIARCH-05 acceptance no longer contains "replaced by a description"; it describes outright removal with no in-app pointer, deferring to the OS lock, citing D-08, and retains "background blur is retained (see UIARCH-04)".
    - `grep -c 'workaround preserved' .planning/REQUIREMENTS.md` returns 0 and `grep -c 'replaced by a description' .planning/REQUIREMENTS.md` returns 0.
    - The UIARCH-04/UIARCH-05 `[x]` checkboxes and the Traceability "Complete" rows are unchanged.
  </acceptance_criteria>
  <done>REQUIREMENTS UIARCH-04 & UIARCH-05 acceptance wording matches the locked D-03 / D-08 decisions.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan is documentation-only and produces NO new code symbols. It edits the acceptance wording of
existing spec entries (ROADMAP Phase 7 criteria 1 & 3; REQUIREMENTS UIARCH-04 & UIARCH-05) to cite and
match locked decisions D-03 and D-08. No blur floor is reintroduced and no UI pointer prose is added.
</artifacts_this_phase_produces>

<verification>
- ROADMAP.md: Phase 7 criteria 1 & 3 cite D-03 / D-08; the old floor-preservation and replacement-prose wording is absent.
- REQUIREMENTS.md: UIARCH-04 & UIARCH-05 acceptance cite D-03 / D-08; the old wording is absent; completion markers untouched.
- No source files changed; no blur floor; no UI pointer prose.
</verification>

<success_criteria>
- Verifier truths #5 and #7 flip to VERIFIED: ROADMAP.md and REQUIREMENTS.md describe the accepted Phase 7 behavior without contradicting locked decisions D-03 and D-08.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-12-SUMMARY.md` when done.
</output>
