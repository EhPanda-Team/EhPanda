---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 10
type: execute
wave: 10
depends_on: ["07-09"]
files_modified:
  - AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift
  - AppPackage/Package.swift
autonomous: true
requirements: [UIARCH-04]
gap_closure: true

must_haves:
  truths:
    - "Automated tests prove the foreground greeting fires exactly once and the clipboard detection fires exactly once when detectsLinksFromClipboard is enabled, with NO withExhaustivity(.off) masking extra/forbidden actions (closes verifier truth #10 / WR-01)."
    - "An automated test proves clipboard detection fires ZERO times when detectsLinksFromClipboard is disabled (the negative case is proven, not merely un-asserted)."
    - "The AppFeatureTests target reaches ComposableArchitecture transitively through AppFeature — the direct ComposableArchitecture target dependency is dropped and the suite still builds and runs green (closes WR-04)."
  artifacts:
    - path: "AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift"
      provides: "exhaustive exactly-once foreground-effect assertions"
      contains: "detectClipboardURL"
    - path: "AppPackage/Package.swift"
      provides: "AppFeatureTests target linking TCA only transitively via AppFeature"
      contains: "appFeatureTests"
  key_links:
    - from: "AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift"
      to: "AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift"
      via: "clipboard detection seam (clipboardClient.changeCount/url) proving detection cardinality"
      pattern: "detectClipboardURL"
---

<objective>
Close GAP-2 (verifier truth #10 / WR-01) and WR-04. The two foreground scene-phase tests currently
wrap their asserted action sequences in `withExhaustivity(.off)`, which permits extra or forbidden
actions: one `receive` proves occurrence but not exactly-once, and "no receive" cannot prove absence.
Make the assertions exhaustive so the tests deterministically prove exactly-one greeting, exactly-one
clipboard action when enabled, and zero clipboard actions when disabled. While in the test target, drop
the AppFeatureTests direct `ComposableArchitecture` dependency (WR-04) and reach TCA transitively
through AppFeature.

Purpose: The exactly-once foreground behavior (re-homed off the scene-phase fold per D-06) is the last
unproven must-have. Exhaustive TestStore assertions make a second/extra dispatch a hard test failure.
Output: Exhaustive foreground tests (no `.off`), a proven negative clipboard case, and an
AppFeatureTests target that no longer relinks a transitive production dependency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-CONTEXT.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-VERIFICATION.md
@AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift
@AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
@AppPackage/Package.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the foreground exactly-once assertions exhaustive</name>
  <files>AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift</files>
  <behavior>
    - Enabled case (detectsLinksFromClipboard = true): a single .onScenePhaseChange(.active) dispatches .setting(.fetchGreeting) exactly once AND .appRoute(.detectClipboardURL) exactly once; a second or extra dispatch of either fails the test.
    - Disabled case (detectsLinksFromClipboard = false): a single .onScenePhaseChange(.active) dispatches .setting(.fetchGreeting) exactly once and .appRoute(.detectClipboardURL) ZERO times; an unexpected clipboard dispatch fails the test at finish.
    - The .active shared-blur write to 0 and .appLogsPump(.startPump) remain asserted.
  </behavior>
  <read_first>
    - AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift (the two foreground tests using `withExhaustivity(.off)` at lines ~29-38 and ~49-54, and the `makeStore` helper — including the `hasLoadedInitialSetting:` parameter added by 07-09)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (the `.active` branch: `.setting(.fetchGreeting)`, `.appLogsPump(.startPump)`, conditional `.appRoute(.detectClipboardURL)`; note `hasEnteredBackground` starts false in these tests so no reconcile fires)
    - AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift lines 130-140 (`.detectClipboardURL` reads `clipboardClient.changeCount()` then `clipboardClient.url()` — the seam a counting test double can hook)
    - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift lines 58-75 (fetchGreeting is guarded by `cookieClient.didLogin` — with `cookieClient = .noop` the request effect is skipped, so `.setting(.fetchGreeting)` terminates without downstream actions)
    - the pfw-testing + swift-testing-pro skills (exhaustive TestStore `receive` idioms, controlled dependencies, `LockIsolated` counters, proving action cardinality)
  </read_first>
  <action>
    Remove `withExhaustivity(.off)` from both foreground tests and assert the exact action cardinality
    against an exhaustive store. For the enabled test: after asserting the `.inactive` mask write, send
    `.onScenePhaseChange(.active)` with a state closure setting `scenePhase = .active` and
    `$privacyMaskBlur = 0`, then `receive` each expected child action exactly once —
    `.setting(.fetchGreeting)`, `.appLogsPump(.startPump)`, and `.appRoute(.detectClipboardURL)` — and
    `finish()`. Because the injected `cookieClient`/`clipboardClient` noops make those child actions
    terminate without further actions, exhaustive mode holds; if a real downstream action appears,
    receive it explicitly rather than re-disabling exhaustivity. For the disabled test: send
    `.onScenePhaseChange(.active)`, `receive` `.setting(.fetchGreeting)` and `.appLogsPump(.startPump)`
    once each, and DO NOT receive `.appRoute(.detectClipboardURL)`; the exhaustive `finish()` then fails
    if it ever fires, proving absence. To make the clipboard cardinality crisp and duplicate-proof, ALSO
    inject a counting `clipboardClient` double whose `changeCount`/`url` increment a `LockIsolated<Int>`,
    and assert the counter equals 1 (enabled) and 0 (disabled). Keep the store constructed with
    `reducer: AppReducer.init` per the SwiftLint child-reducer rule. No `swiftlint:disable`.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AppFeatureTests/AppReducerScenePhaseTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Neither foreground test contains `withExhaustivity(.off)` around its asserted action sequence (grep of the file for `withExhaustivity(.off)` returns 0).
    - The enabled test proves `.setting(.fetchGreeting)` once and `.appRoute(.detectClipboardURL)` once (clipboard counter == 1); the disabled test proves clipboard detection 0 times (counter == 0 and no `detectClipboardURL` receive).
    - A second/extra dispatch of greeting or clipboard would fail the test (exhaustive store + exact counter).
    - `xcodebuild test -scheme AppPackage-Package -only-testing:AppFeatureTests/AppReducerScenePhaseTests` exits 0.
  </acceptance_criteria>
  <done>Foreground greeting/clipboard cardinality is proven exactly-once (enabled) and exactly-zero (disabled) by an exhaustive TestStore, with no `withExhaustivity(.off)`.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the AppFeatureTests direct ComposableArchitecture dependency (WR-04)</name>
  <files>AppPackage/Package.swift</files>
  <read_first>
    - AppPackage/Package.swift lines 837-845 (the `.testTarget(module: .appFeatureTests, dependencies: [.module(.appFeature), .targetDependency(.composableArchitecture)], plugins: swiftLintPlugins)` block)
    - AppPackage/Package.swift (confirm `.appFeature` links `ComposableArchitecture` as a target dependency, so the test target reaches TCA transitively through AppFeature)
    - CLAUDE.md/AGENTS.md project testing guidance (do not relink a transitive production dependency into a test target) and the verifier note WR-04 (AppFeatureTests relinks ComposableArchitecture)
  </read_first>
  <action>
    In the `.testTarget(module: .appFeatureTests, ...)` block, remove the
    `.targetDependency(.composableArchitecture)` entry so the target's `dependencies` is
    `[.module(.appFeature)]` only. Leave the test source's `import ComposableArchitecture` in place — it
    resolves transitively via AppFeature, which links TCA. Do not add `@_exported` anywhere. Rebuild and
    run the AppFeatureTests suite to confirm TCA test helpers (TestStore, etc.) still resolve and the
    suite is green. If — and only if — the transitive import genuinely fails to resolve, document the
    exact compiler error in the SUMMARY and restore the direct dependency; the elected outcome is the
    dropped direct dependency with a green suite.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AppFeatureTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The `.appFeatureTests` testTarget `dependencies` array no longer contains `.targetDependency(.composableArchitecture)` (only `.module(.appFeature)` remains).
    - `grep -n 'composableArchitecture' AppPackage/Package.swift` shows no occurrence inside the `appFeatureTests` testTarget block.
    - The AppFeatureTests suite builds and runs green via `xcodebuild test -scheme AppPackage-Package -only-testing:AppFeatureTests`.
  </acceptance_criteria>
  <done>AppFeatureTests links AppFeature only; ComposableArchitecture is reached transitively and the suite passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols/artifacts introduced by this plan (symbols already created by 07-01…07-08 or 07-09 are NOT new):
- A counting `clipboardClient` test double backed by a `LockIsolated<Int>` invocation counter (test-local, inside AppReducerScenePhaseTests.swift).
- No new production symbols; Task 2 only removes a Package.swift target dependency.
</artifacts_this_phase_produces>

<verification>
- AppReducerScenePhaseTests.swift: no `withExhaustivity(.off)`; enabled/disabled clipboard cardinality proven (1 / 0); greeting proven once.
- Package.swift: `appFeatureTests` testTarget links only `.module(.appFeature)`.
- The `AppFeatureTests` suite runs green under the AppPackage-Package scheme.
- Run xcodebuild single-invocation only — never launch two `xcodebuild test` runs concurrently on this machine.
</verification>

<success_criteria>
- Verifier truth #10 flips to VERIFIED: foreground greeting/clipboard effects are proven exactly-once, including the disabled negative case.
- WR-04 is resolved: the test target no longer relinks ComposableArchitecture directly.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-10-SUMMARY.md` when done.
</output>
