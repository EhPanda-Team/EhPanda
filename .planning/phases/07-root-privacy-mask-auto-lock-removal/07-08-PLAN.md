---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 08
type: execute
wave: 8
depends_on: ["07-07"]
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift
  - AppPackage/Tests/AppFeatureTests/.swiftlint.yml
  - AppPackage/Tests/FeatureTests.xctestplan
autonomous: false
requirements: [UIARCH-04, UIARCH-05]

must_haves:
  truths:
    - "A TestStore test proves onScenePhaseChange writes @Shared(.privacyMaskBlur) = privacyMaskIntensity on .inactive and = 0 on .active, with the re-homed greeting/clipboard effects firing exactly once (D-05/D-06/D-07, Pitfall 1)."
    - "An automated audit confirms exactly 40 .privacyMask() sites and zero residual autoBlur/blurRadius across AppPackage/Sources (D-16)."
    - "A grep-absence sweep confirms all removed symbols (AuthorizationClient, AppLockReducer, appLockState, AutoLockPolicy/autoLockPolicy, backgroundBlurRadius, NSFaceIDUsageDescription, dead l10n keys) are gone (D-15)."
    - "A human verifies no App-Switcher content leak across all 40 surfaces, no NavigationBar collapse at blur 0, and the Privacy Mask control's relocation (D-16/D-03/D-09/D-10)."
  artifacts:
    - path: "AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift"
      provides: "the scenePhase fold TestStore test"
      contains: "privacyMaskBlur"
    - path: "AppPackage/Package.swift"
      provides: "the AppFeatureTests test target"
      contains: "appFeatureTests"
  key_links:
    - from: "AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift"
      to: "AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift"
      via: "TestStore over AppReducer asserting the shared-blur write"
      pattern: "privacyMaskBlur"
---

<objective>
Prove and audit the phase: an elected TestStore test guarding the scenePhase fold (the one
non-mechanical behavior + Pitfall 1), an automated D-16 mask-coverage + leave-no-orphans grep audit,
the full regression suite, and a human-verify gate for the App-Switcher no-leak / navbar / control-
relocation checks that cannot be automated.

Purpose: UIARCH-04's no-content-leak property is the phase's block-on-high security guarantee; this
plan makes it enforceable. The scenePhase test protects the greeting/clipboard exactly-once re-home,
and the grep audit + human leak sweep together confirm uniform mask coverage of all 40 root/modal
surfaces.

Test-target decision (RESEARCH § Validation / VALIDATION.md Wave 0): ELECTED. There is no
AppFeatureTests target today; standing one up is a one-time Wave-0 cost (copy-ready in PATTERNS §
.testTarget). The fold's exactly-once clipboard/greeting behavior is Medium-risk (A2) and unreliable
to reproduce manually, so a tightly-scoped TestStore guard is worth the target, which Phases 8-9
(which rework AppReducer's clients) will reuse.
Output: AppFeatureTests target + scenePhase test; automated coverage/orphan audit green; full suite
green; human sign-off on the visual leak/navbar/relocation checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-VALIDATION.md
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md
@AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
@AppPackage/Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up AppFeatureTests + the scenePhase fold TestStore test</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift, AppPackage/Tests/AppFeatureTests/.swiftlint.yml, AppPackage/Tests/FeatureTests.xctestplan</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-PATTERNS.md section "Package.swift — .testTarget block" (copy the appModelsTests block 933-939 + the 3 accompanying edits) and section "scenePhase -> shared-blur write"
    - AppPackage/Package.swift (the Module enum ~113-122; the appModelsTests testTarget 933-939; the testTarget convenience wrapper 223-251)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (onScenePhaseChange after the 07-02 fold; the hasLoadedInitialSetting gate; the re-homed fetchGreeting/detectClipboardURL)
    - the pfw-testing + swift-testing-pro skills (TestStore, withExhaustivity(.off), asserting @Shared writes) and CLAUDE.md/AGENTS.md (Feature-suffix reducers; SwiftLint child_reducer_shorthand_store rule — use reducer: AppReducer.init, never a bare AppReducer() in a closure)
  </read_first>
  <action>
    Stand up the AppFeatureTests target: add a Module enum case appFeatureTests = "AppFeatureTests"
    near Package.swift 113-122; add a .testTarget(module: .appFeatureTests, dependencies:
    [.module(.appFeature), .targetDependency(.composableArchitecture)], plugins: swiftLintPlugins) in
    the targets array (via the existing testTarget convenience wrapper, mirroring appModelsTests);
    create AppPackage/Tests/AppFeatureTests/.swiftlint.yml with `parent_config: ../../../.swiftlint.yml`
    (AGENTS.md new-module rule); and register the target in FeatureTests.xctestplan (note the package
    scheme AppPackage-Package auto-includes it; the xctestplan is for the app scheme).
    Write AppReducerScenePhaseTests.swift using Swift Testing + TestStore over AppReducer (construct
    with reducer: AppReducer.init per the lint rule). With withExhaustivity(.off), set
    settingState.hasLoadedInitialSetting = true and a known setting.privacyMaskIntensity, then assert:
    (a) send(.onScenePhaseChange(.inactive)) makes @Shared(.privacyMaskBlur) equal privacyMaskIntensity
    (T-07-03 / D-07); (b) send(.onScenePhaseChange(.active)) makes it 0 AND the re-homed
    .setting(.fetchGreeting) + (when detectsLinksFromClipboard) .appRoute(.detectClipboardURL) effects
    fire exactly once (T-07-04 / Pitfall 1); (c) with detectsLinksFromClipboard false, no clipboard
    effect fires. Control the dependencies AppReducer needs via pfw-testing (noop/test values). If the
    full-AppReducer TestStore proves too entangled to assert cleanly, document that and fall back to
    the manual cold-launch check in Task 3 — but the elected goal is this automated guard.
  </action>
  <verify>
    <automated>cd AppPackage; xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' 2>/dev/null | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AppPackage/Tests/AppFeatureTests/AppReducerScenePhaseTests.swift exists and is picked up by the AppPackage-Package scheme.
    - grep -c 'appFeatureTests' AppPackage/Package.swift returns >= 2 (Module case + testTarget).
    - AppPackage/Tests/AppFeatureTests/.swiftlint.yml contains parent_config: ../../../.swiftlint.yml.
    - The scenePhase test passes: .inactive -> privacyMaskBlur == privacyMaskIntensity; .active -> 0 + greeting/clipboard once; no double clipboard.
    - The full AppPackage-Package suite is green.
  </acceptance_criteria>
  <done>AppFeatureTests exists and the scenePhase fold behavior (incl. exactly-once launch effects) is proven by an automated test.</done>
</task>

<task type="auto">
  <name>Task 2: Automated D-16 mask-coverage audit + leave-no-orphans grep sweep + full regression suite</name>
  <files>AppPackage/Package.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md "Enumeration 1" (40 mask sites total) + "Enumeration 3/4" (removed symbols + dead l10n keys)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-VALIDATION.md (the Per-Task Verification map + Manual-Only table)
  </read_first>
  <action>
    Run the automated coverage + orphan audit that enforces UIARCH-04/UIARCH-05 at the source level
    (no code edits — this task is a gate; if any gate fails, the failing removal/swap plan is
    reopened). Enumerate and confirm all 40 .privacyMask() root/modal sites (app root + 39 sheet/cover
    roots incl. the newly-masked AppActivityLogs run-picker) and confirm the removed surface is truly
    gone. Then run the full AppPackage-Package suite as the regression guard. Record the site
    enumeration in the SUMMARY so a reviewer can map each of the 40 to a surface.
  </action>
  <verify>
    <automated>echo "privacyMask sites:"; grep -rc 'privacyMask()' AppPackage/Sources | grep -v ':0' | awk -F: '{s+=$2} END{print s}'; echo "orphans (must be 0):"; grep -rEc 'autoBlur|blurRadius|appLockState|AppLockReducer|AutoLockPolicy|autoLockPolicy|backgroundBlurRadius|AuthorizationClient' AppPackage/Sources | grep -v ':0' | awk -F: '{s+=$2} END{print s+0}'; echo "faceid (must be 0):"; grep -rc 'NSFaceIDUsageDescription' App/Info.plist App/InfoPlist.xcstrings | awk -F: '{s+=$2} END{print s+0}'</automated>
  </verify>
  <acceptance_criteria>
    - Total .privacyMask() sites across AppPackage/Sources == 40 (D-16 uniform coverage).
    - grep across AppPackage/Sources for autoBlur, blurRadius, appLockState, AppLockReducer, AutoLockPolicy, autoLockPolicy, backgroundBlurRadius, AuthorizationClient == 0 total matches.
    - grep NSFaceIDUsageDescription in App/Info.plist + App/InfoPlist.xcstrings == 0.
    - Dead l10n keys (auto_lock_reason, auto_lock, security, background_blur_radius, auto_lock_policy.*) absent; shared seconds/minutes present; privacy_mask/privacy_mask_footer present in 6 locales.
    - The full AppPackage-Package suite (~431 tests) is green.
  </acceptance_criteria>
  <done>Exactly 40 mask sites, zero orphaned symbols/keys/entitlements, and a green full suite — the D-16/D-15 gates hold.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the no-content-leak property + owner overrides (block-on-high gate)</name>
  <action>Manual verification only — no code changes in this task. This is the block-on-high security gate for UIARCH-04's no-content-leak property (D-16) plus the two owner overrides (D-03 navbar-no-collapse, D-09/D-10 control relocation) and Pitfall 1 (single greeting/clipboard at launch). Build and run on a device/simulator and perform the four checks below; the phase must not advance until they pass.</action>
  <what-built>
    The root privacy mask across all 40 surfaces (self-sourcing .privacyMask() driven by the
    scenePhase fold), the auto-lock removal, and the Privacy Mask control relocated to Appearance.
    These require on-device visual confirmation that automation cannot provide (App-Switcher snapshot,
    NavigationBar behavior at blur 0, control placement).
  </what-built>
  <how-to-verify>
    1. App-Switcher no-leak (D-16, block-on-high): open the app, then from EACH major surface (a
       gallery list, an open gallery detail, the reader, each Settings sub-page, and each modal sheet
       — post-comment, filters, archives, torrents, folder-manager, share, webview, the AppActivityLogs
       run-picker) send the app to the background / open the App Switcher, and confirm the snapshot is
       BLURRED with no readable content. Pay special attention to the reader and the AppActivityLogs
       run-picker (a newly-masked sheet).
    2. NavigationBar no-collapse at 0 (D-03): on a large-title NavigationBar screen, background then
       foreground the app; confirm the bar returns intact when the blur clears to 0 (there is no
       max(0.00001, ...) floor). If it DOES collapse, stop and report — D-03's premise would be wrong.
    3. Auto-lock gone / Privacy Mask relocated (D-09/D-10): confirm General settings has NO Security
       section / auto-lock Picker, and Appearance shows a "Privacy Mask" slider directly under the
       tint-color row with an explanatory footer; toggle it and confirm the background blur strength
       changes accordingly.
    4. Launch behavior (D-06, Pitfall 1): cold-launch the app and confirm the daily greeting appears
       once and the clipboard-URL prompt does not double-fire.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe any leak / collapse / placement issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | The block-on-high property: EVERY one of the 40 root/modal surfaces must mask the App-Switcher snapshot. A single missed modal root is the highest-severity leak. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Information Disclosure | uniform mask coverage of all 40 root/modal surfaces (D-16) | high | mitigate | Automated audit asserts exactly 40 .privacyMask() sites + 0 residual autoBlur (Task 2), and the blocking human-verify sweeps the App Switcher for every surface (Task 3). Block-on-high: a missed modal root fails this gate. |
| T-07-18 | Repudiation | scenePhase fold exactly-once launch effects | medium | mitigate | The TestStore test (Task 1) asserts the shared-blur write timing + exactly-once greeting/clipboard; the human check (Task 3 step 4) confirms no double clipboard prompt at cold launch. |
| T-07-19 | Elevation of Privilege | residual re-auth posture after auto-lock removal | low | accept | iOS built-in Require Face ID owns re-auth (D-08); the retained privacy mask is verified above. Accepted intentional posture change; the orphaned Face ID entitlement was already removed (07-03) and re-audited (Task 2). |
</threat_model>

<verification>
- AppFeatureTests target exists; the scenePhase fold test passes (inactive->intensity, active->0, effects once).
- Automated audit: 40 .privacyMask() sites; 0 orphaned symbols/keys/entitlements; dead l10n gone, new keys present.
- Full AppPackage-Package suite green.
- Human sign-off: no App-Switcher leak across all 40 surfaces; no navbar collapse at 0; Privacy Mask control relocated; single greeting/clipboard at launch.
</verification>

<success_criteria>
- The scenePhase fold is proven by an automated TestStore test.
- D-16 uniform coverage (40 sites) and D-15 leave-no-orphans are enforced by automated gates.
- The blocking human-verify confirms the no-content-leak property and the two owner overrides (D-03/D-09/D-10).
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-08-SUMMARY.md` when done.
</output>
