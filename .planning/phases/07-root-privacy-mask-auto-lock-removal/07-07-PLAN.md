---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 07
type: execute
wave: 7
depends_on: ["07-06"]
files_modified:
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/SettingFeature/SettingView.swift
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/SettingFeature/Login/LoginView.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Sources/AppComponents/ViewModifiers.swift
autonomous: true
requirements: [UIARCH-04]

must_haves:
  truths:
    - "No view initializer anywhere in AppPackage takes a blurRadius parameter (UIARCH-04 — the drilling is fully gone)."
    - "ReadingFeature + SettingFeature root/modal surfaces apply .privacyMask(); the previously-unmasked AppActivityLogs run-picker sheet is now masked (D-16)."
    - "The legacy .autoBlur(radius:) modifier is deleted from AppComponents (D-15 — no orphan)."
    - "The DetailView + DownloadsView transitional ReadingView blurRadius: 0 args are removed; TabBarView passes no blurRadius anywhere."
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/ReadingView.swift"
      provides: "ReadingView with no blurRadius param, .privacyMask() sheet masks"
    - path: "AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift"
      provides: "the newly-masked run-picker sheet (D-16 audit gap)"
    - path: "AppPackage/Sources/AppComponents/ViewModifiers.swift"
      provides: "autoBlur removed; only .privacyMask() remains"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift"
      to: "AppPackage/Sources/AppComponents/ViewModifiers.swift"
      via: "the run-picker sheet attaches .privacyMask()"
      pattern: "privacyMask()"
---

<objective>
The final Part-A sweep: clear blurRadius from ReadingFeature (1) and SettingFeature (4 view types),
mask the previously-unmasked AppActivityLogs run-picker sheet (the D-16 audit gap), remove the last
transitional args (ReadingView blurRadius: 0 in DetailView/DownloadsView, SettingView in TabBarView),
and DELETE the legacy .autoBlur(radius:) modifier now that all 40 sites self-source.

Purpose: This completes UIARCH-04's "no view initializer takes blurRadius" across the whole package
and closes the leave-no-orphans loop for the modifier. The single NEW mask site
(AppActivityLogsView:49) is added per D-16's uniform-coverage mandate — a real, separately-presented
sheet that was unmasked today.
Output: zero blurRadius params package-wide; 40 .privacyMask() sites; autoBlur deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@AppPackage/Sources/ReadingFeature/ReadingView.swift
@AppPackage/Sources/SettingFeature/SettingView.swift
@AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
@AppPackage/Sources/AppComponents/ViewModifiers.swift
@AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip blurRadius from ReadingView + swap masks + drop the two caller args</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md "Enumeration 2" row 22 (ReadingView 28/40/45; preview 444) and "Enumeration 1" ReadingFeature rows (masks 107/112)
    - AppPackage/Sources/ReadingFeature/ReadingView.swift
    - AppPackage/Sources/DetailFeature/DetailView.swift + AppPackage/Sources/DownloadsFeature/DownloadsView.swift (the reading fullScreenCover calls that pass ReadingView(blurRadius: 0) after 07-05/07-06)
  </read_first>
  <action>
    ReadingView.swift: delete the blurRadius stored property, init param, and assignment (28/40/45);
    replace .autoBlur(radius: blurRadius) at 107 (readingSetting sheet) and 112 (share ActivityView
    sheet) with .privacyMask(); remove the blurRadius: 0 preview arg (444). Then remove the transitional
    blurRadius: 0 argument from the ReadingView(...) call in DetailView's reading fullScreenCover AND in
    DownloadsView's reading fullScreenCover (the literals left by 07-06 / 07-05). Read the root
    .swiftlint.yml; keep lines <= 120.
  </action>
  <verify>
    <automated>grep -rc 'blurRadius\|autoBlur' AppPackage/Sources/ReadingFeature; grep -rc 'ReadingView(' AppPackage/Sources/DetailFeature AppPackage/Sources/DownloadsFeature</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'blurRadius' AppPackage/Sources/ReadingFeature returns 0; grep -rc 'autoBlur' returns 0.
    - grep -rc 'privacyMask()' AppPackage/Sources/ReadingFeature returns 2 (sites 107/112).
    - No ReadingView(...) call in DetailFeature or DownloadsFeature contains blurRadius.
  </acceptance_criteria>
  <done>ReadingView is blurRadius-free with .privacyMask() masks; its two callers no longer pass a blur.</done>
</task>

<task type="auto">
  <name>Task 2: Strip blurRadius from SettingFeature + mask the AppActivityLogs run-picker (D-16); drop the TabBarView Setting args</name>
  <files>AppPackage/Sources/SettingFeature/SettingView.swift, AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift, AppPackage/Sources/SettingFeature/Login/LoginView.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md "Enumeration 2" rows 12-15 (SettingView 10/12/14 public init, internal 42/54/57, preview 163; AccountSettingView 13/15/17 preview 185; LoginView 12/17/19 preview 154; EhSettingView 12/17/19 preview 140) and "Enumeration 1" SettingFeature rows (AccountSettingView 53, EhSettingView 53, LoginView 76) and "Modal-Root Audit" (AppActivityLogsView:49 — NEW mask site)
    - AppPackage/Sources/SettingFeature/SettingView.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift (the run-picker sheet at 49-51)
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (SettingView calls at ~64-67 tab and ~87-90 iPad setting sheet, both passing blurRadius: 0)
  </read_first>
  <action>
    SettingView (public init), AccountSettingView, LoginView, EhSettingView: delete the blurRadius
    stored property, init param, and assignment in each; replace .autoBlur(radius: blurRadius) at
    AccountSettingView 53, LoginView 76, EhSettingView 53 with .privacyMask(); remove SettingView's
    internal passes into AccountSettingView/LoginView/EhSettingView (42/54/57) and all _Previews
    blurRadius: 0 args (163/185/154/140). SettingView itself has no mask site — it only drilled the
    param. AppActivityLogsView.swift (D-16 NEW site): attach .privacyMask() to the RunPickerSheet(...)
    content inside .sheet(isPresented: $isRunPickerPresented) at 49-51 — this modal root is unmasked
    today; the uniform-coverage mandate requires it. TabBarView.swift: remove the blurRadius: 0
    argument from BOTH SettingView(store:) calls (the tab content and the iPad setting sheet). Read the
    root .swiftlint.yml; keep lines <= 120.
  </action>
  <verify>
    <automated>grep -rc 'blurRadius\|autoBlur' AppPackage/Sources/SettingFeature; grep -c 'privacyMask()' AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift; grep -c 'SettingView(' AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'blurRadius' AppPackage/Sources/SettingFeature returns 0; grep -rc 'autoBlur' returns 0.
    - grep -rc 'privacyMask()' AppPackage/Sources/SettingFeature returns 4 (Account 53, Login 76, EhSetting 53, AppActivityLogs run-picker NEW).
    - grep -c 'privacyMask()' AppActivityLogsView.swift returns 1 (the NEW D-16 site).
    - Neither TabBarView SettingView call contains blurRadius.
  </acceptance_criteria>
  <done>SettingFeature is blurRadius-free, the AppActivityLogs run-picker is masked (D-16), and TabBarView passes no blur to Setting.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the legacy autoBlur modifier; full Part-A build + grep gate</name>
  <files>AppPackage/Sources/AppComponents/ViewModifiers.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/ViewModifiers.swift (the autoBlur func added-alongside in 07-01 — now unused)
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md "Don't Hand-Roll" (autoBlur body kept verbatim minus the floor — it now lives only in PrivacyMaskModifier)
  </read_first>
  <action>
    Delete the `public func autoBlur(radius:)` View extension from ViewModifiers.swift entirely (D-15,
    leave-no-orphans) — every one of the 40 sites now uses .privacyMask(), so autoBlur has no
    callers. Keep PrivacyMaskModifier + .privacyMask() and every other modifier in the file untouched.
    This is the build-green gate for the whole Part-A param-removal: after this the package graph must
    build clean with zero blurRadius drilling and zero autoBlur anywhere.
  </action>
  <verify>
    <automated>grep -rc 'autoBlur' AppPackage/Sources; grep -rEc 'blurRadius' AppPackage/Sources; cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'autoBlur' AppPackage/Sources returns 0 (definition + all call sites gone).
    - grep -rEc 'blurRadius' AppPackage/Sources returns 0 (no view takes/passes blurRadius anywhere; if a legitimate unrelated blurRadius exists, surface it instead of deleting).
    - grep -rc 'privacyMask()' AppPackage/Sources returns 40 (the full mask-site count).
    - The whole AppFeature graph builds green, warning-free (SwiftLint clean).
  </acceptance_criteria>
  <done>autoBlur is deleted; the package has zero blurRadius drilling and 40 .privacyMask() sites; graph builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | Reader sheets (reading-setting, share) + Setting webview sheets + the AppActivityLogs run-picker are separately-presented roots that must attach .privacyMask() or they leak. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-15 | Information Disclosure | AppActivityLogs run-picker sheet (previously unmasked) | high | mitigate | RESEARCH's audit found this modal root unmasked today; D-16's uniform mandate requires it. .privacyMask() is added; grep asserts the site exists. Content sensitivity is low (diagnostic run picker) but uniform coverage is the security guarantee. |
| T-07-16 | Information Disclosure | Reader + Setting webview modal roots | high | mitigate | ReadingView's two sheets + the three Setting webview sheets are swapped to .privacyMask(); the global grep gate (Task 3) confirms 40 total sites and 0 residual autoBlur/blurRadius, closing the last leak surfaces. |
</threat_model>

<verification>
- ReadingFeature + SettingFeature: 0 blurRadius, 0 autoBlur; ReadingView 2 + SettingFeature 4 = 6 new .privacyMask() sites (incl. the AppActivityLogs run-picker NEW site).
- Package-wide: grep autoBlur == 0, grep blurRadius == 0, grep privacyMask() == 40.
- autoBlur modifier deleted from AppComponents.
- Whole graph builds green.
</verification>

<success_criteria>
- No view initializer in AppPackage takes blurRadius (drilling fully removed).
- All 40 root/modal surfaces (incl. the newly-masked AppActivityLogs run-picker) self-source .privacyMask().
- Legacy autoBlur deleted; build green.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-07-SUMMARY.md` when done.
</output>
