---
phase: 07-root-privacy-mask-auto-lock-removal
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - AppPackage/Sources/HomeFeature/HomeView.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularView.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedView.swift
  - AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift
  - AppPackage/Sources/HomeFeature/History/HistoryView.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
autonomous: true
requirements: [UIARCH-04]

must_haves:
  truths:
    - "No HomeFeature or FavoritesFeature view initializer takes a blurRadius parameter (UIARCH-04)."
    - "Every HomeFeature + FavoritesFeature root/modal surface applies .privacyMask() (self-sourcing) instead of .autoBlur(radius:)."
    - "TabBarView no longer passes blurRadius to HomeView or FavoritesView."
  artifacts:
    - path: "AppPackage/Sources/HomeFeature/HomeView.swift"
      provides: "HomeView with no blurRadius param"
    - path: "AppPackage/Sources/FavoritesFeature/FavoritesView.swift"
      provides: "FavoritesView with no blurRadius param, .privacyMask() sites"
  key_links:
    - from: "AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift"
      to: "AppPackage/Sources/HomeFeature/HomeView.swift"
      via: "HomeView(store:) — no blurRadius arg"
      pattern: "HomeView"
---

<objective>
Remove the blurRadius parameter drilling from HomeFeature (6 view types) and FavoritesFeature (1),
swap their ~8 mask sites to the self-sourcing .privacyMask(), and drop the two TabBarView args that
fed them.

Purpose: The first per-module Part-A sweep. Each module's masks come back online (self-sourcing the
live shared blur written by the 07-02 fold) as its params are removed. HomeFeature is internally
self-consistent (HomeView + its children shed the param together); FavoritesView is consumed only by
TabBarView, whose two calls drop the vestigial blurRadius: 0 argument here.
Output: HomeFeature + FavoritesFeature free of blurRadius; their roots masked via .privacyMask().
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md
@AppPackage/Sources/HomeFeature/HomeView.swift
@AppPackage/Sources/FavoritesFeature/FavoritesView.swift
@AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip blurRadius + swap masks across HomeFeature (6 view types)</name>
  <files>AppPackage/Sources/HomeFeature/HomeView.swift, AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift, AppPackage/Sources/HomeFeature/Popular/PopularView.swift, AppPackage/Sources/HomeFeature/Watched/WatchedView.swift, AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift, AppPackage/Sources/HomeFeature/History/HistoryView.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Enumeration 2" rows 1-6 (exact param decl lines + call sites) and "Enumeration 1" HomeFeature rows (mask sites)
    - AppPackage/Sources/HomeFeature/HomeView.swift (param decl 13/17/20; internal passes 93/97/101/105/109/113; preview 185)
    - AppPackage/Sources/AppComponents/ViewModifiers.swift (the .privacyMask() added in 07-01)
  </read_first>
  <action>
    For each of HomeView, FrontpageView, PopularView, WatchedView, ToplistsView, HistoryView: delete
    the `let blurRadius: Double` stored property, the blurRadius initializer parameter, and the
    self.blurRadius assignment (RESEARCH Enumeration 2 gives each line). At the mask sites —
    FrontpageView 41/53, PopularView 39, WatchedView 53/59/71 — replace .autoBlur(radius: blurRadius)
    with the zero-arg .privacyMask(). Remove every intra-HomeFeature blurRadius argument: HomeView's
    internal passes (93/97/101/105/109/113) to Frontpage/Popular/Watched/Toplists/History, and the
    blurRadius: 0 args in each type's _Previews provider. Do NOT change HomeView's public surface
    beyond removing the param (TabBarView's call is updated in Task 2). Read the root .swiftlint.yml;
    keep lines <= 120 chars.
  </action>
  <verify>
    <automated>grep -rc 'blurRadius' AppPackage/Sources/HomeFeature; grep -rc 'autoBlur' AppPackage/Sources/HomeFeature</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'blurRadius' AppPackage/Sources/HomeFeature returns 0.
    - grep -rc 'autoBlur' AppPackage/Sources/HomeFeature returns 0.
    - grep -rc 'privacyMask()' AppPackage/Sources/HomeFeature returns 6 (the 6 mask sites: Frontpage 41/53, Popular 39, Watched 53/59/71).
    - No HomeFeature initializer declares blurRadius.
  </acceptance_criteria>
  <done>HomeFeature has no blurRadius param and its 6 mask sites use .privacyMask().</done>
</task>

<task type="auto">
  <name>Task 2: Strip blurRadius from FavoritesView + drop the TabBarView Home/Favorites args</name>
  <files>AppPackage/Sources/FavoritesFeature/FavoritesView.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</files>
  <read_first>
    - .planning/phases/07-root-privacy-mask-auto-lock-removal/07-RESEARCH.md section "Enumeration 2" row 7 (FavoritesView param 15/19/22; internal 35; mask 64/76; preview 129)
    - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (the HomeView call ~44-47 and FavoritesView call ~49-52 — each passes blurRadius: 0 after 07-03)
  </read_first>
  <action>
    FavoritesView.swift: delete the blurRadius stored property, init param, and assignment (15/19/22);
    replace .autoBlur(radius: blurRadius) at 64/76 with .privacyMask(); remove the internal
    blurRadius: reference at 35 and the blurRadius: 0 preview arg (129). FavoritesView is consumed
    only by TabBarView.
    TabBarView.swift: remove the blurRadius: 0 argument from the HomeView(store:) call and the
    FavoritesView(store:) call (the two calls that 07-03 set to blurRadius: 0). Leave the other
    top-level view calls (SearchRootView/DownloadsView/SettingView/DetailView) still passing
    blurRadius: 0 — they are cleaned in 07-05/07-06/07-07. Read the root .swiftlint.yml.
  </action>
  <verify>
    <automated>grep -rc 'blurRadius\|autoBlur' AppPackage/Sources/FavoritesFeature; grep -c 'HomeView(' AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift; cd AppPackage; xcodebuild build -project ../EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc 'blurRadius' AppPackage/Sources/FavoritesFeature returns 0; grep -rc 'autoBlur' returns 0.
    - grep -rc 'privacyMask()' AppPackage/Sources/FavoritesFeature returns 2.
    - In TabBarView, the HomeView and FavoritesView calls no longer contain blurRadius; SearchRootView/DownloadsView/SettingView/DetailView calls still pass blurRadius: 0.
    - The whole AppFeature graph builds green.
  </acceptance_criteria>
  <done>FavoritesView is blurRadius-free with .privacyMask() masks; TabBarView no longer feeds Home/Favorites a blur; graph builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app foreground -> app switcher / background | Home + Favorites root/modal surfaces (Frontpage/Popular/Watched roots + filter sheets; Favorites root + its two sheets) must attach .privacyMask() or they leak. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Information Disclosure | HomeFeature + FavoritesFeature root/modal surfaces | high | mitigate | All 8 module mask sites are swapped to .privacyMask() (self-sourcing the live shared blur); grep asserts 0 residual .autoBlur and the exact .privacyMask() count per module. The 07-08 audit reconciles the global total. |
| T-07-10 | Information Disclosure | vestigial blurRadius params on other modules | low | accept | Params on Search/Downloads/Detail/Reading/Setting views remain temporarily (fed 0 by TabBarView); their masks are already off (blur 0) and come online in 07-05..07-07. No leak worsens versus pre-phase because the app root (07-03) already masks the top window; per-module roots are dev-only transient until swapped. |
</threat_model>

<verification>
- HomeFeature + FavoritesFeature: 0 blurRadius, 0 autoBlur; 8 .privacyMask() sites total (6 Home + 2 Favorites).
- TabBarView: HomeView + FavoritesView calls have no blurRadius; the other 4 top-level calls still pass blurRadius: 0.
- Whole graph builds green.
</verification>

<success_criteria>
- No HomeFeature/FavoritesFeature initializer takes blurRadius.
- Their root/modal surfaces self-source the mask via .privacyMask().
- Build green.
</success_criteria>

<output>
Create `.planning/phases/07-root-privacy-mask-auto-lock-removal/07-04-SUMMARY.md` when done.
</output>
