---
phase: 01-isolated-dependency-modernization
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Package.resolved
  - AppPackage/Sources/MarkdownExt/.swiftlint.yml
  - AppPackage/Sources/MarkdownExt/MarkdownUtil.swift
  - AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift
  - AppPackage/Sources/TagTranslationFeature/TagTranslation+Markdown.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift
  - AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift
autonomous: true
requirements:
  - DEP-03
must_haves:
  truths:
    - Markdown parsing moves from SwiftCommonMark/CommonMarkExt to swift-markdown through MarkdownExt.
    - TagTranslation output remains fixture-locked for texts, links, images, display values, descriptions, and links.
    - DetailFeature does not import the markdown package directly, per D-08.
  artifacts:
    - New local helper module `MarkdownExt`.
    - New SwiftPM dependency `swift-markdown` product `Markdown`.
    - Removed SwiftCommonMark package and CommonMarkExt target.
  key_links:
    - MarkdownExt.MarkdownUtil -> TagTranslationFeature computed properties -> DetailFeature UI boundary.
---

<objective>
Migrate the markdown parsing seam from SwiftCommonMark to Apple swift-markdown while keeping TagTranslation and DetailFeature behavior intact.

Purpose: remove SwiftCommonMark from the dependency graph and hide the new parser behind a local helper module.
Output: `MarkdownExt`, retargeted TagTranslationFeature, no direct DetailFeature parser dependency, and green markdown parity tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
@.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
@.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
@.planning/phases/01-isolated-dependency-modernization/01-01-SUMMARY.md
@AGENTS.md
@.swiftlint.yml
@AppPackage/Package.swift
@AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift
@AppPackage/Sources/TagTranslationFeature/TagTranslation+Markdown.swift
@AppPackage/Sources/DetailFeature/DetailView.swift
@AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift
@AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift
</context>

<artifacts_this_phase_produces>
- New local helper module: `MarkdownExt`.
- New file: `AppPackage/Sources/MarkdownExt/.swiftlint.yml`.
- New file: `AppPackage/Sources/MarkdownExt/MarkdownUtil.swift`.
- New package helper: `Target.Dependency.markdown` for product `Markdown` from package `swift-markdown`.
- New package symbol: `Module.markdownExt`.
- Removed package symbols: SwiftCommonMark package declaration, `Target.Dependency.commonMark`, `Module.commonMarkExt`.
- Removed direct import: `DetailFeature.DetailView` no longer imports the parser package.
</artifacts_this_phase_produces>

<must_haves>
- D-07 is implemented by matching fixture-locked intended markdown outputs and documenting any intentional parser limitation fix in tests.
- D-08 is implemented by keeping parser dependency out of DetailFeature.
- D-09 is implemented by using external product `Markdown` and helper module `MarkdownExt`.
- D-10 is implemented by focused fixtures for `parseTexts`, `parseLinks`, and `parseImages`.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MarkdownExt backed by swift-markdown</name>
  <files>AppPackage/Package.swift, AppPackage/Sources/MarkdownExt/.swiftlint.yml, AppPackage/Sources/MarkdownExt/MarkdownUtil.swift, AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Package.swift
    @AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift
    @AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
    @.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
  </read_first>
  <behavior>
    - `MarkdownExt.MarkdownUtil.parseTexts`, `parseLinks`, and `parseImages` compile against swift-markdown product `Markdown`.
    - Markdown traversal preserves paragraph-scoped behavior unless a test explicitly locks an intended fix per D-07.
    - URL validation for images remains structured and full-string.
  </behavior>
  <action>Add the research-approved `swift-markdown` Git package at the audited version and add `Target.Dependency.markdown` for product `Markdown`. Create local module `MarkdownExt` with `.swiftlint.yml` and `MarkdownUtil.swift`. Use `Markdown.Document`/walker APIs behind `MarkdownUtil`; do not expose parser node types to TagTranslationFeature or DetailFeature. Keep current image URL validation intent and add tests for both accepted and rejected URLs. Do not carry over the old CommonMark CasePaths retroactive conformance code unless the new parser genuinely requires an equivalent adapter.</action>
  <verify>
    <automated>xcodebuild -project EhPanda.xcodeproj -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,name=&lt;confirmed simulator&gt;' test -only-testing:MarkdownExtTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `MarkdownExt` imports `Markdown` and owns all direct swift-markdown usage.
    - Behavior assertion: `MarkdownExtTests` pass with Wave 0 expected outputs.
    - Security assertion: rejected image URL fixtures remain rejected.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget TagTranslationFeature and DetailFeature</name>
  <files>AppPackage/Sources/TagTranslationFeature/TagTranslation+Markdown.swift, AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift, AppPackage/Package.swift</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Sources/TagTranslationFeature/TagTranslation+Markdown.swift
    @AppPackage/Sources/DetailFeature/DetailView.swift
    @AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift
    @AppPackage/Package.swift
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
  </read_first>
  <behavior>
    - TagTranslationFeature imports `MarkdownExt` and keeps computed-property names and behavior.
    - DetailView no longer imports CommonMark/Markdown directly.
    - TagTranslationFeatureTests pass against MarkdownExt.
  </behavior>
  <action>Change `TagTranslationFeature` from `CommonMarkExt` to `MarkdownExt` without changing the public computed-property surface. Remove the direct markdown-package import from `DetailView`; if any DetailFeature markdown behavior is still needed, route it through TagTranslationFeature or MarkdownExt rather than importing `Markdown` in the view. Update `Package.swift` dependencies for `tagTranslationFeature`, `detailFeature`, and `appFeature` to reflect the helper boundary and remove direct parser product use where it is not imported.</action>
  <verify>
    <automated>xcodebuild -project EhPanda.xcodeproj -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,name=&lt;confirmed simulator&gt;' test -only-testing:MarkdownExtTests -only-testing:TagTranslationFeatureTests -only-testing:DetailFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `DetailView.swift` has no direct parser-package import.
    - Behavior assertion: TagTranslation markdown-derived display, description, image, and link tests pass.
    - Source assertion: TagTranslationFeature still consumes `MarkdownUtil` rather than parser node types.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove SwiftCommonMark and CommonMarkExt</name>
  <files>AppPackage/Package.swift, AppPackage/Package.resolved, AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Package.swift
    @AppPackage/Package.resolved
    @AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
  </read_first>
  <action>After MarkdownExt and TagTranslationFeature tests pass, remove the SwiftCommonMark package declaration, `.commonMark` product helper, `.commonMarkExt` module case, and CommonMarkExt target declaration. Delete `AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift`. Resolve packages so `AppPackage/Package.resolved` includes swift-markdown and no longer resolves SwiftCommonMark. Keep attribution/resource changes out of this task unless build or generated symbols require them; if acknowledgements are edited, follow the `.xcstrings` all-locale rule from AGENTS.md.</action>
  <verify>
    <automated>xcodebuild -project EhPanda.xcodeproj -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,name=&lt;confirmed simulator&gt;' test -only-testing:MarkdownExtTests -only-testing:TagTranslationFeatureTests -only-testing:DetailFeatureTests</automated>
    <automated>xcodebuild -project EhPanda.xcodeproj -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,name=&lt;confirmed simulator&gt;' build</automated>
  </verify>
  <acceptance_criteria>
    - Requirement assertion: SwiftCommonMark is removed from `Package.swift`.
    - Requirement assertion: `MarkdownUtil.parseTexts`, `parseLinks`, and `parseImages` behave according to the fixture-locked expected outputs.
    - CLI assertion: targeted markdown/detail tests and package build succeed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted tag markdown -> MarkdownExt | Community metadata is parsed into text, links, and image URLs. |
| SwiftPM manifest -> dependency resolver | Adds swift-markdown and removes SwiftCommonMark. |
| DetailFeature UI -> parser helper | UI must not own parser package details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04-01 | Spoofing/Tampering | Markdown image URLs | medium | mitigate | Preserve full-string URL validation and accepted/rejected fixtures. |
| T-01-04-02 | Tampering | Markdown traversal | medium | mitigate | Keep paragraph-scope parity unless an intentional fix is named and fixture-locked. |
| T-01-04-SC | Tampering | swift-markdown dependency | high | mitigate | Use the research-approved official SwiftPM Git remote/tag and verify Package.resolved. |
</threat_model>

<verification>
- Run MarkdownExtTests, TagTranslationFeatureTests, DetailFeatureTests, then package build.
- Full FeatureTests should be run after this plan because a shared parser helper changed.
</verification>

<success_criteria>
- SwiftCommonMark/CommonMarkExt are removed.
- swift-markdown is only exposed through MarkdownExt.
- TagTranslation and Detail markdown behavior is preserved.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-dependency-modernization/01-04-SUMMARY.md` when done.
</output>
