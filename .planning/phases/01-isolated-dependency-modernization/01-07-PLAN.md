---
phase: 01-isolated-dependency-modernization
plan: 07
type: execute
wave: 6
depends_on:
  - 01-06
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Package.resolved
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/HomeFeature/HomeReducer.swift
  - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift
  - .planning/phases/01-isolated-dependency-modernization/01-COLORFUL-UAT.md
autonomous: true
requirements:
  - DEP-07
must_haves:
  truths:
    - Colorful is updated to the latest research-approved version and adopted through the package's current API for DEP-07.
    - GalleryCardCell keeps the animated gradient concept and fallback colors per D-17.
    - Final subjective judgment remains a user verification item per D-19.
  artifacts:
    - Updated Colorful package pin and Package.resolved.
    - GalleryCardCell changes only if required for latest Colorful API adoption or clean build.
    - Colorful UAT notes for user visual verification.
  key_links:
    - Package.swift Colorful pin -> GalleryCardCell gradient surface -> HomeReducer color flow -> user visual verification.
---

<objective>
Update Colorful and preserve the Home gallery-card animated gradient behavior.

Purpose: complete the isolated dependency modernization set with a clean Colorful update while keeping the existing visual concept.
Output: updated package pin, clean build, and user-verifiable gradient parity notes.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
@.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
@.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
@AGENTS.md
@.swiftlint.yml
@AppPackage/Package.swift
@AppPackage/Sources/HomeFeature/GalleryCardCell.swift
@AppPackage/Sources/HomeFeature/HomeReducer.swift
@AppPackage/Sources/HomeFeature/HomeReducer+Body.swift
@AppPackage/Sources/LibraryClient/LibraryClient.swift
</context>

<artifacts_this_phase_produces>
- Updated package declaration: Colorful official Git remote at research-approved latest tag.
- Updated package lockfile: `AppPackage/Package.resolved`.
- HomeFeature integration changes only where required to adopt the latest Colorful API cleanly.
- New verification note: `.planning/phases/01-isolated-dependency-modernization/01-COLORFUL-UAT.md`.
- Removed symbol only if required by clean build: direct deprecated `ColorfulView` usage in `GalleryCardCell`, replaced with the package's current Colorful API.
</artifacts_this_phase_produces>

<must_haves>
- DEP-07 is implemented by updating/adopting latest Colorful and preserving the gallery-card animated gradient concept.
- D-17 is implemented by preserving the same concept while allowing minor polish if it improves the result.
- D-18 is implemented by using technical build/test checks for automation.
- D-19 is implemented by recording visual UAT instructions for the user instead of claiming automated subjective proof.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Update the Colorful package pin</name>
  <files>AppPackage/Package.swift, AppPackage/Package.resolved</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Package.swift
    @AppPackage/Package.resolved
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
    @.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
  </read_first>
  <action>Update the Colorful package declaration to the research-approved official remote and latest tag, preserving the existing `Target.Dependency.colorful` product helper if the package name/product remains `Colorful`. Resolve packages so `Package.resolved` reflects the new pin. Keep the package-edit style consistent with the manifest helper enums. Do not add a new UI dependency for the gradient.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' build-for-testing</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Package.swift` pins Colorful to the research-approved latest version.
    - CLI assertion: package resolution/build-for-testing succeeds with the updated pin.
    - Security assertion: `Package.resolved` records the official Colorful identity from research.
  </acceptance_criteria>
  <done>`Package.swift` and `Package.resolved` adopt the research-approved latest Colorful package, and build-for-testing succeeds on the confirmed simulator.</done>
</task>

<task type="auto">
  <name>Task 2: Keep GalleryCardCell gradient clean and warning-free</name>
  <files>AppPackage/Sources/HomeFeature/GalleryCardCell.swift, AppPackage/Sources/HomeFeature/HomeReducer.swift, AppPackage/Sources/HomeFeature/HomeReducer+Body.swift, AppPackage/Package.swift</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Sources/HomeFeature/GalleryCardCell.swift
    @AppPackage/Sources/HomeFeature/HomeReducer.swift
    @AppPackage/Sources/HomeFeature/HomeReducer+Body.swift
    @AppPackage/Sources/LibraryClient/LibraryClient.swift
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
  </read_first>
  <behavior>
    - GalleryCardCell still renders a gray fallback plus animated multicolor gradient using the colors passed from HomeReducer.
    - `animated`, `animation`, `colors`, and identity reset behavior remain semantically equivalent.
    - Build has no deprecation warning from the Colorful integration.
  </behavior>
  <action>Adopt latest Colorful in `GalleryCardCell` per DEP-07. If the existing `ColorfulView` usage compiles cleanly, keep the integration shape and update only what the latest package API requires. If the updated package emits a deprecation warning or exposes a better current API, replace the direct deprecated symbol with the current Colorful API while preserving the existing inputs and `.id(currentID + animated.description)` behavior. Do not delete Colorful or satisfy DEP-07 through another gradient path; if latest Colorful cannot build cleanly, stop and document the blocker in the summary and `01-COLORFUL-UAT.md`. Do not move color extraction out of LibraryClient or change `rawCardColors` state.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: GalleryCardCell preserves the animated gradient concept and uses the colors passed from HomeReducer.
    - CLI assertion: build succeeds without Colorful deprecation warnings.
    - Source assertion: HomeReducer color state and LibraryClient color extraction flow are unchanged.
  </acceptance_criteria>
  <done>`GalleryCardCell` builds with latest Colorful, preserves the animated gradient concept and HomeReducer color flow, and does not replace or remove the Colorful package.</done>
</task>

<task type="auto">
  <name>Task 3: Record visual UAT and run full phase verification</name>
  <files>.planning/phases/01-isolated-dependency-modernization/01-COLORFUL-UAT.md, AppPackage/Package.swift, AppPackage/Package.resolved</files>
  <read_first>
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
    @.planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md
    @AppPackage/Sources/HomeFeature/GalleryCardCell.swift
    @AppPackage/Package.swift
  </read_first>
  <action>Create `01-COLORFUL-UAT.md` with concrete user verification steps for GalleryCardCell dark-mode animated gradients, light-mode fallback, representative gallery cards, and the expected preservation of color flow after latest Colorful adoption. Run the full FeatureTests plan after all Phase 1 package changes. Record in the summary that DEP-02 and DEP-07 subjective visual parity remains for user verification per D-19.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' test</automated>
  </verify>
  <acceptance_criteria>
    - Document assertion: `01-COLORFUL-UAT.md` exists and lists exact visual checks for DEP-07 and D-19.
    - CLI assertion: full FeatureTests plan succeeds.
    - Requirement assertion: DEP-07 package pin/build criteria are satisfied, with user visual verification handled by `$gsd-verify-work`.
  </acceptance_criteria>
  <done>`01-COLORFUL-UAT.md` documents DEP-07 user visual checks, the full FeatureTests plan passes, and the summary records that subjective visual parity is verified by the user per D-19.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SwiftPM manifest -> dependency resolver | Updating Colorful changes external source code compiled into the app. |
| Cached/extracted image colors -> GalleryCardCell UI | Colors influence foreground/background readability and appearance. |
| Automated build checks -> visual parity claim | Build success cannot prove subjective animated gradient parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07-01 | Tampering | Colorful package update | high | mitigate | Use research-approved official remote/tag and verify Package.resolved. |
| T-01-07-02 | Denial of Service | Animated gradient rendering | low | mitigate | Keep existing animation inputs and avoid moving color work into the view. |
| T-01-07-03 | Repudiation | Visual parity verification | medium | mitigate | Record explicit UAT steps and do not claim automated subjective proof. |
| T-01-07-SC | Tampering | SwiftPM package resolution | high | mitigate | No unaudited gradient packages; DEP-07 must update/adopt latest Colorful rather than deleting or replacing the package. |
</threat_model>

<verification>
- Package build succeeds after Colorful update.
- Full FeatureTests plan succeeds.
- Human check during `$gsd-verify-work`: open Home gallery cards in dark and light appearance, confirm animated gradient concept and fallback colors remain acceptable.
</verification>

<success_criteria>
- Latest Colorful is updated/adopted cleanly and warning-free.
- GalleryCardCell gradient behavior is preserved.
- Full Phase 1 tests are green.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-dependency-modernization/01-07-SUMMARY.md` when done.
</output>
