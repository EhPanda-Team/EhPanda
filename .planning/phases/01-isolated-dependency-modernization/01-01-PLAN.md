---
phase: 01-isolated-dependency-modernization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Tests/FeatureTests.xctestplan
  - AppPackage/Tests/SwiftyOpenCCTests/.swiftlint.yml
  - AppPackage/Tests/SwiftyOpenCCTests/ChineseConverterParityTests.swift
  - AppPackage/Tests/UIImageColorsTests/.swiftlint.yml
  - AppPackage/Tests/UIImageColorsTests/UIImageColorsParityTests.swift
  - AppPackage/Tests/FileClientTests/FileClientTests.swift
  - .planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md
autonomous: true
requirements:
  - DEP-01
  - DEP-02
must_haves:
  truths:
    - Conversion and color parity fixtures lock current behavior before DEP-01 or DEP-02 dependency swaps.
    - Simulator availability is confirmed as iPhone Air on iOS 26.5 before final test commands are relied on.
    - D-02 and D-03 are respected by writing app-fit parity tests first, not by preserving public library compatibility.
    - D-18 and D-19 are respected by limiting automated visual/color proof to deterministic technical assertions.
  artifacts:
    - AppPackage test targets for SwiftyOpenCCTests and UIImageColorsTests, each with real test source at registration time.
    - Focused FileClient parity fixture for app-level tag conversion behavior.
    - Updated 01-VALIDATION.md with concrete simulator destination syntax.
  key_links:
    - Package.swift Module cases -> test target declarations -> FeatureTests.xctestplan entries.
    - Current external behavior -> parity fixtures -> later local module swaps.
---

<objective>
Create the first Wave 0 validation lock for DEP-01 and DEP-02 before any conversion or color dependency swap is allowed.

Purpose: make later SwiftyOpenCC and UIImageColors replacement measurable against current app behavior instead of relying on inspection or memory.
Output: fixture-based Swift Testing targets for DEP-01 and DEP-02 plus a confirmed simulator destination recorded in validation metadata.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
@.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
@.planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md
@.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@AGENTS.md
@.swiftlint.yml
@AppPackage/Package.swift
@AppPackage/Tests/FeatureTests.xctestplan
@AppPackage/Sources/OpenCCExt/TagTranslation+ChtConverted.swift
@AppPackage/Sources/FileClient/FileClient.swift
@AppPackage/Sources/LibraryClient/LibraryClient.swift
</context>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Shrink and modernize isolated third-party surface with behavior parity | 01-01..01-07 | COVERED | Validation first, then isolated swaps. |
| REQ | DEP-01 | Local SwiftyOpenCC modernization with unchanged ChineseConverter behavior | 01-01, 01-03 | COVERED | D-01 through D-06 cited in validation and swap tasks. |
| REQ | DEP-02 | Local UIImageColors modernization with unchanged getColors output | 01-01, 01-04 | COVERED | D-16 through D-19 covered. |
| REQ | DEP-03 | SwiftCommonMark to swift-markdown through MarkdownExt | 01-02, 01-05 | COVERED | D-07 through D-10 covered, including D-09 naming. |
| REQ | DEP-06 | DeprecatedAPI removal only if domain-fronting semantics survive | 01-02, 01-06 | COVERED | D-11 through D-15 and assumption delta block covered. |
| REQ | DEP-07 | Latest Colorful with gallery-card gradient parity | 01-07 | COVERED | D-17 through D-19 covered. |
| RESEARCH | R-01 | Package legitimacy audit for SwiftPM Git packages | 01-05, 01-07 | COVERED | Only OK packages added or updated. |
| RESEARCH | R-02 | Supply-chain substitution risk | 01-03, 01-04, 01-05, 01-07 | COVERED | Threat models require official remotes/tags and Package.resolved verification. |
| RESEARCH | R-03 | Markdown URL broadening risk | 01-02, 01-05 | COVERED | Fixtures lock accepted/rejected image URL behavior. |
| RESEARCH | R-04 | Domain-fronting trust and cookie/header regression risks | 01-02, 01-06 | COVERED | DF semantics tests and evidence checkpoint. |
| CONTEXT | D-01 | SwiftyOpenCC and UIImageColors become app-owned local modules | 01-03, 01-04 | COVERED | Local AppPackage modules planned. |
| CONTEXT | D-02 | Best-fit local APIs, not library compatibility burden | 01-01, 01-03, 01-04 | COVERED | Narrow app-facing APIs and app-fit tests specified. |
| CONTEXT | D-03 | Full modernization, tests, lint, parity evidence | 01-01..01-05, 01-07 | COVERED | Fixture-first plan and per-module lint configs. |
| CONTEXT | D-04 | Clean-room import or rewrite only needed implementation | 01-03, 01-04 | COVERED | Full package vendoring is disallowed in actions. |
| CONTEXT | D-05 | Consumed modules use original package names, not Ext | 01-03, 01-04, 01-05 | COVERED | SwiftyOpenCC, UIImageColors, MarkdownExt named explicitly. |
| CONTEXT | D-06 | New modules need parent-linked .swiftlint.yml | 01-03, 01-04, 01-05 | COVERED | Required in files and acceptance criteria. |
| CONTEXT | D-07 | Markdown best-fit behavior with fixture lock | 01-02, 01-05 | COVERED | Fixture names and migration target included. |
| CONTEXT | D-08 | DetailFeature avoids direct markdown package dependency | 01-05 | COVERED | DetailView import and target dependency removal planned. |
| CONTEXT | D-09 | swift-markdown product is Markdown; helper is MarkdownExt | 01-02, 01-05 | COVERED | Product and helper names are cited directly. |
| CONTEXT | D-10 | Focused parseTexts/parseLinks/parseImages fixtures | 01-02, 01-05 | COVERED | Wave 0 and migration tests cover all three. |
| CONTEXT | D-11 | DF stream design may change but feature stays | 01-06 | COVERED | Replacement branch must preserve feature. |
| CONTEXT | D-12 | Keep current path if replacement cannot preserve DF | 01-06 | COVERED | Conditional path in plan. |
| CONTEXT | D-13 | Skip evidence combines docs and technical request verification | 01-06 | COVERED | Evidence document required. |
| CONTEXT | D-14 | Preserve DF request semantics exactly | 01-02, 01-06 | COVERED | Semantics tests enumerate invariants. |
| CONTEXT | D-15 | User-arranged China testers verify viable replacement | 01-06 | COVERED | Human verification noted. |
| CONTEXT | D-16 | UIImageColors behavior unchanged | 01-01, 01-04 | COVERED | RGBA tuple parity fixtures. |
| CONTEXT | D-17 | Latest Colorful preserves animated-gradient concept | 01-07 | COVERED | GalleryCardCell path planned. |
| CONTEXT | D-18 | Automated visual/color coverage stays deterministic | 01-01, 01-04, 01-07 | COVERED | No UI snapshot claim. |
| CONTEXT | D-19 | Final visual judgment belongs to user verification | 01-04, 01-07 | COVERED | Human-check instructions in verification. |
</source_audit>

<artifacts_this_phase_produces>
- New test targets: `SwiftyOpenCCTests`, `UIImageColorsTests`.
- New test file: `AppPackage/Tests/SwiftyOpenCCTests/ChineseConverterParityTests.swift`.
- New test file: `AppPackage/Tests/UIImageColorsTests/UIImageColorsParityTests.swift`.
- Updated test file: `AppPackage/Tests/FileClientTests/FileClientTests.swift`.
- New package symbols: `Module.swiftyOpenCCTests`, `Module.uiImageColorsTests`.
- Confirmed simulator command: `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -showdestinations`.
- Test command destination: `-destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355'`.
</artifacts_this_phase_produces>

<must_haves>
- DEP-01 parity fixtures prove the current Chinese conversion behavior for default, HK, TW, and the custom `full color` case before SwiftyOpenCC is localized.
- DEP-02 parity fixtures prove deterministic `getColors(quality: .lowest)` component tuples before UIImageColors is localized, per D-16 and D-18.
- D-02 and D-03 are reflected in fixture design: tests lock the app-consumed behavior and leave room for full local-module modernization once parity evidence exists.
- The simulator destination used by every later plan is known and recorded as `platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355`.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Record the concrete simulator destination</name>
  <files>.planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @.planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md
    @.planning/codebase/STACK.md
    @.planning/codebase/TESTING.md
  </read_first>
  <action>Run `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -showdestinations` and record the confirmed destination as iPhone Air on iOS 26.5 with id `ADE09605-A44E-4F00-BE12-235970217355` in `01-VALIDATION.md`. Use the id-based destination syntax in all later commands. Do not register new test targets in this task; registration happens with real test source files in Task 2.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -showdestinations</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `xcodebuild -showdestinations` succeeds and lists iPhone Air on iOS 26.5 with id `ADE09605-A44E-4F00-BE12-235970217355`.
    - Document assertion: `01-VALIDATION.md` contains the exact id-based destination syntax.
  </acceptance_criteria>
  <done>Simulator destination is recorded as `platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355`, and no empty SwiftPM test target is registered by this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture-lock conversion and color behavior</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/FeatureTests.xctestplan, AppPackage/Tests/SwiftyOpenCCTests/.swiftlint.yml, AppPackage/Tests/SwiftyOpenCCTests/ChineseConverterParityTests.swift, AppPackage/Tests/UIImageColorsTests/.swiftlint.yml, AppPackage/Tests/UIImageColorsTests/UIImageColorsParityTests.swift, AppPackage/Tests/FileClientTests/FileClientTests.swift</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Package.swift
    @AppPackage/Tests/FeatureTests.xctestplan
    @AppPackage/Sources/OpenCCExt/TagTranslation+ChtConverted.swift
    @AppPackage/Sources/FileClient/FileClient.swift
    @AppPackage/Sources/LibraryClient/LibraryClient.swift
    @AppPackage/Tests/FileClientTests/FileClientTests.swift
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
    @.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
  </read_first>
  <behavior>
    - Chinese conversion tests lock current default, HK, TW, and custom `full color` behavior for DEP-01 and D-01 through D-04.
    - FileClient coverage proves raw tag-translation cache semantics still apply conversion only for `.traditionalChinese`.
    - UIImageColors tests lock deterministic RGBA tuples for background, primary, secondary, and detail colors, per DEP-02, D-16, and D-18.
    - `SwiftyOpenCCTests` and `UIImageColorsTests` are added to Package.swift and FeatureTests only in the same task that creates their first real test source files.
  </behavior>
  <action>Read root `.swiftlint.yml` before touching Swift. Add the `SwiftyOpenCCTests` and `UIImageColorsTests` `Module` cases, `.testTarget` declarations, xctestplan entries, test directories, `.swiftlint.yml` files, and real Swift Testing source files in this task. Link each parity target to the current module or external product it is locking: `SwiftyOpenCCTests` may depend on the current OpenCC/OpenCCExt seam, and `UIImageColorsTests` may depend on the current UIImageColors product. Create focused Swift Testing fixtures using structs, `@Test`, `#require` for setup, and `#expect` or `expectNoDifference` where available. Do not use XCTest APIs. Keep tests deterministic: generated images and fixed data are preferred over live network or device state; avoid `DataCache.shared`. The conversion fixtures must intentionally capture the current behavior even when the later local implementation changes internals. The color fixtures must compare component tuples, not subjective appearance, because D-19 reserves final visual judgment for user verification.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' test -only-testing:SwiftyOpenCCTests -only-testing:UIImageColorsTests -only-testing:FileClientTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AppPackage/Package.swift` contains the two new test target `Module` cases and `.testTarget` declarations.
    - Source assertion: every new test directory has `.swiftlint.yml` whose only configuration is the parent config required by AGENTS.md.
    - Behavior assertion: DEP-01 tests fail if the current Chinese conversion output changes for default/HK/TW/custom cases.
    - Behavior assertion: DEP-02 tests fail if any locked RGBA tuple changes for deterministic image fixtures.
    - Source assertion: no test uses force unwrap, force try, XCTest, live network, or shared image cache state.
  </acceptance_criteria>
  <done>`SwiftyOpenCCTests`, `UIImageColorsTests`, and focused FileClient parity coverage exist, are registered with real Swift source files, and the targeted conversion/color command passes on the confirmed simulator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External package behavior -> Wave 0 fixtures | Current dependency outputs become the baseline for app-owned replacements. |
| Gallery image pixels -> color extraction | Image pixels influence UI colors and readability. |
| SwiftPM manifest -> dependency resolver | Manifest edits determine which tests and products are compiled into the app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01-01 | Tampering | Conversion/color Wave 0 tests | medium | mitigate | Fixture-lock current outputs before implementation plans change dependencies. |
| T-01-01-02 | Denial of Service | Empty SwiftPM test targets | medium | mitigate | Register each new test target only in the task that creates its first real Swift test source. |
| T-01-01-SC | Tampering | SwiftPM package resolution | high | mitigate | No new external packages are installed in this plan; later package plans must use the research-approved official remotes/tags and updated Package.resolved. |
</threat_model>

<verification>
- Run the targeted conversion/color command for `SwiftyOpenCCTests`, `UIImageColorsTests`, and `FileClientTests`.
- Confirm `01-VALIDATION.md` records the selected simulator and the plan map for conversion/color fixtures.
</verification>

<success_criteria>
- DEP-01 and DEP-02 Wave 0 tests compile and pass against the current dependency implementation.
- The confirmed simulator destination is concrete and directly executable in all later verify commands.
- No production dependency has been swapped in this plan.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-dependency-modernization/01-01-SUMMARY.md` when done.
</output>
