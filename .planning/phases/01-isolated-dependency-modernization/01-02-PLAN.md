---
phase: 01-isolated-dependency-modernization
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Tests/FeatureTests.xctestplan
  - AppPackage/Tests/MarkdownExtTests/.swiftlint.yml
  - AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift
  - AppPackage/Tests/TagTranslationFeatureTests/.swiftlint.yml
  - AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/DFRequestSemanticsTests.swift
  - .planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md
autonomous: true
requirements:
  - DEP-03
  - DEP-06
must_haves:
  truths:
    - Markdown and TagTranslation parity fixtures exist before SwiftCommonMark is replaced.
    - D-09 is explicit: the external swift-markdown product/target is `Markdown`, and the app helper module must be `MarkdownExt` without a conflicting app-owned `Markdown` module.
    - Domain-fronting request semantics are locked before any DeprecatedAPI removal branch.
    - D-03 is respected by pairing modernization plans with tests, lint expectations, and parity evidence.
  artifacts:
    - AppPackage test targets for MarkdownExtTests and TagTranslationFeatureTests, each with real test source at registration time.
    - DFRequestSemanticsTests.swift in the existing NetworkingFeatureTests target.
    - Updated 01-VALIDATION.md plan map for markdown and domain-fronting fixtures.
  key_links:
    - CommonMarkExt.MarkdownUtil current behavior -> MarkdownExtTests -> later MarkdownExt migration.
    - URLRequest/DFRequest/DFStreamHandler semantics -> NetworkingFeatureTests -> DEP-06 branch decision.
---

<objective>
Complete the Wave 0 validation lock for DEP-03 and DEP-06 before markdown parser migration or domain-fronting dependency changes.

Purpose: make the parser and domain-fronting swaps measurable against current behavior while keeping test target registration coupled to real test source files.
Output: fixture-based Swift Testing targets for markdown parity and deterministic domain-fronting request semantics.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
@.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
@.planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md
@.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
@.planning/phases/01-isolated-dependency-modernization/01-01-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@AGENTS.md
@.swiftlint.yml
@AppPackage/Package.swift
@AppPackage/Tests/FeatureTests.xctestplan
@AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift
@AppPackage/Sources/TagTranslationFeature/TagTranslation+Markdown.swift
@AppPackage/Sources/NetworkingFeature/DFExtensions.swift
@AppPackage/Sources/NetworkingFeature/DFRequest.swift
@AppPackage/Sources/NetworkingFeature/DFStreamHandler.swift
@AppPackage/Sources/NetworkingFeature/DFURLProtocol.swift
</context>

<artifacts_this_phase_produces>
- New test targets: `MarkdownExtTests`, `TagTranslationFeatureTests`.
- New test file: `AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift`.
- New test file: `AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift`.
- New test file: `AppPackage/Tests/NetworkingFeatureTests/DFRequestSemanticsTests.swift`.
- New package symbols: `Module.markdownExtTests`, `Module.tagTranslationFeatureTests`.
</artifacts_this_phase_produces>

<must_haves>
- DEP-03 fixtures prove `MarkdownUtil.parseTexts`, `parseLinks`, `parseImages`, and TagTranslation computed properties before replacing SwiftCommonMark, per D-07 through D-10.
- D-09 is implemented in the plan contract by reserving `Markdown` for the external swift-markdown product/target and `MarkdownExt` for the app helper module.
- DEP-06 fixtures prove domain-to-IP replacement, Host header preservation, original-URL cookie handling, POST body preservation, redirect propagation, and trust-host selection before any DeprecatedAPI removal, per D-14.
- D-03 is implemented by making parity evidence a blocking input to the later modernization tasks.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture-lock markdown and TagTranslation behavior</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/FeatureTests.xctestplan, AppPackage/Tests/MarkdownExtTests/.swiftlint.yml, AppPackage/Tests/MarkdownExtTests/MarkdownUtilParityTests.swift, AppPackage/Tests/TagTranslationFeatureTests/.swiftlint.yml, AppPackage/Tests/TagTranslationFeatureTests/TagTranslationMarkdownTests.swift</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Package.swift
    @AppPackage/Tests/FeatureTests.xctestplan
    @AppPackage/Sources/CommonMarkExt/MarkdownUtil.swift
    @AppPackage/Sources/TagTranslationFeature/TagTranslation+Markdown.swift
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
    @.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
  </read_first>
  <behavior>
    - Markdown fixtures cover `parseTexts`, `parseLinks`, `parseImages`, image URL accept/reject cases, and TagTranslation markdown-derived properties per D-07 through D-10.
    - `MarkdownExtTests` and `TagTranslationFeatureTests` are added to Package.swift and FeatureTests only in the same task that creates their first real test source files.
    - The tests lock current behavior through `CommonMarkExt` and `TagTranslationFeature`; the later migration retargets these fixtures to `MarkdownExt`.
    - D-09 naming remains explicit: do not create a local module named `Markdown`.
  </behavior>
  <action>Add the `MarkdownExtTests` and `TagTranslationFeatureTests` `Module` cases, `.testTarget` declarations, xctestplan entries, test directories, `.swiftlint.yml` files, and real Swift Testing source files in this task. For Wave 0, target the current `CommonMarkExt` and `TagTranslationFeature` behavior so the later swift-markdown migration has a locked baseline. Add small inline markdown fixtures and TagTranslation model fixtures. Keep paragraph-only markdown traversal expectations unless a test name and assertion explicitly documents an intended parser limitation fix per D-07. Do not broaden markdown URL behavior without a fixture proving the old and intended result.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' test -only-testing:MarkdownExtTests -only-testing:TagTranslationFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AppPackage/Package.swift` contains the two new markdown-related test target `Module` cases and `.testTarget` declarations.
    - Behavior assertion: DEP-03 fixtures fail if markdown text/link/image extraction or TagTranslation computed properties drift unexpectedly.
    - Security assertion: image URL validation remains full-string structured validation and is covered by accepted and rejected cases.
  </acceptance_criteria>
  <done>`MarkdownExtTests` and `TagTranslationFeatureTests` exist with real Swift source files, are registered in Package.swift and FeatureTests, and pass against the current CommonMark-backed behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture-lock domain-fronting request semantics</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/DFRequestSemanticsTests.swift, .planning/phases/01-isolated-dependency-modernization/01-VALIDATION.md</files>
  <read_first>
    @AGENTS.md
    @.swiftlint.yml
    @AppPackage/Sources/NetworkingFeature/DFExtensions.swift
    @AppPackage/Sources/NetworkingFeature/DFRequest.swift
    @AppPackage/Sources/NetworkingFeature/DFStreamHandler.swift
    @AppPackage/Sources/NetworkingFeature/DFURLProtocol.swift
    @.planning/codebase/INTEGRATIONS.md
    @.planning/phases/01-isolated-dependency-modernization/01-CONTEXT.md
    @.planning/phases/01-isolated-dependency-modernization/01-RESEARCH.md
    @.planning/phases/01-isolated-dependency-modernization/01-PATTERNS.md
  </read_first>
  <behavior>
    - DF fixtures cover D-14 semantics: host replacement, Host header behavior, cookie/header handling, POST body preservation, redirect propagation, and trust-host selection.
    - DF tests do not perform live networking and do not require China/SNI conditions.
    - NetworkingFeature production source is not changed in this Wave 0 fixture plan.
  </behavior>
  <action>Add domain-fronting tests around currently testable request, URL, body, cookie/header, redirect, and trust-host invariants. Keep all production NetworkingFeature source unchanged in this plan; if a later implementation needs production helper extraction, that belongs in the DEP-06 evidence plan where the exact source files are listed. Do not remove or weaken any domain-fronting behavior in this Wave 0 plan. Update `01-VALIDATION.md` so DEP-03 and DEP-06 fixture rows point to concrete plan IDs and directly executable commands.</action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -testPlan FeatureTests -destination 'platform=iOS Simulator,id=ADE09605-A44E-4F00-BE12-235970217355' test -only-testing:MarkdownExtTests -only-testing:TagTranslationFeatureTests -only-testing:NetworkingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: DEP-06 fixtures fail if D-14 request semantics drift before the DeprecatedAPI spike.
    - Source assertion: this plan does not modify `AppPackage/Sources/NetworkingFeature`.
    - Document assertion: `01-VALIDATION.md` uses concrete plan IDs and simulator destination syntax for markdown and DF verification.
  </acceptance_criteria>
  <done>`DFRequestSemanticsTests.swift` exists in `NetworkingFeatureTests`, targeted markdown/DF commands pass on the confirmed simulator, and `01-VALIDATION.md` references concrete plan IDs and commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted tag markdown -> MarkdownUtil/TagTranslation | Community tag metadata can contain links/images and must not broaden URL handling without fixtures. |
| User session cookies -> DF request rewrite | Cookies must stay tied to the original request URL when DF rewrites the network host. |
| SwiftPM manifest -> dependency resolver | Manifest edits determine which test targets are compiled into the app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-02-01 | Spoofing/Tampering | Markdown image URL parsing | medium | mitigate | Add accepted/rejected image URL fixtures before swift-markdown migration. |
| T-01-02-02 | Tampering/Information Disclosure | DF request semantics | high | mitigate | Add blocking DF semantics tests for Host, cookies, body, redirect, and trust-host behavior before DEP-06 changes. |
| T-01-02-SC | Tampering | SwiftPM package resolution | high | mitigate | No new external packages are installed in this plan; later package plans must use the research-approved official remotes/tags and updated Package.resolved. |
</threat_model>

<verification>
- Run the targeted markdown and DF test command.
- Run the full `FeatureTests` test plan after all Wave 0 test targets compile.
- Confirm `01-VALIDATION.md` records concrete plan IDs and commands for DEP-03 and DEP-06.
</verification>

<success_criteria>
- DEP-03 and DEP-06 Wave 0 tests compile and pass against the current implementation.
- Every later markdown and domain-fronting dependency change has a failing-signal fixture already in place.
- No production dependency has been swapped in this plan.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-dependency-modernization/01-02-SUMMARY.md` when done.
</output>
