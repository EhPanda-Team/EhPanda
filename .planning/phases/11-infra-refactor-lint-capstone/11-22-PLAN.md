---
phase: 11-infra-refactor-lint-capstone
plan: 22
type: execute
wave: 22
depends_on: [11-21]
files_modified:
  - AppPackage/Tests/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "@MainActor survives on a test file/suite/function ONLY where the test actually requires the main actor (compiler-enforced, e.g. TestStore APIs) or becomes meaningless without it (D-13); every other occurrence across the 45 files is removed"
    - "The FULL suite (EhPanda scheme) passes running in parallel on any thread — LINT-01 success criterion 4's end state"
  artifacts:
    - path: "AppPackage/Tests/"
      provides: "minimal-@MainActor parallel test suite"
  key_links:
    - from: "removed @MainActor annotations"
      to: "Swift Testing parallel execution"
      via: "tests run on arbitrary threads; only genuine main-actor needs remain isolated"
      pattern: "@MainActor"
---

<objective>
Sweep `@MainActor` across the 45 annotated test files (DownloadsFeatureTests 27, SettingFeatureTests 9, ReadingFeatureTests 3, AppFeatureTests 2, DetailFeatureTests 2, ImageClientTests 2) per D-13: remove each annotation, compile, run; restore ONLY where the compiler demands it (TCA TestStore main-actor APIs, UIKit-touching fixture helpers like UIImage rendering) or the test is meaningless without it. Then run the full-suite parallel gate.

Purpose: completes LINT-01 success criterion 4 (with plans 11-19..21); the full suite now runs in parallel on any thread.
Output: minimal-@MainActor test suite + green full-suite parallel run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: @MainActor -->

## Artifacts this phase produces (plan 11-22 portion)

- @MainActor removals across the test tree; survivor inventory (file + reason) in the SUMMARY for the phase-end owner review

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DownloadsFeatureTests @MainActor sweep (27 files)</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/</files>
  <read_first>
    - A sample of the 27 annotated DownloadsFeatureTests files: WHERE is the annotation (suite-level vs function-level) and what APIs actually need main-actor isolation (TestStore calls are main-actor-bound in TCA; pure store-less logic is not)
  </read_first>
  <action>
    For each of the 27 annotated files: remove the annotation at its current scope; if the compiler then errors on main-actor-isolated API use (TestStore construction/send is the common case), restore the annotation at the NARROWEST scope that satisfies the compiler (function over suite, suite over file) — a compiler-required annotation satisfies D-13's "real need" criterion by definition. Where the file compiles clean without it, leave it off. Never restore for convenience or style. Run the DownloadsFeature scheme tests after the sweep. Record survivors (file, scope, one-line reason — "compiler: TestStore isolation" suffices) in the SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Every remaining @MainActor in DownloadsFeatureTests is compiler-required at its scope (spot-verifiable: removing any survivor breaks the build) or carries a one-line justification in the SUMMARY
    - DownloadsFeature scheme tests pass in parallel
  </acceptance_criteria>
  <done>Largest target swept; survivors minimal and justified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remaining targets sweep + FULL-SUITE parallel gate</name>
  <files>AppPackage/Tests/</files>
  <read_first>
    - The remaining annotated files: SettingFeatureTests (9), ReadingFeatureTests (3), AppFeatureTests (2), DetailFeatureTests (2), ImageClientTests (2)
  </read_first>
  <action>
    Apply the same narrow-scope sweep to the remaining 18 files. ImageClientTests note: UIImage-based fixture rendering (scale-1 PNG fixtures) is a likely genuine keeper — UIKit image drawing is main-actor-adjacent; keep only what the compiler or correctness demands. Then run the phase's parallel gate: the FULL suite via the EhPanda scheme — Swift Testing parallel by default, now with the 11-19..21 serialization work landed. Any cross-test pollution surfacing here is fixed by injection (per-test instances), never by re-adding traits or annotations. Complete the survivor inventory (all targets) in the SUMMARY — this is the input for the phase-end owner review of criterion 4.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Full EhPanda-scheme suite passes in parallel
    - grep count of @MainActor across AppPackage/Tests is materially reduced from 45 files, and every survivor appears in the SUMMARY inventory with scope + reason
  </acceptance_criteria>
  <done>Criterion 4 end-state reached: full suite parallel on any thread, minimal documented isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test concurrency → shared process state | Parallel execution exposes any residual shared state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-25 | Denial of Service | flaky suite from premature annotation removal | medium | mitigate | Narrow-scope compiler-driven restore; full-suite gate; inject-over-serialize for pollution |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Full EhPanda-scheme test run green in parallel (single xcodebuild invocation; never overlap)
- Survivor inventory complete in SUMMARY
</verification>

<success_criteria>
- D-13 delivered: @MainActor minimal and justified; full suite parallel and green
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-22-SUMMARY.md` when done
</output>
