---
phase: 11-infra-refactor-lint-capstone
plan: 3
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
  - AppPackage/Sources/DownloadClient/DownloadClient.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Cache.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "Every optional-try site in the DownloadStore/Operations/Cache/PublicAPI/PersistenceNormalize cluster is either an explicit do/catch (logged where failure is unexpected), a propagated error, or a D-02 reason-annotated exception candidate"
    - "Download store/persistence behavior is unchanged: authoritative failures still propagate, probes/cleanup still degrade, all download tests green"
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift"
      provides: "explicit error handling at former probe/cleanup sites"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/*.swift"
      to: "OSLog Logger"
      via: "file-top private let logger where files gain log calls"
      pattern: "private let logger = Logger"
---

<objective>
Root-fix the optional-try sites in DownloadClient's store/persistence cluster (DownloadStore.swift, DownloadStore+Operations.swift, DownloadClient.swift, DownloadClient+Cache.swift, DownloadClient+PublicAPI.swift, DownloadClient+PersistenceNormalize.swift — roughly half of the module's 36 sites), per D-01/D-02/D-03. Phase 9 documented these as intentional probes/metadata fallbacks/cleanup; under D-01 that documentation is the triage input, not the resolution.

Purpose: DownloadClient is the second-largest optional_try contributor (36 sites); the rule cannot flip until each is refactored or exception-annotated.
Output: store/persistence cluster free of unannotated optional-try.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-03 portion)

- do/catch replacements + `private let logger` declarations in the six store/persistence files (no public API changes)
- Zero or more D-02 exception candidates (`// reason:` + `// swiftlint:disable:next optional_try`) for phase-end owner review

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DownloadStore + Operations sites (D-01/D-03)</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore.swift, AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift, AppPackage/Sources/DownloadClient/DownloadClient.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadStore.swift and DownloadStore+Operations.swift (every optional-try site + its Phase 9 rationale comment)
    - AppPackage/Sources/DownloadClient/DownloadClient.swift (same)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (Pattern 1 do/catch shape; D-02 exception form)
  </read_first>
  <action>
    Enumerate optional-try sites in the three files (grep, comment lines excluded). Triage each using its Phase 9 rationale comment, in this priority order per D-01: (1) if the failure is user-relevant and the call chain can carry it — propagate via plain `try` to the owning caller/reducer (D-03); (2) if the degradation is deliberate (probe, metadata fallback, cadence write, cleanup) — convert to explicit do/catch that mirrors the current fallback exactly, logging via `logger.error` with a stable descriptor when the failure is unexpected, or a bare explanatory catch when failure is the normal control-flow path (e.g. existence probes); (3) ONLY if the code genuinely cannot be expressed without optional-try — write a D-02 exception candidate: a `// reason: …` line followed by `// swiftlint:disable:next optional_try` directly above the site (owner reviews the batch at phase end; unapproved candidates get reworked). Where a file gains its first log call, add the file-top `private let logger` declaration; if DownloadClient lacks a module Logger init extension, add a Logger+.swift mirroring ParserFeature's (init-only, moduleName "DownloadClient"). Never log file paths, URLs with credentials, or cookie values — fixed operation descriptors only (Phase 9 FileClient precedent).
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadClient -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered optional-try count in the three files equals 0 excluding lines directly below a reason-annotated disable
    - DownloadClient scheme tests pass unmodified
    - `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>Store/coordinator sites triaged and converted; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cache + PublicAPI + PersistenceNormalize sites</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Cache.swift, AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift, AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift</files>
  <read_first>
    - The three files (all optional-try sites + Phase 9 rationale comments)
  </read_first>
  <action>
    Apply the same D-01 triage (propagate → do/catch mirror → D-02 exception candidate) to every optional-try site in the cache, public-API, and persistence-normalization files. Persistence normalization: failures that would corrupt or silently drop manifest data are propagation candidates; pure probes stay explicit do/catch. Cache housekeeping (fire-and-forget removals) mirrors current behavior with an explanatory catch. Run the DownloadsFeature scheme tests as well — the download reducers exercise these paths end-to-end.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered optional-try count in the three files equals 0 excluding reason-annotated exception sites
    - DownloadsFeature scheme tests pass unmodified
  </acceptance_criteria>
  <done>Cluster complete: six files free of unannotated optional-try; download behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk manifests → store | Persisted download manifests re-enter the store on load |
| Store → OSLog | New log lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information Disclosure | new logger calls in DownloadClient | high | mitigate | Fixed operation descriptors + error values only; never paths/URLs/cookies; `Scripts/check-cookie-logging.sh` gate |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DownloadClient + DownloadsFeature scheme tests green (sequential xcodebuild invocations)
- `bash Scripts/check-cookie-logging.sh` exits 0
</verification>

<success_criteria>
- Store/persistence cluster contributes zero unannotated optional_try violations
- Download behavior parity proven by unmodified green tests
- Any exception candidates carry the exact D-02 form for phase-end review
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-03-SUMMARY.md` when done
</output>
