---
phase: 11-infra-refactor-lint-capstone
plan: 17
type: execute
wave: 17
depends_on: [11-16]
files_modified:
  - AppPackage/Sources/AnimatedImageFeature/
  - AppPackage/Sources/AppModels/
  - AppPackage/Sources/HomeFeature/
  - AppPackage/Sources/URLClient/
  - AppPackage/Sources/FavoritesFeature/
  - .swiftlint.yml
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "unchecked_subscript_index_access is enabled at error (with its Tests and validatedIndex exclusions) and the standalone binary reports 0 violations over all four path roots"
    - "The remaining ~67 matches (AnimatedImageFeature 14, AppModels 13, HomeFeature 10, URLClient 8, FavoritesFeature 7, ~6 smaller modules ~15) are resolved via D-08 idioms"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "unchecked_subscript_index_access active at error with excluded patterns"
      contains: "unchecked_subscript_index_access"
  key_links:
    - from: ".swiftlint.yml"
      to: "SwiftLintBuildToolPlugin"
      via: "build-time enforcement once enabled"
      pattern: "severity: error"
---

<objective>
Resolve the remaining `unchecked_subscript_index_access` matches across AnimatedImageFeature, AppModels, HomeFeature, URLClient, FavoritesFeature, and the ~6 smaller modules, then flip the rule to error in the SAME commit as the last fix (Pitfall 10), keeping the draft's `excluded:` patterns (Tests files + the validatedIndex escape).

Purpose: completes the 239-site subscript inventory and lands the third refactor-gated rule flip.
Output: unchecked_subscript_index_access live at error with zero violations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-17 portion)

- Safe-idiom/guarded access across the six-plus remaining modules
- `.swiftlint.yml`: `unchecked_subscript_index_access` active at error with `excluded:` patterns preserved

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AnimatedImageFeature + AppModels + HomeFeature matches (~37)</name>
  <files>AppPackage/Sources/AnimatedImageFeature/, AppPackage/Sources/AppModels/, AppPackage/Sources/HomeFeature/</files>
  <read_first>
    - .swiftlint.yml (draft rule + exclusions, lines 189–199)
    - Files with matches in the three modules (enumerate by grepping the policed patterns; frame-index math in AnimatedImageFeature, model collection access in AppModels, carousel/ranking indexing in HomeFeature)
  </read_first>
  <action>
    Apply the D-08 idiom ladder module by module (honest renames for Optional Dictionary subscripts; first/last/prefix/zip/indices.contains guards with validatedIndex locals; precondition-checked exceptions with cited invariants as last resort). AnimatedImageFeature frame indexing: frame timing/order must not change (the dual image-stack routing is deliberate — do not touch KF/SD routing). HomeFeature carousel: the tripled-buffer infinite-loop index math is baseline-locked from Phase 3 — cite window invariants for checked exceptions rather than restructuring.
  </action>
  <verify>
    <automated>xcodebuild test -scheme HomeFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Policed-pattern grep across the three modules returns 0 matches outside sanctioned forms
    - HomeFeature scheme tests pass (carousel loop math intact)
  </acceptance_criteria>
  <done>Three modules clean at behavior parity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remaining modules + rule flip at error</name>
  <files>AppPackage/Sources/URLClient/, AppPackage/Sources/FavoritesFeature/, .swiftlint.yml</files>
  <read_first>
    - Files with matches in URLClient, FavoritesFeature, and any other module still showing policed patterns (final repo-wide enumeration)
  </read_first>
  <action>
    Resolve URLClient (8), FavoritesFeature (7), and every remaining match repo-wide (~15 across ~6 smaller modules — enumerate exhaustively; the RESEARCH counts are approximate at execution time). Then, in the SAME commit as the last fix: uncomment `unchecked_subscript_index_access` in .swiftlint.yml exactly as drafted — severity error, keeping both `excluded:` patterns (the Tests-file pattern and the validatedIndex pattern). Run the standalone binary (find it under "$HOME/Library/Developer/Xcode/DerivedData" at path pattern SwiftLintBinary*macos/swiftlint) with --reporter json over AppPackage/Sources AppPackage/Tests App ShareExtension; the rule must report 0. Fix any residual sites the binary finds that greps missed (the binary is authoritative). Build EhPanda + build-for-testing to prove the plugin gate.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - .swiftlint.yml has unchecked_subscript_index_access active at severity error with both excluded patterns
    - Standalone binary JSON report: 0 violations for the rule over all four path roots
    - build-for-testing succeeds
  </acceptance_criteria>
  <done>Rule live at error; 239-site inventory fully resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint config → build pipeline | Third refactor-gated flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-20 | Denial of Service | residual out-of-bounds traps | medium | mitigate | Binary zero-check is exhaustive over the policed patterns; guards route to existing error paths |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Standalone binary: 0 violations for unchecked_subscript_index_access
- HomeFeature tests + EhPanda build-for-testing green (sequential xcodebuild)
</verification>

<success_criteria>
- All 239 draft-rule matches resolved per D-08/D-16; rule enforced at error by every build
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-17-SUMMARY.md` when done
</output>
