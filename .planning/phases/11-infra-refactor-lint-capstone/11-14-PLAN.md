---
phase: 11-infra-refactor-lint-capstone
plan: 14
type: execute
wave: 14
depends_on: [11-13]
files_modified:
  - AppPackage/Sources/ParserFeature/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "ParserFeature's 43 unchecked-subscript matches are resolved via D-08 idioms with parser output unchanged"
    - "ParserFeatureTests pass unmodified — HTML extraction results are byte-identical"
  artifacts:
    - path: "AppPackage/Sources/ParserFeature/"
      provides: "subscript-rule-clean parser module"
  key_links:
    - from: "HTML node/array indexing"
      to: "guard-validated access"
      via: "indices.contains guards / first-last idioms preserving extraction semantics"
      pattern: "validatedIndex|indices.contains"
---

<objective>
Resolve ParserFeature's 43 `unchecked_subscript_index_access` matches per D-08. Parser code indexes into scraped HTML node collections and split-string arrays — untrusted-input indexing where a bounds guard is a genuine robustness win, not ceremony.

Purpose: second-largest subscript cluster; directly hardens the untrusted-HTML path.
Output: ParserFeature contributes zero draft-rule violations at identical parse output.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-14 portion)

- Guard-validated / safe-idiom access across ParserFeature files (no public API changes)
- Zero or more precondition-checked D-08 exception sites

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: List/Shared/Torrent parser files</name>
  <files>AppPackage/Sources/ParserFeature/</files>
  <read_first>
    - .swiftlint.yml (draft rule regex, lines 189–199)
    - AppPackage/Sources/ParserFeature/Parser+List.swift, Parser+Shared.swift, Parser+Torrent.swift (enumerate matches; understand each site's failure semantics — since plans 11-01/11-02, per-row failures log-and-drop and whole-parse failures throw)
  </read_first>
  <action>
    For each match: prefer the inherently safe idiom that preserves current semantics — where a missing element previously produced a thrown parse error or a dropped row, an `indices.contains`/`count` guard should feed the SAME failure path (throw the same parse error or drop the row), not introduce new fallbacks. Use `first`/`last`/`prefix`/`dropFirst` where the index is 0/last-relative; `zip` for parallel arrays; `validatedIndex` locals after explicit guards for computed indices. Precondition-checked exceptions only where a structural invariant (e.g. a regex match guaranteeing group count) makes the bound provable — cite that invariant in the reason comment. Parse output must be byte-identical on all fixtures.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ParserFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - ParserFeatureTests pass with zero assertion edits
    - Policed-pattern grep in the three files returns 0 matches outside validatedIndex/checked-exception sites
  </acceptance_criteria>
  <done>List/Shared/Torrent files clean; fixtures identical.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Detail/Profile/remaining parser files + module audit</name>
  <files>AppPackage/Sources/ParserFeature/</files>
  <read_first>
    - The remaining ParserFeature files with matches (Parser+Detail.swift and others from the Task 1 enumeration)
  </read_first>
  <action>
    Apply the same idiom ladder to the remaining files, then run the module-wide audit: policed-pattern grep across AppPackage/Sources/ParserFeature returns 0 matches outside sanctioned forms. Run the full ParserFeature suite once more; every fixture must produce identical output.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ParserFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Module-wide policed-pattern grep clean (sanctioned forms only)
    - ParserFeatureTests pass with zero assertion edits
  </acceptance_criteria>
  <done>ParserFeature subscript-rule-clean at parse parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted HTML → parser indexing | Malformed pages could previously trap on out-of-bounds access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-17 | Denial of Service | out-of-bounds trap on malformed HTML | high | mitigate | Guards route to existing parse-error paths — the crash surface becomes a handled AppError |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- ParserFeature scheme tests green ×2 runs (sequential xcodebuild)
- Module policed-pattern audit clean
</verification>

<success_criteria>
- 43 matches resolved; malformed-page indexing can no longer trap
- Parse output parity proven by unmodified fixtures
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-14-SUMMARY.md` when done
</output>
