---
phase: 11-infra-refactor-lint-capstone
plan: 18
type: execute
wave: 18
depends_on: [11-17]
files_modified:
  - AppPackage/Sources/GalleryListComponents/
  - AppPackage/Sources/DetailFeature/
  - AppPackage/Sources/ParserFeature/
  - AppPackage/Sources/NetworkingFeature/
  - AppPackage/Sources/DownloadClient/
  - .swiftlint.yml
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "Every multi-element tuple TYPE (return types, properties, typealiases, closure signatures) carries labels on every element, or has been replaced by a small named struct (D-10/D-11)"
    - "A new labeled_tuple_elements custom rule is enabled at error and reports 0 violations with zero false positives on the clean codebase"
    - "large_tuple stays enabled at its defaults — any tuple that would break it became a named struct (D-11)"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "new labeled_tuple_elements custom rule at error"
      contains: "labeled_tuple_elements"
  key_links:
    - from: ".swiftlint.yml labeled_tuple_elements"
      to: "tuple type positions repo-wide"
      via: "regex policing type positions after -> or : (construction literals not policed per D-10)"
      pattern: "labeled_tuple_elements"
---

<objective>
Implement D-10/D-11: label every element of every multi-element tuple type (~35 sites — the `(String, TagTranslation?)` translateAction closure type repeated ×10 across GalleryListComponents/DetailFeature; ParserFeature returns like `(String, String)`, `(GalleryDetail, GalleryState)`, `(URL?, Int)`, `(String, URL)`; NetworkingFeature `(PageNumber, [Gallery])` ×2 and `(String, [Int: String])`; DownloadClient `(DownloadedGallery, DownloadManifest)` and `(Data, URLResponse)` ×2), using a small named struct where labels are awkward or where `large_tuple` would fire. Then add the new `labeled_tuple_elements` custom rule, tune it to zero false positives on the now-clean codebase, and enable it at error in the same commit.

Purpose: the LINT-01 "new labeled-tuple-elements rule" deliverable.
Output: rule live at error, all tuple types labeled or struct-ified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-18 portion)

- Labeled tuple types across the five modules (e.g. the translateAction closure becomes a labeled signature like `(text: String, translation: TagTranslation?)`; URLSession pairs become `(data: Data, response: URLResponse)`)
- Possible small named structs where labels were awkward (executor names them per module conventions)
- `.swiftlint.yml`: new `labeled_tuple_elements` custom rule at error

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Label or struct-ify all multi-element tuple types (D-10/D-11)</name>
  <files>AppPackage/Sources/GalleryListComponents/, AppPackage/Sources/DetailFeature/, AppPackage/Sources/ParserFeature/, AppPackage/Sources/NetworkingFeature/, AppPackage/Sources/DownloadClient/</files>
  <read_first>
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md ("Unlabeled tuple types" inventory + Pattern 6)
    - AppPackage/Tests/ImageClientTests/ImageClientTestHelpers.swift (line 10 — an already-correct labeled tuple return, the target shape)
    - Every file with an unlabeled multi-element tuple type: enumerate repo-wide (Sources AND Tests) by grepping return-arrow and colon positions followed by an open-paren tuple with an unlabeled first element
  </read_first>
  <action>
    Enumerate ALL unlabeled multi-element tuple types repo-wide (the RESEARCH inventory is the starting map; re-enumerate at execution). For each: add semantic labels to every element (labels chosen from the value's domain meaning — e.g. the repeated translateAction closure type gains text/translation labels; parser pairs gain names like key/value or detail/state per site; URLSession result pairs become data/response). Where labeling reads awkwardly, or the tuple has 3+ members such that `large_tuple` (enabled at defaults) would fire, replace with a small named struct per D-11 — file-local or module-internal, minimal, `Feature`-suffix rules do NOT apply to plain structs. Remember Phase 4's compiler crash on a labeled-tuple expression in a task group: if the compiler misbehaves at any site, use the named-struct fix (D-11 sanctions it as equally valid). Update all construction/destructuring sites; construction literals themselves need no labels (D-10 polices types only, though adding them where trivial is fine). All module test suites still compile and pass.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Manual enumeration re-run finds 0 unlabeled multi-element tuple types in Sources and Tests
    - build-for-testing succeeds (all targets compile with the new signatures)
  </acceptance_criteria>
  <done>All tuple types labeled or struct-ified; repo compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add labeled_tuple_elements rule, tune to zero false positives, enable at error</name>
  <files>.swiftlint.yml</files>
  <read_first>
    - .swiftlint.yml (active custom-rule YAML shape, e.g. system_name_image_parameter at lines 180–187)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (Pattern 6 — the starting regex `(?:->|:)\s*\(\s*(?![_a-zA-Z][a-zA-Z0-9_]*\s*:)[^(){}\[\]]*,` and its known limits: nested closure types, generic argument lists, enum associated values correctly ignored)
  </read_first>
  <action>
    Add a `labeled_tuple_elements` custom rule to .swiftlint.yml using the RESEARCH Pattern 6 starting regex, message "Label every element of a multi-element tuple type, or use a small named struct.", excluded_match_kinds comment + string, severity error. Run the standalone binary over the four path roots; since Task 1 cleaned the codebase, ANY reported violation is either a missed site (fix it) or a false positive (tune the regex — iterate until the run reports 0 with no dodges). Sanity-check the rule still FIRES by temporarily adding an unlabeled tuple type to a scratch file under a lint path, observing one violation, then deleting the scratch file (positive control — do not commit it). Enable in the same commit as any final site fixes; build-for-testing proves the plugin gate.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - .swiftlint.yml contains an active labeled_tuple_elements custom rule at severity error
    - Standalone binary JSON report: 0 violations for labeled_tuple_elements over all four path roots
    - Positive control demonstrated in the work log (rule fires on a deliberate violation, then scratch removed)
  </acceptance_criteria>
  <done>New rule live at error with proven detection and zero violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint config → build pipeline | New rule gates all future builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-21 | Denial of Service | false-positive regex breaking builds on valid code | medium | mitigate | Tune-to-zero on clean codebase + positive control before commit |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Standalone binary: 0 violations for labeled_tuple_elements; positive control fired
- build-for-testing green (sequential xcodebuild)
</verification>

<success_criteria>
- D-10/D-11 delivered: all tuple types labeled or struct-ified; new rule enforced at error
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-18-SUMMARY.md` when done
</output>
