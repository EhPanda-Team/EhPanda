---
phase: 11-infra-refactor-lint-capstone
plan: 4
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient+Networking.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidationHelpers.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+BackgroundDownloads.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "The entire DownloadClient module contains zero unannotated optional-try sites"
    - "Download validation/networking/execution behavior is unchanged — primary download results are never replaced by cleanup/probe errors (Phase 9 invariant preserved)"
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift"
      provides: "explicit error handling preserving primary-result semantics"
  key_links:
    - from: "AppPackage/Sources/DownloadClient/DownloadClient+Networking.swift"
      to: "owning download reducers"
      via: "existing throwing APIs — authoritative failures already propagate (Phase 9)"
      pattern: "throws"
---

<objective>
Complete DownloadClient's optional-try elimination: the validation/networking/page-download/execution/background cluster (DownloadClient+Networking.swift, +PageDownload.swift, +ResponseValidation.swift, +ResponseValidationHelpers.swift, +ExecutionSupport.swift, +BackgroundDownloads.swift — the remaining ~20 of 36 sites), per D-01/D-02/D-03.

Purpose: finishes the module so the optional_try flip (plan 11-24) has no DownloadClient residue.
Output: DownloadClient module at zero unannotated optional-try sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: try? -->

## Artifacts this phase produces (plan 11-04 portion)

- do/catch replacements across the six networking/validation/execution files (no public API changes)
- Zero or more D-02 exception candidates for phase-end owner review

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Networking + PageDownload + BackgroundDownloads sites</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+Networking.swift, AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift, AppPackage/Sources/DownloadClient/DownloadClient+BackgroundDownloads.swift</files>
  <read_first>
    - The three files (every optional-try site + Phase 9 rationale comments — parsing probes, optional API fallbacks, cadence writes)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (Pattern 1; D-02 exception form)
  </read_first>
  <action>
    Apply the D-01 triage order from plan 11-03 (propagate per D-03 → explicit do/catch mirroring current fallback, logging unexpected failures → D-02 reason-annotated exception candidate as last resort) to every optional-try site in the three files. Preserve the Phase 9 invariants verbatim: parsing probes select alternates on failure (control flow — bare explanatory catch, no log spam); cadence writes and cleanup never interrupt a download (do/catch mirror); authoritative failures already propagate and must keep doing so. Background-download completion paths must not change ordering or delivery semantics. File-top `private let logger` where files gain log calls, using the module Logger extension from plan 11-03.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadClient -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered count of `try?` in the three files equals 0 excluding lines directly below a reason-annotated disable
    - DownloadClient scheme tests pass unmodified
    - `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>Networking/page/background sites converted; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ResponseValidation + ExecutionSupport sites + module-wide zero audit</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift, AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidationHelpers.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift</files>
  <read_first>
    - The three files (all optional-try sites + rationale comments)
  </read_first>
  <action>
    Convert the remaining validation/execution sites with the same triage. Phase 9 decision to preserve: cleanup and optional-probe failures preserve the primary validation or download result — the catch paths must not introduce a competing error surface. After conversion, run a module-wide audit: comment-filtered count of `try?` across ALL of AppPackage/Sources/DownloadClient must be 0 excluding reason-annotated exception sites; convert any straggler. Run the DownloadsFeature scheme tests to prove reducer-level download flows unchanged (deterministic since commit 557b0425 — any failure is a real regression, not flake).
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air' && grep -rn "try? " AppPackage/Sources/DownloadClient | grep -v "//" | wc -l  # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Module-wide comment-filtered `try?` count in AppPackage/Sources/DownloadClient equals 0 (reason-annotated exception sites excluded)
    - DownloadsFeature scheme tests pass unmodified
  </acceptance_criteria>
  <done>DownloadClient module fully converted; download flows verified green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote responses → validation | Untrusted download responses validated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information Disclosure | new logger calls | high | mitigate | Fixed descriptors + error values only; cookie-logging scan gate |
| T-11-05 | Denial of Service | catch paths altering download control flow | medium | mitigate | Mirror current fallbacks exactly; deterministic DownloadsFeature suite as behavioral gate |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DownloadClient + DownloadsFeature scheme tests green (sequential xcodebuild)
- Module-wide zero audit passes; cookie-logging scan exits 0
</verification>

<success_criteria>
- DownloadClient contributes zero unannotated optional_try violations
- Download behavior parity proven by unmodified green tests
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-04-SUMMARY.md` when done
</output>
