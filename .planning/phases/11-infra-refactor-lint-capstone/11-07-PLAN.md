---
phase: 11-infra-refactor-lint-capstone
plan: 7
type: execute
wave: 7
depends_on: [11-06]
files_modified:
  - AppPackage/Sources/HomeFeature/HomeView.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularView.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedView.swift
  - AppPackage/Sources/HomeFeature/History/HistoryView.swift
  - AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/SearchFeature/SearchRootView.swift
  - AppPackage/Sources/SearchFeature/SearchView.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "No view in HomeFeature, SearchFeature, or FavoritesFeature uses .onAppear/.onDisappear/.task (D-06), except sites carrying a D-02 reason-annotated exception"
    - "Every sub-page still loads its content when first shown — the presenting reducer fires the former onAppear effect on presentation (D-07)"
    - "TCA actions named onAppear are renamed or removed in these modules (the lint regex matches action usage — RESEARCH Pitfall 2)"
  artifacts:
    - path: "AppPackage/Sources/HomeFeature/HomeView.swift"
      provides: "lifecycle-modifier-free view layer"
  key_links:
    - from: "Home/Search/Favorites presenting reducers"
      to: "child feature load effects"
      via: "Destination/StackState presentation transition sends the child's load action"
      pattern: "state\\.(destination|path)"
---

<objective>
Migrate all lifecycle-modifier sites in HomeFeature (7), SearchFeature (2), and FavoritesFeature (1) to presentation-driven lifecycle per D-06/D-07: the reducer that presents a screen kicks off its former onAppear effect as part of the presentation state transition. Rename the corresponding onAppear-named TCA actions so the rule can reach zero (Pitfall 2).

Purpose: first of five lifecycle plans; establishes the migration idiom on the best-understood surfaces (fetch-if-empty-guarded list pages) before the heavier Detail/Reading modules.
Output: three modules lifecycle-modifier-free with load-on-presentation parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .onAppear -->
<!-- planner-discipline-allow: .task -->

## Artifacts this phase produces (plan 11-07 portion)

- Renamed child load actions (e.g. former onAppear actions become explicit load/presentation actions) in Home/Search/Favorites reducers
- Presentation-transition effect sends in HomeReducer/SearchReducer/FavoritesReducer (and sub-page reducers)
- Zero or more D-02 exception candidates (e.g. GalleryCardCell animation start)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HomeFeature sub-pages + root (D-07)</name>
  <files>AppPackage/Sources/HomeFeature/HomeView.swift, AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift, AppPackage/Sources/HomeFeature/Popular/PopularView.swift, AppPackage/Sources/HomeFeature/Watched/WatchedView.swift, AppPackage/Sources/HomeFeature/History/HistoryView.swift, AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift, AppPackage/Sources/HomeFeature/GalleryCardCell.swift</files>
  <behavior>
    - Presenting a sub-page (Frontpage/Popular/Watched/History/Toplists) fires its fetch effect exactly once per presentation — TestStore asserts the load effect on the presentation/navigation action, not on a view callback
    - Fetch-if-empty guards keep re-presentation idempotent (no duplicate loads)
  </behavior>
  <read_first>
    - AppPackage/Sources/HomeFeature/HomeReducer.swift and HomeReducer+Body.swift (lines 20–49 — StackState shape and .path(.element(id:action:)) interception; the presentation seams where load effects will fire)
    - AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift (lines 85–126 — destination presentation + typed-throws fetch pattern)
    - Each listed view file (the exact lifecycle-site shapes being removed; most are fetch-if-empty guarded sends)
    - AppPackage/Tests — Home-related TestStore suites asserting onAppear-driven effects (grep the test targets for the affected action names)
  </read_first>
  <action>
    For each sub-page: delete the view lifecycle modifier; make the PRESENTING reducer fire the child's former effect when it creates the child state — on the navigation/tap action that appends to `state.path` or sets `state.destination`, return the child's load effect (send the child's load action through the path/destination action, or intercept in `.path(.element(id:action:))`). Rename each child action currently named onAppear to a presentation-honest name (e.g. `.load`) — the lint regex matches action usage, so the old name cannot survive anywhere (declaration, reducer case, sends, tests). Cover EVERY construction site of each child state, not just the tap path — deep-link/URL/clipboard presentation routes must also trigger the load (Pitfall 4). HomeView's own site: Home is a tab root shown at launch — hook its initial load off the existing app-launch/tab-selection reducer flow in HomeReducer (the action that first makes Home active), not a view callback. GalleryCardCell's color-animation start is a store-less visual concern: attempt a non-banned mechanism first (e.g. animation driven by a value change or the card's appearance expressed via the existing Colorful API); if it genuinely cannot start without a lifecycle callback, write a D-02 reason + disable:next exception candidate. Audit each migrated site for re-fire-on-return reliance: fetch-if-empty guards are idempotent (parity holds); any non-idempotent site gets flagged in the SUMMARY for owner parity review (Open Question 3). Do not put effects in State.init. Update TestStore tests: assert the load effect fires on the presentation action.
  </action>
  <verify>
    <automated>xcodebuild test -scheme HomeFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens (onAppear/onDisappear/.task) in AppPackage/Sources/HomeFeature returns 0 matches outside reason-annotated exception sites
    - No TCA action named onAppear remains in HomeFeature (declarations, cases, sends, tests)
    - HomeFeature scheme tests pass with load-on-presentation assertions
  </acceptance_criteria>
  <done>HomeFeature lifecycle-free; loads fire on presentation; tests updated and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SearchFeature + FavoritesFeature (D-07)</name>
  <files>AppPackage/Sources/SearchFeature/SearchRootView.swift, AppPackage/Sources/SearchFeature/SearchView.swift, AppPackage/Sources/FavoritesFeature/FavoritesView.swift</files>
  <behavior>
    - Search results page and Favorites fire their former onAppear effects on presentation; TestStore asserts effect-on-presentation
  </behavior>
  <read_first>
    - The three view files (site shapes) and their reducers (SearchRootReducer/SearchReducer/FavoritesReducer — presentation seams)
    - Search/Favorites TestStore suites in AppPackage/Tests referencing the affected actions
  </read_first>
  <action>
    Apply the Task 1 idiom: remove the three lifecycle sites, fire former effects from the presenting reducer's state transition (Search results are pushed/presented by SearchRootReducer; Favorites is a tab root — hook its initial load off the tab-activation flow like HomeView). Rename affected onAppear actions. Same deep-link coverage, idempotence audit, and TestStore updates as Task 1. Then run a cross-module audit: lifecycle-token grep across HomeFeature, SearchFeature, FavoritesFeature must return only reason-annotated exception sites.
  </action>
  <verify>
    <automated>xcodebuild test -scheme SearchFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens in SearchFeature + FavoritesFeature returns 0 matches outside reason-annotated exception sites
    - SearchFeature scheme tests pass; FavoritesFeature builds (run its scheme tests if present)
  </acceptance_criteria>
  <done>All ten sites across the three modules migrated or exception-annotated; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View layer → reducers | Lifecycle initiative moves from views into reducers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Denial of Service | orphaned child effects after dismissal | medium | mitigate | TCA ifLet/forEach auto-cancel child effects on dismissal (verify with first migrated site's TestStore test — RESEARCH assumption A1) |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- HomeFeature + SearchFeature scheme tests green (sequential xcodebuild)
- Lifecycle-token grep clean across the three modules (exceptions annotated)
- A1 verification: one TestStore test proves child effect cancellation on dismissal/pop
</verification>

<success_criteria>
- 10 lifecycle sites migrated to presentation-driven lifecycle or exception-annotated (D-06/D-07)
- Load-on-presentation parity proven by updated TestStore tests
- Non-idempotent sites (if any) flagged in SUMMARY for owner parity review
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-07-SUMMARY.md` when done
</output>
