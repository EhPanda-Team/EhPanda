---
phase: 11-infra-refactor-lint-capstone
plan: 6
type: execute
wave: 6
depends_on: [11-05]
files_modified:
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsReducer.swift
  - AppPackage/Sources/LogsClient/LogsClient.swift
  - AppPackage/Sources/LibraryClient/LibraryClient.swift
  - AppPackage/Sources/ImageClient/ImageClient.swift
  - AppPackage/Sources/NetworkingFeature/Request.swift
  - AppPackage/Sources/NetworkingFeature/Request+Detail.swift
  - AppPackage/Sources/AppComponents/PreviewImageView.swift
  - AppPackage/Sources/AppComponents/TagSuggestionView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/MarkdownExt/MarkdownUtil.swift
  - AppPackage/Sources/FileClient/TagTranslation+ChtConverted.swift
  - AppPackage/Sources/DetailFeature/Components/LinkedText.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "AppPackage/Sources contains zero unannotated optional-try sites (Sources side of the D-15 inventory complete)"
    - "Each converted site preserves its Phase 9-documented fallback: HTML repair, greeting, archive funds, Chinese conversion, image-cache internals, activity-log pump continuity"
  artifacts:
    - path: "AppPackage/Sources/NetworkingFeature/Request.swift"
      provides: "explicit error handling at the HTML-repair fallback sites"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift"
      to: "activity-log pump lifecycle"
      via: "persistence failures stay internal and never interrupt the pump (Phase 9 09-10)"
      pattern: "catch"
---

<objective>
Root-fix the remaining 26 Sources optional-try sites across nine modules: SettingFeature (5), LogsClient (4), LibraryClient (4), ImageClient (4), NetworkingFeature (3), AppComponents (2), ReadingFeature (1), MarkdownExt (1), FileClient (1), DetailFeature (1) — per D-01/D-02/D-03. After this plan, ALL of AppPackage/Sources is optional-try-free (Tests are handled in plans 11-23/11-24 per D-15).

Purpose: completes the Sources half of the 316-site inventory so the flip plan only owes Tests.
Output: zero unannotated optional-try in AppPackage/Sources, App, ShareExtension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: try? -->

## Artifacts this phase produces (plan 11-06 portion)

- do/catch conversions across the thirteen listed files; possible module Logger extensions (init-only Logger+.swift) where a module gains its first log call
- Zero or more D-02 exception candidates for phase-end owner review

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Client modules — LogsClient, LibraryClient, ImageClient, FileClient, NetworkingFeature (16 sites)</name>
  <files>AppPackage/Sources/LogsClient/LogsClient.swift, AppPackage/Sources/LibraryClient/LibraryClient.swift, AppPackage/Sources/ImageClient/ImageClient.swift, AppPackage/Sources/FileClient/TagTranslation+ChtConverted.swift, AppPackage/Sources/NetworkingFeature/Request.swift, AppPackage/Sources/NetworkingFeature/Request+Detail.swift</files>
  <read_first>
    - Each listed file (optional-try sites + Phase 9 rationale comments)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (Pattern 1; D-02 form)
  </read_first>
  <action>
    Triage per D-01 (propagate per D-03 → explicit do/catch mirroring current fallback → D-02 exception candidate last). Phase 9 contracts to preserve verbatim: HTML repair, greeting, archive funds, and Chinese conversion remain optional because each has an explicit behavior-preserving fallback (09 decisions) — express those as do/catch selecting the fallback, with a brief comment for control-flow catches and `logger.error` only for unexpected failures; image-cache and activity-log persistence failures stay internal (never replace successful image acquisition); missing cached translations still recover through the remote fetch. Preserve all public Optional/Result signatures. Never log cookie values, credentials, or URL contents — the NetworkingFeature files are the highest-risk surface for the Phase 8 privacy scan.
  </action>
  <verify>
    <automated>xcodebuild test -scheme NetworkingFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered `try?` count in the six files equals 0 (reason-annotated sites excluded)
    - NetworkingFeature scheme tests pass unmodified
    - `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>Client-module sites converted; networking tests green; privacy scan clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Feature/UI modules + Sources-wide zero audit (10 sites)</name>
  <files>AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsReducer.swift, AppPackage/Sources/AppComponents/PreviewImageView.swift, AppPackage/Sources/AppComponents/TagSuggestionView.swift, AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/MarkdownExt/MarkdownUtil.swift, AppPackage/Sources/DetailFeature/Components/LinkedText.swift</files>
  <read_first>
    - Each listed file (optional-try sites + context)
  </read_first>
  <action>
    Convert the remaining feature/UI sites with the same triage. Activity-log pump: persistence failures must never interrupt the pump (explicit catch keeps the pump alive, log the failure). View-layer sites (PreviewImageView, TagSuggestionView, ReadingView, LinkedText, MarkdownUtil): preserve the render fallback each currently has — a failed parse/decode renders the existing fallback UI; use do/catch yielding the same fallback value. After conversion, run the Sources-wide zero audit: comment-filtered `try?` count across AppPackage/Sources, App/, and ShareExtension/ must equal 0 (reason-annotated sites excluded); convert stragglers. Then build the EhPanda scheme to prove the whole Sources graph compiles.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' && grep -rn "try? " AppPackage/Sources App ShareExtension | grep -v "//" | wc -l  # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered `try?` count across AppPackage/Sources, App, ShareExtension equals 0 (reason-annotated sites excluded)
    - EhPanda scheme builds clean
    - `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>All Sources optional-try sites resolved; app builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote responses → NetworkingFeature | HTML-repair fallback handles untrusted content |
| New log lines → OSLog | Highest-risk surface: networking/cookie-adjacent code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Information Disclosure | new logger calls near request/cookie machinery | high | mitigate | Fixed descriptors + error values only; `Scripts/check-cookie-logging.sh` per task |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- NetworkingFeature scheme tests + EhPanda build green (sequential xcodebuild)
- Sources-wide zero audit passes; cookie-logging scan exits 0
</verification>

<success_criteria>
- AppPackage/Sources + App + ShareExtension contribute zero unannotated optional_try violations
- All Phase 9 fallback contracts preserved; no signature changes
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-06-SUMMARY.md` when done
</output>
