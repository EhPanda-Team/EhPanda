---
phase: 11-infra-refactor-lint-capstone
plan: 20
type: execute
wave: 20
depends_on: [11-19]
files_modified:
  - AppPackage/Tests/DownloadsFeatureTests/DownloadImageParsingTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadImageParsingCacheTests.swift
  - AppPackage/Tests/ImageClientTests/ImageClientTests.swift
  - AppPackage/Tests/CookieClientTests/DidLoginKeyTests.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "The two DownloadImageParsing suites no longer touch the process-shared Kingfisher cache — an injected/isolated ImageCache per test replaces it — and run without serialization (D-12)"
    - "ImageClientTests runs without its vestigial serialization trait (isolation machinery already per-test) and stays green"
    - "DidLoginKeyTests keeps its single-sequential trait WITH the D-14 in-file rationale comment (Sharing's process-global weak reference cache keyed by the constant key id)"
  artifacts:
    - path: "AppPackage/Tests/CookieClientTests/DidLoginKeyTests.swift"
      provides: "documented retained-serialization exception"
      contains: "Serialized:"
  key_links:
    - from: "DownloadImageParsing suites"
      to: "isolated ImageCache instances"
      via: "injected cache (or unique per-test cache instance/path) replacing the shared singleton read"
      pattern: "ImageCache"
---

<objective>
Close the remaining known shared-state seams: inject the Kingfisher `ImageCache` used by the code paths that DownloadImageParsingTests and DownloadImageParsingCacheTests exercise (replacing `KingfisherManager.shared.cache` reliance) so each test uses an isolated cache, drop those two suites' serialization; remove ImageClientTests' vestigial serialization trait; add the missing D-14 rationale comment to DidLoginKeyTests (which legitimately stays sequential).

Purpose: after this plan, only diagnosed-and-documented serialization remains outside DownloadsFeatureTests' bulk (handled next).
Output: two suites parallel via cache seam; one vestigial trait removed; one documented keeper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .serialized -->

## Artifacts this phase produces (plan 11-20 portion)

- Injectable Kingfisher ImageCache seam (production default = shared cache) on the exercised production path, or per-test isolated ImageCache instances where the cache use is test-local
- D-14 rationale comment in DidLoginKeyTests.swift

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Kingfisher cache isolation for the two DownloadImageParsing suites (D-12)</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/DownloadImageParsingTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadImageParsingCacheTests.swift</files>
  <read_first>
    - The two test files (how KingfisherManager.shared.cache enters: which production call reads it, what keys are shared, what the defer cleanup removes)
    - AppPackage/Tests/ImageClientTests/ImageClientTestHelpers.swift (makeIsolatedDataCache — the per-test isolated-instance idiom)
    - The production code under test that resolves the Kingfisher cache (trace from the test's entry points)
  </read_first>
  <action>
    Diagnose precisely where the shared Kingfisher cache enters these suites. If a PRODUCTION path resolves `KingfisherManager.shared.cache`, add an injectable seam on that path (parameter or dependency with the shared cache as production default — Phase 8 DataCache precedent; D-12 sanctions production seams) and have each test inject a uniquely-named/pathed `ImageCache` instance created and torn down per test. If the shared cache is only touched TEST-side (fixture priming), replace it directly with per-test isolated `ImageCache` instances. Either way: remove the serialization trait from both suites, delete the defer-based shared-key cleanup that existed only to serialize safely, and run the DownloadsFeature scheme tests to prove stability alongside the rest of the target.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Neither suite reads the shared Kingfisher cache (grep both files for the shared-manager token returns 0)
    - Neither suite carries a serialization trait
    - DownloadsFeature scheme tests pass
  </acceptance_criteria>
  <done>Kingfisher isolation landed; both suites parallel-capable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ImageClientTests vestigial trait removal + DidLoginKeyTests rationale (D-14)</name>
  <files>AppPackage/Tests/ImageClientTests/ImageClientTests.swift, AppPackage/Tests/CookieClientTests/DidLoginKeyTests.swift</files>
  <read_first>
    - AppPackage/Tests/ImageClientTests/ImageClientTests.swift (confirm per-test isolation: makeIsolatedDataCache + per-sessionID stubs — the trait should be vestigial)
    - AppPackage/Tests/FileClientTests/FileClientTests.swift as it was before plan 11-19 is gone — instead read AppPackage/Tests/DownloadsFeatureTests for any remaining rationale-comment example, plus 11-PATTERNS.md G7 (the exact comment shape to copy)
    - AppPackage/Tests/CookieClientTests/DidLoginKeyTests.swift (current trait, no rationale yet)
  </read_first>
  <action>
    ImageClientTests: remove the serialization trait; run the ImageClient scheme tests twice to confirm the isolation machinery (per-test DataCache instances, per-sessionID URLProtocol stubs) really was sufficient; if a hidden shared dependency surfaces, fix it by injection (never by re-adding the trait). Remember the image-test invariants: per-test DataCache (never a shared one) and pixel-dimension assertions with scale-1 fixtures. DidLoginKeyTests: ADD the D-14 rationale comment directly above the suite trait, following the PATTERNS G7 comment shape, stating: Sharing's reference cache is a process-global weak table keyed by the key's constant id, and per-test key ids would stop testing the production didLogin key — so this suite stays deliberately single-sequential. Do not change its tests.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ImageClient -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - ImageClientTests.swift carries no serialization trait; ImageClient scheme tests pass on two consecutive runs
    - DidLoginKeyTests.swift retains its trait with a rationale comment beginning "// Serialized:" directly above it
  </acceptance_criteria>
  <done>Vestigial trait gone; the one legitimate keeper documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seam → production cache identity | Injected cache must default to the shared production cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-23 | Tampering | production image-cache identity drift via seam | high | mitigate | Production default remains the shared cache; only tests inject; DownloadsFeature suite gates behavior |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DownloadsFeature + ImageClient scheme tests green (sequential xcodebuild; ImageClient ×2)
</verification>

<success_criteria>
- Kingfisher shared-cache dependency eliminated from the two suites (D-12); ImageClientTests parallel; DidLoginKeyTests documented (D-14)
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-20-SUMMARY.md` when done
</output>
