---
phase: 11-infra-refactor-lint-capstone
plan: 19
type: execute
wave: 19
depends_on: [11-18]
files_modified:
  - AppPackage/Sources/FileClient/
  - AppPackage/Tests/FileClientTests/FileClientTests.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "FileClient's tag-translation cache/import endpoints accept an injectable directory root with the current production paths as default (D-12, DownloadStore rootURL precedent)"
    - "FileClientTests runs without .serialized — each test writes under its own UUID-scoped root — and passes in parallel"
    - "Production behavior is unchanged: default roots resolve to the same Caches/Application Support locations as before"
  artifacts:
    - path: "AppPackage/Sources/FileClient/"
      provides: "injectable directory-root seam on cache/import endpoints"
    - path: "AppPackage/Tests/FileClientTests/FileClientTests.swift"
      provides: "parallel-safe suite off fixed paths"
  key_links:
    - from: "AppPackage/Tests/FileClientTests/FileClientTests.swift"
      to: "FileClient injectable root"
      via: "per-test UUID-scoped root injection (inject-over-serialize)"
      pattern: "UUID"
---

<objective>
Land the D-12 FileClient seam: the tag-translation cache/import endpoints currently write fixed real Caches/Application Support paths, forcing FileClientTests to be `.serialized`. Add an injectable directory root defaulting to the production paths (mirroring DownloadStore's `rootURL` shape), rewrite FileClientTests onto per-test UUID-scoped roots, and drop the serialization trait (its rationale comment becomes obsolete and is removed with it).

Purpose: first Wave-0 gap from RESEARCH's Validation Architecture; the model seam for the remaining parallelization work.
Output: parallel-safe FileClientTests on an injected seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .serialized -->

## Artifacts this phase produces (plan 11-19 portion)

- Injectable directory-root parameter/dependency on FileClient's tag-translation cache/import endpoints (production default preserved)
- Rewritten parallel FileClientTests

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FileClient injectable-root seam (D-12)</name>
  <files>AppPackage/Sources/FileClient/</files>
  <read_first>
    - AppPackage/Sources/FileClient/ — the live implementation of the tag-translation cache/import endpoints (which endpoints touch Caches and Application Support, and how the paths are built)
    - AppPackage/Sources/DownloadClient/DownloadStore.swift (lines 59–66 — the injectable rootURL-with-production-default shape) and DownloadClient.swift (lines 38–42 — threading it through)
    - AppPackage/Tests/FileClientTests/FileClientTests.swift (lines 1–20 — the serialization rationale naming the exact fixed paths)
  </read_first>
  <action>
    Add an injectable directory-root seam to every FileClient endpoint that currently derives a fixed Caches or Application Support path: a stored root (or per-endpoint root parameters, matching how the client is structured) whose DEFAULT is the exact current production path derivation — so the live dependency's behavior is bit-identical and only tests override. Follow the DownloadStore shape: default parameter at init/construction, threaded to path builders. Do not add a thin wrapper client; this is a parameter seam on the existing client. Keep FileClient's Phase 9 error contracts (fixed operation descriptors in AppError context — never disclose paths) unchanged.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - FileClient exposes an injectable directory root; the default resolves to the identical production paths (assert by reading the default expression against the previous path derivation)
    - EhPanda scheme builds clean; no call-site changes outside FileClient needed for production paths
  </acceptance_criteria>
  <done>Seam landed with production defaults intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FileClientTests parallel rewrite</name>
  <files>AppPackage/Tests/FileClientTests/FileClientTests.swift</files>
  <behavior>
    - Each test case operates entirely under its own UUID-scoped temporary root (create in setup, remove in cleanup)
    - The full FileClientTests suite passes without any serialization trait
  </behavior>
  <read_first>
    - AppPackage/Tests/FileClientTests/FileClientTests.swift (current fixed-path usage per test)
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift (the UUID-scoped temporary-root helper family — the pattern to mirror)
  </read_first>
  <action>
    Rewrite each FileClientTests case to construct its FileClient (or endpoint invocation) with a per-test UUID-scoped root under the system temporary directory, mirroring the DownloadFeatureTestHelpers idiom. Remove the suite's serialization trait AND its now-obsolete head rationale comment (the comment documents the fixed-path race this seam removes). Cleanup uses explicit do/catch (no optional-try — plans 11-23/24 enforce it; write it clean now). Run the FileClient scheme tests twice consecutively to demonstrate stability.
  </action>
  <verify>
    <automated>xcodebuild test -scheme FileClient -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - FileClientTests.swift contains no serialization trait and no fixed real-directory writes
    - FileClient scheme tests pass on two consecutive runs
  </acceptance_criteria>
  <done>FileClientTests parallel-safe and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seam → production behavior | The injectable root must default to production paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-22 | Tampering | seam changing production storage location | high | mitigate | Default expression preserves exact current derivation; acceptance criterion asserts it; Phase 8 DataCache precedent |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- EhPanda build + FileClient scheme tests ×2 green (sequential xcodebuild)
</verification>

<success_criteria>
- D-12 FileClient seam landed; FileClientTests parallel with zero serialization
- Production path derivation provably unchanged
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-19-SUMMARY.md` when done
</output>
