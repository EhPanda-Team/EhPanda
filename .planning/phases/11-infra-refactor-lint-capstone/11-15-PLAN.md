---
phase: 11-infra-refactor-lint-capstone
plan: 15
type: execute
wave: 15
depends_on: [11-14]
files_modified:
  - AppPackage/Sources/DownloadClient/
  - AppPackage/Sources/NetworkingFeature/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "DownloadClient's 29 and NetworkingFeature's 12 unchecked-subscript matches are resolved via D-08 idioms with behavior unchanged"
    - "Download scheduling/parsing and request assembly behavior proven by unmodified green suites"
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/"
      provides: "subscript-rule-clean download module"
  key_links:
    - from: "chunking/pagination index math"
      to: "guard-validated access"
      via: "safe idioms preserving 25-pair chunking and page ordering invariants"
      pattern: "validatedIndex|indices.contains"
---

<objective>
Resolve the `unchecked_subscript_index_access` matches in DownloadClient (29) and NetworkingFeature (12) per D-08, preserving the Phase 4 invariants: 25-pair gdata chunking, two-in-flight flood control, input-order reconstruction, and page-URL ordering.

Purpose: continues the subscript track through the request/download layer.
Output: both modules contribute zero draft-rule violations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-15 portion)

- Guard-validated / safe-idiom access across DownloadClient and NetworkingFeature (no public API changes)
- Zero or more precondition-checked D-08 exception sites

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DownloadClient matches (29)</name>
  <files>AppPackage/Sources/DownloadClient/</files>
  <read_first>
    - .swiftlint.yml (draft rule regex, lines 189–199)
    - DownloadClient files with matches (enumerate by grep; page-index bookkeeping, chunk assembly, manifest ordering)
  </read_first>
  <action>
    Apply the D-08 idiom ladder (honest rename for Optional Dictionary subscripts → first/last/prefix/zip/indices.contains guards with validatedIndex locals → precondition-checked exceptions with the invariant cited). Ordering-sensitive code (chunk reconstruction, page sequencing) must keep identical output order — the deterministic DownloadsFeature suites are the behavioral gate. No public API changes.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadClient -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Policed-pattern grep in AppPackage/Sources/DownloadClient returns 0 matches outside sanctioned forms
    - DownloadClient scheme tests pass unmodified
  </acceptance_criteria>
  <done>DownloadClient clean; ordering invariants intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NetworkingFeature matches (12) + cross-module audit</name>
  <files>AppPackage/Sources/NetworkingFeature/</files>
  <read_first>
    - NetworkingFeature files with matches (request assembly, response slicing)
  </read_first>
  <action>
    Same idiom ladder for NetworkingFeature. Request assembly and response slicing must produce identical requests/parses — the Phase 4 parity suites (NetworkingFeatureTests) are the gate. Then audit both modules: policed-pattern grep clean across DownloadClient + NetworkingFeature, and run the DownloadsFeature scheme tests to cover the integrated path.
  </action>
  <verify>
    <automated>xcodebuild test -scheme NetworkingFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Policed-pattern grep across both modules returns 0 matches outside sanctioned forms
    - NetworkingFeature scheme tests pass unmodified
  </acceptance_criteria>
  <done>Both modules subscript-rule-clean at parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote responses → slicing/indexing | Malformed responses could previously trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Denial of Service | out-of-bounds trap on malformed response data | high | mitigate | Guards route into existing error paths; parity suites gate behavior |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DownloadClient + NetworkingFeature scheme tests green (sequential xcodebuild)
- Cross-module policed-pattern audit clean
</verification>

<success_criteria>
- 41 matches resolved per D-08 with request/download behavior unchanged
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-15-SUMMARY.md` when done
</output>
