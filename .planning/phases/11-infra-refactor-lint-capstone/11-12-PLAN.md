---
phase: 11-infra-refactor-lint-capstone
plan: 12
type: execute
wave: 12
depends_on: [11-11]
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Sources/PreviewSupport/PreviewIdentifiers.swift
  - AppPackage/Sources/PreviewSupport/.swiftlint.yml
  - AppPackage/Sources/HomeFeature/GalleryRankingCell.swift
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
  - AppPackage/Sources/SearchFeature/GalleryHistoryCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "A PreviewSupport module exists holding a fixed table of 1,000 stable UUID strings, exposed ONLY through a precondition-checked static subscript (D-09)"
    - "The 5 preview files that combined #Preview with random UUID() now use stable identities from the table"
    - "The new module has its own .swiftlint.yml chaining to the parent config and carries the SwiftLint build plugin"
  artifacts:
    - path: "AppPackage/Sources/PreviewSupport/PreviewIdentifiers.swift"
      provides: "count-asserted stable UUID table + checked static subscript"
      contains: "public static subscript"
    - path: "AppPackage/Sources/PreviewSupport/.swiftlint.yml"
      provides: "parent_config chain per repo convention"
      contains: "parent_config"
  key_links:
    - from: "AppPackage/Package.swift"
      to: "PreviewSupport target"
      via: "new target with swiftLintPlugins; consumer targets gain the dependency"
      pattern: "PreviewSupport"
---

<objective>
Create the PreviewSupport module per D-09: a public enum holding 1,000 stable UUID strings, `compactMap(UUID.init(uuidString:))` into `all` with an assertionFailure if the count deviates, exposed only through a `public static subscript(index: Int) -> UUID` whose body is a `precondition(all.indices.contains(index), …)` followed by the single sanctioned reason-annotated disable. Switch the 5 UUID()-in-#Preview consumers to stable identities.

Purpose: deterministic preview fixture identities (D-09) and the in-repo reference implementation of the D-08 checked-subscript idiom before the subscript-rule work scales.
Output: new module + 5 migrated consumers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-12 portion)

- New module `PreviewSupport` (target in AppPackage/Package.swift)
- `public enum PreviewIdentifiers` with `public static subscript(index: Int) -> UUID` and private `all`/string-table storage
- `AppPackage/Sources/PreviewSupport/.swiftlint.yml` (parent_config stub)
- Stable-identity preview fixtures in the 5 consumer files

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PreviewSupport module — table, checked subscript, package wiring (D-09)</name>
  <files>AppPackage/Package.swift, AppPackage/Sources/PreviewSupport/PreviewIdentifiers.swift, AppPackage/Sources/PreviewSupport/.swiftlint.yml</files>
  <read_first>
    - .planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md (§Specifics — the verbatim reproduced design: table shape, count assertion, subscript body, in-file disables; this design came from a reference project reproduced name-free — NEVER name any source project in code, comments, or docs)
    - AppPackage/Package.swift (target declaration shape around the appModels target — copy the `.target(module:dependencies:plugins: swiftLintPlugins)` form)
    - AppPackage/Sources/DownloadClient/.swiftlint.yml (the one-line parent_config stub to copy)
  </read_first>
  <action>
    Add a `PreviewSupport` target to AppPackage/Package.swift (Foundation-only dependencies, `plugins: swiftLintPlugins`) and add it as a dependency of HomeFeature, SearchFeature, and GalleryListComponents. Create `PreviewIdentifiers.swift`: `public enum PreviewIdentifiers` containing (1) a private array of exactly 1,000 UUID string literals — generate them once with `uuidgen` (lowercase or uppercase consistently) and commit them; they must never change afterward; (2) `static let all` built via `compactMap(UUID.init(uuidString:))` with an `assertionFailure` if `all.count` deviates from the string count; (3) the SOLE public access point: `public static subscript(index: Int) -> UUID` whose body is `precondition(all.indices.contains(index), "index out of bounds")` followed by `// reason: bounds are precondition-checked immediately above.` + `// swiftlint:disable:next unchecked_subscript_index_access` + the indexed access. The file will exceed the 1,000-line file_length limit — carry a reason-annotated `swiftlint:disable file_length` at the top (blanket_disable_command is globally disabled so a file-level disable is permitted, but it still requires the preceding `// reason:` line). Create `AppPackage/Sources/PreviewSupport/.swiftlint.yml` containing exactly `parent_config: ../../../.swiftlint.yml` (project rule: every new module gets one).
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - AppPackage/Sources/PreviewSupport/PreviewIdentifiers.swift exists; contains `public static subscript(index: Int) -> UUID` and `precondition(all.indices.contains(index)`
    - The string table has exactly 1,000 entries; `all` is count-asserted
    - AppPackage/Sources/PreviewSupport/.swiftlint.yml contains `parent_config: ../../../.swiftlint.yml`
    - EhPanda scheme builds clean (plugin lints the new module)
  </acceptance_criteria>
  <done>Module builds under the lint plugin with the checked subscript as sole exposure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate the 5 UUID()-in-#Preview consumers</name>
  <files>AppPackage/Sources/HomeFeature/GalleryRankingCell.swift, AppPackage/Sources/HomeFeature/GalleryCardCell.swift, AppPackage/Sources/SearchFeature/GalleryHistoryCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift</files>
  <read_first>
    - The 5 consumer files (their #Preview blocks and how UUID() feeds fixture models)
  </read_first>
  <action>
    In each #Preview block, add `import PreviewSupport` and replace every random `UUID()` fixture identity with `PreviewIdentifiers[n]`, assigning distinct consecutive indices within each file (previews across files may reuse indices — determinism matters, uniqueness within a rendered preview does). Preview content/state matrices stay exactly as delivered in Phase 10 (default environment — no DT/scheme variants per its D-09 constraint). Production (non-preview) code paths must be untouched.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep for `UUID()` inside #Preview blocks of the 5 files returns 0 matches
    - Each file imports PreviewSupport and uses `PreviewIdentifiers[` in previews
    - git diff confirms no production (non-#Preview) lines changed in the 5 files
  </acceptance_criteria>
  <done>All 5 preview consumers use stable identities; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview-only module → app targets | PreviewSupport must not leak into production behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Tampering | stable UUIDs used outside previews | low | mitigate | Consumers restricted to #Preview blocks; diff-checked in acceptance criteria |
| T-11-SC | Tampering | package installs | low | accept | New LOCAL module only — no external packages |
</threat_model>

<verification>
- EhPanda build green (module + consumers under plugin)
- Local reference privacy honored: no source-project name anywhere (absolute rule)
</verification>

<success_criteria>
- D-09 delivered: stable, count-asserted, checked-subscript-only preview identity table
- 5 consumers migrated at preview parity
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-12-SUMMARY.md` when done
</output>
