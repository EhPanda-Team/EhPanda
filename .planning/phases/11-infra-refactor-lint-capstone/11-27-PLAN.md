---
phase: 11-infra-refactor-lint-capstone
plan: 27
type: execute
wave: 27
depends_on: [11-26]
files_modified:
  - AppPackage/Tests/
  - .swiftlint.yml
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "single_line_trailing_closure is enabled at error and the standalone binary reports 0 violations over all four path roots (~74 Tests sites rewrapped)"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "single_line_trailing_closure active at error"
      contains: "single_line_trailing_closure"
  key_links:
    - from: ".swiftlint.yml"
      to: "test targets via the build plugin"
      via: "build-for-testing proves the Tests lint gate"
      pattern: "severity: error"
---

<objective>
Rewrap the Tests half of the `single_line_trailing_closure` inventory (~74 sites) and flip the rule to error in the SAME commit (Pitfall 10), proving the test-target lint gate with build-for-testing (Pitfall 6).

Purpose: completes the 223-site rewrap and lands the rule.
Output: single_line_trailing_closure live at error, zero violations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-27 portion)

- Rewrapped closures across AppPackage/Tests
- `.swiftlint.yml`: `single_line_trailing_closure` active at error

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrap all Tests violations</name>
  <files>AppPackage/Tests/</files>
  <read_first>
    - .swiftlint.yml (draft at lines 162–169 — the policed method list includes withValue/withLock/withDependencies/sink/task, all common in tests)
  </read_first>
  <action>
    Use the plan 11-26 enumeration technique (temporarily uncomment the draft, binary --reporter json over AppPackage/Tests, capture list) and rewrap every Tests site: parenthesized closure or multi-line break, never altering assertions or dependency-override semantics (`withDependencies` blocks are common — keep operation/init-capture semantics exactly; only reformat). Revert the temporary config edit when enumeration is done — the real flip happens in Task 2's commit.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Temporarily-enabled rule reports 0 violations over AppPackage/Tests
    - build-for-testing succeeds
  </acceptance_criteria>
  <done>Tests rewrapped; repo clean for the rule.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip single_line_trailing_closure to error + full verification</name>
  <files>.swiftlint.yml</files>
  <read_first>
    - .swiftlint.yml (the draft block to uncomment verbatim)
  </read_first>
  <action>
    Uncomment the single_line_trailing_closure rule exactly as drafted (severity error) in the same commit as any final rewrap fixes. Run the standalone binary with --reporter json over all four path roots: the rule must report 0 — fix residuals it finds (grep/regex divergence makes the binary authoritative). Then run the full suite to prove zero behavior change from the entire 223-site rewrap.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - .swiftlint.yml has single_line_trailing_closure active at severity error
    - Standalone binary JSON report: 0 violations for the rule over all four path roots
    - Full EhPanda-scheme suite passes in parallel
  </acceptance_criteria>
  <done>Rule live at error; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint config → build pipeline | Mechanical-rule flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-30 | Denial of Service | flip with residual Tests violations | medium | mitigate | Binary zero-check + build-for-testing in the flip commit |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Binary zero-check; build-for-testing + full suite green (sequential xcodebuild)
</verification>

<success_criteria>
- 223-site rewrap complete; rule enforced at error with suite-proven parity
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-27-SUMMARY.md` when done
</output>
