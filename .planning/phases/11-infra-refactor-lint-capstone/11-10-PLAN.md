---
phase: 11-infra-refactor-lint-capstone
plan: 10
type: execute
wave: 10
depends_on: [11-09]
files_modified:
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift
  - AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift
  - AppPackage/Sources/FiltersFeature/FiltersView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "No SettingFeature/FiltersFeature/DownloadsFeature view uses .onAppear/.onDisappear/.task except reason-annotated exceptions (D-06)"
    - "Setting screens load on presentation from the Setting StackState/Destination transitions; onDisappear teardown moves to dismissal interception (D-07)"
    - "The AppActivityLogs pump starts on presentation and stops on dismissal with identical pump semantics (Phase 7 exactly-once tests still green)"
  artifacts:
    - path: "AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift"
      provides: "lifecycle-modifier-free Setting screen"
  key_links:
    - from: "SettingReducer StackState/Destination transitions"
      to: "child screen load/teardown effects"
      via: "presentation sends load; dismissal interception sends teardown"
      pattern: "state\\.(path|destination)"
---

<objective>
Migrate the 8 lifecycle sites in SettingFeature (5 — AccountSetting, AppActivityLogs, EhSetting, GeneralSetting, including an onDisappear teardown), FiltersFeature (1), and DownloadsFeature (2) to presentation-driven lifecycle (D-06/D-07). AccountSetting is the named Pitfall 4 risk (login-state re-check on return) — audit and resolve or flag.

Purpose: completes the feature-module lifecycle migrations before the components/flip plan.
Output: three more modules lifecycle-modifier-free at behavior parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .onAppear -->
<!-- planner-discipline-allow: .onDisappear -->
<!-- planner-discipline-allow: .task -->

## Artifacts this phase produces (plan 11-10 portion)

- Renamed load/teardown actions in Setting/Filters/Downloads reducers
- Dismissal-interception teardown in SettingReducer for the former onDisappear site

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingFeature screens (5 sites incl. onDisappear teardown)</name>
  <files>AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift, AppPackage/Sources/SettingFeature/GeneralSetting/GeneralSettingView.swift</files>
  <behavior>
    - Each Setting screen's former onAppear effect fires when SettingReducer presents it (StackState push / Destination set)
    - The former onDisappear teardown fires on dismissal interception (pop/dismiss), not a view callback
    - Phase 7 activity-log pump exactly-once tests remain green
  </behavior>
  <read_first>
    - The four view files (site shapes; note which are store sends vs view-local)
    - SettingFeature's presenting reducer(s) (SettingReducer + the delegate-driven StackState design from the Phase 3 setting-stack refactor) — the push/pop seams
    - AppPackage/Tests/SettingFeatureTests (suites asserting onAppear-driven effects and pump lifecycle)
  </read_first>
  <action>
    For each screen: remove the view lifecycle modifier; SettingReducer fires the child's former load effect on the StackState/Destination transition that presents it, and fires the former onDisappear teardown by intercepting the pop/dismissal transition (StackAction element removal or ifLet dismissal — TCA auto-cancels child effects on dismissal, so explicit teardown is only needed where the old onDisappear did real work beyond cancellation, e.g. stopping the AppActivityLogs pump). Rename all affected onAppear/onDisappear-named actions. AccountSetting specifically: determine whether its login-state check relied on re-fire when returning from a deeper push (the in-app web login flow); if so, have the login flow's completion action re-send the check explicitly (root fix), or flag the parity question in the SUMMARY for owner review (Pitfall 4 / Open Question 3). Update SettingFeatureTests accordingly — the Phase 7 pump pause-after-actions pattern must keep passing.
  </action>
  <verify>
    <automated>xcodebuild test -scheme SettingFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens in AppPackage/Sources/SettingFeature returns 0 matches outside reason-annotated exception sites
    - No TCA action named onAppear or onDisappear remains in SettingFeature
    - SettingFeature scheme tests pass, including the activity-log pump exactly-once suites
  </acceptance_criteria>
  <done>Setting screens presentation-driven; pump lifecycle intact; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FiltersFeature + DownloadsFeature (3 sites)</name>
  <files>AppPackage/Sources/FiltersFeature/FiltersView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView+Subviews.swift</files>
  <behavior>
    - Filters loads on presentation (presented from list toolbars via Destination — FrontpageReducer.filtersButtonTapped is the canonical presenter)
    - Downloads list/inspector former lifecycle effects fire on presentation/tab activation
  </behavior>
  <read_first>
    - The three view files (site shapes)
    - FiltersFeature and DownloadsFeature reducers (presentation seams; Downloads is a tab root — its initial load hooks the tab-activation flow like Home/Favorites in plan 11-07)
    - AppPackage/Tests/DownloadsFeatureTests reducer suites referencing the affected actions
  </read_first>
  <action>
    Apply the established idiom: remove the three lifecycle sites; presenting reducers fire former effects on presentation transitions (every FiltersReducer.State construction site — grep all presenters, several list screens present Filters); Downloads tab root hooks tab activation. Rename affected actions. Update TestStore tests. Then audit all three modules of this plan: lifecycle-token grep over SettingFeature, FiltersFeature, DownloadsFeature returns only reason-annotated exception sites.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens in FiltersFeature + DownloadsFeature returns 0 matches outside reason-annotated exception sites
    - DownloadsFeature scheme tests pass
  </acceptance_criteria>
  <done>All 8 sites of this plan migrated or exception-annotated; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View layer → reducers | Lifecycle initiative moves into reducers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13 | Denial of Service | pump left running after dismissal | medium | mitigate | Dismissal interception + TCA auto-cancellation; Phase 7 exactly-once tests as gate |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- SettingFeature + DownloadsFeature scheme tests green (sequential xcodebuild)
- Module lifecycle-token audits clean
</verification>

<success_criteria>
- 8 sites migrated per D-06/D-07; teardown semantics preserved via dismissal interception
- AccountSetting re-fire parity resolved at root or flagged for owner review
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-10-SUMMARY.md` when done
</output>
