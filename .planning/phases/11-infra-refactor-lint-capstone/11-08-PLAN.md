---
phase: 11-infra-refactor-lint-capstone
plan: 8
type: execute
wave: 8
depends_on: [11-07]
files_modified:
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DetailFeature/DetailReducer+Actions.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/DetailFeature/Components/PostCommentView.swift
  - AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchView.swift
  - AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift
  - AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift
  - AppPackage/Sources/DetailFeature/Previews/PreviewsReducer.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "No DetailFeature view uses .onAppear/.onDisappear/.task except reason-annotated exceptions (D-06)"
    - "Detail loads on presentation from EVERY construction path (host tap via GalleryNavigation, deep-link, URL, clipboard) — not just the tap path (D-07)"
    - "No TCA action named onAppear remains in DetailFeature (incl. DetailReducer+Actions.swift:70 and PreviewsReducer.swift:106 case matches)"
  artifacts:
    - path: "AppPackage/Sources/DetailFeature/DetailReducer+Actions.swift"
      provides: "presentation-driven load action replacing the onAppear action"
  key_links:
    - from: "host reducers appending .detail to path/destination"
      to: "DetailFeature load effect"
      via: "presentation transition sends the detail load action at every DetailFeature.State construction site"
      pattern: "DetailFeature\\.State|DetailReducer\\.State"
---

<objective>
Migrate DetailFeature's 14 lifecycle sites — the largest lifecycle cluster — to presentation-driven lifecycle (D-06/D-07): DetailView's gid-load plus the sub-screens (Archives, Comments, PostComment, DetailSearch, FolderManager, Previews, Torrents) presented from DetailReducer's own Destination. Rename the onAppear-named actions in DetailReducer+Actions.swift and PreviewsReducer.swift (Pitfall 2).

Purpose: Detail is presented from many hosts; getting its presentation seams right proves the D-07 idiom at full complexity.
Output: DetailFeature lifecycle-modifier-free with load-on-presentation parity from all entry paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .onAppear -->
<!-- planner-discipline-allow: .task -->

## Artifacts this phase produces (plan 11-08 portion)

- Renamed load actions in DetailReducer and PreviewsReducer (former onAppear actions)
- Presentation-transition effect sends at every DetailFeature.State construction site (host modules touched only at those seams)
- Zero or more D-02 exception candidates

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DetailView main load — presentation-driven from all entry paths</name>
  <files>AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DetailFeature/DetailReducer+Actions.swift</files>
  <behavior>
    - Appending/setting a Detail destination fires the detail fetch exactly once per presentation (TestStore assertion at the presenting reducer)
    - Deep-link/URL/clipboard-constructed Detail states also fire the fetch (route-level test or presenting-reducer test)
  </behavior>
  <read_first>
    - AppPackage/Sources/DetailFeature/DetailView.swift (the store.send onAppear site and its guard)
    - AppPackage/Sources/DetailFeature/DetailReducer+Actions.swift (line ~70 — the onAppear case body: the effect to relocate)
    - Every DetailFeature.State construction site: grep AppPackage/Sources for `DetailReducer.State(` / `DetailFeature.State(` — host gallery-navigation seams (Phase 5 routed non-pad entries through GalleryNavigation), deep-link/URL/clipboard presentation routes in AppFeature/PresentationFeature
    - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift (lines 29–49 — the path-interception idiom from plan 11-07)
  </read_first>
  <action>
    Rename DetailReducer's onAppear action to a presentation-honest load action, keeping its fetch-if-empty guard semantics inside the reducer so repeat sends are idempotent. Remove DetailView's lifecycle modifier. At EVERY DetailFeature.State construction site (host tap paths through GalleryNavigation, deep-link/URL/clipboard routes), make the presenting reducer send the load action as part of the same transition that installs the state — via the path/destination action envelope or interception of the presented action. Because presentation fires once (not on pop-back like the old view callback), verify the fetch-if-empty guard makes this parity-equivalent; if any Detail flow relied on re-fire-on-return (e.g. refreshing after posting a comment deeper in the stack), have the child's completion action refresh the parent state explicitly, or flag it in the SUMMARY for owner parity review. Update DetailFeatureTests: assertions move from the old action name to load-on-presentation.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DetailFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - DetailView.swift contains no lifecycle modifiers
    - No TCA action named onAppear remains in DetailReducer (declaration, cases, sends, tests)
    - Every DetailFeature.State construction site in the repo is paired with a load send (grep-verifiable pairing listed in the SUMMARY)
    - DetailFeature scheme tests pass
  </acceptance_criteria>
  <done>Detail main load is presentation-driven from all entry paths; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detail sub-screens (Archives/Comments/PostComment/DetailSearch/FolderManager/Previews/Torrents)</name>
  <files>AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift, AppPackage/Sources/DetailFeature/Comments/CommentsView.swift, AppPackage/Sources/DetailFeature/Components/PostCommentView.swift, AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchView.swift, AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift, AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift, AppPackage/Sources/DetailFeature/Previews/PreviewsReducer.swift, AppPackage/Sources/DetailFeature/Torrents/TorrentsView.swift</files>
  <behavior>
    - Each sub-screen's former onAppear effect fires when DetailReducer presents it (destination set / navigation action)
  </behavior>
  <read_first>
    - The seven view files + PreviewsReducer.swift (line ~106 onAppear case) — site shapes
    - DetailReducer's Destination/navigation wiring (where each sub-screen state is constructed)
  </read_first>
  <action>
    For each sub-screen: remove the view lifecycle modifier; fire the former effect from DetailReducer's presentation transition for that Destination case; rename the sub-screen's onAppear action (PreviewsReducer and any others). Sub-screens presented as sheets/pushes within DetailFeature have exactly one construction site each — the destination-setting case. Keep fetch-if-empty guards in the child reducers. Any purely view-local site that cannot move (verify each honestly) becomes a D-02 exception candidate. Then run the module-wide audit: lifecycle-token grep over AppPackage/Sources/DetailFeature returns only reason-annotated exception sites; update remaining DetailFeatureTests.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DetailFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens in AppPackage/Sources/DetailFeature returns 0 matches outside reason-annotated exception sites
    - No TCA action named onAppear remains anywhere in DetailFeature
    - DetailFeature scheme tests pass
  </acceptance_criteria>
  <done>All 14 DetailFeature sites migrated or exception-annotated; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deep-link/URL/clipboard routes → Detail construction | Untrusted route input constructs Detail states; load must fire on these paths too |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Denial of Service | missed load on a construction path (blank screen) | medium | mitigate | Exhaustive construction-site grep + pairing list in SUMMARY; TestStore per path |
| T-11-11 | Denial of Service | orphaned sub-screen effects | low | mitigate | ifLet auto-cancellation on destination dismissal |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DetailFeature scheme tests green (sequential xcodebuild)
- Construction-site/load pairing list complete in SUMMARY
</verification>

<success_criteria>
- 14 sites migrated per D-06/D-07; onAppear actions renamed (Pitfall 2 cleared for this module)
- Load fires from every Detail entry path; parity flags recorded for owner review where re-fire semantics differed
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-08-SUMMARY.md` when done
</output>
