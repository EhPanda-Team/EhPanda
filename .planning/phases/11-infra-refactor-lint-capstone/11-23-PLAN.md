---
phase: 11-infra-refactor-lint-capstone
plan: 23
type: execute
wave: 23
depends_on: [11-22]
files_modified:
  - AppPackage/Tests/DownloadsFeatureTests/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "DownloadsFeatureTests (33 files — the bulk of the 189 test-side optional-try sites) contains zero unannotated optional-try (D-15: no Tests exclusion)"
    - "Test semantics unchanged: throwing test functions use plain try; defer cleanups use explicit do/catch; probe assertions stay probes"
  artifacts:
    - path: "AppPackage/Tests/DownloadsFeatureTests/"
      provides: "optional-try-free download test target"
  key_links:
    - from: "throwing @Test functions"
      to: "plain try conversion"
      via: "Swift Testing test functions are throws — most sites convert directly (D-15)"
      pattern: "throws"
---

<objective>
Eliminate optional-try from DownloadsFeatureTests' 33 affected files per D-15 (the owner locked test code INTO scope — no Tests path exclusion). Most sites convert to plain `try` (Swift Testing test functions are `throws`); `defer`-block cleanups become explicit do/catch (a defer cannot throw); genuine probe patterns become explicit do/catch preserving their probe meaning.

Purpose: the largest chunk of the 189 test-side sites, cleared before the flip plan.
Output: DownloadsFeatureTests optional-try-free and green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: try? -->

## Artifacts this phase produces (plan 11-23 portion)

- Plain-try / do/catch conversions across the 33 files (no new symbols; possibly one tiny shared cleanup helper in DownloadFeatureTestHelpers.swift if repetition warrants)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert coordinator/store/parsing suite files (first ~17 files)</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/</files>
  <read_first>
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift (helper shapes — a cleanup helper would live here)
    - The first ~17 affected files alphabetically (enumerate by grepping the optional-try token across the target)
  </read_first>
  <action>
    Per site, apply the D-15 conversion ladder: (1) inside a `throws` test function where failure should fail the test — plain `try` (the natural majority); (2) inside `defer` cleanup — `do { try … } catch { }` with a brief comment that cleanup is best-effort (a defer cannot throw; an ignored cleanup failure is the current semantic, now explicit); (3) genuine probes (asserting nil-ness of a failable operation) — explicit do/catch preserving the probe's assertion. Never change what a test asserts. If the defer-cleanup pattern repeats heavily, add ONE small helper in DownloadFeatureTestHelpers.swift (e.g. a best-effort remove function with internal do/catch) and call it — smaller diff than 50 inline do/catch blocks.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered optional-try count in the converted files equals 0
    - DownloadsFeature scheme tests pass with unchanged assertions
  </acceptance_criteria>
  <done>First half converted; suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert remaining suite files + target-wide zero audit</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/</files>
  <read_first>
    - The remaining ~16 affected files
  </read_first>
  <action>
    Convert the remaining files with the same ladder, then run the target-wide audit: comment-filtered optional-try count across AppPackage/Tests/DownloadsFeatureTests equals 0 (D-02 reason-annotated candidates excluded — expected to be rare or zero in test code). Run the DownloadsFeature scheme tests once more; parallel stability must hold (the 11-21 work is not to be regressed — do not reintroduce any serialization).
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air' && grep -rn "try? " AppPackage/Tests/DownloadsFeatureTests | grep -v "//" | wc -l  # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered optional-try count across the target equals 0 (annotated candidates excluded)
    - DownloadsFeature scheme tests pass in parallel
  </acceptance_criteria>
  <done>DownloadsFeatureTests optional-try-free; parallel suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only changes | No production surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-26 | Repudiation | silently weakened test assertions during conversion | medium | mitigate | Conversion ladder forbids assertion changes; suite must pass with identical assertions |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DownloadsFeature scheme tests green (sequential xcodebuild)
- Target-wide zero audit passes
</verification>

<success_criteria>
- The bulk of D-15's test-side sites converted with unchanged test semantics
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-23-SUMMARY.md` when done
</output>
