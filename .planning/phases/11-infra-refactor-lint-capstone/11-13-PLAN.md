---
phase: 11-infra-refactor-lint-capstone
plan: 13
type: execute
wave: 13
depends_on: [11-12]
files_modified:
  - AppPackage/Sources/ReadingFeature/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "ReadingFeature's 61 unchecked-subscript matches are resolved via D-08 idioms: honest renames for Optional-returning Dictionary subscripts, guard/safe idioms for Array indexing, precondition-checked exceptions only where indexing is genuinely required"
    - "Reader paging/page-mapping behavior is unchanged — PageHandler and reducer suites green unmodified"
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/"
      provides: "subscript-rule-clean reader module"
  key_links:
    - from: "renamed Dictionary key variables"
      to: "the name-based lint rule"
      via: "semantically honest non-policed names (e.g. page) for inherently safe Optional subscripts"
      pattern: "\\[page\\]"
---

<objective>
Resolve ReadingFeature's 61 `unchecked_subscript_index_access` matches — the largest cluster — per D-08. Most are `[Int: …]` Dictionary subscripts (imageURLs, localPageURLs, previewLoadingStates) keyed by a variable named `index`: inherently safe Optional-returning accesses whose fix is an honest rename to a non-policed name (the rule deliberately polices names, so honest naming IS the fix). Genuine Array indexing gets guards/safe idioms or the precondition-checked exception form.

Purpose: clears the biggest module before the subscript rule flips in plan 11-17.
Output: ReadingFeature contributes zero violations to the draft subscript rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-13 portion)

- Renamed key variables / guard-validated locals across ReadingFeature (no public API changes)
- Zero or more precondition-checked D-08 exception sites (reason + disable:next form)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Triage + Dictionary-subscript renames (safe majority)</name>
  <files>AppPackage/Sources/ReadingFeature/</files>
  <read_first>
    - .swiftlint.yml (the commented draft rule at lines 189–199 — its exact regex and policed names: integer literals, expr ± int, and the identifiers i/j/k/idx/index/offset/position/row/section)
    - AppPackage/Sources/ReadingFeature/ — every file with a subscript match (enumerate by grepping the policed patterns); pay attention to which collections are Dictionaries ([Int: …] — Optional-returning, safe) vs Arrays (trapping)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (the unchecked_subscript section — why renames are the sanctioned fix for safe sites)
  </read_first>
  <action>
    Enumerate all matches of the draft regex in ReadingFeature (grep for subscripts with integer literals, arithmetic offsets, and the policed identifier names). For each match backed by a Dictionary (Optional-returning — imageURLs, localPageURLs, previewLoadingStates and similar `[Int: …]` stores): rename the key variable/parameter to a semantically honest non-policed name — `page` is the domain-correct choice for reader page keys — updating all uses consistently. NEVER rename at a genuinely-unsafe Array site to dodge the rule (the anti-pattern RESEARCH bans): honest naming is only for provably-safe Optional accesses. Keep renames purely mechanical (no behavior edits) so the diff is reviewable.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - All Dictionary-backed matches are renamed to non-policed, semantically honest names
    - EhPanda scheme builds clean
    - git diff shows only identifier renames for this task (no logic changes)
  </acceptance_criteria>
  <done>Safe-majority sites resolved via honest renames.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Array-index sites — guards, safe idioms, checked exceptions (D-08)</name>
  <files>AppPackage/Sources/ReadingFeature/</files>
  <read_first>
    - The remaining Array-backed match sites from Task 1's enumeration (PageHandler mapping math, gesture/paging index arithmetic)
  </read_first>
  <action>
    For each genuine Array indexing site, apply D-08's idiom ladder: prefer `first`/`last`/`prefix`/`zip`/`indices.contains` guards yielding a `validatedIndex` local (the draft rule's excluded pattern `[validatedIndex]` sanctions exactly this), restructure to iteration where natural, and ONLY where direct indexing is genuinely required use the checked form: `precondition(collection.indices.contains(index), …)` immediately above the reason-annotated disable:next line (the PreviewIdentifiers subscript from plan 11-12 is the in-repo reference). Baseline-locked paging arithmetic (PageHandler dual-page mapping) must produce identical outputs — the existing PageHandler unit suites are the gate; do not weaken any assertion. Run the ReadingFeature scheme tests.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ReadingFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of the draft rule's policed patterns in AppPackage/Sources/ReadingFeature returns 0 matches outside `[validatedIndex]` uses and reason-annotated checked-exception sites
    - ReadingFeature scheme tests pass unmodified (PageHandler/paging parity intact)
  </acceptance_criteria>
  <done>ReadingFeature subscript-rule-clean; reader parity proven by unmodified tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page index math → collections | Out-of-bounds indexing is the crash surface this work removes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-16 | Denial of Service | out-of-bounds trap in reader paging | medium | mitigate | Guards/validated locals; preconditions only where invariants guarantee bounds; PageHandler suites as gate |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- ReadingFeature scheme tests green (sequential xcodebuild)
- Policed-pattern grep clean per acceptance criteria
</verification>

<success_criteria>
- 61 matches resolved per D-08 with zero behavior change
- Renames confined to provably-safe Optional subscripts (anti-dodge rule honored)
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-13-SUMMARY.md` when done
</output>
