---
phase: 11-infra-refactor-lint-capstone
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/ParserFeature/Parser+List.swift
  - AppPackage/Sources/ParserFeature/Parser+Detail.swift
  - AppPackage/Sources/ParserFeature/Parser+Shared.swift
  - AppPackage/Sources/ParserFeature/Parser+Torrent.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "Every ParserFeature Group A (per-row drop) and Group B (per-field default) try? site is an explicit do/catch that logs the swallowed failure and mirrors current behavior exactly"
    - "Existing ParserFeatureTests fixtures pass UNCHANGED — row-drop and field-default behavior is byte-identical"
    - "Exactly 6 optional-try sites remain in ParserFeature (the D-04 Group C whole-parse collapses, handled in plan 11-02)"
  artifacts:
    - path: "AppPackage/Sources/ParserFeature/Parser+List.swift"
      provides: "do/catch + logger.error at former Group A/B sites"
    - path: "AppPackage/Sources/ParserFeature/Parser+Detail.swift"
      provides: "do/catch + logger.error at former Group A/B sites"
  key_links:
    - from: "AppPackage/Sources/ParserFeature/*.swift"
      to: "AppPackage/Sources/ParserFeature/Logger+.swift"
      via: "file-top private let logger = Logger(category:) using the existing ParserFeature Logger extension"
      pattern: "private let logger = Logger"
---

<objective>
Convert ParserFeature's D-04 Group A (23 per-row drop) and Group B (13 per-field default) `try?` sites to explicit do/catch that logs the swallowed failure via OSLog while exactly mirroring current behavior (row skipped, field defaults to nil/[]). Implements D-01 (try? is a violation by default) and D-04 Groups A/B. Group C (6 whole-parse collapses) is deliberately left for plan 11-02.

Purpose: LINT-01's `optional_try` rule cannot flip to error until every site is root-fixed; Groups A/B are the behavior-preserving majority of ParserFeature's 42 sites.
Output: ParserFeature with 36 fewer optional-try sites, every dropped row / defaulted field now observable in logs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<assumption_delta_decision>
Signal: "optional" (detector fired on the phase scope text). Decision: **no-change** — the term is the SwiftLint rule name `optional_try` (Swift's `try?` construct); no data-model field transitions from required to optional this phase. No identity-model change.
</assumption_delta_decision>

## Artifacts this phase produces (plan 11-01 portion)

- File-top `private let logger = Logger(category: …)` declarations in `Parser+List.swift`, `Parser+Detail.swift`, `Parser+Shared.swift`, `Parser+Torrent.swift` (only where those files gain log calls)
- do/catch replacements at 36 former Group A/B sites (no new public symbols)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Group A — 23 per-row drop sites to do/catch + logger (D-04 A)</name>
  <files>AppPackage/Sources/ParserFeature/Parser+List.swift, AppPackage/Sources/ParserFeature/Parser+Detail.swift, AppPackage/Sources/ParserFeature/Parser+Shared.swift, AppPackage/Sources/ParserFeature/Parser+Torrent.swift</files>
  <read_first>
    - .swiftlint.yml (root — full rule inventory; the do/catch you write must pass every active rule, including single_line_trailing_closure conventions that flip later: write multi-line closures)
    - AppPackage/Sources/ParserFeature/Logger+.swift (the module's Logger init convention — Logger(moduleName: "ParserFeature", category:))
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (line 9 — the file-top `private let logger` declaration shape)
    - AppPackage/Sources/ParserFeature/Parser+List.swift (current Group A sites and Phase 9's in-code rationale comments — the triage input)
    - AppPackage/Sources/ParserFeature/Parser+Detail.swift, Parser+Shared.swift, Parser+Torrent.swift (remaining Group A sites)
  </read_first>
  <action>
    Enumerate all optional-try sites in ParserFeature (grep the module directory). Classify against D-04: Group A = a compactMap/loop body dropping one malformed row (galleries, comments, torrents, preview rows — 23 sites); Phase 9's rationale comments mark each. For every Group A site, replace the optional-try expression with an explicit do/catch: the do block performs the throwing parse via plain `try`; the catch block calls `logger.error(...)` with a short stable descriptor of what was dropped (e.g. "Dropped malformed gallery row") plus the interpolated `error` value, then returns nil / continues — exactly the current row-skip behavior. Where a file gains its first log call, add `private let logger = Logger(category: .init(describing: Parser.self))` at file top per the repo convention (Logger+.swift stays init-only). NEVER interpolate raw HTML fragments, URLs, or cookie-bearing values into log messages — only `error` and fixed descriptors (Phase 8 cookie-logging privacy gate scans production sources). Preserve the Phase 9 rationale comments' intent by rewording them as plain doc comments explaining the deliberate degradation (do not delete the WHY). Delete no Group C site: Parser+List.swift lines 10–29 mode-branch fallbacks, Parser+Detail.swift line 128, and Parser+Shared.swift line 38 stay untouched for plan 11-02.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ParserFeature -destination 'platform=iOS Simulator,name=iPhone Air' (run alone — never overlap xcodebuild invocations on this machine)</automated>
  </verify>
  <acceptance_criteria>
    - ParserFeatureTests pass with ZERO assertion edits (Group A behavior is byte-identical: malformed rows still dropped)
    - Every converted site has a catch block containing a `logger.error(` call
    - Each touched file that logs declares `private let logger` at file top
    - No log message interpolates document/node content, URLs, or cookie values — `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>All 23 Group A sites are explicit do/catch with logging; parser tests green unmodified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Group B — 13 per-field default sites to do/catch + logger (D-04 B) + residual audit</name>
  <files>AppPackage/Sources/ParserFeature/Parser+List.swift, AppPackage/Sources/ParserFeature/Parser+Detail.swift, AppPackage/Sources/ParserFeature/Parser+Shared.swift, AppPackage/Sources/ParserFeature/Parser+Torrent.swift</files>
  <read_first>
    - The same four Parser files (current state after Task 1)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (Pattern 1 — the exact do/catch shape)
  </read_first>
  <action>
    For every Group B site (a single optional field defaulting to nil or [] on parse failure, e.g. `let tags = (try? parseGalleryTags(node: gltmNode)) ?? []` — 13 sites), apply the same do/catch + logger.error conversion: catch logs a stable descriptor naming the defaulted field ("Gallery tags failed to parse, defaulting to empty") + `error`, then yields the identical default (nil or []). After conversion, audit the module: count remaining optional-try occurrences (comment lines filtered out) — the count MUST be exactly 6 and each must be one of the D-04 Group C sites (Parser+List.swift:10–29 mode branches ×4, Parser+Detail.swift:128, Parser+Shared.swift:38). If any other site remains, it was missed — convert it per its group.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ParserFeature -destination 'platform=iOS Simulator,name=iPhone Air' && grep -rn "try? " AppPackage/Sources/ParserFeature | grep -v "//" | wc -l  # expect exactly 6</automated>
  </verify>
  <acceptance_criteria>
    - ParserFeatureTests pass with ZERO assertion edits
    - Comment-filtered count of optional-try sites in AppPackage/Sources/ParserFeature equals exactly 6, and all 6 are the named Group C locations
    - `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>All 13 Group B sites converted; exactly the 6 Group C sites remain; module tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote HTML → parser | Untrusted e-hentai/exhentai page content enters ParserFeature |
| Parser → OSLog | New log lines cross into the system log |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | new `logger.error` calls in ParserFeature | high | mitigate | Log only the `error` value and fixed descriptors; never raw HTML/URLs/cookies. Gate: `Scripts/check-cookie-logging.sh` exits 0 per task |
| T-11-SC | Tampering | package installs | low | accept | This phase installs zero packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `xcodebuild test -scheme ParserFeature -destination 'platform=iOS Simulator,name=iPhone Air'` green (one xcodebuild at a time; never kill a mid-launch run)
- Comment-filtered optional-try count in ParserFeature == 6
- `bash Scripts/check-cookie-logging.sh` exits 0
</verification>

<success_criteria>
- 36 Group A/B sites converted to do/catch + logging with byte-identical degradation behavior (per D-04 A/B)
- ParserFeatureTests green with no assertion changes
- Only the 6 Group C sites remain for plan 11-02
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-01-SUMMARY.md` when done
</output>
