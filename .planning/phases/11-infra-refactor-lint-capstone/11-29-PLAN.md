---
phase: 11-infra-refactor-lint-capstone
plan: 29
type: execute
wave: 29
depends_on: [11-28]
files_modified:
  - .planning/phases/11-infra-refactor-lint-capstone/11-EXCEPTIONS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "All seven LINT-01 rules report 0 violations via the standalone binary over AppPackage/Sources, AppPackage/Tests, App, ShareExtension"
    - "The full suite passes IN PARALLEL; every surviving serialization/@MainActor token is on the documented-exception inventory"
    - "11-EXCEPTIONS.md exists: every reason-annotated disable added this phase, grouped by rule with file:line and reason text — the owner's batch-review artifact (D-01/D-02 exception-review flow)"
    - "ROADMAP Phase 11 criterion 3 and REQUIREMENTS LINT-01 wording are amended per D-02: no UNAPPROVED disables (rather than no disables)"
  artifacts:
    - path: ".planning/phases/11-infra-refactor-lint-capstone/11-EXCEPTIONS.md"
      provides: "phase-end owner review inventory"
      contains: "optional_try"
  key_links:
    - from: "11-EXCEPTIONS.md"
      to: "phase verification (owner batch review)"
      via: "unapproved candidates get reworked, not shipped (CONTEXT exception-review flow)"
      pattern: "reason:"
---

<objective>
The capstone gate: run the full verification battery (seven-rule binary zero-check, clean build under the plugin for source AND test targets, full parallel suite, cookie-logging scan), generate the exception inventory for the owner's batch review (D-01/D-02 flow: executors wrote candidates during execution; the owner reviews them at phase-end verification), and amend ROADMAP/REQUIREMENTS wording per D-02.

Purpose: proves LINT-01 end-to-end and produces the review artifact the phase's exception-gated design requires.
Output: verified phase + 11-EXCEPTIONS.md + reconciled docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

## Artifacts this phase produces (plan 11-29 portion)

- `.planning/phases/11-infra-refactor-lint-capstone/11-EXCEPTIONS.md` (new file: the owner review inventory)
- Amended `.planning/ROADMAP.md` Phase 11 criterion 3 and `.planning/REQUIREMENTS.md` LINT-01 wording (D-02)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full verification battery</name>
  <files>.planning/phases/11-infra-refactor-lint-capstone/</files>
  <read_first>
    - .swiftlint.yml (final state: all seven rules active at error)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (the sweep/verification command and Sampling Rate section)
  </read_first>
  <action>
    Run, strictly one xcodebuild invocation at a time: (1) the standalone binary with --reporter json over AppPackage/Sources AppPackage/Tests App ShareExtension, counting per rule_id — sorted_imports, multiline_function_chains, single_line_trailing_closure, labeled_tuple_elements, optional_try, binding_initializer, lifecycle_modifiers, unchecked_subscript_index_access must ALL be absent from the report (0 violations); (2) xcodebuild build -scheme EhPanda (plugin gate, sources); (3) xcodebuild build-for-testing -scheme EhPanda (plugin gate, tests); (4) xcodebuild test -scheme EhPanda — the full suite in parallel; (5) bash Scripts/check-cookie-logging.sh (all the new log lines from plans 11-01..11-06 must pass). Record each command and its result verbatim in the SUMMARY. Any failure: fix at root within this plan (small residuals) or report the gap honestly in the SUMMARY if it needs a dedicated fix plan.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Binary JSON report contains zero entries for all eight enforced rule ids listed above
    - EhPanda build, build-for-testing, and full parallel test run all succeed
    - Scripts/check-cookie-logging.sh exits 0
  </acceptance_criteria>
  <done>LINT-01's technical criteria all proven by tool output.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Exception inventory + D-02 doc reconciliation</name>
  <files>.planning/phases/11-infra-refactor-lint-capstone/11-EXCEPTIONS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (§Phase 11 Success Criteria — criterion 3's current wording)
    - .planning/REQUIREMENTS.md (§LINT-01 — current wording)
    - .planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md (D-02 and the exception-review flow paragraph)
  </read_first>
  <action>
    Generate 11-EXCEPTIONS.md by grepping the repo (Sources, Tests, App, ShareExtension) for every swiftlint disable comment: group by rule id; for each entry record file:line, the full reason text, and whether it pre-dates this phase (git blame) or was added by it. Add two additional sections: retained serialization traits (from the 11-20/11-21 SUMMARY verdicts, each with its in-file rationale quoted) and retained @MainActor annotations (from the 11-22 survivor inventory). Head the file with the review instruction: per the CONTEXT exception-review flow, the owner reviews this batch at phase-end verification; unapproved entries get reworked, not shipped. Then amend the docs per D-02 using scoped edits: ROADMAP Phase 11 criterion 3 becomes "No rule is suppressed or bypassed with an UNAPPROVED disable — every surviving disable carries an owner-reviewed reason comment — and the project builds clean under SwiftLint-as-error"; REQUIREMENTS LINT-01 acceptance gains the matching "no unapproved suppressions" wording. Keep all other criteria text untouched.
  </action>
  <verify>
    <automated>test -s .planning/phases/11-infra-refactor-lint-capstone/11-EXCEPTIONS.md && grep -q "unapproved" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 11-EXCEPTIONS.md exists, grouped by rule, with file:line + reason for every disable, plus serialization and MainActor sections
    - ROADMAP criterion 3 and REQUIREMENTS LINT-01 contain the amended "unapproved" wording
    - No other ROADMAP/REQUIREMENTS content changed (scoped diffs)
  </acceptance_criteria>
  <done>Owner review artifact ready; docs reconciled to the locked decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exception inventory → owner review | The gate that keeps the exception mechanism honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-32 | Repudiation | undocumented exceptions escaping review | medium | mitigate | Grep-built inventory is exhaustive; swiftlint_disable_requires_reason makes reason-less disables a build error |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- All five battery commands green; inventory + doc amendments in place
</verification>

<success_criteria>
- Seven rules at error with zero violations; full suite parallel and green; every exception, retained trait, and retained annotation on the owner-review inventory; D-02 wording landed
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-29-SUMMARY.md` when done
</output>
