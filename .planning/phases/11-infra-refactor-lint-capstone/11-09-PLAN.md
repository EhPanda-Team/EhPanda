---
phase: 11-infra-refactor-lint-capstone
plan: 9
type: execute
wave: 9
depends_on: [11-08]
files_modified:
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift
  - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift
  - AppPackage/Sources/ReadingFeature/ReadingReducer+Body.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "No ReadingFeature view uses .onAppear/.onDisappear/.task except reason-annotated exceptions (D-06)"
    - "Reader behavior parity is fully preserved: page seeding, per-page image loading order, zoom/pan/tap coexistence, cancellation identifiers and send ordering (Phase 4/5 invariants)"
    - "No TCA action named onAppear remains in ReadingFeature (ReadingReducer+Body.swift:79 case included)"
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/ReadingReducer+Body.swift"
      provides: "presentation-driven reader load action"
  key_links:
    - from: "reader presenters (Detail, Downloads)"
      to: "ReadingFeature load effect"
      via: "presentation transition sends the reader load at every ReadingFeature.State construction site"
      pattern: "ReadingReducer\\.State|ReadingFeature\\.State"
---

<objective>
Migrate ReadingFeature's 8 lifecycle sites to presentation-driven lifecycle (D-06/D-07) with extreme parity care: the reader is the highest-frequency request path (Phase 4) and its geometry/seeding logic is baseline-locked (Phase 5). Sites split into reducer-driven loads (migrate) and view-local mechanisms (scroll seeding, per-page image loading) that may be D-02 exception candidates if a non-banned mechanism cannot express them.

Purpose: clears the most parity-sensitive lifecycle cluster.
Output: ReadingFeature lifecycle-modifier-free (or exception-annotated) at full reader parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .onAppear -->
<!-- planner-discipline-allow: .task -->

## Artifacts this phase produces (plan 11-09 portion)

- Renamed reader load action (former onAppear action in ReadingReducer)
- Presentation-transition sends at reader construction sites in presenting modules
- D-02 exception candidates for genuinely view-local reader mechanisms (expected: per-page image loading, scroll seeding)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reader main load + ControlPanel — presentation-driven (D-07)</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift, AppPackage/Sources/ReadingFeature/ReadingReducer+Body.swift</files>
  <behavior>
    - Presenting the reader fires its former onAppear effect exactly once per presentation (TestStore at the presenting reducer)
    - Reader cancellation identifiers and send ordering are unchanged (Phase 4 invariant)
  </behavior>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingReducer+Body.swift (line ~79 — the onAppear case body and its cancellation identifiers)
    - AppPackage/Sources/ReadingFeature/ReadingView.swift (store-send lifecycle sites AND the geometry-gated scroll seeding — width>0 / seeded scrollPosition pairing from Phase 5; do NOT disturb the seeding pair)
    - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift (its lifecycle site shape)
    - Reader construction sites: grep AppPackage/Sources for `ReadingReducer.State(` / `ReadingFeature.State(` (Detail and Downloads presenters)
  </read_first>
  <action>
    Rename ReadingReducer's onAppear action to a presentation-honest load action preserving the exact effect body, cancellation identifiers, and send ordering. Remove the view-side store-send lifecycle hooks; at every reader construction site the presenting reducer sends the load action in the same transition. ControlPanel's site: if it sends a store action, migrate the same way; if it is view-local (e.g. slider appearance sync), attempt a value-change-driven mechanism (onChange of the driving value, onGeometryChange — both non-banned) before falling back to a D-02 exception candidate. The Phase 5 geometry-driven scroll seeding (onGeometryChange width-gating) is NOT a banned construct — leave that machinery intact; only the onAppear/onDisappear/task tokens are in scope. Update ReadingFeatureTests to assert load-on-presentation.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ReadingFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - No TCA action named onAppear remains in ReadingFeature
    - ReadingView.swift and ControlPanel.swift contain no lifecycle modifiers outside reason-annotated exceptions
    - ReadingFeature scheme tests pass (PageHandler/GestureHandler suites untouched and green)
  </acceptance_criteria>
  <done>Reader main lifecycle presentation-driven; parity suites green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ReadingViewComponents per-page loaders + module audit</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift (the image `.task(id:)` loader and any other lifecycle sites — understand what cancels the load when a page leaves)
  </read_first>
  <action>
    The per-page image `.task(id:)` loader is appearance-and-identity-driven view machinery: evaluate honestly whether the reducer's existing page-change actions (scrollPosition-driven) can drive per-page image acquisition at identical order/cancellation semantics. If yes, migrate: the reducer keys image effects to page-index transitions with the same cancellation IDs. If the per-cell load/cancel granularity genuinely cannot be reproduced without the view-attached task (likely for offscreen prefetch cells), write a D-02 reason + disable:next exception candidate per site — reader image behavior parity outranks rule aesthetics, and the owner reviews candidates at phase end. Afterwards run the module audit: lifecycle-token grep across AppPackage/Sources/ReadingFeature returns only reason-annotated exception sites.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ReadingFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens in AppPackage/Sources/ReadingFeature returns 0 matches outside reason-annotated exception sites
    - ReadingFeature scheme tests pass unmodified where they cover paging/gesture parity
    - SUMMARY records the migrate-vs-exception verdict per ReadingViewComponents site with one-line rationale
  </acceptance_criteria>
  <done>ReadingFeature clean or exception-annotated; parity intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View layer → reducer | Reader load initiative moves into presenters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12 | Denial of Service | reader image-load order/cancellation drift | high | mitigate | Exception-candidate path sanctioned for per-cell loaders; cancellation IDs preserved verbatim; ReadingFeatureTests gate |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- ReadingFeature scheme tests green (sequential xcodebuild)
- Module lifecycle-token audit clean (exceptions annotated)
</verification>

<success_criteria>
- 8 sites migrated or exception-annotated per D-06/D-07 with reader parity preserved
- Migrate-vs-exception verdicts documented for phase-end owner review
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-09-SUMMARY.md` when done
</output>
