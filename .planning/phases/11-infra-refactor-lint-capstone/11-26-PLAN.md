---
phase: 11-infra-refactor-lint-capstone
plan: 26
type: execute
wave: 26
depends_on: [11-25]
files_modified:
  - AppPackage/Sources/
  - App/
  - ShareExtension/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "All single_line_trailing_closure violations in Sources (~149 at inventory; recount at execution) are rewrapped — parenthesized closure or broken onto multiple lines — with zero behavior change"
  artifacts:
    - path: "AppPackage/Sources/"
      provides: "single-line-trailing-closure-free source tree"
  key_links:
    - from: "temporary local rule enable"
      to: "violation enumeration"
      via: "uncomment the draft rule locally, enumerate with the binary, revert the config before committing (flip happens in plan 11-27)"
      pattern: "single_line_trailing_closure"
---

<objective>
Mechanically rewrap every `single_line_trailing_closure` violation in Sources (~149 across the inventory's file set; the count drifts — the binary is the enumerator): single-line trailing closures on the policed methods (map/filter/forEach/onChange/withLock/…) become either a parenthesized closure argument `foo.map({ … })` or a multi-line closure. The rule itself flips in plan 11-27 together with the Tests half (Pitfall 10: one flip, with the last fix).

Purpose: first half of the 223-site mechanical rewrap.
Output: Sources clean for the rule; config unchanged (still commented) at commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-26 portion)

- Rewrapped closures across the Sources tree (purely syntactic; no new symbols)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enumerate via temporarily-enabled rule; rewrap first half of Sources</name>
  <files>AppPackage/Sources/, App/, ShareExtension/</files>
  <read_first>
    - .swiftlint.yml (the commented draft at lines 162–169 — the policed method list and the exact regex; note it exempts closures starting with return/break/continue/throw/fallthrough)
  </read_first>
  <action>
    Enumeration technique: temporarily uncomment the single_line_trailing_closure draft in the working-tree .swiftlint.yml, run the standalone binary with --reporter json over AppPackage/Sources App ShareExtension to capture the authoritative violation list (file:line), then REVERT the config edit (git checkout -- .swiftlint.yml) before any commit — the flip belongs to plan 11-27. Work through the first half of the violation list: per site choose the cleaner form — parenthesize the closure `foo.map({ $0.x })` for genuinely short expressions, or break the closure body onto its own line(s) for anything denser. Never alter closure semantics, capture lists, or argument names. SwiftUI modifier sites (onChange and similar in the policed list) rewrap the same way.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The captured first-half violation list is fully addressed (re-run of the temporarily-enabled rule shows those file:lines clean)
    - EhPanda scheme builds clean
    - Committed diff contains NO .swiftlint.yml change
  </acceptance_criteria>
  <done>First half of Sources rewrapped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrap second half of Sources + Sources-clean audit</name>
  <files>AppPackage/Sources/, App/, ShareExtension/</files>
  <read_first>
    - The remaining violation list from Task 1's enumeration
  </read_first>
  <action>
    Complete the remaining Sources sites, then re-run the temporary-enable enumeration one final time: the rule must report 0 over AppPackage/Sources, App, ShareExtension (Tests hits remain — they are plan 11-27's). Revert the config again before committing. Run one representative module test scheme (pick the module with the most rewraps) to confirm zero behavior change.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Temporarily-enabled rule reports 0 violations over Sources/App/ShareExtension paths
    - EhPanda scheme builds clean; committed diff has no config change
  </acceptance_criteria>
  <done>Sources fully clean for the rule; flip deferred to 11-27.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Syntactic rewrites → behavior | Pure formatting; risk is transcription error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-29 | Denial of Service | typo during mass rewrap | low | mitigate | Compiler catches syntax; build gate per task |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- EhPanda build green after each task (sequential xcodebuild)
- Temporary-enable enumeration shows Sources at 0
</verification>

<success_criteria>
- All Sources single_line_trailing_closure sites rewrapped at parity; config untouched
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-26-SUMMARY.md` when done
</output>
