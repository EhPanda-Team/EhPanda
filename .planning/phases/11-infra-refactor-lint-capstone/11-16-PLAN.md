---
phase: 11-infra-refactor-lint-capstone
plan: 16
type: execute
wave: 16
depends_on: [11-15]
files_modified:
  - AppPackage/Sources/ImageColors/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "ImageColors' 27 unchecked-subscript matches are refactored to the D-08 checked idiom — NO module-level exception (D-16)"
    - "The 3 ImageColors parity fixtures pass UNCHANGED — the algorithm's output survived the refactor (D-16's explicit proof requirement)"
  artifacts:
    - path: "AppPackage/Sources/ImageColors/"
      provides: "checked-subscript-clean color-extraction module at algorithm parity"
  key_links:
    - from: "histogram loop indexing"
      to: "precondition-checked access"
      via: "loop-bound invariants cited in reason comments"
      pattern: "precondition|validatedIndex"
---

<objective>
Refactor ImageColors' 27 `unchecked_subscript_index_access` matches to the checked idiom per D-16 — the owner explicitly rejected a module-level exception. The vendored histogram algorithm's loops index with loop-derived variables; each access gets a safe idiom or a precondition-checked exception citing the loop-bound invariant. The 3 parity fixtures are the proof the algorithm's behavior survived — they must pass unchanged.

Purpose: honors D-16 and closes the last big subscript cluster before the flip.
Output: ImageColors subscript-rule-clean at pixel-identical extraction output.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-16 portion)

- Checked/guarded access across ImageColors histogram and sorting code (no API changes, no algorithm changes)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Histogram loop indexing — safe idioms and checked accesses (D-16)</name>
  <files>AppPackage/Sources/ImageColors/</files>
  <read_first>
    - .swiftlint.yml (draft rule regex, lines 189–199)
    - AppPackage/Sources/ImageColors/ — every file with a policed match (the ported algorithm: pixel-buffer walks, histogram bins, sorted-color picks)
    - AppPackage/Tests — the ImageColors parity-fixture suite (locate it; these 3 fixtures must not be re-baselined)
  </read_first>
  <action>
    For each match, choose the least-invasive sanctioned form: where a loop bound already proves the index (e.g. `for i in 0..<buffer.count` indexing `buffer[i]`), either restructure to direct element iteration (`for element in buffer` / `enumerated()`) when that is a pure-mechanical transform, or keep the index and add the checked form — `precondition(collection.indices.contains(index), …)` (or cite the loop invariant directly in the reason comment) above the reason-annotated disable:next. Where the index derives from arithmetic (bin math, stride walks), prefer a guard-validated `validatedIndex` local. DO NOT restructure the algorithm itself: color math, bin thresholds, sort comparators, and iteration order stay verbatim — this is a bounds-annotation pass, not a rewrite. The jathu attribution comment stays.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Policed-pattern grep in AppPackage/Sources/ImageColors returns 0 matches outside validatedIndex/checked-exception sites
    - git diff shows no changes to color-math constants, comparators, or iteration order
  </acceptance_criteria>
  <done>All 27 matches carry sanctioned forms; algorithm text otherwise verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Parity-fixture proof (D-16 gate)</name>
  <files>AppPackage/Sources/ImageColors/</files>
  <read_first>
    - The ImageColors parity-fixture test files (assertion shapes — these encode the pre-fork extraction outputs)
  </read_first>
  <action>
    Run the ImageColors test scheme. The 3 parity fixtures MUST pass with zero edits — no re-baselining, no tolerance widening. If any fixture fails, the refactor changed behavior: revert the offending transform and use the checked-exception form instead (a precondition annotation can never change output; only restructures can). Iterate until fixtures pass unchanged.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ImageColors -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - ImageColors scheme tests pass with ZERO test-file edits (git diff over the test target is empty)
    - All 3 parity fixtures green
  </acceptance_criteria>
  <done>D-16 proven: checked idiom adopted, algorithm behavior identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decoded image buffers → histogram walks | Buffer indexing is the trap surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-19 | Denial of Service | histogram out-of-bounds trap | medium | mitigate | Precondition-checked/guarded access with cited invariants; parity fixtures gate output |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- ImageColors scheme tests green with untouched fixtures (sequential xcodebuild)
- Policed-pattern audit clean
</verification>

<success_criteria>
- D-16 delivered: 27 sites checked, no module exception, 3 fixtures unchanged and green
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-16-SUMMARY.md` when done
</output>
