---
phase: 11-infra-refactor-lint-capstone
plan: 24
type: execute
wave: 25
depends_on: [11-23]
files_modified:
  - AppPackage/Tests/CookieClientTests/
  - AppPackage/Tests/FileClientTests/FileClientTests.swift
  - AppPackage/Tests/ImageClientTests/ImageClientTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/
  - .swiftlint.yml
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "optional_try is enabled at error with NO Tests path exclusion (D-15) and the standalone binary reports 0 violations over AppPackage/Sources, AppPackage/Tests, App, ShareExtension"
    - "All 316 inventoried sites (127 Sources + 189 Tests) are root-fixed or carry the D-02 reason-annotated exception form"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "optional_try active at error, repo-wide scope"
      contains: "optional_try"
  key_links:
    - from: ".swiftlint.yml optional_try"
      to: "SwiftLintBuildToolPlugin on source AND test targets"
      via: "build-time enforcement (test targets carry the plugin — Pitfall 1/6)"
      pattern: "severity: error"
---

<objective>
Convert the last test-side optional-try sites (CookieClientTests, FileClientTests, ImageClientTests, NetworkingFeatureTests — one file each — plus any repo-wide stragglers), then flip `optional_try` to error in the SAME commit (Pitfall 10) with NO Tests exclusion (D-15). Verify with the standalone binary and a build-for-testing run, because test targets carry the lint plugin (Pitfall 6 / the e8589355 incident class).

Purpose: lands the flagship refactor-gated rule — the D-01 exception-gated ban is now compiler-enforced forever.
Output: optional_try live at error, zero violations repo-wide.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: try? -->

## Artifacts this phase produces (plan 11-24 portion)

- Final test-side conversions (4 files + stragglers)
- `.swiftlint.yml`: `optional_try` active at error (drafted regex, no path exclusions)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Final test-target conversions + repo-wide sweep</name>
  <files>AppPackage/Tests/CookieClientTests/, AppPackage/Tests/FileClientTests/FileClientTests.swift, AppPackage/Tests/ImageClientTests/ImageClientTests.swift, AppPackage/Tests/NetworkingFeatureTests/</files>
  <read_first>
    - The four affected test files (site shapes; CookieClientTests cleanup clears live cookie stores — best-effort semantics must survive)
  </read_first>
  <action>
    Apply the plan 11-23 conversion ladder (plain try in throwing tests; do/catch in defer; explicit probes) to the four remaining test files. Then sweep the ENTIRE repo for any optional-try straggler introduced or missed since plan 11-06 (Sources, Tests, App, ShareExtension — comment-filtered grep), and convert per the appropriate plan's rules. The repo must be clean BEFORE the flip.
  </action>
  <verify>
    <automated>xcodebuild test -scheme CookieClient -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered optional-try count across AppPackage/Sources, AppPackage/Tests, App, ShareExtension equals 0 (reason-annotated exception sites excluded)
    - CookieClient scheme tests pass (cookie-store cleanup semantics intact)
  </acceptance_criteria>
  <done>Repo optional-try-clean ahead of the flip.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip optional_try to error (D-15 scope) + authoritative zero-check</name>
  <files>.swiftlint.yml</files>
  <read_first>
    - .swiftlint.yml (the commented draft at lines 144–151; the active-rule YAML shape)
  </read_first>
  <action>
    Uncomment `optional_try` in .swiftlint.yml exactly as drafted — name, regex, message, excluded_match_kinds comment + string, severity error — adding NO `excluded:` path patterns (D-15: tests are in scope). In the same commit: run the standalone binary (locate under "$HOME/Library/Developer/Xcode/DerivedData", path pattern SwiftLintBinary*macos/swiftlint) with --reporter json over AppPackage/Sources AppPackage/Tests App ShareExtension — optional_try must report 0; fix any residual the binary finds (it is authoritative over grep). Then prove both plugin gates: xcodebuild build (Sources targets) AND xcodebuild build-for-testing (test targets carry the plugin — a Tests violation would only surface here, per the test-target lint gap incident). Every D-02 exception candidate remaining in the codebase must have its `// reason:` line — the active swiftlint_disable_requires_reason rule enforces the form automatically.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - .swiftlint.yml has optional_try active at severity error with no path exclusions
    - Standalone binary JSON report: 0 optional_try violations over all four path roots
    - build-for-testing succeeds (test-target lint gate green)
  </acceptance_criteria>
  <done>optional_try enforced at error repo-wide; D-01/D-02/D-15 fully landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint config → build pipeline | Flagship rule flip; gates every future build incl. tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-27 | Denial of Service | flip with residual Tests violations breaking test builds | high | mitigate | build-for-testing in the flip commit (Pitfall 6); binary zero-check first |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Standalone binary: 0 optional_try violations repo-wide
- CookieClient tests + build-for-testing green (sequential xcodebuild)
</verification>

<success_criteria>
- All 316 sites resolved; optional_try at error with tests in scope (D-15)
- Exception candidates all carry the D-02 form for phase-end owner review
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-24-SUMMARY.md` when done
</output>
