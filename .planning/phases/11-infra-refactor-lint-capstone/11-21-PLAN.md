---
phase: 11-infra-refactor-lint-capstone
plan: 21
type: execute
wave: 21
depends_on: [11-20]
files_modified:
  - AppPackage/Tests/DownloadsFeatureTests/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "Every remaining DownloadsFeatureTests suite (37 files) has been individually diagnosed: its serialization trait removed after confirming no shared mutable state, or retained ONLY where the test becomes meaningless/incorrect without it, documented by an in-file rationale comment (D-12/D-14)"
    - "The DownloadsFeature test target passes running in parallel"
  artifacts:
    - path: "AppPackage/Tests/DownloadsFeatureTests/"
      provides: "parallel-capable download test target"
  key_links:
    - from: "per-suite diagnosis"
      to: "trait removal or documented retention"
      via: "the D-14 criterion: meaningless (or incorrect) without it"
      pattern: "Serialized:"
---

<objective>
Diagnose and de-serialize the 37 remaining DownloadsFeatureTests suites (the 39 inventoried minus the two Kingfisher suites fixed in 11-20). The helpers already use UUID-scoped temporary roots and per-sessionID URLProtocol stubs, so much of the serialization is expected to be vestigial caution (RESEARCH assumption A2) — but each suite gets an individual diagnosis (Claude's discretion per CONTEXT), not a blanket removal.

Purpose: the bulk of LINT-01 success criterion 4's serialization cleanup.
Output: DownloadsFeatureTests parallel-capable, with any survivors documented per D-14.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .serialized -->

## Artifacts this phase produces (plan 11-21 portion)

- Trait removals across up to 37 suite files; injectable seams for any newly-discovered shared state
- D-14 rationale comments on any retained traits

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Per-suite diagnosis + trait removal, first half (coordinator/store/observer suites)</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/</files>
  <read_first>
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift (UUID-scoped roots, fixture writers — what isolation already exists)
    - The first ~18 suite files alphabetically (AppReadingFlushTests through DownloadInspectorSkipTests, excluding the two DownloadImageParsing suites fixed in 11-20) — for each: what state does it touch beyond the helpers? Any fixed path, shared singleton, environment/UserDefaults write, or process-global?
  </read_first>
  <action>
    For each suite in the first half: (1) inspect for genuinely shared mutable state — fixed paths, singletons, UserDefaults/environment writes, statics; (2) if none (helpers already isolate), delete the serialization trait; (3) if shared state exists, fix it at root by injection (per-test instance/root — D-12; the Phase 8 DataCache and 11-19/11-20 seams are the models), then delete the trait; (4) ONLY if the test is meaningless or incorrect without sequencing (D-14 criterion), keep the trait and add the in-file rationale comment (PATTERNS G7 shape, beginning "// Serialized:"). Record each suite's verdict (removed / seam+removed / retained+why) in the SUMMARY. After the half, run the DownloadsFeature scheme tests — parallel by default — and fix any instability by injection, never by re-adding traits.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - First-half suites carry no serialization trait except retained ones with a "// Serialized:" rationale directly above
    - DownloadsFeature scheme tests pass in parallel
  </acceptance_criteria>
  <done>First half diagnosed and de-serialized; suite green in parallel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Second half (IpBan/Observer/Process/Retry/Reducer suites) + stability runs</name>
  <files>AppPackage/Tests/DownloadsFeatureTests/</files>
  <read_first>
    - The remaining ~19 suite files (DownloadIpBanTests through ReadingReducerLocalTests) — same per-suite inspection
  </read_first>
  <action>
    Apply the same diagnosis protocol to the second half. Then run the DownloadsFeature scheme tests TWICE consecutively (two separate sequential xcodebuild invocations — never overlapping) to demonstrate parallel stability; DownloadSchedulingTests is deterministic since 557b0425, so ANY failure is a real regression to fix (by injection), not flake to retry. Complete the per-suite verdict table in the SUMMARY: 37 rows, each removed / seam+removed / retained+rationale.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DownloadsFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep for the serialization trait across AppPackage/Tests/DownloadsFeatureTests returns only lines directly below a "// Serialized:" rationale comment
    - DownloadsFeature scheme tests pass on two consecutive parallel runs
    - SUMMARY contains the 37-row verdict table
  </acceptance_criteria>
  <done>DownloadsFeatureTests fully diagnosed; parallel and stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test isolation seams → production defaults | Any new seams must default to production behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-24 | Tampering | hidden shared state surfacing as flake in parallel runs | medium | mitigate | Per-suite diagnosis before removal; double stability runs; inject-over-serialize on any failure |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- DownloadsFeature scheme tests green on consecutive parallel runs (strictly sequential xcodebuild invocations)
- Verdict table complete
</verification>

<success_criteria>
- 37 suites diagnosed; serialization removed or documented per D-12/D-14
- Download test target parallel-stable
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-21-SUMMARY.md` when done
</output>
