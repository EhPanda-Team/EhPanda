---
phase: 11-infra-refactor-lint-capstone
plan: 25
type: execute
wave: 25
depends_on: [11-24]
files_modified:
  - .swiftlint.yml
  - AppPackage/Sources/
  - AppPackage/Tests/
  - App/
  - ShareExtension/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "sorted_imports is opted in at error and reports 0 violations repo-wide (~870 sites across 319 files auto-fixed)"
    - "The 3 conditional-import files (CookieClient.swift, ColorCodable.swift, DeviceType.swift) were manually reviewed after autocorrect — their #if-wrapped imports still compile on all configurations"
    - "The stale excluded: EhPanda/App/Generated entry is corrected while touching the config"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "sorted_imports opted in at error; stale exclusion fixed"
      contains: "sorted_imports"
  key_links:
    - from: ".swiftlint.yml opt_in_rules"
      to: "every Swift file's import block"
      via: "swiftlint --fix autocorrect (verified: sorts by module name, @testable attribute ignored)"
      pattern: "sorted_imports"
---

<objective>
The first mechanical capstone sweep: opt in `sorted_imports` at error, run the standalone binary's `--fix` over explicit paths (verified auto-correctable — sorts by module name, `@testable` ignored for ordering), manually review the 3 `#if`-conditional-import files, fix the stale `excluded:` entry, and land fix + flip in one commit.

Purpose: the highest-count rule (870 violations) resolved almost entirely by the tool itself (Don't Hand-Roll).
Output: sorted_imports live at error, zero violations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-25 portion)

- `.swiftlint.yml`: `sorted_imports` in opt_in_rules with `sorted_imports: severity: error`; corrected `excluded:` list
- Reordered import blocks across ~319 files (mechanical)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Opt in sorted_imports + autocorrect sweep</name>
  <files>.swiftlint.yml, AppPackage/Sources/, AppPackage/Tests/, App/, ShareExtension/</files>
  <read_first>
    - .swiftlint.yml (opt_in_rules block at lines 9–11 and the per-rule severity shape at lines 13–17; the excluded: block at file bottom)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md ("Enabling the mechanical rules" + "Verified sorted_imports autocorrect behavior")
  </read_first>
  <action>
    Edit .swiftlint.yml: add `sorted_imports` to opt_in_rules and a `sorted_imports: severity: error` block (mirroring force_try's shape). Fix the stale exclusion: the `excluded: [EhPanda/App/Generated]` entry points at a dead path — remove it (or correct it to a real generated path if one exists now; verify by ls). Locate the standalone binary (find "$HOME/Library/Developer/Xcode/DerivedData" -path pattern SwiftLintBinary*macos/swiftlint; if purged, re-resolve the package first — note that deletes/regenerates AppPackage/.build) and run: lint --fix --quiet over the four explicit path roots (NEVER from repo root without paths — it would traverse AppPackage/.build checkouts). Then run a plain lint pass with --reporter json: sorted_imports must report 0; hand-fix any file --fix could not settle.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Standalone binary JSON report: 0 sorted_imports violations over all four path roots
    - .swiftlint.yml contains sorted_imports in opt_in_rules at severity error and no dead excluded: path
    - EhPanda scheme builds clean
  </acceptance_criteria>
  <done>Imports sorted repo-wide; rule enforced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Conditional-import review + test-target lint gate</name>
  <files>AppPackage/Sources/CookieClient/CookieClient.swift, AppPackage/Sources/AppModels/, AppPackage/Sources/DeviceClient/</files>
  <read_first>
    - The 3 conditional-import files after autocorrect: CookieClient.swift, ColorCodable.swift, DeviceType.swift (locate the latter two by name under AppPackage/Sources) — verify --fix did not move an import across an #if boundary
  </read_first>
  <action>
    Review the autocorrect diff on the three files whose imports are wrapped in compile conditions (RESEARCH anti-pattern warning): each `#if`-guarded import must remain inside its condition and each unconditional import must remain outside; correct manually if --fix disturbed them. Then, because ~305 of the fixed violations are in Tests files, prove the test-target lint gate: xcodebuild build-for-testing (the app-scheme build alone skips Tests lint — the e8589355 incident class). Finally run one full test pass to confirm no import reorder broke anything (order-sensitive side-effectful imports do not exist in this codebase, but the suite is the proof).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - The 3 conditional-import files compile with imports in their original #if placement
    - Full EhPanda-scheme suite passes (parallel)
  </acceptance_criteria>
  <done>sorted_imports landed safely across source and test targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Autocorrect tool → 319 files | Mechanical rewrite at scale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-28 | Denial of Service | --fix breaking #if-wrapped imports | medium | mitigate | Manual review of the 3 known conditional-import files + full suite run |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Binary zero-check for sorted_imports; EhPanda build + full suite green (sequential xcodebuild)
</verification>

<success_criteria>
- 870 sorted_imports violations resolved via --fix + manual review; rule at error
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-25-SUMMARY.md` when done
</output>
