---
phase: 11-infra-refactor-lint-capstone
plan: 11
type: execute
wave: 11
depends_on: [11-10]
files_modified:
  - AppPackage/Sources/SystemNotification/View+Toast.swift
  - AppPackage/Sources/AppComponents/AppAlertState.swift
  - AppPackage/Sources/AppComponents/PreviewImageView.swift
  - AppPackage/Sources/GalleryListComponents/GalleryList.swift
  - .swiftlint.yml
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "lifecycle_modifiers is enabled at error and the standalone SwiftLint binary reports 0 violations for it over AppPackage/Sources, AppPackage/Tests, App, ShareExtension"
    - "binding_initializer is enabled at error with the D-05 narrowed regex (bans only closure-based Binding(get:set:)) and reports 0 violations; all ~29 projected Binding sites remain untouched and legal"
    - "GalleryList auto-load-on-scroll (Phase 2 D-36) still triggers pagination at parity"
    - "Toast auto-dismiss timing and the alert focus hop behave identically (via migration or reason-annotated exception)"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "lifecycle_modifiers uncommented at error; binding_initializer uncommented with narrowed regex at error"
      contains: "lifecycle_modifiers"
  key_links:
    - from: ".swiftlint.yml"
      to: "SwiftLintBuildToolPlugin"
      via: "build-time enforcement on every target once enabled"
      pattern: "severity: error"
---

<objective>
Finish the lifecycle migration on the store-less component surfaces — SystemNotification toast auto-dismiss (3), AppComponents (AppAlertState focus hop + PreviewImageView task loader), GalleryListComponents auto-load-on-scroll (1), plus the single Tests lifecycle site — then, in the SAME commit as the last fix, flip `lifecycle_modifiers` (as drafted) and `binding_initializer` (with the D-05 narrowed regex) to error. Also refactor the one real closure-based Binding site (AppAlertState.swift:235).

Purpose: lands the first two refactor-gated rule flips (Pitfall 10: flip with last fix so the build gate enforces forever).
Output: two rules live at error with zero violations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: .onAppear -->
<!-- planner-discipline-allow: .task -->
<!-- planner-discipline-allow: Binding(get: -->

## Artifacts this phase produces (plan 11-11 portion)

- `.swiftlint.yml`: `lifecycle_modifiers` active at error; `binding_initializer` active at error with regex `\bBinding\s*(?:<[^>]*>)?\s*\(\s*get\s*:`
- Projected-binding replacement at AppAlertState.swift:235
- D-02 exception candidates for genuinely view-local component mechanisms (expected: toast auto-dismiss)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Component lifecycle sites — SystemNotification, AppComponents, GalleryList, Tests (D-06)</name>
  <files>AppPackage/Sources/SystemNotification/View+Toast.swift, AppPackage/Sources/AppComponents/AppAlertState.swift, AppPackage/Sources/AppComponents/PreviewImageView.swift, AppPackage/Sources/GalleryListComponents/GalleryList.swift</files>
  <read_first>
    - AppPackage/Sources/SystemNotification/View+Toast.swift (toast auto-dismiss onAppear/onDisappear/task(id:) — store-less component; Phase 9 persistent-diagnostic-toast lifecycle must not regress)
    - AppPackage/Sources/AppComponents/AppAlertState.swift (the focus-hop lifecycle site AND the line-235 closure-based Binding — both fixed in this plan)
    - AppPackage/Sources/AppComponents/PreviewImageView.swift (the task(id:) image loader)
    - AppPackage/Sources/GalleryListComponents/GalleryList.swift (the last-cell auto-load trigger — Phase 2 D-36)
  </read_first>
  <action>
    SystemNotification toast auto-dismiss: this is a self-contained store-less component whose dismiss timer is inherently tied to the toast view's own lifetime — the RESEARCH-flagged prime D-06 exception candidate. Attempt a non-banned mechanism honestly (e.g. timing driven by the presentation value change); if it cannot be expressed without a lifecycle callback, write D-02 reason + disable:next candidates and preserve behavior exactly, including the Phase 9 ErrorInfo-toast persistent (non-timeout) variant. GalleryList auto-load: replace the last-cell onAppear trigger with the native scroll-visibility API (`onScrollVisibilityChange` on the sentinel cell, or `scrollTargetVisibility`-adjacent equivalents available on the current iOS 26 target) — a genuine root fix that is not on the banned list; verify auto-load still fires at the same scroll depth; if trigger timing cannot reach parity, exception candidate. PreviewImageView task(id:) loader: same verdict process as ReadingViewComponents (plan 11-09) — migrate to its owning reducer if the load/cancel granularity survives, else exception candidate. AppAlertState focus hop: try FocusState-driven or `defaultFocus` timing; else exception candidate. Also fix the single lifecycle site in AppPackage/Tests (locate by grep; likely a helper — migrate or annotate). Record every migrate-vs-exception verdict in the SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - grep of lifecycle tokens across AppPackage/Sources returns 0 matches outside reason-annotated exception sites
    - Auto-load-on-scroll still paginates (GalleryListComponents/HomeFeature tests or reducer-level assertion)
    - EhPanda scheme builds clean
  </acceptance_criteria>
  <done>All remaining lifecycle sites migrated or exception-annotated; behavior parity noted per site.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AppAlertState Binding fix + flip lifecycle_modifiers and binding_initializer to error (D-05)</name>
  <files>AppPackage/Sources/AppComponents/AppAlertState.swift, .swiftlint.yml</files>
  <read_first>
    - AppPackage/Sources/AppComponents/AppAlertState.swift (line ~235: isPresented: Binding(get: { item != nil }, set: …) — multiline; note which dialog/alert modifier consumes it and on which anchor view, because confirmation-dialog placement rules require the anchor to stay put)
    - .swiftlint.yml (draft blocks at lines 45–52 and 99–106; the active-rule YAML shape at lines 180–187)
  </read_first>
  <action>
    Replace the closure-based Binding at AppAlertState.swift:235 with a projected form: the canonical fix is the SwiftUINavigation/TCA `Binding` presence idiom (e.g. `$item.isPresent()` on an Optional binding, already available via the in-repo TCA dependency graph) or an equivalent projected binding — no get:set: closure pair. The alert/dialog modifier must stay attached to its current anchor (placement rule). Then edit .swiftlint.yml in the SAME commit: (1) uncomment `lifecycle_modifiers` exactly as drafted (severity error); (2) uncomment `binding_initializer` but replace its regex with the D-05 narrowed form `\bBinding\s*(?:<[^>]*>)?\s*\(\s*get\s*:` (message updated to say closure-based Binding(get:set:) is banned; prefer a projected Binding), keeping excluded_match_kinds comment+string and severity error. Locate the SwiftLint standalone binary via: find "$HOME/Library/Developer/Xcode/DerivedData" -path '*SwiftLintBinary*macos/swiftlint' (re-resolve the package if purged). Run it over explicit paths (never traverse AppPackage/.build): lint --quiet --reporter json AppPackage/Sources AppPackage/Tests App ShareExtension — both rules must report 0. Fix any residual sites the greps missed (grep and rule regex diverge — the binary is authoritative). Then build EhPanda scheme (plugin gate) and build-for-testing to prove test targets also pass the new rules.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - .swiftlint.yml has lifecycle_modifiers and binding_initializer active (not commented) at severity error; binding_initializer regex matches only the get:-closure form
    - Standalone binary JSON report shows 0 violations for both rule ids over all four path roots
    - The 29 projected Binding($x) sites are unmodified (git diff shows no changes to them)
    - build-for-testing succeeds (plugin gate green on source AND test targets)
  </acceptance_criteria>
  <done>Both rules live at error with zero violations; build gate enforces them permanently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint config → build pipeline | Config flip changes what every future build enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14 | Denial of Service | premature flip breaking all builds | medium | mitigate | Flip in same commit as last fix; binary zero-check before commit (Pitfall 10) |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Standalone binary: 0 violations for lifecycle_modifiers and binding_initializer over explicit paths
- EhPanda build + build-for-testing green (sequential xcodebuild)
</verification>

<success_criteria>
- Lifecycle migration complete repo-wide (47-site inventory resolved); two refactor-gated rules at error with zero violations
- AppAlertState.swift:235 uses a projected binding; dialog anchor unchanged
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-11-SUMMARY.md` when done
</output>
