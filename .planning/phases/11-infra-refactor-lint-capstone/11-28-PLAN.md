---
phase: 11-infra-refactor-lint-capstone
plan: 28
type: execute
wave: 28
depends_on: [11-27]
files_modified:
  - AppPackage/Sources/
  - .swiftlint.yml
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "multiline_function_chains is opted in at error and reports 0 violations (~85 sites across 20 files reformatted: every call in a multi-line chain sits on its own line)"
  artifacts:
    - path: ".swiftlint.yml"
      provides: "multiline_function_chains opted in at error (SwiftLint defaults otherwise)"
      contains: "multiline_function_chains"
  key_links:
    - from: ".swiftlint.yml opt_in_rules"
      to: "chained-call formatting repo-wide"
      via: "build-plugin enforcement"
      pattern: "multiline_function_chains"
---

<objective>
The final mechanical sweep: reformat the ~85 `multiline_function_chains` violations (20 files — chains that span lines must put each chained call on its own line) and opt the built-in rule in at error in the same commit, using SwiftLint defaults for its configuration (Claude's-discretion note in CONTEXT).

Purpose: last of the seven rule deliverables before the capstone gate.
Output: multiline_function_chains live at error, zero violations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

## Artifacts this phase produces (plan 11-28 portion)

- Reformatted call chains across ~20 Sources files
- `.swiftlint.yml`: `multiline_function_chains` in opt_in_rules with severity error

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reformat all multiline-chain violations</name>
  <files>AppPackage/Sources/</files>
  <read_first>
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (inventory: 85 violations, 20 files, Sources only — 0 in Tests)
  </read_first>
  <action>
    Enumerate with the temporary-enable technique (add multiline_function_chains to opt_in_rules in the working tree, binary --reporter json over AppPackage/Sources App ShareExtension, capture list, revert). Reformat each site: when a function chain spans multiple lines, EVERY chained call goes on its own line (the built-in rule's requirement); chains short enough to fit one line within the 120-char limit may alternatively be joined. Pure formatting — no expression changes. Watch line_length (120 error): breaking chains must not push any line over it.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Temporarily-enabled rule reports 0 violations over Sources/App/ShareExtension
    - EhPanda scheme builds clean
  </acceptance_criteria>
  <done>All chains conform; repo clean for the rule.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Opt in at error + verification</name>
  <files>.swiftlint.yml</files>
  <read_first>
    - .swiftlint.yml (opt_in_rules block and per-rule severity shape)
  </read_first>
  <action>
    Add `multiline_function_chains` to opt_in_rules with a `multiline_function_chains: severity: error` block (defaults otherwise), in the same commit as any final formatting fixes. Binary zero-check over all four path roots (Tests had 0 at inventory but recount — fix any that appeared). Build EhPanda + build-for-testing to prove both plugin gates.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - .swiftlint.yml has multiline_function_chains opted in at severity error
    - Standalone binary JSON report: 0 violations for the rule over all four path roots
    - build-for-testing succeeds
  </acceptance_criteria>
  <done>All seven LINT-01 rules now live at error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint config → build pipeline | Final mechanical flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-31 | Denial of Service | chain reformat introducing line_length violations | low | mitigate | 120-char awareness during reformat; build gate catches |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Binary zero-check; EhPanda build + build-for-testing green (sequential xcodebuild)
</verification>

<success_criteria>
- 85 chain sites reformatted; the seventh and final rule enforced at error
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-28-SUMMARY.md` when done
</output>
