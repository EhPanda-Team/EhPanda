---
phase: 11-infra-refactor-lint-capstone
plan: 2
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - AppPackage/Sources/ParserFeature/Parser+List.swift
  - AppPackage/Sources/ParserFeature/Parser+Detail.swift
  - AppPackage/Sources/ParserFeature/Parser+Shared.swift
  - AppPackage/Tests/ParserFeatureTests/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "An unparseable gallery-list page now throws to the owning reducer instead of silently rendering as an empty list (D-04 Group C)"
    - "ParserFeature contains ZERO optional-try sites (or only owner-review candidates in the D-02 reason + disable form)"
    - "ParserFeatureTests malformed-page fixtures assert thrown errors, not empty results"
  artifacts:
    - path: "AppPackage/Sources/ParserFeature/Parser+List.swift"
      provides: "Mode-branch parsing that propagates AppError instead of collapsing to []"
  key_links:
    - from: "AppPackage/Sources/ParserFeature/Parser+List.swift"
      to: "gallery-list reducers (e.g. FrontpageReducer)"
      via: "existing throws signature — reducers already map failures to .failed(error) + Phase 9 ErrorInfo toast"
      pattern: "loadingState = .failed"
---

<objective>
Convert ParserFeature's 6 D-04 Group C whole-parse collapse sites to real error propagation (D-03): `Parser+List.swift:10–29` mode-branch empty-list fallbacks (×4), `Parser+Detail.swift:128`, and the `Parser+Shared.swift:38` static-regex compile. Update ParserFeatureTests so malformed-page fixtures assert thrown errors (RESEARCH Pitfall 7). After this plan, ParserFeature is optional-try-free.

Purpose: an unparseable page must surface as a failure through the Phase 9 ErrorInfo/toast machinery, never as a silent "no results" (D-03/D-04 C).
Output: ParserFeature at zero optional-try sites; updated parser tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: try? -->

## Artifacts this phase produces (plan 11-02 portion)

- Throwing mode-branch bodies in `parseGalleries` (Parser+List.swift) — no signature changes (already `throws`)
- Compile-time-checked regex (Swift regex literal) or throwing accessor replacing the Parser+Shared.swift:38 optional static-regex compile
- Updated ParserFeatureTests assertions for malformed-list/detail fixtures

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Propagate the 6 Group C sites (D-03/D-04 C)</name>
  <files>AppPackage/Sources/ParserFeature/Parser+List.swift, AppPackage/Sources/ParserFeature/Parser+Detail.swift, AppPackage/Sources/ParserFeature/Parser+Shared.swift</files>
  <read_first>
    - AppPackage/Sources/ParserFeature/Parser+List.swift (lines 1–40 — the four mode-branch `?? []` fallbacks and the response-error banner check ordering)
    - AppPackage/Sources/ParserFeature/Parser+Detail.swift (around line 128)
    - AppPackage/Sources/ParserFeature/Parser+Shared.swift (around line 38 — the static NSRegularExpression compile)
    - AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift (lines 93–126 — the canonical typed-throws fetch + .failed(error) pattern already at every call site)
  </read_first>
  <action>
    Parser+List.swift lines 10–29: each display-mode branch currently collapses a failed parse to an empty array; change each to a plain `try` call so the parse error propagates through the function's existing `throws` signature. Preserve the existing response-error-banner detection semantics: if the page carries an explicit error banner, that error must still win over (or accompany) the parse error — keep the current banner-check ordering so the user sees the most specific error. Parser+Detail.swift:128: same conversion — propagate instead of defaulting. Parser+Shared.swift:38 (static regex compile): prefer replacing the optional NSRegularExpression compile with a Swift regex literal (compile-time validated, no runtime failure path — the strongest root fix); if the pattern uses NSRegularExpression-only features, restructure to a throwing accessor so a compile failure propagates as an AppError instead of being silently discarded. No reducer changes are expected: every call site already handles AppError via `.failed(error)` state + the Phase 9 ErrorInfo toast (FrontpageReducer pattern). Confirm this by grepping call sites of the changed functions.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered optional-try count in AppPackage/Sources/ParserFeature equals 0
    - `parseGalleries` mode branches contain plain `try` calls (no nil-coalescing to empty arrays on parse failure)
    - EhPanda scheme builds clean (no compiler errors from signature ripples)
  </acceptance_criteria>
  <done>All 6 Group C sites propagate errors; ParserFeature is optional-try-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update ParserFeatureTests for thrown-error semantics (Pitfall 7)</name>
  <files>AppPackage/Tests/ParserFeatureTests/</files>
  <behavior>
    - Malformed/empty gallery-list fixture: parse now throws (assert with #expect(throws:)) instead of returning []
    - Well-formed fixtures: unchanged results (Group A/B behavior intact)
  </behavior>
  <read_first>
    - AppPackage/Tests/ParserFeatureTests/ (locate fixtures asserting empty-result on malformed input — grep for the changed function names)
  </read_first>
  <action>
    Find every ParserFeatureTests case that asserts an empty-list/default result for a malformed or unparseable fixture routed through a Group C site; rewrite those assertions to expect a thrown error (Swift Testing `#expect(throws:)`), keeping the fixture inputs identical. All other parser tests must stay green untouched. If a fixture legitimately represents an EMPTY (not malformed) page — e.g. a valid page with zero rows — it must still parse to an empty list; only structurally unparseable inputs now throw. Distinguish the two per fixture.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ParserFeature -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - ParserFeature scheme tests pass
    - At least one test asserts a thrown error for a malformed-list fixture
    - Valid-but-empty page fixtures still assert empty-list results (no over-throwing)
  </acceptance_criteria>
  <done>Parser tests encode the new throw-on-unparseable contract; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote HTML → parser | Untrusted page content; Group C makes handling stricter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Tampering (input validation, ASVS V5) | parseGalleries mode branches | medium | mitigate | Throwing on unparseable input is the mitigation itself — no more silent empty render; existing AppError surface presents it |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- ParserFeature scheme tests green; EhPanda scheme build green (sequential xcodebuild)
- Comment-filtered optional-try count in ParserFeature == 0
</verification>

<success_criteria>
- Unparseable pages throw to owning reducers (D-03/D-04 C); tests prove it
- ParserFeature contributes zero optional_try violations
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-02-SUMMARY.md` when done
</output>
