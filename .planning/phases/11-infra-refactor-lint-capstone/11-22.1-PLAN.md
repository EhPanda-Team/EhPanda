---
phase: 11-infra-refactor-lint-capstone
plan: 22.1
type: execute
wave: 23
depends_on: [11-22]
files_modified:
  - AppPackage/Tests/
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "@MainActor survives on a test file/suite/function ONLY where the test actually requires the main actor (compiler-enforced, e.g. TestStore APIs) or becomes meaningless without it (D-13); every other occurrence across the remaining 18 annotated files is removed"
    - "The FULL suite (EhPanda scheme) passes running in parallel on any thread — LINT-01 success criterion 4's end state"
  artifacts:
    - path: "AppPackage/Tests/"
      provides: "minimal-@MainActor parallel test suite + phase-wide survivor inventory"
  key_links:
    - from: "removed @MainActor annotations"
      to: "Swift Testing parallel execution"
      via: "tests run on arbitrary threads; only genuine main-actor needs remain isolated"
      pattern: "@MainActor"
---

<objective>
Complete the D-13 `@MainActor` sweep on the remaining 18 annotated files (SettingFeatureTests 9, ReadingFeatureTests 3, AppFeatureTests 2, DetailFeatureTests 2, ImageClientTests 2 — DownloadsFeatureTests was swept in 11-22), then run the phase's first FULL-suite parallel gate. Split from 11-22 so the gate — and any cross-test pollution fixes it surfaces — has its own executor context.

Purpose: completes LINT-01 success criterion 4 (with plans 11-19..22); the full suite now runs in parallel on any thread.
Output: minimal-@MainActor test suite + green full-suite parallel run + complete survivor inventory for the phase-end owner review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: @MainActor -->

## Artifacts this phase produces (plan 11-22.1 portion)

- @MainActor removals across the 5 remaining targets; phase-wide survivor inventory (file + scope + reason, merging 11-22's Downloads portion from its SUMMARY) for the phase-end owner review

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remaining targets @MainActor sweep (18 files, 5 targets)</name>
  <files>AppPackage/Tests/SettingFeatureTests/, AppPackage/Tests/ReadingFeatureTests/, AppPackage/Tests/AppFeatureTests/, AppPackage/Tests/DetailFeatureTests/, AppPackage/Tests/ImageClientTests/</files>
  <read_first>
    - The remaining annotated files: SettingFeatureTests (9), ReadingFeatureTests (3), AppFeatureTests (2), DetailFeatureTests (2), ImageClientTests (2)
    - 11-22-SUMMARY.md — the sweep procedure as applied and the Downloads-portion survivor inventory to merge
  </read_first>
  <action>
    Apply the same narrow-scope sweep as 11-22 to the remaining 18 files: remove the annotation at its current scope; if the compiler then errors on main-actor-isolated API use, restore at the NARROWEST scope that satisfies it (function over suite, suite over file). ImageClientTests note: UIImage-based fixture rendering (scale-1 PNG fixtures) is a likely genuine keeper — UIKit image drawing is main-actor-adjacent; keep only what the compiler or correctness demands. Never restore for convenience or style. Record survivors (file, scope, one-line reason) per target.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Every remaining @MainActor in the 5 targets is compiler-required at its scope (spot-verifiable: removing any survivor breaks the build) or carries a one-line justification
    - All test targets compile (build-for-testing green)
  </acceptance_criteria>
  <done>All 45 originally-annotated files swept (27 in 11-22 + 18 here); survivors minimal and justified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FULL-SUITE parallel gate + phase-wide survivor inventory</name>
  <files>AppPackage/Tests/</files>
  <action>
    Run the phase's parallel gate: the FULL suite via the EhPanda scheme — Swift Testing parallel by default, now with the 11-19..21 serialization work and both sweep halves landed. Any cross-test pollution surfacing here is fixed by injection (per-test instances), never by re-adding traits or annotations. Then complete the phase-wide survivor inventory in the SUMMARY: merge 11-22's Downloads-portion inventory (from 11-22-SUMMARY.md) with this plan's Task 1 survivors — this is the input for the phase-end owner review of criterion 4 (and 11-29's 11-EXCEPTIONS.md).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Full EhPanda-scheme suite passes in parallel
    - grep count of @MainActor across AppPackage/Tests is materially reduced from 45 files, and every survivor appears in the SUMMARY inventory with scope + reason
  </acceptance_criteria>
  <done>Criterion 4 end-state reached: full suite parallel on any thread, minimal documented isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test concurrency → shared process state | Parallel execution exposes any residual shared state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-25 | Denial of Service | flaky suite from premature annotation removal | medium | mitigate | Narrow-scope compiler-driven restore; full-suite gate; inject-over-serialize for pollution (same threat as 11-22 — mitigation completes here) |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- Full EhPanda-scheme test run green in parallel (single xcodebuild invocation; never overlap)
- Phase-wide survivor inventory (both sweep halves) complete in SUMMARY
</verification>

<success_criteria>
- D-13 delivered: @MainActor minimal and justified across all 45 files; full suite parallel and green
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-22.1-SUMMARY.md` when done
</output>
