---
phase: 11-infra-refactor-lint-capstone
plan: 5
type: execute
wave: 5
depends_on: [11-04]
files_modified:
  - AppPackage/Sources/AppTools/DataCache.swift
  - AppPackage/Sources/AppTools/Defaults.swift
  - AppPackage/Sources/AppTools/Extensions.swift
  - AppPackage/Sources/AppModels/Persistence/JSONValue.swift
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - "AppTools and AppModels contain zero unannotated optional-try sites"
    - "JSONValue still decodes all six representations via sequential type probes with identical selection order (Phase 9 09-11 invariant)"
    - "DataCache fire-and-forget housekeeping still never replaces cache hit/miss outcomes (Phase 9 09-08 invariant)"
  artifacts:
    - path: "AppPackage/Sources/AppModels/Persistence/JSONValue.swift"
      provides: "explicit do/catch type-probe chain (or D-02 exception candidates) with unchanged decode semantics"
  key_links:
    - from: "AppPackage/Sources/AppTools/DataCache.swift"
      to: "OSLog Logger"
      via: "file-top private let logger for unexpected housekeeping failures"
      pattern: "private let logger = Logger"
---

<objective>
Root-fix the 23 optional-try sites in AppTools (DataCache.swift, Defaults.swift, Extensions.swift — 17) and AppModels (Persistence/JSONValue.swift — 6), per D-01/D-02/D-03, preserving each site's Phase 9-documented contract (encode/decode nil-contracts, URL-detector nil-on-failure, sequential JSON type probes, fire-and-forget cache housekeeping).

Purpose: clears the utility-layer optional_try violations ahead of the rule flip.
Output: AppTools + AppModels free of unannotated optional-try, behavior identical.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-infra-refactor-lint-capstone/11-CONTEXT.md
@.planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md
@.planning/phases/11-infra-refactor-lint-capstone/11-PATTERNS.md
</context>

<!-- planner-discipline-allow: try? -->

## Artifacts this phase produces (plan 11-05 portion)

- Explicit do/catch conversions in DataCache.swift, Defaults.swift, Extensions.swift, JSONValue.swift
- Possible small private helper in JSONValue.swift expressing the probe-chain (executor's discretion) — no public API changes

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AppTools — DataCache + Defaults + Extensions (17 sites)</name>
  <files>AppPackage/Sources/AppTools/DataCache.swift, AppPackage/Sources/AppTools/Defaults.swift, AppPackage/Sources/AppTools/Extensions.swift</files>
  <read_first>
    - The three files (every optional-try site + Phase 9 rationale comments)
    - .planning/phases/11-infra-refactor-lint-capstone/11-RESEARCH.md (Pattern 1; D-02 form)
  </read_first>
  <action>
    Triage per D-01 (propagate → do/catch mirror → D-02 exception candidate). Expected shapes from Phase 9's decisions: DataCache's 13 optional operations are documented cache probes, metadata fallbacks, or fire-and-forget housekeeping — convert to explicit do/catch mirroring the exact fallback; log via `logger.error` only where a failure is genuinely unexpected (e.g. a failed write of freshly-encoded data), keep probes as bare explanatory catches (a miss is normal control flow, logging would spam). Encoding/decoding helpers in Extensions/Defaults with a nil-return contract convert to do/catch returning nil, preserving the public Optional signatures. The URL-detector and regex sites: detector/regex construction failure means invalid input / disabled suggestions — do/catch preserving that contract. Add `private let logger` + a module Logger init extension if AppTools lacks one (mirror ParserFeature's Logger+.swift shape). Never log cached payload contents or URLs.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppTools -destination 'platform=iOS Simulator,name=iPhone Air'</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered `try?` count in AppPackage/Sources/AppTools equals 0 (reason-annotated exception sites excluded)
    - AppTools scheme tests pass unmodified
    - `bash Scripts/check-cookie-logging.sh` exits 0
  </acceptance_criteria>
  <done>AppTools converted with contracts intact; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AppModels — JSONValue probe chain (6 sites)</name>
  <files>AppPackage/Sources/AppModels/Persistence/JSONValue.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Persistence/JSONValue.swift (the six sequential decode attempts and their ordering)
  </read_first>
  <action>
    JSONValue's six sequential decode attempts are type probes where failure selects the next representation (Phase 9 09-11) — failure IS the control flow, so the root fix is an explicit form, not logging: convert each probe to do/catch where the catch's sole meaning is "try the next representation" (a brief comment stating that), or introduce one small private generic helper that performs a single decode attempt in do/catch and returns an Optional, called six times in the same order. Decode semantics, ordering, and the final failure behavior must be bit-identical — the persisted `@Shared` models depend on it (v1 schema policy). If after honest attempt the explicit form materially damages readability, a D-02 exception candidate per site is acceptable (owner reviews at phase end) — but attempt the root fix first. Verify with the AppModels scheme tests (schema/migration suites exercise JSONValue).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppModels -destination 'platform=iOS Simulator,name=iPhone Air' && grep -rn "try? " AppPackage/Sources/AppModels AppPackage/Sources/AppTools | grep -v "//" | wc -l  # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered `try?` count across AppPackage/Sources/AppModels and AppPackage/Sources/AppTools equals 0 (reason-annotated sites excluded)
    - AppModels scheme tests pass unmodified (decode order + outcomes identical)
  </acceptance_criteria>
  <done>JSONValue probes explicit; utility layer optional-try-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted JSON → JSONValue | Stored data re-enters decode probes |
| Cache disk I/O → DataCache | Filesystem failures handled explicitly now |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Information Disclosure | new logger calls in AppTools | medium | mitigate | Never log cached payloads/URLs; cookie-logging scan gate |
| T-11-07 | Tampering | JSONValue decode-order drift corrupting persisted reads | high | mitigate | Bit-identical probe order enforced; AppModels schema/migration tests as gate |
| T-11-SC | Tampering | package installs | low | accept | No packages installed this phase |
</threat_model>

<verification>
- AppTools + AppModels scheme tests green (sequential xcodebuild)
- Zero audit across both modules; cookie-logging scan exits 0
</verification>

<success_criteria>
- AppTools + AppModels contribute zero unannotated optional_try violations
- All Phase 9 documented contracts (nil-returns, probe order, fire-and-forget) preserved
</success_criteria>

<output>
Create `.planning/phases/11-infra-refactor-lint-capstone/11-05-SUMMARY.md` when done
</output>
