---
phase: 09-correctness-structured-error-handling
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - AppPackage/Sources/AppComponents/ErrorInfoView.swift
  - AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings
  - AppPackage/Sources/AppComponents/AppAlertState.swift
  - AppPackage/Sources/SystemNotificationExt/View+Toast.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "ErrorInfoView renders a native Form with Description / Suggested Solution (if any) / Context (if any) / Environment sections and a working close toolbar button, iOS/iPadOS only."
    - "ErrorInfoView Environment section sources app version/build from AppInfo, device from DeviceClient, OS from ProcessInfo — never AppUtil.* or DeviceUtil.*."
    - "AppAlertState carries an optional ErrorInfo payload and an .error(_ errorInfo:) factory whose message uses the error's alertText, keeping the toast button-less and 3s auto-hiding."
    - "The .toast(_:onErrorTap:) modifier routes a tap on an ErrorInfo-bearing toast to a host closure within the 3s window, without the button-less toast store ever sending an action."
  artifacts:
    - AppPackage/Sources/AppComponents/ErrorInfoView.swift
    - AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings
  key_links:
    - "AppAlertState.errorInfo -> View+Toast onErrorTap closure -> (later) PresentationReducer.presentErrorInfo"
    - "ErrorInfoView(errorInfo:) -> errorInfo.error.localizedDescription/.solution + errorInfo.context rows + AppInfo/DeviceClient/ProcessInfo env"
  prohibitions:
    - statement: "No secret reaches ErrorInfoView Context or Environment: context rows render only whitelisted ContextKeys (URL path only), environment renders only app version/build, device type, OS string."
      status: enforced
      verification: "Read ErrorInfoView body — Context ForEach renders ContextKey.rawValue + AnyHashableBox.displayValue only; Environment LabeledContents are exactly AppInfo.version/build, deviceClient.deviceType(), ProcessInfo OS string; no cookie/token/URL-with-query/home-path is referenced."
    - statement: "No #if os(macOS) branch and no Firebase/.analyticsScreen appear in ErrorInfoView (iOS/iPadOS only, D-09)."
      status: enforced
      verification: "grep -c 'os(macOS)\\|analyticsScreen\\|Firebase' ErrorInfoView.swift == 0."
    - statement: "The button-less toast (Action == Never) is not made to send an action; the tap is a modifier-level closure."
      status: enforced
      verification: "grep for store.send inside View+Toast returns nothing new; onErrorTap is a plain closure parameter."
---

<objective>
Build the failure surface for QUAL-04 SC-3: the native `ErrorInfoView` detail surface (D-09), the `AppError`-bearing tappable toast payload (D-08/D-11), and the `onErrorTap` routing seam on the toast modifier (Pitfall 2). Reuse — do not rebuild — the Liquid Glass toast renderer and the full-screen `ErrorView` (D-08). This plan wires the surface; plan 09-04 connects it to the renamed presentation reducer.

Purpose: give users a dismissable, diagnostic error detail surface reached by tapping a failure toast.
Output: `ErrorInfoView.swift` (new, AppComponents) + new AppComponents `.xcstrings` keys; `AppAlertState` gains an `ErrorInfo?` slot + `.error(_ errorInfo:)` factory; `View+Toast` gains `onErrorTap`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
@.planning/phases/09-correctness-structured-error-handling/09-VALIDATION.md
</context>

## Artifacts this phase produces

This plan creates/changes:
- `ErrorInfoView` (AppComponents) + new AppComponents `Localizable.xcstrings` keys (errorInfoDescription, errorInfoSolution, errorInfoContext, errorInfoEnvironment, errorInfoAppVersion, errorInfoDevice, errorInfoOS, close).
- `AppAlertState.errorInfo: ErrorInfo?` slot + `.error(_ errorInfo: ErrorInfo)` factory.
- `onErrorTap` parameter on `.toast(_:onErrorTap:)` (SystemNotificationExt) + inline just-cause comment on the existing `try? await Task.sleep` cancellation survivor.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ErrorInfoView (native Form detail surface, iOS-only, redacted)</name>
  <files>AppPackage/Sources/AppComponents/ErrorInfoView.swift, AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings</files>
  <read_first>
    - AppPackage/Sources/AppComponents/AlertView.swift (ErrorView/AlertView import block, public-init style, error.symbol/error.alertText consumption to mirror; ErrorView stays UNCHANGED)
    - AppPackage/Sources/AppComponents/AppError+Symbol.swift (error.symbol accessor — UNCHANGED)
    - AppPackage/Sources/AppModels/Support/AppError+Context.swift (ErrorInfo/Context/ContextKey/AnyHashableBox from 09-01)
    - AppPackage/Sources/AppModels/Utilities/AppInfo.swift (version/build) and DeviceClient/DeviceClient.swift (deviceType(), @MainActor)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §4 (env sourcing) and §6 (copy-ready ErrorInfoView skeleton) and §"Security Domain" (redaction whitelist)
    - AppPackage/Sources/AppComponents/Resources/Localizable.xcstrings (key pattern, 6 locales)
    - .claude/skills for pfw-modern-swiftui + swift-accessibility-skill (LabeledContent a11y, decorative symbols, clear close label)
    - .swiftlint.yml + AGENTS.md (labeled-format-argument / non-translated-key rules; confirmation-dialog/alert-placement note: keep close on the stable action source)
  </read_first>
  <behavior>
    - Given an ErrorInfo with an error + a context of [.action: "...", .statusCode: 200], ErrorInfoView shows a Description section (error.localizedDescription), a Solution section only when error.solution != nil, a Context section listing each ContextKey.rawValue label + AnyHashableBox.displayValue value, and an Environment section with app version/build, device type, OS string.
    - The close toolbar button dismisses the sheet.
    - Empty/nil context omits the Context section; nil solution omits the Solution section.
  </behavior>
  <action>
    Create `AppPackage/Sources/AppComponents/ErrorInfoView.swift` as `public struct ErrorInfoView: View` taking `public init(errorInfo: ErrorInfo)`. Mirror AlertView.swift's import block + public-init conventions. Body: `NavigationStack { Form { ... } .toolbar { ToolbarItem(placement: .confirmationAction) { Button(.close) { dismiss() } } } }` per RESEARCH §6. Sections: `Section(.errorInfoDescription) { Text(errorInfo.error.localizedDescription) }`; `if let solution = errorInfo.error.solution { Section(.errorInfoSolution) { Text(solution) } }`; `if let context = errorInfo.context, !context.isEmpty { Section(.errorInfoContext) { ForEach over the context sorted by key, rendering LabeledContent(key.rawValue, value: box.displayValue) } }`; `Section(.errorInfoEnvironment) { LabeledContent(.errorInfoAppVersion, value: "\(AppInfo.version) (\(AppInfo.build))"); LabeledContent(.errorInfoDevice, value: String(describing: deviceClient.deviceType())); LabeledContent(.errorInfoOS, value: ProcessInfo.processInfo.operatingSystemVersionString) }`. Resolve `@Dependency(\.deviceClient) private var deviceClient` and read `deviceType()` in the main-actor body (D-09). Use `@Environment(\.dismiss)`. iOS/iPadOS only — NO `#if os(macOS)`, NO Firebase/`.analyticsScreen`. Redaction (T-9-CTX): render ONLY ContextKey.rawValue + displayValue and the three environment data; never reference cookies/tokens/full-URL-with-query/home paths. Add all new `.xcstrings` keys (errorInfoDescription/Solution/Context/Environment section titles, errorInfoAppVersion/Device/OS labels, close) to `AppComponents/Resources/Localizable.xcstrings` in all 6 locales (translated user-facing strings). Accessibility: give the close button a clear label; treat any glyph as decorative (`.accessibilityHidden(true)`).
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ErrorInfoView.swift` builds; `grep -c 'os(macOS)\|analyticsScreen\|Firebase' ErrorInfoView.swift` == 0.
    - Environment section references exactly `AppInfo.version`, `AppInfo.build`, `deviceClient.deviceType()`, `ProcessInfo...operatingSystemVersionString`; `grep -c 'AppUtil\|DeviceUtil' ErrorInfoView.swift` == 0.
    - Context rows render `ContextKey.rawValue` + `AnyHashableBox.displayValue` only; no cookie/token/credential symbol referenced.
    - All new AppComponents xcstrings keys exist in de/en/ja/ko/zh-Hans/zh-Hant.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>ErrorInfoView renders the four sections + close, sourced from AppInfo/DeviceClient/ProcessInfo, with no macOS/Firebase branches and no secret paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppAlertState — ErrorInfo slot + .error(_ errorInfo:) factory</name>
  <files>AppPackage/Sources/AppComponents/AppAlertState.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/AppAlertState.swift (the struct's stored props + the id-excluded ==/Hashable at lines 72-92, and the Action == Never toast factories at 124-175)
    - AppPackage/Sources/AppModels/Support/AppError+Context.swift (ErrorInfo from 09-01)
    - .planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md "AppAlertState.swift" section (factory idiom + Pitfall 2)
    - .claude/skills for pfw-composable-architecture
  </read_first>
  <behavior>
    - AppAlertState<Never>.error(errorInfo) builds a .toast(icon: .error, autoHide: true) whose message is errorInfo.error.alertText and whose stored errorInfo == the passed value.
    - Two error toasts with equal errorInfo compare equal (content-based ==; errorInfo's own id is already excluded from its ==).
    - Existing .error(caption:) / .error(caption: String?) factories are unchanged.
  </behavior>
  <action>
    In `AppAlertState.swift`, add `public var errorInfo: ErrorInfo?` to the struct's stored properties (default nil in the existing inits so alert/text-field inits are unaffected), and include it in `==`/`hash(into:)` (content-based; ErrorInfo already excludes its id from equality). Import AppModels if not already. In the `extension AppAlertState where Action == Never` block, add `public static func error(_ errorInfo: ErrorInfo) -> Self` that constructs a `.toast(icon: .error, autoHide: true)` with `title: TextState(localized: .error)` and `message: TextState(errorInfo.error.alertText)`, then stores the `errorInfo`. Keep it button-less (Action == Never; the tap is routed at the modifier layer, Pitfall 2). Do NOT alter the existing `.error(caption:)` overloads.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `AppAlertState.swift` declares `var errorInfo: ErrorInfo?` and a `static func error(_ errorInfo: ErrorInfo)`; module builds.
    - The new factory sets `errorInfo` and uses `errorInfo.error.alertText` for the message (grep confirms).
    - Existing `.error(caption:)` overloads and the id-excluded == pattern are preserved.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>The toast can carry an AppError (via ErrorInfo) so a tap can later route to ErrorInfoView.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: View+Toast — onErrorTap routing seam + comment the Task.sleep survivor</name>
  <files>AppPackage/Sources/SystemNotificationExt/View+Toast.swift</files>
  <read_first>
    - AppPackage/Sources/SystemNotificationExt/View+Toast.swift (the .toast(_:) modifier at 23-29, ToastViewModifier gesture/task wiring at 31-84, and the `try? await Task.sleep(for: .seconds(3))` cancellation survivor at line 77)
    - .planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md "View+Toast.swift" section (onTapGesture placement + Pitfall 2)
    - .planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md D-02/D-11 (survivor comment rule; 3s auto-hide preserved)
    - .swiftlint.yml
  </read_first>
  <behavior>
    - .toast(_:onErrorTap:) presents identically to today; when the presented toast has a non-nil errorInfo, tapping it within the auto-hide window invokes onErrorTap(errorInfo) exactly once.
    - The 3s auto-hide and downward-swipe dismissal are unchanged; a loading toast (no errorInfo) does nothing on tap.
  </behavior>
  <action>
    In `View+Toast.swift`, add a second overload/parameter so the public modifier is `.toast(_ item:onErrorTap:)` taking `onErrorTap: @escaping (ErrorInfo) -> Void` (provide a default `{ _ in }` so existing call sites keep compiling until 09-04 wires it). Thread `onErrorTap` into `ToastViewModifier`. Add `.onTapGesture { if let errorInfo = store.state.errorInfo { onErrorTap(errorInfo) } }` on the `ToastMessageView` beside the existing `dismissGesture`/`.task(id:)`, guarded on the presented store's `errorInfo` (Pitfall 2 — the button-less toast store never `send`s). Do NOT touch the 3s auto-hide timer. Add an inline just-cause comment on the existing `try? await Task.sleep(for: .seconds(3))` line (line 77) stating it is a deliberate cancellation-swallow survivor: `Task.sleep` only throws `CancellationError` on replacement/dismissal, which is the intended no-op with no `AppError` to raise (D-02 bucket-c survivor). Import AppModels if needed for `ErrorInfo`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `View+Toast.swift` exposes `onErrorTap` on `.toast(...)`; module builds and existing `.toast($store...)` call sites still compile (default closure).
    - The onTapGesture reads `store.state.errorInfo` and does NOT call `store.send` (grep: no new `store.send` in View+Toast).
    - The `try? await Task.sleep` line has an adjacent `//` just-cause comment; no new `swiftlint:disable`.
  </acceptance_criteria>
  <done>The toast modifier can route an ErrorInfo tap to the host, the 3s auto-hide is intact, and the one SystemNotificationExt try? survivor is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ErrorInfo.context/environment -> ErrorInfoView (user-visible) | Diagnostic + environment data reaches the screen; must expose no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | `ErrorInfoView` Context + Environment sections | high | mitigate | Context ForEach renders only whitelisted ContextKey rawValue + displayValue (URL is path-only); Environment renders only AppInfo version/build, device type, OS string. No cookies/tokens/full-URL-with-query/IP/home-path referenced. Enforced by the ErrorInfoView redaction grep gate (Task 1). ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `xcodebuild build -scheme AppPackage-Package` green.
- ErrorInfoView redaction grep gate passes (no AppUtil/DeviceUtil/macOS/Firebase/secret).
- New AppComponents xcstrings keys filled in 6 locales.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
ErrorInfoView (native Form, Description/Solution/Context/Environment + close, redacted, iOS-only), an ErrorInfo-bearing toast factory, and an onErrorTap routing seam all build; the toast's 3s auto-hide is preserved.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-03-SUMMARY.md` when done.
</output>
