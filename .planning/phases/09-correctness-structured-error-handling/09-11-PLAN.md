---
phase: 09-correctness-structured-error-handling
plan: 11
type: execute
wave: 11
depends_on: [09-10]
files_modified:
  - AppPackage/Sources/AppModels/Persistence/JSONValue.swift
  - AppPackage/Sources/AppComponents/TagSuggestionView.swift
  - AppPackage/Sources/AppComponents/PreviewImageView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/MarkdownExt/MarkdownUtil.swift
  - AppPackage/Sources/DetailFeature/Components/LinkedText.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "The type-probe decodes in AppModels/JSONValue.swift and the remaining view/markdown try? sites are documented decode-with-default/best-effort survivors with inline just-cause comments (not forced into do/catch)."
    - "Every remaining try? in AppPackage/Sources carries an adjacent inline just-cause comment — no undocumented swallowing survives the phase."
    - "The full AppPackage-Package suite is green and SwiftLint is clean (the build-tool plugin runs the ruleset at error), with no new // swiftlint:disable introduced by Phase 9."
  artifacts:
    - AppPackage/Sources/AppModels/Persistence/JSONValue.swift
  key_links:
    - "documented survivors -> Phase 11 optional_try flip (owner-blessed exceptions, D-03/D-04)"
  prohibitions:
    - statement: "No remaining try? in AppPackage/Sources lacks an adjacent inline just-cause comment."
      status: enforced
      verification: "Audit: for every `try?` match in Sources, an adjacent `//` just-cause comment exists (the phase-gate task enumerates and confirms)."
    - statement: "No new // swiftlint:disable was introduced anywhere in Phase 9 (the sanctioned suppression is deferred to Phase 11 per D-03)."
      status: enforced
      verification: "git diff across the phase shows zero added `// swiftlint:disable` lines."
    - statement: "optional_try stays commented in .swiftlint.yml (D-04) — this phase does not enable it."
      status: enforced
      verification: "grep -n 'optional_try' .swiftlint.yml shows it still commented; the file is unchanged by Phase 9."
---

<objective>
Finish the `try?` sweep tail — the type-probe decodes in `AppModels/Persistence/JSONValue.swift` (6, bucket-b: `try Bool, then Int, then Double...` where the throw is the control flow, Pitfall 3) plus the remaining single view/markdown sites (AppComponents 2, ReadingFeature 1, MarkdownExt 1, DetailFeature 1) — as documented survivors, then run the phase gate: every remaining `try?` in `AppPackage/Sources` carries an inline just-cause comment, no new `// swiftlint:disable` was introduced (D-03/D-04 defer the sanctioned suppression to Phase 11), the full suite is green, and SwiftLint is clean. This drives QUAL-04's `try?` count to a fully-documented residual so Phase 11 can flip `optional_try` at error.

Purpose: close the sweep and prove the phase's structured-error posture end-to-end.
Output: annotated tail source + the phase-gate audit results in the summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
@.planning/phases/09-correctness-structured-error-handling/09-VALIDATION.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): the tail try? sites annotated as documented survivors; the phase-gate audit. (SystemNotificationExt's one try? survivor was documented in 09-03.)

<tasks>

<task type="auto">
  <name>Task 1: JSONValue type-probe + remaining view/markdown survivors (11)</name>
  <files>AppPackage/Sources/AppModels/Persistence/JSONValue.swift, AppPackage/Sources/AppComponents/TagSuggestionView.swift, AppPackage/Sources/AppComponents/PreviewImageView.swift, AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/MarkdownExt/MarkdownUtil.swift, AppPackage/Sources/DetailFeature/Components/LinkedText.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Persistence/JSONValue.swift (6 `try? container.decode(T.self)` type-probes — the throw IS the control flow; RESEARCH §2 bucket-b + Pitfall 3: do NOT convert to do/catch)
    - AppPackage/Sources/AppComponents/TagSuggestionView.swift (1) + PreviewImageView.swift (1) + AppPackage/Sources/ReadingFeature/ReadingView.swift (1) + AppPackage/Sources/MarkdownExt/MarkdownUtil.swift (1) + AppPackage/Sources/DetailFeature/Components/LinkedText.swift (1)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (buckets) + Pitfall 3
    - .swiftlint.yml
  </read_first>
  <action>
    For JSONValue.swift's 6 type-probe decodes: keep each `try?` and add an inline just-cause comment stating it is a deliberate type-probe (try one type, fall through to the next) that genuinely cannot be `do/catch` because the throw is the control flow (D-02 bucket-b, Pitfall 3). For the 5 remaining single sites (TagSuggestionView, PreviewImageView, ReadingView, MarkdownUtil, LinkedText): classify each — a genuine decode-with-default/best-effort keeps `try?` + inline just-cause comment; a real surfaceable failure converts to `do throws(AppError)` (Pattern 1). Preserve behavior on all of them (these feed suggestions, previews, reading, markdown rendering — parity-sensitive surfaces). Do NOT change StrictDecodingTests behavior.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:AppModelsTests 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:AppModelsTests` exits 0 (JSONValue decoding parity).
    - Every remaining `try?` across these 6 files has an adjacent inline just-cause comment.
    - No JSONValue type-probe was forced into do/catch.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>The tail survivors are documented at parity; JSONValue type-probes stay as commented probes.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — residual try? audit + full suite + SwiftLint clean</name>
  <files>AppPackage/Sources (residual try? audit — add a just-cause comment to any remaining match that lacks one)</files>
  <read_first>
    - .swiftlint.yml (optional_try must stay commented — D-04; the build-tool plugin runs the ruleset at error during build)
    - .planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md D-02/D-03/D-04 (survivor rule; suppression deferred to Phase 11)
    - .planning/phases/09-correctness-structured-error-handling/09-VALIDATION.md (phase-gate: full suite green + SwiftLint clean before verify)
    - STATE.md (no-overlapping-xcodebuild rule — this is the only invocation in this wave)
  </read_first>
  <action>
    Run the residual audit: enumerate every `try?` match in `AppPackage/Sources` (`grep -rn 'try?' AppPackage/Sources`), and confirm each has an adjacent inline just-cause comment (from this phase's sweep) — record the residual count + a one-line justification-category per file in the summary. Confirm no `// swiftlint:disable` was added anywhere by Phase 9 (`git diff` for the phase shows zero added suppressions) and that `.swiftlint.yml` `optional_try` is still commented (D-04 — Phase 9 does not enable it). Then run the full `AppPackage-Package` suite (single invocation — never overlap xcodebuild): the build-tool SwiftLintPlugin runs the ruleset at error during the build, so a green build+suite proves SwiftLint is clean. If any residual `try?` lacks a comment, add the just-cause comment (or convert) before finishing.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Full `AppPackage-Package` suite exits 0 (build + SwiftLintPlugin + all tests).
    - `grep -rn 'try?' AppPackage/Sources` residual: every match has an adjacent `//` just-cause comment (audit recorded in summary).
    - `git diff` for the phase adds zero `// swiftlint:disable` lines; `.swiftlint.yml` optional_try still commented.
    - No new `// swiftlint:disable` anywhere.
  </acceptance_criteria>
  <done>Every surviving try? is a documented exception, the full suite is green, SwiftLint is clean, and optional_try remains commented — Phase 11 can flip it with owner-blessed suppressions on the documented survivors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decode/probe failure -> default (internal) | Type-probe/decode-with-default degradation is internal; no secret enters an error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | tail survivors + phase-gate | high | mitigate | Tail survivors degrade to defaults internally; none surface a secret. The phase-gate audit confirms no undocumented swallowing and (via the earlier plans) that no secret reaches ErrorInfoView context/environment or logs. ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Full `AppPackage-Package` suite green (build + SwiftLint plugin + tests).
- Residual try? audit: every survivor commented; no new `// swiftlint:disable`.
- `.swiftlint.yml` optional_try still commented (D-04).
</verification>

<success_criteria>
The try? sweep is complete: every remaining try? in Sources is a documented exception, the full suite is green, SwiftLint is clean, no new suppression was introduced, and optional_try stays commented for Phase 11 to flip (QUAL-04).
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-11-SUMMARY.md` when done.
</output>
