---
phase: 09-correctness-structured-error-handling
plan: 10
type: execute
wave: 10
depends_on: [09-09]
files_modified:
  - AppPackage/Sources/LogsClient/LogsClient.swift
  - AppPackage/Sources/LibraryClient/LibraryClient.swift
  - AppPackage/Sources/ImageClient/ImageClient.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift
  - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsReducer.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "Every LogsClient/LibraryClient/ImageClient/SettingFeature-activity-logs try? site is converted (propagate/log/plain-try in a typed context) or kept as a documented survivor with an inline just-cause comment."
    - "Image cache writes stay best-effort, activity-log pumping behaves identically, and ImageClientTests + SettingFeatureTests stay green."
  artifacts:
    - AppPackage/Sources/ImageClient/ImageClient.swift
    - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift
  key_links:
    - "ImageClient best-effort cache writes -> already inside async client fns (bucket-d/c); never surfaced"
  prohibitions:
    - statement: "Best-effort cache writes and log-pump reads are not surfaced to the user; they log or stay documented survivors."
      status: enforced
      verification: "No ImageClient/log-pump try? conversion routes to a toast/ErrorInfo."
    - statement: "Every surviving try? carries an adjacent inline just-cause comment (D-02)."
      status: enforced
      verification: "Each remaining `try?` in the 5 files has an adjacent `//` just-cause comment."
    - statement: "No new // swiftlint:disable is introduced."
      status: enforced
      verification: "grep across the 4 modules shows no new swiftlint:disable from this plan."
---

<objective>
Sweep the client tail — LogsClient (4), LibraryClient (4), ImageClient (4), and SettingFeature's activity-logs reducers (5: AppActivityLogsPump 4 + AppActivityLogs 1) — per QUAL-04 D-01/D-02/D-12. ImageClient's `try? await dataCache.store/removeData/readerImageData` are best-effort cache writes inside async client fns (bucket-c/d: plain `try` if the enclosing fn already `throws(AppError)`, else log or commented survivor). Log/library/pump sites classified and resolved the same way, at behavior parity.

Purpose: complete the client-layer sweep for the reworked seams (aligned with QUAL-02's covered clients).
Output: converted/annotated source across 4 modules; ImageClientTests + SettingFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): the client-tail try? sites resolved per RESEARCH §2 buckets.

<tasks>

<task type="auto">
  <name>Task 1: LogsClient + LibraryClient + ImageClient sweep (12)</name>
  <files>AppPackage/Sources/LogsClient/LogsClient.swift, AppPackage/Sources/LibraryClient/LibraryClient.swift, AppPackage/Sources/ImageClient/ImageClient.swift</files>
  <read_first>
    - AppPackage/Sources/ImageClient/ImageClient.swift (4: `try? await dataCache.store/removeData/readerImageData` — best-effort cache writes; RESEARCH §2 bucket-d/c)
    - AppPackage/Sources/LogsClient/LogsClient.swift (4) + AppPackage/Sources/LibraryClient/LibraryClient.swift (4)
    - AppPackage/Sources/AppModels/Support/AppError.swift
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (bucket-c/d) + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml + Logger placement rule
    - AppPackage/Tests/ImageClientTests (cache/pixel parity, per-test DataCache)
  </read_first>
  <action>
    Resolve each try? site. ImageClient's best-effort cache writes: if the enclosing fn already `throws(AppError)`, make it a plain `try` (bucket-d); otherwise keep `try?` + inline just-cause comment (best-effort cache write, non-fatal) or `do/catch` that logs — never surface a cache-write failure. LogsClient/LibraryClient: convert bucket-(a) surfaceable failures to `do throws(AppError)` propagating (Pattern 1); keep genuine best-effort as commented survivors. Any logging uses a top-of-file `private let logger` (Logger placement rule). Preserve behavior: cache hits/misses and log/library reads unchanged.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:ImageClientTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:ImageClientTests` exits 0 (cache/pixel parity).
    - Every remaining `try?` in the three client files has an adjacent inline just-cause comment.
    - ImageClient cache-write failures are not surfaced (no toast/ErrorInfo route); any logging uses a top-of-file logger.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>Logs/Library/Image client try? sites are resolved at parity; ImageClientTests green.</done>
</task>

<task type="auto">
  <name>Task 2: SettingFeature activity-logs reducers sweep (5)</name>
  <files>AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift, AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsReducer.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsPumpReducer.swift (4) + AppActivityLogsReducer.swift (1)
    - STATE.md Phase-7 notes (the long-lived activity-log pump; tests pause the pump to preserve TestStore exhaustivity)
    - AppPackage/Sources/AppModels/Support/AppError.swift
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (bucket-c: any `try? await Task.sleep` in the pump is a cancellation-swallow survivor) + 09-PATTERNS.md Pattern 1
    - AppPackage/Tests/SettingFeatureTests
    - .swiftlint.yml
  </read_first>
  <action>
    Classify each of the 5 activity-logs try? sites. A `try? await Task.sleep(...)` in the pump loop is a bucket-(c) cancellation swallow — keep `try?` + inline just-cause comment (only throws CancellationError, intended no-op). A best-effort log read/decode that degrades to a default — keep `try?` + comment. A surfaceable failure — convert to `do throws(AppError)` propagating (Pattern 1). Preserve the pump's long-lived effect behavior exactly (the tests pause the pump after expected actions — TestStore exhaustivity must hold).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:SettingFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:SettingFeatureTests` exits 0.
    - Every remaining `try?` in the two reducers has an adjacent inline just-cause comment.
    - The pump's effect/action sequence is unchanged.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>Activity-logs try? sites are resolved at parity; SettingFeatureTests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/pump failure -> log or default (never user) | Best-effort failures stay internal; logs carry no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | client cache/log messages | high | mitigate | Any catch-logging carries operation descriptors only, no credential/cookie/full-URL; follows the Phase-8 no-.public-secret discipline. Cache/log failures are never surfaced to the user. ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `-only-testing:ImageClientTests` and `-only-testing:SettingFeatureTests` green.
- Every surviving try? in the 5 files carries a just-cause comment.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
All 17 Logs/Library/Image/SettingFeature-activity-logs try? sites are converted or commented survivors, at behavior parity, with ImageClientTests + SettingFeatureTests green.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-10-SUMMARY.md` when done.
</output>
