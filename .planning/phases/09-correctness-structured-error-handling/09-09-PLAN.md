---
phase: 09-correctness-structured-error-handling
plan: 09
type: execute
wave: 9
depends_on: [09-08]
files_modified:
  - AppPackage/Sources/ParserFeature/Parser+List.swift
  - AppPackage/Sources/ParserFeature/Parser+Detail.swift
  - AppPackage/Sources/ParserFeature/Parser+Shared.swift
  - AppPackage/Sources/ParserFeature/Parser+Profile.swift
  - AppPackage/Sources/ParserFeature/Parser+Image.swift
  - AppPackage/Sources/ParserFeature/Parser+Torrent.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "Every ParserFeature try? site (44 across 6 files) is classified: legitimate decode-with-default / optional-field survivors keep try? with an inline just-cause comment; any site whose failure should fail the whole parse becomes do throws(AppError)."
    - "Parser output is byte-identical on the existing fixtures — ParserFeatureTests stay green (this is the core parse load-bearing path)."
  artifacts:
    - AppPackage/Sources/ParserFeature/Parser+List.swift
  key_links:
    - "surviving optional-field try? -> a documented, intentional degrade-to-default (D-02); the parse does not fail on a missing sub-field"
  prohibitions:
    - statement: "No parser survivor swallows a whole-parse failure silently — survivors are only intentional per-sub-field degradations, each commented."
      status: enforced
      verification: "Each remaining `try?` is an optional-field/decode-with-default extraction with an adjacent `//` just-cause comment; whole-parse failures (if any) throw AppError."
    - statement: "Parser output does not change on any existing fixture."
      status: enforced
      verification: "-only-testing:ParserFeatureTests exits 0."
    - statement: "No new // swiftlint:disable is introduced."
      status: enforced
      verification: "grep in ParserFeature shows no new swiftlint:disable from this plan."
---

<objective>
Sweep ParserFeature (44 sites) — the largest but highest-survivor-density module (RESEARCH §2). `Parser+List.swift` is almost entirely `(try? parseX(...)) ?? default` / `guard let x = try? parseX(...)` optional-field extraction: HTML parsing that intentionally degrades a missing sub-field to a default rather than failing the whole list parse. These are legitimate D-02 bucket-(b) survivors: keep `try?`, add an inline just-cause comment each. Convert to `do throws(AppError)` only a site whose failure genuinely should fail the whole parse. All at strict parse parity.

Purpose: document every parser survivor as an intentional exception (D-02/D-03) so Phase 11 can flip optional_try, without regressing the load-bearing parse path.
Output: annotated (mostly) ParserFeature source; ParserFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): ParserFeature try? sites annotated as documented survivors (bucket-b) or converted (rare bucket-a), per RESEARCH §2.

<tasks>

<task type="auto">
  <name>Task 1: Parser+List.swift survivor sweep (25)</name>
  <files>AppPackage/Sources/ParserFeature/Parser+List.swift</files>
  <read_first>
    - AppPackage/Sources/ParserFeature/Parser+List.swift (all 25 try? sites — `(try? parseX) ?? default` and `guard let = try? parseX` optional-field extraction)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 bucket-(b) (Parser+List is the densest survivor cluster) + Pitfall 3 (don't force optional-field/type-probe into do/catch)
    - AppPackage/Sources/AppModels/Support/AppError.swift (parseFailed — for the rare whole-parse-failure case)
    - AppPackage/Tests/ParserFeatureTests (fixture parity)
    - .swiftlint.yml
  </read_first>
  <action>
    For each of the 25 Parser+List.swift try? sites: if it is an intentional optional-field/decode-with-default extraction (the failure degrades one sub-field to a default and the parse continues), keep `try?` and add a concise inline just-cause comment stating the intentional degradation (D-02). If a site's failure should instead fail the whole list parse, convert it to `do throws(AppError) { ... } catch { throw .parseFailed }` (Pattern 1) — but only where that matches current behavior (RESEARCH expects most here to be survivors). Do NOT change parse output on any fixture; where a shared per-site comment style helps, use one form consistently. Keep the HTML-degradation semantics exactly.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:ParserFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:ParserFeatureTests` exits 0 (fixture parity).
    - Every remaining `try?` in Parser+List.swift has an adjacent inline just-cause comment.
    - Any conversion uses `do throws(AppError)`; no swallowing catch.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>Parser+List survivors are documented and the list-parse output is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Parser+Detail / +Shared / +Profile / +Image / +Torrent sweep (19)</name>
  <files>AppPackage/Sources/ParserFeature/Parser+Detail.swift, AppPackage/Sources/ParserFeature/Parser+Shared.swift, AppPackage/Sources/ParserFeature/Parser+Profile.swift, AppPackage/Sources/ParserFeature/Parser+Image.swift, AppPackage/Sources/ParserFeature/Parser+Torrent.swift</files>
  <read_first>
    - AppPackage/Sources/ParserFeature/Parser+Detail.swift (13) + Parser+Shared.swift (3) + Parser+Profile.swift (1) + Parser+Image.swift (1) + Parser+Torrent.swift (1)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (buckets) + Pitfall 3
    - AppPackage/Tests/ParserFeatureTests (fixture parity)
    - .swiftlint.yml
  </read_first>
  <action>
    Apply the same classification to the remaining 19 ParserFeature try? sites across the 5 files: legitimate optional-field/decode-with-default -> keep `try?` + inline just-cause comment (D-02); a genuine whole-parse-failure -> `do throws(AppError)` mapping to `.parseFailed` (Pattern 1). Preserve detail/profile/image/torrent parse output on all fixtures.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:ParserFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:ParserFeatureTests` exits 0.
    - Every remaining `try?` across these 5 files has an adjacent inline just-cause comment.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>All ParserFeature try? sites are documented survivors or justified conversions at parse parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML parse failure -> default value or parseFailed | Parse degradation is internal; no secret enters a parse error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | parse error context (if any is surfaced later) | low | mitigate | Parser sites throw only `.parseFailed` (no payload) or degrade to a default; no raw HTML/response body or credential is placed in a surfaced AppError. Context (action/reason/gid) is added at the reducer, not here. ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `-only-testing:ParserFeatureTests` green (fixture parity).
- Every surviving try? in ParserFeature carries a just-cause comment.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
All 44 ParserFeature try? sites are documented survivors (bucket-b) or justified conversions, at strict parse parity, with ParserFeatureTests green.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-09-SUMMARY.md` when done.
</output>
