---
phase: 09-correctness-structured-error-handling
plan: 05
type: execute
wave: 5
depends_on: [09-04]
files_modified:
  - AppPackage/Sources/FileClient/FileClient.swift
  - AppPackage/Sources/FileClient/TagTranslation+ChtConverted.swift
  - AppPackage/Sources/NetworkingFeature/Request.swift
  - AppPackage/Sources/NetworkingFeature/Request+Account.swift
  - AppPackage/Sources/NetworkingFeature/Request+Detail.swift
  - AppPackage/Sources/NetworkingFeature/Request+GData.swift
  - AppPackage/Sources/NetworkingFeature/Request+Image.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "Every FileClient/NetworkingFeature try? site is either converted to a do throws(AppError) that propagates a proper AppError (never swallowing) or kept as a documented decode-with-default survivor with an inline just-cause comment."
    - "Load-bearing happy paths (file read/write, tag-translator import, request response probing) behave identically — FileClientTests and NetworkingFeatureTests stay green."
  artifacts:
    - AppPackage/Sources/FileClient/FileClient.swift
    - AppPackage/Sources/NetworkingFeature/Request.swift
  key_links:
    - "converted throw sites -> owning reducer (nearest surface, D-12) decides present vs silent"
  prohibitions:
    - statement: "No converted site swallows an error (every do/catch either rethrows/propagates AppError or applies a justified, commented fallback in catch)."
      status: enforced
      verification: "Read each converted catch block; no empty catch that discards the error without a commented fallback."
    - statement: "Every surviving try? in these files carries an adjacent inline just-cause comment."
      status: enforced
      verification: "For each remaining `try?` match, an adjacent `//` comment states the just cause (D-02)."
    - statement: "No new // swiftlint:disable is introduced."
      status: enforced
      verification: "grep -rn 'swiftlint:disable' the two module dirs shows no new match attributable to this plan."
---

<objective>
Begin the QUAL-04 `try?` sweep (D-01/D-02/D-12) with the two cleanest bucket-(a) modules: FileClient (8 sites) and NetworkingFeature (9 sites). Convert genuine network/file/decode failures to `do throws(AppError)` that propagate (Pattern 1), keep only true decode-with-default survivors as commented `try?` (D-02), all at behavior parity.

Purpose: make swallowed file/network failures propagate to the owning reducer (D-12), driving the `try?` count down.
Output: converted/annotated FileClient + NetworkingFeature source; module tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): FileClient + NetworkingFeature `try?` sites converted or annotated. The four D-02 classification buckets and the Pattern-1 target shape are defined in 09-RESEARCH.md §2 and 09-PATTERNS.md ("try? -> do throws(AppError) sweep").

<tasks>

<task type="auto">
  <name>Task 1: FileClient try? sweep (8 sites)</name>
  <files>AppPackage/Sources/FileClient/FileClient.swift, AppPackage/Sources/FileClient/TagTranslation+ChtConverted.swift</files>
  <read_first>
    - AppPackage/Sources/FileClient/FileClient.swift (all 7 try? sites: JSONDecoder import, createDirectory, data.write, Data(contentsOf:) x3, removeItem)
    - AppPackage/Sources/FileClient/TagTranslation+ChtConverted.swift (the ChineseConverter try? — best-effort conversion fallback to self)
    - AppPackage/Sources/AppModels/Support/AppError.swift (fileOperationFailed(String) is the natural AppError for file ops)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (buckets; FileClient:35/49 are bucket-a examples) + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml
    - AppPackage/Tests/FileClientTests (existing parity coverage)
  </read_first>
  <action>
    Classify each FileClient try? per RESEARCH §2. Convert bucket-(a) sites — the tag-translator import decode (`try? JSONDecoder()...`), `try? data.write(to:options:)`, directory creation, and the `Data(contentsOf:)` reads whose failure is a real surfaceable file error — to `do throws(AppError) { ... } catch { ... }` (Pattern 1), mapping to `.fileOperationFailed(<reason>)` and propagating to the caller (never swallowing); update the enclosing function signature to `throws(AppError)` and its callers only as far as needed to keep behavior identical (a failure that was previously a silent nil now surfaces where D-12 wants it). For `TagTranslation+ChtConverted.swift`'s `try? ChineseConverter(...) else { return self }` — this is a legitimate best-effort conversion that intentionally falls back to the untranslated value; keep `try?` and add an inline just-cause comment (D-02 bucket-b). Preserve all happy-path behavior; do not change what a successful read/write/convert returns.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:FileClientTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:FileClientTests` exits 0.
    - Every remaining `try?` in the two FileClient files has an adjacent inline just-cause comment (`grep -n 'try?'` cross-checked against adjacent `//`).
    - Converted sites use `do throws(AppError)` and `.fileOperationFailed`; no empty/ swallowing catch.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>FileClient file/decode failures propagate as AppError or are documented survivors; FileClientTests green.</done>
</task>

<task type="auto">
  <name>Task 2: NetworkingFeature try? sweep (9 sites)</name>
  <files>AppPackage/Sources/NetworkingFeature/Request.swift, AppPackage/Sources/NetworkingFeature/Request+Account.swift, AppPackage/Sources/NetworkingFeature/Request+Detail.swift, AppPackage/Sources/NetworkingFeature/Request+GData.swift, AppPackage/Sources/NetworkingFeature/Request+Image.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift (3), Request.swift (2), Request+Detail.swift (2), Request+GData.swift (1), Request+Image.swift (1)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationReducer.swift lines 184-195 (the canonical do throws(AppError) response() shape — Pattern 1)
    - AppPackage/Sources/AppModels/Support/AppError.swift (networkingFailed/parseFailed mapping)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (Request+*.swift response probes = bucket-a) + Pitfall 3 (do NOT convert type-probe decodes)
    - AppPackage/Tests/NetworkingFeatureTests (existing parity coverage — the Phase-4 offline harness)
    - .swiftlint.yml
  </read_first>
  <action>
    Classify each NetworkingFeature try? per RESEARCH §2. Convert bucket-(a) response/parse probes to `do throws(AppError) { ... } catch { ... }` matching the established `response()` shape (Pattern 1), mapping to `.networkingFailed`/`.parseFailed` and propagating — preserving the Phase-4 retry placement and the one-AppError-per-thrown-boundary discipline recorded in STATE.md. If any site is a type-probe decode (try one type, then another) or a genuine best-effort default, keep `try?` + inline just-cause comment (bucket-b, Pitfall 3). Do NOT alter request retry counts, chunking, or map/compactMap semantics (the frozen Phase-4 behavior); the offline harness tests must still pass unchanged.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:NetworkingFeatureTests` exits 0 (Phase-4 parity preserved).
    - Every remaining `try?` in the NetworkingFeature files has an adjacent inline just-cause comment.
    - Converted sites use `do throws(AppError)`; no swallowing catch; no type-probe decode forced into do/catch.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>NetworkingFeature request/parse failures propagate as AppError or are documented survivors; NetworkingFeatureTests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network/file failure -> AppError -> reducer | Failures now propagate; the reducer decides surfacing — no secret enters an AppError case (they are message/reason strings) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | `.fileOperationFailed(reason)` / error reasons | high | mitigate | Reason strings placed in AppError carry no credential/cookie/full-URL-with-query; file-op reasons are operation descriptors, not user home paths. Reviewed per converted site. ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `-only-testing:FileClientTests` and `-only-testing:NetworkingFeatureTests` green.
- Every surviving try? in these files carries a just-cause comment.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
All 17 FileClient + NetworkingFeature try? sites are converted-and-propagating or commented survivors, at behavior parity, with both module test targets green.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-05-SUMMARY.md` when done.
</output>
