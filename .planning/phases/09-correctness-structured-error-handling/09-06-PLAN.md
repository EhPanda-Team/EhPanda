---
phase: 09-correctness-structured-error-handling
plan: 06
type: execute
wave: 6
depends_on: [09-05]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadStore.swift
  - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "Every DownloadStore / DownloadStore+Operations try? site is converted to a do throws(AppError) that propagates, or kept as a documented decode-with-default/best-effort survivor with an inline just-cause comment."
    - "Download manifest read/write and page persistence behave identically — DownloadsFeatureTests stay green."
  artifacts:
    - AppPackage/Sources/DownloadClient/DownloadStore.swift
  key_links:
    - "DownloadStore errors -> DownloadFailure / download reducer (nearest surface, D-12)"
  prohibitions:
    - statement: "No converted site swallows an error without a commented, justified fallback."
      status: enforced
      verification: "Read each converted catch; propagates AppError or applies a commented fallback."
    - statement: "Every surviving try? carries an adjacent inline just-cause comment (D-02)."
      status: enforced
      verification: "Each remaining `try?` match has an adjacent `//` just-cause comment."
    - statement: "No new // swiftlint:disable is introduced."
      status: enforced
      verification: "grep in DownloadClient shows no new swiftlint:disable from this plan."
---

<objective>
Sweep the densest DownloadClient conversion file, `DownloadStore.swift` (14 try? sites) plus `DownloadStore+Operations.swift` (2), per QUAL-04 D-01/D-02/D-12. Convert genuine manifest/page persistence failures to `do throws(AppError)` (Pattern 1) that propagate to the download reducer / map to the existing `DownloadFailure` model; keep only true decode-with-default/best-effort survivors as commented `try?`. All at behavior parity.

Purpose: the DownloadClient holds the most swallowed failures — make them propagate so download errors surface where warranted (D-12).
Output: converted/annotated DownloadStore source; DownloadsFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): DownloadStore try? sites converted or annotated per the RESEARCH §2 buckets + PATTERNS Pattern 1.

<tasks>

<task type="auto">
  <name>Task 1: Classify + convert DownloadStore.swift try? sites (14)</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadStore.swift (all 14 try? sites)
    - AppPackage/Sources/AppModels/Download/DownloadFailure.swift (the existing failure model + its AppError mapping switches — the natural surface for download errors)
    - AppPackage/Sources/AppModels/Support/AppError.swift (fileOperationFailed/parseFailed cases)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (buckets: DownloadStore is bucket-a/b mixed) + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml
  </read_first>
  <action>
    Classify each of the 14 DownloadStore.swift try? sites. Convert bucket-(a) sites — manifest read/decode, page image read/write, folder operations whose failure is a real surfaceable error — to `do throws(AppError) { ... } catch { ... }` (Pattern 1), mapping to `.fileOperationFailed`/`.parseFailed` and propagating (or feeding the existing DownloadFailure surface), never swallowing. Keep genuine decode-with-default or fire-and-forget best-effort sites as `try?` + inline just-cause comment (D-02). Preserve manifest schema/version behavior and the download state machine exactly (STATE.md download-scheduling determinism); the DownloadsFeatureTests must stay green (DownloadSchedulingTests is deterministic — a failure there is a real regression).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:DownloadsFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:DownloadsFeatureTests` exits 0.
    - Every remaining `try?` in DownloadStore.swift has an adjacent inline just-cause comment.
    - Converted sites use `do throws(AppError)`; no swallowing catch.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>DownloadStore.swift failures propagate as AppError or are documented survivors; download tests green.</done>
</task>

<task type="auto">
  <name>Task 2: DownloadStore+Operations.swift try? sweep (2)</name>
  <files>AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadStore+Operations.swift (both try? sites)
    - AppPackage/Sources/AppModels/Support/AppError.swift
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml
  </read_first>
  <action>
    Classify + convert the 2 try? sites in DownloadStore+Operations.swift the same way: bucket-(a) -> `do throws(AppError)` propagating; genuine best-effort -> `try?` + inline just-cause comment (D-02). Preserve the operation's success behavior.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:DownloadsFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:DownloadsFeatureTests` exits 0.
    - Both try? sites are converted or carry an inline just-cause comment.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>DownloadStore+Operations try? sites are resolved at parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| download failure -> AppError/DownloadFailure -> reducer | Download errors propagate; reasons carry no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | download error reasons / paths | high | mitigate | Error reasons are operation descriptors; no user-home absolute path, cookie, or token is embedded in a surfaced AppError. Reviewed per converted site. ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `-only-testing:DownloadsFeatureTests` green.
- Every surviving try? in the two files carries a just-cause comment.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
All 16 DownloadStore(+Operations) try? sites are converted-and-propagating or commented survivors, at behavior parity, with DownloadsFeatureTests green.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-06-SUMMARY.md` when done.
</output>
