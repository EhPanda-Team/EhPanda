---
phase: 09-correctness-structured-error-handling
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - AppPackage/Sources/AppModels/Gallery/Category.swift
  - AppPackage/Tests/AppModelsTests/CategoryFilterValueTests.swift
autonomous: true
requirements: [QUAL-03]
user_setup: []

must_haves:
  truths:
    - "Category.private.filterValue returns 0 in every build configuration and never calls fatalError."
    - "Invoking Category.private.filterValue in a Debug/test run reports a dev-time issue via IssueReporting.reportIssue (non-fatal), not fatalError or assertionFailure."
    - "A doc comment on the .private branch explains that .private is a display-only category contributing no filter bit."
    - "No all-categories filter iteration can trap: URLUtil.categoryValue only sums the explicit non-private cases, and Category.allFiltersCases excludes .private."
  artifacts:
    - AppPackage/Sources/AppModels/Gallery/Category.swift
    - AppPackage/Tests/AppModelsTests/CategoryFilterValueTests.swift
  key_links:
    - "Category.filterValue -> URLUtil.categoryValue (consumer; explicit non-private cases only)"
    - "Category.private.filterValue -> IssueReporting.reportIssue (dev-time signal) + withExpectedIssue (test proof)"
  prohibitions:
    - statement: "fatalError / assertionFailure / precondition is not used for the .private filterValue path."
      status: enforced
      verification: "grep -n 'fatalError\\|assertionFailure\\|precondition' Category.swift returns nothing in the filterValue extension."
    - statement: "The reportIssue message carries no secret (it is a static developer diagnostic about misuse)."
      status: enforced
      verification: "Read the reportIssue string literal in Category.swift — it names only the API misuse, no runtime/credential value."
---

<objective>
Kill the `Category.private.filterValue` `fatalError` landmine (QUAL-03, D-13). `.private` is a display-only category and contributes no search-filter bit, so `filterValue` returns `0` in production with a doc comment explaining why; the `fatalError` is replaced by a dev-time `IssueReporting.reportIssue(_:)` — a non-fatal purple runtime warning in Debug, an inert fault log in Release — and covered by a `withExpectedIssue { }` test. Confirm no all-categories iteration can trap.

Purpose: remove a release crash landmine and satisfy QUAL-03's "no crash for .private, no all-categories iteration traps, covered by a test."
Output: `Category.swift` edit + a new `AppModelsTests` case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan creates/changes:
- `Category.private.filterValue` returns 0 + `reportIssue` (replaces `fatalError`).
- New test case `CategoryFilterValueTests` (`withExpectedIssue { #expect(Category.private.filterValue == 0) }`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the .private filterValue fatalError with reportIssue + return 0</name>
  <files>AppPackage/Sources/AppModels/Gallery/Category.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Gallery/Category.swift (the fatalError at the .private branch, lines 42-46; the `value` switch already handles .private gracefully — proof it is a real reachable display-only case)
    - AppPackage/Sources/AppModels/Utilities/URLUtil.swift lines 199-211 (categoryValue sums explicit non-private cases; confirm no `.private` reference)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §1 (reportIssue API, copy-ready template) and §"Common Pitfalls" Pitfall 4
    - .claude/skills for pfw-issue-reporting (reportIssue guidance)
    - .swiftlint.yml (conform from the start; no swiftlint:disable)
  </read_first>
  <behavior>
    - Category.private.filterValue == 0 in all configurations; no crash.
    - In a test/Debug context the call emits exactly one reportIssue.
    - Non-private cases return their existing bit values unchanged.
  </behavior>
  <action>
    In `Category.swift`, `import IssueReporting`. Replace the `.private` branch of `filterValue` (currently building `message`, logging `.error`, and calling `fatalError(message)`) with `reportIssue("`Category.private` has no `filterValue` — it is display-only and must be excluded from filter math.")` followed by `return 0` (D-13; template in RESEARCH §1). Add a doc comment on the branch stating `.private` is a display-only category (a user's private-gallery bucket), not a search-filter category, so it contributes no filter bit; reaching here is a programming error reported in dev and inert in release. Do NOT override `IssueReporters.current` (that would re-introduce a crash — RESEARCH Anti-Patterns). Remove the now-unused `message`/`logger.error` landmine lines for that branch; leave the file's `logger` declaration if still referenced elsewhere, otherwise remove it if it becomes unused (avoid an unused-warning).
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fatalError\|assertionFailure\|precondition' AppPackage/Sources/AppModels/Gallery/Category.swift` returns nothing.
    - `grep -n 'reportIssue' Category.swift` shows the new call; `grep -n 'return 0' Category.swift` shows the .private path returns 0.
    - The module builds with no unused-symbol warning for the removed landmine.
    - No `// swiftlint:disable` introduced.
  </acceptance_criteria>
  <done>.private.filterValue returns 0 with a dev-time report and doc comment; the fatalError is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: withExpectedIssue test + all-categories-iteration audit</name>
  <files>AppPackage/Tests/AppModelsTests/CategoryFilterValueTests.swift</files>
  <read_first>
    - AppPackage/Tests/AppModelsTests/StrictDecodingTests.swift (Swift Testing style in this target)
    - AppPackage/Sources/AppModels/Gallery/Category.swift (allFiltersCases = allCases.dropLast() — drops .private)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §1 (withExpectedIssue test, copy-ready) and Pitfall 4 (audit existing all-categories tests)
    - .claude/skills for swift-testing-pro (withKnownIssue / withExpectedIssue)
  </read_first>
  <behavior>
    - `withExpectedIssue { #expect(Category.private.filterValue == 0) }` passes: it both asserts the return value and proves the dev-time issue fired (fails with "Known issue was not recorded" if it did not).
    - Summing filterValue over Category.allFiltersCases (which excludes .private) never traps.
  </behavior>
  <action>
    Create `AppPackage/Tests/AppModelsTests/CategoryFilterValueTests.swift` (Swift Testing): `import Testing`, `import IssueReporting`, `@testable import AppModels`. Add `@Test func privateFilterValueReportsIssueAndReturnsZero()` wrapping `#expect(Category.private.filterValue == 0)` in `withExpectedIssue { }` (RESEARCH §1). Add a second test that reduces `Category.allFiltersCases.map(\.filterValue)` and asserts the sum matches the current all-filters total (proving no trap and no `.private` inclusion). Audit the existing AppModelsTests for any test that iterates `Category.allCases` and touches `.private.filterValue`; if one exists, wrap that call in `withExpectedIssue` too (Pitfall 4) — otherwise note none found in the summary.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:AppModelsTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:AppModelsTests` exits 0 with `CategoryFilterValueTests` present.
    - The test uses `withExpectedIssue` (grep confirms), not `withKnownIssue`-only-without-expect nor an unwrapped call.
    - The all-filters sum test passes and references `Category.allFiltersCases`.
  </acceptance_criteria>
  <done>QUAL-03 SC-1 test is green and the no-trap iteration is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time diagnostic -> logs/reporter | The reportIssue message surfaces to the developer/test reporter; must carry no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CAT | Information Disclosure | `reportIssue` message | low | accept | The message is a static developer diagnostic naming only the API misuse (no runtime, credential, or user value). No secret is interpolated. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed — IssueReporting already resolved (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `xcodebuild test -scheme AppPackage-Package -only-testing:AppModelsTests` green.
- No `fatalError`/`assertionFailure` in Category.swift filterValue.
- No `// swiftlint:disable` introduced.
</verification>

<success_criteria>
Category.private.filterValue returns 0 (release-safe), fires a dev-time issue, cannot trap in all-categories iteration, and is covered by a passing withExpectedIssue test (QUAL-03).
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-02-SUMMARY.md` when done.
</output>
