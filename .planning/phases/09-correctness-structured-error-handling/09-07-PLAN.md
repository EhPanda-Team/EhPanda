---
phase: 09-correctness-structured-error-handling
plan: 07
type: execute
wave: 7
depends_on: [09-06]
files_modified:
  - AppPackage/Sources/DownloadClient/DownloadClient.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Networking.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidationHelpers.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+Cache.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+BackgroundDownloads.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "Every remaining DownloadClient try? site (across the 10 files, ~20 sites) is converted to a do throws(AppError) that propagates, or kept as a documented survivor with an inline just-cause comment."
    - "Download networking, response validation, page download, background download, and public API behavior are identical — DownloadsFeatureTests stay green."
  artifacts:
    - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift
  key_links:
    - "validation/networking failures -> DownloadFailure / AppError -> download reducer (D-12)"
  prohibitions:
    - statement: "No converted site swallows an error without a commented, justified fallback."
      status: enforced
      verification: "Read each converted catch; propagates or commented fallback."
    - statement: "Every surviving try? carries an adjacent inline just-cause comment (D-02)."
      status: enforced
      verification: "Each remaining `try?` match has an adjacent `//` just-cause comment."
    - statement: "No new // swiftlint:disable is introduced."
      status: enforced
      verification: "grep in DownloadClient shows no new swiftlint:disable from this plan."
---

<objective>
Finish the DownloadClient `try?` sweep — the remaining ~20 sites across 10 files (Networking, ResponseValidation(+Helpers), ExecutionSupport, PersistenceNormalize, Cache, PageDownload, BackgroundDownloads, PublicAPI, DownloadClient.swift) — per QUAL-04 D-01/D-02/D-12. Convert bucket-(a) validation/networking failures to `do throws(AppError)` propagating (or feeding the existing `DownloadFailure` surface), keep true best-effort/decode-with-default survivors commented, at behavior parity.

Purpose: complete DownloadClient so it holds no undocumented swallowed failure.
Output: converted/annotated DownloadClient source; DownloadsFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): the remaining DownloadClient try? sites, per RESEARCH §2 buckets + PATTERNS Pattern 1.

<tasks>

<task type="auto">
  <name>Task 1: Validation + networking files sweep</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift, AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidationHelpers.swift, AppPackage/Sources/DownloadClient/DownloadClient+Networking.swift, AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadClient+ResponseValidation.swift (4) + DownloadClient+ResponseValidationHelpers.swift (3, incl. the `guard case .fileOperationFailed(let reason) = error` at line 349 — matches an AppError, do not break it) + DownloadClient+Networking.swift (3)
    - AppPackage/Sources/DownloadClient/DownloadClient+PageDownload.swift (1, incl. the switch over AppError cases at 314-316)
    - AppPackage/Sources/AppModels/Download/DownloadFailure.swift + AppModels/Support/AppError.swift
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml
  </read_first>
  <action>
    Classify + convert the try? sites in ResponseValidation, ResponseValidationHelpers, Networking, and PageDownload. Bucket-(a) validation/networking failures -> `do throws(AppError)` propagating to the existing DownloadFailure/AppError surface (Pattern 1); genuine best-effort/decode-with-default -> `try?` + inline just-cause comment (D-02). Leave the existing AppError pattern-matching (`guard case .fileOperationFailed = error`, the PageDownload switch) intact — the AppError enum is unchanged (09-01 kept it byte-stable), so these matches keep working. Preserve validation outcomes exactly.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:DownloadsFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:DownloadsFeatureTests` exits 0.
    - Every remaining `try?` in these four files has an adjacent inline just-cause comment.
    - The existing AppError `case`/`switch` matches still compile and behave identically.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>Validation/networking/page-download failures propagate or are documented survivors; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining DownloadClient files sweep (ExecutionSupport, PersistenceNormalize, Cache, BackgroundDownloads, PublicAPI, DownloadClient.swift)</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+PersistenceNormalize.swift, AppPackage/Sources/DownloadClient/DownloadClient+Cache.swift, AppPackage/Sources/DownloadClient/DownloadClient+BackgroundDownloads.swift, AppPackage/Sources/DownloadClient/DownloadClient+PublicAPI.swift, AppPackage/Sources/DownloadClient/DownloadClient.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift (3), +PersistenceNormalize.swift (1), +Cache.swift (1), +BackgroundDownloads.swift (1), +PublicAPI.swift (1), DownloadClient.swift (2)
    - AppPackage/Sources/AppModels/Support/AppError.swift
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 (bucket-c fire-and-forget cache/cleanup guidance: convert-and-log vs commented survivor) + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml (Logger placement rule — declare `private let logger` at file top if logging in a catch)
  </read_first>
  <action>
    Classify + resolve the remaining ~9 try? sites. For bucket-(a) execution/persistence failures, convert to `do throws(AppError)` propagating (Pattern 1). For bucket-(c) fire-and-forget cache/cleanup (Cache, some ExecutionSupport), choose per site: convert to `do/catch` that logs via the module `logger` (declare `private let logger` at the file top if introducing logging — Logger placement rule), or keep `try?` + inline just-cause comment (D-02) — the owner reviews each survivor (D-03). Preserve public API signatures and behavior.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:DownloadsFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:DownloadsFeatureTests` exits 0.
    - Every remaining `try?` across these files has an adjacent inline just-cause comment.
    - Any new catch-logging uses a top-of-file `private let logger` (no inline Logger construction).
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>The rest of DownloadClient holds no undocumented swallowed failure; DownloadsFeatureTests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| download validation/networking failure -> AppError -> reducer/log | Failures propagate/log; no secret embedded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | download error reasons + any catch logging | high | mitigate | Error reasons/log messages carry no cookie/token/full-URL-with-query; logging follows the Phase-8 privacy discipline (no credential value at .public). ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `-only-testing:DownloadsFeatureTests` green.
- Every surviving try? in DownloadClient carries a just-cause comment.
- No new `// swiftlint:disable`; catch-logging uses a top-of-file logger.
</verification>

<success_criteria>
All remaining DownloadClient try? sites are converted-and-propagating or commented survivors, at behavior parity, with DownloadsFeatureTests green.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-07-SUMMARY.md` when done.
</output>
