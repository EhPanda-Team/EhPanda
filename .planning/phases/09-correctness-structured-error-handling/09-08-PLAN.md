---
phase: 09-correctness-structured-error-handling
plan: 08
type: execute
wave: 8
depends_on: [09-07]
files_modified:
  - AppPackage/Sources/AppTools/DataCache.swift
  - AppPackage/Sources/AppTools/Extensions.swift
  - AppPackage/Sources/AppTools/Defaults.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "Every AppTools try? site (DataCache 13, Extensions 3, Defaults 1) is either converted to do/catch (propagate or log) or kept as a documented best-effort survivor with an inline just-cause comment."
    - "DataCache housekeeping stays fire-and-forget (no behavior change to cache hits/misses); cache-backed tests stay green."
  artifacts:
    - AppPackage/Sources/AppTools/DataCache.swift
  key_links:
    - "DataCache fire-and-forget cleanup -> module logger (if converted) — never surfaced to the user"
  prohibitions:
    - statement: "DataCache cleanup failures are not surfaced to the user (they are genuine housekeeping); they are logged or documented-survivor only."
      status: enforced
      verification: "No DataCache try? conversion routes to a toast/ErrorInfo; catch either logs or the site stays a commented survivor."
    - statement: "Every surviving try? carries an adjacent inline just-cause comment (D-02)."
      status: enforced
      verification: "Each remaining `try?` in AppTools has an adjacent `//` just-cause comment."
    - statement: "No new // swiftlint:disable is introduced."
      status: enforced
      verification: "grep in AppTools shows no new swiftlint:disable from this plan."
---

<objective>
Sweep AppTools (17 sites: DataCache.swift 13, Extensions.swift 3, Defaults.swift 1) per QUAL-04 D-01/D-02/D-12. The DataCache cluster is bucket-(c) fire-and-forget filesystem housekeeping (removeItem / touchAccessDate / setResourceValues) — resolve each per D-02: convert to `do/catch` that logs via the module logger, or keep `try?` + inline just-cause comment (owner reviews, D-03). Extensions/Defaults sites classified and resolved the same way. All at behavior parity.

Purpose: remove undocumented swallowing from the cache/util layer while keeping housekeeping genuinely best-effort.
Output: converted/annotated AppTools source; cache-backed tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan changes (no new symbols): AppTools try? sites resolved per RESEARCH §2 bucket-(c) guidance.

<tasks>

<task type="auto">
  <name>Task 1: DataCache.swift fire-and-forget cleanup sweep (13)</name>
  <files>AppPackage/Sources/AppTools/DataCache.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/DataCache.swift (all 13 try? sites: removeItem / touchAccessDate / setResourceValues housekeeping — the injected cache actor since Phase 8)
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 bucket-(c) (fire-and-forget filesystem cleanup: convert-and-log OR commented survivor) + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml + Logger placement rule (declare `private let logger` at file top if adding logging)
    - STATE.md Phase-8 notes (DataCache is one canonical actor; UUID-scoped per-test instances)
  </read_first>
  <action>
    Resolve each of the 13 DataCache try? sites per D-02. These are cache housekeeping where failure is genuinely ignorable — do NOT surface any of them to the user (prohibition). Per site, either: convert to `do { ... } catch { logger.error(...) }` logging the housekeeping failure via a top-of-file `private let logger` (Logger placement rule), or keep `try?` + an inline just-cause comment stating it is deliberate fire-and-forget cache housekeeping (D-02 bucket-c). Keep the cache behavior identical — a cleanup failure must remain non-fatal and must not change cache hit/miss results. Do not alter the actor's public surface.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:ImageClientTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:ImageClientTests` exits 0 (DataCache-backed behavior parity).
    - Every remaining `try?` in DataCache.swift has an adjacent inline just-cause comment.
    - Any added logging uses a top-of-file `private let logger`; no DataCache failure routes to a toast/ErrorInfo.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>DataCache housekeeping is documented/logged fire-and-forget at parity; cache tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Extensions.swift + Defaults.swift sweep (4)</name>
  <files>AppPackage/Sources/AppTools/Extensions.swift, AppPackage/Sources/AppTools/Defaults.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/Extensions.swift (3 try? sites) + AppPackage/Sources/AppTools/Defaults.swift (1)
    - AppPackage/Sources/AppModels/Support/AppError.swift
    - .planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md §2 + 09-PATTERNS.md Pattern 1
    - .swiftlint.yml
  </read_first>
  <action>
    Classify each of the 4 Extensions/Defaults try? sites. Convert bucket-(a) (a real surfaceable decode/operation failure) to `do throws(AppError)` propagating (Pattern 1); keep genuine best-effort/decode-with-default as `try?` + inline just-cause comment (D-02). Preserve behavior; if a utility currently returns a default on failure, keeping the documented `try?` is correct.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The module builds.
    - Every remaining `try?` in Extensions.swift/Defaults.swift has an adjacent inline just-cause comment.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>Extensions/Defaults try? sites are resolved at parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache/util failure -> log (never user) | Housekeeping failures stay internal; logs carry no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | DataCache cleanup logging | high | mitigate | Any housekeeping log carries file-operation descriptors only, no credential/cookie/full-URL; follows the Phase-8 no-.public-secret discipline. Cache failures are never surfaced to the user. ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `-only-testing:ImageClientTests` green (DataCache parity); AppTools builds.
- Every surviving try? in AppTools carries a just-cause comment.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
All 17 AppTools try? sites are converted (propagate/log) or commented survivors, cache housekeeping stays fire-and-forget at parity, and cache-backed tests are green.
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-08-SUMMARY.md` when done.
</output>
