---
phase: 09-correctness-structured-error-handling
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - AppPackage/Sources/AppFeature/DataFlow/PresentationReducer.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Tests/AppFeatureTests/PresentationReducerTests.swift
autonomous: true
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "AppRouteReducer is renamed to PresentationReducer across exactly its 3 source files, and the app builds and behaves identically."
    - "PresentationReducer.Destination has an @ReducerCaseIgnored .errorInfo(ErrorInfo) case and a presentErrorInfo(ErrorInfo) action that sets destination = .errorInfo(errorInfo)."
    - "The gallery-fetch failure toast is now an ErrorInfo-bearing tappable error toast that keeps the 3s auto-hide; tapping it presents ErrorInfoView."
    - "TabBarView presents ErrorInfoView via .sheet(item:) from destination.errorInfo and wires .toast(_:onErrorTap:) to send presentErrorInfo."
  artifacts:
    - AppPackage/Sources/AppFeature/DataFlow/PresentationReducer.swift
    - AppPackage/Tests/AppFeatureTests/PresentationReducerTests.swift
  key_links:
    - "onErrorTap closure (TabBarView) -> store.send(.appRoute(.presentErrorInfo(errorInfo))) -> destination = .errorInfo -> .sheet(item:) ErrorInfoView"
    - "fetchGalleryDone failure -> .setToast(.error(ErrorInfo(...))) (ErrorInfo-bearing tappable toast)"
  prohibitions:
    - statement: "The rename touches only the 3 known files (PresentationReducer.swift, AppReducer.swift, TabBarView.swift); no other source references AppRouteReducer/appRouteState/appRoute."
      status: enforced
      verification: "grep -rn 'AppRouteReducer' AppPackage/Sources App ShareExtension returns nothing after the rename."
    - statement: "The failure-toast ErrorInfo carries no secret — its context is assembled from safe whitelisted keys at the reducer (the nearest surface, D-12)."
      status: enforced
      verification: "Read the fetchGalleryDone failure branch — any Context built uses only ContextKey whitelist members; no cookie/token/full-URL-with-query."
---

<objective>
Rename `AppRouteReducer` -> `PresentationReducer` (D-10; blast radius exactly 3 files per RESEARCH §5), add the `@ReducerCaseIgnored .errorInfo(ErrorInfo)` destination + `presentErrorInfo` action (D-10), turn the gallery-fetch failure toast into an `ErrorInfo`-bearing tappable toast that keeps its 3s auto-hide (D-11), and wire `TabBarView` to present `ErrorInfoView` and route `onErrorTap` (D-11/D-12). Cover the route with a TestStore test (QUAL-04 SC-3).

Purpose: connect the failure surface (09-03) to the app-root presentation reducer so a tapped failure toast opens ErrorInfoView.
Output: renamed reducer file + `.errorInfo` case/action + AppReducer/TabBarView edits + a new AppFeatureTests route test (existing AppRouteReducerTests renamed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md
@.planning/phases/09-correctness-structured-error-handling/09-RESEARCH.md
@.planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md
@.planning/phases/09-correctness-structured-error-handling/09-VALIDATION.md
</context>

## Artifacts this phase produces

This plan creates/changes:
- `PresentationReducer` (renamed from `AppRouteReducer`); optional companion rename of `appRouteState`/`appRoute` -> `presentationState`/`presentation` (A2, recommended — stays within the 3 files).
- `PresentationReducer.Destination.errorInfo(ErrorInfo)` case + `presentErrorInfo(ErrorInfo)` action.
- ErrorInfo-bearing failure toast at `fetchGalleryDone`.
- TabBarView `.sheet(item:)` ErrorInfoView + `.toast(_:onErrorTap:)` wiring.
- New test `PresentationReducerTests` (route test + the migrated clipboard tests).

<tasks>

<task type="auto">
  <name>Task 1: Rename AppRouteReducer -> PresentationReducer + add .errorInfo destination/action + ErrorInfo-bearing failure toast</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/PresentationReducer.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift (the whole file: struct at line 15, Destination enum 16-22, presentSetting/presentNewDawn at 106-111, fetchGalleryDone failure at 197-208, extensions at 229-230)
    - AppPackage/Sources/AppModels/Support/AppError+Context.swift (ErrorInfo/Context/ContextKey from 09-01)
    - AppPackage/Sources/AppComponents/AppAlertState.swift (the .error(_ errorInfo:) factory from 09-03)
    - .planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md "PresentationReducer.swift" section (exact idiom excerpts) and RESEARCH §5 (rename blast radius)
    - .claude/skills for pfw-composable-architecture (@Presents/@ReducerCaseIgnored, Delegate sibling-not-nested)
    - .swiftlint.yml (child_reducer_shorthand_scope: use Scope(...child: Reducer.init); no swiftlint:disable)
  </read_first>
  <action>
    Rename `struct AppRouteReducer` -> `struct PresentationReducer` and the two trailing `extension AppRouteReducer.Destination.State/Action` lines; rename the file to `PresentationReducer.swift`. Add `@ReducerCaseIgnored case errorInfo(ErrorInfo)` as a sibling of `.setting`/`.newDawn` in the Destination enum (D-10). Add `case presentErrorInfo(ErrorInfo)` to the Action enum and a reducer branch `state.destination = .errorInfo(errorInfo); return .none` mirroring `presentSetting`/`presentNewDawn`. In the `fetchGalleryDone` failure branch (currently `.setToast(.error())` after a 500ms sleep), build a `Context` from the AppError + the failing URL's PATH ONLY (`ContextKey.url` = url.path, strip query — Security Domain) plus `.action`/`.reason`, and send `.setToast(.error(ErrorInfo(error: error, context: context)))` (D-11/D-12) — keep the 500ms delay + 3s auto-hide. The failure `result` already binds the `AppError` (the branch currently discards it — capture it). Do NOT change the success branch. Keep `Scope(...child:)` shorthand and the Destination sibling structure.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The type is `PresentationReducer`; `grep -rn 'AppRouteReducer' AppPackage/Sources/AppFeature/DataFlow/PresentationReducer.swift` returns nothing.
    - Destination declares `@ReducerCaseIgnored case errorInfo(ErrorInfo)`; a `presentErrorInfo` action sets `.errorInfo`.
    - The fetchGalleryDone failure branch sends `.error(ErrorInfo(...))` carrying the bound AppError; the context uses `url.path` (not the full URL) and only whitelisted ContextKeys.
    - No new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>The presentation reducer owns the .errorInfo route and emits an ErrorInfo-bearing failure toast, with the rename applied in its own file.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate the rename into AppReducer + TabBarView; present ErrorInfoView + wire onErrorTap</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (appRouteState at 24, appRoute action at 45, Scope at 315, and the ~10 references at 67-361 listed in RESEARCH §5)
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (the .sheet(item:) idioms at 69-94, the .toast scope at 95, .onOpenURL at 97, and the appRouteState refs at 32/80/82)
    - AppPackage/Sources/AppComponents/ErrorInfoView.swift (ErrorInfoView(errorInfo:) from 09-03)
    - .planning/phases/09-correctness-structured-error-handling/09-PATTERNS.md "AppReducer.swift" + "TabBarView.swift" sections
  </read_first>
  <action>
    In `AppReducer.swift`, update every `AppRouteReducer.State/Action`/`AppRouteReducer.init` reference to `PresentationReducer` (line 24 var, 45 action case, 315 Scope, plus each `.appRoute(...)`/`state.appRouteState...` at 67-361). Optionally rename `appRouteState`/`appRoute` -> `presentationState`/`presentation` for consistency (A2, recommended) — apply consistently across all 3 files if chosen. In `TabBarView.swift`, update the same references, add `.sheet(item: $store.<presentation>State.destination.errorInfo) { errorInfo in ErrorInfoView(errorInfo: errorInfo.wrappedValue) }` mirroring the existing `.setting`/`.newDawn` sheets, and change `.toast($store.scope(\.<presentation>State.$toast, action: \.<presentation>.toast))` to also pass `onErrorTap: { errorInfo in store.send(.<presentation>(.presentErrorInfo(errorInfo))) }` (D-11). Keep the existing privacyMask/accentColor modifiers on the new sheet as the sibling sheets do.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'generic/platform=iOS' 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'AppRouteReducer\|appRouteState\|appRoute\b' AppPackage/Sources App ShareExtension` returns nothing (rename complete across the 3 files, no stragglers elsewhere).
    - TabBarView presents `ErrorInfoView` via `.sheet(item:)` from `destination.errorInfo` and passes `onErrorTap:` to `.toast(...)` routing to `presentErrorInfo`.
    - The app builds; no new `// swiftlint:disable`.
  </acceptance_criteria>
  <done>The rename is fully propagated and the failure toast tap opens ErrorInfoView through the presentation reducer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route test (presentErrorInfo -> .errorInfo) + migrate existing AppRouteReducerTests</name>
  <files>AppPackage/Tests/AppFeatureTests/PresentationReducerTests.swift</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests/AppRouteReducerTests.swift (the existing TestStore tests + the `AppRouteReducer.init`/`.State()` usage to rename)
    - .planning/phases/09-correctness-structured-error-handling/09-VALIDATION.md (QUAL-04 SC-3 route test row)
    - .claude/skills for pfw-composable-architecture (TestStore) + swift-testing-pro
  </read_first>
  <behavior>
    - Sending presentErrorInfo(errorInfo) sets destination == .errorInfo(errorInfo).
    - The existing clipboard-detection tests still pass under the renamed type.
  </behavior>
  <action>
    Rename `AppRouteReducerTests.swift` -> `PresentationReducerTests.swift` and update `AppRouteReducer.State()/init` -> `PresentationReducer.State()/init` (and `appRouteState`/`appRoute` if renamed). Add `@Test func presentErrorInfoRoutesToErrorInfoDestination()` that builds a `TestStore(initialState: PresentationReducer.State(), reducer: PresentationReducer.init)`, sends `.presentErrorInfo(ErrorInfo(error: .parseFailed, context: [.action: "test"]))`, and asserts `state.destination = .errorInfo(...)` (ErrorInfo == is id-excluded so the expected state matches). Keep the two migrated clipboard tests intact.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -only-testing:AppFeatureTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:AppFeatureTests` exits 0 with `PresentationReducerTests` present; the old `AppRouteReducerTests.swift` no longer exists.
    - The route test asserts `destination == .errorInfo(...)` after `presentErrorInfo`.
    - The migrated clipboard tests still pass.
  </acceptance_criteria>
  <done>QUAL-04 SC-3 route resolution is proven by a green reducer test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer error -> ErrorInfo.context -> toast/ErrorInfoView | The reducer (nearest surface, D-12) assembles context that reaches the user; must carry no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-CTX | Information Disclosure | `fetchGalleryDone` context assembly + `.errorInfo` route | high | mitigate | Context is built from `url.path` (query stripped), action, and reason only — the whitelisted ContextKeys. No cookie/token/full-URL/IP is placed in the ErrorInfo. Enforced by the context-review acceptance criterion (Task 1). ASVS V7. |
| T-9-SC | Tampering | package installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `xcodebuild test -scheme AppPackage-Package -only-testing:AppFeatureTests` green.
- `grep -rn 'AppRouteReducer' AppPackage/Sources App ShareExtension` returns nothing.
- fetchGalleryDone context uses url.path only.
- No new `// swiftlint:disable`.
</verification>

<success_criteria>
The renamed PresentationReducer routes a tappable ErrorInfo failure toast to ErrorInfoView (3s auto-hide preserved), the rename is confined to 3 files, and the route is proven by a TestStore test (QUAL-04 SC-3).
</success_criteria>

<output>
Create `.planning/phases/09-correctness-structured-error-handling/09-04-SUMMARY.md` when done.
</output>
