---
phase: 05-adaptive-layout-universal-orientation
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - AppPackage/Sources/AppComponents/AlertView.swift
  - AppPackage/Sources/AppComponents/Placeholder.swift
  - AppPackage/Sources/AppComponents/CategoryView.swift
  - AppPackage/Sources/AppComponents/NewDawnView.swift
autonomous: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "No AppComponents view sizes itself from `DeviceUtil.windowW`/`absWindowW` or an `isPadWidth`/`isSEWidth` breakpoint — widths are container-relative and breakpoints are size-class / adaptive-grid decisions."
    - "The `AlertView` width conversion does not move any `.alert`/`.confirmationDialog` anchor off its stable, action-source control."
  artifacts: []
  key_links:
    - "`NewDawnView`'s device-class factor (`isPad`) reads `deviceClient.deviceType()`; its window-fraction offsets go container-relative."
---

<objective>
Convert the four AppComponents metric sites off `DeviceUtil` (D-02): `AlertView` and `Placeholder`
window-fraction widths → `containerRelativeFrame`; `CategoryView` `isPadWidth` grid-min → size-class;
`NewDawnView` window-fraction offsets → container-relative and its `isPad` factor →
`deviceClient.deviceType()`.

Purpose: Retire these `DeviceUtil` consumers ahead of the final deletion (05-10), replacing
screen-metric math with native adaptive sizing at appearance parity.
Output: 4 AppComponents files free of `DeviceUtil`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AlertView + Placeholder window-fraction widths → container-relative</name>
  <files>AppPackage/Sources/AppComponents/AlertView.swift, AppPackage/Sources/AppComponents/Placeholder.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/AlertView.swift line 118 (`.frame(maxWidth: DeviceUtil.windowW * 0.8)`) and the surrounding view — identify where any `.alert`/`.confirmationDialog` modifier is anchored so the conversion does not move it.
    - AppPackage/Sources/AppComponents/Placeholder.swift line 36 (`.frame(width: DeviceUtil.absWindowW * (isDualPage ? 0.25 : 0.5))`).
    - 05-RESEARCH.md § Standard Stack (`containerRelativeFrame`) and § Common Pitfalls #3 (placeholder sizing is benign appearance delta).
    - AGENTS.md § "Confirmation dialog / alert placement" (do not move a dialog/alert anchor off its stable action-source control).
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro.
  </read_first>
  <action>
    In `AlertView.swift` replace `.frame(maxWidth: DeviceUtil.windowW * 0.8)` with
    `.containerRelativeFrame(.horizontal) { width, _ in width * 0.8 }` applied as `maxWidth`, or an
    `onGeometryChange`-captured container width if the alert card is not inside a recognized relative
    container. Do NOT relocate any existing `.alert`/`.confirmationDialog` modifier — attach the
    width conversion to the same view element only. In `Placeholder.swift` replace
    `DeviceUtil.absWindowW * (isDualPage ? 0.25 : 0.5)` with a container-relative fraction:
    `.containerRelativeFrame(.horizontal) { width, _ in width * (isDualPage ? 0.25 : 0.5) }`. Note
    (Pitfall #3): this placeholder frame sizes only the placeholder/error tile, not the loaded image
    (`scaledToFit`), so the current-dimension container width is a benign parity delta.
  </action>
  <verify>
    <automated>! grep -n "DeviceUtil" AppPackage/Sources/AppComponents/AlertView.swift AppPackage/Sources/AppComponents/Placeholder.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Neither file references `DeviceUtil`.
    - The `AlertView` width uses a container-relative fraction and no `.alert`/`.confirmationDialog` anchor was moved.
    - `Placeholder` width uses a container-relative fraction preserving the `0.25`/`0.5` dual-page factors.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>AlertView + Placeholder widths are container-relative; no dialog anchor moved; no `DeviceUtil` remains.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryView grid breakpoint → size-class; NewDawnView offsets + idiom → native</name>
  <files>AppPackage/Sources/AppComponents/CategoryView.swift, AppPackage/Sources/AppComponents/NewDawnView.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/CategoryView.swift line 40 (`GridItem(.adaptive(minimum: DeviceUtil.isPadWidth ? 100 : 80, maximum: 100))`) — note `gridItems` is a stored `let`, so a size-class read must move into `body`/a computed property.
    - AppPackage/Sources/AppComponents/NewDawnView.swift lines 11 (`offset = DeviceUtil.windowW * 0.2`) and 14 (`sunWidth = DeviceUtil.windowW * (DeviceUtil.isPad ? 0.5 : 0.6)`) — a MIXED site: window-fraction metric + `isPad` idiom.
    - 05-RESEARCH.md § Common Pitfalls #4 (`isPadWidth` ≠ `horizontalSizeClass == .regular`; the swap is an intended reviewed delta) and § Standard Stack (`horizontalSizeClass`).
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro (moving `@Environment(\.horizontalSizeClass)` reads into `body`; `@Dependency` in a `View`).
  </read_first>
  <action>
    In `CategoryView.swift` replace the `DeviceUtil.isPadWidth ? 100 : 80` grid minimum with a
    size-class decision: add `@Environment(\.horizontalSizeClass) private var horizontalSizeClass` and
    compute the `gridItems` (or the `.adaptive(minimum:)` value) in `body`/a computed property as
    `horizontalSizeClass == .regular ? 100 : 80`. Because a stored `let` cannot read the environment,
    move `gridItems` to a computed property. Note this is a reviewed breakpoint delta (Pitfall #4),
    not byte-for-byte. In `NewDawnView.swift` replace the two window-fraction reads with a captured
    container width via `onGeometryChange(for: CGFloat.self) { $0.size.width }` (write to a `@State`)
    driving `offset = width * 0.2` and `sunWidth = width * factor`; replace the `DeviceUtil.isPad`
    factor with `@Dependency(\.deviceClient).deviceType() == .pad ? 0.5 : 0.6` (device-class
    preserved). Keep the greeting layout/appearance identical.
  </action>
  <verify>
    <automated>! grep -n "DeviceUtil" AppPackage/Sources/AppComponents/CategoryView.swift AppPackage/Sources/AppComponents/NewDawnView.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Neither file references `DeviceUtil`.
    - `CategoryView` grid minimum is a `horizontalSizeClass`-driven value read inside `body`/a computed property.
    - `NewDawnView` offsets derive from an `onGeometryChange`-captured width and its factor from `deviceClient.deviceType()`.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>CategoryView uses size-class; NewDawnView uses captured width + `deviceType()`; both `DeviceUtil`-free with preserved appearance (reviewed breakpoint delta noted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-layer sizing/idiom conversion; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | (none) | AppComponents metric/idiom swaps | low | accept | No security-relevant surface. Container geometry + device class are OS-supplied. Privacy blur / auto-lock untouched (Phase 7). |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `DeviceUtil` reads from AlertView, Placeholder, CategoryView, NewDawnView.
This plan **creates**: `onGeometryChange`-captured width state in NewDawnView (new-to-codebase `onGeometryChange` idiom).
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- `xcodebuild build -scheme AppPackage-Package` compiles clean.
- SwiftLint clean.
- `grep -rn "DeviceUtil" AppPackage/Sources/AppComponents/AlertView.swift AppPackage/Sources/AppComponents/Placeholder.swift AppPackage/Sources/AppComponents/CategoryView.swift AppPackage/Sources/AppComponents/NewDawnView.swift` → empty.
</verification>

<success_criteria>
All four AppComponents sites are `DeviceUtil`-free using native adaptive sizing / `deviceType()`; the AlertView conversion preserved its dialog/alert anchor; appearance parity holds (reviewed breakpoint/short-edge deltas noted); build/lint green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-04-SUMMARY.md` when done.
</output>
