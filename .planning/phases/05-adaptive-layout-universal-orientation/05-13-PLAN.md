---
phase: 05-adaptive-layout-universal-orientation
plan: 13
type: execute
wave: 13
depends_on: [05-12]
files_modified:
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "The Home slideshow card renders at the carousel's card-width slot in both orientations, matching the scroll pitch and centered peek (G-05-1 defect 3)."
  artifacts:
    - "GalleryCardCell fills its already-sized carousel slot with no independent width calculation."
  key_links:
    - "`CardSlideSection` (HomeView+Sections.swift) already owns `cardWidth = carouselWidth * 0.8`, applies it to the Button slot AND to pitch/centering; the inner second 80% sizing in `GalleryCardCell` (line 85) is the divergence to remove."
---

<objective>
Close G-05-1 defect 3: `CardSlideSection` already computes `cardWidth = carouselWidth * 0.8`, frames
the card slot with it, and reuses it for scroll snapping and centering margins. Commit `da6cd6d2`
then independently added a **second** 80%-of-container sizing inside `GalleryCardCell`, which
resolves against the ScrollView container/content-margin contract rather than simply filling the
already-sized slot — so the rendered card width diverges from the scroll pitch and the slideshow
card looks broken in both orientations. Make the carousel the **sole** card-width owner by removing
the inner cell sizing, restoring 05-06-PLAN's contract that the cell fills its slot.

Purpose: One card-width source (the carousel) keeps the card, pitch, and centered peek consistent
under rotation and container resizing.
Output: `GalleryCardCell` fills its carousel slot; no second width calculation remains.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/debug/phase-05-layout-regressions.md
@AppPackage/Sources/HomeFeature/HomeView+Sections.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove GalleryCardCell's independent card-width sizing</name>
  <files>AppPackage/Sources/HomeFeature/GalleryCardCell.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/GalleryCardCell.swift (the `body` at lines 58-92 — the outer
      `ZStack`, and specifically the inner container-relative 80%-width modifier applied to it at
      line 85, followed by `.cornerRadius(15)` and the `.animation`/`.onChange`).
    - AppPackage/Sources/HomeFeature/HomeView+Sections.swift — `CardSlideSection`: the card slot is
      already framed at `cardWidth` in `card(for:)` (line 184, `.frame(width: cardWidth, height: ...)`),
      and `cardWidth`/`cardPitch`/`centeringMargin` (lines 158-167) all derive from the one captured
      `carouselWidth`. This is the sole owner the cell must defer to.
    - .planning/debug/phase-05-layout-regressions.md (Evidence: `da6cd6d2` added a second inner 80%
      sizing that contradicts 05-06-PLAN's "cell fills the already-sized slot" contract).
  </read_first>
  <action>
    In `GalleryCardCell.body`, remove the inner container-relative 80%-of-width sizing modifier applied
    to the outer `ZStack` (the modifier at line 85 that resolves a second `width * 0.8`), so the cell
    simply fills the frame its enclosing carousel Button already imposes (`cardWidth` at the call site
    in `CardSlideSection.card(for:)`). Keep everything else on the `ZStack` intact: `.cornerRadius(15)`,
    `.animation(.easeInOut(duration: 0.5), value: animated)`, and the `.onChange(of: colorScheme)`
    handler. Do not change the carousel (`HomeView+Sections.swift`), the `cardWidth`/pitch/centering
    math, or the `GalleryCardCell` preview's fixed layout. After the change the cell owns no width
    calculation; the carousel is the single source of card width.
    <!-- planner-discipline-allow: containerRelativeFrame -->
  </action>
  <verify>
    <automated>! grep -n "containerRelativeFrame" AppPackage/Sources/HomeFeature/GalleryCardCell.swift 2>/dev/null && grep -q "cornerRadius(15)" AppPackage/Sources/HomeFeature/GalleryCardCell.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "containerRelativeFrame" AppPackage/Sources/HomeFeature/GalleryCardCell.swift` returns nothing (the inner second sizing is gone).
    - `.cornerRadius(15)`, `.animation(... value: animated)`, and `.onChange(of: colorScheme)` remain on the `ZStack`.
    - `AppPackage/Sources/HomeFeature/HomeView+Sections.swift` is unchanged (the carousel still owns `cardWidth`/`cardPitch`/`centeringMargin`).
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild).
    - Human-check (record in SUMMARY): the Home slideshow card is centered, matches the scroll pitch, and shows correct peek in portrait and landscape on phone and iPad.
  </acceptance_criteria>
  <done>`GalleryCardCell` fills its carousel slot with no independent width; the slideshow card matches the pitch and peek in both orientations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Removing a redundant sizing modifier from a SwiftUI card introduces no input, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | (none) | GalleryCardCell sizing | low | accept | Layout-only change; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `GalleryCardCell`'s inner container-relative 80%-of-width sizing modifier on
its outer `ZStack`, making the carousel (`CardSlideSection`) the sole card-width owner.

<verification>
- Build green (command above).
- `grep -n "containerRelativeFrame" AppPackage/Sources/HomeFeature/GalleryCardCell.swift` empty.
- Human-check: slideshow card centered, correct pitch/peek in both orientations.
</verification>

<success_criteria>
The Home slideshow card renders at the carousel's slot width in both orientations, closing G-05-1 defect 3.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-13-SUMMARY.md` when done.
</output>
