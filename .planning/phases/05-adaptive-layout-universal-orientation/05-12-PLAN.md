---
phase: 05-adaptive-layout-universal-orientation
plan: 12
type: execute
wave: 12
depends_on: [05-11]
files_modified:
  - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "Reader loading and failed page placeholders aspect-fit the reader viewport in landscape as well as portrait, without collapsing to an undersized height (G-05-1 defect 2)."
  artifacts:
    - "ImageContainer placeholder sized against BOTH container axes (horizontal + vertical) so `.aspectRatio(.fit)` can choose a landscape-fit size."
  key_links:
    - "The idle placeholder (ReadingViewComponents.swift ~lines 233-236) and the loading/failed ZStack (~lines 274-277) both size from one-axis `.containerRelativeFrame(.horizontal)` today; both must bound against the reader page's vertical extent too."
---

<objective>
Close G-05-1 defect 2: commit `69c035d4` replaced the reader placeholder's explicit width+height
with `.containerRelativeFrame(.horizontal)` + a fixed `Defaults.ImageSize.contentAspect` aspect
ratio. Because the placeholder resolves from a horizontal-only proposal, `.aspectRatio(.fit)` cannot
aspect-fit the landscape viewport and the loading/failed page renders too small. Size both
placeholders against **both** container axes so they fit the reader area in every orientation, at
parity with how a loaded page's `.scaledToFit()` image fills the page.

Purpose: Restore correctly sized reader loading/failed pages in landscape (and split view / resized
containers), reusing the page's own scroll container as the sizing source.
Output: both `ImageContainer` placeholders aspect-fit the reader page in portrait and landscape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
@.planning/debug/phase-05-layout-regressions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Size both reader placeholders against both container axes</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift — the `ImageContainer.placeholder(_:)`
      helper (lines 224-237: `.containerRelativeFrame(.horizontal) { width, _ in width / (isDualPage ? 2 : 1) }`
      then `.aspectRatio(Defaults.ImageSize.contentAspect, contentMode: .fit)`), the loading/failed
      branch `ZStack` in `body` (lines 258-278, same horizontal-only frame + aspect ratio), and the
      idle branch (lines 249-257) where a loaded image uses `.scaledToFit()` to fill the page.
    - .planning/debug/phase-05-layout-regressions.md (Evidence: `69c035d4` migrated to width-only
      sizing + a portrait aspect ratio, ignoring available landscape height).
    - .planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md — Phase 3 reader page
      width is already `.containerRelativeFrame(.horizontal)`; the page's scroll container is the
      reader area, so the same container resolves the vertical extent.
  </read_first>
  <action>
    In `ImageContainer`, change both placeholder sizings (the `placeholder(_:)` helper AND the
    loading/failed `ZStack` in `body`) so they bound against both axes instead of horizontal-only.
    Replace the single-axis `.containerRelativeFrame(.horizontal) { ... }` with a two-axis
    `.containerRelativeFrame([.horizontal, .vertical]) { length, axis in ... }` closure that returns
    `length / (isDualPage ? 2 : 1)` for the horizontal axis and the full `length` for the vertical
    axis, then keep `.aspectRatio(Defaults.ImageSize.contentAspect, contentMode: .fit)`. Bounding both
    axes lets `.aspectRatio(.fit)` choose a size that fits the wide landscape viewport (height-bounded)
    exactly as `.scaledToFit()` does for a loaded page. Keep `isDualPage` half-width behavior, the
    background color, page number, reload button, and `ProgressView` logic unchanged. Do not touch the
    idle loaded-image branch (`image(url:).scaledToFit()`).
    <!-- planner-discipline-allow: containerRelativeFrame(.horizontal) -->
  </action>
  <verify>
    <automated>grep -c "containerRelativeFrame(\[.horizontal, .vertical\])" AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift | grep -qx 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "containerRelativeFrame(\[.horizontal, .vertical\])" AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift` equals 2 (both the idle placeholder helper and the loading/failed ZStack).
    - `grep -c "containerRelativeFrame(.horizontal)" AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift` equals 0.
    - `.aspectRatio(Defaults.ImageSize.contentAspect, contentMode: .fit)` is still applied to both placeholders; the dual-page half-width divisor is preserved.
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild).
    - Human-check (record in SUMMARY): a loading/failed page in landscape (single- and dual-page) fills the viewport at the same footprint a loaded page would; portrait is unchanged.
  </acceptance_criteria>
  <done>Both reader placeholders aspect-fit the reader page against both axes; landscape loading/failed pages are correctly sized, portrait unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A SwiftUI sizing-modifier change on reader placeholders introduces no input, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | (none) | Reader placeholder sizing | low | accept | Layout-only change; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **changes**: `ImageContainer` (in `ReadingViewComponents.swift`) — both the idle
`placeholder(_:)` helper and the loading/failed `ZStack` switch from one-axis
`.containerRelativeFrame(.horizontal)` to two-axis `.containerRelativeFrame([.horizontal, .vertical])`
sizing while keeping the dual-page half-width divisor and `Defaults.ImageSize.contentAspect` fit.

<verification>
- Build green (command above).
- Grep gates: two-axis frame count == 2; horizontal-only frame count == 0.
- Human-check: landscape loading/failed pages correctly sized (single + dual page); portrait unchanged.
</verification>

<success_criteria>
Reader loading and failed placeholders aspect-fit the viewport in every orientation, closing G-05-1 defect 2.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-12-SUMMARY.md` when done.
</output>
