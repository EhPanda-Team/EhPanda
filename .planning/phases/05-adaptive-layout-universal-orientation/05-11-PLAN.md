---
phase: 05-adaptive-layout-universal-orientation
plan: 11
type: execute
wave: 11
depends_on: [05-10]
files_modified:
  - AppPackage/Sources/SettingFeature/Components/AboutView.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "About's copyright and app version stay visible in every orientation, including compact/landscape navigation bars (G-05-1 defect 1)."
  artifacts:
    - "AboutView copyright/version rendered as stable, scrollable Form content (not a large-title-only toolbar slot)."
  key_links:
    - "The copyright/version strings (`.Constant.copyright` + the `version` computed property) move from the `.largeSubtitle` ToolbarItem into the Form's scrollable content so no navigation-bar style can hide them."
---

<objective>
Close G-05-1 defect 1: the About screen renders its copyright and version **only** inside a
`ToolbarItem(placement: .largeSubtitle)`, a slot that compact/landscape navigation bars do not
expose, so the metadata vanishes in landscape. Re-render that metadata as stable, scrollable Form
content that survives every orientation and size class.

Purpose: Restore the always-visible app-identity metadata the UAT flagged as missing in landscape.
Output: `AboutView` shows copyright + version in scrollable content in portrait and landscape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/debug/phase-05-layout-regressions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render About copyright/version as scrollable Form content</name>
  <files>AppPackage/Sources/SettingFeature/Components/AboutView.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Components/AboutView.swift (whole file — the `version`
      computed property at lines 8-14, the `Form` body at lines 16-43, and the
      `ToolbarItem(placement: .largeSubtitle)` block at lines 45-56 that currently owns the only
      copyright/version rendering).
    - .planning/debug/phase-05-layout-regressions.md (Evidence bullet on AboutView: the metadata
      lives exclusively in a large-title-only toolbar placement — traces to commit `f61208866`,
      predates Phase 5 — and disappears in compact landscape bars).
    - .planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md gap G-05-1 (locked
      re-derivation: render About metadata in stable scrollable content).
  </read_first>
  <action>
    Move the copyright and version rendering out of the `ToolbarItem(placement: .largeSubtitle)`
    and into the `Form` so it is scrollable content that renders in every orientation. Add a `Section`
    (either a leading/trailing section or a section footer) that shows `Text(.Constant.copyright)` and
    `Text(version)` using the same `.foregroundStyle(.gray)` + `.font(.caption2.bold())` styling,
    left-aligned. Delete the now-empty `.toolbar { ToolbarItem(placement: .largeSubtitle) { ... } }`
    modifier (remove the whole `.toolbar` if the largeSubtitle item was its only content). Keep
    `.navigationTitle(.ehPanda)` and the `version` computed property unchanged. Do not alter the
    contact / special-thanks / contributors / acknowledgements sections, and change no localized keys
    (`.Constant.copyright` and `.version` are reused verbatim).
  </action>
  <verify>
    <automated>! grep -n "largeSubtitle" AppPackage/Sources/SettingFeature/Components/AboutView.swift 2>/dev/null && grep -q "Constant.copyright" AppPackage/Sources/SettingFeature/Components/AboutView.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "largeSubtitle" AppPackage/Sources/SettingFeature/Components/AboutView.swift` returns nothing.
    - `Text(.Constant.copyright)` and `Text(version)` are rendered inside the `Form` (a `Section`), not inside a `ToolbarItem`.
    - No `.xcstrings` key added or removed (only `.Constant.copyright` / `.version` reused).
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild invocations).
    - Human-check (record in SUMMARY): open About in landscape on iPhone and iPad — copyright + version are visible; portrait appearance is unchanged.
  </acceptance_criteria>
  <done>About copyright/version render as scrollable Form content visible in portrait and landscape; the large-title-only toolbar rendering is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Relocating static, already-shipped copyright/version strings within a SwiftUI Form crosses no security boundary. No new input, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | (none) | AboutView metadata placement | low | accept | UI-only relocation of constant strings; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **changes**: `AboutView` — the copyright/version rendering moves from a
`.largeSubtitle` ToolbarItem into a scrollable `Form` `Section`; the `.largeSubtitle` toolbar item
is removed.
This plan **creates**: a Form section/footer in `AboutView` showing copyright + version.
This plan **removes**: `AboutView`'s `ToolbarItem(placement: .largeSubtitle)` metadata block.

<verification>
- Build green (command above).
- `grep -n "largeSubtitle" AppPackage/Sources/SettingFeature/Components/AboutView.swift` empty.
- Human-check: About metadata visible in portrait and landscape on phone and iPad.
</verification>

<success_criteria>
About's copyright and version are always visible, in stable scrollable content, in every orientation and size class — closing G-05-1 defect 1.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-11-SUMMARY.md` when done.
</output>
