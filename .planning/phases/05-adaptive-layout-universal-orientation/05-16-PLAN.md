---
phase: 05-adaptive-layout-universal-orientation
plan: 16
type: execute
wave: 16
depends_on: [05-15]
files_modified:
  - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-03]

must_haves:
  truths:
    - "In an iPad window with macOS-style window controls, the reader's custom upper toolbar clears the window-control region; full-screen iPad and iPhone reader chrome is unchanged (G-05-4 defect 7)."
  artifacts:
    - "The reader `UpperPanel` reserves the window-control safe-area exclusion region on iPad instead of hugging the container edge under `.ignoresSafeArea()`."
  key_links:
    - "The reader container `.ignoresSafeArea()` (ReadingView.swift:164) makes the overlaid `ControlPanel` ignore safe area; `UpperPanel` currently only compensates 20pt horizontal + 8pt top for non-pad landscape and explicitly assumes iPad is already inset."
---

<objective>
Close G-05-4 defect 7: the reader's full-width custom `UpperPanel` uses fixed 20pt horizontal
padding and a top-only 8pt branch for non-pad landscape, reserving no exclusion region for iPad
window controls. Under `.ignoresSafeArea()` on the reader container, the toolbar hugs the very edge,
so in an iPad windowed context with macOS-style leading window controls the custom dismiss control
overlaps them. Make the reader upper toolbar aware of the iPad window-control safe geometry so it
never overlaps the controls, at full-screen/iPhone parity.

Purpose: The reader dismiss/title chrome stays clear of iPad window controls while preserving the
existing immersive full-screen and iPhone reader appearance.
Output: `UpperPanel` respects the window-control safe region on iPad; full-screen/iPhone unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/debug/phase-05-ipad-window-navigation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reserve the iPad window-control exclusion region for the reader upper toolbar</name>
  <files>AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift — `ControlPanel.body` (lines 71-101),
      `upperPanelTopPadding` (lines 61-65: `deviceType() != .pad && isLandscape ? 8 : 0`), `isLandscape`
      (lines 67-69: `containerSize.width > containerSize.height`), the injected `containerSize` (line 22)
      and `deviceClient` (line 12), and the `UpperPanel` layout (lines 104-238: leading dismiss button +
      title cluster, trailing controls cluster, `.padding(.horizontal, 20)` at line 236).
    - .planning/debug/phase-05-ipad-window-navigation.md (Evidence timestamp 17:14: `UpperPanel` is a
      full-width HStack padded 20pt, only compensates 8pt for non-pad landscape, and its comment assumes
      iPad is already inset — it never consumes safe-area/window-control geometry).
    - .planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md D-06 (avoid `GeometryReader`;
      `.onGeometryChange` is the sanctioned size/geometry reader). Root `.swiftlint.yml` (no suppressions;
      no `systemName`/`systemImage` — use `systemSymbol`).
    - AppPackage/Sources/ReadingFeature/ReadingView.swift lines 164, 184-185 — the reader container
      `.ignoresSafeArea()` and the single `.onGeometryChange` writer into `gestureHandler.containerSize`
      (the established single source of container geometry).
  </read_first>
  <action>
    Make the reader `UpperPanel` respect the window-control safe-area region on iPad so its leading
    dismiss/title cluster never overlaps macOS-style window controls, WITHOUT re-introducing
    `GeometryReader` (Phase 5 constraint — use `.safeAreaPadding` or an `onGeometryChange`-captured
    inset). Preferred approach: apply the window's leading/top safe-area inset to the `UpperPanel` on
    iPad only (e.g. `.safeAreaPadding` on the upper toolbar, or capture the container's
    `safeAreaInsets` via an `.onGeometryChange` transform reading `GeometryProxy.safeAreaInsets` and
    reserve that leading exclusion region), gated so it applies on `deviceClient.deviceType() == .pad`.
    Preserve the existing behavior everywhere else: the non-pad landscape 8pt `upperPanelTopPadding`
    branch, the 20pt horizontal padding baseline, and the full-screen iPad / portrait appearance must
    be unchanged (a full-screen iPad window has no leading window-control inset, so the reserved region
    must resolve to zero there — do not push the toolbar inward when there are no window controls). Do
    not change `LowerPanel`, the container geometry writer in `ReadingView`, or any gesture/paging code.
  </action>
  <verify>
    <automated>! grep -n "GeometryReader" AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift 2>/dev/null && grep -qE "safeAreaPadding|safeAreaInsets" AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GeometryReader" AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift` returns nothing (no new GeometryReader).
    - The `UpperPanel`/`ControlPanel` reserves a window-control exclusion region gated on `deviceType() == .pad` (grep: `safeAreaPadding` or `safeAreaInsets` present).
    - The non-pad landscape `upperPanelTopPadding` 8pt branch and 20pt horizontal baseline are preserved.
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild).
    - Human-check (record in SUMMARY): in an iPad window with window controls, the reader dismiss button/title clears the traffic-light controls; full-screen iPad and iPhone reader chrome is visually unchanged.
  </acceptance_criteria>
  <done>The reader upper toolbar clears iPad window controls via a device-gated safe-area exclusion region, with full-screen/iPhone appearance preserved and no new GeometryReader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Insetting a SwiftUI overlay by safe-area geometry introduces no input, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16 | (none) | Reader upper toolbar safe geometry | low | accept | Layout-only change; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **changes**: `ControlPanel`/`UpperPanel` — adds a device-gated window-control safe-area
exclusion region (via `.safeAreaPadding` or an `onGeometryChange`-captured safe-area inset) so the
reader upper toolbar clears iPad window controls; preserves the non-pad landscape top padding and the
horizontal baseline.

<verification>
- Build green (command above).
- Grep gates: no `GeometryReader`; `safeAreaPadding`/`safeAreaInsets` present.
- Human-check: iPad windowed toolbar clears window controls; full-screen iPad + iPhone unchanged.
</verification>

<success_criteria>
The reader custom upper toolbar never overlaps iPad window controls, at full-screen/iPhone parity — closing G-05-4 defect 7.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-16-SUMMARY.md` when done.
</output>
