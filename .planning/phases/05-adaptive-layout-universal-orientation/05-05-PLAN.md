---
phase: 05-adaptive-layout-universal-orientation
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift
  - AppPackage/Sources/DetailFeature/Components/TagDetailView.swift
  - AppPackage/Sources/AppComponents/PreviewImageView.swift
autonomous: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "No DetailFeature view sizes itself from `DeviceUtil.windowW`/`absWindowW` or an `isPadWidth` breakpoint — widths are container-relative and breakpoints are size-class decisions."
    - "The preview-image sizing constants (`previewMinW`/`previewMaxW`/`previewAvgW`) are no longer read from a device-derived `Defaults` global — each consumer computes its size natively (size-class or fixed)."
  artifacts: []
  key_links:
    - "`ArchivesView`/`PreviewsView`/`DetailView+Subviews`/`TagDetailView`/`PreviewImageView` no longer reference the device-derived `Defaults.FrameSize`/`ImageSize` props — those props become orphaned for deletion in 05-10."
---

<objective>
Convert the DetailFeature metric sites off `DeviceUtil` and dissolve their device-derived `Defaults`
preview-size consumers (D-02/D-07): comment/preview widths → container-relative; `archiveGridWidth`
and `isPadWidth` font/breakpoint → size-class; `previewMinW`/`previewMaxW`/`previewAvgW` consumers →
native size-class-selected (or fixed, for the non-view pixel cap) values.

Purpose: Retire these `DeviceUtil` reads and stop referencing the device-derived `Defaults.FrameSize`/
`ImageSize` props so 05-10 can delete both `DeviceUtil` and those props. Preview sizing stays at
appearance parity via size-class breakpoints (reviewed `isPadWidth`→size-class delta).
Output: 6 files free of `DeviceUtil` and of device-derived `Defaults` props.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
@AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment + detail widths → container-relative</name>
  <files>AppPackage/Sources/DetailFeature/Comments/CommentsView.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift</files>
  <read_first>
    - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift line 221 (`.frame(width: DeviceUtil.windowW / widthFactor)`) and where `widthFactor` is defined.
    - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift line 41 (`max(DeviceUtil.absWindowW / 5, 80)`) and lines 288-289 (`width = Defaults.ImageSize.previewAvgW`, `height = width / previewAspect`).
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift lines 28-31 (`previewMinW`/`previewMaxW`/`previewAvgW` definitions — `isPadWidth ? 180/220 : 100/120`, avg = (min+max)/2).
    - 05-RESEARCH.md § Standard Stack + § Common Pitfalls #4.
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro.
  </read_first>
  <action>
    In `CommentsView.swift` replace `DeviceUtil.windowW / widthFactor` with a container-relative
    fraction `.containerRelativeFrame(.horizontal) { width, _ in width / widthFactor }` (or an
    `onGeometryChange`-captured width if not inside a relative container). In
    `DetailView+Subviews.swift`: replace `max(DeviceUtil.absWindowW / 5, 80)` with a captured
    container width via `onGeometryChange(for: CGFloat.self)` driving `max(width / 5, 80)` (or a
    `containerRelativeFrame` fraction if it applies to a framed child); and replace the
    `Defaults.ImageSize.previewAvgW` `width` computed property with a native size-class-selected value
    — add `@Environment(\.horizontalSizeClass)` and compute `previewAvg = horizontalSizeClass ==
    .regular ? (180 + 220) / 2 : (100 + 120) / 2` inline (matching the old min/max pairs), keeping
    `height = previewAvg / previewAspect`. Do not reference `Defaults.ImageSize.previewAvgW/MinW/MaxW`
    afterward.
  </action>
  <verify>
    <automated>! grep -En "DeviceUtil|previewAvgW|previewMinW|previewMaxW" AppPackage/Sources/DetailFeature/Comments/CommentsView.swift AppPackage/Sources/DetailFeature/DetailView+Subviews.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Neither file references `DeviceUtil` or the device-derived `Defaults.ImageSize.preview*` props.
    - Widths are container-relative; the preview average width is a `horizontalSizeClass`-selected value.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>Comment + detail widths are container-relative and the preview width is size-class-selected; no `DeviceUtil`/device-derived `Defaults` props referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Archive grid + preview grid/font breakpoints → size-class</name>
  <files>AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift, AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift</files>
  <read_first>
    - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift lines 82-83 (grid `minimum`/`maximum: Defaults.FrameSize.archiveGridWidth`), 152 (`archiveGridWidth`), 209 (`DeviceUtil.isPadWidth`).
    - AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift lines 26-27 (grid `minimum: previewMinW`, `maximum: previewMaxW`), 49 (`DeviceUtil.isPadWidth ? .callout : .caption`).
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift line 10 (`archiveGridWidth = isPadWidth ? 175 : isSEWidth ? 125 : 150`) and lines 28-29 (`previewMinW`/`previewMaxW`).
    - 05-RESEARCH.md § Common Pitfalls #4 (reviewed breakpoint delta; `.adaptive(minimum:)`).
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro.
  </read_first>
  <action>
    In `ArchivesView.swift` replace the `Defaults.FrameSize.archiveGridWidth` reads (lines 82-83, 152)
    with a size-class-selected inline value: add `@Environment(\.horizontalSizeClass)` and compute
    `archiveGridWidth = horizontalSizeClass == .regular ? 175 : 150` (the old `isSEWidth ? 125`
    small-phone branch collapses into the compact value — a reviewed delta; note it). Replace the
    `DeviceUtil.isPadWidth` read at line 209 with `horizontalSizeClass == .regular`. In
    `PreviewsView.swift` replace the grid `minimum: previewMinW`/`maximum: previewMaxW` with
    size-class-selected inline values (`horizontalSizeClass == .regular ? 180 : 100` /
    `... ? 220 : 120`) and the `DeviceUtil.isPadWidth ? .callout : .caption` font at line 49 with
    `horizontalSizeClass == .regular ? .callout : .caption`. Do not reference the device-derived
    `Defaults` props afterward.
  </action>
  <verify>
    <automated>! grep -En "DeviceUtil|archiveGridWidth|previewMinW|previewMaxW" AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Neither file references `DeviceUtil` or the device-derived `Defaults.FrameSize.archiveGridWidth`/`Defaults.ImageSize.previewMinW`/`previewMaxW`.
    - Grid sizes and the preview font are `horizontalSizeClass`-driven.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>Archive + preview grids/fonts use size-class; the SE/compact collapse noted as a reviewed delta; no device-derived `Defaults`/`DeviceUtil` remain.</done>
</task>

<task type="auto">
  <name>Task 3: Preview pixel cap + TagDetail width → native / fixed</name>
  <files>AppPackage/Sources/AppComponents/PreviewImageView.swift, AppPackage/Sources/DetailFeature/Components/TagDetailView.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/PreviewImageView.swift line 10 (`public static let defaultMaxPixelSize = Defaults.ImageSize.previewMaxW * 3` — a non-view file-scope `static let`, so there is no `body`/environment to read a size-class from).
    - AppPackage/Sources/DetailFeature/Components/TagDetailView.swift line 53 (`Defaults.ImageSize.previewAvgW`).
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift lines 28-30.
    - 05-RESEARCH.md § Common Pitfalls #3 (downsample pixel cap is not layout — a fixed value is a benign parity delta).
  </read_first>
  <action>
    In `PreviewImageView.swift` replace `Defaults.ImageSize.previewMaxW * 3` with a FIXED constant
    equal to the former regular-width value times 3 (i.e., `220 * 3 = 660`) — this is a downsampling
    pixel-size cap, not layout, so a device-independent constant is the correct native form and a
    reviewed benign parity delta (Pitfall #3), not size-class-dependent. In `TagDetailView.swift`
    replace `Defaults.ImageSize.previewAvgW` with a size-class-selected value inside `body`/a computed
    property (add `@Environment(\.horizontalSizeClass)`; `horizontalSizeClass == .regular ? 200 : 110`
    — the averages of the old 180/220 and 100/120 pairs). Do not reference the device-derived
    `Defaults` props afterward.
  </action>
  <verify>
    <automated>! grep -En "previewMaxW|previewAvgW|DeviceUtil" AppPackage/Sources/AppComponents/PreviewImageView.swift AppPackage/Sources/DetailFeature/Components/TagDetailView.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PreviewImageView.defaultMaxPixelSize` is a fixed constant (no `Defaults.ImageSize.previewMaxW`).
    - `TagDetailView` preview width is a `horizontalSizeClass`-selected value (no `Defaults.ImageSize.previewAvgW`).
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>The preview pixel cap is a fixed constant; TagDetail width is size-class-selected; the device-derived preview `Defaults` props now have no remaining consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-layer sizing conversion; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | (none) | Detail metric / Defaults dissolution | low | accept | No security-relevant surface. Container geometry + size class are OS-supplied. Privacy blur / auto-lock untouched (Phase 7). |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `DeviceUtil` reads and device-derived `Defaults.FrameSize.archiveGridWidth` /
`Defaults.ImageSize.previewMinW`/`previewMaxW`/`previewAvgW` consumers from CommentsView,
DetailView+Subviews, ArchivesView, PreviewsView, TagDetailView, PreviewImageView (making those
`Defaults` props orphaned for deletion in 05-10).
This plan **creates**: nothing new beyond native size-class/container-relative expressions.
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- `xcodebuild build -scheme AppPackage-Package` compiles clean.
- SwiftLint clean.
- `grep -rnE "DeviceUtil|previewMinW|previewMaxW|previewAvgW|archiveGridWidth" AppPackage/Sources/DetailFeature AppPackage/Sources/AppComponents/PreviewImageView.swift` → empty.
</verification>

<success_criteria>
All 6 files are free of `DeviceUtil` and device-derived `Defaults` props, using native size-class / container-relative sizing at appearance parity (reviewed breakpoint/pixel-cap deltas noted); build/lint green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-05-SUMMARY.md` when done.
</output>
