---
phase: 05-adaptive-layout-universal-orientation
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Sources/AppComponents/TagSuggestionView.swift
  - AppPackage/Sources/SearchFeature/SearchRootView+Keywords.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift
autonomous: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "The device-class view branches (TabBar settings tab, tag-suggestion overlay, search keywords) read `@Dependency(\\.deviceClient).deviceType()` — no `DeviceUtil.isPad`/`isPhone` static."
    - "The EhSetting width/height metric reads no longer touch `DeviceUtil.windowW`/`windowH` — they use native container-relative sizing."
  artifacts: []
  key_links:
    - "Each converted view holds `@Dependency(\\.deviceClient)` and reads `deviceType()` in `body` (sync `@MainActor`, no `await`)."
---

<objective>
Convert the four remaining small non-reader view sites off `DeviceUtil`: three device-class idiom
views (TabBarView, TagSuggestionView, SearchRootView+Keywords) swap `DeviceUtil.isPad`/`isPhone` to
`@Dependency(\.deviceClient).deviceType()` (D-03, device-class semantics preserved); the EhSetting
metric reads (`DeviceUtil.windowW`/`windowH` fractions) go native (D-02).

Purpose: Retire these `DeviceUtil` consumers ahead of the final `DeviceUtil` deletion (05-10). All
idiom swaps are parity-preserving device-class branches (NOT size-class).
Output: 4 view files free of `DeviceUtil`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the 3 idiom views to deviceClient.deviceType()</name>
  <files>AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift, AppPackage/Sources/AppComponents/TagSuggestionView.swift, AppPackage/Sources/SearchFeature/SearchRootView+Keywords.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift line 30 (`if tab == .setting, DeviceUtil.isPad`).
    - AppPackage/Sources/AppComponents/TagSuggestionView.swift lines 27, 83 (`if DeviceUtil.isPhone`).
    - AppPackage/Sources/SearchFeature/SearchRootView+Keywords.swift line 40 (`if !DeviceUtil.isPad`).
    - AppPackage/Sources/DeviceClient/DeviceClient.swift (the reshaped `deviceType()` from 05-01).
    - 05-PATTERNS.md § "@Dependency injection over static globals" and the DeviceClient idiom-consumer analog.
    - `.claude/skills` pfw-dependencies / pfw-modern-swiftui (`@Dependency` in a SwiftUI `View`).
  </read_first>
  <action>
    In each of the three views add `@Dependency(\.deviceClient) private var deviceClient` and replace
    the static read: `DeviceUtil.isPad` → `deviceClient.deviceType() == .pad`; `!DeviceUtil.isPad` →
    `deviceClient.deviceType() != .pad`; `DeviceUtil.isPhone` → `deviceClient.deviceType() == .phone`.
    `deviceType()` is sync `@MainActor` so it reads directly in `body`/computed contexts with no
    `await`. Add `import Dependencies` (and the module exposing `DeviceType`) where needed; remove
    `import AppTools` from a file only if `DeviceUtil` was its sole use. Preserve exact branch
    behavior — device class, not size class.
  </action>
  <verify>
    <automated>! grep -rn "DeviceUtil" AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift AppPackage/Sources/AppComponents/TagSuggestionView.swift AppPackage/Sources/SearchFeature/SearchRootView+Keywords.swift 2>/dev/null && grep -q "deviceClient.deviceType()" AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - None of the three files reference `DeviceUtil`.
    - Each reads `deviceClient.deviceType()` for its former `isPad`/`isPhone` branch.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>The 3 idiom views branch on `deviceType()`; behavior unchanged; no `DeviceUtil` remains in them.</done>
</task>

<task type="auto">
  <name>Task 2: Convert EhSetting width/height metric reads to native</name>
  <files>AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift lines 112, 153 (`.frame(width: DeviceUtil.windowW * 0.25)`) and 189 (`.frame(maxHeight: DeviceUtil.windowH * 0.3)`).
    - 05-RESEARCH.md § Standard Stack (`containerRelativeFrame` / `onGeometryChange` per-site choice) and § Architecture Patterns Pattern 1.
    - 05-PATTERNS.md § "Container-relative sizing (already shipped)".
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro (container-relative sizing).
  </read_first>
  <action>
    Replace the `DeviceUtil.windowW * 0.25` width frames (lines 112, 153) with a container-relative
    fraction: `.containerRelativeFrame(.horizontal) { width, _ in width * 0.25 }` (or an
    `onGeometryChange`-captured width if the enclosing container is not a recognized relative
    container — planner's per-site pick). Replace the `DeviceUtil.windowH * 0.3` max-height (line 189)
    with `.containerRelativeFrame(.vertical) { height, _ in height * 0.3 }` used as the max height, or
    capture the container height via `onGeometryChange(for: CGFloat.self)` and apply `maxHeight:
    captured * 0.3`. Note per site: `windowW` was the orientation-independent SHORT edge (min dim);
    container-relative uses the CURRENT container dimension — an intended, reviewed parity delta on
    now-rotatable devices (per 05-RESEARCH § Common Pitfalls #3/#4), not byte-for-byte. Do not alter
    surrounding layout.
  </action>
  <verify>
    <automated>! grep -n "DeviceUtil" AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DeviceUtil" AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift` returns nothing.
    - The two width sites and the one max-height site use `containerRelativeFrame`/`onGeometryChange` fractions.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>EhSetting width/height metrics are container-relative; no `DeviceUtil` remains; layout preserved at parity (reviewed short-edge→current-dim delta noted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure view-layer layout/idiom conversion; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | (none) | idiom/metric view swaps | low | accept | No security-relevant surface. Device class + container geometry are OS-supplied. Privacy blur / auto-lock untouched (Phase 7). |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `DeviceUtil` reads from TabBarView, TagSuggestionView, SearchRootView+Keywords, EhSettingView+Sections3.
This plan **creates**: nothing new (uses the `deviceClient.deviceType()` from 05-01 and native SwiftUI sizing).
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- `xcodebuild build -scheme AppPackage-Package` compiles clean.
- SwiftLint clean.
- `grep -rn "DeviceUtil" AppPackage/Sources/AppFeature/View/TabBar AppPackage/Sources/AppComponents/TagSuggestionView.swift AppPackage/Sources/SearchFeature/SearchRootView+Keywords.swift AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift` → empty.
</verification>

<success_criteria>
The three idiom views branch on `deviceType()`; EhSetting metrics are container-relative; all four files are `DeviceUtil`-free with preserved behavior; build/lint green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-03-SUMMARY.md` when done.
</output>
