---
phase: 05-adaptive-layout-universal-orientation
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - AppPackage/Sources/HomeFeature/HomeView+Sections.swift
  - AppPackage/Sources/HomeFeature/GalleryCardCell.swift
autonomous: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "The Home carousel derives card width AND its centering peek inset from ONE locally-captured container width (`onGeometryChange`), not from `DeviceUtil.windowW`/`Defaults.FrameSize.cardCellSize` (D-07)."
    - "The ranking cell width and the card cell width no longer read a device-derived `Defaults` global; `cardCellHeight` stays a genuine constant."
    - "The Home `isPad` section branch reads `deviceClient.deviceType() == .pad`."
  artifacts: []
  key_links:
    - "The carousel's `cardWidth` and `centeringMargin` are computed from the same captured width, keeping `preferredItemSize`/peek-inset coupling intact."
---

<objective>
Dissolve the Home carousel's device-derived size coupling into one local `onGeometryChange`
(D-07): capture the container width once, compute `cardWidth = width * 0.8` and the centering peek
inset from it; convert `rankingCellWidth` to a size-class-scaled container fraction; keep
`cardCellHeight` as a constant; swap the Home `isPad` section branch to `deviceClient.deviceType()`;
and give `GalleryCardCell` its width from the container (D-02).

Purpose: This is the D-07 carousel coupling the research called out (card width feeds both the frame
and the `(windowW - cardWidth)/2` peek inset). Retire the last Home `DeviceUtil`/device-`Defaults`
consumers so 05-10 can delete both.
Output: HomeView+Sections + GalleryCardCell free of `DeviceUtil` and device-derived `Defaults` props.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
@AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home carousel + ranking → one onGeometryChange capture; isPad branch → deviceType()</name>
  <files>AppPackage/Sources/HomeFeature/HomeView+Sections.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/HomeView+Sections.swift lines 106 (`(DeviceUtil.windowW - cardWidth) / 2` peek margin — locate the enclosing carousel scroll view + its `preferredItemSize`/`contentMargins`), 108 (`.frame(height: Defaults.FrameSize.cardCellHeight)` — KEEP), 154 (`cardWidth = Defaults.FrameSize.cardCellSize.width`), 162 (peek), 180 (`.frame(width: cardWidth, height: cardCellSize.height)`), 334 (`if DeviceUtil.isPad`), 375 (`.frame(width: Defaults.FrameSize.rankingCellWidth)`).
    - 05-RESEARCH.md § Code Examples "Home-carousel coupling via one local `onGeometryChange` (D-07)" — the exact `carouselWidth`/`cardWidth`/`centeringMargin = carouselWidth * 0.1` pattern and the `.contentMargins(.horizontal, centeringMargin, for: .scrollContent)` + `.frame(height: cardCellHeight)` shape.
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift lines 13-19 (`cardCellWidth = windowW * 0.8`, `cardCellHeight` constant, `rankingCellWidth = (isPadWidth ? 0.4 : 0.7) * windowW`).
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro (`onGeometryChange`, `containerRelativeFrame`).
  </read_first>
  <action>
    Add a local `@State private var carouselWidth: CGFloat = 0` and attach
    `.onGeometryChange(for: CGFloat.self) { $0.size.width } action: { carouselWidth = $0 }` to the
    carousel scroll view. Compute `cardWidth` as `carouselWidth * 0.8` and the centering peek inset as
    `(carouselWidth - cardWidth) / 2` (== `carouselWidth * 0.1`), feeding both the card frame and the
    `.contentMargins(.horizontal, ..., for: .scrollContent)` — replacing the `Defaults.FrameSize.cardCellSize.width`
    and `DeviceUtil.windowW` reads at lines 106/154/162/180. KEEP `Defaults.FrameSize.cardCellHeight`
    (a genuine constant) for the card height. For the ranking cell (line 375) replace
    `Defaults.FrameSize.rankingCellWidth` with a size-class-scaled container fraction: add
    `@Environment(\.horizontalSizeClass)` and use `.containerRelativeFrame(.horizontal) { width, _ in
    width * (horizontalSizeClass == .regular ? 0.4 : 0.7) }` (the old `isPadWidth` factor — reviewed
    delta). Replace `if DeviceUtil.isPad` (line 334) with `if deviceClient.deviceType() == .pad`
    (add `@Dependency(\.deviceClient)`). Do not disturb the `.viewAligned`/`preferredItemSize` carousel
    wiring beyond re-sourcing the width.
  </action>
  <verify>
    <automated>! grep -En "DeviceUtil|cardCellSize|rankingCellWidth" AppPackage/Sources/HomeFeature/HomeView+Sections.swift 2>/dev/null && grep -q "onGeometryChange" AppPackage/Sources/HomeFeature/HomeView+Sections.swift && grep -q "cardCellHeight" AppPackage/Sources/HomeFeature/HomeView+Sections.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `HomeView+Sections.swift` references no `DeviceUtil`, no `Defaults.FrameSize.cardCellSize`/`cardCellWidth`/`rankingCellWidth`.
    - The carousel captures width via one `onGeometryChange`; `cardWidth` and the peek inset derive from it.
    - `Defaults.FrameSize.cardCellHeight` is still used for the card height.
    - The Home section branch reads `deviceClient.deviceType() == .pad`.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>The carousel width/peek coupling runs off one captured width; ranking is size-class-scaled; the `isPad` branch is `deviceType()`; `cardCellHeight` kept; no `DeviceUtil`/device-`Defaults` remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: GalleryCardCell width + idiom → container-relative / deviceType()</name>
  <files>AppPackage/Sources/HomeFeature/GalleryCardCell.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/GalleryCardCell.swift line 49 (`guard !DeviceUtil.isPad, trimmedTitle.count > 20`) and line 82 (`.frame(width: Defaults.FrameSize.cardCellWidth).cornerRadius(15)` — `cardCellWidth = windowW * 0.8`, the same width the carousel now supplies).
    - HomeView+Sections.swift (Task 1) — the card container already receives `cardWidth = carouselWidth * 0.8` from the carousel frame, so the cell should fill its provided slot rather than re-deriving from a global.
    - 05-PATTERNS.md § "Container-relative sizing (already shipped)".
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro.
  </read_first>
  <action>
    Replace `.frame(width: Defaults.FrameSize.cardCellWidth)` (line 82) with a container-relative
    width `.containerRelativeFrame(.horizontal) { width, _ in width * 0.8 }` so the card tracks the
    carousel container width (equivalent to the former `windowW * 0.8` when the carousel spans the
    window — a reviewed parity delta under multitasking). Keep `.cornerRadius(15)`. Replace `guard
    !DeviceUtil.isPad, ...` (line 49) with `guard deviceClient.deviceType() != .pad, ...` (add
    `@Dependency(\.deviceClient)`). Keep the title-trim behavior identical.
  </action>
  <verify>
    <automated>! grep -En "DeviceUtil|cardCellWidth" AppPackage/Sources/HomeFeature/GalleryCardCell.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `GalleryCardCell.swift` references no `DeviceUtil` and no `Defaults.FrameSize.cardCellWidth`.
    - The card width is container-relative; the `!isPad` title branch reads `deviceType() != .pad`.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>GalleryCardCell width is container-relative and its idiom branch reads `deviceType()`; no `DeviceUtil`/device-`Defaults` remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Home view-layer sizing/idiom conversion; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | (none) | Home carousel / card sizing | low | accept | No security-relevant surface. Container geometry + device class are OS-supplied. Privacy blur / auto-lock untouched (Phase 7). |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `DeviceUtil` reads and the device-derived `Defaults.FrameSize.cardCellWidth`/
`cardCellSize`/`rankingCellWidth` consumers from HomeView+Sections and GalleryCardCell (leaving those
`Defaults` props orphaned for deletion in 05-10). Keeps `Defaults.FrameSize.cardCellHeight`.
This plan **creates**: the Home carousel's local `onGeometryChange` width capture.
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- `xcodebuild build -scheme AppPackage-Package` compiles clean.
- SwiftLint clean.
- `grep -rnE "DeviceUtil|cardCellWidth|cardCellSize|rankingCellWidth" AppPackage/Sources/HomeFeature/HomeView+Sections.swift AppPackage/Sources/HomeFeature/GalleryCardCell.swift` → empty.
</verification>

<success_criteria>
The Home carousel derives width + peek inset from one captured width; ranking/card widths are container-relative/size-class; the Home idiom branches read `deviceType()`; `cardCellHeight` kept; both files `DeviceUtil`/device-`Defaults`-free at appearance parity; build/lint green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-06-SUMMARY.md` when done.
</output>
