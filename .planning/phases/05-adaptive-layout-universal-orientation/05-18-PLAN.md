---
phase: 05-adaptive-layout-universal-orientation
plan: 18
type: execute
wave: 18
depends_on: [05-17]
files_modified:
  - AppPackage/Sources/DetailFeature/GalleryNavigation.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "On iPhone, opening a gallery detail from every gallery host uses a navigation push (not a modal sheet), preserving the iPad-modal / non-pad-push contract — or, if no modal-on-iPhone path can be reproduced, the finding is surfaced for human confirmation rather than silently passed (G-05-4 defect 10)."
  artifacts:
    - "An exhaustive, exercised enumeration of non-pad gallery entry paths, and either the identified push-contract-bypassing routing site restored to push, or a documented no-repro finding flagged for human confirmation."
  key_links:
    - "`GalleryNavigation.routeGalleryDetail` (GalleryNavigation.swift:11-19) is the shared branch `deviceType() == .pad ? present() : push()`; the standard gallery-tap path is proven to push on iPhone by `DownloadsReducerActionTests`. The regression is on some entry path that bypasses this branch (candidate hosts + the app-level `DetailView` sheet + deep-link/URL entry)."
---

<objective>
Close G-05-4 defect 10 (UNRESOLVED root cause): the user observed iPhone detail pages opening as a
modal sheet instead of a navigation push. The diagnosis established this was NOT an intentional Phase
5 change — Plan 05-01 / commit `5901ee63` only swapped `isPad()` → `deviceType() == .pad`, the
contract is explicitly "iPad presents, iPhone pushes", and `DownloadsReducerActionTests` confirms
standard gallery taps still push on iPhone in the checked-in reducer. So the reported iPhone sheet is
a regression or a path-specific routing mismatch on an entry path the reducer test did not exercise.
Investigate-then-fix: reproduce the exact iPhone entry path that presents a modal detail, pinpoint
the routing site that bypasses the push contract for non-pad, and restore navigation-push for every
non-pad gallery host while preserving the iPad-modal / non-pad-push contract. If no modal-presenting
iPhone path can be reproduced after exhausting the entry points, surface the finding for human
confirmation — do not silently pass.

Purpose: Restore (or definitively confirm) the iPhone navigation-push contract for gallery detail.
Output: the bypassing routing site fixed to push on iPhone, or a documented no-repro finding flagged
for human confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/debug/phase-05-ipad-window-navigation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the iPhone modal-detail entry path and restore push (or surface a no-repro finding)</name>
  <files>AppPackage/Sources/DetailFeature/GalleryNavigation.swift</files>
  <read_first>
    - AppPackage/Sources/DetailFeature/GalleryNavigation.swift lines 7-19 — `routeGalleryDetail(deviceType:present:push:)`
      = `deviceType() == .pad ? present() : push()`, the shared device branch every gallery host uses.
    - .planning/debug/phase-05-ipad-window-navigation.md (Eliminated hypotheses + Evidence 17:17/17:24:
      the routing contract is "iPad presents; iPhone pushes"; `5901ee63` is a direct `isPad()` →
      `deviceType() == .pad` substitution; the app-level `DetailView` sheet is reached ONLY through the
      `present` delegate; standard gallery taps still push on iPhone per the reducer test).
    - .planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md (navigation contract:
      iPad = modal present; non-pad = navigation push — the invariant to preserve).
    - The routing call sites to audit (each host's `routeGalleryDetail` and `pushGalleryDetail`):
      AppPackage/Sources/HomeFeature/HomeReducer+Body.swift lines 28-42 (galleryTapped + nested
      Frontpage/Popular/Toplists/Watched/History `pushDetail` delegates);
      AppPackage/Sources/SearchFeature/SearchRootReducer.swift line 112;
      AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift line 116;
      AppPackage/Sources/DownloadsFeature/DownloadsReducer.swift lines 115-129 (the tested path).
    - The modal presentation surface: AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift lines
      258-266 (`present` delegates → `appRoute(.presentGalleryDetail)`),
      AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift lines 27, 114 (the modal detail root),
      and AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift lines 94-112 (the app-level
      `DetailView` `.sheet`). Deep-link/URL/clipboard entry presents modally by design regardless of
      device — treat that as the known non-regression baseline, not the defect.
    - AppPackage/Tests/DownloadsFeatureTests (`DownloadsReducerActionTests`) — the existing pattern for
      asserting phone-push vs pad-present routing, to mirror for whatever path is fixed.
  </read_first>
  <action>
    Investigate first, then fix. Enumerate and exercise every non-pad gallery-detail entry path on an
    iPhone destination: each host's `galleryTapped`/`routeGalleryDetail` (Home + its nested
    Frontpage/Popular/Toplists/Watched/History delegates, Search/SearchRoot, Favorites, Downloads),
    the Home carousel and cover-wall/toplists taps, Detail's own onward gallery navigation
    (comments/detailSearch `pushDetail`), and the app-level `DetailView` `.sheet` reached via the
    `present` delegate. For each, determine on iPhone whether detail is pushed onto the host's
    navigation stack or presented modally. Identify any routing site that presents a modal on iPhone in
    violation of the `deviceType() != .pad ? push` contract — i.e. any host that routes to the `present`
    delegate (or otherwise reaches the app-level sheet) for non-pad, or bypasses `routeGalleryDetail`
    with an unconditional present. If found, restore navigation-push for that non-pad path while keeping
    iPad on the modal present (do not alter the iPad branch, the deep-link/URL/clipboard modal baseline,
    or the shared `routeGalleryDetail` semantics for pad), and add/extend a reducer test mirroring
    `DownloadsReducerActionTests` that asserts the fixed host pushes on iPhone and presents on iPad. If,
    after exhausting every enumerated entry path, no modal-presenting iPhone path can be reproduced,
    do NOT silently pass: record in the SUMMARY the exhaustive entry-path list, the per-path push/present
    result, and the evidence, and flag the finding for human confirmation (whether the user's report was
    a specific unlisted path or a since-fixed transient). Adjust this plan's `files_modified` in the
    SUMMARY to the actual set touched by the fix (if any).
  </action>
  <verify>
    <automated>xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' test -only-testing:DownloadsFeatureTests/DownloadsReducerActionTests -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every non-pad gallery-detail entry path is enumerated AND exercised (list recorded in the SUMMARY with each path's push/present result on iPhone).
    - EITHER: the push-contract-bypassing routing site is identified and restored to navigation-push on iPhone while iPad still presents modally, AND a reducer test (mirroring `DownloadsReducerActionTests`) asserts the fixed host pushes on phone / presents on pad and passes; the deep-link/URL/clipboard modal baseline and the shared `routeGalleryDetail` pad branch are unchanged.
    - OR: no modal-on-iPhone path reproduced after exhausting the entry points — the SUMMARY records the exhaustive enumeration + evidence and explicitly flags the finding for human confirmation (not a silent pass).
    - `DownloadsReducerActionTests` still passes (the standard phone-push / pad-present contract is intact); relevant xcodebuild invocation run alone (never overlap xcodebuild).
    - Human-check (record in SUMMARY): on iPhone, opening a gallery detail from Home, Search, Favorites, and Downloads pushes (no modal sheet); on iPad it still presents modally.
  </acceptance_criteria>
  <done>The iPhone modal-detail routing site is identified and restored to push (with a passing routing test) preserving the iPad-modal / non-pad-push contract, or a no-repro finding is documented and flagged for human confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Navigation routing (push vs modal present) is presentation-only; it crosses no security boundary and adds no input, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | (none) | Gallery detail routing (push vs present) | low | accept | Presentation-routing-only investigation/fix; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **produces**: an exhaustive enumeration of non-pad gallery-detail entry paths and their
iPhone push/present result; and EITHER a routing fix restoring navigation-push for the offending
non-pad host (plus a mirroring reducer test) while preserving the iPad-modal / non-pad-push contract,
OR a documented no-repro finding flagged for human confirmation. The actual `files_modified` set is
finalized in the SUMMARY (the candidate fix site is `GalleryNavigation.swift` and/or the specific
host reducer that bypasses the push contract).

<verification>
- `DownloadsReducerActionTests` green (standard phone-push / pad-present contract intact).
- Enumeration + per-path result recorded in SUMMARY.
- Fixed-path routing test green (if a fix was applied) OR no-repro finding flagged for human confirmation.
- Human-check: iPhone gallery detail pushes across hosts; iPad still presents modally.
</verification>

<success_criteria>
Every non-pad gallery host pushes gallery detail on iPhone (iPad still presents), or the absence of a reproducible modal-on-iPhone path is documented and surfaced for human confirmation — closing G-05-4 defect 10 without silently passing.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-18-SUMMARY.md` when done.
</output>
