---
phase: 05-adaptive-layout-universal-orientation
plan: 07
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - AppPackage/Sources/SettingFeature/Login/LoginView.swift
  - AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift
  - AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift
autonomous: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "No `GeometryReader` remains in `AppPackage/Sources` — all three legacy sites read size via `onGeometryChange`/`containerRelativeFrame`."
    - "The `LiveTextView` OCR overlay boxes map onto the captured size pixel-identically before/after (Canvas fill + `.position`/`.frame` overlay share ONE size)."
  artifacts: []
  key_links:
    - "`LiveTextView`'s `Canvas` closure and its `ForEach(.position)` overlay both read the same captured `size`, guarded against the one-frame `.zero` first pass."
---

<objective>
Eliminate the three pre-existing `GeometryReader` sites so the codebase is GeometryReader-free
(D-06/D-06b): `LoginView` decorative wave offsets and `GalleryInfosView` `proxy.size.width / 3`
column width convert to `onGeometryChange`/`containerRelativeFrame`; `LiveTextView`'s OCR-box `Canvas`
overlay converts with a careful coordinate-mapping parity check (the delicate one).

Purpose: Satisfy UIARCH-01 acceptance #1 ("`GeometryReader` is avoided"). `onGeometryChange` has zero
existing occurrences in the tree, so the implementation follows the cited Apple API + 05-RESEARCH
Code Examples, not a local analog.
Output: three GeometryReader sites converted; `grep -r GeometryReader AppPackage/Sources` empty.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoginView + GalleryInfosView GeometryReader → onGeometryChange / containerRelativeFrame</name>
  <files>AppPackage/Sources/SettingFeature/Login/LoginView.swift, AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Login/LoginView.swift lines 23-47 (the greedy full-body `GeometryReader { proxy in ... .offset(y: proxy.size.height * 0.3) ... .padding(.horizontal, proxy.size.width * 0.2) }`).
    - AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift lines 93-100 (`GeometryReader { proxy in List(infos) { ... .frame(width: proxy.size.width / 3) ... } }`).
    - 05-RESEARCH.md § Architecture Patterns Pattern 1 (`onGeometryChange(for: CGSize.self) { $0.size } action:`, `Equatable`-gated) and § State of the Art (GeometryReader → onGeometryChange).
    - 05-PATTERNS.md § "Native geometry capture (`onGeometryChange`) — NEW TO CODEBASE" (cite Apple docs; result type must be `Equatable`).
    - Apple docs: developer.apple.com/documentation/swiftui/view/ongeometrychange(for:of:action:)
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro.
  </read_first>
  <action>
    In `LoginView.swift` remove the wrapping `GeometryReader`; capture the container size into a
    `@State private var size: CGSize = .zero` via `.onGeometryChange(for: CGSize.self) { $0.size }
    action: { size = $0 }` on the (now un-wrapped) body, and drive the decorative offsets from it
    (`.offset(y: size.height * 0.3)`, `.padding(.horizontal, size.width * 0.2)`). Since the layout is
    decorative, guard the one-frame `size == .zero` first pass is acceptable (same first-pass behavior
    as GeometryReader). In `GalleryInfosView.swift` remove the `GeometryReader` wrapping the `List`;
    replace `.frame(width: proxy.size.width / 3)` on the title `HStack` with
    `.containerRelativeFrame(.horizontal) { width, _ in width / 3 }` (the `List` no longer needs a
    geometry wrapper). Preserve the row layout and copy-button behavior.
  </action>
  <verify>
    <automated>! grep -n "GeometryReader" AppPackage/Sources/SettingFeature/Login/LoginView.swift AppPackage/Sources/DetailFeature/GalleryInfos/GalleryInfosView.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains `GeometryReader`.
    - `LoginView` reads size via `onGeometryChange`; `GalleryInfosView` uses `containerRelativeFrame`.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>Both easy GeometryReader sites are converted with preserved layout; no `GeometryReader` in either file.</done>
</task>

<task type="auto">
  <name>Task 2: LiveTextView Canvas OCR overlay → onGeometryChange (pixel-identical mapping)</name>
  <files>AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift lines 20-95 (the `GeometryReader { proxy in }` reading `proxy.size`, the `Canvas` fill/path math using `bounds.topLeft * size` etc., and the `ForEach` overlay `.frame(width: group.width * width, ...)` + `.position(x: (group.minX + group.width/2) * width, y: ...)`).
    - 05-RESEARCH.md § Code Examples "`LiveTextView` Canvas without GeometryReader (D-06b — the delicate one)" — the `@State size` + `onGeometryChange(for: CGSize.self)` shape, "Canvas closure may use its own `canvasSize` for fill/path; the `.position`/`.frame` overlay must read the SAME captured `size`", and the parity check + `.zero` first-pass guard.
    - 05-VALIDATION.md § Manual-Only Verifications (LiveTextView pixel-accuracy; add SnapshotTesting only if a cheap fixture exists).
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro (Canvas; `onGeometryChange`).
  </read_first>
  <action>
    Remove the `GeometryReader`; add `@State private var size: CGSize = .zero` and attach
    `.onGeometryChange(for: CGSize.self) { $0.size } action: { size = $0 }` to the container (the
    `ZStack` holding the `Canvas` + overlay). In the `Canvas` closure use the closure's own
    `canvasSize` argument for the fill/path branch where available; use the captured `size` for every
    `ForEach` `.position`/`.frame` overlay computation so BOTH layers share one size. Preserve the
    exact normalized-corner arithmetic (`group.width * size.width`, `(group.minX + group.width/2) *
    size.width`, and the height analogues). Guard the one-frame `size == .zero` first pass (overlay
    renders at 0 until the first `onGeometryChange`, matching GeometryReader's first pass — confirm no
    visible flash). Encode a pixel-identical parity expectation: OCR selection boxes must land on the
    same glyphs as before, in portrait & landscape, single & dual-page.
  </action>
  <verify>
    <automated>! grep -n "GeometryReader" AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift 2>/dev/null && grep -q "onGeometryChange" AppPackage/Sources/ReadingFeature/Support/LiveTextView.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `LiveTextView.swift` contains no `GeometryReader` and reads size via `onGeometryChange`.
    - The `Canvas` fill/path and the `ForEach` overlay use one consistent size; the normalized mapping arithmetic is unchanged.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
    - Manual UAT (recorded for phase gate): OCR boxes align with glyphs in portrait & landscape, single & dual-page (05-VALIDATION Manual-Only).
  </acceptance_criteria>
  <done>LiveTextView maps OCR boxes onto a captured size pixel-identically without GeometryReader; the `.zero` first pass is guarded; manual parity check recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-layer geometry-read conversion; OCR boxes are derived from already-decoded local image data, not new external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | (none) | GeometryReader → onGeometryChange | low | accept | No security-relevant surface. Geometry is OS-supplied. Privacy blur / auto-lock untouched (Phase 7). |
</threat_model>

## Artifacts this phase produces

This plan **removes**: all remaining `GeometryReader` occurrences (LoginView, GalleryInfosView, LiveTextView).
This plan **creates**: `onGeometryChange`-captured sizes in LoginView and LiveTextView (new-to-codebase `onGeometryChange` idiom).
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- `xcodebuild build -scheme AppPackage-Package` compiles clean.
- SwiftLint clean.
- `grep -rn "GeometryReader" AppPackage/Sources` → empty.
</verification>

<success_criteria>
The codebase is GeometryReader-free; LoginView/GalleryInfosView layout preserved; LiveTextView OCR-box mapping is pixel-identical (manual UAT recorded); build/lint green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-07-SUMMARY.md` when done.
</output>
