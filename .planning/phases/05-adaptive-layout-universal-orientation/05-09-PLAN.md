---
phase: 05-adaptive-layout-universal-orientation
plan: 09
type: execute
wave: 9
depends_on: [05-08]
files_modified:
  - AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift
  - AppPackage/Sources/ReadingFeature/Support/GestureHandler.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift
  - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift
  - AppPackage/Sources/AppTools/TouchHandler.swift
  - AppPackage/Sources/AppFeature/RootView.swift
autonomous: true
requirements: [UIARCH-01, UIARCH-03]

must_haves:
  truths:
    - "Tap location comes from `SpatialTapGesture.location` and pinch anchor from `MagnifyGesture.startAnchor`; `TouchHandler` (module + `.shared`) and `RootView.addTouchHandler` are gone."
    - "The reader's `isLandscape` (dual-page eligibility) is `containerSize.width > containerSize.height` from the single captured size (D-04) — no `DeviceUtil.isLandscape`/`windowW` read anywhere in ReadingFeature."
    - "Reading zoom/pan/tap parity holds: `GestureHandlerTests` + `PageHandlerTests` stay green after the source swap."
  artifacts: []
  key_links:
    - "The single `onGeometryChange` size (from 05-08) feeds the D-04 landscape flag, the dual-page half-width, and — threaded down — `ControlPanel`'s layout."
    - "Tap-to-turn stays gated to `scale == 1` (`.gesture(tapGesture, isEnabled: gestureHandler.scale == 1)`), where local == layout, so the captured-width normalization is exact."
---

<objective>
Complete the reader re-plumb (D-04/D-05, UIARCH-01 + the reader half of UIARCH-03): swap the gesture
SOURCES to native (`SpatialTapGesture.location`, `MagnifyGesture.startAnchor`); replace every reader
`DeviceUtil.isLandscape`/`windowW` read with the D-04 aspect-ratio flag derived from the single
captured `containerSize`; convert `ReadingViewComponents`/`ControlPanel` metrics; swap the reader
`!isPad` idiom to `deviceClient.deviceType()`; and delete `TouchHandler` + `RootView.addTouchHandler`.

Purpose: With the arithmetic locked by 05-08's `GestureHandlerTests`, this plan changes only the
SOURCES — so any coordinate drift turns a green test red. It retires the `TouchHandler` global (the
last reader `.shared`) and makes dual-page eligibility follow container shape, correctly enabling
dual-page on now-rotatable landscape phones (UIARCH-03).
Output: native reader gestures; `TouchHandler` deleted; ReadingFeature free of `DeviceUtil`; tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap gesture sources to SpatialTapGesture.location + MagnifyGesture.startAnchor</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift, AppPackage/Sources/ReadingFeature/Support/GestureHandler.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift (post-05-08: `TapGesture(count:1/2)` passing `TouchHandler.shared.currentPoint`; `MagnificationGesture()` passing `value` + a location).
    - AppPackage/Sources/ReadingFeature/Support/GestureHandler.swift (post-05-08 pure methods: `onSingleTapGestureEnded(location:...)`, `onDoubleTapGestureEnded(location:...)`, `onMagnificationGestureChanged(value:location:...)` calling `correctScaleAnchor(point:)`).
    - 05-RESEARCH.md § Architecture Patterns Pattern 2 (the SpatialTapGesture/MagnifyGesture example + coordinate-space equivalence), § Common Pitfalls #1 (scale==1 gate) and #2 (`value.magnification` not the bare value; `.startAnchor` replaces `correctScaleAnchor`).
    - 05-PATTERNS.md § "`ReadingView+Gestures.swift`".
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro (gesture value handling).
  </read_first>
  <action>
    In `ReadingView+Gestures.swift`: change the single-tap and double-tap from `TapGesture(count:1/2)`
    to `SpatialTapGesture(count: 1/2, coordinateSpace: .local)` and pass `value.location` (a real
    `CGPoint`) into `onSingleTapGestureEnded(location:...)` / `onDoubleTapGestureEnded(location:...)`
    — replacing the `TouchHandler.shared.currentPoint` source. Change `MagnificationGesture()` to
    `MagnifyGesture()`; in `onChanged`/`onEnded` pass `value.magnification` (NOT the bare value) and
    `value.startAnchor` (a `UnitPoint`). In `GestureHandler.swift` change the magnify methods to accept
    `anchor: UnitPoint` and set `scaleAnchor = anchor` directly (this is the `correctScaleAnchor`
    replacement for the pinch path per D-05 — the 05-08 equivalence test guards it); keep
    `onDoubleTapGestureEnded(location:)` using `correctScaleAnchor(point: location)` (SpatialTapGesture
    supplies a `CGPoint`). Preserve `ExclusiveGesture(doubleTap, singleTap)`, the `DragGesture`
    (unchanged — already `.local`), and every ReadingView `isEnabled:`/`highPriorityGesture` gate,
    especially `.gesture(tapGesture, isEnabled: gestureHandler.scale == 1)` and
    `.scrollDisabled(gestureHandler.scale != 1)`.
  </action>
  <verify>
    <automated>grep -q "SpatialTapGesture" AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift && grep -q "MagnifyGesture" AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift && grep -q "startAnchor" AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift && ! grep -En "TouchHandler|MagnificationGesture" AppPackage/Sources/ReadingFeature/ReadingView+Gestures.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ReadingView+Gestures.swift` uses `SpatialTapGesture(coordinateSpace: .local)` (location) and `MagnifyGesture` (`value.magnification` + `value.startAnchor`); no `TouchHandler` / `MagnificationGesture`.
    - The pinch anchor path sets `scaleAnchor = startAnchor`; double-tap still uses `correctScaleAnchor(point:)` with the tap location.
    - The scale==1 tap gate and scale!=1 scroll-disable wiring are unchanged.
    - `xcodebuild test -scheme AppPackage-Package -only-testing:ReadingFeatureTests/GestureHandlerTests` exits 0 (regression guard stays green).
  </acceptance_criteria>
  <done>Reader gestures are native (SpatialTapGesture/MagnifyGesture); the anchor uses `startAnchor`; GestureHandlerTests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the D-04 captured-size landscape flag; convert Components + ControlPanel; reader idiom → deviceType()</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift, AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingView.swift lines 82 (`!DeviceUtil.isPad && DeviceUtil.isLandscape`), 133/181/246/279/283/321/344 (`isLandscape: DeviceUtil.isLandscape` args to `containerDataSource`/`imageContainerConfigs`), 252/337 (the `mapFromPager`/`mapToPager` calls that 05-08 threaded with `DeviceUtil.isLandscape`).
    - AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift lines 178-182 (`ImageContainer.width = DeviceUtil.windowW / (isDualPage ? 2 : 1)` — placeholder/error sizing only; the loaded image uses `scaledToFit`).
    - AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift lines 59 (`!DeviceUtil.isPad && DeviceUtil.isLandscape`), 156 (`DeviceUtil.isLandscape && direction != .vertical`), 286 (`DeviceUtil.windowW * 0.6`), 340/360/364 (`DeviceUtil.isPadWidth` font/count, nested `DeviceUtil.isLandscape`), 386 (`(DeviceUtil.windowW - spacing) / count`).
    - 05-RESEARCH.md § Common Pitfalls #3 (`windowW` placeholder-only → container fraction is benign) and #4 (`isPadWidth` → `horizontalSizeClass`, reviewed delta); § Architecture Patterns (D-04 `isLandscape = width > height`).
    - `.claude/skills` pfw-modern-swiftui / swiftui-pro.
  </read_first>
  <action>
    Define the D-04 landscape flag in `ReadingView` as `gestureHandler.containerSize.width >
    gestureHandler.containerSize.height` (the single captured size from 05-08) and use it in place of
    every `DeviceUtil.isLandscape` argument to `containerDataSource`/`imageContainerConfigs`/`imageStack`
    and the `mapFromPager`/`mapToPager` calls (lines 252, 337). Swap the line-82 reader-sheet idiom
    `!DeviceUtil.isPad && DeviceUtil.isLandscape` to `deviceClient.deviceType() != .pad && <captured
    landscape>` (add `@Dependency(\.deviceClient)`). In `ReadingViewComponents.swift` replace
    `ImageContainer.width = DeviceUtil.windowW / (isDualPage ? 2 : 1)` with a container-relative
    placeholder width — apply `.containerRelativeFrame(.horizontal) { w, _ in w / (isDualPage ? 2 : 1)
    }` to the placeholder/error frame (Pitfall #3: placeholder-only, benign delta; the loaded image
    keeps `scaledToFit`). In `ControlPanel.swift` thread the captured `containerSize` down from
    `ReadingView` as an init parameter, derive `isLandscape = containerSize.width >
    containerSize.height` (single source, D-05), and use `containerSize.width` for the `* 0.6` and
    `(width - spacing) / count` sites; replace `DeviceUtil.isPad` with `@Dependency(\.deviceClient)
    .deviceType() == .pad` and each `DeviceUtil.isPadWidth` with `@Environment(\.horizontalSizeClass)
    == .regular` (reviewed delta). No `DeviceUtil` read may remain in any of the three files.
  </action>
  <verify>
    <automated>! grep -rn "DeviceUtil" AppPackage/Sources/ReadingFeature/ReadingView.swift AppPackage/Sources/ReadingFeature/ReadingViewComponents.swift AppPackage/Sources/ReadingFeature/Support/ControlPanel.swift 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - None of ReadingView / ReadingViewComponents / ControlPanel reference `DeviceUtil`.
    - The reader `isLandscape` everywhere is `containerSize.width > containerSize.height`; ControlPanel receives the captured size from ReadingView (one source, D-05).
    - The reader `!isPad` / `isPad` idioms read `deviceClient.deviceType()`; `isPadWidth` sites use `horizontalSizeClass`.
    - `xcodebuild test -scheme AppPackage-Package -only-testing:ReadingFeatureTests` exits 0.
  </acceptance_criteria>
  <done>The D-04 aspect-ratio flag from the single captured size drives all reader dual-page eligibility; Components + ControlPanel metrics are native; reader idioms read `deviceType()`; ReadingFeature is `DeviceUtil`-free.</done>
</task>

<task type="auto">
  <name>Task 3: Delete TouchHandler and RootView.addTouchHandler</name>
  <files>AppPackage/Sources/AppTools/TouchHandler.swift, AppPackage/Sources/AppFeature/RootView.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/TouchHandler.swift (whole file — the `TouchHandler: NSObject` + `.shared` global).
    - AppPackage/Sources/AppFeature/RootView.swift lines 14-24 (`onAppear(perform: addTouchHandler)` + the `addTouchHandler()` method installing a `UITapGestureRecognizer` on `DeviceUtil.keyWindow`).
    - 05-PATTERNS.md § "Deletion touch-point pruning" (TouchHandler consumers: GestureHandler [done 05-08], `DeviceClient.touchPoint` [removed 05-01], `RootView.addTouchHandler`).
    - AGENTS.md § "Remove emptied actions" (remove the call site with the method — no empty stub).
  </read_first>
  <action>
    Delete `AppPackage/Sources/AppTools/TouchHandler.swift`. In `RootView.swift` remove the
    `.onAppear(perform: addTouchHandler)` modifier from the `TabBarView` and delete the
    `addTouchHandler()` method entirely (it read `TouchHandler.shared` + `DeviceUtil.keyWindow`, both
    gone) — leave `TabBarView(...).accentColor(.primary)` as the body; remove any now-unused `import
    AppTools`/`import UIKit` only if nothing else in the file uses them. Confirm no other `TouchHandler`
    reference remains anywhere (`DeviceClient.touchPoint` was already removed in 05-01, GestureHandler
    in 05-08).
  </action>
  <verify>
    <automated>! test -e AppPackage/Sources/AppTools/TouchHandler.swift && ! grep -rn "TouchHandler\|addTouchHandler" AppPackage/Sources 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/AppTools/TouchHandler.swift` does not exist.
    - `grep -rn "TouchHandler" AppPackage/Sources` returns nothing; `RootView` has no `addTouchHandler`.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>`TouchHandler` and `RootView.addTouchHandler` are fully deleted; no `TouchHandler` reference remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesture input → reader math | Tap/pinch coordinates are OS-supplied SwiftUI gesture values; retiring the `TouchHandler` UIKit global does not add an input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | (none) | reader gesture/geometry source swap | low | accept | No security-relevant surface. Removing an orientation-derived layout branch is not a confidentiality change (orientation is not a security boundary — D-09). Privacy blur / biometric auto-lock untouched (Phase 7). GestureHandlerTests guard behavior parity. |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `TouchHandler` (module type + `.shared`), `RootView.addTouchHandler`, and all
reader `DeviceUtil.isLandscape`/`windowW` reads (ReadingView / ReadingViewComponents / ControlPanel);
replaces the tap/pinch source with `SpatialTapGesture.location` / `MagnifyGesture.startAnchor`.
This plan **creates**: the D-04 `isLandscape = containerSize.width > containerSize.height` derived
flag; a `containerSize` parameter threaded into `ControlPanel`.
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- `xcodebuild test -scheme AppPackage-Package -only-testing:ReadingFeatureTests` green (GestureHandlerTests + PageHandlerTests + ContainerDataSourceTests) — the regression guard confirms zoom/pan/tap parity.
- SwiftLint clean.
- `grep -rn "DeviceUtil\|TouchHandler\|MagnificationGesture" AppPackage/Sources/ReadingFeature AppPackage/Sources/AppTools/TouchHandler.swift` → empty.
- Manual UAT (phase gate, 05-VALIDATION): rotate reader portrait/landscape, single & dual-page, RTL; confirm zoom/pan/tap and page-turn zones behave identically, dual-page toggles on rotation, and landscape dual-page RESUME opens on the correct page (the init self-correction from 05-08).
</verification>

<success_criteria>
Reader tap/pinch use native gesture values; `TouchHandler` is deleted; dual-page eligibility follows container aspect ratio from the single captured size; zoom/pan/tap parity is green (GestureHandlerTests) and dual-page maps stay green (PageHandlerTests); ReadingFeature is `DeviceUtil`-free; build/lint green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-09-SUMMARY.md` when done.
</output>
