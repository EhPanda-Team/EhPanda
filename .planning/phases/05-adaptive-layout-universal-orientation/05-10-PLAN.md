---
phase: 05-adaptive-layout-universal-orientation
plan: 10
type: execute
wave: 10
depends_on: [05-09]
files_modified:
  - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
  - AppPackage/Sources/ApplicationClient/ApplicationClient.swift
  - AppPackage/Sources/AppTools/DeviceUtil.swift
autonomous: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "No view reads `DeviceUtil.window*`/`screen*`/`absWindow*` for layout; `DeviceUtil` no longer exists — layout is size-class / container-relative and `GeometryReader` is gone (Success Criterion 1)."
    - "`TouchHandler` is retired via `SpatialTapGesture.location` + `MagnifyGesture.startAnchor`, reading zoom/pan/tap parity preserved (Success Criterion 2)."
    - "`Defaults.FrameSize`/`ImageSize` no longer derive size from a global — the device-derived computed props are dissolved (Success Criterion 3)."
    - "All pages rotate with the device; `AppOrientationMask`, `AppDelegateClient.setOrientation*`, `setOrientationPortrait`, and `Setting.enablesLandscape` are removed (Success Criterion 4)."
  artifacts:
    - "ApplicationClient's private window-scene lookup (replacing DeviceUtil.keyWindow/anyWindow)"
  key_links:
    - "Deleting `DeviceUtil.swift` is safe only after every metric/idiom/Defaults/reader consumer (05-01…05-09) is migrated — this plan is the terminal gate."
---

<objective>
Terminal cleanup for the phase (D-07 tail + seam f + the `DeviceUtil` deletion): dissolve the
device-derived `Defaults.FrameSize`/`ImageSize` props now that every consumer is native; rehome
`ApplicationClient`'s non-layout window access into a private inline window-scene lookup; delete
`DeviceUtil.swift`; and prove the four phase success criteria with residual grep gates + a full green
suite.

Purpose: This is the last consumer removal that lets `DeviceUtil` be deleted. It also finishes D-07
by removing the orphaned device-derived `Defaults` props (all consumers were converted in
05-03…05-06) and rehomes the one non-layout `DeviceUtil.keyWindow` use (`overrideUserInterfaceStyle`),
which is not a layout metric and needs a local home rather than a resurrected global.
Output: `DeviceUtil` gone; `Defaults` device-props dissolved; ApplicationClient self-contained; all
phase gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-CONTEXT.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-RESEARCH.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dissolve the device-derived Defaults props</name>
  <files>AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift lines 9-31 (`FrameSize`: `archiveGridWidth`, `cardCellWidth`, `cardCellHeight` [KEEP — genuine constant], `cardCellSize`, `rankingCellWidth`, `alertWidthFactor`; `ImageSize` extension: `previewMinW`, `previewMaxW`, `previewAvgW`). The `Defaults.URL` extension (lines 33-43, uses `AppUtil`) is UNRELATED and stays.
    - 05-CONTEXT.md D-07 (dissolve device-derived props to per-site native; keep genuine constants like `cardCellHeight`).
    - Confirm (grep) no remaining consumer references these props before deleting them — all were converted in 05-03 (nothing here), 05-05 (Detail/preview), 05-06 (Home). `alertWidthFactor` had zero consumers (dead).
  </read_first>
  <action>
    Remove the device-derived computed props from `Defaults+Runtime.swift`: from `Defaults.FrameSize`
    delete `archiveGridWidth`, `cardCellWidth`, `cardCellSize`, `rankingCellWidth`, and
    `alertWidthFactor`; from the `Defaults.ImageSize` extension delete `previewMinW`, `previewMaxW`,
    and `previewAvgW`. KEEP `cardCellHeight` (a genuine constant). If `FrameSize` no longer needs
    `@MainActor` (only a non-isolated `static let cardCellHeight` remains), drop the isolation; keep
    the struct otherwise intact. Leave the `Defaults.URL` extension and its `import AppTools` (for
    `AppUtil`) untouched. Before committing, grep the whole source tree to confirm none of the deleted
    prop names remain referenced.
  </action>
  <verify>
    <automated>! grep -rnE "archiveGridWidth|cardCellWidth|cardCellSize|rankingCellWidth|alertWidthFactor|previewMinW|previewMaxW|previewAvgW" AppPackage/Sources 2>/dev/null && grep -q "cardCellHeight" AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "archiveGridWidth|cardCellWidth|cardCellSize|rankingCellWidth|alertWidthFactor|previewMinW|previewMaxW|previewAvgW" AppPackage/Sources` returns nothing.
    - `Defaults.FrameSize.cardCellHeight` still exists.
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>The device-derived `Defaults.FrameSize`/`ImageSize` props are dissolved; only genuine constants remain; no consumer references them.</done>
</task>

<task type="auto">
  <name>Task 2: Rehome ApplicationClient window access into a private inline lookup</name>
  <files>AppPackage/Sources/ApplicationClient/ApplicationClient.swift</files>
  <read_first>
    - AppPackage/Sources/ApplicationClient/ApplicationClient.swift line 38 (`(DeviceUtil.keyWindow ?? DeviceUtil.anyWindow)?.overrideUserInterfaceStyle = userInterfaceStyle`) and line 3 (`import AppTools`, still needed for `FileUtil` at line 49).
    - AppPackage/Sources/AppTools/DeviceUtil.swift lines 21-31 (the `keyWindow` = connectedScenes filter `foregroundActive` → `UIWindowScene` → `isKeyWindow`; `anyWindow` = last window of last scene) — the exact lookup to inline.
    - 05-RESEARCH.md § Don't Hand-Roll (window lookup for `overrideUserInterfaceStyle` → inline a private window-scene lookup locally, since `DeviceUtil` is deleted and this is a non-layout side-effect).
  </read_first>
  <action>
    Add a private `@MainActor` window-scene lookup to `ApplicationClient` (a `private` helper
    replicating `DeviceUtil.keyWindow` — `UIApplication.shared.connectedScenes` filtered to
    `.foregroundActive`, compactMapped to `UIWindowScene`, last `isKeyWindow` window — with the
    `anyWindow` fallback = last window of the last scene). Use it in the `setUserInterfaceStyle`
    closure to set `overrideUserInterfaceStyle`, replacing the `DeviceUtil.keyWindow ??
    DeviceUtil.anyWindow` read. Keep `import AppTools` (still used for `FileUtil`). Do not change any
    other `ApplicationClient` behavior.
  </action>
  <verify>
    <automated>! grep -n "DeviceUtil" AppPackage/Sources/ApplicationClient/ApplicationClient.swift 2>/dev/null && grep -q "overrideUserInterfaceStyle" AppPackage/Sources/ApplicationClient/ApplicationClient.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ApplicationClient.swift` references no `DeviceUtil`; a private window-scene lookup sets `overrideUserInterfaceStyle`.
    - `import AppTools` remains (used by `FileUtil`).
    - `xcodebuild build -scheme AppPackage-Package` compiles.
  </acceptance_criteria>
  <done>The non-layout window access is inlined privately in ApplicationClient; no `DeviceUtil` remains there.</done>
</task>

<task type="auto">
  <name>Task 3: Delete DeviceUtil.swift and prove all four phase success criteria</name>
  <files>AppPackage/Sources/AppTools/DeviceUtil.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/DeviceUtil.swift (whole file — confirm every getter now has zero consumers after 05-01…05-09 + Tasks 1-2 here).
    - .planning/ROADMAP.md § "Phase 5" 4 Success Criteria.
    - 05-VALIDATION.md § Sampling Rate / § Manual-Only Verifications (full suite green + device rotation UAT for UIARCH-03).
  </read_first>
  <action>
    Delete `AppPackage/Sources/AppTools/DeviceUtil.swift`. Run the residual grep gates below and the
    full test suite; any non-empty grep or red test means a missed consumer — fix at its root (do not
    reinstate `DeviceUtil`). Do a clean build to clear any stale `.build` artifacts. Record the
    device-rotation UAT result for UIARCH-03 (reader portrait/landscape single & dual-page + RTL; grid
    ~4-column landscape phone per D-08; detail/home) in the SUMMARY as the phase manual gate.
  </action>
  <verify>
    <automated>! test -e AppPackage/Sources/AppTools/DeviceUtil.swift && ! grep -rn "DeviceUtil" AppPackage/Sources 2>/dev/null && ! grep -rn "GeometryReader" AppPackage/Sources 2>/dev/null && ! grep -rnE "TouchHandler|AppOrientationMask|AppDelegateClient|enablesLandscape|setOrientationPortrait" AppPackage/Sources 2>/dev/null && grep -rq "onGeometryChange" AppPackage/Sources && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/AppTools/DeviceUtil.swift` does not exist and `grep -rn "DeviceUtil" AppPackage/Sources` returns nothing (Criterion 1).
    - `grep -rn "GeometryReader" AppPackage/Sources` returns nothing; `grep -rq "onGeometryChange" AppPackage/Sources` succeeds (Criterion 1).
    - `grep -rnE "TouchHandler|AppOrientationMask|AppDelegateClient|enablesLandscape|setOrientationPortrait" AppPackage/Sources` returns nothing (Criteria 2 + 4).
    - `xcodebuild test -scheme AppPackage-Package` full suite exits 0 (incl. ReadingFeatureTests/GestureHandlerTests + PageHandlerTests — Criterion 2 parity).
    - Device-rotation UAT recorded in SUMMARY (Criterion 4 / UIARCH-03).
  </acceptance_criteria>
  <done>`DeviceUtil` is deleted; all four ROADMAP success criteria are grep-/test-/UAT-verifiable and pass; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Removing a global + inlining a window lookup crosses no security boundary; `overrideUserInterfaceStyle` is a cosmetic side-effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | (none) | DeviceUtil deletion / Defaults dissolution / ApplicationClient window lookup | low | accept | No security-relevant surface introduced or removed. The inlined window lookup is the same connected-scene enumeration as before, used only to set the interface style. Privacy blur / biometric auto-lock untouched (Phase 7). Orientation lock (removed in 05-02) is not a confidentiality control (D-09). |
</threat_model>

## Artifacts this phase produces

This plan **removes**: `DeviceUtil` (whole file), the device-derived `Defaults.FrameSize`
(`archiveGridWidth`/`cardCellWidth`/`cardCellSize`/`rankingCellWidth`/`alertWidthFactor`) and
`Defaults.ImageSize` (`previewMinW`/`previewMaxW`/`previewAvgW`) props, and `ApplicationClient`'s
`DeviceUtil.keyWindow`/`anyWindow` reads.
This plan **creates**: `ApplicationClient`'s private window-scene lookup helper. Keeps
`Defaults.FrameSize.cardCellHeight`.
(Phase-wide created/removed symbol set: see 05-01-PLAN.md "Artifacts this phase produces".)

<verification>
- Full suite: `xcodebuild test -scheme AppPackage-Package` green.
- SwiftLint clean (no suppressions except the one owner-authorized `// reason:`-ed `DeviceType.tv` disable).
- Residual gates (all empty): `DeviceUtil`, `GeometryReader`, `TouchHandler`, `AppOrientationMask`, `AppDelegateClient`, `enablesLandscape`, `setOrientationPortrait` across `AppPackage/Sources`.
- Present: `onGeometryChange` in `AppPackage/Sources`.
- Manual: device-rotation UAT for UIARCH-03 recorded.
</verification>

<success_criteria>
All four ROADMAP Phase-5 success criteria hold and are verifiable: (1) no window/screen metric reads, no `GeometryReader`, breakpoints are size-class/container-relative; (2) `TouchHandler` retired via native gestures with zoom/pan/tap parity green; (3) `Defaults.FrameSize`/`ImageSize` no longer derive from a global; (4) all pages rotate, the custom orientation lock is fully removed. `DeviceUtil` is deleted; build/lint/full-suite green.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-10-SUMMARY.md` when done.
</output>
