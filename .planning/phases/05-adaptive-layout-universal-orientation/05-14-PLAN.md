---
phase: 05-adaptive-layout-universal-orientation
plan: 14
type: execute
wave: 14
depends_on: [05-13]
files_modified:
  - AppPackage/Sources/AppComponents/SettingTextField.swift
  - AppPackage/Sources/FiltersFeature/FiltersView.swift
  - AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift
  - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "The Filters page-range fields show no visible prompt/placeholder text (only the row's leading `Text(.pagesRange)` label), while VoiceOver still announces each field (G-05-1 defect 4, user-locked)."
    - "Every reusable sheet root presented from many hosts (Filters, Quick Search, Date Seek) has an untitled cancellation-role dismiss control (G-05-1 defect 5, user-locked)."
  artifacts:
    - "SettingTextField renders `title` as an accessibility label only; the visible in-field placeholder comes solely from `promptText`."
    - "FiltersView, QuickSearchView, DateSeekPickerView each install a `ToolbarItem(placement: .cancellationAction)` holding an untitled `Button(role: .cancel)` driven by `@Environment(\\.dismiss)`."
  key_links:
    - "`FiltersView.PagesRangeSetter` passes `title: .pagesRange` to two 50pt `SettingTextField`s; the redundant title must not leak as a clipped in-field placeholder."
    - "Neighboring roots (DownloadInspector, FolderManager, AppActivityLogs) already install cancellation actions — the same idiom applies here."
---

<objective>
Close two user-locked G-05-1 defects on reusable sheet content:

- Defect 4 (locked): the Filters page-range fields render a redundant `.pagesRange` title that leaks
  as a clipped in-field prompt inside the fixed 50pt fields (the row already shows a leading
  `Text(.pagesRange)`). Remove the visible prompt while keeping an accessibility label only.
- Defect 5 (locked): `FiltersView`, `QuickSearchView`, and `DateSeekPickerView` each build their own
  `NavigationStack` but expose no dismiss control; presented as `.sheet` from many hosts, drag-dismiss
  is their only escape. Add an untitled `Button(role: .cancel)` cancellation-role toolbar action to
  each.

Purpose: Unclipped filter controls and dismissible reusable sheets, exactly as the user dictated.
Output: page-range prompt gone (a11y retained); each reusable sheet has an untitled cancel action.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/debug/phase-05-layout-regressions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make SettingTextField's title accessibility-only (remove page-range visible prompt)</name>
  <files>AppPackage/Sources/AppComponents/SettingTextField.swift</files>
  <read_first>
    - AppPackage/Sources/AppComponents/SettingTextField.swift (whole file — the `prompt` computed
      property lines 17-20 from `promptText`, the `body` lines 35-45 with `TextField(text:prompt:) { Text(title) }`
      then `.labelsHidden()`, and the doc comment at lines 36-37 that claims "only the prompt shows").
    - AppPackage/Sources/FiltersFeature/FiltersView.swift — `PagesRangeSetter` (lines 183-196): the
      HStack renders a leading `Text(.pagesRange)` and two `SettingTextField(text:title: .pagesRange)`
      fixed at 50pt; the `title` must not leak as an in-field placeholder here.
    - The other four `SettingTextField` call sites to keep correct: `EhSettingView+Sections1.swift:53`
      and `EhSettingView+Sections2.swift:47` (both pass no `promptText`, wide `width: nil`, normally
      pre-filled) and `EhSettingView+Sections2.swift:88` (`promptText: .ratingsColorPrompt`, must still
      show its prompt).
    - Root `.swiftlint.yml` custom rules `accessibility_text_argument` (accessibility modifiers must
      receive a `LocalizedStringResource`/`String` directly, never wrapped in `Text`) and
      `accessibility_empty_string` (never pass an empty string to an accessibility modifier).
  </read_first>
  <action>
    Change `SettingTextField` so `title` is used ONLY as the accessibility label, never as a visible
    in-field placeholder — making the component honest to its own documented "only the prompt shows"
    contract. Give the `TextField` an empty visible label (so `title` no longer leaks as an in-field
    placeholder when `promptText` is nil) and apply `.accessibilityLabel(title)` passing the
    `LocalizedStringResource` directly (satisfies `accessibility_text_argument`; `title` is non-empty so
    `accessibility_empty_string` is satisfied — do NOT pass an empty string into any `.accessibility*`
    modifier). Keep the visible in-field placeholder sourced solely from `promptText` (the existing
    `prompt` computed property), so the `ratingsColor` field still shows `.ratingsColorPrompt` and the
    Filters page-range fields (which pass no `promptText`) show no visible prompt. Update the doc comment
    to state the new contract (title = accessibility label only; only `promptText` renders visibly).
    Do not change the `SettingTextField` initializer signature, `width`/`alignment`/`background`
    handling, keyboard/autocap modifiers, or any call site.
  </action>
  <verify>
    <automated>grep -q "accessibilityLabel(title)" AppPackage/Sources/AppComponents/SettingTextField.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accessibilityLabel(title)" AppPackage/Sources/AppComponents/SettingTextField.swift` is at least 1, and `title` is passed as a `LocalizedStringResource` (not wrapped in `Text`).
    - No empty string literal appears inside any `.accessibility*` modifier in the file (SwiftLint `accessibility_empty_string` / `accessibility_text_argument` clean).
    - The `SettingTextField` initializer signature is unchanged; all five call sites still compile.
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild).
    - Human-check (record in SUMMARY): Filters page-range fields show no visible prompt text (only the leading `Text(.pagesRange)`); the EhSetting ratings-color field still shows its `.ratingsColorPrompt`; VoiceOver announces the page-range fields.
  </acceptance_criteria>
  <done>The Filters page-range fields show no visible prompt while retaining an accessibility label; promptText-bearing fields still show their prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Add untitled cancellation-role dismiss to the three reusable sheet roots</name>
  <files>AppPackage/Sources/FiltersFeature/FiltersView.swift, AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift, AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift</files>
  <read_first>
    - AppPackage/Sources/FiltersFeature/FiltersView.swift (`body` NavigationStack + Form, lines 28-45 —
      no toolbar today).
    - AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift (`body` NavigationStack, lines 19-77,
      and the existing `toolbar()` returning a trailing `CustomToolbarItem`, lines 89-104).
    - AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift (`body` NavigationStack + Form,
      lines 31-86 — no toolbar today; store-agnostic reusable component).
    - Analog idiom to replicate exactly: AppPackage/Sources/DetailFeature/FolderManager/FolderManagerView.swift
      lines 115-119 (`@Environment(\.dismiss) private var dismiss` + `ToolbarItem(placement: .cancellationAction) { Button(role: .close, action: dismiss.callAsFunction) }`)
      and AppPackage/Sources/SettingFeature/AppActivityLogs/AppActivityLogsView.swift lines 141-145
      (`ToolbarItem(placement: .cancellationAction) { Button(role: .cancel, action: dismissAction) }`).
    - AppPackage/Sources/FavoritesFeature/FavoritesView.swift lines 56-77 — confirms these three views
      are presented as `.sheet(item:)`, so an `@Environment(\.dismiss)` inside the sheet content nils
      the host's item binding.
  </read_first>
  <action>
    Add an untitled cancellation-role dismiss control to each of the three reusable sheet roots
    (user-locked wording: `Button(role: .cancel)` with no title). In each view add
    `@Environment(\.dismiss) private var dismiss` and install a
    `ToolbarItem(placement: .cancellationAction) { Button(role: .cancel, action: dismiss.callAsFunction) }`
    on the view's own `NavigationStack` content:
    - FiltersView: attach a `.toolbar { ToolbarItem(placement: .cancellationAction) { Button(role: .cancel, action: dismiss.callAsFunction) } }` to the `Form` inside the `NavigationStack` (it has no toolbar yet).
    - QuickSearchView: extend the existing `toolbar()` to return the cancellation-action item alongside the current trailing `CustomToolbarItem` (wrap both in a `Group` so `toolbar()` still returns `some ToolbarContent`); the leading cancel item lives at `.cancellationAction`.
    - DateSeekPickerView: add the same `.toolbar { ToolbarItem(placement: .cancellationAction) { Button(role: .cancel, action: dismiss.callAsFunction) } }` to the `Form` inside its `NavigationStack`.
    Use the no-label `Button(role: .cancel, action:)` initializer (renders the standard system cancel
    affordance — no title), matching the analog roots. Do not add any localized string, do not change
    the sheet presentation sites, and do not alter the views' other content.
  </action>
  <verify>
    <automated>for f in AppPackage/Sources/FiltersFeature/FiltersView.swift AppPackage/Sources/QuickSearchFeature/QuickSearchView.swift AppPackage/Sources/DateSeekFeature/DateSeekPickerView.swift; do grep -q "cancellationAction" "$f" && grep -q "role: .cancel" "$f" || { echo "MISSING $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of `FiltersView.swift`, `QuickSearchView.swift`, `DateSeekPickerView.swift` contains a `ToolbarItem(placement: .cancellationAction)` holding a `Button(role: .cancel, ...)` (no title) and declares `@Environment(\.dismiss)`.
    - No new localized `.xcstrings` key was added (the cancel control has no title).
    - QuickSearchView's existing trailing add/edit `CustomToolbarItem` still renders (its `toolbar()` returns both items).
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild).
    - Human-check (record in SUMMARY): each sheet (Filters, Quick Search, Date Seek) presents an untitled cancel control that dismisses it; presenting hosts observe the sheet close.
  </acceptance_criteria>
  <done>All three reusable sheet roots expose an untitled `Button(role: .cancel)` cancellation-action that dismisses the sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Accessibility-label scoping on a text field and adding a system dismiss control cross no security boundary. No new input validation, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | (none) | SettingTextField a11y label + reusable sheet cancel actions | low | accept | UI-only changes; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **changes**: `SettingTextField` — `title` becomes an accessibility label only
(`.accessibilityLabel(title)`), visible placeholder now sourced solely from `promptText`.
This plan **adds**: an `@Environment(\.dismiss)` property and a
`ToolbarItem(placement: .cancellationAction)` with an untitled `Button(role: .cancel)` to each of
`FiltersView`, `QuickSearchView`, and `DateSeekPickerView`.

<verification>
- Build green (command above).
- SwiftLint clean (`accessibility_text_argument` / `accessibility_empty_string` satisfied; no suppressions).
- Grep gates: `.accessibilityLabel(title)` present in SettingTextField; `.cancellationAction` + `role: .cancel` present in all three sheet roots.
- Human-check: page-range prompt gone (a11y kept); each reusable sheet dismissible via its cancel control.
</verification>

<success_criteria>
The two user-locked G-05-1 fixes land exactly as dictated: the Filters page-range visible prompt is removed (accessibility label retained), and every reusable sheet root has an untitled cancellation-role dismiss control.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-14-SUMMARY.md` when done.
</output>