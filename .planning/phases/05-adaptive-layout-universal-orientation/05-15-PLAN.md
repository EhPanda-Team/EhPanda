---
phase: 05-adaptive-layout-universal-orientation
plan: 15
type: execute
wave: 15
depends_on: [05-14]
files_modified:
  - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
autonomous: true
gap_closure: true
requirements: [UIARCH-01]

must_haves:
  truths:
    - "The Favorites date-seek toolbar control is reachable and responds when date-seek metadata is available, and is clearly unavailable (not a dead top-level control) when it is absent (G-05-1 defect 6)."
  artifacts:
    - "FavoritesView toolbar regrouped so it does not overpack four independent controls into one `CustomToolbarItem` HStack; secondary actions (including date seek) sit in a `ToolbarFeaturesMenu` per the established list-screen pattern."
  key_links:
    - "`DateSeekButton` (ToolbarItems.swift) is `.disabled(navigation == nil)`; inside a menu a disabled row reads as unavailable, whereas as one of four crammed top-level controls it appeared inert. `store.dateSeekNavigation` is `rawDateSeekNavigation[index]` (nil until fetched)."
---

<objective>
Close G-05-1 defect 6: `FavoritesView` packs four independent controls (`FavoritesIndexMenu`,
`SortOrderMenu`, `DateSeekButton`, `QuickSearchButton`) into a single `CustomToolbarItem` HStack,
and the `DateSeekButton` is `.disabled` whenever `rawDateSeekNavigation[index]` is nil — so a visible
calendar control can be inert before any reducer action fires. Regroup the Favorites toolbar with
valid grouping (do not overpack one item) following the established list-screen pattern, and handle
unavailable date-seek metadata explicitly instead of presenting a dead top-level control.

Purpose: A responsive, correctly grouped Favorites toolbar where date seek works when available and
reads as unavailable when metadata is absent.
Output: Favorites toolbar regrouped; date-seek control reachable/responsive; nil metadata explicit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adaptive-layout-universal-orientation/05-UAT.md
@.planning/debug/phase-05-layout-regressions.md
@AppPackage/Sources/AppComponents/ToolbarItems.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regroup the Favorites toolbar and make date-seek availability explicit</name>
  <files>AppPackage/Sources/FavoritesFeature/FavoritesView.swift</files>
  <read_first>
    - AppPackage/Sources/FavoritesFeature/FavoritesView.swift — the `toolbar()` builder (lines 101-120)
      that crams `FavoritesIndexMenu`, `SortOrderMenu`, `DateSeekButton(navigation: store.dateSeekNavigation)`,
      and `QuickSearchButton(hideText: true)` into ONE `CustomToolbarItem(tint: .primary)`.
    - AppPackage/Sources/AppComponents/ToolbarItems.swift — `CustomToolbarItem` (lines 8-33: wraps all
      content in one `ToolbarItem`/`HStack(spacing: 14)`), `ToolbarFeaturesMenu` (lines 35-52: a `Menu`
      with an ellipsis label), and `DateSeekButton` (lines 114-131: `Button { navigation.map(action) }`
      rendered as `Label(.dateSeek, systemSymbol: .calendar)`, `.disabled(navigation == nil)`).
    - The established analog: AppPackage/Sources/HomeFeature/Watched/WatchedView.swift lines 95-109 —
      `CustomToolbarItem { ToolbarFeaturesMenu { DateSeekButton { ... }; FiltersButton { ... }; QuickSearchButton { ... } } }`
      (date seek lives inside the features menu, not as a bare top-level control).
    - AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift lines 44-57 — `dateSeekNavigation` is
      `rawDateSeekNavigation[index]` (nil until a fetch populates it); the reducer path from
      `.dateSeekButtonTapped` to the `.dateSeek` destination sheet is coherent (do not change it).
  </read_first>
  <action>
    Restructure `FavoritesView.toolbar()` so it no longer overpacks four independent controls into a
    single `CustomToolbarItem`. Follow the WatchedView pattern: keep the primary category control
    (`FavoritesIndexMenu`) reachable, and move the secondary actions — `SortOrderMenu`, `DateSeekButton`,
    and `QuickSearchButton` — into a `ToolbarFeaturesMenu` (the ellipsis menu) so they are individually
    reachable menu rows rather than crammed adjacent top-level controls. Keep every existing action
    closure and its `store.send(...)` payload unchanged (`setFavoritesIndex`, `fetchGalleries(nil, order)`,
    `dateSeekButtonTapped(navigation)`, `quickSearchButtonTapped`). Because `DateSeekButton` is already
    `.disabled(navigation == nil)`, placing it inside the menu makes the unavailable state read
    explicitly as a disabled menu row (handle-unavailable-metadata-explicitly) instead of a dead
    top-level control. Do not modify `ToolbarItems.swift`, the reducer, or the `.sheet` destinations;
    keep `FavoritesIndexMenu` as the direct toolbar affordance so category switching stays one tap away.
  </action>
  <verify>
    <automated>grep -q "ToolbarFeaturesMenu" AppPackage/Sources/FavoritesFeature/FavoritesView.swift && grep -q "DateSeekButton" AppPackage/Sources/FavoritesFeature/FavoritesView.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `FavoritesView.swift` no longer places `DateSeekButton`, `SortOrderMenu`, and `QuickSearchButton` as siblings directly inside a single `CustomToolbarItem` HStack; the secondary actions are inside a `ToolbarFeaturesMenu` (grep: `ToolbarFeaturesMenu` present in the file).
    - `DateSeekButton(navigation: store.dateSeekNavigation)` and its `store.send(.dateSeekButtonTapped(navigation))` closure are preserved; `FavoritesReducer.swift` and `ToolbarItems.swift` are unchanged.
    - `xcodebuild -workspace AppPackage/.swiftpm/xcode/package.xcworkspace -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air,OS=26.5' build` compiles (run alone; never overlap xcodebuild).
    - Human-check (record in SUMMARY): on Favorites, the date-seek control is reachable in the features menu and opens the date-seek sheet when metadata is loaded; when metadata is absent it appears as a disabled/unavailable row, not an inert visible calendar button.
  </acceptance_criteria>
  <done>The Favorites toolbar is validly grouped (no overpacking); date seek is reachable/responsive when available and explicitly unavailable when metadata is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Regrouping toolbar controls introduces no input, network, auth, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | (none) | Favorites toolbar grouping | low | accept | UI-only regrouping; reducer/routing untouched; no attack surface introduced. |
</threat_model>

## Artifacts this phase produces

This plan **changes**: `FavoritesView.toolbar()` — the four crammed controls are regrouped so
`SortOrderMenu`, `DateSeekButton`, and `QuickSearchButton` move into a `ToolbarFeaturesMenu`, with
`FavoritesIndexMenu` kept as the direct toolbar affordance; the `DateSeekButton` disabled-when-nil
state now reads as an explicit unavailable menu row.

<verification>
- Build green (command above).
- Grep gates: `ToolbarFeaturesMenu` and `DateSeekButton` present in `FavoritesView.swift`; `ToolbarItems.swift` + `FavoritesReducer.swift` unchanged.
- Human-check: date seek reachable/responsive when available; unavailable state explicit.
</verification>

<success_criteria>
The Favorites date-seek toolbar control is responsive when available and explicitly unavailable otherwise, with valid toolbar grouping — closing G-05-1 defect 6.
</success_criteria>

<output>
Create `.planning/phases/05-adaptive-layout-universal-orientation/05-15-SUMMARY.md` when done.
</output>
