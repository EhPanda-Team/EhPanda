---
phase: 04-concurrency-framework-migration
plan: 10
type: execute
wave: 10
depends_on: [04-06, 04-07, 04-08, 04-09]
files_modified:
  - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift
  - AppPackage/Sources/HomeFeature/Toplists/ToplistsReducer.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift
  - AppPackage/Sources/HomeFeature/History/HistoryReducer.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift
  - AppPackage/Sources/SearchFeature/SearchRootReducer.swift
  - AppPackage/Sources/SearchFeature/SearchReducer.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "All 21 Home/Search/Favorites effect sites consume the typed-throws async path via the D-03 convention (explicit typed do/catch inside .run) with Done actions and their handlers untouched"
    - "Reducer behavior is unchanged: same actions, same Result payloads, existing TestStore tests pass without modification"
  artifacts:
    - path: "AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift"
      provides: "canonical converted effect (the PATTERNS Group 3 'after' shape)"
      contains: "do throws(AppError)"
  key_links:
    - from: "AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+Gallery.swift"
      via: "effects call the typed-throws response() on concrete request types"
      pattern: "try await"
---

<objective>
Switch the Home, Search, and Favorites reducer effects (9 files, 21 sites) from the Combine-backed
facade to the parity-proven typed-throws path — the D-01 call-site migration under the D-03
convention. Error handling stays behaviorally identical: the catch arm sends the same
`…Done(.failure(error))` the facade produced.

Output: 21 converted effect sites; zero reducer state-machine changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift
</context>

## Artifacts this phase produces

Symbols created by this plan: none — this plan only rewrites existing effect closures to the
`do throws(AppError)` convention. No new types, actions, or files.

<!-- planner-discipline-allow: legacyResponse -->

<tasks>

<task type="auto">
  <name>Task 1: HomeFeature effects (6 files, 14 sites)</name>
  <files>AppPackage/Sources/HomeFeature/HomeReducer+Body.swift, AppPackage/Sources/HomeFeature/Toplists/ToplistsReducer.swift, AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift, AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift, AppPackage/Sources/HomeFeature/History/HistoryReducer.swift, AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift</files>
  <read_first>
    - .planning/phases/04-concurrency-framework-migration/04-PATTERNS.md (Group 3 before/after — the exact conversion shape; Pitfall 5 boundary)
    - AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift (lines 74-97: the canonical site AND its Done handler that must NOT change)
    - .planning/phases/04-concurrency-framework-migration/04-RESEARCH.md (§Pattern 3 and §Pitfall 1 — the one load-bearing keyword)
  </read_first>
  <action>
    Convert every legacyResponse effect site in the six files to the D-03 convention. Per site,
    inside the existing `.run { send in … }` closure:
    - open an explicit `do throws(AppError)` block (NEVER a bare do — closure catch inference
      binds any Error, Pitfall 1);
    - `let value = try await <ConcreteRequest>(…).response()` with the identical constructor
      arguments;
    - `await send(.<same>Done(.success(value)))` in the do arm, `await send(.<same>Done(.failure(error)))`
      in the catch arm — `error` binds as AppError directly; no casting, no `?? .unknown`.
    Keep `.cancellable(id:)` modifiers, animation arguments, and any surrounding logic exactly
    where they are. DO NOT touch the `…Done(Result)` case handlers, action enums, or any
    `switch result` in the reducer body (Pitfall 5 — that is Phase 9 territory and would break
    the parity bar). Sites that inline-switch on the awaited Result today convert their switch
    arms into the do/catch arms with identical sends.
  </action>
  <verify>
    <automated>test "$(grep -rn "legacyResponse" AppPackage/Sources/HomeFeature | wc -l | tr -d ' ')" -eq 0 && test "$(grep -rn "as? AppError" AppPackage/Sources/HomeFeature | wc -l | tr -d ' ')" -eq 0 && xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo GATE-OK</automated>
  </verify>
  <done>All 14 Home sites on the typed-throws path; no facade calls, no AppError casts introduced; build green.</done>
  <acceptance_criteria>
    - `grep -rc "do throws(AppError)" AppPackage/Sources/HomeFeature/**/*.swift` totals >= 14 across the six files
    - Zero facade calls remain in HomeFeature; zero `as? AppError` casts introduced
    - `git diff` shows no change to any `case .…Done` handler or action enum
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Search + Favorites effects (3 files, 7 sites)</name>
  <files>AppPackage/Sources/SearchFeature/SearchRootReducer.swift, AppPackage/Sources/SearchFeature/SearchReducer.swift, AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift</files>
  <read_first>
    - AppPackage/Sources/SearchFeature/SearchReducer.swift and SearchRootReducer.swift (current sites)
    - AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift (current sites)
  </read_first>
  <action>
    Same conversion convention as Task 1 for the 4 Search sites and 3 Favorites sites. Done
    actions and handlers untouched.
  </action>
  <verify>
    <automated>test "$(grep -rn "legacyResponse" AppPackage/Sources/SearchFeature AppPackage/Sources/FavoritesFeature | wc -l | tr -d ' ')" -eq 0 && cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -3</automated>
  </verify>
  <done>All 21 plan sites converted; full suite green with existing reducer/TestStore tests unmodified.</done>
  <acceptance_criteria>
    - Zero facade calls remain in SearchFeature and FavoritesFeature
    - Full suite passes; `git diff --stat` shows no test-file modifications in this plan
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reducer effects → request layer | error-path fidelity across the switch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Tampering | effect conversion drift | medium | mitigate | Done actions/payloads untouched (Pitfall 5); existing TestStore suites must pass unmodified |
| T-04-23 | Repudiation | error swallowed at conversion | medium | mitigate | Catch arm always sends the same failure action; typed binding removes cast-and-default risk (gate greps for casts) |
</threat_model>

<verification>
- Build + full suite green; no test modified; no facade call remains in the three feature dirs.
</verification>

<success_criteria>
- 21 sites consume the proven async path with literal reducer parity.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-10-SUMMARY.md` when done.
</output>
