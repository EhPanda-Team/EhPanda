---
phase: 04-concurrency-framework-migration
plan: 07
type: execute
wave: 7
depends_on: [04-03, 04-06]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+Account.swift
  - AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "All 14 Request+Account structs expose func response() async throws(AppError) with the frozen 04-03 account assertions passing unchanged through the async path"
    - "LoginRequest still yields the HTTPURLResponse optionally; IgneousRequest's no-castable-response case still surfaces as AppError.unknown (compactMap-empty semantics preserved)"
  artifacts:
    - path: "AppPackage/Sources/NetworkingFeature/Request+Account.swift"
      provides: "typed-throws response() bodies for all 14 account requests"
      contains: "async throws(AppError)"
  key_links:
    - from: "AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+Account.swift"
      via: "flipped acquisition exercises the typed-throws bodies"
      pattern: "capture \\{"
---

<objective>
Migrate the 14 account requests to typed-throws async bodies (D-01/D-02) and prove parity by
flipping the 04-03 account baseline onto the async path — frozen assertions unchanged.

Publisher properties remain until 04-13 (transient by design; the phase ends Combine-free).
Output: 14 proven async bodies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request+Account.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- `func response() async throws(AppError) -> Response` concrete methods on all 14 Request+Account structs

<tasks>

<task type="auto">
  <name>Task 1: Async bodies for the first 7 account structs (through SubmitEhSettingChangesRequest)</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Account.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift (full file — each publisher pipeline is the translation source; URLRequest assembly blocks are reused verbatim)
    - AppPackage/Sources/NetworkingFeature/Request.swift (fetch helper signature from 04-06; Pattern: fetch → parse-in-do → mapAppError funnel)
  </read_first>
  <action>
    Add `public func response() async throws(AppError) -> Response` to the first 7 structs in
    file order (keep publishers). Mechanical translation per PATTERNS Group 2: reuse the exact
    URLRequest assembly block from the publisher (POST method/body/setURLEncodedContentType
    lines verbatim), fetch via the 04-06 helper with the stored urlSession, then the same
    parse/map steps inside a do/mapAppError funnel. Two non-generic mappings to get right:
    - LoginRequest: `let (_, response) = try await fetch(request, in: urlSession)` then return
      `response as? HTTPURLResponse` (optional cast result IS the Response — no throw on nil).
    - IgneousRequest: guard-cast to HTTPURLResponse, else `throw AppError.unknown` — this
      preserves today's compactMap-empty → continuation-finished-without-value → .unknown
      behavior that the 04-03 baseline locked.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo GATE-OK</automated>
  </verify>
  <done>First 7 structs compile with typed-throws bodies alongside their publishers; assembly blocks byte-reused.</done>
  <acceptance_criteria>
    - 7 occurrences of `func response() async throws(AppError)` exist in Request+Account.swift after this task
    - IgneousRequest's body contains a guard-cast that throws AppError.unknown
    - Quick build succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remaining 7 account bodies + parity flip of the account baseline</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Account.swift, AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift (remaining structs)
    - AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift (acquisition lines to flip; assertions frozen)
  </read_first>
  <action>
    1. Add typed-throws bodies to the remaining 7 structs, same translation rules.
    2. Flip every acquisition in AccountRequestBaselineTests from the legacy facade to
       `await capture { try await <Req>(…).response() }` — concrete types inside the closure,
       assertions/fixtures byte-identical.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 14 account structs proven: frozen assembly (structural body equality), response mapping, and retry assertions pass through the async path.</done>
  <acceptance_criteria>
    - 14 occurrences of `func response() async throws(AppError)` in Request+Account.swift
    - AccountRequestBaselineTests.swift contains no legacyResponse() call
    - `git diff` of AccountRequestBaselineTests.swift touches only acquisition lines
    - Targeted NetworkingFeatureTests run passes; full suite green (`cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air'`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → E-Hentai account endpoints | credentialed POST operations (login, votes, favorites) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Tampering | POST body assembly | high | mitigate | Assembly blocks reused verbatim; structural body assertions frozen in 04-03 must pass through the async path |
| T-04-16 | Denial of service | account-op retry drift | medium | mitigate | Representative 4-attempt lock from 04-03 re-proven post-flip |
| T-04-17 | Information disclosure | credentials in new code | low | mitigate | No logging added; bodies only reuse existing param assembly (cookie handling untouched — Phase 8 scope per D-08) |
</threat_model>

<verification>
- Frozen account assertions green through the async path; full suite green.
</verification>

<success_criteria>
- 14/14 account bodies landed and parity-proven; production still on the facade (switched in 04-10..04-12).
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-07-SUMMARY.md` when done.
</output>
