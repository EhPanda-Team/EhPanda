---
phase: 04-concurrency-framework-migration
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02]
files_modified:
  - AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataBaselineTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The 12 Request+Gallery list requests have frozen URLRequest-assembly + fixture-parse baselines against the current Combine layer (D-05/D-06)"
    - "The gdata plumbing contract is frozen: JSON POST shape (method=gdata, gidlist, namespace=1), retried fetch, decode path, uniform error mapping"
  artifacts:
    - path: "AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift"
      provides: "baseline for the 12 gallery-list requests"
      contains: "PopularGalleriesRequest"
    - path: "AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataBaselineTests.swift"
      provides: "baseline for GalleriesMetadataRequest through the gdata plumbing"
      contains: "GalleriesMetadataRequest"
  key_links:
    - from: "AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+Gallery.swift"
      via: "acquisition through the Combine facade at lock time"
      pattern: "legacyResponse\\(\\)"
    - from: "AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataBaselineTests.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+GData.swift"
      via: "gdata JSON POST assembly asserted structurally"
      pattern: "gdata"
---

<objective>
Lock the Wave-0 parity baseline (D-05/D-06) for the gallery-list family (12 requests) and the
gdata metadata plumbing against TODAY'S Combine layer. Frozen assertions are re-run against the
typed-throws path in plan 04-08.

Purpose: the highest-traffic list requests (search/frontpage/popular/watched/favorites/toplists)
get mechanical parity coverage before any rewrite.
Output: two characterization suites, green against current code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@AppPackage/Sources/NetworkingFeature/Request+Gallery.swift
@AppPackage/Sources/NetworkingFeature/Request+GData.swift
@AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- `GalleryRequestBaselineTests` suite (+ inline fixtures)
- `GalleriesMetadataBaselineTests` suite (+ inline fixtures)

<tasks>

<task type="auto">
  <name>Task 1: Gallery-list baseline — 12 requests</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Gallery.swift (all 12 structs — URL construction incl. filters/page params, parse steps)
    - AppPackage/Tests/ParserFeatureTests/List/ListParserTests.swift (list-page fixture shapes to borrow)
    - AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift (harness API)
    - AppPackage/Tests/NetworkingFeatureTests/RoutineRequestBaselineTests.swift (suite conventions established in 04-03)
  </read_first>
  <action>
    Create GalleryRequestBaselineTests locking, for each of the 12 structs in
    Request+Gallery.swift (SearchGalleriesRequest, MoreSearchGalleriesRequest,
    FrontpageGalleriesRequest, MoreFrontpageGalleriesRequest, PopularGalleriesRequest,
    WatchedGalleriesRequest, MoreWatchedGalleriesRequest, FavoritesGalleriesRequest,
    MoreFavoritesGalleriesRequest, ToplistsGalleriesRequest, MoreToplistsGalleriesRequest,
    DateSeekGalleriesRequest):

    1. Assembly: the received URLRequest's full URL (including query items produced by the
       filter/keyword/page arguments actually passed in the test), method GET, nil body. Choose
       representative constructor arguments per request and assert the exact resulting URL string.
    2. Parse output: serve one shared minimal list-page HTML fixture (structure borrowed from
       ListParserTests) and assert deterministic fields of the parsed result (page number,
       gallery count, first gallery's identifying fields — GalleriesResult /
       FavoritesGalleriesResult shape as applicable).
    3. Error path: one representative request with a persistent transportFailure script →
       .failure(.networkingFailed) and attempts == 4.

    Acquisition via legacyResponse() on a stubbed session throughout. All 12 structs must appear;
    where several share a parser, sharing one fixture is fine — the per-request lock is the
    assembled URL plus the parse output through THAT request's pipeline.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 12 gallery-list structs have assembly + parse baselines; suite green against current code.</done>
  <acceptance_criteria>
    - All 12 struct names appear in GalleryRequestBaselineTests.swift
    - At least one test asserts attempts == 4 on persistent transport failure
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: gdata plumbing baseline — GalleriesMetadataRequest</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+GData.swift (the gdata plumbing: JSON body keys, retry placement, decode + mapAppError funnel)
    - AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift (GalleriesMetadataRequest decode)
    - AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataDecodeTests.swift (existing JSON fixtures — reuse their payload shapes)
  </read_first>
  <action>
    Create GalleriesMetadataBaselineTests locking the gdata contract through
    GalleriesMetadataRequest with an injected stubbed session:

    1. Assembly: POST to Defaults.URL.api; body decoded via JSONSerialization (never
       byte-compared — dictionary key order is nondeterministic) equals
       {"method": "gdata", "namespace": 1, "gidlist": [[gid, token], …]} for the test's input
       pairs.
    2. Decode path: serve a valid gdata JSON fixture (reuse the payload shape from
       GalleriesMetadataDecodeTests) → assert the decoded result's deterministic fields.
    3. Error paths: malformed JSON fixture → .failure(.parseFailed); persistent transportFailure
       → attempts == 4 then .failure(.networkingFailed).

    Acquisition via legacyResponse(). The ≤25-pairs chunking rule is caller-side and out of
    scope here (do not test it) — note that in the suite doc-comment.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>gdata JSON POST shape, decode path, retry count, and error mapping frozen; suite green.</done>
  <acceptance_criteria>
    - GalleriesMetadataBaselineTests.swift compares the POST body via JSONSerialization (structural), not Data equality
    - Tests cover success decode, parseFailed, and networkingFailed-with-4-attempts
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → E-Hentai origin + api.php | list-page and gdata API assembly locked here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Denial of service | gdata/list retry policy | high | mitigate | 4-attempt cap frozen; post-migration retry amplification fails the baseline |
| T-04-09 | Tampering | request assembly drift | medium | mitigate | Exact URL strings and structural body assertions frozen per request |
</threat_model>

<verification>
- Targeted NetworkingFeatureTests run green after each task; no live network anywhere.
</verification>

<success_criteria>
- 13 more request structs frozen (12 gallery + metadata); gdata plumbing contract locked ahead of its async rewrite in 04-08.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-04-SUMMARY.md` when done.
</output>
