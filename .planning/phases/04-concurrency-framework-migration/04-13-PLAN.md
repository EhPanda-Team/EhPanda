---
phase: 04-concurrency-framework-migration
plan: 13
type: execute
wave: 13
depends_on: [04-10, 04-11, 04-12]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request.swift
  - AppPackage/Sources/NetworkingFeature/Request+Account.swift
  - AppPackage/Sources/NetworkingFeature/Request+Detail.swift
  - AppPackage/Sources/NetworkingFeature/Request+GData.swift
  - AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift
  - AppPackage/Sources/NetworkingFeature/Request+Gallery.swift
  - AppPackage/Sources/NetworkingFeature/Request+Image.swift
  - AppPackage/Sources/ApplicationClient/ApplicationClient.swift
  - AppPackage/Sources/AuthorizationClient/AuthorizationClient.swift
  - AppPackage/Sources/ImageClient/ImageClient.swift
  - AppPackage/Sources/LibraryClient/LibraryClient.swift
  - AppPackage/Tests/ParserFeatureTests/Other/DownloadPageErrorParserTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The Request protocol requires func response() async throws(AppError) -> Response and no publisher requirement exists"
    - "The package source tree is Combine-free: zero Combine imports and zero publisher types remain anywhere under AppPackage/Sources (CONC-01 acceptance grep)"
    - "All Combine-era bridges are gone (D-04): the Result facade, the retry publisher extension, the continuation shim, the main-queue hop, and the Combine gdata plumbing"
    - "The four clients no longer import Combine (D-13) while their APIs and behavior are untouched"
  artifacts:
    - path: "AppPackage/Sources/NetworkingFeature/Request.swift"
      provides: "final typed-throws protocol + helpers only (no Combine)"
      contains: "func response() async throws(AppError) -> Response"
  key_links:
    - from: "AppPackage/Sources/NetworkingFeature/Request+Gallery.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request.swift"
      via: "all 44 structs conform through their concrete typed-throws bodies"
      pattern: "func response\\(\\) async throws\\(AppError\\)"
---

<objective>
Tear down the Combine layer (D-04 + D-13): flip the protocol requirement to the typed-throws
method (RESEARCH Pattern 1 — the compiler becomes the completeness checklist), delete every
publisher body and every bridge, and drop the four dead client imports. The package ends
Combine-free — the CONC-01 acceptance grep goes to zero.

This commit is deletion-only plus a 3-line protocol edit: all 44 structs already carry exactly
the method the new protocol requires, and no production or test code calls the facade anymore
(04-06..04-12). The one non-deletion edit: the test-local FailingHTMLRequest double in
ParserFeatureTests must adopt the new conformance in the same commit (the protocol flip breaks
its publisher-based conformance).

Output: Combine-free AppPackage/Sources; CONC-01 complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request.swift
@AppPackage/Tests/ParserFeatureTests/Other/DownloadPageErrorParserTests.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- The `Request` protocol's new requirement signature `func response() async throws(AppError) -> Response` (replacing the publisher requirement)
- `FailingHTMLRequest.response()` typed-throws test-double body (ParserFeatureTests)

Symbols DELETED by this plan (expected to disappear): the publisher requirement and all 44
publisher properties; the legacy Result facade and its main-queue hop; the Publisher retry/async
continuation extensions; the Combine gdata plumbing; 7 NetworkingFeature Combine imports; 4
client Combine imports.

<!-- planner-discipline-allow: import Combine -->
<!-- planner-discipline-allow: AnyPublisher -->
<!-- planner-discipline-allow: legacyResponse -->

<tasks>

<task type="auto">
  <name>Task 1: Protocol flip + delete the Combine surface in NetworkingFeature + migrate the test double</name>
  <files>AppPackage/Sources/NetworkingFeature/Request.swift, AppPackage/Sources/NetworkingFeature/Request+Account.swift, AppPackage/Sources/NetworkingFeature/Request+Detail.swift, AppPackage/Sources/NetworkingFeature/Request+GData.swift, AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift, AppPackage/Sources/NetworkingFeature/Request+Gallery.swift, AppPackage/Sources/NetworkingFeature/Request+Image.swift, AppPackage/Tests/ParserFeatureTests/Other/DownloadPageErrorParserTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request.swift (protocol lines 9-13; facade lines 20-23; Publisher extension lines 145-180 — the delete map per PATTERNS Group 1)
    - AppPackage/Tests/ParserFeatureTests/Other/DownloadPageErrorParserTests.swift (FailingHTMLRequest at lines ~98-112 and its call at ~87)
    - .planning/phases/04-concurrency-framework-migration/04-PATTERNS.md (Group 1 DELETE/KEEP lists)
  </read_first>
  <action>
    One atomic commit (the compiler enforces completeness — any straggler publisher reference
    fails the build):
    1. Request.swift: replace the protocol's publisher requirement with
       `func response() async throws(AppError) -> Response`; delete the legacyResponse facade
       (and with it the main-queue receive hop — D-04: TCA marshals sends, the hop is dead
       weight; add no replacement); delete the entire `extension Publisher` block (the retry
       combinator and the continuation shim); delete the Combine import.
    2. The six Request+ files: delete every `var publisher: AnyPublisher…` property (44 total
       across the seven files including Request.swift) and every Combine import; in
       Request+GData.swift also delete the Combine gdata plumbing function, keeping the async
       companion and carrying its single-place-contract doc comment over if it lives on the
       deleted one.
    3. KEEP verbatim (Pitfall 6 / D-07 seam): ResponseParsingError, urlRequest(url:allowsCellular:),
       htmlDocument, htmlDocumentWithUTF8Fallback, both parseResponse overloads, mapAppError,
       fetch, setURLEncodedContentType, dictString.
    4. DownloadPageErrorParserTests.swift: give FailingHTMLRequest a typed-throws body —
       `func response() async throws(AppError) -> Void` that runs the same
       parseResponse(doc:)-then-throw pipeline through mapAppError (preserving what the test
       asserts: the parsed server error survives mapping); convert the test's acquisition +
       switch to the same capture-free do/catch shape used at production sites; remove the Just/
       publisher body and the file's Combine import if nothing else uses it.
  </action>
  <verify>
    <automated>xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && test "$(grep -rn "AnyPublisher" AppPackage/Sources | wc -l | tr -d ' ')" -eq 0 && test "$(grep -rn "legacyResponse" AppPackage/Sources AppPackage/Tests | wc -l | tr -d ' ')" -eq 0 && echo GATE-OK</automated>
  </verify>
  <done>Protocol requires the typed-throws method; zero publisher properties, zero bridges, zero facade references anywhere; build green.</done>
  <acceptance_criteria>
    - Request.swift protocol body contains exactly the associatedtype and the typed-throws response requirement
    - `grep -rc "AnyPublisher" AppPackage/Sources/NetworkingFeature/*.swift` totals 0; the kept helpers are byte-identical (git diff shows pure deletions around them)
    - DownloadPageErrorParserTests still asserts the ipBanned mapping and passes
    - Quick build succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-13 client import deletions + phase-level Combine-free gates</name>
  <files>AppPackage/Sources/ApplicationClient/ApplicationClient.swift, AppPackage/Sources/AuthorizationClient/AuthorizationClient.swift, AppPackage/Sources/ImageClient/ImageClient.swift, AppPackage/Sources/LibraryClient/LibraryClient.swift</files>
  <read_first>
    - The four client files (verify the import is still dead: no publisher/Future/Subject/sink usage — confirmed by research, re-verify by grep before deleting)
  </read_first>
  <action>
    Delete the single dead Combine import line from each of the four clients (D-13 — including
    AuthorizationClient, whose full removal remains Phase 7 scope; only the import goes here).
    No other change to any client. Then run the phase-level CONC-01 gates: the package-wide
    Combine grep must be empty, and the full suite plus the existing DF semantics tests must be
    green.
  </action>
  <verify>
    <automated>test "$(grep -rn "import Combine" AppPackage/Sources | wc -l | tr -d ' ')" -eq 0 && cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -3</automated>
  </verify>
  <done>CONC-01 acceptance grep empty (zero Combine imports under AppPackage/Sources); full suite green; client behavior untouched.</done>
  <acceptance_criteria>
    - `grep -rn "import Combine" AppPackage/Sources | wc -l` returns 0
    - `git diff` for the four client files shows exactly one deleted line each
    - Full suite passes (all baseline suites now run exclusively through the typed-throws path)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → all remote origins | the entire transport now runs through the async path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-28 | Tampering | accidental helper mutation during deletion | high | mitigate | KEEP list enforced; parse/error helpers must be byte-identical (frozen baselines + DF tests catch any drift) |
| T-04-29 | Spoofing | DF trust path | high | mitigate | No DF file touched in this plan; DFRequestSemanticsTests S1-S7 green is a gate |
| T-04-30 | Repudiation | server-text funnel loss | medium | mitigate | FailingHTMLRequest migration keeps the ipBanned assertion intact |
</threat_model>

<verification>
- Combine grep and publisher-type grep empty under Sources; facade name gone repo-wide; full suite green.
- The intentional cancellation-behavior delta (documented in 04-06) is the ONLY known behavior difference — record it in the SUMMARY for the verifier.
</verification>

<success_criteria>
- CONC-01 fully satisfied: async request layer, migrated consumers, Combine-free package, behavior/error paths proven preserved by the frozen baselines.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-13-SUMMARY.md` when done.
</output>
