---
phase: 04-concurrency-framework-migration
plan: 14
type: execute
wave: 14
depends_on: [04-13]
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Package.resolved
  - EhPanda.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularView.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedView.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift
  - AppPackage/Sources/SearchFeature/SearchRootView.swift
  - AppPackage/Sources/SearchFeature/SearchView.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchView.swift
  - AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift
  - AppPackage/Sources/DownloadsFeature/DownloadsView.swift
autonomous: true
requirements: [CONC-02]
must_haves:
  truths:
    - "AppPackage/Package.swift pins TCA from: 1.25.3 with the ComposableArchitecture2Deprecations and ComposableArchitecture2DeprecationOverloads traits, and both Package.resolved files are regenerated"
    - "A positive-control recon proved the traits took effect (an expected deprecation warning appeared at a known non-projected scope site) BEFORE any fix was applied"
    - "Zero TCA deprecation warnings remain across the app scheme and package build; reducers/stores behave identically (full suite green, no reducer file modified)"
  artifacts:
    - path: "AppPackage/Package.swift"
      provides: "TCA floor + both 2.0-prep traits"
      contains: "ComposableArchitecture2DeprecationOverloads"
  key_links:
    - from: "AppPackage/Sources/HomeFeature/Popular/PopularView.swift"
      to: "AppPackage/Package.swift"
      via: "projected destination scope syntax required by the trait-gated deprecations"
      pattern: "\\$destination"
---

<objective>
Deliver CONC-02: pin TCA `from: 1.25.3` with the two 2.0-deprecation traits (D-12), run the D-10
reconnaissance with a positive control (RESEARCH Pitfall 2: Xcode trait application can silently
fail — zero warnings at recon means the traits did NOT take, not that the code is clean), then
drive the surfaced deprecations to zero. Sequenced after CONC-01 per D-09.

Pre-sized surface (verified by grep + official migration guide): exactly one pattern — 24
non-projected destination scopes (21 sheet, 3 fullScreenCover) across 11 view files, all
targeting reducer cases of @Reducer enum Destinations, migrating to the projected syntax. All
other trait-gated APIs have zero hits, and the codebase already builds warning-free on TCA
1.26.0, so hard deprecations are already absent.

D-11 stop condition: if recon surfaces warnings OUTSIDE this pattern, or materially more than
~24, HALT after categorizing and surface the inventory to the owner before fixing anything.

Output: traits pinned, zero TCA deprecation warnings, identical reducer/store behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Package.swift
</context>

## Artifacts this phase produces

Symbols created by this plan: none in code — this plan changes the manifest (TCA floor + traits)
and rewrites 24 view-modifier scope arguments to the projected form. No new types or files.

<!-- planner-discipline-allow: state: \.destination? -->

<tasks>

<task type="auto">
  <name>Task 1: Flip traits, resolve, recon with positive control (D-10/D-12)</name>
  <files>AppPackage/Package.swift, AppPackage/Package.resolved, EhPanda.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved</files>
  <read_first>
    - AppPackage/Package.swift (the TCA .package entry at lines 23-26)
    - .planning/phases/04-concurrency-framework-migration/04-RESEARCH.md (§Installation — the exact replacement syntax; §Pitfall 2 — positive control; §Pitfall 7 — build-time note; §Deprecation Surface — the expected inventory)
  </read_first>
  <action>
    1. Record a baseline build time: time a clean-enough build of the AppFeature scheme before
       the manifest edit (note the wall-clock in the SUMMARY — Pitfall 7 wants before/after).
    2. Replace the TCA .package entry with the RESEARCH §Installation form: same URL,
       `from: "1.25.3"`, and a traits array containing exactly
       "ComposableArchitecture2Deprecations" and "ComposableArchitecture2DeprecationOverloads".
       Both traits stay pinned (requirement wins over the guide's "temporary" advice; note any
       type-check-time regression for the owner instead of dropping a trait).
    3. Resolve: run swift package resolve inside AppPackage (project memory: delete
       AppPackage/.build first if resolve misbehaves). Expect the resolved TCA version to remain
       1.26.0 (already newest compatible; D-12's floor is the real diff) — only originHash should
       change; the workspace mirror regenerates on the next xcodebuild.
    4. Recon build (D-10): build the AppFeature scheme, capture the full log to a scratch file,
       and extract TCA deprecation warnings (case-insensitive match on deprecation wording,
       excluding SwiftLint lines). POSITIVE CONTROL: the log MUST contain a deprecation warning
       at a known non-projected scope site (PopularView.swift, the sheet scope around line 36).
       If zero warnings surface: traits did not apply — delete DerivedData for this project,
       re-resolve, rebuild; do not proceed past a silent recon.
    5. Categorize the inventory: expected result is ~24 warnings, all the destination-scope
       pattern, in exactly the 11 view files this plan owns. D-11 STOP CONDITION: any warning
       outside that pattern, or a materially larger count → write the categorized inventory to
       the SUMMARY, halt the plan, and surface to the owner for a scope decision. Do not
       proceed to Task 2 in that case.
    6. Commit the manifest + resolved files with the recon inventory in the commit body.
  </action>
  <verify>
    <automated>grep -A6 "swift-composable-architecture" AppPackage/Package.swift | grep -c "ComposableArchitecture2Deprecations\|ComposableArchitecture2DeprecationOverloads" | grep -qx "2" && grep -q "1.25.3" AppPackage/Package.swift && echo MANIFEST-OK</automated>
  </verify>
  <done>Traits pinned and proven active via the positive control; deprecation inventory categorized and matching the pre-sized expectation (or plan halted per D-11).</done>
  <acceptance_criteria>
    - AppPackage/Package.swift contains both trait strings and the 1.25.3 floor
    - AppPackage/Package.resolved still resolves swift-composable-architecture at >= 1.26.0 from the pointfreeco URL
    - The recon log (referenced in the SUMMARY) shows the expected warning at the PopularView control site before any fix
    - Before/after build-time numbers recorded in the SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Projected-scope migration part 1 (7 files)</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/FavoritesFeature/FavoritesView.swift, AppPackage/Sources/HomeFeature/Popular/PopularView.swift, AppPackage/Sources/HomeFeature/Watched/WatchedView.swift, AppPackage/Sources/HomeFeature/Frontpage/FrontpageView.swift, AppPackage/Sources/SearchFeature/SearchRootView.swift, AppPackage/Sources/SearchFeature/SearchView.swift</files>
  <read_first>
    - .planning/phases/04-concurrency-framework-migration/04-PATTERNS.md (Group 7 before/after — the single-argument rewrite)
    - AppPackage/Sources/HomeFeature/Popular/PopularView.swift (lines 35-40 — canonical site)
  </read_first>
  <action>
    Rewrite every non-projected destination scope in the seven files to the projected form per
    PATTERNS Group 7: the scope call becomes
    `$store.scope(state: \.$destination, action: \.destination)` with the case appended as a
    chained accessor (e.g. `.filters`) on the scope result. This is purely an argument-syntax
    change: every sheet/fullScreenCover modifier stays attached to its current anchor view
    (CLAUDE.md dialog-placement rule — do not relocate modifiers), @Presents properties and
    reducers untouched. Enumerate sites by grepping the deprecated pattern per file before
    editing; cross-check against the recon inventory from Task 1.
  </action>
  <verify>
    <automated>test "$(grep -rn 'state: \\.destination?' AppPackage/Sources/ReadingFeature AppPackage/Sources/FavoritesFeature AppPackage/Sources/HomeFeature AppPackage/Sources/SearchFeature | wc -l | tr -d ' ')" -eq 0 && xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo GATE-OK</automated>
  </verify>
  <done>Zero non-projected scopes remain in the four feature dirs; build green; no modifier moved off its anchor.</done>
  <acceptance_criteria>
    - The deprecated scope pattern has zero hits in ReadingFeature/FavoritesFeature/HomeFeature/SearchFeature
    - `git diff` shows only scope-argument lines changed (no modifier relocation, no reducer edits)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Projected-scope migration part 2 (4 files) + zero-warning gate</name>
  <files>AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchView.swift, AppPackage/Sources/DetailFeature/Previews/PreviewsView.swift, AppPackage/Sources/DownloadsFeature/DownloadsView.swift</files>
  <read_first>
    - The four view files (their destination scope sites — note DownloadsView carries sites the earlier pattern-map file list missed; the grep enumeration is authoritative)
  </read_first>
  <action>
    Rewrite the remaining non-projected scopes in the four files (same rules as Task 2). Then run
    the CONC-02 acceptance gates: rebuild the app scheme with a captured log and assert zero TCA
    deprecation warnings remain (the same extraction used at recon now returns empty); run the
    full suite (reducer/store parity — no reducer file was modified in this plan); record the
    post-migration build time next to Task 1's numbers. Real-UI reducer/store behavior is
    additionally confirmed by the phase-level /gsd-verify-work UAT after execution (per
    04-VALIDATION.md manual-verification table) — note that pointer in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(grep -rn 'state: \\.destination?' AppPackage/Sources | wc -l | tr -d ' ')" -eq 0 && xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | grep -iE "warning:.*deprecat" | grep -viE "swiftlint" | wc -l | grep -qx "0" && cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -3</automated>
  </verify>
  <done>Zero deprecated scopes and zero TCA deprecation warnings package-wide; full suite green; CONC-02 acceptance met.</done>
  <acceptance_criteria>
    - The deprecated scope pattern has zero hits under AppPackage/Sources
    - A fresh app-scheme build log contains zero TCA deprecation warnings (SwiftLint lines excluded)
    - Full suite passes with no reducer file modified in this plan (`git diff --stat` confirms view/manifest files only)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package manifest → remote dependency | version floor + trait flags on an existing dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | TCA dependency bump | high | mitigate | No new package (RESEARCH §Package Legitimacy Audit: canonical pointfreeco repo, already resolved at 1.26.0; traits verified deprecation-only with unconditional dependencies — resolution cannot change); acceptance asserts the resolved URL/version post-resolve |
| T-04-31 | Denial of service | compile-time tax of the overloads trait | low | accept | Requirement pins both traits; before/after build times recorded and surfaced to the owner instead of silently dropping a trait (Pitfall 7) |
| T-04-32 | Tampering | presentation anchor drift | medium | mitigate | Argument-syntax-only edits; dialog-placement rule enforced by diff review criterion; UAT covers sheet/cover presentation |
</threat_model>

<verification>
- Positive control observed before fixes; zero TCA deprecation warnings after; full suite green; resolved files regenerated; build times recorded.
</verification>

<success_criteria>
- CONC-02 acceptance: manifest pins from: 1.25.3 + both traits; zero TCA deprecation warnings; reducers/stores behave identically (suite + later UAT).
- Do NOT churn Effect type annotations to EffectOf (the 1.25.3 revert makes that unnecessary — anti-pattern).
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-14-SUMMARY.md` when done.
</output>
