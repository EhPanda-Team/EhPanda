---
phase: 04-concurrency-framework-migration
plan: 08
type: execute
wave: 8
depends_on: [04-04, 04-06]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+GData.swift
  - AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift
  - AppPackage/Sources/NetworkingFeature/Request+Gallery.swift
  - AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataBaselineTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "An async gdata plumbing companion exists (same JSON body, retried fetch, decode, uniform error mapping) and GalleriesMetadataRequest is proven on it via the frozen 04-04 assertions"
    - "All 12 Request+Gallery structs expose func response() async throws(AppError) with the frozen 04-04 assertions passing unchanged"
  artifacts:
    - path: "AppPackage/Sources/NetworkingFeature/Request+GData.swift"
      provides: "gdataResponse async companion to the Combine plumbing"
      contains: "func gdataResponse"
    - path: "AppPackage/Sources/NetworkingFeature/Request+Gallery.swift"
      provides: "typed-throws response() bodies for the 12 gallery-list requests"
      contains: "async throws(AppError)"
  key_links:
    - from: "AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+GData.swift"
      via: "metadata request's async body delegates to the shared gdata plumbing"
      pattern: "gdataResponse"
---

<objective>
Migrate the gdata plumbing and the gallery-list family to typed-throws async (D-01/D-02), proving
parity by flipping the 04-04 baselines. The async gdata helper also unblocks 04-09's
GalleryVersionMetadataRequest (same plumbing, Detail family).

Publisher properties and gdataPublisher remain until 04-13.
Output: async gdata plumbing + 13 proven async bodies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request+GData.swift
@AppPackage/Sources/NetworkingFeature/Request+Gallery.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- `Request.gdataResponse(gidlist:urlSession:decode:)` — async typed-throws companion to the gdata plumbing
- `func response() async throws(AppError) -> Response` concrete methods on GalleriesMetadataRequest and all 12 Request+Gallery structs

<tasks>

<task type="auto">
  <name>Task 1: gdataResponse plumbing + GalleriesMetadataRequest body + metadata flip</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+GData.swift, AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift, AppPackage/Tests/NetworkingFeatureTests/GalleriesMetadataBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+GData.swift (the Combine plumbing — body keys, retry placement, decode funnel, and its doc comment explaining the single-place contract; carry that doc comment's intent onto the async companion)
    - AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift (GalleriesMetadataRequest wiring)
    - AppPackage/Sources/NetworkingFeature/Request.swift (fetch helper)
  </read_first>
  <action>
    1. In Request+GData.swift add the async companion:
       `func gdataResponse<T>(gidlist: [[Any]], urlSession: URLSession, decode: (Data) throws -> T) async throws(AppError) -> T`
       — identical params dictionary (method=gdata, gidlist, namespace=1), identical URLRequest
       assembly (POST to Defaults.URL.api, JSONSerialization body), fetch via the 04-06 helper
       (retried, fetch scope only), then `parseResponse(data:decode)` inside a do/mapAppError
       funnel. Keep gdataPublisher untouched beside it.
    2. Add `response()` to GalleriesMetadataRequest delegating to gdataResponse with its current
       gidlist/decode arguments.
    3. Flip GalleriesMetadataBaselineTests acquisitions to `capture { try await … .response() }`;
       assertions frozen.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>gdata async plumbing proven: structural JSON body, decode, parseFailed and 4-attempt networkingFailed paths all pass frozen.</done>
  <acceptance_criteria>
    - Request+GData.swift contains `func gdataResponse` with `async throws(AppError)`
    - GalleriesMetadataBaselineTests.swift contains no legacyResponse() call; git diff touches only acquisition lines
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 12 gallery-list bodies + gallery baseline flip</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Gallery.swift, AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Gallery.swift (all 12 publisher pipelines — translation sources)
    - AppPackage/Tests/NetworkingFeatureTests/GalleryRequestBaselineTests.swift (acquisition lines; assertions frozen)
  </read_first>
  <action>
    Add typed-throws `response()` bodies to all 12 gallery-list structs (keep publishers):
    identical URL construction, fetch via the helper with the stored urlSession, identical parse
    steps (htmlDocument or htmlDocumentWithUTF8Fallback exactly as each publisher chooses today)
    inside a do/mapAppError funnel. Then flip GalleryRequestBaselineTests acquisitions to
    `capture { try await … .response() }` — assertions byte-identical.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 12 gallery-list structs proven parity through the async path; full suite still green.</done>
  <acceptance_criteria>
    - 12 occurrences of `func response() async throws(AppError)` in Request+Gallery.swift
    - GalleryRequestBaselineTests.swift contains no legacyResponse() call; git diff touches only acquisition lines
    - Full suite green (`cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air'`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → E-Hentai list pages + api.php | list fetches and gdata batch metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18 | Denial of service | gdata/list retry drift | high | mitigate | Frozen 4-attempt assertions re-proven post-flip |
| T-04-19 | Tampering | gdata body drift | medium | mitigate | Structural JSON body assertion frozen in 04-04 passes through gdataResponse |
</threat_model>

<verification>
- Frozen 04-04 assertions green through the async path; full suite green.
</verification>

<success_criteria>
- gdata plumbing + 13 structs parity-proven; 04-09 unblocked for GalleryVersionMetadataRequest.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-08-SUMMARY.md` when done.
</output>
