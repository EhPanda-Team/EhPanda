---
phase: 04-concurrency-framework-migration
plan: 09
type: execute
wave: 9
depends_on: [04-05, 04-06, 04-08]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+Detail.swift
  - AppPackage/Sources/NetworkingFeature/Request+Image.swift
  - AppPackage/Tests/NetworkingFeatureTests/DetailRequestBaselineTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "All 7 Request+Detail structs expose func response() async throws(AppError) with frozen 04-05 assertions passing unchanged"
    - "All 6 Request+Image structs are migrated; the fan-out request reproduces Combine semantics via withThrowingTaskGroup: per-child retry (4 attempts), one failing child fails the whole request and cancels siblings, index-keyed order restoration"
    - "After this plan, all 44 request structs have parity-proven typed-throws bodies (CONC-01's request layer fully migrated, pending consumer switch + teardown)"
  artifacts:
    - path: "AppPackage/Sources/NetworkingFeature/Request+Image.swift"
      provides: "typed-throws bodies incl. the withThrowingTaskGroup fan-out"
      contains: "withThrowingTaskGroup"
  key_links:
    - from: "AppPackage/Sources/NetworkingFeature/Request+Detail.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+GData.swift"
      via: "GalleryVersionMetadataRequest delegates to the async gdata plumbing"
      pattern: "gdataResponse"
---

<objective>
Migrate the detail and image families — the chained and fan-out pipelines — to typed-throws async
(D-01/D-02), proving parity by flipping the 04-05 baselines. This completes async bodies for all
44 request structs.

Publisher properties remain until 04-13.
Output: 13 proven async bodies incl. the task-group fan-out.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request+Detail.swift
@AppPackage/Sources/NetworkingFeature/Request+Image.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- `func response() async throws(AppError) -> Response` concrete methods on all 7 Request+Detail structs and all 6 Request+Image structs

<tasks>

<task type="auto">
  <name>Task 1: Detail bodies + detail baseline flip</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Detail.swift, AppPackage/Tests/NetworkingFeatureTests/DetailRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Detail.swift (all 7 pipelines; GalleryDetailRequest lines 28-72 is the canonical shape; GalleryVersionMetadataRequest at line 115 uses the gdata plumbing)
    - AppPackage/Sources/NetworkingFeature/Request+GData.swift (gdataResponse from 04-08)
    - AppPackage/Tests/NetworkingFeatureTests/DetailRequestBaselineTests.swift (acquisition lines; assertions frozen)
  </read_first>
  <action>
    Add typed-throws `response()` bodies to all 7 detail structs (keep publishers): identical
    URL/URLRequest assembly, fetch via the 04-06 helper with the stored urlSession, identical
    parse steps in a do/mapAppError funnel. GalleryVersionMetadataRequest delegates to
    gdataResponse. Any multi-fetch chain translates step-by-step with the SAME per-step retry
    placement the baseline locked (retried steps via the helper, un-retried steps via bare
    urlSession.data(for:) in a do/mapAppError funnel). Then flip DetailRequestBaselineTests
    acquisitions to `capture { try await … .response() }` — assertions byte-identical.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 7 detail structs proven parity; targeted run green.</done>
  <acceptance_criteria>
    - 7 occurrences of `func response() async throws(AppError)` in Request+Detail.swift
    - DetailRequestBaselineTests.swift contains no legacyResponse() call; git diff touches only acquisition lines
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Image bodies incl. task-group fan-out + image baseline flip</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Image.swift, AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Image.swift (all 6 pipelines; the fan-out at lines 86-117)
    - .planning/phases/04-concurrency-framework-migration/04-RESEARCH.md (§Pattern 4 — the withThrowingTaskGroup shape to copy: per-child fetch helper call INSIDE each child, group's untyped throw funneled once through mapAppError at the boundary)
    - AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift (acquisition lines; assertions frozen)
  </read_first>
  <action>
    Add typed-throws bodies to all 6 image structs (keep publishers). Single-fetch structs follow
    the standard shape. GalleryNormalImageURLsRequest follows RESEARCH Pattern 4 exactly:
    withThrowingTaskGroup of (index, imageURL, originalImageURL) tuples; each child calls the
    RETRY helper (per-child retry parity) then the same htmlDocument/parseResponse steps;
    results collected into index-keyed dictionaries; the whole body wrapped in do/mapAppError
    because the group throws untyped (the signature still guarantees throws(AppError)). Group
    failure cancels sibling children — matching Combine's teardown, and pinned by the frozen
    per-URL attempt assertions. Then flip ImageRequestBaselineTests acquisitions —
    assertions byte-identical.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 6 image structs proven parity incl. fan-out ordering, per-child retry, and fail-fast propagation; full suite green.</done>
  <acceptance_criteria>
    - 6 occurrences of `func response() async throws(AppError)` in Request+Image.swift
    - Request+Image.swift contains withThrowingTaskGroup
    - ImageRequestBaselineTests.swift contains no legacyResponse() call; git diff touches only acquisition lines
    - Full suite green (`cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air'`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → E-Hentai + Hath image hosts | fan-out thumbnail/image URL fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-20 | Denial of service | fan-out retry amplification | high | mitigate | Frozen per-URL attempt caps (4 failing / 1 healthy) re-proven; task-group fail-fast cancels siblings |
| T-04-21 | Repudiation | detail-page server-error text | medium | mitigate | ResponseParsingError funnel preserved verbatim in every body; frozen fixtures enforce |
</threat_model>

<verification>
- Frozen 04-05 assertions green through the async path; full suite green.
- Milestone: 44/44 request structs now carry parity-proven typed-throws bodies.
</verification>

<success_criteria>
- Detail + image families migrated and proven; the request layer's async rewrite is complete pending consumer switch (04-10..12) and Combine teardown (04-13).
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-09-SUMMARY.md` when done.
</output>
