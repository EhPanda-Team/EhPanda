---
phase: 04-concurrency-framework-migration
plan: 11
type: execute
wave: 11
depends_on: [04-06, 04-07, 04-08, 04-09]
files_modified:
  - AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsReducer.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesReducer.swift
  - AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchReducer.swift
  - AppPackage/Sources/DetailFeature/Previews/PreviewsReducer.swift
  - AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "All 25 Detail/Reading/AppFeature effect sites consume the typed-throws async path via the D-03 convention with Done actions and handlers untouched"
    - "Reading image-fetch effects (the highest-frequency request path in the app) behave identically: same actions, same cancellation ids"
  artifacts:
    - path: "AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift"
      provides: "6 converted image-fetch effect sites"
      contains: "do throws(AppError)"
  key_links:
    - from: "AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+Image.swift"
      via: "reading effects call the typed-throws fan-out/image requests"
      pattern: "try await"
---

<objective>
Switch the Detail, Reading, and AppFeature reducer effects (8 files, 25 sites) to the
parity-proven typed-throws path — D-01 call-site migration under the D-03 convention, Done
actions untouched (Pitfall 5).

Output: 25 converted effect sites; zero reducer state-machine changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift
</context>

## Artifacts this phase produces

Symbols created by this plan: none — existing effect closures rewritten to the
`do throws(AppError)` convention only.

<!-- planner-discipline-allow: legacyResponse -->

<tasks>

<task type="auto">
  <name>Task 1: DetailFeature effects (6 files, 18 sites)</name>
  <files>AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift, AppPackage/Sources/DetailFeature/Torrents/TorrentsReducer.swift, AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift, AppPackage/Sources/DetailFeature/Archives/ArchivesReducer.swift, AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchReducer.swift, AppPackage/Sources/DetailFeature/Previews/PreviewsReducer.swift</files>
  <read_first>
    - .planning/phases/04-concurrency-framework-migration/04-PATTERNS.md (Group 3 before/after; Pitfall 5 boundary; note the ~10 existing .run(operation:catch:) handlers in DetailReducer+Download / PreviewsReducer wrap DownloadClient/file work, NOT request calls — they are OUT of this sweep, leave them alone)
    - AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift (6 sites — the densest file)
  </read_first>
  <action>
    Convert every legacyResponse effect site in the six Detail files to the D-03 convention
    (explicit `do throws(AppError)` inside the existing .run closure; try-await the concrete
    request; identical success/failure Done sends; keep .cancellable ids and surrounding logic).
    Done handlers, action enums, and result switches in reducer bodies stay untouched. Do not
    modify any `.run(operation:catch:)` effect that wraps DownloadClient or file work.
  </action>
  <verify>
    <automated>test "$(grep -rn "legacyResponse" AppPackage/Sources/DetailFeature | wc -l | tr -d ' ')" -eq 0 && test "$(grep -rn "as? AppError" AppPackage/Sources/DetailFeature | wc -l | tr -d ' ')" -eq 0 && xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo GATE-OK</automated>
  </verify>
  <done>All 18 Detail sites converted; no facade calls or casts; build green; download/file effects untouched.</done>
  <acceptance_criteria>
    - Zero facade calls remain in DetailFeature; zero `as? AppError` introduced
    - `git diff` shows no change to any Done-case handler, action enum, or .run(operation:catch:) download effect
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reading + AppFeature effects (2 files, 7 sites)</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift, AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift (6 sites — image URL fetch/refetch/MPV effects; note their cancellation ids and any inline result handling)
    - AppPackage/Sources/AppFeature/DataFlow/AppRouteReducer.swift (1 site)
  </read_first>
  <action>
    Same conversion convention for the 6 Reading image-fetch sites and the 1 AppRoute site.
    Reading effects drive the reader's page loading — keep every cancellation id and send
    ordering identical; convert inline result switches into do/catch arms with identical sends.
  </action>
  <verify>
    <automated>test "$(grep -rn "legacyResponse" AppPackage/Sources/ReadingFeature AppPackage/Sources/AppFeature | wc -l | tr -d ' ')" -eq 0 && cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -3</automated>
  </verify>
  <done>All 25 plan sites converted; full suite green (ReadingFeatureTests unchanged and passing).</done>
  <acceptance_criteria>
    - Zero facade calls remain in ReadingFeature and AppFeature
    - Full suite passes; no test-file modifications in this plan
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reducer effects → request layer | error-path fidelity across the switch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-24 | Tampering | reading image pipeline drift | high | mitigate | Fan-out/image requests parity-proven in 04-09; effect sends and cancellation ids preserved verbatim; ReadingFeatureTests must pass unmodified |
| T-04-25 | Repudiation | error swallowed at conversion | medium | mitigate | Catch arms always send the same failure actions; cast-grep gate |
</threat_model>

<verification>
- Build + full suite green; no test modified; no facade call remains in the three dirs.
</verification>

<success_criteria>
- 25 sites consume the proven async path with literal reducer parity.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-11-SUMMARY.md` when done.
</output>
