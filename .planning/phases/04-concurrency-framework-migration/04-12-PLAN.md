---
phase: 04-concurrency-framework-migration
plan: 12
type: execute
wave: 12
depends_on: [04-06, 04-07, 04-08, 04-09]
files_modified:
  - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift
  - AppPackage/Sources/SettingFeature/SettingReducer+Body.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingReducer.swift
  - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "All 11 Setting effect sites and 7 DownloadClient plain-async sites consume the typed-throws path; Done actions, DownloadClient public signatures, and download orchestration behavior unchanged"
    - "The TagTranslator setting flow still treats AppError.noUpdates as benign control flow (same handling as today)"
  artifacts:
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift"
      provides: "direct try-await request consumption (Result unwrapping dropped)"
      contains: "try await"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request.swift"
      via: "TagTranslator flow calls the typed-throws body"
      pattern: "TagTranslatorRequest"
---

<objective>
Switch the last facade consumers — Setting reducers (4 files, 11 sites) and the DownloadClient
plain-async functions (2 files, 7 sites) — to the typed-throws path. Completes the D-01 64-site
call-site migration. DownloadClient keeps its public signatures (RESEARCH Open Question 3
resolution: convert minimally; DownloadClient decomposition is out of milestone scope).

Output: final 18 converted sites; the facade has zero remaining callers in Sources.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift
</context>

## Artifacts this phase produces

Symbols created by this plan: none — existing call sites rewritten only.

<!-- planner-discipline-allow: legacyResponse -->

<tasks>

<task type="auto">
  <name>Task 1: SettingFeature effects (4 files, 11 sites)</name>
  <files>AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift, AppPackage/Sources/SettingFeature/SettingReducer+Body.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingReducer.swift, AppPackage/Sources/SettingFeature/Login/LoginReducer.swift</files>
  <read_first>
    - .planning/phases/04-concurrency-framework-migration/04-PATTERNS.md (Group 3 convention; Pitfall 5 boundary)
    - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift (the TagTranslator flow — today it switches on the awaited Result inline; its noUpdates handling must convert to a catch arm with IDENTICAL outcomes per case)
  </read_first>
  <action>
    Convert the 11 Setting sites to the D-03 convention (explicit `do throws(AppError)` in .run;
    try-await concrete requests; identical Done sends; handlers untouched). The TagTranslator
    helper's inline result switch becomes do/catch: the success arm keeps its current success
    handling, and the catch arm reproduces today's per-case behavior — in particular
    AppError.noUpdates keeps its benign treatment (whatever the current switch does for it:
    same sends, same state effects, nothing new surfaced).
  </action>
  <verify>
    <automated>test "$(grep -rn "legacyResponse" AppPackage/Sources/SettingFeature | wc -l | tr -d ' ')" -eq 0 && xcodebuild build -project EhPanda.xcodeproj -scheme AppFeature -destination 'generic/platform=iOS Simulator' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo GATE-OK</automated>
  </verify>
  <done>All 11 Setting sites converted; noUpdates path behaviorally identical; build green.</done>
  <acceptance_criteria>
    - Zero facade calls remain in SettingFeature
    - The catch arm in the TagTranslator flow branches on the typed AppError (contains a noUpdates case) with the same outcomes as the previous switch
    - No Done handler or action enum changed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DownloadClient plain-async sites (2 files, 7 sites)</name>
  <files>AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift</files>
  <read_first>
    - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionFetch.swift (lines 12-23 and ~116: the `.get()`-style sites — PATTERNS Group 4 before/after)
    - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift (lines ~180, 197, 339, 351, 376: the switch-style sites)
  </read_first>
  <action>
    Convert the 7 DownloadClient sites per PATTERNS Group 4:
    - The two ExecutionFetch sites drop their Result unwrapping: `try await <Req>(…).response()`
      directly (these are function bodies already typed to throw).
    - The five ExecutionSupport switch-sites convert to explicit `do throws(AppError)` / catch
      (use the explicit form even in function bodies — grep-able convention consistency), with
      each catch arm reproducing the exact behavior of the previous .failure arm.
    Keep every enclosing function's public signature unchanged. Do not touch the existing
    `as? AppError` casts elsewhere in DownloadClient (DownloadPageDownloader, +Execution,
    +BackgroundDownloads) — they wrap non-request work and are out of scope.
  </action>
  <verify>
    <automated>test "$(grep -rn "legacyResponse" AppPackage/Sources | wc -l | tr -d ' ')" -eq 1 && cd AppPackage && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -3</automated>
  </verify>
  <done>All 64 production call sites migrated — the only remaining facade reference in Sources is its definition in Request.swift (count == 1); full suite green incl. DownloadSchedulingTests (a failure there is a REAL regression, not flake).</done>
  <acceptance_criteria>
    - `grep -rn "legacyResponse" AppPackage/Sources | wc -l` returns exactly 1 (the definition)
    - DownloadClient public function signatures unchanged (`git diff` shows no signature line edits)
    - Full suite passes; no test-file modifications in this plan
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Download orchestration → request layer | scheduling/retry behavior of downloads must not drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-26 | Denial of service | download fetch loops | high | mitigate | Request-level retry caps proven in 04-09; DownloadClient conversion preserves each failure arm verbatim; DownloadSchedulingTests (deterministic) must pass |
| T-04-27 | Tampering | login/setting submission flows | medium | mitigate | Effect sends preserved; account bodies parity-proven in 04-07 |
</threat_model>

<verification>
- Build + full suite green; facade has exactly one remaining reference (its own definition).
</verification>

<success_criteria>
- 64/64 call sites on the typed-throws path; production no longer exercises Combine; teardown (04-13) unblocked.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-12-SUMMARY.md` when done.
</output>
