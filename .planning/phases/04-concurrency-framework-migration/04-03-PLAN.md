---
phase: 04-concurrency-framework-migration
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - AppPackage/Tests/NetworkingFeatureTests/RoutineRequestBaselineTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The current Combine layer's behavior is frozen for the 4 routine requests and all 14 account requests: URLRequest assembly, fixture parse output, retry counts, and error mapping (D-05: baseline locked BEFORE any swap)"
    - "TagTranslatorRequest's two-step chain is locked: date-gated download, noUpdates early-out (second fetch never issued), retried first fetch (4 attempts), un-retried second fetch (1 attempt)"
    - "The mapAppError table is locked, including the ResponseParsingError server-text path (ban text survives to a typed AppError)"
  artifacts:
    - path: "AppPackage/Tests/NetworkingFeatureTests/RoutineRequestBaselineTests.swift"
      provides: "baseline for GreetingRequest/UserInfoRequest/FavoriteCategoriesRequest/TagTranslatorRequest + generic retry semantics + mapAppError table"
      contains: "TagTranslatorRequest"
    - path: "AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift"
      provides: "baseline for the 14 Request+Account structs (POST assembly, response mapping)"
      contains: "LoginRequest"
  key_links:
    - from: "AppPackage/Tests/NetworkingFeatureTests/RoutineRequestBaselineTests.swift"
      to: "AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift"
      via: "every baseline acquires results through a stubbed session"
      pattern: "makeStubbedSession"
    - from: "AppPackage/Tests/NetworkingFeatureTests/RoutineRequestBaselineTests.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request.swift"
      via: "acquisition through the Combine facade at lock time"
      pattern: "legacyResponse\\(\\)"
---

<objective>
Lock the Wave-0 parity baseline (D-05/D-06) for the routine and account request families against
TODAY'S Combine layer — before any async body exists. These frozen assertions are re-run verbatim
against the typed-throws path in plans 04-06/04-07; passing both sides IS the parity proof.

Scope boundary (D-08): NetworkingFeature only — no CookieClient/ImageClient coverage (Phase 8).
Purpose: CONC-01's "request behavior and error paths preserved" becomes mechanically checkable.
Output: two characterization suites, green against the current code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@.planning/phases/04-concurrency-framework-migration/04-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request.swift
@AppPackage/Sources/NetworkingFeature/Request+Account.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- `RoutineRequestBaselineTests` suite (+ its inline fixture strings)
- `AccountRequestBaselineTests` suite (+ its inline fixture strings)

Note on method: these are CHARACTERIZATION tests — they must pass against the CURRENT code from
the first run (not TDD red/green; the D-05 method is lock-then-swap). Expected values are read
from the current implementation's actual deterministic output.

<tasks>

<task type="auto">
  <name>Task 1: Routine baseline — 4 requests + retry semantics + mapAppError table</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/RoutineRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request.swift (the 4 request bodies, lines 255-341; mapAppError lines 111-136; ResponseParsingError lines 15-18)
    - AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift and Support/RequestHarness.swift (harness API)
    - AppPackage/Tests/NetworkingFeatureTests/DFRequestSemanticsTests.swift (doc-comment + suite conventions to copy)
    - AppPackage/Tests/ParserFeatureTests/Other/GreetingParserTests.swift and Other/DownloadPageErrorParserTests.swift (fixture shapes for parseGreeting and for server-error text such as IP-ban pages)
  </read_first>
  <action>
    Create RoutineRequestBaselineTests (Swift Testing, @testable import NetworkingFeature; header
    doc-comment per the DF-tests idiom stating this suite freezes the request-layer contract per
    D-05/D-06 and never opens a socket). Acquire every result via
    `await <Request>(…, urlSession: session).legacyResponse()` on a stubbed session. Lock, using
    inline minimal fixtures derived from the ParserFeatureTests analogs:

    1. Greeting/UserInfo/FavoriteCategories — received URLRequest (URL from Defaults.URL.news /
       URLUtil.userInfo(uid:) / Defaults.URL.uConfig, GET, nil body) and parse output on a
       fixture (assert a small set of deterministic fields of the parsed model).
    2. Generic retry semantics via GreetingRequest: persistent transportFailure script →
       attempts == 4 and .failure(.networkingFailed); success script → attempts == 1.
    3. TagTranslatorRequest (all four paths, per-URL scripts):
       a) newer published_at JSON on the githubAPI URL + payload bytes on the githubDownload URL
          → .success with the exact payload data and parsed date; attempts(githubAPI) == 1,
          attempts(githubDownload) == 1;
       b) not-newer date → .failure(.noUpdates) AND attempts(githubDownload) == 0 (early-out);
       c) malformed JSON → .failure(.parseFailed);
       d) fetch-2 transportFailure after a good fetch-1 → attempts(githubDownload) == 1 exactly
          (the download step is NOT retried — asymmetry to preserve) and .failure(.networkingFailed).
    4. mapAppError table (call it directly on a request instance): URLError → .networkingFailed;
       Kanna ParseError → .parseFailed; a DecodingError → .parseFailed; an AppError passes
       through unchanged; an arbitrary Error → .unknown; and end-to-end server-text path — serve
       ban-style HTML (structure borrowed from DownloadPageErrorParserTests) through
       FavoriteCategoriesRequest so the parse failure carries the parsed response error out as
       the mapped AppError (e.g. .ipBanned) instead of a generic .parseFailed.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>Suite green against the current Combine layer; all four TagTranslator paths and the full mapAppError table locked; retry counts asserted via stub attempt counters.</done>
  <acceptance_criteria>
    - RoutineRequestBaselineTests.swift asserts attempts == 4 for a persistent transport failure and attempts == 1 for success
    - The noUpdates test asserts the download URL was never loaded (attempts == 0)
    - The server-text test expects a specific non-parseFailed AppError case from fixture HTML
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Account baseline — 14 POST/GET requests, assembly + response mapping</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift (all 14 structs — read fully; note LoginRequest maps to HTTPURLResponse?, IgneousRequest compactMaps to HTTPURLResponse)
    - AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift (body-capture API)
  </read_first>
  <action>
    Create AccountRequestBaselineTests locking, for each of the 14 Request+Account structs:

    1. Assembly: received URL, httpMethod, Content-Type header where set
       (setURLEncodedContentType), and the POST body. CRITICAL: form bodies are built via
       `params.dictString()` over a Dictionary — key ORDER is nondeterministic. Never
       byte-compare bodies; decode the urlencoded body into a [String: String] and compare
       dictionaries (same for any JSON body via JSONSerialization round-trip). Assert the exact
       expected key/value pairs (e.g. LoginRequest: b, bt, CookieDate, UserName, PassWord,
       ipb_login_submit with their current literal values).
    2. Response mapping per struct on fixture scripts: LoginRequest yields the HTTPURLResponse
       optionally (assert status/url of the returned response); IgneousRequest's current
       compactMap behavior — a load that produces no HTTPURLResponse-castable response value
       completes empty and surfaces as .failure(.unknown) through the continuation (lock this
       exact mapping); parse-backed requests (VerifyEhProfileRequest, EhProfileRequest,
       EhSettingRequest, SubmitEhSettingChangesRequest, and the vote/favor/rate/comment ops)
       assert their parse output on minimal fixtures derived from the corresponding
       ParserFeatureTests analogs, or their Void/simple success where the pipeline discards
       content.
    3. Retry: one representative account POST with persistent transport failure → attempts == 4
       (retry applies to account ops identically).

    Acquisition via legacyResponse() throughout. If a struct's parser needs an impractically
    large fixture, borrow the fixture content from ParserFeatureTests rather than inventing one —
    do NOT skip the struct; all 14 must appear.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 14 account structs have assembly + response-mapping baselines; suite green against current code.</done>
  <acceptance_criteria>
    - `grep -c "@Test" AppPackage/Tests/NetworkingFeatureTests/AccountRequestBaselineTests.swift` is >= 14
    - Each of the 14 struct names appears in the file (LoginRequest, IgneousRequest, VerifyEhProfileRequest, EhProfileRequest, EhSettingRequest, SubmitEhSettingChangesRequest, FavorGalleryRequest, UnfavorGalleryRequest, RateGalleryRequest, CommentGalleryRequest, EditGalleryCommentRequest, VoteGalleryCommentRequest, VoteGalleryTagRequest, SendDownloadCommandRequest)
    - No raw-bytes equality on any dictionary-built body (bodies compared structurally)
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → E-Hentai origin | request assembly + retry policy locked here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Denial of service | retry policy | high | mitigate | Baseline pins 4-attempts-max and the un-retried TagTranslator download; any post-migration drift (retry amplification against the origin → self-ban) fails these tests |
| T-04-06 | Repudiation | server ban/block messaging | medium | mitigate | Server-text path locked end-to-end (ResponseParsingError → mapAppError); a rewrite that flattens it to .parseFailed fails the baseline |
| T-04-07 | Information disclosure | login credentials in tests | low | mitigate | Fixtures use dummy credentials only; no real cookies/credentials appear in test code |
</threat_model>

<verification>
- Targeted NetworkingFeatureTests run green after each task (characterization: green on first run against current code).
- No live network: every test acquires through a stubbed session.
</verification>

<success_criteria>
- 18 request structs (4 routine + 14 account) have frozen assembly/parse/error baselines; retry counts and the TagTranslator asymmetry are locked; mapAppError table locked including server-text.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-03-SUMMARY.md` when done.
</output>
