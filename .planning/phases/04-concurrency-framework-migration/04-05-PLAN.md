---
phase: 04-concurrency-framework-migration
plan: 05
type: execute
wave: 5
depends_on: [04-01, 04-02]
files_modified:
  - AppPackage/Tests/NetworkingFeatureTests/DetailRequestBaselineTests.swift
  - AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The 7 Request+Detail structs have frozen assembly + fixture-parse baselines against the current Combine layer (D-05/D-06)"
    - "The 6 Request+Image structs are frozen, including the GalleryNormalImageURLsRequest fan-out contract: N per-thumbnail fetches, per-child retry (4 attempts), one failing child fails the whole request, index-keyed order restoration"
  artifacts:
    - path: "AppPackage/Tests/NetworkingFeatureTests/DetailRequestBaselineTests.swift"
      provides: "baseline for the 7 detail-family requests"
      contains: "GalleryDetailRequest"
    - path: "AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift"
      provides: "baseline for the 6 image-family requests incl. fan-out semantics"
      contains: "GalleryNormalImageURLsRequest"
  key_links:
    - from: "AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift"
      to: "AppPackage/Sources/NetworkingFeature/Request+Image.swift"
      via: "fan-out attempts counted per thumbnail URL through the stub"
      pattern: "attempts"
---

<objective>
Lock the Wave-0 parity baseline (D-05/D-06) for the detail and image families — the two families
with multi-step/fan-out pipelines — against TODAY'S Combine layer. Frozen assertions are re-run
against the typed-throws path in plan 04-09.

Purpose: the structurally hardest rewrites (task-group fan-out, chained fetches) get the tightest
behavioral net before they are touched.
Output: two characterization suites, green against current code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concurrency-framework-migration/04-CONTEXT.md
@.planning/phases/04-concurrency-framework-migration/04-RESEARCH.md
@AppPackage/Sources/NetworkingFeature/Request+Detail.swift
@AppPackage/Sources/NetworkingFeature/Request+Image.swift
</context>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):
- `DetailRequestBaselineTests` suite (+ inline fixtures)
- `ImageRequestBaselineTests` suite (+ inline fixtures)

<tasks>

<task type="auto">
  <name>Task 1: Detail baseline — 7 requests</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/DetailRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Detail.swift (all 7 structs incl. GalleryDetailRequest lines 28-72 and the gdata-backed GalleryVersionMetadataRequest at line 115)
    - AppPackage/Tests/ParserFeatureTests/Gallery/GalleryDetailParserTests.swift (detail-page fixture to borrow — do not hand-craft a minimal detail page if a working fixture exists)
    - AppPackage/Tests/NetworkingFeatureTests/Support/CountingStubProtocol.swift (harness API)
  </read_first>
  <action>
    Create DetailRequestBaselineTests locking, for each of the 7 structs in Request+Detail.swift
    (GalleryDetailRequest, GalleryVersionMetadataRequest, GalleryArchiveRequest,
    GalleryArchiveFundsRequest, GalleryTorrentsRequest, GalleryReverseRequest, MPVKeysRequest —
    confirm the exact set by reading the file; every struct in the file must appear):

    1. Assembly: received URL/method/headers/body for representative constructor arguments
       (structural body comparison for any dictionary-built body).
    2. Parse output on fixtures: borrow detail-page HTML from GalleryDetailParserTests for
       GalleryDetailRequest (assert deterministic fields of the response tuple/struct); minimal
       fixtures for the smaller parsers; GalleryVersionMetadataRequest goes through the gdata
       plumbing — assert its structural JSON body and decode result like 04-04 did.
    3. One error-path test per multi-step pipeline: where a struct chains fetches (read the file;
       any flatMap chain must have its per-step attempt counts locked the way 04-03 locked
       TagTranslatorRequest — retried steps == 4, un-retried steps == 1).

    Acquisition via legacyResponse() on stubbed sessions (these structs already accept an
    injected session as of 04-01).
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 7 detail structs frozen (assembly + parse + chain step counts where applicable); suite green.</done>
  <acceptance_criteria>
    - Every struct declared in Request+Detail.swift appears in DetailRequestBaselineTests.swift
    - GalleryVersionMetadataRequest's gdata body is asserted structurally via JSONSerialization
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Image baseline — 6 requests incl. fan-out contract</name>
  <files>AppPackage/Tests/NetworkingFeatureTests/ImageRequestBaselineTests.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Image.swift (all 6 structs; the fan-out at lines 86-117 — per-child dataTaskPublisher + genericRetry + collect)
    - AppPackage/Tests/ParserFeatureTests/Gallery/GalleryImageURLParserTests.swift (image-page fixture shapes)
  </read_first>
  <action>
    Create ImageRequestBaselineTests locking, for each of the 6 structs in Request+Image.swift
    (ThumbnailURLsRequest, GalleryNormalImageURLsRequest, GalleryNormalImageURLRefetchRequest,
    GalleryMPVImageURLRequest, GalleryPreviewURLsRequest, and the sixth per the file — confirm by
    reading; every struct must appear):

    1. Assembly + parse on fixtures for the single-fetch requests (note
       GalleryPreviewURLsRequest's response carries an HTTPURLResponse — assert its presence and
       status alongside the parsed URLs).
    2. GalleryNormalImageURLsRequest fan-out contract, with per-URL scripts across 3 thumbnail
       URLs:
       a) all children succeed → result dictionaries keyed by the ORIGINAL indexes regardless of
          response ordering (serve children with distinguishable fixture pages);
       b) per-child retry: one child scripted to fail persistently → that child's URL shows
          attempts == 4 and the whole request fails (one failure fails all);
       c) children that succeeded are not retried (attempts == 1 for each healthy child URL).
    3. Any chained pipeline in the refetch/MPV requests gets per-step attempt locks as in Task 1.

    Acquisition via legacyResponse() on stubbed sessions.
  </action>
  <verify>
    <automated>cd AppPackage && xcodebuild test -scheme AppPackage-Package -only-testing:NetworkingFeatureTests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | grep -E "✔ Test run with [0-9]+ tests" | tail -2</automated>
  </verify>
  <done>All 6 image structs frozen; the fan-out's per-child retry, failure propagation, and index-keyed ordering locked; suite green.</done>
  <acceptance_criteria>
    - Every struct declared in Request+Image.swift appears in ImageRequestBaselineTests.swift
    - Fan-out tests assert per-URL attempt counts (4 for the failing child, 1 for healthy children) and index-keyed results
    - Targeted NetworkingFeatureTests run passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → E-Hentai + Hath image hosts | image URL fetch fan-out crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Denial of service | fan-out retry amplification | high | mitigate | Per-child 4-attempt cap and fail-fast propagation frozen; a task-group rewrite that retries healthy children or loops the failing child fails the baseline |
| T-04-11 | Repudiation | detail-page server errors | medium | mitigate | Parse-error mapping through ResponseParsingError already locked in 04-03 and exercised here on detail fixtures |
</threat_model>

<verification>
- Targeted NetworkingFeatureTests run green after each task; no live network anywhere.
- After this plan, all 44 request structs have frozen baselines (4+14 in 04-03, 13 in 04-04, 13 here) — D-06 fully covered alongside the existing DF semantics tests.
</verification>

<success_criteria>
- Detail + image families frozen, including the fan-out contract that plan 04-09 must reproduce with withThrowingTaskGroup.
</success_criteria>

<output>
Create `.planning/phases/04-concurrency-framework-migration/04-05-SUMMARY.md` when done.
</output>
