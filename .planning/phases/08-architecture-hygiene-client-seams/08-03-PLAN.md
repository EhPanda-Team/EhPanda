---
phase: 08-architecture-hygiene-client-seams
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+Gallery.swift
  - AppPackage/Sources/SearchFeature/SearchReducer.swift
  - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift
  - AppPackage/Sources/HomeFeature/Toplists/ToplistsReducer.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift
  - AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchReducer.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "Every gallery-list Request struct (Search/Frontpage/Popular/Watched/Favorites/Toplists + their More* variants + DateSeek) stores an explicit host: GalleryHost and passes it into the URLUtil builder — no gallery-list request resolves host from a global."
    - "Each constructing reducer supplies host: setting.galleryHost from @Shared/@SharedReader(.setting)."
    - "Request URLs are byte-identical for the active host — NetworkingFeatureTests green."
  artifacts:
    - "host: GalleryHost stored property + init parameter on the 12 gallery-list Request structs."
  key_links:
    - "Reducer @Shared(.setting).galleryHost → Request(host:) → URLUtil.<builder>(host:)."
---

<objective>
Seam A step 2 (D-03): thread **explicit** host through the gallery-list request layer — the 12
`Request+Gallery.swift` conformers store a `host: GalleryHost` and pass it to their `URLUtil`
builder, and every constructing reducer supplies `setting.galleryHost`. This removes the transitional
default's role for these requests (they now pass host explicitly).

Purpose: the list requests are the highest-traffic host consumers. D-03 makes host an explicit
request parameter (single source of truth = `@Shared(.setting).galleryHost`). Guarded by the Phase-4
`NetworkingFeatureTests` baselines.

Output: gallery-list requests + their reducers thread explicit host; NetworkingFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request+Gallery.swift
</context>

## Artifacts this phase produces

New signatures: `host: GalleryHost` stored property + init parameter on the 12 gallery-list Request
structs (SearchGalleriesRequest, MoreSearchGalleriesRequest, DateSeekGalleriesRequest,
FrontpageGalleriesRequest, MoreFrontpageGalleriesRequest, PopularGalleriesRequest,
WatchedGalleriesRequest, MoreWatchedGalleriesRequest, FavoritesGalleriesRequest,
MoreFavoritesGalleriesRequest, ToplistsGalleriesRequest, MoreToplistsGalleriesRequest). Exclude from
drift verification.

<execution_notes>
xcodebuild invocations must never overlap — one at a time.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Give the gallery-list Request structs an explicit host</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Gallery.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Gallery.swift:8-38 (SearchGalleriesRequest — the analog: stored properties + init + response()).
    - 08-PATTERNS.md §Pattern 3 (host as an explicit Request parameter).
  </read_first>
  <action>
    For each of the 12 conformers in Request+Gallery.swift, add `public let host: GalleryHost`, add
    `host: GalleryHost` to the `init` (first parameter, no default — explicit), and pass `host: host`
    into the `URLUtil.<builder>(...)` call inside `response()` (e.g.
    `URLUtil.searchList(host: host, keyword: keyword, filter: filter)`). Keep every other property,
    the `urlSession` seam, and the `response()` body identical. Do not change error mapping or retry
    placement.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - All 12 gallery-list Request structs declare `host: GalleryHost` and pass it to their URLUtil builder.
    - No gallery-list request relies on the URLUtil host default (each call passes `host:` explicitly).
    - Build succeeds.
  </acceptance_criteria>
  <done>Gallery-list requests carry explicit host; URLUtil builders receive it directly.</done>
</task>

<task type="auto">
  <name>Task 2: Supply setting.galleryHost at every gallery-list construction site</name>
  <files>AppPackage/Sources/SearchFeature/SearchReducer.swift, AppPackage/Sources/HomeFeature/HomeReducer+Body.swift, AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift, AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift, AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift, AppPackage/Sources/HomeFeature/Toplists/ToplistsReducer.swift, AppPackage/Sources/FavoritesFeature/FavoritesReducer.swift, AppPackage/Sources/DetailFeature/DetailSearch/DetailSearchReducer.swift</files>
  <read_first>
    - Each reducer's existing gallery-request construction call (grep for the request type names).
    - The existing `@Shared(.setting)` / `@SharedReader(.setting)` declaration in each reducer's State (SearchReducer/Frontpage/Popular/Watched/Toplists/Favorites already have it; HomeReducer+Body and DetailSearchReducer must read it too).
  </read_first>
  <action>
    At each construction site, pass `host: state.setting.galleryHost` (or the reducer's existing
    `@Shared(.setting)`/`@SharedReader(.setting)` accessor) into the Request initializer. Where a
    reducer does not yet hold the setting (HomeReducer+Body, DetailSearchReducer), add a
    `@SharedReader(.setting) var setting` (read-only) to its State and use `state.setting.galleryHost`
    — matching the D-03 single-source-of-truth pattern and Phase-5 precedent. Do not otherwise change
    reducer behavior, action flow, or effect cancellation IDs.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:NetworkingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - Every gallery-list request is constructed with `host: <setting>.galleryHost`.
    - No construction site relies on an implicit host default.
    - NetworkingFeatureTests passes; build succeeds.
  </acceptance_criteria>
  <done>All gallery-list requests receive host from @Shared(.setting) at their reducers; request-URL parity holds.</done>
</task>

</tasks>

<verification>
- Build + `-only-testing:NetworkingFeatureTests` green (single, non-overlapping invocation).
- `grep -c "host:" AppPackage/Sources/NetworkingFeature/Request+Gallery.swift` shows host threaded through all 12 conformers.
</verification>

<success_criteria>
The gallery-list request path resolves host only from `@Shared(.setting)` via an explicit parameter,
at request-URL parity.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-03-SUMMARY.md` when done.
</output>
