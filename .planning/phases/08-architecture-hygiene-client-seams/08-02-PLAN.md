---
phase: 08-architecture-hygiene-client-seams
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - AppPackage/Sources/AppModels/Utilities/URLUtil.swift
  - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "Every URLUtil builder that resolved the gallery host from the global now accepts a host: GalleryHost and builds URLs from host.url — with a transitional default that keeps all existing callers compiling."
    - "Each host-derived Defaults.URL.* (api/myTags/uConfig/popular/watched/favorites/galleryPopups/galleryTorrents/toplist) is available as a host-taking helper function; the global properties remain temporarily so untouched callers still compile."
    - "The full NetworkingFeatureTests suite (Phase-4 baselines) stays green — request URLs are unchanged for the default host."
  artifacts:
    - "URLUtil builders with host: GalleryHost parameters."
    - "Defaults.URL.api(host:)/myTags(host:)/uConfig(host:)/popular(host:)/watched(host:)/favorites(host:)/galleryPopups(host:)/galleryTorrents(host:)/toplist(host:) helper functions."
  key_links:
    - "URLUtil builder host param default → AppUtil.galleryHost (transitional bridge, removed in 08-07 teardown)."
---

<objective>
Seam A step 1 (largest-blast-radius wave, D-03): introduce host-taking URL construction **without
breaking any caller yet**. Give `URLUtil`'s builders a `host: GalleryHost` parameter (build from
`host.url`) and add host-taking helper functions alongside the host-derived `Defaults.URL.*`
globals, using a transitional default so the tree stays green with zero caller edits.

Purpose: `URLUtil` (D-06 pure namespace) and `Defaults.URL` currently resolve the active host from
a hidden global (`Defaults.URL.host` → `AppUtil.galleryHost` → a `UserDefaults` mirror). D-03
parameterizes host all the way. This step lays the parameterized rails; plans 08-03…08-06 thread
explicit host through callers; 08-07 deletes the global + defaults.

Output: parameterized `URLUtil` + `Defaults.URL` host helpers; build + NetworkingFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/AppModels/Utilities/URLUtil.swift
@AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
@AppPackage/Sources/AppModels/Utilities/AppUtil.swift
</context>

## Artifacts this phase produces

New/changed symbols introduced here: `host: GalleryHost` parameters on the 19 `URLUtil` builders;
host-taking `Defaults.URL.<name>(host:)` helper functions. These are net-new signatures — exclude
from drift verification. The transitional `= AppUtil.galleryHost` default is removed in 08-07.

<execution_notes>
xcodebuild invocations must never overlap on this machine — run each build/test as a single
invocation, never concurrently.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Add host-taking helper functions to Defaults.URL (globals kept)</name>
  <files>AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift:13-23 (the host global + the 8 host-derived computed properties).
    - AppPackage/Sources/AppModels/Persistent/Setting.swift:149-162 (GalleryHost enum + `url` + `cookieURLs`).
    - 08-PATTERNS.md §"Defaults+Runtime.swift (host global → host-taking helpers)".
  </read_first>
  <action>
    Add host-taking helper functions to the `Defaults.URL` extension: `api(host:)`, `myTags(host:)`,
    `uConfig(host:)`, `galleryPopups(host:)`, `galleryTorrents(host:)`, `popular(host:)`,
    `watched(host:)`, `favorites(host:)` — each returning `host.url.appendingPathComponent(<the
    same path string the current property uses>)` (e.g. `api(host:)` → `host.url.appendingPathComponent("api.php")`).
    Also add `toplist(host:)` matching the existing `Defaults.URL.toplist` path. **Keep** the
    existing global `host` property and the 8 derived global properties in place for now (they still
    read `AppUtil.galleryHost`) — untouched callers depend on them until 08-07. Preserve the exact
    path strings so URLs are byte-identical.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `Defaults.URL.api(host:)` and each sibling helper exist and return `host.url` + the same path component string as the matching global property.
    - The global `host` property and the 8 derived global properties still exist (untouched callers compile).
    - Build succeeds.
  </acceptance_criteria>
  <done>Host-taking Defaults.URL helpers exist alongside the (still-live) globals; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize URLUtil builders with host: GalleryHost (transitional default)</name>
  <files>AppPackage/Sources/AppModels/Utilities/URLUtil.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Utilities/URLUtil.swift (all 19 static builders; note which read Defaults.URL.host / popular / watched / favorites / toplist / galleryTorrents / galleryPopups).
    - 08-PATTERNS.md §"URLUtil.swift (builders gain host:)" and §Pattern 3 (SearchGalleriesRequest analog).
    - 08-RESEARCH.md §"Open Questions" Q1 (host-taking helpers preferred over inlining).
  </read_first>
  <action>
    Prepend a `host: GalleryHost` parameter to every `URLUtil` builder that currently resolves the
    active host from the global — `searchList`, `moreSearchList`, `frontpageList`,
    `moreFrontpageList`, `popularList`, `watchedList`, `moreWatchedList`, `favoritesList`,
    `moreFavoritesList`, `toplistsList`, `moreToplistsList`, `galleryTorrents`, `addFavorite`, and
    any other builder reading a host-derived `Defaults.URL.*`. Replace each internal
    `Defaults.URL.host` with `host.url` and each host-derived `Defaults.URL.popular/watched/
    favorites/toplist/galleryTorrents/galleryPopups` with the matching `Defaults.URL.<name>(host:)`
    helper from Task 1. Give the new `host:` parameter a **transitional default**
    `= AppUtil.galleryHost` so all current call sites compile unchanged; this default is removed in
    08-07. Do NOT add host to GitHub builders (`githubAPI`/`githubDownload`), `userInfo`,
    `detailPage`, `galleryDetail`, `combinedPreviewURL` — they never read the host global. Keep the
    private `applyingFilter` extension unchanged.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:NetworkingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - Every host-dependent URLUtil builder takes `host: GalleryHost` (transitional default `= AppUtil.galleryHost`) and builds from `host.url` / the Defaults.URL host-helpers.
    - `grep -c "Defaults.URL.host" AppPackage/Sources/AppModels/Utilities/URLUtil.swift` returns 0 (URLUtil no longer reads the host global directly).
    - Non-host builders (githubAPI/githubDownload/userInfo/detailPage/galleryDetail/combinedPreviewURL) are unchanged.
    - NetworkingFeatureTests passes (request URLs byte-identical for the default host).
  </acceptance_criteria>
  <done>URLUtil builds host-taking URLs with a compile-preserving default; Phase-4 request-URL baselines green.</done>
</task>

</tasks>

<verification>
- `xcodebuild build` succeeds; `-only-testing:NetworkingFeatureTests` passes (single, non-overlapping invocation).
- URLUtil no longer references `Defaults.URL.host`; Defaults.URL host-helpers exist; the globals remain for now.
</verification>

<success_criteria>
Host-taking URL construction exists end-to-end at parity, with a transitional bridge that keeps
every caller compiling — the rails for threading explicit host in 08-03…08-06.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-02-SUMMARY.md` when done.
</output>
