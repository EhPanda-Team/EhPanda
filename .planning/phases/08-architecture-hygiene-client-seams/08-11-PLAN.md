---
phase: 08-architecture-hygiene-client-seams
plan: 11
type: execute
wave: 11
depends_on: ["08-10"]
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Tests/CookieClientTests/CookieClientTests.swift
  - AppPackage/Tests/CookieClientTests/.swiftlint.yml
autonomous: true
requirements: [QUAL-02, HYG-01]
must_haves:
  truths:
    - "A dedicated CookieClientTests target exists (wired in Package.swift + a parent_config .swiftlint.yml) and runs in the suite."
    - "didLogin is covered across the full matrix: eh vs ex hosts × igneous present/mystery/absent × expiry — proving the CookieClient.didLogin accessor that will replace CookieUtil.didLogin at 12 login-gated sites."
    - "setCredentials + setSkipServer Set-Cookie header parsing, syncExCookies (host-exact), fulfillAnotherHostField, and importAutomationCookies are covered; untouched paths (editCookie/removeYay/loadCookiesState) are NOT padded."
    - "Tests are deterministic and green, using CookieClient.testing(...) and CookieClient.live(cookieStorage:) with per-test HTTPCookieStorage (never .shared)."
  artifacts:
    - "AppPackage/Tests/CookieClientTests/ target (Module enum case cookieClientTests + testTarget + .swiftlint.yml)."
  key_links:
    - "CookieClient.didLogin truth values (this suite) → the 12 login-gated view sites migrated in 08-12."
---

<objective>
Seam F/QUAL-02 + HYG-01 login-parity (D-10, Pitfall 2): stand up a dedicated `CookieClientTests`
target covering the reworked cookie seam with a behavior matrix — deep, not padded. This proves the
`CookieClient.didLogin` accessor is semantically identical to the `CookieUtil.didLogin` it replaces
**before** 08-12 deletes `CookieUtil` (the 12 view sites are login-gated real controls).

Purpose: QUAL-02 requires deterministic CookieClient coverage; this suite is also the guard for the
08-12 CookieUtil deletion (Pitfall 2: write the didLogin matrix first, confirm truth values, then
delete). CookieClient signatures are final here (apiuid(host:)/setSkipServer(response:host:) landed in
08-05/08-06).

Output: green, deterministic CookieClientTests over the D-10 matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@.planning/phases/08-architecture-hygiene-client-seams/08-VALIDATION.md
@AppPackage/Sources/CookieClient/CookieClient.swift
</context>

## Artifacts this phase produces

New: `CookieClientTests` test target — `Module` enum case `cookieClientTests = "CookieClientTests"`, a
`.testTarget` entry, `Tests/CookieClientTests/.swiftlint.yml` (parent_config), and the test file.
Exclude these newly-created symbols from drift verification.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. New test dir MUST carry
`parent_config: ../../../.swiftlint.yml`. Use the in-repo doubles — do NOT hand-roll a cookie store
(RESEARCH §Don't Hand-Roll).
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Wire the CookieClientTests target</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/CookieClientTests/.swiftlint.yml</files>
  <read_first>
    - AppPackage/Package.swift:121 + :945-951 (Module enum case + `.imageColorsTests` testTarget analog); :890-898 (fileClientTests dependency shape).
    - AppPackage/Tests/ImageColorsTests/.swiftlint.yml.
  </read_first>
  <action>
    Add `case cookieClientTests = "CookieClientTests"` to the `Module` enum and a `.testTarget(module:
    .cookieClientTests, dependencies: [.module(.cookieClient), .module(.appModels)], plugins:
    swiftLintPlugins)` entry (add `.composableArchitecture` only if the test file imports it directly).
    Create `Tests/CookieClientTests/.swiftlint.yml` containing `parent_config: ../../../.swiftlint.yml`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - Package.swift declares the `cookieClientTests` module case + testTarget with `.module(.cookieClient)` + `.module(.appModels)` + swiftLintPlugins.
    - `Tests/CookieClientTests/.swiftlint.yml` contains exactly `parent_config: ../../../.swiftlint.yml`.
    - Build succeeds.
  </acceptance_criteria>
  <done>The CookieClientTests target is registered and lint-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-10 CookieClient behavior matrix</name>
  <files>AppPackage/Tests/CookieClientTests/CookieClientTests.swift</files>
  <read_first>
    - AppPackage/Sources/CookieClient/CookieClient.swift:189-198 (didLogin), :294-324 (setCredentials/setSkipServer Set-Cookie parsing), :217-230 (syncExCookies host-exact), :235-252 (fulfillAnotherHostField), :115-156 (importAutomationCookies), :485-540 (testing(...) double), :23 (live(cookieStorage:)).
    - AppPackage/Sources/CookieClient/CookieClient.swift:369-388 (CookieClientTestingCookie.matches — host-exact matching, the syncExCookies collateral-write guard).
    - 08-RESEARCH.md §Code Examples (setCredentials header-parse test) + §Pitfall 2 (didLogin semantic drift axes).
    - TESTING.md (Swift Testing @Suite/@Test patterns; never .shared).
  </read_first>
  <action>
    Write Swift Testing cases covering: **didLogin** — eh-only login (true), ex login with igneous
    present (true), ex login with igneous == mystery (false), ex login with igneous absent (false),
    no cookies (false), and an expired-cookie case (false) using `CookieClient.live(cookieStorage:)`
    with a per-test `HTTPCookieStorage()` seeded with a past `expiresDate`; use
    `CookieClient.testing(memberID:passHash:igneous:)` for the host/igneous axes. **setCredentials** /
    **setSkipServer** — build a per-test `HTTPCookieStorage()` client, feed a synthetic
    `HTTPURLResponse` with a `Set-Cookie` header, and assert the parsed cookie values (setSkipServer
    takes the new `host:` parameter). **syncExCookies** — assert ex→sexhentai propagation and that
    host-exact matching does not collaterally clobber a sibling host. **fulfillAnotherHostField** —
    assert eh→ex and ex→eh backfill. **importAutomationCookies** — assert the eh/ex/sexhentai member/
    pass/igneous population + that igneous is set only on ex/sexhentai. Do NOT add cases for
    editCookie/removeYay/loadCookiesState (D-10 no-pad). Each storage-touching test uses its own
    HTTPCookieStorage/testing store — never `.shared`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:CookieClientTests</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:CookieClientTests` passes deterministically across the full didLogin matrix + the 5 other methods.
    - The expiry case uses `live(cookieStorage:)` with a per-test storage and a past expiresDate; no test touches `.shared`.
    - A syncExCookies case asserts host-exact matching prevents sibling-host cookie loss.
    - No editCookie/removeYay/loadCookiesState cases (no padding).
  </acceptance_criteria>
  <done>The reworked CookieClient seam has deep, deterministic coverage; didLogin truth values are locked before CookieUtil is deleted.</done>
</task>

</tasks>

<verification>
- Build + `-only-testing:CookieClientTests` (and full suite at wave merge) green — single, non-overlapping invocations.
- New test dir carries the parent_config `.swiftlint.yml`; lint clean at error level.
</verification>

<success_criteria>
QUAL-02's CookieClient share is a dedicated, deterministic behavior matrix, and CookieClient.didLogin
parity is proven — clearing the path for the 08-12 CookieUtil deletion.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-11-SUMMARY.md` when done.
</output>
