---
phase: 08-architecture-hygiene-client-seams
plan: 08
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
  - AppPackage/Sources/AppModels/Utilities/URLUtil.swift
  - AppPackage/Sources/AppModels/Utilities/AppUtil.swift
  - AppPackage/Sources/AppTools/UserDefaultsUtil.swift
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingReducer.swift
  - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "The Defaults.URL host global property and its 8 host-derived global properties are deleted; only the host-taking helper functions remain."
    - "AppUtil.galleryHost and the URLUtil transitional host default are removed — host lives only in @Shared(.setting)."
    - "The UserDefaults galleryHost mirror is gone: AccountSettingReducer.galleryHostChanged write + action removed, SettingReducer+Helpers launch restore removed, AppUserDefaults shrunk to clipboardChangeCount."
    - "A lost setting blob resets host to .ehentai (accepted behavior change, D-03); build + full suite green."
  artifacts:
    - "AppUserDefaults enum with a single case: clipboardChangeCount."
  key_links:
    - "@Shared(.setting).galleryHost is the sole host source; no UserDefaults / AppUtil / Defaults.URL.host path survives."
---

<!-- planner-discipline-allow: Defaults.URL.host -->
<!-- planner-discipline-allow: AppUtil.galleryHost -->

<objective>
Seam A step 7 — teardown (D-03): delete the gallery-host global fan-out now that every consumer is
threaded. Remove `Defaults.URL.host` and its 8 host-derived global properties, `AppUtil.galleryHost`,
the URLUtil transitional host default, and the entire `UserDefaults` mirror (the
`AccountSettingReducer.galleryHostChanged` write + action and the `SettingReducer+Helpers` launch
restore); shrink `AppUserDefaults` to `clipboardChangeCount` only.

Purpose: this is the requirement-defining deletion — after it, the active gallery host lives **only**
in `@Shared(.setting)`. The accepted, documented behavior change: if the setting blob is lost, host
resets to `.ehentai` (a redundant durability path removed, D-03).

Output: no host global / mirror anywhere; build + full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift
@AppPackage/Sources/AppModels/Utilities/AppUtil.swift
@AppPackage/Sources/AppTools/UserDefaultsUtil.swift
</context>

## Artifacts this phase produces

Deletions (no new symbols): the `Defaults.URL` host global + 8 derived global properties,
`AppUtil.galleryHost`, the URLUtil `host` transitional default, the `galleryHostChanged` action +
mirror write, the launch restore. `AppUserDefaults` shrinks to a single `clipboardChangeCount` case.
The surviving `AppUtil.version`/`build`/`isTesting` are relocated in 08-14.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. Removing the URLUtil host default surfaces
any missed caller as a compile error (there should be none after 08-03…08-07 — thread it if one appears).
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Delete the host globals + the URLUtil transitional default</name>
  <files>AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift, AppPackage/Sources/AppModels/Utilities/URLUtil.swift, AppPackage/Sources/AppModels/Utilities/AppUtil.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Utilities/Defaults+Runtime.swift:13-23 (the host global + 8 derived global properties — the host-taking helper functions from 08-02 stay).
    - AppPackage/Sources/AppModels/Utilities/URLUtil.swift (the `host: GalleryHost = ...` transitional defaults added in 08-02).
    - AppPackage/Sources/AppModels/Utilities/AppUtil.swift:21-24 (the `galleryHost` computed property — delete only this member; version/build/isTesting/dispatchMainSync stay for 08-14).
  </read_first>
  <action>
    In Defaults+Runtime.swift delete the global `host` computed property and the 8 host-derived global
    computed properties (api/myTags/uConfig/galleryPopups/galleryTorrents/popular/watched/favorites),
    keeping the host-taking helper functions from 08-02. In URLUtil.swift remove the transitional
    `= <the AppUtil host accessor>` default on every builder's `host` parameter (make `host` required).
    In AppUtil.swift delete only the `galleryHost` computed property. Leave version/build/isTesting/
    dispatchMainSync in place (08-14 handles them).
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - Grepping the AppModels utilities for the host global computed property and the `galleryHost` accessor returns no definition (only the host-taking helper functions remain in Defaults+Runtime.swift).
    - URLUtil builders declare `host` with no default.
    - Build succeeds (no straggler caller relies on the deleted globals/default).
  </acceptance_criteria>
  <done>The host global fan-out and the URLUtil host default are gone; host is required everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the UserDefaults galleryHost mirror + shrink AppUserDefaults</name>
  <files>AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingReducer.swift, AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift, AppPackage/Sources/AppTools/UserDefaultsUtil.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingReducer.swift:52 (`galleryHostChanged` action), :128-129 (the `userDefaultsClient.setValue(host.rawValue, .galleryHost)` mirror write).
    - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift:23-25 (the launch restore reading `getValue(.galleryHost)` into `state.$setting`).
    - AppPackage/Sources/AppTools/UserDefaultsUtil.swift:9-12 (the `AppUserDefaults` enum with `galleryHost` + `clipboardChangeCount`).
    - Memory: "Remove emptied actions" — delete the action case + all its call sites, not an empty stub.
  </read_first>
  <action>
    Delete the `galleryHostChanged` mirror write and its action case in AccountSettingReducer (and any
    send site for it — the setting is written directly through `@Shared(.setting)` already, so no
    replacement effect is needed). Delete the launch-restore block in SettingReducer+Helpers:23-25.
    Remove the `galleryHost` case from the `AppUserDefaults` enum, leaving only `clipboardChangeCount`.
    Keep the enum in AppTools (its fold into UserDefaultsClient is 08-13). Do not leave an empty action
    stub. Add a one-line doc comment where the launch restore was, noting host now lives only in
    `@Shared(.setting)` and resets to `.ehentai` if the blob is lost (D-03 accepted behavior change).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\.galleryHost\b" AppPackage/Sources/AppTools/UserDefaultsUtil.swift` shows no `galleryHost` enum case; the enum has exactly `clipboardChangeCount`.
    - No `galleryHostChanged` action or mirror write/restore remains (grep across SettingFeature returns 0).
    - Full suite passes; build succeeds.
  </acceptance_criteria>
  <done>The redundant UserDefaults host mirror is fully removed; @Shared(.setting) is the sole host source.</done>
</task>

</tasks>

<verification>
- Build + full `xcodebuild test` suite green (single, non-overlapping invocation); NetworkingFeatureTests confirm request-URL parity end-to-end.
- No source reads the host global, `AppUtil.galleryHost`, or the UserDefaults galleryHost mirror.
</verification>

<success_criteria>
The gallery host lives only in `@Shared(.setting)`, threaded explicitly through every request/view;
the global fan-out and UserDefaults mirror are deleted at behavior parity (host resets to `.ehentai`
only on setting-blob loss, per D-03).
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-08-SUMMARY.md` when done.
</output>
