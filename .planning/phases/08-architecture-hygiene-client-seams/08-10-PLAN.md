---
phase: 08-architecture-hygiene-client-seams
plan: 10
type: execute
wave: 10
depends_on: ["08-09"]
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Tests/ImageClientTests/ImageClientTests.swift
  - AppPackage/Tests/ImageClientTests/ImageClientTestHelpers.swift
  - AppPackage/Tests/ImageClientTests/.swiftlint.yml
  - AppPackage/Tests/DownloadsFeatureTests/ReaderImageDataTests.swift
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "A dedicated ImageClientTests target exists (wired in Package.swift + a parent_config .swiftlint.yml) and runs in the suite."
    - "It covers ImageClient cache hit/miss, fetch-failure, and placeholder-fingerprint handling using a per-test isolated DataCache instance and comparing decoded pixel dimensions (never point size, never a process-global)."
    - "The equivalent cases are relocated out of DownloadsFeatureTests (no duplicate coverage) and the tests are deterministic and green."
  artifacts:
    - "AppPackage/Tests/ImageClientTests/ target (Module enum case imageClientTests + testTarget entry + .swiftlint.yml)."
  key_links:
    - "makeIsolatedDataCache() (per-test DataCache(rootURL: UUID)) → ImageClient fetch/cache assertions on cgImage width/height."
---

<objective>
Seam F/QUAL-02 (D-09/D-10): stand up a dedicated `ImageClientTests` target covering the reworked
`ImageClient`/`DataCache` seam — cache hit/miss, fetch-failure, and placeholder handling — using a
per-test isolated `DataCache` and comparing **decoded pixel dimensions**. Relocate/adapt the existing
`ReaderImageDataTests` cases (which live in `DownloadsFeatureTests` and drag `@testable AppFeature`)
into the new target, testing `ImageClient` directly.

Purpose: QUAL-02 requires deterministic client-layer coverage for the reworked ImageClient seam, and
this suite also verifies the 08-09 DataCache dependency reshape (cache coherence, no process-global).
RESEARCH Open-Q A2: ImageClient has NO retry loop (the 4-attempt retry is in NetworkingFeature) — test
cache/failure/placeholder handling, not a nonexistent ImageClient retry.

Output: green, deterministic ImageClientTests; no duplicate coverage in DownloadsFeatureTests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@.planning/phases/08-architecture-hygiene-client-seams/08-VALIDATION.md
@AppPackage/Tests/DownloadsFeatureTests/ReaderImageDataTests.swift
@AppPackage/Sources/ImageClient/ImageClient.swift
</context>

## Artifacts this phase produces

New: `ImageClientTests` test target — `Module` enum case `imageClientTests = "ImageClientTests"`, a
`.testTarget` entry in Package.swift, `Tests/ImageClientTests/.swiftlint.yml` (parent_config), and the
test + helper files. Exclude these newly-created symbols from drift verification.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. New test dir MUST carry
`parent_config: ../../../.swiftlint.yml` (project rule + RESEARCH Pitfall 5). Keep `@Suite(.serialized)`
only for the shared URLProtocol handler registry — the DataCache is already per-test isolated
("inject over serialize", not `.serialized`, for cache isolation).
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Wire the ImageClientTests target (Package.swift + .swiftlint.yml + helpers)</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/ImageClientTests/.swiftlint.yml, AppPackage/Tests/ImageClientTests/ImageClientTestHelpers.swift</files>
  <read_first>
    - AppPackage/Package.swift:121 (Module enum `imageColorsTests` case) and :945-951 (the `.imageColorsTests` testTarget analog).
    - AppPackage/Package.swift:890-898 (`fileClientTests` testTarget dependency shape).
    - AppPackage/Tests/DownloadsFeatureTests/ReaderImageDataTests.swift:309-331 (makeIsolatedDataCache, makeStubbedSession, SharedSessionStubURLProtocol, makePNGData).
    - AppPackage/Tests/ImageColorsTests/.swiftlint.yml (single-line parent_config).
  </read_first>
  <action>
    Add `case imageClientTests = "ImageClientTests"` to the `Module` enum and a `.testTarget(module:
    .imageClientTests, dependencies: [.module(.imageClient), .module(.appTools), .module(.appModels),
    .module(.testingSupport)], plugins: swiftLintPlugins)` entry. Create
    `Tests/ImageClientTests/.swiftlint.yml` containing `parent_config: ../../../.swiftlint.yml`.
    Create `ImageClientTestHelpers.swift` holding the relocated `makeIsolatedDataCache()`,
    `makeStubbedSession()`, `SharedSessionStubURLProtocol`, and `makePNGData()` helpers, imported from
    `ImageClient`/`AppTools`/`TestingSupport` — NOT `@testable AppFeature` (test ImageClient directly).
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - Package.swift declares the `imageClientTests` module case + testTarget with the listed module deps + swiftLintPlugins.
    - `Tests/ImageClientTests/.swiftlint.yml` contains exactly `parent_config: ../../../.swiftlint.yml`.
    - Helpers compile against ImageClient/AppTools/TestingSupport with no AppFeature dependency.
    - Build succeeds.
  </acceptance_criteria>
  <done>The ImageClientTests target is registered, lint-covered, and its helpers build without AppFeature.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ImageClient cache/failure/placeholder cases + relocate from DownloadsFeatureTests</name>
  <files>AppPackage/Tests/ImageClientTests/ImageClientTests.swift, AppPackage/Tests/DownloadsFeatureTests/ReaderImageDataTests.swift</files>
  <read_first>
    - AppPackage/Tests/DownloadsFeatureTests/ReaderImageDataTests.swift:13-227 (existing cache-hit/miss + failure/placeholder cases to adapt).
    - AppPackage/Sources/ImageClient/ImageClient.swift:60-126 (fetchImageAsset + readerImageData: cache read, placeholder-fingerprint purge/reject, decode-guard, primary-key store).
    - 08-RESEARCH.md §Code Examples ("ImageClient cache-hit test with pixel-dimension comparison") + §Assumption A2.
    - TESTING.md ("Don't use DataCache.shared in image tests — inject a per-test DataCache and compare pixel dims").
  </read_first>
  <action>
    In ImageClientTests.swift, write `@MainActor @Test` cases (Swift Testing) covering: (1) cache hit
    serves without a network round-trip; (2) cache miss fetches, decodes, and stores under the primary
    key; (3) a fetch failure / non-2xx surfaces as an error (no crash, no cache write); (4) a cached
    placeholder-fingerprint entry is purged and re-fetched; (5) a 200 carrying non-decodable bytes is
    rejected (parseFailed, not cached). Use `makeIsolatedDataCache()` per test and assert on
    `asset.image.cgImage?.width/height` (decoded pixel dims from a known 2×2 PNG), never point size.
    Inject the isolated cache via the ImageClient property and the stubbed session. Remove the
    now-relocated equivalent cases from ReaderImageDataTests.swift; if it is left with no cases, delete
    the file — otherwise keep only cases that genuinely require AppFeature. Do NOT add coverage for a
    nonexistent ImageClient retry loop (A2).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ImageClientTests</automated>
  </verify>
  <acceptance_criteria>
    - `-only-testing:ImageClientTests` passes deterministically (cache hit/miss, failure, placeholder, non-decodable).
    - Assertions compare `cgImage?.width`/`height` (pixel dims), each test uses its own DataCache instance.
    - No duplicate ImageClient cache/fetch cases remain in DownloadsFeatureTests; full suite green at wave merge.
  </acceptance_criteria>
  <done>The reworked ImageClient/DataCache seam has deterministic, pixel-accurate, isolated coverage in its own target.</done>
</task>

</tasks>

<verification>
- Build + `-only-testing:ImageClientTests` (and full suite at wave merge) green — single, non-overlapping invocations.
- New test dir carries the parent_config `.swiftlint.yml`; lint clean at error level.
</verification>

<success_criteria>
QUAL-02's ImageClient share is a dedicated, deterministic, per-test-isolated suite that also proves
the 08-09 DataCache reshape preserves cache behavior.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-10-SUMMARY.md` when done.
</output>
