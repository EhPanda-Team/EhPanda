---
phase: 08-architecture-hygiene-client-seams
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+Account.swift
  - AppPackage/Sources/NetworkingFeature/Request.swift
  - AppPackage/Sources/SettingFeature/SettingReducer+Body.swift
  - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingReducer.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "The Setting-consumed account/misc requests (VerifyEhProfile/EhProfile/EhSetting/SubmitEhSettingChanges/FavoriteCategories) take an explicit host: GalleryHost and build URLs from the host-taking Defaults.URL helpers."
    - "Direct Defaults.URL.host reads inside the Setting reducers (cookie read/write + hostURL) resolve host from setting.galleryHost.url instead of the global."
    - "Setting flows (profile verify, EhSetting load/submit, favorite categories) behave identically."
  artifacts:
    - "host: GalleryHost parameters on the Setting-consumed account/misc Request structs."
  key_links:
    - "SettingFeature @Shared(.setting).galleryHost → Request(host:) and setting.galleryHost.url for direct cookie reads."
---

<objective>
Seam A step 3 (D-03): thread explicit host through the **SettingFeature**-consumed account/misc
requests and the Setting reducers' direct host-global reads. Requests consumed only by Setting
(profile verify, EhSetting profile load/submit, favorite categories) take an explicit
`host: GalleryHost`; the Setting reducers supply `setting.galleryHost` and read cookies from
`setting.galleryHost.url` instead of `Defaults.URL.host`.

Purpose: continue the bottom-up host parameterization by module. Un-threaded requests keep working
via the globals/URLUtil default kept in 08-02; the mirror machinery is removed later in 08-08 teardown.

Output: Setting-owned host reads are explicit; build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request+Account.swift
@AppPackage/Sources/NetworkingFeature/Request.swift
</context>

## Artifacts this phase produces

New signatures: `host: GalleryHost` on VerifyEhProfileRequest, EhProfileRequest, EhSettingRequest,
SubmitEhSettingChangesRequest (Request+Account.swift) and FavoriteCategoriesRequest (Request.swift).
Exclude from drift verification.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. The RESEARCH host-consumer inventory
(§Pitfall 1) is authoritative for which requests read the host global.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Give the Setting-consumed account/misc requests an explicit host</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Account.swift, AppPackage/Sources/NetworkingFeature/Request.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift:62-165 (VerifyEhProfileRequest/EhProfileRequest/EhSettingRequest/SubmitEhSettingChangesRequest — all read `Defaults.URL.uConfig`).
    - AppPackage/Sources/NetworkingFeature/Request.swift:279-297 (FavoriteCategoriesRequest — reads `Defaults.URL.uConfig`).
    - 08-RESEARCH.md §Pitfall 1 (host-derived Defaults.URL.* consumer inventory).
  </read_first>
  <action>
    Add `public let host: GalleryHost` + a `host: GalleryHost` init parameter (explicit, no default)
    to VerifyEhProfileRequest, EhProfileRequest, EhSettingRequest, SubmitEhSettingChangesRequest
    (Request+Account.swift) and FavoriteCategoriesRequest (Request.swift). Replace their
    `Defaults.URL.uConfig` reads with `Defaults.URL.uConfig(host: host)` (the helper added in 08-02).
    Do NOT touch LoginRequest, IgneousRequest, GreetingRequest, UserInfoRequest, TagTranslatorRequest
    (they read fixed/non-host URLs — news/forum/login). Keep all response()/parse logic identical.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - The 5 named requests declare `host: GalleryHost` and build from `Defaults.URL.uConfig(host:)`.
    - Non-host requests in the same files are unchanged.
    - Build succeeds (un-threaded requests still resolve via the kept globals/URLUtil default).
  </acceptance_criteria>
  <done>Setting-consumed requests carry explicit host at parity.</done>
</task>

<task type="auto">
  <name>Task 2: Supply setting.galleryHost from the Setting reducers + convert their direct host reads</name>
  <files>AppPackage/Sources/SettingFeature/SettingReducer+Body.swift, AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingReducer.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/SettingReducer+Body.swift:109 (`getCookie(Defaults.URL.host, ...)`) and its VerifyEhProfile/EhProfile/FavoriteCategories constructions.
    - AppPackage/Sources/SettingFeature/SettingReducer+Helpers.swift:127 (`let hostURL = Defaults.URL.host`). (Do NOT remove the launch mirror-restore at lines 23-25 here — that is 08-08 teardown.)
    - AppPackage/Sources/SettingFeature/EhSetting/EhSettingReducer.swift:113 (`setOrEditCookie(for: Defaults.URL.host, ...)`) and its EhProfile/EhSetting/SubmitEhSettingChanges constructions.
    - The existing `@Shared(.setting)` / `@SharedReader(.setting)` accessor in each reducer's State.
  </read_first>
  <action>
    At each Setting construction site for the 5 requests, pass `host: state.setting.galleryHost`.
    Replace each direct `Defaults.URL.host` read in these three files with
    `state.setting.galleryHost.url` (SettingReducer+Body:109 cookie read; SettingReducer+Helpers:127
    hostURL; EhSettingReducer:113 cookie write). Where a reducer lacks the setting, add
    `@SharedReader(.setting) var setting` to its State. Leave the mirror write/restore machinery
    (galleryHostChanged, getValue(.galleryHost) restore) untouched — 08-08 removes it. Do not change
    action flow or cancellation IDs.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:SettingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Defaults.URL.host" AppPackage/Sources/SettingFeature/SettingReducer+Body.swift AppPackage/Sources/SettingFeature/EhSetting/EhSettingReducer.swift` returns nothing (except any line that is the mirror machinery deferred to teardown).
    - The 5 requests are constructed with `host: setting.galleryHost`.
    - SettingFeatureTests passes; build succeeds.
  </acceptance_criteria>
  <done>SettingFeature resolves host solely from @Shared(.setting); its request + cookie reads are explicit.</done>
</task>

</tasks>

<verification>
- Build + `-only-testing:SettingFeatureTests` green (single, non-overlapping invocation).
- SettingReducer+Helpers still contains the mirror restore (removed in 08-08), but its `hostURL` read is now `setting.galleryHost.url`.
</verification>

<success_criteria>
SettingFeature's host consumption is fully parameterized from `@Shared(.setting)` at behavior parity.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-04-SUMMARY.md` when done.
</output>
