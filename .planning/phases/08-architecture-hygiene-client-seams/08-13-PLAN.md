---
phase: 08-architecture-hygiene-client-seams
plan: 13
type: execute
wave: 13
depends_on: ["08-12"]
files_modified:
  - AppPackage/Sources/HapticsClient/HapticsClient.swift
  - AppPackage/Sources/AppTools/HapticsUtil.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift
  - AppPackage/Sources/AppComponents/CategoryView.swift
  - AppPackage/Sources/AppComponents/SubSection.swift
  - AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift
  - AppPackage/Sources/UserDefaultsClient/UserDefaultsClient.swift
  - AppPackage/Sources/AppTools/UserDefaultsUtil.swift
  - AppPackage/Sources/AppTools/AppUserDefaults.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "HapticsUtil's device-detection + feedback implementation is folded verbatim into HapticsClient.live (preserving @MainActor); the standalone HapticsUtil is deleted."
    - "The 4 HapticsUtil.generateFeedback view sites read @Dependency(\\.hapticsClient) and fire identical feedback on the same interactions."
    - "UserDefaultsUtil.value(forKey:) is folded into UserDefaultsClient.getValue; the standalone UserDefaultsUtil struct is deleted; the AppUserDefaults enum survives in AppTools."
  artifacts:
    - "HapticsClient.live owning the folded haptics implementation."
    - "AppTools/AppUserDefaults.swift (relocated enum: clipboardChangeCount only)."
  key_links:
    - "@Dependency(\\.hapticsClient).generateFeedback(style:) at the 4 view sites; UserDefaultsClient.getValue reads UserDefaults directly."
---

<!-- planner-discipline-allow: HapticsUtil -->
<!-- planner-discipline-allow: UserDefaultsUtil -->

<objective>
Seam B (D-05): fold the two remaining redundant AppTools utilities into their clients and delete
them. `HapticsUtil`'s implementation moves into `HapticsClient.live` (the 4 direct view call sites
become `@Dependency(\.hapticsClient)` reads); `UserDefaultsUtil.value(forKey:)` folds into
`UserDefaultsClient.getValue` (already its only real consumer once the host mirror is gone). The
`AppUserDefaults` enum stays in AppTools.

Purpose: HYG-01 de-globalizes the `*Util` statics into injected clients; `HapticsClient` /
`UserDefaultsClient` already wrap these Utils, so folding just internalizes the impl and migrates the
direct callers. Preserve haptic timing (same interactions) and UserDefaults read semantics.

Output: HapticsUtil + UserDefaultsUtil deleted; build + full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/HapticsClient/HapticsClient.swift
@AppPackage/Sources/UserDefaultsClient/UserDefaultsClient.swift
@AppPackage/Sources/AppTools/HapticsUtil.swift
@AppPackage/Sources/AppTools/UserDefaultsUtil.swift
</context>

## Artifacts this phase produces

`HapticsClient.live` gains the folded implementation; `AppTools/AppUserDefaults.swift` is the
relocated enum (single case `clipboardChangeCount`). Deletions: `HapticsUtil.swift`,
`UserDefaultsUtil.swift` (struct). No new public API surface beyond the relocated enum.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. Preserve `@MainActor` on the HapticsClient
closures; move `isLegacyTapticEngine` + `generateLegacyFeedback` verbatim (08-PATTERNS.md §"Folding
HapticsUtil"). Analog for the view swaps: `ReadingView.swift:19`.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Fold HapticsUtil into HapticsClient.live + migrate 4 view sites + delete HapticsUtil</name>
  <files>AppPackage/Sources/HapticsClient/HapticsClient.swift, AppPackage/Sources/AppTools/HapticsUtil.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections3.swift, AppPackage/Sources/AppComponents/CategoryView.swift, AppPackage/Sources/AppComponents/SubSection.swift, AppPackage/Sources/DetailFeature/Archives/ArchivesView.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/HapticsUtil.swift (generateFeedback / generateNotificationFeedback / private isLegacyTapticEngine / private generateLegacyFeedback — the impl to move).
    - AppPackage/Sources/HapticsClient/HapticsClient.swift:11-19 (live currently delegates to the Util).
    - The 4 `HapticsUtil.generateFeedback` call sites in EhSettingView+Sections3 / CategoryView / SubSection / ArchivesView.
    - AppPackage/Sources/ReadingFeature/ReadingView.swift:19 (in-body @Dependency analog).
    - 08-PATTERNS.md §"Folding HapticsUtil into HapticsClient.live".
  </read_first>
  <action>
    Move `HapticsUtil`'s device-detection + feedback logic into `HapticsClient.live`: inline the
    `guard !isLegacyTapticEngine { generateLegacyFeedback(); return }` body into the `generateFeedback`
    / `generateNotificationFeedback` closures (preserve `@MainActor`), and move the private static
    `isLegacyTapticEngine` and `generateLegacyFeedback` into `HapticsClient` verbatim. In the 4 view
    sites, add `@Dependency(\.hapticsClient) private var hapticsClient` and replace
    `HapticsUtil.generateFeedback(style:)` with `hapticsClient.generateFeedback(style)`. Then delete
    `AppPackage/Sources/AppTools/HapticsUtil.swift`. Do not change which interactions fire feedback.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - A tree-wide grep for the deleted haptics utility type returns 0; `HapticsUtil.swift` no longer exists.
    - `HapticsClient.live` owns the guard/legacy logic (no delegation to a deleted type).
    - The 4 view sites call `hapticsClient.generateFeedback(...)` via `@Dependency`.
    - Build succeeds.
  </acceptance_criteria>
  <done>Haptics logic lives in HapticsClient.live; the 4 sites use the injected client; the Util is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Fold UserDefaultsUtil into UserDefaultsClient + relocate AppUserDefaults enum</name>
  <files>AppPackage/Sources/UserDefaultsClient/UserDefaultsClient.swift, AppPackage/Sources/AppTools/UserDefaultsUtil.swift, AppPackage/Sources/AppTools/AppUserDefaults.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/UserDefaultsUtil.swift (the `value(forKey:)` struct + the `AppUserDefaults` enum, now `clipboardChangeCount` only after 08-08).
    - AppPackage/Sources/UserDefaultsClient/UserDefaultsClient.swift:16-19 (getValue delegating to the Util — its only real consumer).
    - 08-PATTERNS.md §"UserDefaultsClient.swift (fold UserDefaultsUtil in)".
  </read_first>
  <action>
    Inline the Util's body into `UserDefaultsClient.getValue`: `UserDefaults.standard.value(forKey:
    key.rawValue) as? T`. Relocate the `AppUserDefaults` enum into a new
    `AppPackage/Sources/AppTools/AppUserDefaults.swift` (it must stay in AppTools — both
    UserDefaultsClient and any remaining reader depend on it), keeping the single `clipboardChangeCount`
    case. Delete `AppPackage/Sources/AppTools/UserDefaultsUtil.swift`. Do not change getValue's
    generic signature or read semantics.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - A tree-wide grep for the deleted user-defaults utility type returns 0; `UserDefaultsUtil.swift` no longer exists.
    - `AppTools/AppUserDefaults.swift` holds the enum (case `clipboardChangeCount`); UserDefaultsClient.getValue reads UserDefaults directly.
    - Full suite passes; build succeeds.
  </acceptance_criteria>
  <done>UserDefaults access flows through the client; the redundant Util is gone; the enum survives in AppTools.</done>
</task>

</tasks>

<verification>
- Build + full `xcodebuild test` suite green (single, non-overlapping invocation).
- Tree-wide greps for the two deleted utility types return 0.
- Manual (phase gate): device tap on the 4 haptic sites confirms identical feedback.
</verification>

<success_criteria>
`HapticsUtil` and `UserDefaultsUtil` are folded into their clients and deleted; the 4 haptic view
sites use `@Dependency(\.hapticsClient)`; UserDefaults access flows through `UserDefaultsClient` — all
at behavior parity.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-13-SUMMARY.md` when done.
</output>
