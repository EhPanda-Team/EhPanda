---
phase: 08-architecture-hygiene-client-seams
plan: 06
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+Image.swift
  - AppPackage/Sources/NetworkingFeature/Request+GData.swift
  - AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift
  - AppPackage/Sources/NetworkingFeature/Request+Detail.swift
  - AppPackage/Sources/CookieClient/CookieClient.swift
  - AppPackage/Sources/ParserFeature/Parser+Shared.swift
  - AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Sources/DetailFeature/Torrents/TorrentsReducer.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "Every remaining NetworkingFeature / ParserFeature / CookieClient reader of the host global (Request+Image refetch/MPV, Request+GData, Request+GalleriesMetadata, GalleryTorrentsRequest, Parser.parseDateSeekNavigation host, CookieClient.setSkipServer) takes host explicitly."
    - "Their callers (ReadingReducer+ImageFetch, DownloadClient+ExecutionSupport, TorrentsReducer, the date-seek path) supply host from setting.galleryHost."
    - "After this plan, no source under NetworkingFeature / ParserFeature / CookieClient reads Defaults.URL.host."
  artifacts:
    - "host parameters on the remaining image/gdata/metadata/torrents requests, Parser.parseDateSeekNavigation, and CookieClient.setSkipServer(host:)."
  key_links:
    - "ReadingFeature/DownloadClient @Shared(.setting).galleryHost → image-refetch/MPV requests + setSkipServer(host:)."
---

<objective>
Seam A step 5 (D-03): drain the **remaining** host-global readers in the request / parser / cookie
layer — the reader-image refetch + MPV requests, the gdata + galleries-metadata requests, the
gallery-torrents request, `Parser.parseDateSeekNavigation`'s host, and `CookieClient.setSkipServer` —
so nothing under NetworkingFeature / ParserFeature / CookieClient resolves host from the global.
Callers supply `setting.galleryHost`.

Purpose: this is the tail of the request-layer parameterization. After it, only the 12
`AppUtil.galleryHost` view/reducer reads (08-07) remain before the 08-08 teardown can delete the
globals.

Output: request/parser/cookie layer is host-global-free; build + NetworkingFeature/Reading/Download
tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/NetworkingFeature/Request+Image.swift
@AppPackage/Sources/CookieClient/CookieClient.swift
</context>

## Artifacts this phase produces

New signatures: `host: GalleryHost` on GalleryNormalImageURLRefetchRequest, GalleryMPVImageURLRequest,
the Request+GData request, the Request+GalleriesMetadata request, GalleryTorrentsRequest;
`CookieClient.setSkipServer(response:host:)`; a required `host` on `Parser.parseDateSeekNavigation`.
Exclude from drift verification.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. Use the RESEARCH §Pitfall 1 inventory +
`grep -rn "Defaults.URL.host" AppPackage/Sources/{NetworkingFeature,ParserFeature,CookieClient}` to
find every reader; thread host into all of them (this plan's completion gate is that grep → 0).
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Thread host into the remaining request + parser + setSkipServer readers</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Image.swift, AppPackage/Sources/NetworkingFeature/Request+GData.swift, AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift, AppPackage/Sources/NetworkingFeature/Request+Detail.swift, AppPackage/Sources/CookieClient/CookieClient.swift, AppPackage/Sources/ParserFeature/Parser+Shared.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Image.swift:331 (GalleryNormalImageURLRefetchRequest `Defaults.URL.host.appendingPathComponent`), :267/:287 (GalleryMPVImageURLRequest `apiURL = Defaults.URL.api`).
    - AppPackage/Sources/NetworkingFeature/Request+GData.swift:27 (`Defaults.URL.api`), Request+GalleriesMetadata.swift:52 (`galleryURL: Defaults.URL.host`), Request+Detail.swift:290 (GalleryTorrentsRequest `URLUtil.galleryTorrents`).
    - AppPackage/Sources/CookieClient/CookieClient.swift:311-324 (setSkipServer reads Defaults.URL.host:319).
    - AppPackage/Sources/ParserFeature/Parser+Shared.swift:94-96 (`parseDateSeekNavigation(doc:host:)` default `= Defaults.URL.host`).
  </read_first>
  <action>
    Add an explicit `host: GalleryHost` to GalleryNormalImageURLRefetchRequest,
    GalleryMPVImageURLRequest, the Request+GData request, the Request+GalleriesMetadata request, and
    GalleryTorrentsRequest, replacing their internal `Defaults.URL.host` / `Defaults.URL.api` /
    `Defaults.URL.api` default / `URLUtil.galleryTorrents(...)` reads with the host-taking form
    (`host.url`, `Defaults.URL.api(host:)`, `URLUtil.galleryTorrents(host: host, ...)`). Convert
    `CookieClient.setSkipServer(response:)` to `setSkipServer(response:host:)` using `host.url` in the
    `setCookie(for:...)` call. Change `Parser.parseDateSeekNavigation`'s `host` from a defaulted
    `URL = Defaults.URL.host` to a required parameter (caller supplies it in Task 2). Preserve all
    parse/response bodies and error mapping.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Defaults.URL.host" AppPackage/Sources/NetworkingFeature/Request+Image.swift AppPackage/Sources/NetworkingFeature/Request+GalleriesMetadata.swift AppPackage/Sources/CookieClient/CookieClient.swift AppPackage/Sources/ParserFeature/Parser+Shared.swift` returns 0 for each file.
    - setSkipServer takes `host:`; parseDateSeekNavigation requires `host`.
    - Build succeeds.
  </acceptance_criteria>
  <done>The remaining request/parser/cookie readers take host explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Supply setting.galleryHost from the reader / download / torrents / date-seek callers</name>
  <files>AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift, AppPackage/Sources/DetailFeature/Torrents/TorrentsReducer.swift</files>
  <read_first>
    - AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift:253 (setSkipServer call) + its GalleryNormalImageURLRefetchRequest / GalleryMPVImageURLRequest constructions.
    - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift (the same two image-refetch/MPV request constructions).
    - AppPackage/Sources/DetailFeature/Torrents/TorrentsReducer.swift (GalleryTorrentsRequest construction).
    - The caller of `Parser.parseDateSeekNavigation` (grep for it; supply `host: setting.galleryHost.url`).
  </read_first>
  <action>
    At each construction / call site, pass `host: <setting>.galleryHost` (or
    `.galleryHost.url` where a URL is expected, e.g. parseDateSeekNavigation). `ReadingReducer`
    already has `@Shared(.setting)`; DownloadClient is a client — thread host from the reducer that
    invokes the download execution (or read `@Dependency`/`@Shared(.setting)` in the execution
    support entry point as the surrounding code already does for other setting-derived values).
    setSkipServer becomes `cookieClient.setSkipServer(response: response, host: state.setting.galleryHost)`.
    Do not change effect ordering, cancellation IDs, or retry counts.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ReadingFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Defaults.URL.host" AppPackage/Sources/NetworkingFeature AppPackage/Sources/ParserFeature AppPackage/Sources/CookieClient` returns 0.
    - Every image-refetch/MPV/torrents/setSkipServer/date-seek call passes explicit host.
    - ReadingFeatureTests passes; build succeeds.
  </acceptance_criteria>
  <done>The request/parser/cookie layer no longer reads the host global anywhere; parity holds.</done>
</task>

</tasks>

<verification>
- Build + `-only-testing:ReadingFeatureTests` (and a full-suite pass at wave merge) green — single, non-overlapping invocations.
- `grep -rn "Defaults.URL.host" AppPackage/Sources/{NetworkingFeature,ParserFeature,CookieClient}` → 0.
</verification>

<success_criteria>
Only the 12 `AppUtil.galleryHost` view/reducer reads remain before teardown; the entire request /
parser / cookie layer resolves host explicitly at behavior parity.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-06-SUMMARY.md` when done.
</output>
