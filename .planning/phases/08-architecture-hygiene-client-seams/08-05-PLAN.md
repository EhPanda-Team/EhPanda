---
phase: 08-architecture-hygiene-client-seams
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - AppPackage/Sources/NetworkingFeature/Request+Account.swift
  - AppPackage/Sources/CookieClient/CookieClient.swift
  - AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "The Detail-consumed account requests (FavorGallery/UnfavorGallery/RateGallery/VoteGalleryComment/VoteGalleryTag) take an explicit host: GalleryHost."
    - "CookieClient.apiuid becomes a host-taking accessor apiuid(host:) reading host.url instead of the Defaults.URL.host global; its 3 Detail call sites pass setting.galleryHost."
    - "Favorite/rate/comment/tag-vote flows behave identically (same apiuid + request URLs)."
  artifacts:
    - "host: GalleryHost on the 5 Detail-consumed account Request structs."
    - "CookieClient.apiuid(host: GalleryHost) host-taking accessor."
  key_links:
    - "DetailFeature @Shared(.setting).galleryHost → cookieClient.apiuid(host:) and Request(host:)."
---

<objective>
Seam A step 4 (D-03, Open-Q3): thread explicit host through the **DetailFeature**-consumed account
requests and make `CookieClient.apiuid` a host-taking accessor. The favor/unfavor/rate/comment-vote/
tag-vote requests take an explicit `host: GalleryHost`, and `apiuid` reads `host.url` instead of the
`Defaults.URL.host` global; Detail reducers supply `setting.galleryHost`.

Purpose: `CookieClient.apiuid` reads the host global internally (line 200) — RESEARCH Open-Q3
recommends an explicit `host` parameter since every caller already has `@Shared/@SharedReader(.setting)`.
This removes another hidden host-global read.

Output: Detail account-request path + apiuid resolve host explicitly; build + DetailFeatureTests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/CookieClient/CookieClient.swift
@AppPackage/Sources/NetworkingFeature/Request+Account.swift
</context>

## Artifacts this phase produces

New signatures: `host: GalleryHost` on FavorGalleryRequest, UnfavorGalleryRequest, RateGalleryRequest,
VoteGalleryCommentRequest, VoteGalleryTagRequest; `CookieClient.apiuid(host: GalleryHost)` (accessor →
method). Exclude from drift verification.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. Note: the `apiuid` **stored property** on
RateGalleryRequest/CommentGalleryRequest/VoteGalleryTagRequest (their own init param) is a different
thing than `CookieClient.apiuid` — do not confuse them.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Give the Detail-consumed account requests explicit host + make apiuid host-taking</name>
  <files>AppPackage/Sources/NetworkingFeature/Request+Account.swift, AppPackage/Sources/CookieClient/CookieClient.swift</files>
  <read_first>
    - AppPackage/Sources/NetworkingFeature/Request+Account.swift: FavorGalleryRequest (uses URLUtil.addFavorite), UnfavorGalleryRequest (Defaults.URL.favorites:290), RateGalleryRequest (Defaults.URL.api:366), VoteGalleryCommentRequest (Defaults.URL.api:473), VoteGalleryTagRequest (Defaults.URL.api:518).
    - AppPackage/Sources/CookieClient/CookieClient.swift:199-201 (apiuid accessor reading Defaults.URL.host).
    - 08-RESEARCH.md §"Open Questions" Q3 (apiuid/setSkipServer host source).
  </read_first>
  <action>
    Add `public let host: GalleryHost` + explicit `host:` init parameter to FavorGalleryRequest,
    UnfavorGalleryRequest, RateGalleryRequest, VoteGalleryCommentRequest, VoteGalleryTagRequest, and
    pass host into their URL construction: FavorGalleryRequest → `URLUtil.addFavorite(host: host,
    gid:, token:)`; UnfavorGalleryRequest → `Defaults.URL.favorites(host: host)`; the api-reading
    three → `Defaults.URL.api(host: host)`. In CookieClient.swift convert `apiuid` from a computed
    `var` to `public func apiuid(host: GalleryHost) -> String` reading `getCookie(host.url,
    Defaults.Cookie.ipbMemberId).rawValue`. Do not change the `apiuid` **stored property** on the
    request structs. Keep all other logic identical.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - The 5 Detail account requests declare `host: GalleryHost` and build URLs from host.
    - `CookieClient.apiuid(host:)` exists; the old parameterless `apiuid` accessor is gone.
    - `grep -c "Defaults.URL.host" AppPackage/Sources/CookieClient/CookieClient.swift` drops by one (apiuid line; setSkipServer at :319 is threaded in 08-06).
    - Build succeeds.
  </acceptance_criteria>
  <done>Detail account requests + apiuid resolve host explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Supply setting.galleryHost from the Detail reducer call sites</name>
  <files>AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift, AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift</files>
  <read_first>
    - AppPackage/Sources/DetailFeature/DetailReducer+Fetch.swift:115,178 (`Int(cookieClient.apiuid)`) + its FavorGallery/UnfavorGallery/RateGallery/VoteGalleryTag constructions.
    - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift:227 (`Int(cookieClient.apiuid)`) + its VoteGalleryComment construction.
    - The reducers' existing `@Shared(.setting)` / `@SharedReader(.setting)` accessor.
  </read_first>
  <action>
    Replace `cookieClient.apiuid` with `cookieClient.apiuid(host: state.setting.galleryHost)` at all 3
    sites, and pass `host: state.setting.galleryHost` into each of the 5 request constructions. Add
    `@SharedReader(.setting) var setting` to a reducer's State if it does not already hold the setting.
    Do not change action flow, `guard` semantics, or cancellation IDs.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:DetailFeatureTests</automated>
  </verify>
  <acceptance_criteria>
    - All 3 `apiuid` reads pass `host: setting.galleryHost`; all 5 requests constructed with `host: setting.galleryHost`.
    - DetailFeatureTests passes; build succeeds.
  </acceptance_criteria>
  <done>DetailFeature resolves host solely from @Shared(.setting) for account requests + apiuid.</done>
</task>

</tasks>

<verification>
- Build + `-only-testing:DetailFeatureTests` green (single, non-overlapping invocation).
- `apiuid(host:)` used at all Detail call sites; no parameterless `apiuid` remains.
</verification>

<success_criteria>
The DetailFeature account-request + apiuid host path is fully parameterized from `@Shared(.setting)`
at behavior parity.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-05-SUMMARY.md` when done.
</output>
