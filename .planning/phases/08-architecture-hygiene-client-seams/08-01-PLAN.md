---
phase: 08-architecture-hygiene-client-seams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - Scripts/check-cookie-logging.sh
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "ROADMAP Phase-8 goal and success criterion 2 no longer mention Keychain / cookie storage — scope is logging-audit-only per D-01."
    - "REQUIREMENTS QUAL-01 no longer requires Keychain; it requires only the cookie-logging audit and notes the D-06 tightening (URLUtil/AppUtil kept as pure namespaces, not clients)."
    - "No logger interpolation emits a cookie value (ipb_member_id / ipb_pass_hash / igneous value or getCookiesDescription) at .public privacy — proven by a static gate that currently passes."
  artifacts:
    - "Scripts/check-cookie-logging.sh — the static cookie-logging gate."
  key_links:
    - "getCookiesDescription flows only to AccountSettingReducer.copyCookies (clipboard) — never to a logger."
---

<objective>
Reconcile the two planning documents to the **logging-audit-only** scope for QUAL-01 (the
Keychain cookie migration is dropped per D-01 — out of milestone, NOT deferred), and lock in the
cookie-logging audit with a deterministic static gate.

Purpose: QUAL-01 was written assuming a Keychain migration. D-01 rescoped it. The ROADMAP Phase-8
goal + success criterion 2 and the REQUIREMENTS QUAL-01 clause are stale and must be reconciled in
this phase (same "honest rescope" pattern as Phase 6). The audit itself is a **confirm-and-lock**:
RESEARCH verified zero offending logger sites exist today, so the deliverable is a gate that keeps
it that way.

Output: reconciled ROADMAP.md + REQUIREMENTS.md; a committed `Scripts/check-cookie-logging.sh`
static gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-VALIDATION.md
</context>

## Artifacts this phase produces

This plan creates: `Scripts/check-cookie-logging.sh` (static cookie-logging gate). No new Swift
symbols. (Phase-wide new symbols — `DataCacheKey`, `\.dataCache`, `CookieClientTests` /
`ImageClientTests` targets, `host:` request params, relocated version/build/isTesting namespace —
are produced by later plans and are excluded from drift verification.)

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ROADMAP + REQUIREMENTS to logging-audit-only (D-01)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 8: Architecture Hygiene & Client Seams" (goal line + Success Criteria list, esp. criterion 2 "Durable auth cookies are stored via Keychain").
    - .planning/REQUIREMENTS.md §QUAL "QUAL-01" (the "Move session cookies to Keychain" title + "Durable auth cookies stored via Keychain" acceptance clause).
    - 08-CONTEXT.md §D-01 (rescope rationale) and §D-06 (URLUtil/AppUtil stay pure namespaces — this "tightens" the requirement's "convert URLUtil/AppUtil to clients" wording).
  </read_first>
  <action>
    In ROADMAP.md Phase 8: rewrite the **Goal** to drop "move session cookies to Keychain" — the
    cookie work is a logging audit only. Rewrite **success criterion 2** to state: no cookie value
    is ever emitted to logs at `.public` privacy; cookie at-rest storage is unchanged
    (HTTPCookieStorage), Keychain migration dropped per D-01 (out of milestone, not deferred). Keep
    criteria 1 (de-globalize Utils / remove DataCache.shared) and 3 (client-layer tests) intact but
    reword criterion 1 to reflect D-06 (URLUtil/AppUtil retained as pure namespaces rather than
    "converted to clients"). In REQUIREMENTS.md QUAL-01: retitle to "Audit cookie logging"; replace
    the "Durable auth cookies stored via Keychain" acceptance clause with the logging-audit clause;
    add a one-line note that D-06 tightens HYG-01's "convert URLUtil/AppUtil to clients" to "retain
    as pure namespaces (anti-wrapper rule)". Do NOT touch QUAL-02, QUAL-03/04, or other requirements.
    Use scoped edits — do not rewrite whole files.
  </action>
  <verify>
    <automated>grep -niE "keychain" .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -iE "cookie|QUAL-01|Phase 8|session" ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - No residual Keychain/cookie-storage clause remains in the Phase-8 goal, Phase-8 success criterion 2, or QUAL-01 acceptance text (grep for "Keychain" near cookie/QUAL-01/Phase 8 returns nothing).
    - QUAL-01 acceptance text asserts "no cookie value ... at .public privacy".
    - REQUIREMENTS.md QUAL-01 notes the D-06 tightening (URLUtil/AppUtil pure namespaces).
    - QUAL-02 and all other requirement rows are byte-unchanged except QUAL-01.
  </acceptance_criteria>
  <done>Both docs describe QUAL-01 as logging-audit-only; no Keychain cookie clause survives; D-06 tightening noted.</done>
</task>

<task type="auto">
  <name>Task 2: Add the cookie-logging static gate (D-02)</name>
  <files>Scripts/check-cookie-logging.sh</files>
  <read_first>
    - 08-RESEARCH.md §"Cookie-logging audit gate (QUAL-01, D-02)" and §Security Domain (V7 Logging).
    - 08-VALIDATION.md §"Cookie-Logging Static Gate".
    - AppPackage/Sources/CookieClient/CookieClient.swift:267 (getCookiesDescription — clipboard-only, must never reach a logger).
    - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingReducer.swift (copyCookies — the only getCookiesDescription consumer).
  </read_first>
  <action>
    Create an executable POSIX shell script `Scripts/check-cookie-logging.sh` that scans
    `AppPackage/Sources` for any `logger.` call whose interpolation includes a cookie value —
    the identifiers `ipb_member_id` / `ipb_pass_hash` / `igneous` cookie **values** or a
    `getCookiesDescription` result — and fails (non-zero exit) if any such logger site exists at
    `.public` privacy or without a `.private` privacy specifier. The script also asserts
    `getCookiesDescription` is referenced only from `AccountSettingReducer` (clipboard export),
    never from a `logger.` line. It must print the offending file:line on failure and exit 0 when
    clean (the current expected state — the audit found zero offending sites). Keep the token list
    (Defaults.Cookie constant names/values) in one place at the top of the script. Do not embed the
    literal cookie token strings anywhere in Swift source; they live only in this script.
  </action>
  <verify>
    <automated>chmod +x Scripts/check-cookie-logging.sh && ./Scripts/check-cookie-logging.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./Scripts/check-cookie-logging.sh` exits 0 on the current tree (no offending sites).
    - The script fails (non-zero, prints file:line) if a `logger.` interpolation carrying a cookie value at `.public` is introduced (verify by a temporary local edit, then revert).
    - getCookiesDescription appears only under SettingFeature/AccountSetting (clipboard), never on a `logger.` line.
  </acceptance_criteria>
  <done>An automated, deterministic gate proves no cookie value is logged at .public and can be run at every phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cookie value → diagnostic log sink | Auth cookies (ipb_member_id / ipb_pass_hash / igneous) must not cross into OSLog output at a readable privacy level. |
| cookie value → at-rest store | Auth cookies live in HTTPCookieStorage (unchanged); Keychain migration deliberately dropped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `logger.*` interpolations across AppPackage/Sources | high | mitigate | No cookie value emitted at `.public`; `.private` or omit. Locked by `Scripts/check-cookie-logging.sh` static gate (D-02). Audit found zero offending sites today. |
| T-08-02 | Information Disclosure | `getCookiesDescription` (clipboard export) | low | accept | Deliberate, user-initiated cross-device portability (Settings UI + clipboard). Out of the audit's scope (not a log). Gate asserts it never reaches a logger. |
| T-08-03 | Information Disclosure / Tampering | Auth cookies at rest in HTTPCookieStorage | medium | accept | Keychain migration dropped (D-01): sideload Team-ID re-sign orphans Keychain items (errSecMissingEntitlement -34018); years of zero observed loss; credentials already user-portable by design. Documented **scope** decision, not an unmitigated risk. |

No package-manager installs occur in this phase (RESEARCH §Package Legitimacy Audit: N/A) — no supply-chain (T-SC) row applies.
</threat_model>

<verification>
- Both docs reconciled; `grep -niE "keychain" .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows no surviving cookie-storage clause in Phase-8 goal/criterion-2/QUAL-01.
- `./Scripts/check-cookie-logging.sh` exits 0.
- No Swift build touched by this plan (docs + shell only).
</verification>

<success_criteria>
QUAL-01 is reconciled to logging-audit-only in ROADMAP + REQUIREMENTS, and a committed static gate
proves (and will keep proving) that no cookie value is logged at `.public`.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-01-SUMMARY.md` when done.
</output>
