---
phase: 08-architecture-hygiene-client-seams
plan: 12
type: execute
wave: 12
depends_on: ["08-11"]
files_modified:
  - AppPackage/Sources/AppTools/CookieUtil.swift
  - AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift
  - AppPackage/Sources/FavoritesFeature/FavoritesView.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedView.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DetailFeature/DetailView+Subviews.swift
  - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift
  - AppPackage/Sources/DetailFeature/DetailView+Navigation.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "All 12 CookieUtil.didLogin view call sites read @Dependency(\\.cookieClient).didLogin instead — login-gated controls (download/archive/comment/favorite buttons) enable/disable identically."
    - "CookieUtil.swift is deleted; no source references CookieUtil."
    - "Read timing is per render (parity with the former static); the CookieClientTests didLogin matrix (08-11) guards the semantic equivalence."
  artifacts:
    - "In-body @Dependency(\\.cookieClient) reads at the 12 former CookieUtil.didLogin sites."
  key_links:
    - "CookieClient.didLogin (proven in 08-11) → the 12 login-gated view sites."
---

<!-- planner-discipline-allow: CookieUtil -->

<objective>
Seam B (D-04/D-05): delete the redundant `CookieUtil` and migrate its 12 `didLogin` view call sites
to in-body `@Dependency(\.cookieClient).didLogin`. `CookieUtil.didLogin` is exactly
`CookieClient.didLogin` (proven by the 08-11 matrix), so this is a parity swap of a login predicate.

Purpose: `CookieUtil` is a redundant second implementation of the login check. HYG-01 removes it;
D-04 makes view-layer reads `@Dependency` (direct `.live`/`.shared`/static forbidden in consumers,
extending Phase-5 D-01). The 12 sites gate real controls, so parity is user-visible — 08-11's matrix
is the guard.

Output: CookieUtil deleted; login gating unchanged; build + full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/CookieClient/CookieClient.swift
@AppPackage/Sources/ReadingFeature/ReadingView.swift
</context>

## Artifacts this phase produces

This plan adds in-body `@Dependency(\.cookieClient)` reads at the 12 sites and deletes
`AppPackage/Sources/AppTools/CookieUtil.swift`. No new public symbol.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. Analog for the in-body read:
`ReadingView.swift:19` (`@Dependency(\.deviceClient) private var deviceClient`). This plan REQUIRES
08-11 to have proven the didLogin matrix first (Pitfall 2).
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 12 didLogin view sites to @Dependency(\.cookieClient)</name>
  <files>AppPackage/Sources/SettingFeature/AccountSetting/AccountSettingView.swift, AppPackage/Sources/FavoritesFeature/FavoritesView.swift, AppPackage/Sources/HomeFeature/Watched/WatchedView.swift, AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DetailFeature/DetailView+Subviews.swift, AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift, AppPackage/Sources/DetailFeature/DetailView+Navigation.swift, AppPackage/Sources/DetailFeature/Comments/CommentsView.swift</files>
  <read_first>
    - Each file's `CookieUtil.didLogin` usage (grep `CookieUtil.didLogin` across AppPackage/Sources for the exact 12 sites — several DetailView split files hold more than one).
    - AppPackage/Sources/ReadingFeature/ReadingView.swift:19 (property form) and :41 (init-scope form) — the in-body @Dependency analog.
    - AppPackage/Sources/CookieClient/CookieClient.swift:189-198 (the didLogin accessor now used).
    - 08-CONTEXT.md §D-04 (view @Dependency-only rule; parity of enable/disable).
  </read_first>
  <action>
    In each view, add `@Dependency(\.cookieClient) private var cookieClient` (or the init-scope form
    where the read happens in `init`) and replace every `CookieUtil.didLogin` with
    `cookieClient.didLogin`. On DetailView.swift:64, the host term is already `setting.galleryHost`
    (08-07) — change only the `CookieUtil.didLogin` term to `cookieClient.didLogin`. Do not change any
    control's layout, disabled logic, or surrounding condition. Direct `.live`/`.shared`/static cookie
    access is forbidden — use the injected client only.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "cookieClient.didLogin" AppPackage/Sources/DetailFeature AppPackage/Sources/FavoritesFeature AppPackage/Sources/HomeFeature AppPackage/Sources/SettingFeature` accounts for all 12 former sites.
    - No view references the deleted utility's `didLogin` any longer.
    - Build succeeds.
  </acceptance_criteria>
  <done>All 12 login-gated sites read the injected CookieClient; enable/disable behavior is identical.</done>
</task>

<task type="auto">
  <name>Task 2: Delete CookieUtil</name>
  <files>AppPackage/Sources/AppTools/CookieUtil.swift</files>
  <read_first>
    - AppPackage/Sources/AppTools/CookieUtil.swift (the file to delete — didLogin/verify, both now redundant with CookieClient.didLogin).
  </read_first>
  <action>
    Delete `AppPackage/Sources/AppTools/CookieUtil.swift` entirely. Confirm nothing imports or
    references the type across the source tree (all 12 sites migrated in Task 1). Do not leave a stub.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/AppTools/CookieUtil.swift` no longer exists.
    - A tree-wide grep for the deleted type name returns 0 references.
    - Full suite passes (incl. CookieClientTests didLogin matrix); build succeeds.
  </acceptance_criteria>
  <done>The redundant cookie utility is gone; login gating flows entirely through CookieClient.</done>
</task>

</tasks>

<verification>
- Build + full `xcodebuild test` suite green (single, non-overlapping invocation).
- Tree-wide grep for the deleted utility type returns 0.
- Manual (phase gate): device check that download/archive/comment/favorite login-gated controls appear/disable identically for logged-in vs logged-out.
</verification>

<success_criteria>
`CookieUtil` is deleted and every login-gated view resolves login state from the injected
`CookieClient` at behavior parity, guarded by the 08-11 didLogin matrix.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-12-SUMMARY.md` when done.
</output>
