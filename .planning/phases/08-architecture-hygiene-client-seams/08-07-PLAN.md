---
phase: 08-architecture-hygiene-client-seams
plan: 07
type: execute
wave: 7
depends_on: ["08-06"]
files_modified:
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift
  - AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections2.swift
  - AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift
  - AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift
  - AppPackage/Sources/AppComponents/CategoryView.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift
  - AppPackage/Sources/DetailFeature/DetailReducer+Download.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "None of the 12 view/reducer sites read AppUtil.galleryHost; each resolves host from setting.galleryHost (store state, @SharedReader(.setting), or a threaded parameter)."
    - "Category colors, Toplists eh-gating, and detail header/category rendering are visually identical (host value unchanged)."
  artifacts:
    - "@SharedReader(.setting) (or a threaded host parameter) at the former AppUtil.galleryHost view/reducer sites."
  key_links:
    - "gallery.color(host: setting.galleryHost) at the list cells and detail header."
---

<objective>
Seam A step 6 (D-03/D-04): eliminate every remaining `AppUtil.galleryHost` read (the 12 view/reducer
sites). Each resolves host from `@Shared(.setting).galleryHost` — via store state, an in-view
`@SharedReader(.setting)`, or a parameter threaded from the parent — instead of the global. This is
the last consumer removal before the 08-08 teardown deletes the globals.

Purpose: leaf cells and detail/setting views read the host global directly today. D-03 makes
`@Shared(.setting).galleryHost` the single source of truth; D-04 keeps view-layer reads at parity
(same value each render).

Output: no `AppUtil.galleryHost` reads remain in views/reducers; build + full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
</context>

## Artifacts this phase produces

This plan adds `@SharedReader(.setting)` reads (or threaded `host: GalleryHost` parameters) at the 12
former `AppUtil.galleryHost` sites — no net-new public symbol. (The `Defaults.URL.host` /
`AppUtil.galleryHost` globals themselves are deleted in 08-08.)

<execution_notes>
xcodebuild invocations must never overlap — one at a time. Do NOT touch `CookieUtil.didLogin` on
DetailView.swift:64 — only its `AppUtil.galleryHost` half changes here; the `didLogin` half is 08-12.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Convert the leaf-cell + component host reads to setting.galleryHost</name>
  <files>AppPackage/Sources/GalleryListComponents/Cells/GalleryDetailCell.swift, AppPackage/Sources/GalleryListComponents/Cells/GalleryThumbnailCell.swift, AppPackage/Sources/AppComponents/CategoryView.swift, AppPackage/Sources/HomeFeature/Toplists/ToplistsView.swift</files>
  <read_first>
    - GalleryDetailCell.swift:137, GalleryThumbnailCell.swift:48, CategoryView.swift:75 (`gallery.color(host: AppUtil.galleryHost)` / `category.color(host: AppUtil.galleryHost)`).
    - ToplistsView.swift:54 (`if AppUtil.galleryHost == .ehentai`).
    - An existing `@SharedReader(.setting)` usage in the same module (or a comparable view) to confirm the module imports ComposableArchitecture/Sharing.
    - 08-CONTEXT.md §D-04 (view-layer reads stay at parity; @Dependency-only / store-state rule).
  </read_first>
  <action>
    In each leaf cell / component, read host via `@SharedReader(.setting) private var setting` and use
    `setting.galleryHost` in the `color(host:)` call and the Toplists `== .ehentai` check. If a module
    does not depend on the Sharing/ComposableArchitecture surface for `@SharedReader`, instead thread
    `host: GalleryHost` in as an init parameter supplied by the parent list/view (GalleryList /
    ToplistsView owner). Read timing is per render — identical to the old static read (parity). Do not
    change layout, colors, or any other rendering.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "AppUtil.galleryHost" AppPackage/Sources/GalleryListComponents AppPackage/Sources/AppComponents AppPackage/Sources/HomeFeature` returns 0.
    - Each `color(host:)` / eh-check uses `setting.galleryHost` (or a threaded host param).
    - Build succeeds.
  </acceptance_criteria>
  <done>Leaf cells + components resolve host from the shared setting at visual parity.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the Setting + Detail host reads to setting.galleryHost</name>
  <files>AppPackage/Sources/SettingFeature/EhSetting/EhSettingView.swift, AppPackage/Sources/SettingFeature/EhSetting/EhSettingView+Sections2.swift, AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/DetailFeature/DetailView+HeaderSection.swift, AppPackage/Sources/DetailFeature/DetailReducer+Download.swift</files>
  <read_first>
    - EhSettingView.swift:14 (`private var galleryHost: GalleryHost { AppUtil.galleryHost }`), EhSettingView+Sections2.swift:44 (`category.color(host: AppUtil.galleryHost)`).
    - DetailView.swift:64 (the `AppUtil.galleryHost == .ehentai` half of the enable condition — leave `CookieUtil.didLogin` untouched), DetailView+HeaderSection.swift:60 (`gallery.color(host: AppUtil.galleryHost)`), DetailReducer+Download.swift:192 (`host: AppUtil.galleryHost`).
    - The existing store/setting access in each of these EhSetting/Detail views + reducers.
  </read_first>
  <action>
    Replace each `AppUtil.galleryHost` with the store/setting-sourced host: in EhSettingView, back the
    `galleryHost` computed property with the reducer store's `setting.galleryHost` (or
    `@SharedReader(.setting)`); in the Detail views/header use the Detail store's `setting.galleryHost`;
    in DetailReducer+Download:192 use `state.setting.galleryHost`. On DetailView.swift:64 change only
    the `AppUtil.galleryHost == .ehentai` term to `setting.galleryHost == .ehentai`, leaving
    `CookieUtil.didLogin` for 08-12. Do not change any other behavior.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "AppUtil.galleryHost" AppPackage/Sources` returns only Defaults+Runtime.swift:14 (the global itself, deleted in 08-08).
    - DetailView.swift:64 still contains `CookieUtil.didLogin` (untouched) but its host term now reads `setting.galleryHost`.
    - Full suite passes; build succeeds.
  </acceptance_criteria>
  <done>All view/reducer AppUtil.galleryHost reads resolve from @Shared(.setting); only the global definition remains.</done>
</task>

</tasks>

<verification>
- Build + full `xcodebuild test` suite green (single, non-overlapping invocation).
- `grep -rn "AppUtil.galleryHost" AppPackage/Sources` → only the Defaults+Runtime.swift:14 global definition remains.
</verification>

<success_criteria>
Every consumer of the gallery host now resolves it from `@Shared(.setting)`; the globals are fully
orphaned and ready to delete in 08-08.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-07-SUMMARY.md` when done.
</output>
