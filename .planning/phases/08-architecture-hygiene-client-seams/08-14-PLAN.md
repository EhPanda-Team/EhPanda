---
phase: 08-architecture-hygiene-client-seams
plan: 14
type: execute
wave: 14
depends_on: ["08-13"]
files_modified:
  - AppPackage/Sources/AppModels/Utilities/AppUtil.swift
  - AppPackage/Sources/AppModels/Utilities/AppInfo.swift
  - AppPackage/Sources/SettingFeature/Components/AboutView.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "The AppUtil type is eliminated: dispatchMainSync (zero callers) is deleted; version/build/isTesting are relocated to a small pure constants namespace (AppInfo)."
    - "AboutView and AppDelegateReducer read version/build/isTesting from the new namespace; behavior is identical (same Bundle keys, same XCTestConfigurationFilePath env read)."
    - "The stale empty AppPackage/Sources/AuthorizationClient/ directory is removed with no dangling Package.swift/import reference."
    - "No *Util static global helper with side effects remains; URLUtil and FileUtil are retained as pure namespaces (D-06)."
  artifacts:
    - "AppPackage/Sources/AppModels/Utilities/AppInfo.swift (pure version/build/isTesting namespace)."
  key_links:
    - "AppInfo.version/build (Bundle) → AboutView; AppInfo.isTesting (env) → AppDelegateReducer."
---

<!-- planner-discipline-allow: AppUtil -->

<objective>
Seam C (D-07 + cleanup): eliminate the `AppUtil` type. Delete the dead `dispatchMainSync` (zero
callers); relocate the surviving `version`/`build`/`isTesting` constants into a small pure namespace
(`AppInfo`); and delete the stale empty `AppPackage/Sources/AuthorizationClient/` directory left by
Phase 7. `URLUtil` and `FileUtil` stay as pure namespaces (D-06 anti-wrapper).

Purpose: HYG-01's closing deletion — after it, no `*Util` static global helper with side effects
remains; the only retained `*Util` namespaces (URLUtil/FileUtil) are pure, deterministic constants.

Output: AppUtil gone; version/build/isTesting relocated; AuthorizationClient dir removed; the phase
gate (full suite + SwiftLint clean + cookie-logging gate) passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-architecture-hygiene-client-seams/08-CONTEXT.md
@.planning/phases/08-architecture-hygiene-client-seams/08-RESEARCH.md
@.planning/phases/08-architecture-hygiene-client-seams/08-PATTERNS.md
@AppPackage/Sources/AppModels/Utilities/AppUtil.swift
@AppPackage/Sources/AppTools/FileUtil.swift
</context>

## Artifacts this phase produces

New: `AppPackage/Sources/AppModels/Utilities/AppInfo.swift` — a pure constants namespace holding
`version`/`build`/`isTesting` (analog: `FileUtil`). Exclude from drift verification. Deletions:
`AppUtil.swift` (type eliminated: dispatchMainSync + galleryHost already gone) and the empty
`AppPackage/Sources/AuthorizationClient/` directory.

<execution_notes>
xcodebuild invocations must never overlap — one at a time. `dispatchMainSync` has zero callers
(verify with a grep before deleting). The `XCTestConfigurationFilePath` env read moves with
`isTesting` (no secret rename). Confirm no `case authorizationClient` / `.module(.authorizationClient)`
remains in Package.swift before removing the directory (RESEARCH A4 verified none).
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Relocate version/build/isTesting to AppInfo and delete AppUtil</name>
  <files>AppPackage/Sources/AppModels/Utilities/AppInfo.swift, AppPackage/Sources/AppModels/Utilities/AppUtil.swift, AppPackage/Sources/SettingFeature/Components/AboutView.swift, AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Utilities/AppUtil.swift:5-19 (version/build Bundle constants + isTesting env read; :26-32 dispatchMainSync — dead).
    - AppPackage/Sources/AppTools/FileUtil.swift (the pure static-namespace reference shape).
    - AppPackage/Sources/SettingFeature/Components/AboutView.swift:11 (`AppUtil.version`, `AppUtil.build`).
    - AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift:62 (`AppUtil.isTesting`).
    - 08-CONTEXT.md §D-07 (placement is Claude's discretion — a small pure constants namespace chosen here).
  </read_first>
  <action>
    Create `AppInfo.swift` in AppModels/Utilities as a pure static namespace (enum/struct like
    `FileUtil`) exposing `version` (CFBundleShortVersionString), `build` (CFBundleVersion), and
    `isTesting` (the `#if DEBUG` XCTestConfigurationFilePath env read) — moved verbatim from AppUtil.
    Update AboutView to `AppInfo.version` / `AppInfo.build` and AppDelegateReducer to `AppInfo.isTesting`.
    Confirm `dispatchMainSync` has no callers, then delete `AppUtil.swift` entirely (its version/build/
    isTesting now live in AppInfo; galleryHost + dispatchMainSync are gone). Do not change the Bundle
    keys or the env-read behavior.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - A tree-wide grep for the eliminated `AppUtil` type returns 0 references; `AppUtil.swift` no longer exists.
    - `AppInfo.version`/`build`/`isTesting` exist and are used by AboutView / AppDelegateReducer.
    - No `dispatchMainSync` remains anywhere.
    - Build succeeds.
  </acceptance_criteria>
  <done>AppUtil is eliminated; its surviving constants live in a pure AppInfo namespace at parity.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the stale empty AuthorizationClient directory + phase gate</name>
  <files>AppPackage/Sources/AuthorizationClient</files>
  <read_first>
    - `ls -la AppPackage/Sources/AuthorizationClient/` (confirm empty — Phase-7 leftover).
    - AppPackage/Package.swift Module enum + target list (confirm no `authorizationClient` case / `.module(.authorizationClient)` reference remains).
    - 08-RESEARCH.md §Runtime State Inventory (A4: orphaned dir, safe to delete).
  </read_first>
  <action>
    Remove the empty `AppPackage/Sources/AuthorizationClient/` directory. If any stale
    `authorizationClient` Module case or target/dependency reference is found in Package.swift, remove
    it too (RESEARCH verified none, but confirm). Then run the phase gate: full `xcodebuild test` suite,
    SwiftLint at error level (build-tool plugin, no suppressions), and `Scripts/check-cookie-logging.sh`.
  </action>
  <verify>
    <automated>test ! -d AppPackage/Sources/AuthorizationClient && ./Scripts/check-cookie-logging.sh && xcodebuild test -scheme AppPackage-Package -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/AuthorizationClient/` no longer exists; no `authorizationClient` reference in Package.swift.
    - Full suite passes; SwiftLint reports no violations at error level; `Scripts/check-cookie-logging.sh` exits 0.
    - No `*Util` static global helper with side effects remains (only pure URLUtil/FileUtil namespaces).
  </acceptance_criteria>
  <done>The stale directory is gone and the phase gate is green — HYG-01 is fully delivered.</done>
</task>

</tasks>

<verification>
- Build + full `xcodebuild test` suite green (single, non-overlapping invocation).
- SwiftLint clean at error level (no suppressions); `Scripts/check-cookie-logging.sh` exits 0.
- Tree-wide grep for the eliminated `AppUtil` type returns 0; `URLUtil`/`FileUtil` remain as pure namespaces.
</verification>

<success_criteria>
`AppUtil` is eliminated, `dispatchMainSync` deleted, version/build/isTesting relocated to a pure
namespace, and the stale AuthorizationClient directory removed — completing HYG-01 with no static
global helper with side effects remaining, at behavior parity.
</success_criteria>

<output>
Create `.planning/phases/08-architecture-hygiene-client-seams/08-14-SUMMARY.md` when done.
</output>
