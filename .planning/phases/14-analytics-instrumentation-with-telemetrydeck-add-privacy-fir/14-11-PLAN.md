---
phase: 14-analytics-instrumentation
plan: 11
type: execute
wave: 6
depends_on: ["14-06", "14-09"]
files_modified:
  - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift
  - AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift
  - AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift
  - AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift
  - AppPackage/Tests/HomeFeatureTests/AnalyticsEmissionTests.swift
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "All five Home destinations emit a home-section signal — the two section-tap destinations and the three misc-grid destinations — through one shared mapping"
    - "Pushing a gallery detail from Home emits the same gallery-detail signal shape as the other four entry paths"
    - "Opening a filter or quick-search panel from a Home sub-screen emits a signal naming which screen it was opened from, with no free-form screen name"
    - "No Home emission carries a gallery id, token, title, URL or tag value"
  artifacts:
    - path: "AppPackage/Tests/HomeFeatureTests/AnalyticsEmissionTests.swift"
      provides: "TestStore proof for every HomeFeature emission site"
  key_links:
    - from: "AppPackage/Sources/HomeFeature/HomeReducer+Body.swift"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift"
      via: "HomeSectionType and HomeMiscGridType both map onto HomeSection through one private helper"
      pattern: "HomeSection"
---

<objective>
Instrument `HomeFeature`: the five Home destinations, the Home gallery-detail push, and the filter / quick-search panels on the Frontpage, Popular and Watched sub-screens.

Purpose: D-05 family 1 lists five Home sections, and they arrive through **two** different actions — a section-tap action carrying two destinations and a misc-grid action carrying the other three. Instrumenting only one of them silently loses three of the five sections, which is why both are named explicitly here.
Output: seven emission sites in `HomeFeature` plus their `TestStore` proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- New file `AppPackage/Tests/HomeFeatureTests/AnalyticsEmissionTests.swift`
- A private `HomeSection` mapping helper in `HomeReducer+Body.swift`
- No new public production symbols; four existing reducers gain an `@Dependency(\.analyticsClient)` property and emission effects

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home section, misc-grid and gallery-detail emissions</name>
  <files>AppPackage/Sources/HomeFeature/HomeReducer+Body.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift (whole file — the section-tap case, the misc-tap case, and the gallery-detail push case near line 47 which routes through the shared gallery-navigation helper)
    - AppPackage/Sources/HomeFeature/HomeReducer.swift (the action enum and the existing reducer-scope dependency properties)
    - AppPackage/Sources/HomeFeature/HomeView.swift (lines 128-165 — the two source enums: the misc-grid enum has exactly popular, watched and history; the section enum has exactly frontpage and toplists. Confirmed at plan time.)
    - AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift (the five-case `HomeSection` target enum)
    - AppPackage/Sources/AnalyticsClient/TagNamespaceCounts.swift
    - AppPackage/Sources/HomeFeature/Toplists/ToplistsReducer.swift (line 146 — the single-effect fire-and-forget client call form used in this module)
    - .swiftlint.yml (repo root)
  </read_first>
  <behavior>
    - The section-tap case emits `homeSectionViewed` with the tapped section mapped to `HomeSection`.
    - The misc-tap case emits `homeSectionViewed` with the tapped misc destination mapped to `HomeSection`. Together these two cases cover all five sections D-05 family 1 names.
    - Both mappings go through **one** private helper — two `switch` statements, one per source enum, both exhaustive with no `default:` arm — so adding a Home destination later is a compile error rather than a silently unmeasured screen.
    - The gallery-detail push case emits `galleryDetailOpened`, with the category from the pushed gallery and the namespaces derived through `TagNamespaceCounts` from its tags. The payload derivation is identical to the one the other four entry paths use.
    - Existing behavior is untouched: each emission is merged alongside the case's current return value, never replacing it, and no state mutation changes.
  </behavior>
  <action>
    Declare `@Dependency(\.analyticsClient) private var analyticsClient` at reducer scope alongside the existing dependency properties.

    Add a private extension in `HomeReducer+Body.swift` holding the two exhaustive mappings onto `HomeSection`. Keep both in one place — that is what makes "all five sections are covered" checkable by reading a single helper.

    Add the emission effect to each of the three cases, merged with the case's existing return. Follow this module's existing fire-and-forget client-call form; where the case already builds an effect list, append to it rather than restructuring.

    Do not emit from the gallery-tap case that precedes the push — it routes to either a push or a modal presentation depending on device, and emitting there would double-count against the app-root modal emission that plan 14-10 adds. Emit from the push case only, and record that reasoning in a one-line comment at the site.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `HomeReducer+Body.swift` contains `.homeSectionViewed(` exactly twice and `.galleryDetailOpened(` exactly once
    - The private mapping helper contains two `switch` statements and no `default:` arm
    - The gallery-tap case contains no analytics call and carries the explanatory comment
    - `grep -c "onAppear\|\.task(" AppPackage/Sources/HomeFeature/HomeReducer+Body.swift` returns 0
    - Build succeeds with zero new warnings; SwiftLint reports zero violations for `AppPackage/Sources/HomeFeature`
  </acceptance_criteria>
  <done>All five Home sections and the Home gallery-detail push emit, through one auditable mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter and quick-search panel emissions on the Home sub-screens</name>
  <files>AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift, AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift, AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift</files>
  <read_first>
    - AppPackage/Sources/HomeFeature/Frontpage/FrontpageReducer.swift (the filter-panel case near line 93)
    - AppPackage/Sources/HomeFeature/Popular/PopularReducer.swift (the filter-panel case near line 75)
    - AppPackage/Sources/HomeFeature/Watched/WatchedReducer.swift (the filter-panel case near line 106 and the quick-search case near line 112 — this is the only Home sub-screen with both)
    - AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift (the `SearchSurface` enum: the frontpage, popular and watched cases are the ones this task uses)
    - AppPackage/Sources/SettingFeature/Components/LaboratorySettingReducer.swift (lines 19-33 — the canonical call-site shape)
  </read_first>
  <behavior>
    - Each filter-panel case emits `filterPanelOpened` naming its own surface: frontpage, popular, or watched.
    - The Watched quick-search case emits `quickSearchPanelOpened` naming the watched surface.
    - Each emission is merged with the case's existing return; the panel still presents exactly as before and its presentation-driven load action still fires.
    - These signals record that a panel was **opened**, not that a filter was applied. Applying a filter changes a persisted shared value with no distinct reducer action, so there is no D-14-compliant seam for it; the D-11 default parameters already carry the settings that matter. Record this limitation in the summary rather than inventing an action for it.
  </behavior>
  <action>
    In each of the three reducers, declare `@Dependency(\.analyticsClient) private var analyticsClient` at reducer scope and merge an emission effect into the relevant case, using the surface value that names that screen.

    Before writing, verify by search which of these three reducers actually own a quick-search case — at plan time only the watched screen does. If the search finds more, instrument each one it finds and record the corrected inventory in the summary; if it finds fewer, do the same. Do not assume this recitation is complete.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc ".filterPanelOpened(" AppPackage/Sources/HomeFeature` totals 3 across the three sub-screen reducers
    - `grep -rc ".quickSearchPanelOpened(" AppPackage/Sources/HomeFeature` totals 1
    - Each emission names a distinct surface value; no two Home sub-screens report the same surface
    - Build succeeds with zero new warnings; SwiftLint reports zero violations
    - The summary records the verified inventory of filter and quick-search cases in this module
  </acceptance_criteria>
  <done>Every Home sub-screen panel opening emits, naming its own screen through the closed surface vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestStore proofs for every HomeFeature emission site</name>
  <files>AppPackage/Tests/HomeFeatureTests/AnalyticsEmissionTests.swift</files>
  <read_first>
    - AppPackage/Tests/HomeFeatureTests/FiltersPresentationLifecycleTests.swift (the target's suite conventions and the analytics no-op override plan 14-09 added)
    - AppPackage/Tests/SettingFeatureTests/AppActivityLogsReducerTests.swift (lines 18-40 — the `LockIsolated` spy idiom)
    - AppPackage/Sources/HomeFeature/HomeReducer+Body.swift (what Task 1 built)
    - AppPackage/Tests/AnalyticsClientTests/AnalyticsClientGateTests.swift (the spy shape proven in plan 14-06)
  </read_first>
  <behavior>
    Each test drives its reducer through a store whose analytics client is a `LockIsolated`-backed spy and asserts the exact recorded signal sequence.
    - Tapping each of the two section destinations records exactly one home-section signal with the matching section — asserted for both, parameterized over the source enum's `allCases` so a new destination fails the suite.
    - Tapping each of the three misc-grid destinations records exactly one home-section signal with the matching section — likewise parameterized over `allCases`.
    - Pushing a gallery detail records exactly one gallery-detail signal whose category and namespace counts match the fixture, and whose recorded value contains none of the fixture's sentinel title or tag text.
    - Opening each sub-screen's filter panel records exactly one panel signal naming that screen; opening the Watched quick-search panel records exactly one quick-search signal.
  </behavior>
  <action>
    Create `AppPackage/Tests/HomeFeatureTests/AnalyticsEmissionTests.swift` implementing the behavior block as a Swift Testing `@Suite` with `@MainActor` on each store-driving member and never on the type.

    Parameterize the two destination sweeps over the source enums' `allCases` rather than listing destinations by hand. That is what turns "all five sections are covered" from a claim into a test that fails when a sixth destination is added.

    Use fixture galleries whose title and tag text are distinctive sentinel tokens, and assert via the reflection helper from plan 14-03 that no sentinel survives into a recorded signal.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:HomeFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `AnalyticsEmissionTests.swift` contains two `@Test(arguments:` sweeps driven by the source enums' `allCases`
    - The file asserts an exact recorded-signal sequence at every site, not merely a non-empty collection
    - The file contains a sentinel-based reflection assertion over the recorded gallery-detail signal
    - `xcodebuild test ... -only-testing:HomeFeatureTests` exits TEST SUCCEEDED
    - The full default test plan is green
  </acceptance_criteria>
  <done>Every Home emission is pinned, and the destination sweeps fail if a new Home screen ships unmeasured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer → AnalyticsClient | seven emission sites cross this boundary in this module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure | Home gallery-detail emission | high | mitigate | The payload is a closed category enum plus `TagNamespaceCounts`, proven content-free in plan 14-03; the emission test additionally asserts fixture sentinels do not survive into the recorded signal. |
| T-14-13 | Repudiation | gallery-detail metric | medium | mitigate | Emission is placed on the push case only, not the device-branching tap case, so a phone push and an iPad modal each count exactly once across this plan and plan 14-10. |
| T-14-14 | Information Disclosure | a new Home screen shipping unmeasured or mis-mapped | low | mitigate | The two mappings are exhaustive with no catch-all arm, and the tests sweep both source enums' `allCases`, so a new destination is both a compile error and a test failure. |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:HomeFeatureTests` reports TEST SUCCEEDED.
- The full default test plan is green.
- No view-lifecycle callback was added anywhere in `AppPackage/Sources/HomeFeature`.
- SwiftLint reports zero violations; no suppression directive was added.
</verification>

<success_criteria>
- All five Home sections emit, through one exhaustive mapping.
- The Home gallery-detail push emits the shared payload shape.
- All Home sub-screen filter and quick-search panels emit with distinct surface values.
- Every site is proven by an exact-sequence `TestStore` assertion.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-11-SUMMARY.md` when done.
</output>
