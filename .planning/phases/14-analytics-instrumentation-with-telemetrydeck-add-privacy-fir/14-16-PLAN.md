---
phase: 14-analytics-instrumentation
plan: 16
type: execute
wave: 6
depends_on: ["14-06", "14-08", "14-09"]
files_modified:
  - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift
  - AppPackage/Tests/SettingFeatureTests/AnalyticsEmissionTests.swift
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "A failed login emits exactly one login-failure signal classified by kind, and never a username, password or cookie value (D-06)"
    - "A successful login emits nothing"
    - "Encountering a Cloudflare challenge emits exactly one signal per challenge round, so an exhausted-rounds sequence is visible as repeated encounters"
    - "A user-cancelled challenge emits nothing beyond the encounter already recorded — cancelling is not a failure"
    - "A login failure emits the specific login-failure signal and not the generic error signal, so one failure is counted once"
  artifacts:
    - path: "AppPackage/Tests/SettingFeatureTests/AnalyticsEmissionTests.swift"
      provides: "TestStore proof for the login-failure and Cloudflare-challenge emissions"
  key_links:
    - from: "AppPackage/Sources/SettingFeature/Login/LoginReducer.swift"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift"
      via: "the login failure's AppError maps onto the closed login-failure kind enum"
      pattern: "LoginFailureKind"
---

<objective>
Instrument the login flow: classified login failures and Cloudflare challenge encounters.

Purpose: D-05 family 4's login failures and Cloudflare challenges. Phase 12 named both error cases precisely, so the classification already exists in the domain — this plan maps it onto the closed analytics vocabulary without letting any credential material near the boundary.
Output: two emission sites plus their `TestStore` proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- New file `AppPackage/Tests/SettingFeatureTests/AnalyticsEmissionTests.swift`
- No new production symbols; one existing reducer gains an emission at two cases

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classified login-failure and Cloudflare-challenge emissions</name>
  <files>AppPackage/Sources/SettingFeature/Login/LoginReducer.swift</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (whole file — the login-completion case around lines 178-220, whose failure branch already computes a single classified error value and raises a toast from it; the challenge-detected case around line 150, which is bounded by a per-attempt round counter; the user-cancel case around line 173, which is silent by design; and the existing fire-and-forget haptics call form around line 146)
    - AppPackage/Sources/AppModels/Support/AppError.swift (the two Phase 12 error cases this maps: the captcha-required case and the Cloudflare-challenge-failed case)
    - AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift (the five-case login-failure kind enum)
    - AppPackage/Sources/AnalyticsClient/AnalyticsSignal.swift (the login-failure and Cloudflare-encounter cases)
    - .swiftlint.yml (repo root)
  </read_first>
  <behavior>
    - The login-completion case's failure branch emits exactly one login-failure signal, mapping the already-computed classified error onto the closed kind enum: the captcha-required error maps to the captcha kind; the Cloudflare-challenge-failed error maps to the Cloudflare kind; a networking error maps to the networking kind; the unknown error — which is what a plainly rejected credential produces — maps to the rejected kind; anything else maps to the catch-all kind. Write the mapping as a `switch` over the error, not an `if` chain, so its shape is auditable.
    - The success branch emits nothing.
    - The failure branch emits **only** the login-failure signal, not the generic error signal, even though it also raises an error toast. The login-failure signal is strictly more informative, and emitting both would count one failure twice and make the error distribution look login-heavy. This exclusion is the reason the generic error emission lives on the app-root toast action rather than on every reducer that raises a toast.
    - The challenge-detected case emits exactly one Cloudflare-encounter signal per round, including the round that exhausts the bound and converts into a failure. That failure then also emits its own login-failure signal, which is correct: an encounter and a failure are different facts.
    - The user-cancel case emits nothing. Cancelling a challenge is a deliberate user choice, not a failure, and the flow is silent by design.
    - No credential material, cookie, clearance value, username or password is passed into any emission. The signals carry a closed enum and nothing else.
  </behavior>
  <action>
    Declare `@Dependency(\.analyticsClient) private var analyticsClient` at reducer scope beside the existing dependency properties.

    In the login-completion case's failure branch, add the error-to-kind mapping and append the emission effect to the branch's existing effect list, which the case already builds and merges.

    In the challenge-detected case, merge the encounter emission into the case's return, including the exhausted-rounds arm that converts into a failure send.

    Add a two-line comment in the failure branch recording that the generic error signal is deliberately not emitted here and why, so a later contributor does not add it for consistency with the app-root toast site.

    Do not touch the toast construction, the diagnostics context it carries, the round counter, the cancellation identifier or the credential-setting ordering. Phase 12's summary records that the ordering in this case is load-bearing and was itself the subject of a bug.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `LoginReducer.swift` contains `.loginFailed(` exactly once and `.cloudflareChallengeEncountered` exactly once
    - `LoginReducer.swift` contains no `.errorSurfaced(` and the failure branch carries the explanatory comment
    - The error-to-kind mapping is a `switch`, and the emission argument contains no reference to the credential, cookie or clearance values
    - `git diff` shows no change to the toast construction, the round counter, the cancellation identifier, or the ordering of the credential-setting call
    - Build succeeds with zero new warnings; SwiftLint reports zero violations for `AppPackage/Sources/SettingFeature`
  </acceptance_criteria>
  <done>Login failures and challenge encounters emit as distinct classified facts, with the login flow's load-bearing ordering untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestStore proofs for the login emissions</name>
  <files>AppPackage/Tests/SettingFeatureTests/AnalyticsEmissionTests.swift</files>
  <read_first>
    - AppPackage/Tests/SettingFeatureTests (the existing login-related suites, their store construction, their isolation of the shared Cloudflare-clearance holder, and the analytics no-op override plan 14-08 added)
    - AppPackage/Tests/SettingFeatureTests/AppActivityLogsReducerTests.swift (lines 18-40 — the `LockIsolated` spy idiom)
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (what Task 1 built)
  </read_first>
  <behavior>
    Each test drives the reducer through a store whose analytics client is a `LockIsolated`-backed spy and asserts the exact recorded signal sequence.
    - A login completing with each classifiable error records exactly one login-failure signal with the expected kind — parameterized over the mapped error cases plus one unmapped case exercising the catch-all kind.
    - A successful login records **zero** signals.
    - A login failure records exactly one signal, not two: no generic error signal accompanies it. Asserted as an exact one-element sequence rather than a membership check.
    - Detecting a challenge records exactly one encounter signal; walking two full challenge rounds through the reducer records two encounters.
    - Cancelling a challenge records **zero** additional signals.
    - The recorded signals contain no part of a sentinel username or password placed in the store's initial state — asserted with the reflection helper from plan 14-03.
  </behavior>
  <action>
    Create `AppPackage/Tests/SettingFeatureTests/AnalyticsEmissionTests.swift` implementing the behavior block as a Swift Testing `@Suite` with `@MainActor` on each store-driving member and never on the type.

    Isolate the process-wide Cloudflare-clearance holder per case the way the existing Phase 12 suites in this target do, so this suite neither pollutes nor is polluted by them.

    Assert the failure case as an exact one-element sequence. A membership assertion would pass even if a second, generic error signal were emitted alongside — which is precisely the double-count this plan's exclusion exists to prevent.

    Put a sentinel username and password in the store's initial state and assert via reflection that neither survives into any recorded signal.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `AnalyticsEmissionTests.swift` contains a `@Test(arguments:` sweep over the mapped error cases plus one unmapped case
    - The file asserts an exact one-element recorded sequence for a login failure
    - The file contains at least two assertions of an empty recorded-signal collection
    - The file contains a sentinel-credential reflection assertion
    - `xcodebuild test ... -only-testing:SettingFeatureTests` exits TEST SUCCEEDED
    - The full default test plan is green
  </acceptance_criteria>
  <done>Login failure classification, challenge counting and the no-double-count exclusion are all pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer → AnalyticsClient | two emission sites cross this boundary in the module that handles credentials |
| credential handling → analytics | usernames, passwords, cookies and the Cloudflare clearance must be structurally unable to cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure | credential material | high | mitigate | Both signals carry a single closed enum with no field capable of holding a string, and the emission test places sentinel credentials in state and asserts via reflection that neither survives. The reducer's clearance value was already kept out of user-visible strings by Phase 12; this plan does not widen that surface. |
| T-14-13 | Repudiation | error distribution metric | medium | mitigate | The failure branch emits the specific login-failure signal only, never the generic error signal, and the test asserts an exact one-element sequence so a later addition of the generic signal fails the suite. |
| T-14-16 | Tampering | login flow correctness | medium | mitigate | Phase 12 recorded that this case's credential-setting ordering is load-bearing and was itself a bug. The plan forbids touching it and the acceptance criteria assert the diff leaves it unchanged. |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests` reports TEST SUCCEEDED.
- The full default test plan is green.
- The login case's toast construction, round counter, cancellation identifier and credential-setting ordering are unchanged.
- SwiftLint reports zero violations; no suppression directive was added.
</verification>

<success_criteria>
- Login failures emit one classified signal each; successes emit none.
- Challenge encounters are counted per round; cancellation is silent.
- No credential material can reach a signal, proven by a sentinel reflection assertion.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-16-SUMMARY.md` when done.
</output>
