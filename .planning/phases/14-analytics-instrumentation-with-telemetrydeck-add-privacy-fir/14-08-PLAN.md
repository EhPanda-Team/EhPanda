---
phase: 14-analytics-instrumentation
plan: 08
type: execute
wave: 5
depends_on: ["14-06"]
files_modified:
  - AppPackage/Tests/SettingFeatureTests
  - AppPackage/Tests/AppFeatureTests
  - AppPackage/Tests/HomeFeatureTests
  - AppPackage/Tests/DetailFeatureTests
  - AppPackage/Tests/ReadingFeatureTests
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "Every store in the five remaining affected test targets resolves the analytics dependency to the no-op client before any reducer is instrumented"
    - "The hardened-store count reconciles against the store count in each target, so no site is left to fail in a later wave"
    - "The unimplemented default still applies to every target not touched here, so an unexpected analytics call outside the instrumented modules still fails loudly"
  artifacts:
    - path: "AppPackage/Tests/SettingFeatureTests"
      provides: "28 hardened stores across 11 files"
    - path: "AppPackage/Tests/AppFeatureTests"
      provides: "10 hardened stores across 3 files"
    - path: "AppPackage/Tests/HomeFeatureTests"
      provides: "8 hardened stores across 2 files"
    - path: "AppPackage/Tests/DetailFeatureTests"
      provides: "5 hardened stores across 3 files"
    - path: "AppPackage/Tests/ReadingFeatureTests"
      provides: "1 hardened store"
  key_links:
    - from: "AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift"
      via: "the existing makeStore factory gains the analytics override, covering the suite at once"
      pattern: "analyticsClient = .noop"
---

<objective>
Harden the remaining five affected test targets — 52 store constructions across 20 files — against the analytics dependency, ahead of instrumentation.

Purpose: the same reason as plan 14-07, for the other half of the blast radius. Running as a sibling of 14-07 in the same wave is safe because the two plans touch disjoint targets, and completing both before wave 6 is what makes the seven instrumentation plans independently parallel.
Output: every store in `SettingFeatureTests`, `AppFeatureTests`, `HomeFeatureTests`, `DetailFeatureTests` and `ReadingFeatureTests` resolves analytics to the no-op client.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- No new symbols. This plan modifies existing test files in five targets only.

<tasks>

<task type="auto">
  <name>Task 1: Harden SettingFeatureTests</name>
  <files>AppPackage/Tests/SettingFeatureTests</files>
  <read_first>
    - AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift (lines 18-36 — this target already carries the repository's canonical `private @MainActor func makeStore(...) -> TestStoreOf<…>` factory; extend it rather than editing its call sites)
    - AppPackage/Tests/SettingFeatureTests/AppActivityLogsReducerTests.swift (lines 18-40 — a suite that builds a spy client by mutating a no-op value; confirm that idiom still compiles against the analytics client's `var` closure properties)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift
    - .swiftlint.yml (repo root — `sorted_imports` at error)
  </read_first>
  <action>
    Enumerate the store constructions in this target and record the per-file counts in the summary (research counted 28 across 11 files; confirm rather than trust).

    For each suite: if it already has a store factory, add the analytics no-op override there once. Otherwise introduce one following the `SettingReducerNavigationTests` shape, or — where a suite constructs a single store — add the override to that store's existing trailing dependency-configuration closure.

    Add the `AnalyticsClient` import to every touched file, keeping the import block sorted.

    Change no assertion, no state expectation, no effect expectation and no unrelated dependency override.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:SettingFeatureTests` exits TEST SUCCEEDED
    - Every file in the target containing `TestStore(` either contains `analyticsClient = .noop` or routes its stores through a factory that does
    - The summary reconciles the target's store count against its hardened-store count
    - `git diff` shows no change to any `#expect`, `store.send`, `store.receive` or state-mutation closure
  </acceptance_criteria>
  <done>All Setting stores resolve analytics to the no-op client, with assertions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Harden AppFeatureTests and HomeFeatureTests</name>
  <files>AppPackage/Tests/AppFeatureTests, AppPackage/Tests/HomeFeatureTests</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests (every file containing a store construction — note this target drives `AppReducer`, which composes every feature reducer, so it is the target most exposed to a sibling plan's instrumentation)
    - AppPackage/Tests/HomeFeatureTests/FiltersPresentationLifecycleTests.swift (lines 28-40 — the per-store inline override form)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift
  </read_first>
  <action>
    Apply the same treatment to both targets. Record the per-file counts (research counted 10 across 3 files for `AppFeatureTests` and 8 across 2 for `HomeFeatureTests`; confirm).

    Pay particular attention to `AppFeatureTests`: because `AppReducer` scopes every feature reducer, a signal emitted by any wave-6 plan can surface here. Every store in this target must be hardened, including any store constructed for a scoped child reducer.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests -only-testing:HomeFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:AppFeatureTests -only-testing:HomeFeatureTests` exits TEST SUCCEEDED
    - Every file in both targets containing `TestStore(` is covered, directly or through a factory
    - The summary reconciles store counts against hardened-store counts for both targets
  </acceptance_criteria>
  <done>Both targets are hardened, including every scoped-child store in the app-root target.</done>
</task>

<task type="auto">
  <name>Task 3: Harden DetailFeatureTests and ReadingFeatureTests, then prove the whole hardening pass with a real emission</name>
  <files>AppPackage/Tests/DetailFeatureTests, AppPackage/Tests/ReadingFeatureTests</files>
  <read_first>
    - AppPackage/Tests/DetailFeatureTests/CommentsReducerTests.swift (an existing per-store dependency override in this target — the shape to follow)
    - AppPackage/Tests/ReadingFeatureTests (the single store construction in this target)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift
  </read_first>
  <action>
    Apply the same treatment to both targets (research counted 5 across 3 files and 1 respectively; confirm).

    Then prove the pass behaviorally rather than by grep alone. Temporarily add a throwaway analytics emission to one reducer case in each of `SettingFeature`, `AppFeature`, `HomeFeature`, `DetailFeature` and `ReadingFeature` that the corresponding target exercises. Run all five targets, confirm they stay green, then revert every temporary edit and confirm `git status --porcelain AppPackage/Sources` is empty.

    A grep count proves the override text exists; only a real emission proves it takes effect through the store's dependency resolution. Record the five results in the summary.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - The full default test plan reports TEST SUCCEEDED
    - Every file across all five targets containing `TestStore(` is covered, directly or through a factory
    - The summary records the temporary-emission experiment as PASS for all five modules
    - `git status --porcelain AppPackage/Sources` is empty at task end
  </acceptance_criteria>
  <done>All five targets are hardened and each has survived a real analytics emission from its own module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test host → AnalyticsClient | tests must never reach a live client; the unimplemented default plus explicit no-op overrides define this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | owner's analytics dataset | medium | mitigate | Two independent controls: the absent-credential gate under the test host, and an explicit no-op override at every store. Neither is relied on alone. |
| T-14-12 | Denial of Service | test suite reliability | medium | mitigate | Hardening runs in its own wave ahead of all instrumentation, so no wave-6 plan can turn a sibling's target red. Proven per target by count reconciliation and by a temporary real emission from that module. |
</threat_model>

<verification>
- The full default test plan reports TEST SUCCEEDED.
- Store counts and hardened-store counts reconcile in all five targets.
- The temporary-emission experiment passes for all five modules and leaves no residue in the working tree.
</verification>

<success_criteria>
- All 52 remaining store constructions resolve analytics to the no-op client.
- Every affected target survives a real emission from its own module.
- No assertion or unrelated dependency override was changed.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-08-SUMMARY.md` when done.
</output>
