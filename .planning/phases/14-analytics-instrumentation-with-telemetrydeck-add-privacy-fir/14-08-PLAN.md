---
phase: 14-analytics-instrumentation
plan: 08
type: execute
wave: 5
depends_on: ["14-06"]
files_modified:
  - AppPackage/Tests/SettingFeatureTests
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "Every store in SettingFeatureTests resolves the analytics dependency to the no-op client before any reducer is instrumented"
    - "The hardened-store count reconciles against the store count in the target, so no site is left to fail in a later wave"
    - "The unimplemented default still applies to every target not touched here, so an unexpected analytics call outside the hardened targets still fails loudly"
    - "The hardening is proven by a real emission from SettingFeature, not by a grep count alone"
  artifacts:
    - path: "AppPackage/Tests/SettingFeatureTests"
      provides: "28 hardened stores across 11 files — the largest single target in the phase's blast radius after Downloads"
  key_links:
    - from: "AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift"
      via: "the existing makeStore factory gains the analytics override, covering the suite at once"
      pattern: "analyticsClient = .noop"
---

<objective>
Harden `SettingFeatureTests` — 28 store constructions across 11 files — against the analytics dependency, ahead of instrumentation.

Purpose: the same reason as plan 14-07, for the Setting slice of the blast radius. `SettingFeatureTests` is the second-largest affected target and the one instrumented last (plan 14-16), so leaving it to the instrumenting plan would put a 28-site sweep inside a plan that also writes reducer code. Running as a sibling of 14-07 and 14-09 in the same wave is safe because all three touch disjoint targets, and completing all three before wave 6 is what makes the seven instrumentation plans independently parallel.
Output: every store in `SettingFeatureTests` resolves analytics to the no-op client, proven by a real emission from `SettingFeature`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- No new symbols. This plan modifies existing test files in one target only.

<tasks>

<task type="auto">
  <name>Task 1: Harden every store in SettingFeatureTests</name>
  <files>AppPackage/Tests/SettingFeatureTests</files>
  <read_first>
    - AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift (lines 18-36 — this target already carries the repository's canonical `private @MainActor func makeStore(...) -> TestStoreOf<…>` factory; extend it rather than editing its call sites)
    - AppPackage/Tests/SettingFeatureTests/AppActivityLogsReducerTests.swift (lines 18-40 — a suite that builds a spy client by mutating a no-op value; confirm that idiom still compiles against the analytics client's `var` closure properties)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift
    - .swiftlint.yml (repo root — `sorted_imports` at error)
  </read_first>
  <action>
    Enumerate the store constructions in this target and record the per-file counts in the summary (research counted 28 across 11 files; confirm rather than trust).

    For each suite: if it already has a store factory, add the analytics no-op override there once. Otherwise introduce one following the `SettingReducerNavigationTests` shape, or — where a suite constructs a single store — add the override to that store's existing trailing dependency-configuration closure.

    Add the `AnalyticsClient` import to every touched file, keeping the import block sorted.

    Change no assertion, no state expectation, no effect expectation and no unrelated dependency override.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:SettingFeatureTests` exits TEST SUCCEEDED
    - Every file in the target containing `TestStore(` either contains `analyticsClient = .noop` or routes its stores through a factory that does
    - The summary reconciles the target's store count against its hardened-store count
    - `git diff` shows no change to any `#expect`, `store.send`, `store.receive` or state-mutation closure
  </acceptance_criteria>
  <done>All Setting stores resolve analytics to the no-op client, with assertions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the hardening with a real emission from SettingFeature</name>
  <files>AppPackage/Tests/SettingFeatureTests</files>
  <read_first>
    - AppPackage/Sources/SettingFeature/Login/LoginReducer.swift (the reducer plan 14-16 will instrument — the natural place to site the temporary probe emission)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift (the `testValue = .unimplemented` default this experiment is exercising)
  </read_first>
  <action>
    Prove the pass behaviorally rather than by grep alone. Temporarily add a throwaway analytics emission to one reducer case in `SettingFeature` that this target exercises. Run the target, confirm it stays green, then revert the temporary edit and confirm `git status --porcelain AppPackage/Sources` is empty.

    A grep count proves the override text exists; only a real emission proves it takes effect through the store's dependency resolution — `TestStore` resolves dependencies per store, so a file-level import or an override on the wrong store reads as coverage without being coverage.

    If the probe turns the target red, that is the plan's real finding: locate the unhardened store, harden it, and re-run before reverting. Record the probe result and any store found this way in the summary.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:SettingFeatureTests` exits TEST SUCCEEDED
    - The summary records the temporary-emission experiment as PASS for `SettingFeature`, and lists any store the probe exposed
    - `git status --porcelain AppPackage/Sources` is empty at task end
    - `git diff --name-only` lists no file outside `AppPackage/Tests/SettingFeatureTests`
  </acceptance_criteria>
  <done>SettingFeatureTests has survived a real analytics emission from its own module, and the working tree carries no probe residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test host → AnalyticsClient | tests must never reach a live client; the unimplemented default plus explicit no-op overrides define this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | owner's analytics dataset | medium | mitigate | Two independent controls: the absent-credential gate under the test host, and an explicit no-op override at every store. Neither is relied on alone. |
| T-14-12 | Denial of Service | test suite reliability | medium | mitigate | Hardening runs in its own wave ahead of all instrumentation, so no wave-6 plan can turn a sibling's target red. Proven by count reconciliation and by a temporary real emission from `SettingFeature`. |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:SettingFeatureTests` reports TEST SUCCEEDED.
- The target's store count and hardened-store count reconcile in the summary.
- The temporary-emission experiment passes and leaves no residue in the working tree.
</verification>

<success_criteria>
- All 28 store constructions in `SettingFeatureTests` resolve analytics to the no-op client.
- The target survives a real emission from `SettingFeature`.
- No assertion or unrelated dependency override was changed.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-08-SUMMARY.md` when done.
</output>
