---
phase: 14-analytics-instrumentation
plan: 07
type: execute
wave: 5
depends_on: ["14-06"]
files_modified:
  - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "Every DownloadsFeatureTests store resolves the analytics dependency to the no-op client, so instrumenting DownloadsReducer in a later wave cannot turn this target red"
    - "The number of hardened stores equals the number of stores in the target — no site is left behind to fail later in a different plan's wave"
    - "The unimplemented default still applies everywhere it was not explicitly overridden, so an unexpected analytics call in an untouched suite still fails loudly"
  artifacts:
    - path: "AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift"
      provides: "The target's shared test helper surface, extended with the analytics stub"
  key_links:
    - from: "AppPackage/Tests/DownloadsFeatureTests"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift"
      via: "every TestStore overrides analyticsClient with the no-op client"
      pattern: "analyticsClient = .noop"
---

<objective>
Harden every store in `DownloadsFeatureTests` — the largest affected target, 75 store constructions across 23 files — against the analytics dependency, **before** any reducer is instrumented.

Purpose: D-12 locks `testValue` to the unimplemented client, which is the correct loud default but means the first instrumented action turns existing suites red. Doing this hardening as its own wave, ahead of instrumentation, is what lets the seven instrumentation plans in the next wave run in parallel without any of them inheriting another plan's test breakage. Research counted this target alone at 75 sites and called it out as real, mechanical work that must not be deferred to one heroic cleanup.
Output: every store in the target resolves analytics to the no-op client, with the count verified against the store count.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- No new symbols. This plan modifies existing test files in `AppPackage/Tests/DownloadsFeatureTests/` only.

<tasks>

<task type="auto">
  <name>Task 1: Harden the DownloadsFeatureTests stores — first half</name>
  <files>AppPackage/Tests/DownloadsFeatureTests (the first half of the files containing a TestStore construction, in `git ls-files` order)</files>
  <read_first>
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift (whole file — the `DownloadFeatureTestCase` protocol and the free helper functions this target already shares; if a shared store builder exists, extend it instead of touching its callers)
    - AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift (lines 18-36 — the repository's established per-suite `private @MainActor func makeStore(...) -> TestStoreOf<…>` factory, which is the preferred shape wherever a suite constructs several similar stores)
    - AppPackage/Tests/HomeFeatureTests/FiltersPresentationLifecycleTests.swift (lines 28-40 — the per-store inline override form, for suites where a factory would not fit)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift (confirm the no-op value's spelling)
    - .swiftlint.yml (repo root — `sorted_imports` at error; adding an `AnalyticsClient` import to a test file must keep the import block sorted)
  </read_first>
  <action>
    First, enumerate the work precisely: run a search for `TestStore(` across `AppPackage/Tests/DownloadsFeatureTests`, record the per-file counts and the total in the summary, and split the file list in half. This task handles the first half; Task 2 handles the second.

    For each store construction in this task's files, ensure the analytics dependency resolves to the no-op client:
    - If the file's suite already has a shared store factory, add the override there once and change nothing at the call sites.
    - If the suite constructs several similar stores without a factory, introduce a `private @MainActor func makeStore(...) -> TestStoreOf<…>` following the `SettingReducerNavigationTests` shape and route the suite's stores through it. That is the repository's established answer to exactly this problem and it shrinks the diff.
    - Otherwise add the override to the store's existing trailing dependency-configuration closure, or add such a closure if the site has none.

    Add the `AnalyticsClient` import to each touched file, keeping the import block sorted — the sorted-imports rule is error severity and `@testable` imports sort by module name, not by the prefix.

    Do not change any assertion, any state expectation, any effect expectation, or any other dependency override. This is a pure dependency-hardening pass; if a test's behavior changes, something has gone wrong and the diff must be revisited.

    Do not add a suite-level dependency trait. That would require declaring an additional test-support package that is currently only a transitive dependency, which is a larger change than this phase should carry and sits awkwardly with the milestone's dependency-reduction theme.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:DownloadsFeatureTests` exits TEST SUCCEEDED
    - Every file in this task's half that contains `TestStore(` either contains `analyticsClient = .noop` or routes all its stores through a factory that does
    - `git diff` for this task shows no change to any `#expect`, `store.send`, `store.receive` or state-mutation closure
    - The summary records the per-file `TestStore(` counts and the total for the whole target
  </acceptance_criteria>
  <done>The first half of the target's stores resolve analytics to the no-op client, with assertions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the DownloadsFeatureTests stores — second half, and prove full coverage</name>
  <files>AppPackage/Tests/DownloadsFeatureTests (the remaining files containing a TestStore construction)</files>
  <read_first>
    - AppPackage/Tests/DownloadsFeatureTests/DownloadFeatureTestHelpers.swift
    - The summary or working notes from Task 1 (the file list and per-file counts)
    - AppPackage/Tests/SettingFeatureTests/SettingReducerNavigationTests.swift (the factory shape)
  </read_first>
  <action>
    Apply the same treatment as Task 1 to the remaining files.

    Then prove coverage rather than assuming it. Produce two counts across the whole target: the number of store constructions, and the number of stores that resolve analytics to the no-op client (counting a factory as covering every store routed through it). Reconcile them explicitly in the summary — if they differ, every difference must be named and justified, not rounded away. A store left unhardened is a suite that will turn red in a different plan's wave, which is precisely the cross-wave failure this plan exists to prevent.

    As a second, behavior-level proof, temporarily add a throwaway analytics emission to one `DownloadsReducer` case that this target exercises heavily, run the target, confirm it stays green, then revert that edit and confirm `git status` is clean for `AppPackage/Sources`. A grep count proves the text is present; this proves the override actually takes effect through the store's dependency resolution.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:DownloadsFeatureTests` exits TEST SUCCEEDED
    - The summary reconciles the store count against the hardened-store count, naming any difference
    - The temporary-emission experiment is recorded in the summary as PASS, and `git status --porcelain AppPackage/Sources` is empty at task end
    - The full default test plan is green
  </acceptance_criteria>
  <done>Every store in the target is hardened, proven by count reconciliation and by a temporary real emission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test host → AnalyticsClient | tests must never reach a live client; the unimplemented default plus explicit no-op overrides define this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | owner's analytics dataset | medium | mitigate | Two independent controls keep test runs out of the dataset: the absent-credential gate resolves `live` to no-op under the test host (plan 14-04/14-06), and every store here explicitly resolves the dependency to no-op. Neither alone is relied upon. |
| T-14-12 | Denial of Service | test suite reliability | medium | mitigate | Hardening in its own wave, ahead of instrumentation, is what prevents one plan's instrumentation from turning a sibling plan's target red mid-wave. Coverage is proven by count reconciliation plus a temporary real emission, not by a green run alone. |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DownloadsFeatureTests` reports TEST SUCCEEDED.
- Store count and hardened-store count reconcile, with any difference named.
- No assertion, expectation or unrelated dependency override was modified.
</verification>

<success_criteria>
- All 75 store constructions in `DownloadsFeatureTests` resolve analytics to the no-op client.
- The target survives a real analytics emission from an instrumented reducer case.
- The full default test plan is green.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-07-SUMMARY.md` when done.
</output>
