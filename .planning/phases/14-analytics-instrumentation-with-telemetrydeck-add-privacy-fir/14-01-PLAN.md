---
phase: 14-analytics-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Package.swift
  - AppPackage/Sources/AnalyticsClient/.swiftlint.yml
  - AppPackage/Sources/AnalyticsClient/Buckets.swift
  - AppPackage/Tests/AnalyticsClientTests/.swiftlint.yml
  - AppPackage/Tests/AnalyticsClientTests/BucketTests.swift
  - AppPackage/Tests/FeatureTests.xctestplan
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "The TelemetryDeck SPM package resolves to a 2.x stable tag — no 3.0.0 pre-release is pinned (D-12 supply-chain hygiene)"
    - "AnalyticsClient exists as a compilable SPM module carrying its own SwiftLint config, per the repository's new-module rule"
    - "AnalyticsClientTests runs in the default test plan — an unregistered target reports zero tests and looks green"
    - "CountBucket and DurationBucket map every integer/duration input to exactly one bucket, with no gaps or overlaps at the boundaries (D-08)"
    - "Every feature module and test target that will emit or stub analytics can already resolve the AnalyticsClient module, so no later wave needs to touch Package.swift for dependency edges"
  artifacts:
    - path: "AppPackage/Sources/AnalyticsClient/Buckets.swift"
      provides: "D-08 bucket vocabulary — CountBucket and DurationBucket"
      contains: "public enum CountBucket"
    - path: "AppPackage/Sources/AnalyticsClient/.swiftlint.yml"
      provides: "Lint coverage for the new source module"
      contains: "parent_config"
    - path: "AppPackage/Tests/AnalyticsClientTests/.swiftlint.yml"
      provides: "Lint coverage for the new test target"
      contains: "parent_config"
    - path: "AppPackage/Tests/AnalyticsClientTests/BucketTests.swift"
      provides: "Exhaustive boundary coverage for both bucket enums"
  key_links:
    - from: "AppPackage/Package.swift"
      to: "AppPackage/Tests/FeatureTests.xctestplan"
      via: "AnalyticsClientTests declared and registered in the default unit plan"
      pattern: "AnalyticsClientTests"
    - from: "AppPackage/Package.swift"
      to: "AppPackage/Sources/AnalyticsClient"
      via: "telemetryDeck product alias resolved onto the analyticsClient target"
      pattern: "telemetryDeck"
---

<objective>
Create the `AnalyticsClient` SPM module and its test target, add the TelemetryDeck SDK dependency, and land the D-08 bucket vocabulary as the module's first real content.

Purpose: everything else in Phase 14 depends on this module existing. This plan is also the **sole owner of `AppPackage/Package.swift` dependency edges for the phase** — it wires `.module(.analyticsClient)` into every feature target and test target that later waves instrument, so no instrumentation plan has to touch the manifest and later waves can run fully parallel.
Output: a compilable, lint-covered `AnalyticsClient` module with a registered, green `AnalyticsClientTests` target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- New SPM package dependency: `github.com/TelemetryDeck/SwiftSDK`, product `TelemetryDeck`
- `Target.Dependency` alias `telemetryDeck` in `AppPackage/Package.swift`
- New `Module` enum cases: `analyticsClient = "AnalyticsClient"`, `analyticsClientTests = "AnalyticsClientTests"`
- New SPM source target `AnalyticsClient`; new SPM test target `AnalyticsClientTests`
- New files: `AppPackage/Sources/AnalyticsClient/.swiftlint.yml`, `AppPackage/Sources/AnalyticsClient/Buckets.swift`, `AppPackage/Tests/AnalyticsClientTests/.swiftlint.yml`, `AppPackage/Tests/AnalyticsClientTests/BucketTests.swift`
- New public types: `CountBucket`, `DurationBucket`
- New `FeatureTests.xctestplan` entry: `AnalyticsClientTests`

<tasks>

<task type="auto">
  <name>Task 1: Add the TelemetryDeck dependency and declare the AnalyticsClient source + test targets</name>
  <files>AppPackage/Package.swift, AppPackage/Sources/AnalyticsClient/.swiftlint.yml, AppPackage/Tests/AnalyticsClientTests/.swiftlint.yml</files>
  <read_first>
    - AppPackage/Package.swift (whole file — the `dependencies` array ordering, the `Target.Dependency` alias extension, the `Module` enum, the `.target(module:dependencies:plugins:)` and `.testTarget(module:dependencies:plugins:)` helper conventions, and every existing feature/test target this task must extend)
    - AppPackage/Sources/HapticsClient/.swiftlint.yml (the exact one-line per-module lint config to copy)
    - AppPackage/Tests/CookieClientTests/.swiftlint.yml (same file at test-target depth)
    - .swiftlint.yml (repo root — read before writing any Swift this phase; `sorted_imports`, `single_line_trailing_closure`, `labeled_tuple_elements`, `line_length` 120 are all error severity and not suppressible)
    - CLAUDE.md (repo root — the new-module `.swiftlint.yml` rule and the no-suppressions rule)
  </read_first>
  <action>
    In `AppPackage/Package.swift`:

    1. Add to the `dependencies` array, in the existing alphabetical-by-owner position (after `SimplyDanny/SwiftLintPlugins`, before `apple/swift-markdown`): `.package(url: "https://github.com/TelemetryDeck/SwiftSDK", .upToNextMajor(from: "2.14.1"))`. Use the explicit `.upToNextMajor` range rather than bare `from:` — research assumption A5 flags that a bare `from:` risks resolving a `3.0.0-beta.*` pre-release, and 3.0.0 has no stable tag. Add a short comment above it in the style of the existing pinned entries, naming the product/repo mismatch (repo is `SwiftSDK`, product is `TelemetryDeck`).

    2. Add to the `PackageDescription.Target.Dependency` extension, alphabetically after `sharing`: `static let telemetryDeck: Self = .product(name: "TelemetryDeck", package: "SwiftSDK")`. Research assumption A1 is unverified: the package directory name is `SwiftSDK` but its manifest declares `name: "TelemetryDeck"`. Try `package: "SwiftSDK"` first (matching the existing `ColorfulX`/`SwiftyOpenCC` URL-last-component convention); if resolution fails, change that one string to `"TelemetryDeck"`. Record which value worked in the summary.

    3. Add `case analyticsClient = "AnalyticsClient"` to the `Module` enum, alphabetically before `animatedImageFeature`. Add `case analyticsClientTests = "AnalyticsClientTests"` to the test-target region of the same enum, following the local ordering there.

    4. Declare the source target, following the `cookieClient` shape (a client with module edges):
       `.target(module: .analyticsClient, dependencies: [.module(.appModels), .module(.cookieClient), .targetDependency(.composableArchitecture), .targetDependency(.sharing), .targetDependency(.telemetryDeck)], plugins: swiftLintPlugins)`.
       The `.module(.cookieClient)` edge is a deliberate plan-time decision (RESEARCH flagged it as optional): D-11's login-state default parameter reads `@SharedReader(.didLogin)`, whose key lives in `CookieClient`. Threading login state in from the launch site instead was rejected because it would freeze the value at launch, contradicting D-11's per-signal snapshot semantics.

    5. Declare the test target following the `cookieClientTests` shape:
       `.testTarget(module: .analyticsClientTests, dependencies: [.module(.analyticsClient), .module(.appModels), .module(.cookieClient), .targetDependency(.composableArchitecture), .targetDependency(.sharing)], plugins: swiftLintPlugins)`.

    6. **Wire the dependency edges every later wave needs, so no other plan edits this manifest.** Add `.module(.analyticsClient)` to the `dependencies` of these existing source targets: `appFeature`, `homeFeature`, `searchFeature`, `quickSearchFeature`, `favoritesFeature`, `detailFeature`, `readingFeature`, `downloadsFeature`, `settingFeature`. Add `.module(.analyticsClient)` to these existing test targets: `appFeatureTests`, `homeFeatureTests`, `detailFeatureTests`, `readingFeatureTests`, `downloadsFeatureTests`, `settingFeatureTests`. Use the exact module-case spellings already present in the `Module` enum; if any of the listed feature modules is spelled differently in the enum, use the manifest's spelling and note the deviation in the summary. An unused target dependency is inert in SwiftPM, so declaring these ahead of use costs nothing and removes a cross-wave file conflict.

    Create `AppPackage/Sources/AnalyticsClient/.swiftlint.yml` and `AppPackage/Tests/AnalyticsClientTests/.swiftlint.yml`, each containing exactly one line: `parent_config: ../../../.swiftlint.yml`.
  </action>
  <verify>
    <automated>swift package --package-path AppPackage resolve 2>&1 | tail -3 && grep -c "AnalyticsClient" AppPackage/Package.swift</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Package.swift` contains `https://github.com/TelemetryDeck/SwiftSDK` and `.upToNextMajor(from: "2.14.1")`
    - `AppPackage/Package.swift` contains `analyticsClient = "AnalyticsClient"` and `analyticsClientTests = "AnalyticsClientTests"`
    - `grep -c 'module(.analyticsClient)' AppPackage/Package.swift` returns at least 15
    - `AppPackage/Sources/AnalyticsClient/.swiftlint.yml` and `AppPackage/Tests/AnalyticsClientTests/.swiftlint.yml` each contain `parent_config: ../../../.swiftlint.yml`
    - `swift package --package-path AppPackage resolve` exits 0
    - `grep -A3 'TelemetryDeck' AppPackage/Package.resolved` shows a version beginning `2.` — no `-beta` substring anywhere in the SwiftSDK pin
  </acceptance_criteria>
  <done>The manifest declares the SDK and both new targets, every later-wave dependency edge is already in place, and resolution pins a 2.x stable tag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the D-08 bucket vocabulary with exhaustive boundary tests</name>
  <files>AppPackage/Sources/AnalyticsClient/Buckets.swift, AppPackage/Tests/AnalyticsClientTests/BucketTests.swift</files>
  <read_first>
    - AppPackage/Sources/AnalyticsClient/.swiftlint.yml (confirm lint coverage is in place before writing Swift)
    - .swiftlint.yml (repo root)
    - AppPackage/Sources/AppModels/Support/AppError.swift (the repo's closed-enum + derived-rendering house style)
    - AppPackage/Tests/CookieClientTests/CookieClientTests.swift (Swift Testing suite conventions for a client-level target: `@Suite` on the type, sorted imports, no force-unwraps)
    - .planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md (D-08 and its single documented exception)
  </read_first>
  <behavior>
    `CountBucket.init(count: Int)` — total, no gaps, no overlaps:
    - count <= 0 → `.zero`, rendering `"0"`
    - count == 1 → `.one`, rendering `"1"`
    - 2...5 → `.twoToFive`, rendering `"2-5"`
    - 6...20 → `.sixToTwenty`, rendering `"6-20"`
    - 21...50 → `.twentyOneToFifty`, rendering `"21-50"`
    - 51 and above → `.fiftyOnePlus`, rendering `"51+"`
    Negative input clamps to `.zero` rather than trapping (a negative counter is a programming error, not a user event, and analytics must never crash the app).

    `DurationBucket.init(seconds: TimeInterval)` — total, no gaps, no overlaps:
    - seconds < 10 (including zero and negative) → `.underTenSeconds`, rendering `"0-10s"`
    - 10 ..< 60 → `.tenToSixtySeconds`, rendering `"10-60s"`
    - 60 ..< 300 → `.oneToFiveMinutes`, rendering `"1-5m"`
    - 300 ..< 1200 → `.fiveToTwentyMinutes`, rendering `"5-20m"`
    - 1200 and above → `.overTwentyMinutes`, rendering `"20m+"`

    Both enums are `CaseIterable`, `Equatable`, `Sendable`, with a `String` raw value carrying the rendering above. Both boundary values on every edge are asserted in the tests (e.g. 1/2, 5/6, 20/21, 50/51 for counts; 9.999/10, 59.999/60, 299.999/300, 1199.999/1200 for durations).
  </behavior>
  <action>
    Create `AppPackage/Sources/AnalyticsClient/Buckets.swift` with two public `String`-raw-value enums, `CountBucket` and `DurationBucket`, exactly as specified in the behavior block. Give each a public failable-free initializer taking the raw measurement (`init(count: Int)` / `init(seconds: TimeInterval)`) implemented with a `switch` over ranges — not a chain of `if` statements — so the compiler's exhaustiveness checking carries the "no gaps" guarantee.

    Document at the top of the file, in one short comment, that these two enums are the sole numeric vocabulary permitted in analytics payloads per D-08, and that the one documented exception (exact search-keyword length) is carried elsewhere. Do not mention any external project.

    Reuse one bucket vocabulary everywhere rather than per-metric boundaries: it keeps the D-08 guarantee auditable in one file and makes cross-metric comparison possible on the dashboard.

    Create `AppPackage/Tests/AnalyticsClientTests/BucketTests.swift` — a Swift Testing `@Suite` (no `@MainActor`; these are pure functions) using `@Test(arguments:)` parameterized fixtures that assert the bucket AND its raw value for every boundary pair listed in the behavior block, plus one representative interior value per bucket, plus the negative-input clamp for both types.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AnalyticsClientTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/AnalyticsClient/Buckets.swift` contains `public enum CountBucket` and `public enum DurationBucket`
    - Buckets.swift contains `init(count: Int)` and `init(seconds: TimeInterval)`
    - `grep -c 'case ' AppPackage/Sources/AnalyticsClient/Buckets.swift` returns at least 11 (6 count cases + 5 duration cases)
    - `AppPackage/Tests/AnalyticsClientTests/BucketTests.swift` contains `@Test(arguments:`
    - `xcodebuild test ... -only-testing:AnalyticsClientTests` exits TEST SUCCEEDED
    - SwiftLint reports zero violations for `AppPackage/Sources/AnalyticsClient` (the build-tool plugin runs during the build)
  </acceptance_criteria>
  <done>Both bucket enums exist with total, gap-free mappings proven at every boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Register AnalyticsClientTests in the default test plan and prove it runs</name>
  <files>AppPackage/Tests/FeatureTests.xctestplan</files>
  <read_first>
    - AppPackage/Tests/FeatureTests.xctestplan (the `CookieClientTests` entry near line 127 is the exact shape to copy: containerPath "container:AppPackage", identifier and name)
    - .planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-VALIDATION.md (the registration-hazard note and its correction)
  </read_first>
  <action>
    Add an `AnalyticsClientTests` entry to the `testTargets` array in `AppPackage/Tests/FeatureTests.xctestplan`, copying the `CookieClientTests` entry's exact JSON shape (`"containerPath" : "container:AppPackage"`, `"identifier" : "AnalyticsClientTests"`, `"name" : "AnalyticsClientTests"`) and the file's existing indentation and key spacing.

    Do **not** edit `EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme`. `14-RESEARCH.md` claims a scheme edit is also required; that claim is verified false and corrected in `14-VALIDATION.md` — the scheme's test action holds only test-plan references and no testable references, so the test plan is the sole registration surface. Confirm this rather than assume it: the acceptance criteria below assert the scheme is unchanged.

    Then run the whole default plan once and record the wall-clock runtime in the summary, so `14-VALIDATION.md`'s feedback-latency figure can be filled in.
  </action>
  <verify>
    <automated>grep -c "AnalyticsClientTests" AppPackage/Tests/FeatureTests.xctestplan && xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AnalyticsClientTests" AppPackage/Tests/FeatureTests.xctestplan` returns 2
    - `git diff --name-only` does NOT list `EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme`
    - `grep -c "TestableReference" EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme` returns 0 (confirming the corrected registration model)
    - A full `xcodebuild test -scheme EhPanda` run reports TEST SUCCEEDED and its output lists `AnalyticsClientTests` among the executed suites
    - The full-suite wall-clock runtime is recorded in the plan summary
  </acceptance_criteria>
  <done>The new test target executes in the default plan, verified by name in the run output rather than by a green build alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | a third-party SPM package enters the dependency graph for the first time this milestone |
| app process → TelemetryDeck ingestion | every signal that will later be emitted crosses this outbound boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-SC | Tampering | SPM dependency `TelemetryDeck/SwiftSDK` | high | mitigate | Package legitimacy audited in `14-RESEARCH.md` §Package Legitimacy Audit (verdict OK, approved; repo age ~5.8 yrs, active release cadence, URL obtained from the vendor's own setup guide and confirmed against the GitHub API). Pin with `.upToNextMajor(from: "2.14.1")` and assert no `-beta` substring in the resolved pin. |
| T-14-02 | Information Disclosure | `CountBucket` / `DurationBucket` | medium | accept | D-08 bucketing is the owner-chosen mitigation for counter-based re-identification; boundary totality is proven by test. Residual aggregate risk is accepted per D-07/D-08. |
| T-14-04 | Tampering | analytics parameter keys | low | mitigate | Bucket raw values are payload *values*, never keys; the reserved-key collision surface is addressed where keys are minted (plan 14-05). |
</threat_model>

<verification>
- `swift package --package-path AppPackage resolve` succeeds and `Package.resolved` pins a 2.x SwiftSDK tag with no pre-release suffix.
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'` reports TEST SUCCEEDED with `AnalyticsClientTests` present in the executed-suite list.
- SwiftLint reports zero violations across `AppPackage/Sources/AnalyticsClient` and `AppPackage/Tests/AnalyticsClientTests`; no suppression directive was added anywhere.
</verification>

<success_criteria>
- `AnalyticsClient` and `AnalyticsClientTests` are declared targets with per-module SwiftLint configs.
- The TelemetryDeck SDK is a resolved dependency at a 2.x stable tag.
- `CountBucket` and `DurationBucket` exist with total, boundary-proven mappings.
- Every feature and test target that later waves touch already resolves `AnalyticsClient`, so no later plan modifies `AppPackage/Package.swift` for dependency edges.
- The full existing suite is still green.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-01-SUMMARY.md` when done.
</output>
