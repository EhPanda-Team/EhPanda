---
phase: 14-analytics-instrumentation
plan: 17
type: execute
wave: 7
depends_on: ["14-10", "14-11", "14-12", "14-13", "14-14", "14-15", "14-16"]
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .swiftlint.yml
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "The README discloses that anonymous usage data is collected, what is collected, what is never collected, and links to the vendor's privacy policy (D-03)"
    - "The README states that a build without the local analytics configuration silently sends nothing, so a release built without it is not mistaken for a working one"
    - "No planning or repository artifact still describes this phase's analytics as opt-in — D-01 chose default-on with no opt-out"
    - "The phase's requirement ID, opened in wave 1 by plan 14-01, is verified against what shipped and closed out here, so milestone coverage accounting stays honest"
    - "If the owner approved it, a lint rule makes the single-SDK-import boundary structural rather than conventional (D-18)"
  artifacts:
    - path: "README.md"
      provides: "The phase's only user-facing disclosure surface (D-03)"
      contains: "Analytics"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ANALYTICS-01 verified against the shipped signal surface and marked complete"
      contains: "ANALYTICS-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "ANALYTICS-01 appears in both the requirement list and the phase's requirement line"
      pattern: "ANALYTICS-01"
---

<objective>
Close the phase's documentation and bookkeeping: the README disclosure D-03 requires, the stale roadmap wording D-01 invalidated, the close-out of the requirement plan 14-01 opened, and — if the owner approved it in D-18 — the lint rule that makes the single-SDK-import boundary structural.

Purpose: D-03 makes the README the *only* disclosure surface in the entire product, so its completeness is not a documentation nicety — it is the whole of what a user can learn about this. The bookkeeping items are small but each one is a place where a later reader would otherwise be misled.
Output: a README section, corrected roadmap wording, a verified and closed-out requirement, and a conditional lint rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-DECISIONS.md
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/COVERAGE.md
</context>

## Artifacts this phase produces (this plan's share)

- New `README.md` section titled Analytics
- `ANALYTICS-01` in `.planning/REQUIREMENTS.md` verified against the shipped surface and marked complete (the entry itself is created by plan 14-01 in wave 1)
- Corrected `.planning/ROADMAP.md` Phase 14 lines — both the top-of-file checklist entry and the Phase Details goal and requirement lines
- Conditional new `custom_rules` entry in the repository `.swiftlint.yml`

<tasks>

<task type="auto">
  <name>Task 1: The README Analytics disclosure</name>
  <files>README.md</files>
  <read_first>
    - README.md (whole file — the section order is Installation, System Requirements, Content and Copyright, Questions and Feedback, Screenshots; every heading is second level with no third-level headings anywhere; prose sections are short and declarative with no bullet lists; a single bolded sentence carries the warning in the copyright section; links are inline. Line 20 documents the sideload distribution that the legal basis in D-02 depends on.)
    - .planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md (D-02's legal reasoning, D-03's required content, D-06's never-send list, D-07's allow-list, D-10's identifier)
    - AppPackage/Sources/AnalyticsClient/AnalyticsSignal.swift (the shipped signal list — describe what actually ships, not what was planned)
    - AppPackage/Sources/AnalyticsClient/AnalyticsDefaultParameters.swift (the six settings on every signal)
    - Config/Analytics.xcconfig (the build-time credential the release build needs)
  </read_first>
  <behavior>
    A new second-level Analytics section placed after the Content and Copyright section, matching the file's prose conventions: short declarative sentences, no bullet lists in the prose, one bolded sentence carrying the sentence a reader most needs, inline links.

    It must state, accurately against what shipped:
    - That the app sends anonymous usage data to the analytics vendor, and that this is on by default with no in-app setting to turn it off.
    - What is collected, described in categories a user can understand: which screens are opened, searches performed as a shape rather than as text, reading sessions as bucketed page counts and durations, download outcomes, and which errors occur — plus the current values of a handful of app settings on every event.
    - What is **never** collected, listed explicitly from D-06: gallery identifiers, tokens and titles; any gallery or page URL; search keyword text; tag values; usernames; cookies or any credential; and file paths.
    - That events carry the vendor's built-in anonymous per-install identifier, hashed on device, with no IP address retained — described without over-claiming its stability, since the underlying vendor identifier regenerates if every app from the same vendor is removed from the device.
    - A link to the vendor's privacy policy.
    - That a build made without the local analytics configuration file sends nothing at all — which matters both to contributors building from source and to anyone cutting a release.

    It must **not** describe the feature as opt-in, offer an opt-out that does not exist, or promise a setting.
  </behavior>
  <action>
    Write the section per the behavior block, placed after the Content and Copyright section.

    Describe what shipped, not what was planned: read the signal enum and the default-parameter file first and reconcile the prose against them. If the shipped signal set differs from the plan's expectation in any way, the README follows the code and the summary records the difference.

    Keep the never-collected list explicit and complete. It is the part of this section that a privacy-conscious reader will actually read, and an incomplete list is worse than none because it implies the omitted items are collected.

    Do not name any other local project. Do not write an absolute home-directory path anywhere in the section.
  </action>
  <verify>
    <automated>grep -c "^## Analytics" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a line matching `^## Analytics`
    - The section contains the words gallery, keyword, cookie and path in its never-collected sentence, and contains no third-level heading
    - `grep -ci "opt-in\|opt out\|opt-out" ` over the new section returns matches only in the sentence stating there is no opt-out
    - The section contains an inline link to the vendor's privacy policy
    - The section states that a build without the local configuration sends nothing
    - `grep -c "/Users/" README.md` returns 0
  </acceptance_criteria>
  <done>The product's only disclosure surface accurately describes what ships, including what never leaves the device.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale roadmap wording and close out ANALYTICS-01</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (**two** places describe Phase 14 and both must be checked: the top-of-file phase checklist entry at line 37, and the Phase Details goal line near line 748. At the time of planning, line 37 still described the phase as opt-in and line 748 was already correct — verify both rather than trusting that.)
    - .planning/REQUIREMENTS.md (whole file — the `ANALYTICS-01` entry plan 14-01 opened in wave 1, its indented acceptance-criteria line, the requirement-to-phase mapping table, and the coverage counts)
    - .planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md (D-01, and the note that the roadmap wording is stale)
    - AppPackage/Sources/AnalyticsClient/AnalyticsSignal.swift (what actually shipped, for judging whether the requirement can be marked complete)
  </read_first>
  <behavior>
    - Every line of `.planning/ROADMAP.md` that describes Phase 14 states what was built: privacy-first analytics that are on by default with no opt-out. This includes both the top-of-file checklist entry (line 37) and the Phase Details goal line — the checklist entry is the one that was stale at planning time. Wording that implies user consent must not survive anywhere in the file, because D-01 chose the opposite and stale wording is how a future reader concludes a consent surface was dropped rather than never intended.
    - The Phase 14 requirement line in the roadmap names `ANALYTICS-01`.
    - `.planning/REQUIREMENTS.md` already carries the `ANALYTICS-01` entry, its mapping row and its coverage counts — plan 14-01 created them in wave 1 so the ID every plan cites resolved from the first wave. This task verifies them against what shipped and marks the entry complete; it does not re-create them.
    - Mark the entry complete only if every instrumentation plan's summary reports green. Otherwise leave it unchecked and say so in the summary.
  </behavior>
  <action>
    Correct every Phase 14 line in `.planning/ROADMAP.md` that describes the analytics as consent-gated. Start with the top-of-file checklist entry at line 37 — at planning time it read "privacy-first, opt-in analytics" while the Phase Details goal line was already correct, so the change is **not** confined to the Phase Details section and must not be scoped that way. Use scoped edits that touch only the Phase 14 lines; never rewrite the whole file.

    Verify `.planning/REQUIREMENTS.md`'s `ANALYTICS-01` entry against what actually shipped: read the signal enum and confirm the acceptance-criteria line still describes the delivered guarantees. Correct the wording if the shipped surface differs from what wave 1 anticipated, and update the mapping row or coverage counts only if they are wrong. Then tick the entry's checkbox if every instrumentation plan's summary reports green, and update the file's trailing last-updated line in the style it already uses.

    Then sweep the repository's planning artifacts and source for any remaining description of this phase's analytics as opt-in, and correct each one found. The context and research documents record the historical discussion and may legitimately mention the stale wording as history — leave those historical notes intact, but correct anything that reads as a present-tense description of the shipped behavior. Record in the summary which files were swept and which were corrected.
  </action>
  <verify>
    <automated>grep -c "ANALYTICS-01" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains zero occurrences of `opt-in` in any Phase 14 line — both the top-of-file checklist entry and the Phase Details goal line are corrected
    - `.planning/ROADMAP.md`'s Phase 14 requirement line contains `ANALYTICS-01`
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to Phase 14 lines — no other phase's entry is touched
    - `.planning/REQUIREMENTS.md` still contains the `ANALYTICS-01` entry with an indented acceptance-criteria line, a mapping row to Phase 14, and coverage counts arithmetically consistent with the entry list
    - The entry's checkbox state matches the summary's statement about instrumentation-plan greenness
    - The summary lists every file swept for stale consent wording and every one corrected
  </acceptance_criteria>
  <done>Every present-tense description of Phase 14's analytics matches what shipped, and the phase's requirement is verified and closed out.</done>
</task>

<task type="auto">
  <name>Task 3: The conditional single-import lint rule</name>
  <files>.swiftlint.yml</files>
  <read_first>
    - .planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-DECISIONS.md (D-18 — if the owner declined, this task is a no-op and the summary must say so rather than implementing it anyway)
    - .swiftlint.yml (repo root — the `custom_rules` block starting near line 40. Read several existing rules end to end: each carries a name, an included or excluded path scope where relevant, a regex, a message, and a severity, and the repository's rules are uniformly error severity. Note also the excluded-match-kinds mechanism some rules use to avoid firing inside comments and doc comments — Phase 11 recorded that the doc-comment kind spelling matters and that an invalid entry silently discards the whole rule configuration.)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift (the one file legitimately carrying the SDK import, which the rule must exclude)
  </read_first>
  <behavior>
    - If D-18 approved the rule: a new `custom_rules` entry rejects the analytics SDK's import statement everywhere except the analytics client module, at error severity, with a message naming the decision it enforces. The rule excludes the analytics client module by path and excludes comment and doc-comment match kinds so a comment discussing the boundary does not trip it.
    - The rule is verified to actually fire: temporarily add the import to a file outside the client module, confirm the build reports the violation, then revert.
    - The rule is verified not to fire falsely: with the temporary file reverted, a clean build reports zero violations across the whole repository.
    - If D-18 declined: change nothing, and record in the summary that the type wall in the signal API remains the sole enforcement of the boundary.
  </behavior>
  <action>
    Read `14-DECISIONS.md` first. If D-18 declined, do nothing but record it.

    Otherwise add the rule to the `custom_rules` block in the repository lint configuration, following the surrounding rules' structure exactly. Scope it so the analytics client module is excluded, set error severity, and exclude the comment and doc-comment match kinds — Phase 11 recorded that an invalid match-kind spelling silently discards the entire rule configuration, so verify the kind names against a rule that already uses them rather than guessing.

    Verify the rule both ways, as described in the behavior block. A rule that never fires is indistinguishable from a rule that is misconfigured, and this repository has already hit that exact failure once.

    Do not add any suppression directive anywhere. If the rule fires on a legitimate site, the correct response is to narrow the rule's scope, not to silence it at the site.

    <!-- planner-discipline-allow: import TelemetryDeck -->
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - If D-18 approved: `.swiftlint.yml` contains a new `custom_rules` entry naming the analytics boundary, at `severity: error`, excluding the analytics client module path
    - If D-18 approved: the summary records both verification results — the rule firing on a temporary out-of-module import, and zero violations after reverting
    - If D-18 declined: `.swiftlint.yml` is unchanged and the summary states that the type wall is the sole enforcement
    - `grep -rl "import TelemetryDeck" AppPackage/Sources App ShareExtension | grep -v "AppPackage/Sources/AnalyticsClient"` produces no output
    - `grep -rc "swiftlint:disable" AppPackage/Sources/AnalyticsClient` returns 0
    - Build succeeds with zero warnings
  </acceptance_criteria>
  <done>The single-import boundary is either structurally enforced and proven to fire, or explicitly left to the type wall by owner decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product → user | the README is the only place a user can learn what is collected; its completeness is the disclosure control |
| contributor → codebase | the lint rule, if approved, is the structural control on where the SDK may be reached |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-17 | Repudiation | user disclosure | high | mitigate | D-03 makes the README the sole disclosure surface, so it must be complete and must match what shipped. The plan requires reconciling the prose against the shipped signal enum and default-parameter file rather than against the plan, and requires the never-collected list to be explicit and complete. |
| T-14-01 | Information Disclosure | future SDK misuse | high | mitigate | Pending D-18, a lint rule rejects the SDK import outside the client module at error severity, backed by a verification that the rule actually fires. The closed signal API remains the primary control either way. |
| T-14-18 | Repudiation | project records | low | mitigate | Stale consent wording in the roadmap would read as a dropped feature rather than a deliberate decision. The roadmap describes Phase 14 in two places and only one was stale at planning time, so the acceptance criterion is a zero-occurrence assertion over the whole file rather than a scoped-diff check. Corrected here, with a sweep recorded in the summary. |
</threat_model>

<verification>
- The README carries a complete Analytics section reconciled against the shipped code.
- No present-tense artifact describes the shipped analytics as opt-in.
- `ANALYTICS-01` is verified against the shipped signal surface, carries a phase mapping and consistent coverage counts, and its checkbox state matches reality.
- If approved, the lint rule is proven to fire and then to be clean; if declined, nothing changed.
- Build succeeds with zero warnings and the full default test plan is green.
</verification>

<success_criteria>
- The disclosure D-03 requires exists and is accurate.
- The phase's requirement traceability is honest: the ID resolved from wave 1 and its completion state now matches what shipped.
- The single-import boundary has an explicit, owner-decided enforcement posture.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-17-SUMMARY.md` when done.
</output>
