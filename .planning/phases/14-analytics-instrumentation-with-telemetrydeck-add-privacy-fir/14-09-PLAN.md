---
phase: 14-analytics-instrumentation
plan: 09
type: execute
wave: 6
depends_on: ["14-06", "14-08"]
files_modified:
  - AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
  - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift
  - AppPackage/Tests/AppFeatureTests/AnalyticsEmissionTests.swift
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "The analytics SDK is initialized exactly once per process, from the existing launch-finish reducer action, and never from a view lifecycle callback (D-14)"
    - "Switching to a different tab emits exactly one tab-open signal; re-tapping the tab already showing emits none"
    - "Opening a gallery detail through the app-root modal path emits the same gallery-detail signal as the four push paths, built from the same derivation"
    - "An error that reaches the user as a toast carrying diagnostics emits exactly one error signal, classified by AppError case with none of its String payloads"
    - "Drilling into the error-detail screen emits nothing, so one error is counted once"
  artifacts:
    - path: "AppPackage/Tests/AppFeatureTests/AnalyticsEmissionTests.swift"
      provides: "TestStore proof for every AppFeature emission site"
  key_links:
    - from: "AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift"
      via: "analyticsClient.start() merged into the existing onLaunchFinish effect list"
      pattern: "analyticsClient"
    - from: "AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift"
      via: "TabBarItemType mapped to AppTab on a genuine tab switch only"
      pattern: "AppTab"
---

<objective>
Instrument the app-root module: initialize the SDK at launch, emit tab-open signals from the tab-selection action, and emit the modal gallery-detail and user-visible-error signals from `PresentationFeature`.

Purpose: D-05 family 1's tab opens and the fifth gallery-detail entry path both live here, as does the centralized user-visible error surface that D-05 family 4 targets. This is also where the SDK's one-shot initialization is sequenced, alongside the launch work the app already does there.
Output: four emission sites plus their `TestStore` proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-CONTEXT.md
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- New file `AppPackage/Tests/AppFeatureTests/AnalyticsEmissionTests.swift`
- No new production symbols; three existing reducers gain an `@Dependency(\.analyticsClient)` property and emission effects

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize the SDK at launch and emit tab-open signals</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift, AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift (whole file — the `.onLaunchFinish` case already merges several one-shot client calls, including `libraryClient.initializeWebImage()`, each wrapped as `.run(operation: { _ in … })`; note that `@Dependency` properties are declared `private` at reducer scope, and that the `AppDelegate` entry point already guards this send behind the existing testing check)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (lines 188-246 — the tab-selection case. The branch where the selected type equals the current one is the re-tap behavior, which is a refresh, not a tab open; the `else` branch is the genuine switch and is the only correct emission point)
    - AppPackage/Sources/AppModels/Support/TabBarItemType.swift
    - AppPackage/Sources/AnalyticsClient/AnalyticsVocabulary.swift (the `AppTab` mirror and its initializer from `TabBarItemType`)
    - AppPackage/Sources/SettingFeature/Components/LaboratorySettingReducer.swift (lines 19-33 — the canonical fire-and-forget client call from a reducer case, merged alongside the case's existing effect)
    - .swiftlint.yml (repo root — `single_line_trailing_closure` at error is why the parenthesized `.run(operation:)` form is used, and `lifecycle_modifiers` at error is why no view callback is an option)
  </read_first>
  <behavior>
    - `AppDelegateReducer`'s launch-finish case gains one more merged effect that calls the analytics client's start closure. It sits alongside the existing one-shot client calls, in the same wrapped form.
    - Initialization happens exactly once per process: the launch-finish action is sent once, from the app-delegate entry point, already behind the existing testing guard. No view callback is involved, which both satisfies D-14 and stays clear of the error-severity lifecycle-modifier lint rule.
    - `AppReducer`'s tab-selection case emits `tabOpened` **only** in the branch taken when the selected tab differs from the current one. Re-tapping the tab already showing runs the refresh/pop-to-root branch and emits nothing — it is not a tab open, and counting it would inflate the metric with scroll-to-top gestures.
    - The launch-time tab is not emitted as a tab open. The SDK's own session-started signal already marks the launch, and the app's initial tab is available as a segment on every signal through D-11's default parameters if it is ever wanted.
  </behavior>
  <action>
    In `AppDelegateReducer`, declare `@Dependency(\.analyticsClient) private var analyticsClient` beside the existing dependency properties and append one more element to the launch-finish case's merged effect list that invokes the client's start closure, matching the surrounding elements' wrapped form exactly.

    In `AppReducer`, declare the same dependency property and, in the tab-selection case's genuine-switch branch, append an effect emitting `tabOpened` with the tab mapped through `AppTab`'s initializer. Append it to the branch's existing effect list rather than restructuring the case — the case already builds a list and merges it.

    Add a one-line comment at the emission site recording why the equal-tab branch is excluded, following the precedent in this file of explaining non-obvious reducer decisions inline.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `AppDelegateReducer.swift` contains `@Dependency(\.analyticsClient) private var analyticsClient` and a launch-finish effect invoking the client's start closure
    - `AppReducer.swift` contains `.tabOpened(` exactly once
    - `grep -c "onAppear\|\.task(" AppPackage/Sources/AppFeature/DataFlow/AppDelegateReducer.swift` returns 0 (D-14: no view-lifecycle emission)
    - Build succeeds with zero new warnings; SwiftLint reports zero violations for `AppPackage/Sources/AppFeature`
  </acceptance_criteria>
  <done>The SDK initializes once at launch, and a genuine tab switch is the only thing that emits a tab open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the modal gallery-detail signal and the user-visible-error signal from PresentationFeature</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (whole file — the modal gallery-detail case near line 136, the toast-setting case near line 150, the two error-toast assignments near lines 182 and 258, and the multi-effect append-and-merge form near lines 198-213 which is the shape to follow)
    - AppPackage/Sources/AppComponents/AppAlertState.swift (the `errorInfo` property — a toast carrying it is an error that reached the user; a caption-only toast carries no classifiable AppError)
    - AppPackage/Sources/AppModels/Support/ErrorInfo.swift (the `error` property feeding the kind mirror, and the Phase 9 separation of case from per-incident diagnostics)
    - AppPackage/Sources/AnalyticsClient/AnalyticsSignal.swift (the `galleryDetailOpened` and `errorSurfaced` cases)
    - AppPackage/Sources/AnalyticsClient/TagNamespaceCounts.swift, AppPackage/Sources/AnalyticsClient/AppErrorKind.swift
  </read_first>
  <behavior>
    - The modal gallery-detail case emits `galleryDetailOpened`, with the category taken from the presented gallery and the tag namespaces derived through `TagNamespaceCounts` from the gallery's tags. When the case was seeded from a local download rather than a fetched gallery, the payload is derived from that download's gallery projection so both branches produce the same shape.
    - The toast-setting case emits `errorSurfaced` **only** when the toast carries diagnostics, classifying it through `AppErrorKind` from the carried error. A caption-only error toast carries no classifiable `AppError` and emits nothing.
    - The error-detail presentation case emits **nothing**. It is the user drilling into a toast that has already been counted; emitting there would double-count every error the user is curious about and skew the distribution toward whichever errors have interesting details.
    - No signal carries a gallery id, token, title or URL — the whole payload is a closed category enum plus bucketed namespace counts.
  </behavior>
  <action>
    Declare `@Dependency(\.analyticsClient) private var analyticsClient` at reducer scope alongside the existing dependency properties.

    In the modal gallery-detail case, append an emission effect to the case's existing return, deriving the payload as described in the behavior block. Use the same derivation entry points the four push sites use in the sibling wave-6 plans — `TagNamespaceCounts`'s tag initializer and the gallery's category — so all five gallery-detail entry paths produce identical payload shapes. Do not build a bespoke derivation here.

    In the toast-setting case, append an emission effect guarded on the toast carrying diagnostics.

    Add a one-line comment at the error-detail presentation case recording that it deliberately emits nothing and why, so a later reader does not add it for symmetry.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `PresentationFeature.swift` contains `.galleryDetailOpened(` exactly once and `.errorSurfaced(` exactly once
    - `PresentationFeature.swift` contains `TagNamespaceCounts(` and `AppErrorKind(`
    - The error-detail presentation case contains no analytics call, and carries the explanatory comment
    - `grep -c "gid\|token\|galleryURL" ` over the lines added by this task returns 0 — no identifier or URL is in any emission argument
    - Build succeeds with zero new warnings; SwiftLint reports zero violations
  </acceptance_criteria>
  <done>The fifth gallery-detail entry path and the centralized error surface both emit, and the drill-down does not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestStore proofs for every AppFeature emission site</name>
  <files>AppPackage/Tests/AppFeatureTests/AnalyticsEmissionTests.swift</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests (the target's existing suites — store construction conventions, and the analytics no-op override plan 14-08 added)
    - AppPackage/Tests/SettingFeatureTests/AppActivityLogsReducerTests.swift (lines 18-40 — the `LockIsolated` spy idiom: take the no-op client, replace one closure with a collector)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift, AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (what Tasks 1 and 2 built)
    - AppPackage/Tests/AnalyticsClientTests/AnalyticsClientGateTests.swift (the spy shape proven in plan 14-06)
  </read_first>
  <behavior>
    Each test drives the reducer through a store whose analytics client is a `LockIsolated`-backed spy, and asserts the exact recorded signal sequence — not merely that something was recorded.
    - Selecting a tab different from the current one records exactly one tab-open signal for that tab.
    - Selecting the tab already current records **zero** signals. This negative assertion is the one that protects the metric from scroll-to-top inflation, and it is easy to lose in a later refactor, so it is asserted explicitly rather than inferred.
    - Presenting a modal gallery detail records exactly one gallery-detail signal whose category and namespace counts match a fixture gallery, and whose recorded value contains no fixture gid, token, title or URL — asserted with a distinctive sentinel in the fixture's title and tag text.
    - Setting a diagnostics-carrying error toast records exactly one error signal with the expected kind.
    - Setting a caption-only error toast records zero signals.
    - Presenting the error-detail screen records zero signals.
  </behavior>
  <action>
    Create `AppPackage/Tests/AppFeatureTests/AnalyticsEmissionTests.swift` implementing the behavior block as a Swift Testing `@Suite` with `@MainActor` on each store-driving member and never on the type — the repository's documented convention, because a main-actor type would make the suite's protocol conformances main-actor-isolated too.

    Build fixture galleries whose title and tag text are distinctive sentinel tokens, and assert those sentinels appear nowhere in the recorded signals' reflected value graph, reusing the reflection helper written in plan 14-03 rather than reimplementing it.

    Include the three zero-signal assertions from the behavior block. They are the tests that would catch an over-eager future contributor, and they cost nothing to keep.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `AnalyticsEmissionTests.swift` contains at least six `@Test` functions covering the six behaviors listed
    - The file contains at least three assertions of an empty recorded-signal collection
    - The file contains a sentinel-based reflection assertion over the recorded signals
    - `xcodebuild test ... -only-testing:AppFeatureTests` exits TEST SUCCEEDED
    - The full default test plan is green
  </acceptance_criteria>
  <done>Every AppFeature emission and non-emission is pinned by an exact-sequence TestStore assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer → AnalyticsClient | four emission sites cross this boundary in this module |
| user-visible error surface → analytics | classified error data crosses here; the per-incident diagnostics must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure | modal gallery-detail and error emissions | high | mitigate | Payloads are built only from `TagNamespaceCounts` and `AppErrorKind`, both of which are proven content-free in plan 14-03; the emission tests additionally assert fixture sentinels do not survive into the recorded signal. |
| T-14-13 | Repudiation | tab-open and error metrics | medium | mitigate | Double-counting corrupts the dataset in ways that look like real behavior. Guarded by emitting only on a genuine tab switch and only on the toast, with three explicit zero-signal tests pinning the exclusions. |
| T-14-11 | Denial of Service | app stability | medium | mitigate | Initialization runs once from the existing launch-finish action, behind the app delegate's existing testing guard, and the client's started-flag guard makes any earlier signal inert rather than fatal. |
</threat_model>

<verification>
- `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests` reports TEST SUCCEEDED.
- The full default test plan is green.
- No view-lifecycle callback was added anywhere in `AppPackage/Sources/AppFeature`.
- SwiftLint reports zero violations; no suppression directive was added.
</verification>

<success_criteria>
- The SDK initializes once per process from a reducer action.
- Tab opens, modal gallery details and user-visible errors each emit exactly once, and the three excluded cases emit nothing.
- Every emission and exclusion is proven by an exact-sequence `TestStore` assertion.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-09-SUMMARY.md` when done.
</output>
