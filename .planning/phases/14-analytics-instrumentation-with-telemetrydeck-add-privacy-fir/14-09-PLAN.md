---
phase: 14-analytics-instrumentation
plan: 09
type: execute
wave: 5
depends_on: ["14-06"]
files_modified:
  - AppPackage/Tests/AppFeatureTests
  - AppPackage/Tests/HomeFeatureTests
  - AppPackage/Tests/DetailFeatureTests
  - AppPackage/Tests/ReadingFeatureTests
autonomous: true
requirements: [ANALYTICS-01]
must_haves:
  truths:
    - "Every store in AppFeatureTests, HomeFeatureTests, DetailFeatureTests and ReadingFeatureTests resolves the analytics dependency to the no-op client before any reducer is instrumented"
    - "AppFeatureTests is hardened in full, because AppReducer scopes every feature reducer and so is exposed to every wave-6 plan's emissions, not only AppFeature's own"
    - "The hardened-store count reconciles against the store count in each target, so no site is left to fail in a later wave"
    - "The unimplemented default still applies to every target not touched here, so an unexpected analytics call outside the hardened targets still fails loudly"
  artifacts:
    - path: "AppPackage/Tests/AppFeatureTests"
      provides: "10 hardened stores across 3 files — the app-root target every wave-6 plan can reach"
    - path: "AppPackage/Tests/HomeFeatureTests"
      provides: "8 hardened stores across 2 files"
    - path: "AppPackage/Tests/DetailFeatureTests"
      provides: "5 hardened stores across 3 files"
    - path: "AppPackage/Tests/ReadingFeatureTests"
      provides: "1 hardened store"
  key_links:
    - from: "AppPackage/Tests/AppFeatureTests"
      to: "AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift"
      via: "every store in the app-root target — including scoped-child stores — resolves analytics to the no-op client"
      pattern: "analyticsClient = .noop"
---

<objective>
Harden the four remaining affected test targets — `AppFeatureTests`, `HomeFeatureTests`, `DetailFeatureTests` and `ReadingFeatureTests`, 24 store constructions across 9 files — against the analytics dependency, ahead of instrumentation.

Purpose: the same reason as plans 14-07 and 14-08, for the rest of the blast radius. `AppFeatureTests` is the load-bearing one: `AppReducer` scopes every feature reducer, so a signal emitted by *any* wave-6 plan can surface in this target. That is why every wave-6 plan depends on this plan and not only on the plan that hardened its own target. Running as a sibling of 14-07 and 14-08 in the same wave is safe because all three touch disjoint targets.
Output: every store in the four targets resolves analytics to the no-op client, each proven by a real emission from its own module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- No new symbols. This plan modifies existing test files in four targets only.

<tasks>

<task type="auto">
  <name>Task 1: Harden AppFeatureTests and HomeFeatureTests</name>
  <files>AppPackage/Tests/AppFeatureTests, AppPackage/Tests/HomeFeatureTests</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests (every file containing a store construction — note this target drives `AppReducer`, which composes every feature reducer, so it is the target most exposed to a sibling plan's instrumentation)
    - AppPackage/Tests/HomeFeatureTests/FiltersPresentationLifecycleTests.swift (lines 28-40 — the per-store inline override form)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift
    - .swiftlint.yml (repo root — `sorted_imports` at error)
  </read_first>
  <action>
    Enumerate the store constructions in both targets and record the per-file counts in the summary (research counted 10 across 3 files for `AppFeatureTests` and 8 across 2 for `HomeFeatureTests`; confirm rather than trust).

    For each suite: if it already has a store factory, add the analytics no-op override there once. Otherwise add the override to each store's existing trailing dependency-configuration closure, following the `FiltersPresentationLifecycleTests` inline shape.

    Pay particular attention to `AppFeatureTests`: because `AppReducer` scopes every feature reducer, a signal emitted by any wave-6 plan can surface here. Every store in this target must be hardened, including any store constructed for a scoped child reducer.

    Add the `AnalyticsClient` import to every touched file, keeping the import block sorted. Change no assertion, no state expectation, no effect expectation and no unrelated dependency override.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests -only-testing:HomeFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:AppFeatureTests -only-testing:HomeFeatureTests` exits TEST SUCCEEDED
    - Every file in both targets containing `TestStore(` is covered, directly or through a factory
    - The summary reconciles store counts against hardened-store counts for both targets
    - `git diff` shows no change to any `#expect`, `store.send`, `store.receive` or state-mutation closure
  </acceptance_criteria>
  <done>Both targets are hardened, including every scoped-child store in the app-root target.</done>
</task>

<task type="auto">
  <name>Task 2: Harden DetailFeatureTests and ReadingFeatureTests</name>
  <files>AppPackage/Tests/DetailFeatureTests, AppPackage/Tests/ReadingFeatureTests</files>
  <read_first>
    - AppPackage/Tests/DetailFeatureTests/CommentsReducerTests.swift (an existing per-store dependency override in this target — the shape to follow)
    - AppPackage/Tests/ReadingFeatureTests (the single store construction in this target)
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift
  </read_first>
  <action>
    Apply the same treatment to both targets (research counted 5 across 3 files and 1 respectively; confirm).

    These two targets are small enough that per-store inline overrides are the right shape; do not introduce a factory where one store exists.

    Add the `AnalyticsClient` import to every touched file, keeping the import block sorted. Change no assertion and no unrelated dependency override.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:DetailFeatureTests -only-testing:ReadingFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:DetailFeatureTests -only-testing:ReadingFeatureTests` exits TEST SUCCEEDED
    - Every file in both targets containing `TestStore(` is covered
    - The summary reconciles store counts against hardened-store counts for both targets
  </acceptance_criteria>
  <done>Both remaining targets are hardened, with assertions untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the pass with a real emission from each of the four modules</name>
  <files>AppPackage/Tests/AppFeatureTests, AppPackage/Tests/HomeFeatureTests, AppPackage/Tests/DetailFeatureTests, AppPackage/Tests/ReadingFeatureTests</files>
  <read_first>
    - AppPackage/Sources/AnalyticsClient/AnalyticsClient.swift (the `testValue = .unimplemented` default this experiment is exercising)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (a reducer case the app-root target exercises — the natural probe site for the scoped-child exposure)
  </read_first>
  <action>
    Prove the pass behaviorally rather than by grep alone. Temporarily add a throwaway analytics emission to one reducer case in each of `AppFeature`, `HomeFeature`, `DetailFeature` and `ReadingFeature` that the corresponding target exercises. Run the four targets, confirm they stay green, then revert every temporary edit and confirm `git status --porcelain AppPackage/Sources` is empty.

    A grep count proves the override text exists; only a real emission proves it takes effect through the store's dependency resolution.

    Site the `AppFeature` probe on a case that a *scoped child* store exercises as well as the root store, since the scoped-child stores are the ones most likely to have been missed. If any probe turns a target red, that is the plan's real finding: locate the unhardened store, harden it, and re-run before reverting.

    Record the four results in the summary, along with any store a probe exposed.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests -only-testing:HomeFeatureTests -only-testing:DetailFeatureTests -only-testing:ReadingFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - The four-target filtered run reports TEST SUCCEEDED
    - The summary records the temporary-emission experiment as PASS for all four modules, and lists any store a probe exposed
    - `git status --porcelain AppPackage/Sources` is empty at task end
    - `git diff --name-only` lists no file outside the four test target directories
  </acceptance_criteria>
  <done>All four targets are hardened and each has survived a real analytics emission from its own module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test host → AnalyticsClient | tests must never reach a live client; the unimplemented default plus explicit no-op overrides define this boundary |
| scoped child reducer → app-root test store | `AppReducer` composes every feature reducer, so any wave-6 emission crosses into `AppFeatureTests` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | owner's analytics dataset | medium | mitigate | Two independent controls: the absent-credential gate under the test host, and an explicit no-op override at every store. Neither is relied on alone. |
| T-14-12 | Denial of Service | test suite reliability | medium | mitigate | Hardening runs in its own wave ahead of all instrumentation, so no wave-6 plan can turn a sibling's target red. Proven per target by count reconciliation and by a temporary real emission from that module. |
| T-14-19 | Denial of Service | app-root test target | medium | mitigate | `AppFeatureTests` is exposed to every wave-6 plan through reducer scoping, so a miss here fails a sibling's wave rather than this plan's. Mitigated by hardening scoped-child stores explicitly and by siting the probe on a case a scoped-child store exercises. |
</threat_model>

<verification>
- The four-target filtered run reports TEST SUCCEEDED.
- Store counts and hardened-store counts reconcile in all four targets.
- The temporary-emission experiment passes for all four modules and leaves no residue in the working tree.
</verification>

<success_criteria>
- All 24 store constructions across the four targets resolve analytics to the no-op client.
- Every affected target survives a real emission from its own module.
- No assertion or unrelated dependency override was changed.
</success_criteria>

<output>
Create `.planning/phases/14-analytics-instrumentation-with-telemetrydeck-add-privacy-fir/14-09-SUMMARY.md` when done.
</output>
