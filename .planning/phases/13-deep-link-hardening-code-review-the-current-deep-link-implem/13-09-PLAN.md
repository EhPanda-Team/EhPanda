---
phase: 13-deep-link-hardening
plan: 09
type: execute
wave: 9
depends_on: ["13-08"]
files_modified:
  - EhPandaUITests/DeepLinkSchemeUITests.swift
  - EhPandaUITests/Support/DeepLinkLauncher.swift
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - "All three routes land on their locked destinations via ehpanda:// scheme opens in BOTH cold-launch and warm-foreground variants: /g/ → detail only; /s/ → detail then reader auto-presented at the linked page; #c → detail then Comments pushed scrolled to the linked comment (D-09, locked routing contract)"
    - "A malformed ehpanda:// open surfaces the persistent error toast, and tapping it opens ErrorInfoView — in both variants (D-01 proven end-to-end)"
    - "Deep links never skip the detail page and jump straight to the reader (locked contract, asserted on the /s/ tests)"
  artifacts:
    - path: "EhPandaUITests/DeepLinkSchemeUITests.swift"
      provides: "The 8-test scheme matrix (D-09 full-density leg)"
  key_links:
    - from: "EhPandaUITests/DeepLinkSchemeUITests.swift"
      to: "EhPandaUITests/Support/DeepLinkLauncher.swift"
      via: "every test launches/opens through the pinned 13-08 helpers"
      pattern: "openCold|openWarm"
---

<objective>
Write the full-density scheme matrix (D-09): the three routes plus the malformed-link error test, each in cold-launch and warm-foreground variants — 8 hermetic UI tests proving SC-2's core and SC-3's end-to-end failure surface.

Purpose: the locked routing contract, machine-verified through the real app on the primary iPhone matrix (D-08).
Output: 8 green tests in the UI plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-08-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- `DeepLinkSchemeUITests` (XCTest class, 8 tests): testGalleryLinkColdLaunchLandsOnDetail, testGalleryLinkWarmForegroundLandsOnDetail, testPageLinkColdLaunchOpensReaderAtPage, testPageLinkWarmForegroundOpensReaderAtPage, testCommentLinkColdLaunchScrollsToComment, testCommentLinkWarmForegroundScrollsToComment, testMalformedLinkColdLaunchShowsErrorToast, testMalformedLinkWarmForegroundShowsErrorToast

<tasks>

<task type="auto">
  <name>Task 1: Happy-route matrix — /g/, /s/, #c in cold and warm variants (6 tests)</name>
  <files>EhPandaUITests/DeepLinkSchemeUITests.swift, EhPandaUITests/Support/DeepLinkLauncher.swift</files>
  <read_first>
    - .swiftlint.yml (repo root — UI-test code is scanned; no force-unwraps, no optional-try, sorted imports)
    - EhPandaUITests/Support/DeepLinkLauncher.swift and UITestConstants.swift (pinned mechanisms + fixture constants from 13-08)
    - EhPandaUITests/DeepLinkSmokeUITests.swift (the working arrival-assertion shape to extend)
  </read_first>
  <action>
    Six tests using the ehpanda:// scheme URL builders from UITestConstants (fixture gid/token/page/commentID):
    - /g/ cold + warm: assert `detail_view` exists (marker title present) AND `reading_view` does NOT appear (detail only — locked contract).
    - /s/ cold + warm: assert `detail_view` appears first, then `reading_view` auto-presents, then `reading_page_indicator`'s label/value reflects the linked page number (the pre-seeded progress landing). Waits are event-driven (`waitForExistence`) — never sleeps.
    - #c cold + warm: assert `detail_view`, then `comments_view` is pushed, then `comment_cell_<fixture last comment ID>` exists and is hittable (visible — the scroll-to-comment proof; the last-comment fixture choice makes an unscrolled list fail this assertion).
    Warm variants: `launchStubbed` first, then `openWarm`; cold variants: `openCold` per the pinned mechanism. Add any shared arrival-assertion helpers to DeepLinkLauncher rather than duplicating waits. Images load through Kingfisher's own session and degrade to placeholders — assert structure only, never image content (Pitfall 4).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' -retry-tests-on-failure -test-iterations 3 -only-testing:EhPandaUITests/DeepLinkSchemeUITests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - All six tests exist with the exact names listed in the artifacts section and pass under the retry-flag invocation
    - The /s/ tests assert both the detail-first arrival and the reader page indicator value
    - The #c tests assert hittability of the linked comment's cell
    - No sleep-based waiting anywhere in the class
  </acceptance_criteria>
  <done>The three locked routes are machine-verified end-to-end in both lifecycle variants.</done>
</task>

<task type="auto">
  <name>Task 2: Malformed-link error-surface tests (2 tests) + full-plan gate</name>
  <files>EhPandaUITests/DeepLinkSchemeUITests.swift</files>
  <read_first>
    - EhPandaUITests/Support/UITestConstants.swift (add a malformed-link constant here: an ehpanda:// URL with an unrecognized host carrying a gallery-shaped path — the spoof class the parser rejects)
    - AppPackage/Sources/AppModels/Resources/Localizable.xcstrings (the en value of the unsupported-link description — asserted as toast text)
  </read_first>
  <action>
    Two tests (cold + warm): open the malformed ehpanda:// link; assert `toast_message` appears and its static text contains the en unsupported-link description; tap the toast; assert `error_info_view` appears (the D-01 toast → ErrorInfoView chain, end-to-end). Assert NO detail screen appeared. Then run the complete UI plan (all classes) as the wave gate.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' -retry-tests-on-failure -test-iterations 3 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Both malformed tests pass; the toast-tap opens the error surface in each
    - Full UI plan green under the retry-flag invocation (smoke + all 8 matrix tests)
  </acceptance_criteria>
  <done>SC-3's failure UX is proven through the real UI in both lifecycle variants; the whole plan is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System open → app | Scheme payloads delivered by the OS in both lifecycle states |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Denial of Service | malformed scheme opens | medium | mitigate | The malformed matrix proves no-crash + recoverable surface end-to-end |
| T-13-09 | Repudiation | flaky single-run evidence | medium | mitigate | Retry flags on every invocation; event-driven waits only |
</threat_model>

<verification>
- Full UI plan + default unit plan both green.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- D-09's full-density scheme leg complete: 8 tests, all green, hermetic, on the D-08 iPhone matrix.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-09-SUMMARY.md` when done.
</output>
