---
phase: 13-deep-link-hardening
plan: 06
type: execute
wave: 6
depends_on: ["13-05"]
files_modified:
  - AppPackage/Sources/SystemNotification/View+Toast.swift
  - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift
  - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift
  - AppPackage/Tests/DetailFeatureTests/CommentsReducerTests.swift
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "The loading-toast → error-toast hand-off is a direct state replacement animated by the toast overlay — no fixed 500ms gap remains in PresentationFeature or CommentsReducer (D-14)"
    - "Toast replacement animates as a transition keyed on the presented toast's identity; the auto-dismiss timer still restarts per toast id (existing cancellation-safe behavior preserved)"
    - "No timing-based sequencing remains anywhere in the deep-link routing path (D-14 exit criterion, combined with 13-05)"
  artifacts:
    - path: "AppPackage/Sources/SystemNotification/View+Toast.swift"
      provides: "Identity-keyed replacement animation in the toast overlay"
  key_links:
    - from: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      to: "AppPackage/Sources/SystemNotification/View+Toast.swift"
      via: "reducer sets .error state directly; the overlay's identity-keyed animation renders the cross-transition"
      pattern: "\\.error"
---

<objective>
Root-fix the two 500ms loading→error toast sleeps (D-14, fragile spot F7) with research Pattern 4 option 1: key the toast overlay's animation on the presented toast's identity so replacement cross-transitions, then have both reducers set the error toast directly with zero timing.

Purpose: SC-1 — the last timing-based sequencing in the routing path dies; combined with 13-05, D-14 is complete.
Output: identity-keyed overlay animation + sleep-free reducers + updated reducer tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

- No new public symbols. Behavior change: `View+Toast.swift` overlay animation keyed on presented-toast identity; both `fetchGalleryDone` failure arms set `.error` state directly.

<tasks>

<task type="auto">
  <name>Task 1: Identity-keyed toast replacement + direct error sets in both reducers</name>
  <files>AppPackage/Sources/SystemNotification/View+Toast.swift, AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift, AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift</files>
  <read_first>
    - .swiftlint.yml (repo root)
    - AppPackage/Sources/SystemNotification/View+Toast.swift (the overlay: content rendered under `.id(id)` where `id = store.state.id`, `.transition(toastTransition)`, and the trailing `.animation(toastAnimation, value: item != nil)` that only animates presence; the `.task(id:)` auto-dismiss timer is already replacement-cancellation-safe — do not touch it)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (`fetchGalleryDone` failure arm: clears toast, sleeps 500ms, then sends setToast .error with the galleryFailure ErrorInfo)
    - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift (`fetchGalleryDone` failure arm ~276-287: same shape with a caption-only auto-hide `.error()`; the unrelated post-comment sequencing sleeps at ~119/145-153 are OUT of D-14's routing-path scope — leave them)
  </read_first>
  <action>
    View change: rekey the overlay's trailing animation from presence (`item != nil`) to the presented toast's identity — the same state UUID already used for `.id(id)` and the dismiss timer (i.e. an optional identity value that is nil when no toast is presented). Presence transitions (nil→toast, toast→nil) keep animating exactly as today; replacement (loading id → error id) now animates as a cross-transition instead of an instant swap. Keep the existing note explaining why the animation is scoped inside the overlay.

    Reducer changes (both files, same shape): in the `fetchGalleryDone` failure arm, replace the clear + 500ms-delay + setToast effect with a direct synchronous state set of the error toast (`state.toast = .error(errorInfo)` in PresentationFeature with its existing galleryFailure ErrorInfo; `state.toast = .error()` in CommentsReducer), returning `.none` from that arm. The success arms keep clearing the toast as today. Delete the now-obsolete "let the loading toast animate out" comments — the overlay owns the visual hand-off now.
  </action>
  <verify>
    <automated>! grep -q "milliseconds(500)" AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift && ! grep -q "milliseconds(500)" AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift && xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - View+Toast.swift's trailing animation value is the presented toast's identity, not a Bool presence check
    - Neither reducer's fetchGalleryDone failure arm contains a delay effect; both set the error toast synchronously
    - PresentationFeature.swift contains zero sleep-based effects in the deep-link path (handleDeepLink + fetchGallery + fetchGalleryDone chain — the D-14 exit criterion with 13-05)
    - CommentsReducer's post-comment sleeps outside the routing path are untouched
    - Build succeeds
  </acceptance_criteria>
  <done>Loading→error is a single state replacement the overlay animates; reducers carry no toast timing.</done>
</task>

<task type="auto">
  <name>Task 2: Update reducer tests for the direct-set failure arms</name>
  <files>AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift, AppPackage/Tests/DetailFeatureTests/CommentsReducerTests.swift</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift (existing galleryFailure toast tests — they currently expect the delayed setToast action)
    - AppPackage/Tests/DetailFeatureTests/ (locate the Comments reducer suite; file name may differ — grep for fetchGalleryDone in DetailFeatureTests and update the actual file, adjusting this plan's file list in the summary if the suite lives elsewhere)
  </read_first>
  <action>
    Update every test that awaited the delayed setToast error action: the failure arm now mutates `state.toast` to `.error` in the same reduction that receives `fetchGalleryDone`, with no follow-up action. Assert the direct state change (including the ErrorInfo payload in the PresentationFeature sanitized-context cases — those fixtures must keep passing unchanged). Remove any test-clock advancing that existed solely to flush the 500ms effect. Add one case per reducer asserting the failure arm emits no further actions (exhaustive store).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests -only-testing:DetailFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - No reducer test advances a clock to observe the error toast
    - Both suites green; the 09-12 sanitized-context fixtures still pass
  </acceptance_criteria>
  <done>Tests pin the sleep-free failure arms exhaustively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reducer state → toast overlay | Error presentation timing moves fully into view-layer animation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Denial of Service (UI race) | toast replacement | low | mitigate | Identity-keyed animation is deterministic; the auto-dismiss `.task(id:)` timer already restarts per id (verified in source) |
</threat_model>

<verification>
- Full default-plan unit run green.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- D-14 fully closed: zero timing-based sequencing in the deep-link routing path (13-05 + this plan).
- Toast replacement animates via identity keying; reducers set error state directly.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-06-SUMMARY.md` when done.
</output>
