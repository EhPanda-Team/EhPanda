---
phase: 13-deep-link-hardening
plan: 08
type: execute
wave: 8
depends_on: ["13-07"]
files_modified:
  - EhPanda.xcodeproj/project.pbxproj
  - EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme
  - UITests.xctestplan
  - EhPandaUITests/.swiftlint.yml
  - EhPandaUITests/Support/UITestConstants.swift
  - EhPandaUITests/Support/DeepLinkLauncher.swift
  - EhPandaUITests/Fixtures/GalleryDetail.html
  - EhPandaUITests/Fixtures/GalleryDetailAlt.html
  - EhPandaUITests/Fixtures/GallerySinglePage.html
  - EhPandaUITests/Fixtures/FrontPageList.html
  - EhPandaUITests/DeepLinkSmokeUITests.swift
  - AppPackage/Sources/DetailFeature/DetailView.swift
  - AppPackage/Sources/ReadingFeature/ReadingView.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsView.swift
  - AppPackage/Sources/SystemNotification/ToastMessageView.swift
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "An XCUITest bundle target exists in EhPanda.xcodeproj, runs via a second non-default test plan on the existing scheme, and everyday xcodebuild test stays unit-only (D-05/D-07)"
    - "A smoke test cold-opens an ehpanda:// gallery link hermetically and lands on a detail screen showing the fixture marker title — proving URL delivery, env delivery, stub interception, and routing in one pass"
    - "The cold-launch delivery mechanism is probed and pinned (XCUIApplication.open when it delivers URL+env on this toolchain; the launch-automation env entry otherwise — research Pattern 7)"
    - "Destination screens carry stable accessibility identifiers so UI assertions never query localized labels"
  artifacts:
    - path: "UITests.xctestplan"
      provides: "Second test plan referencing the UI target"
      contains: "EhPandaUITests"
    - path: "EhPandaUITests/DeepLinkSmokeUITests.swift"
      provides: "Delivery probe + hermetic smoke test"
    - path: "EhPandaUITests/Support/DeepLinkLauncher.swift"
      provides: "Launch/open helpers encapsulating the pinned cold/warm mechanisms + stub env wiring"
    - path: "EhPandaUITests/.swiftlint.yml"
      provides: "Lint coverage for the UI target"
      contains: "parent_config"
  key_links:
    - from: "EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme"
      to: "UITests.xctestplan"
      via: "second TestPlanReference without default=YES (FeatureTests stays default)"
      pattern: "UITests.xctestplan"
    - from: "EhPandaUITests/Support/DeepLinkLauncher.swift"
      to: "AppPackage/Sources/AppFeature/UITestSupport/UITestAutomation.swift"
      via: "launchEnvironment sets the 13-07 env keys; fixture dir resolved from the UI test bundle's resources"
      pattern: "EHPANDA_UITEST"
---

<objective>
Stand up the project's first UI-automation infrastructure (D-05/D-07): the `EhPandaUITests` xcodeproj target, `UITests.xctestplan` as a second non-default plan, fixture resources, accessibility-identifier assertion hooks, and the Wave-0 cold-delivery probe (research Open Question 1) capped by one hermetic smoke test.

Purpose: SC-2's harness — 13-09/13-10 write the matrix against exactly this infrastructure.
Output: buildable, runnable UI plan with a green smoke test and a pinned cold-launch mechanism.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-RESEARCH.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-07-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- xcodeproj target `EhPandaUITests` (productType com.apple.product-type.bundle.ui-testing)
- `UITests.xctestplan` (repo root)
- `EhPandaUITests/` folder: `.swiftlint.yml`, `Support/UITestConstants.swift` (fixture gid/token/page/commentID + deep-link URL builders + env key names mirrored by convention), `Support/DeepLinkLauncher.swift` (stubbed launch/openWarm/openCold helpers), `Fixtures/` (4 HTML files), `DeepLinkSmokeUITests.swift`
- Accessibility identifiers (new, repo currently has zero): `detail_view`, `reading_view`, `reading_page_indicator`, `comments_view`, `comment_cell_<id>` per row, `toast_message`, `error_info_view`

<tasks>

<task type="auto">
  <name>Task 1: Accessibility-identifier assertion hooks on destination screens</name>
  <files>AppPackage/Sources/DetailFeature/DetailView.swift, AppPackage/Sources/ReadingFeature/ReadingView.swift, AppPackage/Sources/DetailFeature/Comments/CommentsView.swift, AppPackage/Sources/SystemNotification/ToastMessageView.swift</files>
  <read_first>
    - .swiftlint.yml (repo root)
    - Each target view file (locate the stable root container per screen; file paths above are best-known — if a view lives elsewhere, grep for the type name and record the actual path in the summary)
    - AppPackage/Sources/AppComponents/ (grep for the ErrorInfoView implementation file — Phase 9 artifact; add its identifier where it lives)
  </read_first>
  <action>
    Add `.accessibilityIdentifier(...)` hooks (identifier-only — never alter labels/traits; identifiers are invisible to VoiceOver users):
    - DetailView root scroll container → "detail_view"
    - ReadingView root container → "reading_view"; the reader's visible page-index indicator element → "reading_page_indicator" (13-09 asserts the /s/ landing page through it)
    - CommentsView list → "comments_view"; each comment row → "comment_cell_" + the comment's ID (13-09 asserts scroll-to-comment through it)
    - ToastMessageView container → "toast_message"
    - ErrorInfoView root Form → "error_info_view"
    Pure metadata additions: no layout, behavior, or appearance change (behavior-parity phase).
  </action>
  <verify>
    <automated>grep -rq "detail_view" AppPackage/Sources/DetailFeature && grep -rq "reading_page_indicator" AppPackage/Sources/ReadingFeature && grep -rq "comment_cell_" AppPackage/Sources/DetailFeature && xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - All seven identifier strings exist in Sources, each attached to its named element
    - Full default-plan unit suite still green (no behavior change)
  </acceptance_criteria>
  <done>UI assertions have locale-independent hooks on every destination surface.</done>
</task>

<task type="auto">
  <name>Task 2: UI-test target, test plan, scheme wiring, lint config, fixtures</name>
  <files>EhPanda.xcodeproj/project.pbxproj, EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme, UITests.xctestplan, EhPandaUITests/.swiftlint.yml, EhPandaUITests/Fixtures/GalleryDetail.html, EhPandaUITests/Fixtures/GalleryDetailAlt.html, EhPandaUITests/Fixtures/GallerySinglePage.html, EhPandaUITests/Fixtures/FrontPageList.html, EhPandaUITests/Support/UITestConstants.swift</files>
  <read_first>
    - EhPanda.xcodeproj/project.pbxproj (objectVersion 100, PBXFileSystemSynchronizedRootGroup folders, the EhPanda PBXNativeTarget object ID and TargetAttributes block — the anatomy to extend)
    - EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme (the TestPlans block — FeatureTests reference keeps default="YES")
    - AppPackage/Tests/FeatureTests.xctestplan (plan JSON shape to adapt)
    - AppPackage/Sources/TestingSupport/Resources/Parser/Gallery/GalleryDetail.html and GalleryNormalImageURL.html and one FrontPage list fixture under Resources/Parser/List/ (fixture sources)
    - 13-RESEARCH.md §Pattern 6 (target mechanics) and §Pitfall 5 (fixture-internal URL consistency)
    - 13-07-SUMMARY.md (route table filenames the app-side stub expects)
  </read_first>
  <action>
    pbxproj (hand-edit; research A4 marks GUI creation as fallback): add a PBXNativeTarget "EhPandaUITests" with productType com.apple.product-type.bundle.ui-testing, its own build configurations (Debug/Release) with TEST_TARGET_NAME = EhPanda and standard UI-test settings (bundle id suffix, iOS deployment target matching the app), a PBXFileSystemSynchronizedRootGroup for the EhPandaUITests/ folder, a target dependency on the EhPanda app target (PBXContainerItemProxy + PBXTargetDependency), and a TargetAttributes entry carrying TestTargetID = the EhPanda target's object ID. Validate with `xcodebuild -list` before proceeding; if the hand-edit proves malformed beyond one repair round, recreate via the documented structure rather than iterating blindly, and record what differed.

    Fixtures (Pitfall 5 — one shared constant set): copy GalleryDetail.html from TestingSupport, patching its gallery title elements to the marker "EhPanda UITest Fixture" (env-delivery proof: the real site would never serve this title). Create GalleryDetailAlt.html as a second copy patched to marker "EhPanda UITest Fixture Alt" (serves gid 2930572 — the gallery URL that exists inside the fixture's own comments, used by 13-10). Copy GalleryNormalImageURL.html as GallerySinglePage.html, patching its parsed gallery URL to point at the primary fixture gallery path (/g/ + fixture gid + token) so the /s/ reverse-parse resolves to a served URL. Copy one FrontPage list fixture as FrontPageList.html. Derive `UITestConstants.swift` from the actual fixture content: gid, token, a page index within the fixture page count (e.g. 2), the LAST comment ID present in the fixture (so scroll-to-comment is observable), the Alt gallery gid 2930572, deep-link URL builders (https and ehpanda schemes), and the 13-07 env key names (mirrored by convention — the two targets share no code).

    `UITests.xctestplan` (repo root): FeatureTests.xctestplan shape with containerPath "container:EhPanda.xcodeproj", the new target's identifier/name, plus testRepetitionMode retryOnFailure / maximumTestRepetitions 3 for GUI runs (CLI still needs the retry flags — Xcode 26 regression, Pitfall 2). Scheme: add a second TestPlanReference for it WITHOUT default="YES". `EhPandaUITests/.swiftlint.yml`: exactly `parent_config: ../.swiftlint.yml` (repo-root-level directory depth).
  </action>
  <verify>
    <automated>xcodebuild -list 2>/dev/null | grep -q "EhPandaUITests" && plutil -lint UITests.xctestplan && grep -q 'UITests.xctestplan' EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme && ! grep -A1 'UITests.xctestplan' EhPanda.xcodeproj/xcshareddata/xcschemes/EhPanda.xcscheme | grep -q 'default = "YES"' && xcodebuild build-for-testing -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -list` shows the EhPandaUITests target; build-for-testing with -testPlan UITests succeeds
    - FeatureTests.xctestplan remains the default plan (its reference alone carries default="YES")
    - All four fixture files exist; GalleryDetail.html contains the marker title; GallerySinglePage.html's embedded gallery URL matches the primary fixture path
    - EhPandaUITests/.swiftlint.yml contains `parent_config: ../.swiftlint.yml`
  </acceptance_criteria>
  <done>The harness builds; the default unit run is untouched; fixtures are internally consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Cold-delivery probe + hermetic smoke test</name>
  <files>EhPandaUITests/Support/DeepLinkLauncher.swift, EhPandaUITests/DeepLinkSmokeUITests.swift</files>
  <read_first>
    - 13-RESEARCH.md §Pattern 7 (the delivery matrix: warm = launch + XCUIDevice.shared.system.open; cold candidate = XCUIApplication.open(_:); fallback B = launch with EHPANDA_AUTOMATION_GALLERY_URL) and §Pitfall 1
    - AppPackage/Sources/AppLaunchAutomationClient/AppLaunchAutomation.swift (the fallback-B env key contract)
    - EhPandaUITests/Support/UITestConstants.swift (Task 2 constants)
  </read_first>
  <action>
    `DeepLinkLauncher.swift` (XCTest — UI automation is XCTest-only; lint applies: no force-unwrap, use XCTUnwrap): helpers on XCUIApplication — `launchStubbed(extraEnvironment:)` setting the stub-network flag + fixture-dir path (resolved from the UI test bundle's resource URL) before launch; `openWarm(_ url:)` via XCUIDevice.shared.system.open; `openCold(_ url:)` encapsulating the pinned cold mechanism behind one switch point.

    Probe (research Open Question 1, run once during this task's implementation): attempt cold delivery via XCUIApplication.open(_:) with launchEnvironment set — assert the app reaches runningForeground AND a detail screen shows the fixture marker title (marker title = env+stub delivered; detail arrival = URL delivered). If it fails either leg, pin `openCold` to fallback B (launchStubbed with EHPANDA_AUTOMATION_GALLERY_URL carrying the link — env applied, launch-time routing exercised; warm variants still prove real onOpenURL scheme delivery). Record the probe outcome and chosen mechanism in the summary AND as a doc comment on `openCold`.

    `DeepLinkSmokeUITests.swift`: one test — cold-open the ehpanda:// /g/ fixture link hermetically, assert `detail_view` exists within a generous timeout and the marker title text is present. Include `XCTAssertTrue(app.wait(for: .runningForeground, timeout:))` before element queries. Run the UI plan with the mandatory CLI retry flags (test-plan retry settings are ignored on Xcode 26 — Pitfall 2).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' -retry-tests-on-failure -test-iterations 3 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - The smoke test passes hermetically (marker title asserted — impossible from live network)
    - `openCold`'s doc comment records the probe outcome and pinned mechanism
    - Default unit invocation (`xcodebuild test -scheme EhPanda -destination ...` without -testPlan) still runs zero UI tests
  </acceptance_criteria>
  <done>Cold delivery is pinned by evidence; the harness proves a full hermetic deep-link round trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI test runner → app process | Env keys + fixture paths cross via launchEnvironment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Elevation of Surface | UI target / fixtures in release | high | mitigate | Fixtures live in the UI-test bundle only; the app-side seam stayed DEBUG-gated (13-07); test target never links into the app product |
| T-13-09 | Repudiation | flaky evidence from single-run UI tests | medium | mitigate | CLI retry flags mandatory on every scripted invocation; identifiers replace localized-label queries |
</threat_model>

<verification>
- UI plan green with retry flags; default unit plan unchanged and green.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- D-05/D-07 landed: first UI-automation target, second non-default plan, unit runs untouched.
- Cold delivery mechanism pinned by probe; hermetic smoke green.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-08-SUMMARY.md` when done. Record: probe outcome + pinned cold mechanism, the UI target's PBX identifier, and the final fixture constant set.
</output>
