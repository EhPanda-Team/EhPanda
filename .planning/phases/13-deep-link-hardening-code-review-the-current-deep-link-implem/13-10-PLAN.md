---
phase: 13-deep-link-hardening
plan: 10
type: execute
wave: 10
depends_on: ["13-09"]
files_modified:
  - EhPandaUITests/DeepLinkEntryUITests.swift
  - EhPandaUITests/ShareSheetUITests.swift
  - EhPandaUITests/DeepLinkPadUITests.swift
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "The clipboard entry has one representative happy-path test: a stubbed clipboard gallery URL on cold launch lands on detail (D-09), with the Allow Paste prompt structurally sidestepped (Pitfall 3)"
    - "The comments-view entry has one representative test: tapping a gallery link inside a comment pushes the linked gallery's detail (D-09)"
    - "The ShareExtension entry is tested true E2E through the real share sheet: Safari → share → EhPanda extension → app lands on detail (D-10, owner-accepted flake cost with retry budget)"
    - "The iPad-exclusive presentGalleryDetail tab-modal entry is covered, including a deep link replacing the open tab-modal through the 13-05 dismissal coordination (D-08)"
  artifacts:
    - path: "EhPandaUITests/DeepLinkEntryUITests.swift"
      provides: "Clipboard + comments-entry representatives"
    - path: "EhPandaUITests/ShareSheetUITests.swift"
      provides: "D-10 real share-sheet E2E"
    - path: "EhPandaUITests/DeepLinkPadUITests.swift"
      provides: "D-08 iPad tab-modal + replace-while-open coverage"
  key_links:
    - from: "EhPandaUITests/ShareSheetUITests.swift"
      to: "ShareExtension/ShareViewController.swift"
      via: "the real share sheet invokes the extension's (13-04 hardened) scheme rewrite; the app-side arrival is asserted on detail_view"
      pattern: "detail_view"
---

<objective>
Complete the D-09 matrix with the three representative entry tests (clipboard, comments-view, share-sheet true-E2E per D-10) plus the D-08 iPad tab-modal test, then run the phase's full verification gate (unit plan + complete UI plan).

Purpose: every deep-link entry's arrival leg is proven; all entries converge on the shared consumption site, so per-entry coverage needs exactly this (D-09 rationale).
Output: 4 green tests + the phase-complete test evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-RESEARCH.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-08-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- `DeepLinkEntryUITests` (2 tests): testClipboardGalleryLinkColdLaunchLandsOnDetail, testCommentLinkTapPushesLinkedGallery
- `ShareSheetUITests` (1 test): testShareSheetHandoffLandsOnDetail
- `DeepLinkPadUITests` (1 test): testPadTabModalReplacedByDeepLink

<tasks>

<task type="auto">
  <name>Task 1: Clipboard + comments-entry representatives</name>
  <files>EhPandaUITests/DeepLinkEntryUITests.swift</files>
  <read_first>
    - EhPandaUITests/Support/DeepLinkLauncher.swift and UITestConstants.swift (13-08 helpers/constants, incl. the clipboard env key and the Alt gallery gid)
    - EhPandaUITests/DeepLinkSchemeUITests.swift (arrival-assertion shapes)
    - 13-RESEARCH.md §Pitfall 3 (why the clipboard content read is stubbed) and §Pitfall 8 (state bleed between tests)
  </read_first>
  <action>
    - Clipboard test: `launchStubbed` with the clipboard env key set to the fixture /g/ https URL (cold launch — the cold-launch clipboard detection leg). Assert detail_view + marker title. The stub's per-launch-distinct change count (13-07) guarantees detection despite persisted change counts from earlier tests.
    - Comments-entry test: cold-open the #c fixture link to land in comments (already-proven leg), then tap the gallery link inside the fixture comment (the link text targets the Alt gallery gid — locate the tappable link element within the comment cell, probing link/text element kinds as Comments renders them). Assert a NEW detail screen appears showing the Alt marker title ("EhPanda UITest Fixture Alt" — distinguishable from the origin gallery, proving the push actually navigated).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' -retry-tests-on-failure -test-iterations 3 -only-testing:EhPandaUITests/DeepLinkEntryUITests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Both tests pass under the retry invocation
    - No test writes the real UIPasteboard (Allow Paste never triggers)
    - The comments-entry assertion distinguishes the pushed gallery from the origin via the Alt marker title
  </acceptance_criteria>
  <done>Clipboard and comments arrival legs are proven with real routing.</done>
</task>

<task type="auto">
  <name>Task 2: Share-sheet true E2E (D-10)</name>
  <files>EhPandaUITests/ShareSheetUITests.swift</files>
  <read_first>
    - 13-RESEARCH.md §Pattern 8 (Safari drive skeleton; chrome identifiers are probe-and-pin values on iOS 26.5, not constants) and §Pitfall 2 (retry flags)
    - EhPandaUITests/Support/DeepLinkLauncher.swift (launchStubbed — the app must be pre-launched with the stub env so the hand-off foregrounds the stubbed instance)
  </read_first>
  <action>
    One test: `launchStubbed` the app first (research: the share hand-off must foreground the already-stubbed instance, not cold-launch an unstubbed one). Drive Safari (XCUIApplication bundleIdentifier com.apple.mobilesafari): navigate to the fixture https gallery URL (type in the address bar + return; do NOT wait for page load — sharing needs only the URL, and the Safari leg inherently touches the network); open the share sheet; find and tap the EhPanda cell in the activity view (scroll the activity view if needed); assert the app reaches runningForeground and detail_view + marker title appear. Probe-and-pin Safari's chrome identifiers on the current simulator during implementation — treat every identifier as version-drifting. Budget generous event-driven timeouts (10-15s) on every cross-app wait; this is the flakiest test in the plan by design (owner accepted the cost, D-10) and rides the CLI retry flags.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' -retry-tests-on-failure -test-iterations 3 -only-testing:EhPandaUITests/ShareSheetUITests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - The test passes within the 3-iteration retry budget
    - The app-side assertion is hermetic (marker title — the arrival was served by the stub)
    - Waits are event-driven with explicit timeouts; no fixed sleeps
  </acceptance_criteria>
  <done>The ShareExtension leg is proven through the real share sheet, exactly as the owner chose.</done>
</task>

<task type="auto">
  <name>Task 3: iPad tab-modal coverage + phase test gate</name>
  <files>EhPandaUITests/DeepLinkPadUITests.swift</files>
  <read_first>
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (presentGalleryDetail — the iPad tab-modal entry sharing the 13-05-coordinated sheet)
    - EhPandaUITests/Support/DeepLinkLauncher.swift and UITestConstants.swift
  </read_first>
  <action>
    One test in a dedicated class guarded by XCTSkipUnless on the pad idiom (so the shared plan stays runnable on iPhone): `launchStubbed` on iPad; from the Home list (served by the FrontPageList fixture), tap a gallery cell — asserting the tab-modal detail presents (the presentGalleryDetail entry, D-08's iPad-exclusive path); then `openWarm` the Alt-gallery ehpanda:// link while the modal is up; assert the modal is replaced — detail now shows the Alt marker title. This exercises the 13-05 dismissal-completion coordination end-to-end on the device class that shares the sheet.

    Phase test gate (final): run (sequentially — xcodebuild invocations must never overlap on this machine) (1) the default unit plan, (2) the full UI plan on iPhone, (3) this class on the iPad destination. All three green is the phase's automated exit evidence for /gsd-verify-work.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "TEST SUCCEEDED" && xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPhone Air' -retry-tests-on-failure -test-iterations 3 2>&1 | tail -5 | grep -q "TEST SUCCEEDED" && xcodebuild test -scheme EhPanda -testPlan UITests -destination 'platform=iOS Simulator,name=iPad Pro 11-inch (M5)' -retry-tests-on-failure -test-iterations 3 -only-testing:EhPandaUITests/DeepLinkPadUITests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - The iPad test passes on the iPad destination and skips (not fails) on iPhone
    - The replace-while-open assertion distinguishes old vs new modal via the Alt marker title
    - All three gate invocations green
  </acceptance_criteria>
  <done>D-08's targeted iPad coverage lands; the full phase test evidence is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safari/share sheet → ShareExtension → app | Real cross-process hand-off exercised end-to-end |
| Clipboard stub → routing | Stubbed content read; real routing decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | share-sheet hand-off integrity | medium | mitigate | True-E2E test proves the hardened rewrite delivers the exact gallery route through the real extension |
| T-13-09 | Repudiation | share-sheet flake masking regressions | medium | mitigate | Retry budget + hermetic app-side assertions (marker titles) separate flake from failure |
</threat_model>

<verification>
- All three gate invocations green (unit plan, full UI plan on iPhone, iPad class on iPad).
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- The complete 13-test matrix (1 smoke + 8 scheme + 2 entry + 1 share + 1 iPad) is green.
- Every deep-link entry's arrival leg is machine-verified; SC-2 complete.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-10-SUMMARY.md` when done. Record the final test inventory and any probe-and-pin Safari identifiers for future maintenance.
</output>
