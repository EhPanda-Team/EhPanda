---
phase: 13-deep-link-hardening
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - AppPackage/Sources/AppModels/Support/AppError.swift
  - AppPackage/Sources/AppModels/Support/AppError+Context.swift
  - AppPackage/Sources/AppModels/Resources/Localizable.xcstrings
  - AppPackage/Tests/AppModelsTests/UnsupportedDeepLinkErrorTests.swift
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "AppError.unsupportedDeepLink exists with its own localized description and a recovery suggestion naming what links EhPanda can open (D-03)"
    - "All three new localization keys carry entries for all six locales (en, de, ja, ko, zh-Hans, zh-Hant)"
    - "The unsupported-link ErrorInfo context carries a sanitized link rendering — never the raw URL's query, fragment, userinfo, or full token-bearing path (Phase 9 09-12 invariant reconciled with D-01)"
  artifacts:
    - path: "AppPackage/Sources/AppModels/Support/AppError.swift"
      provides: "The D-03 dedicated failure case"
      contains: "unsupportedDeepLink"
    - path: "AppPackage/Sources/AppModels/Resources/Localizable.xcstrings"
      provides: "3 new keys x 6 locales"
      contains: "app_error.unsupported_deep_link"
    - path: "AppPackage/Tests/AppModelsTests/UnsupportedDeepLinkErrorTests.swift"
      provides: "Case wiring + context sanitization regressions"
  key_links:
    - from: "AppPackage/Sources/AppModels/Support/AppError.swift"
      to: "AppPackage/Sources/AppModels/Resources/Localizable.xcstrings"
      via: "String(localized:) symbols generated from the three new keys"
      pattern: "appErrorUnsupportedDeepLink"
---

<objective>
Add the dedicated `AppError.unsupportedDeepLink` case (D-03) with localization in all six catalog locales, plus a sanitized ErrorInfo context builder so the D-01 toast → ErrorInfoView surface can show "the URL and reason" without violating the Phase 9 09-12 rule that access-bearing route components never enter persisted `Context`.

Purpose: SC-3's user-facing failure vocabulary — the Phase 12 D-10 precedent (`cloudflareChallengeFailed`) applied to a genuinely new, user-actionable failure kind.
Output: error case + l10n + context builder + unit regressions, ready for plan 13-04's policy wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

- `AppError.unsupportedDeepLink` case (+ `isRetryable`/`localizedDescription`/`alertText`/`solution` arms)
- Localization keys `app_error.unsupported_deep_link`, `app_error.unsupported_deep_link.description`, `app_error.unsupported_deep_link.solution` (symbols `appErrorUnsupportedDeepLink`, `appErrorUnsupportedDeepLinkDescription`, `appErrorUnsupportedDeepLinkSolution`)
- `Context` builder for unsupported links (e.g. `Context.unsupportedLink(url:)`) with a sanitized link `ContextKey` row
- New test file `AppPackage/Tests/AppModelsTests/UnsupportedDeepLinkErrorTests.swift`

<tasks>

<task type="auto">
  <name>Task 1: AppError.unsupportedDeepLink case + six-locale localization</name>
  <files>AppPackage/Sources/AppModels/Support/AppError.swift, AppPackage/Sources/AppModels/Resources/Localizable.xcstrings</files>
  <read_first>
    - .swiftlint.yml (repo root)
    - AppPackage/Sources/AppModels/Support/AppError.swift (the five-touch pattern on `cloudflareChallengeFailed`: case decl ~line 19, isRetryable ~34, localizedDescription ~56, alertText ~92, solution ~117; LocalizedError conformance needs no change)
    - AppPackage/Sources/AppModels/Resources/Localizable.xcstrings (entry shape of `app_error.cloudflare_challenge_failed*`: snake-case dotted keys, extractionState manual, 6 locales with state translated)
    - CLAUDE.md (repo root — localization conventions: labeled numeric arguments; non-translated keys filled for every locale)
  </read_first>
  <action>
    Copy the exact five-touch `cloudflareChallengeFailed` pattern for a new `unsupportedDeepLink` case (per D-03): case declaration, listed non-retryable in `isRetryable`, `localizedDescription` returning `String(localized: .appErrorUnsupportedDeepLink)`, `alertText` returning the `...Description` symbol, `solution` returning the `...Solution` symbol. The case carries no associated value — per-incident diagnostics ride ErrorInfo.context (Phase 12 12-01 precedent).

    Add the three keys to Localizable.xcstrings across all six locales (en, de, ja, ko, zh-Hans, zh-Hant), each `"state": "translated"`. Content requirements: the short form names the failure ("Unsupported link" register); the description states the link was not recognized as a gallery link; the solution names concretely what EhPanda can open — e-hentai.org / exhentai.org gallery links (`/g/...`), gallery page links (`/s/...`), and gallery comment links — mirroring Phase 12's "fail toward the alternatives" register (D-03, owner's "recovery suggestion names what works"). No numeric format arguments are expected; if one becomes necessary, use a named `%#@variable@` substitution per CLAUDE.md. Translate all six locales properly (the repo's existing app_error entries show the register per language).
  </action>
  <verify>
    <automated>plutil -lint AppPackage/Sources/AppModels/Resources/Localizable.xcstrings && python3 -c "import json;d=json.load(open('AppPackage/Sources/AppModels/Resources/Localizable.xcstrings'));ks=[k for k in d['strings'] if k.startswith('app_error.unsupported_deep_link')];assert len(ks)==3,ks;[print(k) for k in ks];assert all(set(d['strings'][k]['localizations'])=={'en','de','ja','ko','zh-Hans','zh-Hant'} for k in ks)"</automated>
  </verify>
  <acceptance_criteria>
    - AppError.swift contains `case unsupportedDeepLink` and all four switch arms reference it (isRetryable, localizedDescription, alertText, solution)
    - Localizable.xcstrings contains exactly 3 keys prefixed `app_error.unsupported_deep_link`, each with all 6 locales present
    - The en solution string names at least the two gallery hosts and the /g/ link kind
    - Build succeeds (string-symbol generation resolves all three symbols)
  </acceptance_criteria>
  <done>The error case is fully wired through the Phase 9/12 error machinery with complete localization.</done>
</task>

<task type="auto">
  <name>Task 2: Sanitized unsupported-link Context builder + regressions</name>
  <files>AppPackage/Sources/AppModels/Support/AppError+Context.swift, AppPackage/Tests/AppModelsTests/UnsupportedDeepLinkErrorTests.swift</files>
  <read_first>
    - AppPackage/Sources/AppModels/Support/AppError+Context.swift (the ContextKey whitelist ~lines 61-92 — it deliberately has no raw-URL slot; `galleryFailure(url:action:reason:)` retains only a validated decimal gid)
    - AppPackage/Tests/AppModelsTests/ (existing suite conventions — pick the matching file style)
    - .planning/phases/09-correctness-structured-error-handling/09-CONTEXT.md (error-surface conventions, if present)
  </read_first>
  <action>
    Add a static `Context` builder for the unsupported-link failure (e.g. `Context.unsupportedLink(url:)`) following the `galleryFailure` shape. Design point (flagged by research Security table, resolving D-01 "ErrorInfoView with the URL and reason" against the 09-12 no-raw-URL invariant): the stored link row is a sanitized rendering built from URLComponents — scheme + host + first path component only, with any further path components elided by a single ellipsis (so `https://evil.com/g/123/realtoken?x=1#c9` renders as `https://evil.com/g/…`). Query, fragment, userinfo, and token-bearing path slots must never be stored. Add whatever new `ContextKey` case the row needs, keeping the whitelist discipline (a dedicated key, not a free-form URL slot). Include an action row ("Open link" register matching existing context rows) and a reason row stating the link is not a recognized gallery link.

    Write `UnsupportedDeepLinkErrorTests.swift` (Swift Testing, AppModelsTests conventions): parameterized cases proving (1) the sanitized rendering for URLs carrying query/fragment/userinfo/deep paths never contains those parts (assert absence of the query value, fragment value, and third-path-component token fixtures), (2) `AppError.unsupportedDeepLink` is non-retryable and its errorDescription/recoverySuggestion resolve to non-empty localized strings.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppModelsTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - AppError+Context.swift contains a builder accepting a URL for the unsupported-link case (named `unsupportedLink` or equivalent) that constructs the sanitized form via URLComponents accessors, not string scanning
    - Tests assert a fixture secret query value and a fixture third-path-component token do NOT appear in any stored context row value
    - AppModelsTests suite green
  </acceptance_criteria>
  <done>The D-01 surface has a context payload that shows an identifiable link without leaking access-bearing components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Failure diagnostics → ErrorInfoView/Context | Untrusted URL content crosses into persisted, user-visible diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Information Disclosure | Context.unsupportedLink sanitized rendering | high | mitigate | Scheme+host+first-path-component-only rendering; unit regressions assert query/fragment/token absence; no raw-URL ContextKey slot introduced |
</threat_model>

<verification>
- Full default-plan unit run stays green.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- `AppError.unsupportedDeepLink` fully wired with six-locale localization and a D-03-compliant recovery suggestion.
- Sanitization regressions prove the 09-12 privacy invariant holds for the new surface.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-02-SUMMARY.md` when done. Record the context builder's exact name and ContextKey addition — plan 13-04 consumes them.
</output>
