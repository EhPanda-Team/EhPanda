---
phase: 13-deep-link-hardening
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift
  - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift
  - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "A deep link arriving while the modal detail sheet is up re-presents the new detail only after the sheet's dismissal completion is reported — no fixed-duration wait remains in that path (D-14)"
    - "A user-initiated sheet dismissal with no pending deep link triggers no re-present and does not fight the existing dismiss handler (Pitfall 6)"
    - "The fetch still starts immediately on link arrival (it overlapped the dismissal today); only the presentation gates on completion"
  artifacts:
    - path: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      provides: "Deterministic dismissal-completion coordination state machine"
      contains: "detailDismissalCompleted"
    - path: "AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift"
      provides: "onDismiss completion report from the shared detail sheet"
      contains: "onDismiss"
  key_links:
    - from: "AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift"
      to: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      via: "sheet onDismiss sends .presentation(.detailDismissalCompleted); the reducer owns every decision (Phase 11 lifecycle rule — the view only reports a fact)"
      pattern: "detailDismissalCompleted"
---

<objective>
Root-fix the 1000ms modal-dismissal sleep in `handleDeepLink` (D-14, fragile spot F6's timing half) with deterministic dismissal-completion coordination: the sheet's `onDismiss:` reports the only observable completion fact; reducer state gates the re-present.

Purpose: SC-1 — a slow dismissal can no longer race the re-present; no timing-based sequencing remains in the modal-replacement path.
Output: reducer state machine + TabBarView onDismiss wiring + TestStore coordination regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

- `PresentationFeature.Action.detailDismissalCompleted` (new action)
- `PresentationFeature.State.isAwaitingDetailDismissal: Bool` (new stored state)
- `PresentationFeature.State.pendingGalleryLink: PendingGalleryLink?` (new stored state; `PendingGalleryLink` is a small named struct holding the fetched `url: URL` and `gallery: Gallery` — a named struct, not a tuple: labeled_tuple_elements is at error)
- `onDismiss:` closure on the TabBarView detail sheet

<tasks>

<task type="auto">
  <name>Task 1: Dismissal-completion state machine replacing the fixed delay</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift, AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift</files>
  <read_first>
    - .swiftlint.yml (repo root — note lifecycle_modifiers bans onAppear/onDisappear/task only; sheet onDismiss is clean)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (handleDeepLink delay block; `fetchGalleryDone` success arm sending handleGalleryLink; the `.detail(.dismiss)` handler at ~80-82 that runs path.removeAll — the completion action must not fight it)
    - AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift (the shared detail sheet at ~82-94 — also serves presentGalleryDetail on iPad; onOpenURL at ~104 stays as-is)
  </read_first>
  <action>
    Reducer changes (all decisions stay in the reducer; the view only reports):
    - `handleDeepLink` (recognized-link arm): when `state.detail != nil`, set `isAwaitingDetailDismissal = true` alongside the existing `state.detail = nil` + `state.path.removeAll()`, and send `fetchGallery` immediately — delete the delay variable and the timing-based sleep effect entirely (fetch overlap with dismissal is today's behavior; keep it).
    - `fetchGalleryDone` success arm: when `isAwaitingDetailDismissal` is true, stash `PendingGalleryLink(url:gallery:)` into `pendingGalleryLink` instead of sending `handleGalleryLink`; otherwise behave as today. Failure arm unchanged (13-06 owns its timing).
    - New `detailDismissalCompleted` handler: clear `isAwaitingDetailDismissal`; if `pendingGalleryLink` is non-nil, clear it and send `handleGalleryLink(url:gallery:)` with the stashed pair; else `.none`. This makes ordinary user dismissals (nil pending, flag false) a strict no-op, and dismissal-completing-before-fetch resolves via the flag: fetchGalleryDone then presents directly.
    - A second deep link arriving while awaiting dismissal simply overwrites the pending stash (latest wins — deterministic).
    - Do not modify the `.detail(.dismiss)` handler; the completion action must coexist with it (Pitfall 6).
    View change: add `onDismiss: { store.send(.presentation(.detailDismissalCompleted)) }` to the shared detail sheet in TabBarView. The modifier stays on the same sheet (it also serves the iPad presentGalleryDetail entry — user dismissals there hit the strict no-op path).
  </action>
  <verify>
    <automated>! grep -q "milliseconds(1000)" AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift && grep -q "detailDismissalCompleted" AppPackage/Sources/AppFeature/View/TabBar/TabBarView.swift && xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - PresentationFeature.swift no longer contains a 1000-millisecond delay effect in handleDeepLink; the handler mutates isAwaitingDetailDismissal and starts the fetch in the same reduction
    - TabBarView's detail sheet carries onDismiss sending detailDismissalCompleted
    - PendingGalleryLink is a named struct (no anonymous tuple state)
    - Build succeeds
  </acceptance_criteria>
  <done>Modal replacement is coordinated by the dismissal-completion fact, not a stopwatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestStore coordination regressions</name>
  <files>AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift (TestStore conventions; exhaustive-silence pattern from Phase 12)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (the Task 1 state machine)
  </read_first>
  <behavior>
    - Test 1 (replace-while-open, dismissal completes AFTER fetch): with detail presented, send a recognized deep link → assert isAwaitingDetailDismissal true, detail nil, fetch effect received; on fetchGalleryDone success → pendingGalleryLink stashed, no presentation; on detailDismissalCompleted → handleGalleryLink received, detail re-presented with the fetched gallery, pending state cleared
    - Test 2 (dismissal completes BEFORE fetch): detailDismissalCompleted arrives first → flag cleared, no action emitted; fetchGalleryDone success then presents directly (no stash left behind)
    - Test 3 (user dismissal, no pending link): detailDismissalCompleted with clean state → exhaustive store receives nothing and no state change beyond the flag reset
    - Test 4 (no modal up): recognized deep link with detail nil → no awaiting flag, fetch flows straight through to presentation as today
  </behavior>
  <action>
    Implement the four cases in PresentationFeatureTests using the repo's TestStore conventions (@MainActor on members, dependency overrides for haptics/date as existing tests do). Use the same fixture gallery shapes existing tests construct. Silence in Test 3 is proven by exhaustivity, not negative assertions (Phase 12 12-05 precedent).
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - All four coordination cases exist and pass
    - No test uses a real-time sleep to sequence assertions
    - AppFeatureTests green
  </acceptance_criteria>
  <done>Every ordering of dismissal-completion vs fetch-completion is pinned by a deterministic test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View layer → reducer | The dismissal-completion fact crosses from SwiftUI into reducer state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Denial of Service (UI race) | sheet re-present coordination | medium | mitigate | Completion-gated presentation with explicit pending state; both orderings + user-dismissal no-op pinned by tests |
</threat_model>

<verification>
- Full default-plan unit run green.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- The 1000ms wait is gone from the routing path; replacement is completion-driven and race-free under tests.
- iPad presentGalleryDetail dismissals ride the same seam as strict no-ops.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-05-SUMMARY.md` when done.
</output>
