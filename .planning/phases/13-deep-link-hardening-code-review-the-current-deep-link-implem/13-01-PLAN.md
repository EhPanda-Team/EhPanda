---
phase: 13-deep-link-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppPackage/Sources/AppTools/GalleryURLParser.swift
  - AppPackage/Package.swift
  - AppPackage/Tests/AppToolsTests/.swiftlint.yml
  - AppPackage/Tests/AppToolsTests/GalleryURLParserTests.swift
  - AppPackage/Tests/FeatureTests.xctestplan
autonomous: true
requirements: [SC-1, SC-3]
must_haves:
  truths:
    - "GalleryURLParser.parse rejects a URL whose host merely embeds e-hentai.org in a query/path (exact host match, closing the spoofable substring check)"
    - "www.e-hentai.org and www.exhentai.org links parse as gallery links; s.exhentai.org links do not"
    - "Parse failure returns nil — no empty-string sentinel survives anywhere in the parser API"
    - "An ehpanda:// scheme URL parses identically to its https:// equivalent without an optional round-trip at call sites"
  artifacts:
    - path: "AppPackage/Sources/AppTools/GalleryURLParser.swift"
      provides: "Pure URL-to-route parser (D-11/D-12/D-13)"
      contains: "enum GalleryURLParser"
    - path: "AppPackage/Tests/AppToolsTests/GalleryURLParserTests.swift"
      provides: "Exhaustive parser unit suite (Swift Testing)"
    - path: "AppPackage/Tests/AppToolsTests/.swiftlint.yml"
      provides: "Lint coverage for the new test module"
      contains: "parent_config"
  key_links:
    - from: "AppPackage/Sources/AppTools/GalleryURLParser.swift"
      to: "AppPackage/Sources/AppTools/Defaults.swift"
      via: "recognized-host set derived from Defaults.URL.ehentai/exhentai (D-12 parity anchor)"
      pattern: "Defaults\\.URL"
    - from: "AppPackage/Tests/FeatureTests.xctestplan"
      to: "AppPackage/Package.swift"
      via: "AppToolsTests registered in the default unit plan (Phase 11 lesson: unadded targets are silently skipped)"
      pattern: "AppToolsTests"
---

<objective>
Build the pure `GalleryURLParser` that replaces `URLClient`'s defective recognition/analysis (D-11, D-12, D-13), with an exhaustive unit suite in a new `AppToolsTests` target. This plan creates the parser and proves it; call-site migration and `URLClient` deletion follow in plan 13-03.

Purpose: root-fix fragile spots F1 (spoofable substring host check), F2 (empty-string-on-failure gid), F3 (string-range `#c` extraction), F4 (confusing `URL?` scheme-resolver contract) — the SC-1 core, and the input-validation half of SC-3.
Output: `GalleryURLParser` in AppTools + green `AppToolsTests` suite registered in the default test plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

- `GalleryURLParser` (public enum, AppTools) with:
  - `GalleryURLParser.Route` (public struct: `gid: String`, `pageIndex: Int?`, `commentID: String?`, `isGalleryImageURL: Bool`, plus the normalized `url: URL`)
  - `GalleryURLParser.parse(_:) -> Route?`
  - `GalleryURLParser.isMPVURL(_:) -> Bool`
- New SPM test target `AppToolsTests` (Package.swift `Module` case `appToolsTests`)
- New file `AppPackage/Tests/AppToolsTests/GalleryURLParserTests.swift`
- New file `AppPackage/Tests/AppToolsTests/.swiftlint.yml`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GalleryURLParser in AppTools and register the AppToolsTests target</name>
  <files>AppPackage/Sources/AppTools/GalleryURLParser.swift, AppPackage/Package.swift, AppPackage/Tests/AppToolsTests/.swiftlint.yml</files>
  <read_first>
    - .swiftlint.yml (repo root — write lint-clean from the start; all Phase 11 rules at error)
    - AppPackage/Sources/URLClient/URLClient.swift (the code being replaced — route facts to preserve, defects NOT to copy)
    - AppPackage/Sources/AppTools/Defaults.swift (lines 60-70 — `Defaults.URL.ehentai`/`exhentai`/`sexhentai` host anchors)
    - AppPackage/Sources/AppTools/Extensions/URL+Components.swift (`replaceScheme(to:)` — the URLComponents-backed scheme swap to reuse)
    - AppPackage/Sources/AppTools/FileUtil.swift (the pure-namespace shape to copy — Phase 8 D-06 precedent)
    - AppPackage/Package.swift (Module enum + target/testTarget helper conventions)
  </read_first>
  <behavior>
    Route-fact truth table the parser MUST preserve (verified against URLClient.swift):
    - `/g/<gid>/<token>` → Route(gid: path slot 2, pageIndex: nil, commentID from fragment, isGalleryImageURL: false)
    - `/s/<imgkey>/<gid>-<page>` → Route(gid: token-slot prefix before "-", pageIndex: Int after "-", isGalleryImageURL: true when no commentID)
    - Fragment `c<ID>` → commentID = ID (fragment-based via URLComponents/url.fragment — replaces the F3 string-range scan); a commentID forces isGalleryImageURL to false (today's analyzeURL rule, load-bearing for GalleryDeepLink precedence)
    - `ehpanda://` scheme is normalized to `https://` internally before host matching (F4 — callers never see an optional round-trip)
    - Recognized hosts (D-12): exactly `e-hentai.org`, `exhentai.org`, `www.e-hentai.org`, `www.exhentai.org` — derived from `Defaults.URL.ehentai`/`exhentai` host() values plus computed `www.` variants; `s.exhentai.org` (Defaults.URL.sexhentai) MUST NOT match; comparison case-insensitive, exact match (F1 fix)
    - Rejections → nil (F2): unrecognized host, host embedded only in query/path (`https://evil.com/g/123/tok?ref=https://e-hentai.org/`), route kind outside {g, s}, missing token slot, empty gid, non-decimal gid (gid must match [0-9]+ — the Phase 9 09-12 validated-decimal-gid convention), `/s/` token without a parsable page suffix keeps gid but pageIndex nil only if that matches today's Int-parse behavior (Int(token[after "-"]) fails → pageIndex nil, isGalleryImageURL still true — preserve exactly)
  </behavior>
  <action>
    Create `AppPackage/Sources/AppTools/GalleryURLParser.swift`: a public uninhabited `enum GalleryURLParser` namespace (Phase 8 D-06 precedent — pure helpers are namespaces, no DependencyKey, no noop/unimplemented ceremony; not a reducer, so no Feature suffix). Public surface: nested `Route` struct (Equatable, Sendable; stored: `url` — the normalized https URL, `gid`, `pageIndex`, `commentID`, `isGalleryImageURL`), `static func parse(_ url: URL) -> Route?`, and `static func isMPVURL(_ url: URL?) -> Bool` migrated verbatim from URLClient (pathComponents.dropFirst().first == "mpv"). Implement per the behavior table above using URLComponents / `url.host()` / `pathComponents` / fragment accessors only — never absoluteString scanning. Reuse `replaceScheme(to:)` from URL+Components.swift for the ehpanda→https normalization. Derive the recognized-host set from `Defaults.URL.ehentai` and `Defaults.URL.exhentai` (single-source the D-12 parity anchor) rather than fresh string literals.

    In `AppPackage/Package.swift`: add `case appToolsTests = "AppToolsTests"` to the Module enum and a `testTarget(module: .appToolsTests, dependencies: [.module(.appTools)])` entry following the existing testTarget helper convention. Create `AppPackage/Tests/AppToolsTests/.swiftlint.yml` containing exactly `parent_config: ../../../.swiftlint.yml` (AGENTS.md new-module rule; same depth as the Sources modules).
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - AppPackage/Sources/AppTools/GalleryURLParser.swift contains `enum GalleryURLParser` and `func parse(` and `func isMPVURL(`
    - GalleryURLParser.swift contains `Defaults.URL` (host set derived from the parity anchor) and does NOT contain `absoluteString.contains` or `range(of:` string scanning
    - AppPackage/Package.swift contains `appToolsTests = "AppToolsTests"`
    - AppPackage/Tests/AppToolsTests/.swiftlint.yml contains `parent_config: ../../../.swiftlint.yml`
    - Build succeeds; SwiftLint reports zero violations for the new file (plugin runs in build)
  </acceptance_criteria>
  <done>The parser compiles as a pure AppTools namespace with the exact D-12 host set, and the AppToolsTests target exists in Package.swift.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaustive parser test suite, registered in the default test plan</name>
  <files>AppPackage/Tests/AppToolsTests/GalleryURLParserTests.swift, AppPackage/Tests/FeatureTests.xctestplan</files>
  <read_first>
    - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift (repo Swift Testing conventions: `@Test(arguments:)` parameterized fixtures, `try #require(URL(string:))` — never force-unwrap, sorted imports; pure-function tests need no @MainActor)
    - AppPackage/Tests/FeatureTests.xctestplan (entry shape: containerPath "container:AppPackage", identifier/name pairs)
    - AppPackage/Sources/AppTools/GalleryURLParser.swift (what Task 1 built)
  </read_first>
  <action>
    Write `GalleryURLParserTests.swift` (Swift Testing, no @MainActor — pure functions) covering every row of Task 1's behavior table with `@Test(arguments:)` fixtures. Must-include cases, each asserting the full Route (gid, pageIndex, commentID, isGalleryImageURL) or nil:
    - Accept: `https://e-hentai.org/g/3103480/0000000000/`, same on exhentai.org, same on www.e-hentai.org and www.exhentai.org (D-12), uppercase-host variant, `ehpanda://e-hentai.org/g/3103480/0000000000/` (scheme normalization), `/s/abc123/3103480-2` (gid extracted from token prefix, pageIndex 2, isGalleryImageURL true), `/g/3103480/0000000000/#c5143504` (commentID "5143504"), `/s/abc123/3103480-2#c99` (pageIndex AND commentID set, isGalleryImageURL false — the analyzeURL cancellation rule)
    - Reject (nil): `https://evil.com/g/123/token?ref=https://e-hentai.org/` (F1 spoof), `https://s.exhentai.org/g/1/t` (image host), `https://e-hentai.org/mpv/3103480/0000000000/` (kind outside g/s), `/g/3103480` (missing token), `/g//token` (empty gid), `/g/abc/token` (non-decimal gid), `https://e-hentai.org/` (no route), a relative/scheme-less URL
    - isMPVURL: true for `/mpv/...` paths, false for `/g/`, false for nil
    Add an `AppToolsTests` entry to `AppPackage/Tests/FeatureTests.xctestplan` (containerPath "container:AppPackage", identifier and name "AppToolsTests") so the default unit run executes it — Phase 11 found the scheme silently skips unregistered targets.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppToolsTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - GalleryURLParserTests.swift contains `@Test(arguments:` and `#require(URL(string:` and covers the spoof-rejection URL from the F1 defect
    - FeatureTests.xctestplan contains an "AppToolsTests" target entry
    - `xcodebuild test ... -only-testing:AppToolsTests` exits TEST SUCCEEDED
    - No force-unwraps or optional-try in the test file (repo lint at error covers tests)
  </acceptance_criteria>
  <done>The parser's full accept/reject matrix is proven green in a target the default plan runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URL → parser | Arbitrary URLs from any app (scheme), web pages (share), or clipboard cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing | GalleryURLParser.parse host check | high | mitigate | Exact case-insensitive host match against the closed D-12 set; spoof fixture (`evil.com` with embedded host in query) locked in the unit suite |
| T-13-02 | Denial of Service | GalleryURLParser.parse on arbitrary input | medium | mitigate | Optional-returning parse, zero force-unwraps/subscript trust (repo lint at error); malformed-shape fixtures in the suite |
</threat_model>

<verification>
- Full default-plan unit run stays green: `xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air'`.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- `GalleryURLParser` exists in AppTools with the exact D-12 host set and the preserved route-fact truth table.
- AppToolsTests target runs in the default test plan and is green.
- The spoof URL class that passes today's substring check is proven rejected.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-01-SUMMARY.md` when done. Record the final Route field list — plan 13-03's call-site migration consumes it.
</output>
