---
phase: 13-deep-link-hardening
plan: 04
type: execute
wave: 4
depends_on: ["13-02", "13-03"]
files_modified:
  - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift
  - ShareExtension/ShareViewController.swift
  - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift
autonomous: true
requirements: [SC-3, SC-1]
must_haves:
  truths:
    - "An explicit open (ehpanda:// scheme or ShareExtension hand-off) of an unrecognized link surfaces the persistent tappable error toast carrying AppError.unsupportedDeepLink — the silent no-op is gone (D-01)"
    - "A clipboard URL that is not a gallery link stays a silent no-op — no toast, no action (D-02)"
    - "A well-formed gallery link whose fetch fails keeps the existing GalleryReverseRequest error mapping into the toast → ErrorInfoView (D-04, unchanged)"
    - "The ShareExtension scheme rewrite mutates only the scheme component — a URL whose query embeds the original scheme string survives intact (F8)"
  artifacts:
    - path: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      provides: "Entry-source policy: explicit failure toast, clipboard pre-validation"
      contains: "unsupportedDeepLink"
    - path: "ShareExtension/ShareViewController.swift"
      provides: "URLComponents-backed scheme swap"
      contains: "URLComponents"
  key_links:
    - from: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      to: "AppPackage/Sources/AppModels/Support/AppError.swift"
      via: "explicit-open failure sets toast .error with ErrorInfo(unsupportedDeepLink, unsupported-link context from 13-02)"
      pattern: "unsupportedDeepLink"
---

<objective>
Implement the entry-source failure policy (D-01/D-02, fragile spots F6-silent-no-op and F9) and root-fix the ShareExtension's whole-string scheme rewrite (F8).

Purpose: SC-3 — malformed or unresolvable explicit opens fail loudly and recoverably; clipboard noise stays silent; the D-04 fetch-failure mapping is untouched.
Output: policy split in PresentationFeature + hardened ShareViewController + TestStore regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-02-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- Entry-source policy in `PresentationFeature`: clipboard pre-validation inside `detectClipboardURL`; `handleDeepLink` becomes the explicit-open handler whose parse failure raises the D-01 toast
- ShareViewController URLComponents scheme swap (no new symbols; local `URLComponents` mutation)
- New TestStore cases in `PresentationFeatureTests.swift`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entry-source policy — clipboard pre-validation + explicit-open failure toast</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift, AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift</files>
  <read_first>
    - .swiftlint.yml (repo root)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (post-13-03 state: `detectClipboardURL` ~144-154, `handleDeepLink`, `fetchGalleryDone` failure arm — the error-toast shape to copy)
    - AppPackage/Sources/AppModels/Support/AppError+Context.swift (the 13-02 unsupported-link context builder — exact name in 13-02-SUMMARY.md)
    - AppPackage/Tests/AppFeatureTests/PresentationFeatureTests.swift (TestStore conventions: @MainActor on members, @Test(arguments:), #require)
  </read_first>
  <behavior>
    - Test 1 (D-01): sending `handleDeepLink` with an unrecognized URL (e.g. an evil-host spoof) → state.toast becomes `.error` carrying ErrorInfo whose error is `.unsupportedDeepLink` and whose context is the 13-02 sanitized unsupported-link context; no fetch effect runs
    - Test 2 (D-02): `detectClipboardURL` with a stubbed clipboard returning a non-gallery URL and a fresh change count → the change count is persisted, and an exhaustive TestStore receives NO routing action and no toast (silence proven by exhaustivity, the Phase 12 12-05 pattern)
    - Test 3 (D-02 positive): clipboard returning a recognized gallery URL → `handleDeepLink` is sent (arrival leg intact)
    - Test 4 (D-04 unchanged): recognized gallery URL whose fetch fails → existing galleryFailure context flows to the error toast exactly as before (existing test may already cover; extend, don't duplicate)
  </behavior>
  <action>
    Split the entry sources per research Pattern 2 (pre-validation keeps D-02 literally free):
    - `detectClipboardURL`: after reading `clipboardClient.url()`, forward to `handleDeepLink` only when `GalleryURLParser.parse(url) != nil`. Unparseable clipboard URLs do nothing beyond the existing change-count persistence — no new action, no toast (D-02).
    - `handleDeepLink` (now reached only by explicit opens — TabBarView `onOpenURL` for scheme + share hand-off, AppReducer launch automation — and by pre-validated clipboard URLs): on `GalleryURLParser.parse(url)` nil, set `state.toast = .error(ErrorInfo(error: .unsupportedDeepLink, context: <13-02 builder>(url)))` and return `.none` (D-01; direct set — no loading toast precedes this failure). The success path is unchanged from 13-03.
    - Do NOT touch the 1000ms delay logic or the fetch-failure 500ms effect in this plan (13-05/13-06 own D-14); do not alter `handleGalleryLink`, `presentGalleryDetail`, or the Downloads cell path (locked routing contract: non-deep-link navigation untouched).
    Note the AppReducer automation gate (13-03) already filters unparseable URLs to the initialTab fallback, preserving automation semantics; explicit opens through onOpenURL are the D-01 surface.
    Write the four behavior tests. For Test 2, override `clipboardClient` and `userDefaultsClient` per existing conventions; exhaustive store, no received actions after the change-count write effect completes.
  </action>
  <verify>
    <automated>xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' -only-testing:AppFeatureTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - PresentationFeature.swift: the unrecognized-explicit-open arm sets an error toast with `unsupportedDeepLink` (the bare `return .none` guard for that case is gone)
    - PresentationFeature.swift: the clipboard branch contains a `GalleryURLParser.parse` gate before sending `handleDeepLink`
    - All four behavior tests exist and pass; AppFeatureTests green
  </acceptance_criteria>
  <done>Explicit opens fail loudly through the Phase 9 surface; clipboard stays silent; fetch-failure mapping untouched.</done>
</task>

<task type="auto">
  <name>Task 2: ShareExtension scheme rewrite via URLComponents (F8)</name>
  <files>ShareExtension/ShareViewController.swift</files>
  <read_first>
    - .swiftlint.yml (repo root — the ShareExtension target is scanned)
    - ShareExtension/ShareViewController.swift (defect at ~lines 21-23: whole-string replacingOccurrences of the scheme corrupts URLs embedding the scheme string elsewhere)
    - AppPackage/Sources/AppTools/Extensions/URL+Components.swift (`replaceScheme(to:)` — the reference implementation; ShareExtension does not link AppPackage, so reproduce the URLComponents mutation locally rather than adding a package dependency for five lines)
  </read_first>
  <action>
    Replace the `absoluteString.replacingOccurrences` construction with a local URLComponents mutation: build `URLComponents(url:resolvingAgainstBaseURL:)`, set `scheme = "ehpanda"`, use its `url`. Preserve the existing guard flow (bail to `completeRequest` when the item is not a URL or components fail) and the `openMainApp` hand-off untouched. Do not reintroduce any string surgery on the URL.
  </action>
  <verify>
    <automated>! grep -q "replacingOccurrences" ShareExtension/ShareViewController.swift && grep -q "URLComponents" ShareExtension/ShareViewController.swift && xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - ShareViewController.swift mutates only the scheme component via URLComponents; no string-replacement call remains in the file
    - Build (app + embedded extension) succeeds with zero SwiftLint violations
  </acceptance_criteria>
  <done>A shared URL whose query contains its own scheme string reaches the app uncorrupted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Other apps → ehpanda:// scheme | Arbitrary payloads reach handleDeepLink |
| Safari share sheet → ShareExtension | Arbitrary web URLs are rewritten and handed off |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Denial of Service | handleDeepLink on arbitrary explicit input | medium | mitigate | Optional parse + typed error surface; no crash path; TestStore spoof/malformed fixtures |
| T-13-05 | Tampering | ShareViewController scheme rewrite | medium | mitigate | Component-scoped mutation replaces whole-string replacement; guard flow preserved |
| T-13-03 | Information Disclosure | D-01 toast context | high | mitigate | Only the 13-02 sanitized context builder feeds ErrorInfo — no raw URL enters Context |
</threat_model>

<verification>
- Full default-plan unit run green.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- D-01/D-02 policy proven by exhaustive TestStore tests; D-04 mapping unchanged.
- F8 closed with a component-scoped rewrite.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-04-SUMMARY.md` when done.
</output>
