---
phase: 13-deep-link-hardening
plan: 03
type: execute
wave: 3
depends_on: ["13-01"]
files_modified:
  - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift
  - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift
  - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift
  - AppPackage/Sources/ReadingFeature/ReadingReducer.swift
  - AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift
  - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift
  - AppPackage/Package.swift
  - AppPackage/Tests/AppFeatureTests/PresentationLifecycleTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadObserverReadingTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/ReadingReducerLocalTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/ReadingReducerDownloadTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadObserverRefreshTests.swift
  - AppPackage/Tests/DownloadsFeatureTests/DownloadAutomationTests.swift
  - AppPackage/Tests/ParserFeatureTests/Other/SettingDownloadTests.swift
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "Every former URLClient call site resolves through GalleryURLParser directly — no injected dependency, no noop/unimplemented ceremony (D-13)"
    - "The URLClient module no longer exists: no source directory, no Package.swift target, no dependency references, no imports anywhere"
    - "Destination-routing behavior for currently-supported links is byte-for-byte unchanged in this plan (policy changes land in 13-04; this plan is a pure seam swap)"
  artifacts:
    - path: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      provides: "handleDeepLink/handleGalleryLink resolving via GalleryURLParser"
      contains: "GalleryURLParser"
    - path: "AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift"
      provides: "MPV check via GalleryURLParser.isMPVURL"
      contains: "GalleryURLParser.isMPVURL"
  key_links:
    - from: "AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift"
      to: "AppPackage/Sources/AppTools/GalleryURLParser.swift"
      via: "direct static parse calls replacing the four injected closures"
      pattern: "GalleryURLParser\\.parse"
---

<objective>
Migrate all URLClient consumers to `GalleryURLParser` and delete the URLClient module (D-13, fragile spot F5) — at strict behavior parity: the silent no-op for unrecognized explicit opens and both magic sleeps remain in place until plans 13-04/13-05/13-06 change policy deliberately.

Purpose: kill the injected-dependency ceremony around a pure function; make every routing decision run real parsing in tests.
Output: URLClient module gone; all six consumer files + seven test files on the parser; suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-CONTEXT.md
@.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- No new symbols. Removals: `URLClient` type, `URLAnalysisResult`, `URLClientKey`, `DependencyValues.urlClient`, the `Sources/URLClient/` directory, the Package.swift `urlClient` Module case and target, and every `.module(.urlClient)` dependency entry.

<!-- planner-discipline-allow: urlClient -->
<!-- planner-discipline-allow: URLClient -->

<tasks>

<task type="auto">
  <name>Task 1: Migrate the six Sources call sites to GalleryURLParser at behavior parity</name>
  <files>AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift, AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift, AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift, AppPackage/Sources/ReadingFeature/ReadingReducer.swift, AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift, AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift</files>
  <read_first>
    - .swiftlint.yml (repo root)
    - AppPackage/Sources/AppTools/GalleryURLParser.swift (the Route surface from 13-01)
    - AppPackage/Sources/AppFeature/DataFlow/PresentationFeature.swift (sites: dependency decl ~line 63; handleDeepLink ~156-170; handleGalleryLink ~172-187)
    - AppPackage/Sources/AppFeature/DataFlow/AppReducer.swift (runLaunchAutomation gate ~line 161 with initialTab fallback)
    - AppPackage/Sources/DetailFeature/Comments/CommentsReducer.swift (dependency ~89; handleCommentLink ~158-164 with the browser fallback that MUST stay; handleGalleryLink ~166-167)
    - AppPackage/Sources/ReadingFeature/ReadingReducer+ImageFetch.swift (~167 checkIfMPVURL) and ReadingReducer.swift (dependency + import)
    - AppPackage/Sources/DownloadClient/DownloadClient+ExecutionSupport.swift (~193 static URLClient.isMPVURL)
  </read_first>
  <action>
    Pure seam swap, one site at a time, preserving today's observable behavior exactly:
    - PresentationFeature `handleDeepLink`: replace resolveAppSchemeURL + checkIfHandleable + analyzeURL with a single `GalleryURLParser.parse(url)`. On nil, keep `return .none` (the D-01 policy change belongs to 13-04, not here). On success, use `route.url` (normalized) where the resolved URL was used, `route.isGalleryImageURL` for the fetch flag. Keep the delay/sleep logic untouched (13-05's concern).
    - PresentationFeature `handleGalleryLink`: replace analyzeURL with `GalleryURLParser.parse(url)`; build `GalleryDeepLink(pageIndex: route?.pageIndex, commentID: route?.commentID)` preserving nil-in/nil-out for unparseable URLs exactly as the old analyzer's empty result did.
    - AppReducer `runLaunchAutomation`: replace the recognizer gate with `GalleryURLParser.parse(galleryURL) != nil`, keeping the initialTab fallback arm identical.
    - CommentsReducer `handleCommentLink`: replace the recognizer guard with parse; the non-handleable arm MUST keep opening in the browser via applicationClient (existing fallback). `handleGalleryLink`: same swap as PresentationFeature.
    - ReadingReducer+ImageFetch: `GalleryURLParser.isMPVURL(url)` replaces the injected closure. DownloadClient+ExecutionSupport: swap the old static namespace for `GalleryURLParser.isMPVURL`.
    - In each file: drop the old module import, add `AppTools` if missing, keep imports sorted (sorted_imports at error), and delete the now-unused `@Dependency` property for the old client.
  </action>
  <verify>
    <automated>xcodebuild build -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - All six files contain `GalleryURLParser.` calls and none contains a `urlClient` identifier or the old module import
    - CommentsReducer's non-gallery-link arm still calls `applicationClient.openURL` (browser fallback preserved)
    - AppReducer's automation arm still falls back to `setTabBarItemType(initialTab)` when the URL does not parse
    - handleDeepLink still returns `.none` for unrecognized URLs (policy unchanged in this plan)
    - Build succeeds
  </acceptance_criteria>
  <done>Every consumer resolves parsing statically; observable routing behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the URLClient module and migrate the seven test files</name>
  <files>AppPackage/Package.swift, AppPackage/Tests/AppFeatureTests/PresentationLifecycleTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadObserverReadingTests.swift, AppPackage/Tests/DownloadsFeatureTests/ReadingReducerLocalTests.swift, AppPackage/Tests/DownloadsFeatureTests/ReadingReducerDownloadTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadObserverRefreshTests.swift, AppPackage/Tests/DownloadsFeatureTests/DownloadAutomationTests.swift, AppPackage/Tests/ParserFeatureTests/Other/SettingDownloadTests.swift</files>
  <read_first>
    - AppPackage/Package.swift (Module case ~line 106; target block ~line 831; dependency refs — grep `.module(.urlClient)` for the authoritative list including appFeature, detailFeature, readingFeature, downloadClient, parserFeatureTests, downloadsFeatureTests)
    - Each listed test file's dependency-override blocks (they set the old client on TestStore dependencies; grep the old identifier per file)
  </read_first>
  <action>
    Delete `AppPackage/Sources/URLClient/` entirely (URLClient.swift + its module .swiftlint.yml). In Package.swift remove the Module enum case, the target definition, and every `.module(.urlClient)` dependency reference (Sources and Tests).

    In the seven test files: remove the old-module imports and every TestStore dependency override that set the old client. Where an override previously used the always-false recognizer to keep a URL un-routable, verify the fixture URL genuinely does not parse under `GalleryURLParser` (non-gallery hosts/paths stay inert with no override needed); where an override supplied real parsing behavior, the direct static call now provides it. Adjust assertions only where an override's artificial behavior (not production behavior) was load-bearing — document any such adjustment in the summary. Real parsing now runs in every reducer test, which is D-13's stated intent.
  </action>
  <verify>
    <automated>test ! -d AppPackage/Sources/URLClient && ! grep -rq "urlClient\|URLClient" AppPackage/Sources AppPackage/Tests AppPackage/Package.swift && xcodebuild test -scheme EhPanda -destination 'platform=iOS Simulator,name=iPhone Air' 2>&1 | tail -5 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `AppPackage/Sources/URLClient/` does not exist
    - Zero occurrences of the old client identifier (either case form) across AppPackage/Sources, AppPackage/Tests, and Package.swift
    - Full default-plan unit suite green (all former override-dependent tests updated)
  </acceptance_criteria>
  <done>The module is gone, the dependency ceremony is gone, and the full suite passes against real parsing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URL → reducers | All four deep-link entries now cross into GalleryURLParser's exact-match validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing | every migrated recognizer call site | high | mitigate | All sites now route through the 13-01 exact-host parser; no site retains a substring check (Task 1 removes the last consumers of the old code) |
| T-13-06 | Tampering | test fidelity | low | accept | Tests exercise real parsing instead of stubs — a behavior-visibility gain; residual risk is only test-fixture drift, caught by the suite itself |
</threat_model>

<verification>
- Full default-plan unit run green; no old-client identifier anywhere in the package.
- Spec-less probe note: the edge/prohibition probe was visibly skipped this run (phase has no requirement IDs); must_haves derive from ROADMAP §13 success criteria + CONTEXT.md locked decisions.
</verification>

<success_criteria>
- D-13 landed: pure parser consumed directly everywhere; module and ceremony deleted.
- Behavior parity held: no policy or timing change in this plan.
</success_criteria>

<output>
Create `.planning/phases/13-deep-link-hardening-code-review-the-current-deep-link-implem/13-03-SUMMARY.md` when done. List any test whose assertions changed because an artificial override was load-bearing.
</output>
