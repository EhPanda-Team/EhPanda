name: Test
on: [push]
env:
   SCHEME_NAME: 'EhPanda'
   DEVELOPER_DIR: /Applications/Xcode_13.0.app
jobs:
  Test:
    runs-on: macos-latest
    steps:
#       - name: Checkout
#         uses: actions/checkout@v2
#       - name: Show Xcode version
#         run: xcodebuild -version
#       - name: Run tests
#         run: xcodebuild
#           -scheme ${{ env.SCHEME_NAME }} -sdk iphonesimulator
#           -destination 'platform=iOS Simulator,name=iPhone 12'
#           clean test
      - name: Post a message to Telegram
        run: curl "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage?parse_mode=markdown&chat_id=${{ secrets.TELEGRAM_CHENNEL_ID_TEST }}&text=testing"
